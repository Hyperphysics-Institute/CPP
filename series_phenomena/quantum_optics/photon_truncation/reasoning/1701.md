# Reasoning — Patch 1701 — TP-1 v0.2: divergence class derived (OPEN-TP-1 first half)

**Patch:** 1701 (band 1700–1799)
**Date:** 20 June 2026
**Window:** TP-1 / quantum-optics phenomena lane
**Deliverable:** TP-1.tex v0.1 → v0.2 + this fragment + `scripts/1701_divergence_class.py`
**Source consulted:** RGS full paper + Supplemental Material (arXiv:2510.21636v2),
fetched in-session.

---

## 0. Goal

v0.1 carried the headline ceiling ⟨N⟩_max ≈ 64κ but left the divergence CLASS (log vs
power) and κ as assumed/open — the load-bearing weakness review would target. Discharge
the first half of OPEN-TP-1: settle the class from the RGS kernel itself.

## 1. What the RGS supplemental actually gives (the decisive facts)

- **Divergence is LOGARITHMIC, and they derive it.** Main text, "Gradual removal":
  instantaneous removal makes ⟨n⟩ = ∞ because "there are modes ξ(ω) such that ξ(x)≠0 at
  x=0; then the Heaviside function leads to a Fourier transform with asymptotic behavior
  ∝ 1/ω for large ω. Together with E(ω) ∝ √ω, the norms diverge **logarithmically**."
  → dN/dω ~ 1/ω is DERIVED. The v0.1 κ/ω model is vindicated, not assumed.
  Chain: truncation = θ(±x)·ξ(x) with ξ(0)≠0 → step discontinuity → FT ~ 1/ω →
  ξ_±(ω)=E(ω)·FT ~ √ω·(1/ω)=1/√ω → |ξ_±|²~1/ω → ⟨n⟩=Σ‖ξ_±‖²~∫dω/ω = log.

- **RGS already use a UV cutoff.** Supplemental Eq. (B14): "we have introduced a
  high-frequency cutoff for the quantum field integral expressions. While the cutoff can
  be arbitrarily high, it has been fixed to make the zero slab thickness meaningful."
  → This is THE hook. CPP's job is to identify that formal "arbitrarily high" cutoff with
  a PHYSICAL scale: ω_P = 1/t_P. The cleanest possible statement of the CPP-specific content.

- **Gradual-removal bound (their Eq. 29/B47):** ⟨n⟩ ≤ κ₀/(4T) + κ₀²/(16T²), with
  |T(ω)|² = 1/(1+(ωκ₀)²/4), valid in 1/ω₀ ≪ κ₀ ≪ T. Optical example: ω₀/2π=1e15 Hz,
  |T|²=1e-4 ⟹ ω₀κ₀=200 ⟹ κ₀=3.18e-14 s; T can be ~1e-14 s before ⟨n⟩~1.
  Script 1701 reproduces this (T=1e-14 ⟹ bound 1.43). ✓

- **Mechanism, in their words:** "mirror removal converts vacuum energy to real photon
  energy in the forward- and backward-propagating modes"; they invoke the dynamical
  Casimir effect [refs 4-12] and the Unruh-effect squeezed-vacuum/partial-trace analogy
  [ref 27]. This is EXACTLY the CPP driven-DP-Sea-boundary + QM-4 partial-trace reading.
  Confirms the compatibility map's mechanism leg from their own text.

- **Local equivalence (their Eqs. 32-33, Fig 2):** truncated state is locally a single
  photon (x<t) and locally vacuum (x>t+T), with a transition region carrying the high
  energy density. Strict localization via Knight's theorem ⟹ "not surprising that (19)
  involves arbitrarily high photon numbers." Maps onto QM-4 partial-trace + Nexus exactly.

## 2. The corrected physical picture — TWO REGIMES (this fixes a v0.1 over-claim)

v0.1 implied the lattice cutoff actively regularizes "every physical shutter." Truer:

- **Regime A — realistic shutter (T ≫ 1/ω₀).** RGS's own mechanical smoothing gives
  ⟨n⟩ ≪ 1; the lattice cutoff is DORMANT because t_P ≪ T (≈27.5 orders below 1/ω₀ for
  optical). CPP and continuum agree. The lattice does nothing here.
- **Regime B — idealized instantaneous shutter (T → 0).** This is where the continuum has
  its genuine (logarithmic) divergence. CPP forbids T < t_P (one Absolute Moment), so
  ω_cut ≤ ω_P and ⟨n⟩ ≤ C·ln(ω_P/ω₀). The lattice removes the idealization's infinity.

So the CPP content is precisely: the continuum's instantaneous-shutter infinity is a
cutoff artifact; CPP supplies the finite answer the idealization should have had. This is
a FOUNDATIONAL/consistency result, NOT an experimentally accessible prediction — say so
plainly (don't ape EU-1's falsifiable n_s; the honest analog here is "finiteness," not a
measured number).

- **Consistency check (script §4):** the gradual-bound regime 1/ω₀ ≪ κ₀ ≪ T cannot even
  be pushed to T=t_P (would need κ₀ ≫ 1.6e-16 s AND κ₀ ≪ 5.4e-44 s — impossible). So the
  t_P scale is only reached in the sharp/log regime, where the log law governs. Clean.

## 3. The numeric ceiling (now derived class, O(1) prefactor open)

⟨N⟩_max = C·ln(ω_P/ω₀). For RGS optical ω₀=2π·1e15: ln(ω_P/ω₀)=63.25 ⟹ **⟨N⟩_max ≈ 63 C**.
C = O(1), set by |ξ(0)|² (photon amplitude at the cut) and the mode-sum measure in
⟨n⟩=Σ_ξ(‖ξ⁻₋‖²+‖ξ⁻₊‖²). Pinning C exactly = evaluating that trace for a given cut
profile; that's the ONLY remaining residual.

## 4. OPEN-TP-1 status change (NARROWED, not closed)

- v0.1 OPEN-TP-1 = "derive the whole spectrum + κ."
- v0.2 narrows it: **spectral class CLOSED (logarithmic, from RGS Eq. 16+22); only the
  O(1) leading-log prefactor C remains.** Proposition 1's "Argument" upgrades to a
  derivation of the class. This is a partial closure, registry-tracked in the DEFERRED
  integration patch.

## 5. Routes ruled out / not taken

- **Pin C exactly here.** Not done — requires the full HS-norm trace ‖T₂‖²_HS for a
  specific cut profile; faking a number is worse than an honest O(1). Stays as OPEN-TP-1.
- **Claim experimental falsifiability of the ceiling.** Rejected — Regime A shows real
  shutters never reach the lattice scale; the result is foundational, not measurable.
  v0.2 must DEMOTE the "empirical (in principle)" falsifier wording from v0.1 accordingly.
- **Keep v0.1's "regularizes every physical shutter."** Corrected to the two-regime
  statement (lattice dormant in Regime A; active only on the idealized limit in Regime B).

## 6. Registry (DEFERRED — batched integration patch, NOT 1701)

Unchanged target set from 1700, plus: narrow OPEN-TP-1 (class closed, prefactor open);
PROP-TP-1-1 now rests on a derived class. NONE of these in patch 1701 — 1701 touches only
the TP-1.tex (own file) + new greenfield reasoning/script.

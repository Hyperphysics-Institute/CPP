# Reasoning — Patch 1706 — TP-1 v0.4: OPEN-TP-1 advance (intrinsic band-top cutoff)

**Patch:** 1706 (band 1700–1799)
**Date:** 20 June 2026
**Deliverables:** TP-1.tex (v0.3→v0.4) + scripts/1706_band_top_cutoff.py + reviews-TP-1.md note
+ this fragment.

## The question OPEN-TP-1 had to answer
ChatGPT (T3) and Copilot both flagged the v0.3 cutoff framing as the weak point: identifying
RGS's formal cutoff with ω_P is framework-specific *only if* CPP independently establishes a
physical maximum mode frequency for this calculation — otherwise it is "continuum log + an
imposed Planck cutoff," i.e. a renamed regulator. So the pivotal question was: does the 600-cell
make the cutoff INTRINSIC and STRUCTURE-SPECIFIC, or generic?

## Result (it is intrinsic and z-specific)
QM-5 quantises the field on the 600-cell adjacency matrix, dispersion ω_k = c√|λ_k|/l_P, six
distinct eigenvalues {12, 1+φ, φ−1, 1−φ, −φ, −(1+φ)}. The largest is λ_max = z = 12 (the
coordination number). Therefore the field has a HARD BAND TOP:

    ω_max = √λ_max · c/l_P = √12 / t_P = 2√3 / t_P = 6.43e43 rad/s

with NO modes above it. Two consequences:
1. The truncation sum is finite because the lattice spectrum is finite — the 1/ω tail simply
   TERMINATES at ω_max. Not an imposed cutoff: an intrinsic finite-spectrum regularization.
   This is exactly the "physical maximum mode frequency" ChatGPT said was required.
2. ω_max = √z/t_P = √12/t_P carries the 600-cell coordination z=12 as a fingerprint —
   distinct from a generic Planck cutoff at 1/t_P. The ceiling becomes
   ⟨N⟩_max = C·ln(√12/(t_P ω_γ)) = C·64.5 (vs 63.3 with the naive 1/t_P).
   The shift is exactly ln√12 = ½ln12 = 1.24 inside the log.

This converts the v0.3 "we instantiate a cutoff" into v0.4 "the lattice HAS a band top, here it
is, and it carries z" — answering the panel's deepest caveat at its root.

## Honest residual (still OPEN-TP-1)
The precise O(1) coefficient C still requires the Hilbert–Schmidt mode sum ‖T₂‖²_HS over the
600-cell modes, with the band-top density of states (van Hove). The CUTOFF is now grounded; the
PREFACTOR is not. So OPEN-TP-1: v0.1 OPEN → v0.2 PARTIAL (class) → v0.4 cutoff grounded; only C
numerical value remains. The falsifier is sharpened: if the band-top DOS reshapes the 1/ω tail
before ω_max, the clean ln(ω_max/ω_γ) ceiling is modified — the open edge.

## Numbers (script 1706, stdlib, runs clean)
λ_max=12; ω_max=√12/t_P=6.43e43; ln(ω_max/ω_γ)=64.49 → 64.5 C. Comparison: naive 1/t_P → 63.25C;
continuum-limit π/l_P → 64.40C; intrinsic band top √12/t_P → 64.49C. t_P=5.391247e-44 s.

## Review status
This is a REVIEW-RESPONSIVE strengthening (it closes the exact gap ChatGPT/Copilot named), not a
new unreviewed claim. Folded into v0.4; flagged in reviews-TP-1.md. Physics elsewhere unchanged;
two-regime / Regime-A dormancy unchanged (band top still ~28 orders above optical). Compiles 13pp.

## Next step
v1.0 + Phase-5 registry integration (gated, shared Tier-A). The C / HS-mode-sum closure is the
substantive post-v1.0 research arc.

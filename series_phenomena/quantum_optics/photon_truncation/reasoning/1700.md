# Reasoning — Patch 1700 — TP-1 origination (the truncated photon)

**Patch:** 1700 (band 1700–1799)
**Date:** 20 June 2026
**Window:** TP-1 / quantum-optics phenomena lane
**Deliverable:** `series_phenomena/quantum_optics/photon_truncation/TP-1/TP-1_truncated_photon.tex` v0.1
+ this fragment + `scripts/1700_truncation_regularization.py`

---

## 0. Trigger

Thomas surfaced Rukan, Gulla & Skaar, *A truncated photon* (arXiv:2510.21636,
accepted PRL; v2 dated 27 May 2026) via a Live Science write-up and asked whether
the effect is compatible with the CPP postulates, then directed it be turned into a
phenomena-series paper.

## 1. What the RGS result actually is (stripped of the headline)

NOT "splitting an elementary particle." The real content, within standard QFT:

- A single-photon Fock state has **infinite temporal tails**; you cannot write
  "photon to the left, vacuum to the right" with a compactly-supported temporal mode.
- Applying a hard shutter (strict projection of the field onto a region) yields a
  **superposition/mixture of photon numbers from 0 to ∞**, yet one **locally
  equivalent** to a single photon on one side and vacuum on the other.
- Mechanism RGS name explicitly: photon creation is **a consequence of violation of
  time-translational invariance**; by Noether they expect apparent energy
  non-conservation. The shutter is the symmetry-breaking agent.
- ⟨N⟩ **diverges only in the instantaneous-shutter limit**; for realistic speeds
  "even 1000 photons would be extremely unlikely" (a SLOW divergence — see §4).
- Contested: Faccio (Glasgow) reportedly first called it "nonsense." So compatibility
  ≠ endorsement of the framing; we assess against CPP and add the substrate content.

## 2. The compatibility map (Layer C — structural, three-for-three)

Each RGS surprise lands on a STANDING CPP mechanism, not a new posit:

1. **Cuttable photon** ⟸ photon = perturbation of the **DP Sea** (master_glossary
   line 56: "Perturbations of the DP Sea are what we observe as photons…"). A real
   medium carries a real propagating disturbance; truncating it is unremarkable. CPP
   is structurally MORE comfortable with an extended cuttable photon-wave than
   continuum QFT.
2. **0→∞ Fock mixture** ⟸ **bosonic multi-occupation of 600-cell eigenmodes**
   (QM-5 §2: ψᵢ=Σ aₖuₖ(i), promote aₖ→operators, bosonic photon modes pile up
   freely). Many-photon states are native.
3. **Local-simple / global-complex** ⟸ **partial trace over the DP Sea + Nexus
   global unitarity** (QM-4 §4–5: reduced ρ simple, global state retains structure,
   Nexus enforces consistency). "Single photon left, vacuum right, 0→∞ global" is
   the standard system/environment split across a spatial boundary. Expected.

**Mechanism convergence (the strong contact).** RGS locate creation in broken
time-translation symmetry. CPP says the same in its vocabulary: the shutter is a
macroscopic time-dependent reconfiguration of the DP-Sea boundary doing work on the
Sea — **dynamical-Casimir-type** creation from a driven boundary. The Nexus enforces
GLOBAL consistency at each Absolute Moment; a driven external boundary injects energy
into the photon subsystem, which is not isolated. So CPP REPRODUCES the Noether
reasoning rather than conflicting with it.

## 3. The CPP-specific content (the part worth a paper)

Compatibility alone is near-automatic: QM-5 claims continuum-limit reproduction of
standard QFT, and RGS *is* standard QFT, so CPP must reproduce it — including the
divergence. The non-trivial, falsifiable CPP content is the **regularization**:

> The RGS divergence is **ultraviolet** in origin (it is infinite ONLY as the shutter
> → instantaneous, i.e. only when unbounded high-frequency content is demanded). CPP
> carries a hard UV cutoff at **k_max = π/l_P** (QM-5 §7, the same cutoff that makes
> the electron self-energy finite). Therefore in CPP ⟨N⟩ is **finite for every
> physical shutter**, and bounded for ALL shutters because no shutter can switch
> faster than one **Absolute Moment t_P**. Continuum ∞ → finite, exactly the QM-5
> infinite-renormalization → finite-renormalization move, one sector over.

This needs only two already-established facts — (a) UV origin of the divergence [RGS],
(b) CPP has a UV cutoff [QM-5] — so the **finiteness conclusion is robust** and does
NOT depend on reproducing the RGS kernel.

## 4. The numeric ceiling (Layer B/C — scaling solid, coefficient open)

Model the created-photon spectrum with the mild/log UV form dN/dω = κ/ω on [ω_γ, ω_cut]:
⟨N⟩(ω_cut) = κ ln(ω_cut/ω_γ).

- Continuum instantaneous: ω_cut→∞ ⟹ ⟨N⟩→∞ **logarithmically** (slow). This is the
  divergence class that REPRODUCES the RGS qualitative statement "even 1000 photons
  extremely unlikely for realistic speeds" — log growth means reaching N photons needs
  ω_cut ~ ω_γ e^{N/κ}, absurdly sharp. So RGS's own qualitative claim *selects* the
  mild class, which is the class giving an order-tens CPP ceiling. Internal consistency.
- CPP ceiling: ω_cut ≤ ω_P = 1/t_P. For optical ω_γ (500 nm, 3.767e15 rad/s) and
  ω_P = 1.855e43 rad/s: **L = ln(ω_P/ω_γ) = 63.76** ⟹ ⟨N⟩_max ≈ **64 κ** — order tens,
  finite, Planck-set.
- Sharper than RGS: reaching even ~100 photons (κ~O(0.1–1)) needs τ_s < t_P, which CPP
  forbids by construction. RGS say "extremely unlikely"; CPP says "impossible past the
  Planck ceiling."

κ is the O(1) model coefficient. Pinning it = importing the RGS Bogoliubov kernel
B(ω,ω′) and cutting the ω-integral at π/l_P. **That is OPEN-TP-1** (the falsification/
maturity gate). Until then the paper is v0.1, framework-conditional, NO THEO — exactly
EU-1's honesty posture.

## 5. Routes considered and ruled out / deferred

- **Reproduce the RGS kernel from scratch here.** Deferred (OPEN-TP-1). Doing it badly
  fabricates false precision; the finiteness result doesn't need it.
- **Claim a registered THEO.** Rejected. This is a phenomena paper, framework-
  conditional, like EU-1 (NO THEO). At most a labelled in-paper Proposition + a global
  PROP/PRED in the DEFERRED registry patch.
- **Power-law (p<1) spectrum** as the headline. Rejected as headline (gives ~1e14 κ at
  the cut — wildly model-dependent, and inconsistent with "1000 unlikely"). Kept only
  as a robustness check that finiteness-under-cutoff is spectrum-independent.
- **Strong (linear/quadratic) divergence.** Inconsistent with the RGS "even 1000
  unlikely" statement; not used.

## 6. Placement / registry (DEFERRED — batched integration patch, not 1700)

- Home: `series_phenomena/quantum_optics/photon_truncation/` (new domain folder, first
  member — allowed: "a domain folder appears when its first phenomenon does").
- Frontier home for the registry entry: `frontier_sectors/QM.md` (driving physics =
  QM-4 + QM-5), paralleling EU-1's SR.md home.
- DEFERRED registry edits (one flagged integration patch, after refresh):
  OPEN-TP-1 (pin κ via RGS kernel @ π/l_P); PROP-TP-1-1 (lattice regularization);
  PRED-O (the order-tens ceiling, falsifiable-in-principle); paper_catalog.md;
  README.md; INDEX.md; series_phenomena/README.md "Current members" row;
  master_glossary.md (truncated photon, Bogoliubov-from-driven-boundary).
  NONE of these are in patch 1700.

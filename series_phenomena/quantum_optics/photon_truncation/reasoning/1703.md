# Reasoning — Patch 1703 — TP-1 Review 1 logged (panel 1 of 3)

**Patch:** 1703 (band 1700–1799)
**Date:** 20 June 2026
**Deliverable:** `TP-1/review/reviews-TP-1.md` (new) + this fragment.

## Decisions
- **Capture now, revise later.** Review 1 logged immediately (capture-at-time), but the paper
  is NOT revised yet: held at v0.2 until DG-3 is complete (3 reviewers), then one consolidated
  v0.3 patch. Avoids version churn (v0.3→v0.4→…) from per-reviewer edits.
- **Verdict:** Review 1 = SHIP toward v1.0, no verdict-flipping objections. The core test
  ("just standard QFT plus a renamed cutoff?") was explicitly answered NO: the ontology mappings
  are genuine instantiations and the cutoff acquires physical identity. T1–T4 all HOLD; §7 code
  SCRIPT-EXECUTED with matching numbers.
- **Two calibrations queued** (non-conflicting, trivial): (1) note √ω = standard EM mode
  normalization; (2) one sentence "within CPP the cutoff is physical; within RGS it stays formal."
  Both strengthen, neither is verdict-flipping; folded into v0.3 with the rest of the panel.
- **Attribution.** Operator pasted one block under ambiguous chatgpt:/Grok: labels. Recorded as
  ChatGPT (first label + overclaim/verdict-honesty steer match); Grok slot stays open. Re-tag if
  needed — does not affect physics.
- **Gemini is outside the DG-3 default panel** (ChatGPT/Grok/Copilot). The operator does not
  strictly need it; Copilot + Grok complete the standard. Noted so the cycle isn't gated on Gemini.

## Registry (DEFERRED, unchanged)
OPEN-TP-1 PARTIAL, PROP-TP-1-1, paper_catalog, README, INDEX, series_phenomena/README,
frontier_sectors/QM.md, master_glossary — still deferred to the post-panel integration patch.
1703 is greenfield (one new reviews file) + this fragment; own-subtree only.

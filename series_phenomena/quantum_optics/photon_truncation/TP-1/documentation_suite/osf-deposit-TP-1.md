# OSF Deposit Record — TP-1

Section-F registration record. Claude cannot deposit to OSF directly; this is prepared metadata for Thomas (or Isak) to create the OSF component under the parent CPP project. Fill the minted DOI back into this file and the paper header.

## Parent project
- Create TP-1 as a **component** of the parent CPP OSF project (consistent with prior paper deposits; confirm the parent DOI from a recent paper title block at deposit time).

## Component metadata

**Title:** TP-1: The Truncated Photon and the Lattice Regularization of Shutter-Induced Photon Creation

**Contributors:** Thomas Lee Abshier, ND (Hyperphysics Institute); Claude Opus (Anthropic).

**Category:** Project / Paper preprint.

**License:** match the parent project's license (confirm at deposit time).

**Tags:** truncated photon; dynamical Casimir effect; quantum optics; Conscious Point Physics; 600-cell; band top; lattice regularization; intrinsic UV cutoff; shutter-induced photon creation; framework-conditional; QM-4; QM-5; Rukan–Gulla–Skaar.

**Description / abstract (deposit abstract — honest-scope version):**
> TP-1 reads the Rukan–Gulla–Skaar truncated photon (PRL 2026) against the Conscious Point Physics (CPP) substrate. The cuttable photon is a DP-Sea perturbation; the $0\to\infty$ Fock mixture is bosonic 600-cell mode occupation (QM-5); the local-simple/global-complex structure is the QM-4 partial trace under the Nexus; and the shutter is a driven DP-Sea boundary (dynamical Casimir). The instantaneous-shutter divergence is derived logarithmic from the RGS kernel, and is regularized intrinsically: the 600-cell mode spectrum has a hard band top $\omega_{\max} = \sqrt{12}/t_P = 2\sqrt3/t_P$ (from the QM-5 dispersion and the largest adjacency eigenvalue $\lambda_{\max} = z = 12$), above which no modes exist, giving a finite ceiling $\langle N\rangle_{\max} = C\ln(\omega_{\max}/\omega_\gamma) \approx 64.5\,C$. The contribution is compatibility plus foundational regularization — not a new mechanism and not an experimental prediction. NO THEO, NO PRED; framework-conditional. Open residual OPEN-TP-1: the $O(1)$ coefficient $C$ from the 600-cell Hilbert–Schmidt mode sum.

## Files to deposit
- `TP-1_truncated_photon.tex` (+ compiled PDF, 14 pp).
- `review/` (package + reviews).
- `documentation_suite/` (this suite).
- `scripts/1700,1701,1706` (verification).
- `reasoning/1700…1708` (per-patch).

## DOI (fill on mint)
- TP-1 component DOI: `__________` (paste into the paper title block and this file once minted).

# 1868 — J7 audited: the rod's "wall" is the coat's effective radius, and the convention's own normalization already knew it

**STATUS:** verbatim, at-patch (Opus, DM 18xx lane, 4 July 2026). D4 executed under founder-delegated judgment
("Please proceed using your best judgment"). Judgment call made: before inventing a mechanism (D4-A — which the
founder inversion forbids) or demoting (D4-D), AUDIT the load-bearing idealization itself (J7, the hard-capsule
floor). Verify: `code/1868_coat_radius_floor.py`. **Layer: idealization audit (no new parameters; uses only the
registered coat scales E_ee = 0.9 MeV [1813], r_scr = 1.0 fm [SF-5], r_c = 1 fm).**

## The idea

A rod has no material wall. Its elastic size is the effective interaction radius of the screened coat
potential: b_eff(v) solving V_coat(b) = KE(v). The 1856 hard-capsule MC implicitly froze b_eff at its
low-velocity value; the physical floor inherits b_eff(v) — velocity dependence for free.

## The self-consistency hit (the result that licenses the reading)

Equating the 1860 convention normalization (ε·0.11·N at N = 16) to the 1856 MC's σ_T = 23.72 r² implies an
effective capsule radius **r_impl = 9.46 fm**. The coat potential independently gives **b_eff(50 km/s,
segment encounter, r_scr = 1.0) = 9.21 fm** — agreement to **3%** with zero tuning. The convention's own
normalization IS the low-velocity coat radius. The 0.11·N "geometric" cross-section was never a material size;
it was the coat radius evaluated in the dwarf-velocity regime where the convention was anchored.

## The variant grid and the honest spread

floor(v) = ε·0.11·N·[b_eff(v)/b_eff(50)]^k, four variants (μ = segment/rod × k = 2/1): cluster(1500) spans
0.028–0.245 — the compact assemblies (k = 2) pass the entire bound ladder including Andrade < 0.13; the
long-rod k = 1 passes only Sagunski. **Stress test (§6):** feeding r = b_eff(v) through the MC's own f(A)
table (hard contacts retained inside b_eff) gives k_eff ≈ 0.4 and cluster = 0.385 — still violating. So the
suppression exponent spans k_eff ≈ 0.4–2 across defensible assemblies and the verdict swings across the ladder
over that span. The hard-contact-inside-b_eff reading double-counts hardness at high v (interior contacts also
soften as KE grows); the truth requires treating the softness exactly. **Decidable, not arguable → Patch 1869
computes the exact classical deflection-integral shape.** Group-point note: every variant converts 1867's
"unfit hit" (0.5–0.67) into an undershoot (0.05–0.28 vs 0.5 ± 0.2) — the hit was borrowed from freezing b_eff.

## Ledger

J7 → J7′: hard-capsule floor identified as idealization artifact; coat-radius floor adopted pending the exact
shape (1869). J8 registered: rod element length/spacing unpinned in-session (L ≈ 85 fm at N = 18 implied by
A_ref = N/4 at r_impl — needs registry pin). J9 registered-and-immediately-superseded: assembly exponent k
(resolved by 1869's exact computation).

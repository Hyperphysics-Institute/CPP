# Reasoning fragment — Patch 2932 (A4 blocked; the dependency graph tells the truth)

**STATUS: contemporaneous. Readout at `anchor_a4_readout.md`.
Grep-and-read only.**

The A4 readout took twenty minutes and returned the unwelcome answer:
SF-6's ε₀ is tuned, not derived, by the paper's own exemplary
self-labeling, and the anchor the triangle most needed is blocked
behind an EM-sector open problem registered months ago. I want to
note what made this readout easy: SF-6's two-tier rigor ledger. The
paper said "toy model, parameter-tuned, not zero-parameter" in five
separate places, so the readout required no judgment call at all —
past honesty is present infrastructure. If SF-6 had oversold its
constants, this patch would have been a week of forensic tuning-
archaeology instead of an afternoon.

The uncomfortable finding is the dependency graph: the dark-matter
deposit now runs through OPEN-FP-6-CONSTANTS, an EM-sector item
nobody associated with the DM campaign. I stated it plainly rather
than softening it, because the alternative — quietly hoping the A3
audit or a new channel makes FP-6-CONSTANTS unnecessary — is the
shape of wish-driven planning. Maybe they will; the finding stands
either way.

What I deliberately did not do: start deriving ε₀ from the 600-cell
stiffness on the spot. The temptation was real (this session already
built exact Sea-response machinery that feels adjacent), but
FP-6-CONSTANTS is a registered EM-sector flagship-adjacent problem
with its own history, and attacking it as a side-quest inside a DM
readout patch would violate both per-patch discipline and the
sector's ownership of its own frontier. If it is to be attacked, it
gets its own arc with its own prereg.

Next: the A3 independence audit (provenance tracing of the ZBW/
Compton conversion — does it import d_DP = a from the α1 chain?),
then the CONV-001 motion bundle. Session context is long; if Thomas
closes after this turn, the handover carries the sequence.

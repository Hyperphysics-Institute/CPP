# Reasoning fragment — Patch 2751 (X7-NSCAN-EXT record)

Verbatim, at-patch.

Execution: six ensembles, full frozen length, frozen seeds 789–794,
chunked foreground with checkpoint carry; no phantom-process events
this act (killed before the EXT block); accounting exact.

The frozen H-fork fired H4 on the inversion 80-ENH → 96-UNENH. Before
writing any interpretation I ran two post-hoc diagnostics, both
labeled as such in the record:

1. The IAT audit was the mandatory first suspicion: a ratio curve that
   oscillates with ~1.5σ flips at adjacent sizes is the classic
   signature of block-SEMs undercounting long correlation times — and
   a driven-phase slow mode would itself be candidate-(b) physics, so
   the audit could not be skipped. Result: IAT 1.4–1.8 samples on all
   fourteen chains, ESS ~10^3 everywhere. The errors are honest. The
   oscillation is scatter.
2. Given honest errors, the correct aggregate instrument is pooling:
   the six measured sizes above 64 are mutually consistent with one
   constant (χ²/dof 6.01/5) at r = 1.461 ± 0.082 — 5.6σ above unity.
   The step-location scan puts the equal-best split at 64 itself. So
   the point-wise band classification (built for a bracket that the
   scatter denies) under-reads what the ensemble of points states
   plainly: everything at and above 80 is enhanced at the plateau
   level, and the switch-on is confined to (64, 80].

Why record-not-enact: the pooled statement is post-hoc — the 2750
prereg froze point-wise bands, and H4's clause routes arbitration to
the automaton. The honest structure is therefore: H4 as frozen; the
pooled description disclosed as the input to the NEXT frozen
instrument. That instrument is now obvious and cheap: B-CHECK at 80
(the onset-adjacent size), measured at ≈ 0.9 h with the carried-energy
B design — a single dedicated block. If B reproduces the enhancement
at 80, candidate (a) dies at the onset itself; if B shows tilt-level
response, the incremental path is impeached specifically in the
window where the automaton can adjudicate cheapest.

Seed hygiene: 795 reserved for H3 was not consumed (H3 did not fire);
returned to pool, noted in the record ledger.

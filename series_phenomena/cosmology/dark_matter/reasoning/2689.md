# reasoning/2689.md — R1-L5 hop-Laplacian branch (verbatim, at-patch)

STATUS: verbatim, captured at Patch 2689, 21 July 2026.

First, the trigger call, because it was mine to make and votable. The
frozen text says L5 runs "if legs L1-L4 leave a live representation-
dependence question." After J3-REVISE I could not honestly say the
question was closed: the scale had just been shown normalization-
sensitive WITHIN the 1/r assembly, and L2's committed test had failed.
If I skipped L5 the packet would carry "the structure is representation-
independent" resting entirely on arena variation inside one assembly.
So I ran it, and flagged the determination itself for the panel.

The branch turned out to have a theorem in it. Before solving anything I
noticed the hop-Laplacian operator A = -Delta + 8I is an M-matrix, and
M-matrix inverses are elementwise non-negative — so this assembly CANNOT
stagger, not as a numerical finding but as a structural fact. That
sharpens what the staggering IS: the 1/r assembly's kernel has both signs
in its spectrum (the zone-boundary modes with negative G-tilde), and the
staggered evanescent response lives on exactly those modes; a
nearest-neighbour diffusive kernel has no such sector. The numerics then
confirmed both the positivity (neg-frac exactly zero on FCC and HCP) and
the analytically predicted scale, 0.1955 fm from cosh(q/sqrt2) = 2
against 0.199-0.200 measured, R^2 up to 0.999.

The reading I put in the record is the one the frozen class demands and
no more: J6-DIVERGE, a representation-dependence finding in its own
right, adverse to neither branch. But the shape of the full picture is
now clean enough to state for the packet: within the derived 1/r
assembly the staggered envelope at 1/(2 kappa) is robust across every
arena we could build; across ASSEMBLIES it is not universal — and the
hop branch's scale, ~0.2 fm, is essentially the 1/r assembly's own
weak-coupling Yukawa scale, which suggests the two assemblies agree in
the diffusive regime and part company exactly where the multiple-
scattering physics turns on. The S1b derivation chain grounds the 1/r
form; whether that grounding survives the panel's re-examination is the
returns' business, not this leg's.

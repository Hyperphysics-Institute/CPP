# Reasoning fragment — Patch 2980 (W-6: the C-5 discrete-spectrum lemma package)

## Why C-5 discharged in one patch

The panel's named gap was a grade gap, not a truth gap — L-3's conclusion was never contested, only the object it was proved on (a continuum integral vs the finite Moment sum the engine computes). L-3′ therefore isn't a repair; it's the same two-line argument run on the right object, and running it there BUYS things the continuum version couldn't offer: exact finite-sum coefficients instead of O(·), a Lagrange remainder bound instead of an asymptotic claim, and Gemini's entire-function observation doing real work (a trigonometric polynomial converges globally, so the only restriction is the drive class, and the drive class is < Nyquist by construction of Moment-stepped drives — aliasing becomes a domain statement, not a failure mode). CHECK 7/8 is the point made numerically: the identity is exact at K = 8, where any continuum quadrature reading of the same profile is visibly wrong.

## C-5(ii): withdrawing rather than defending

The superposition remark was the session's cheapest fix and the most instructive one. The theorem never used per-DP decomposition: L-1 defines γ as the dressed kernel of the FULL linearized dynamics (relay effects included), and T-1's zero is a total-force statement, so inserting a constant drive kills the total DC weight directly. Defending the remark would have meant proving a traversal-independence lemma the argument doesn't need; withdrawing it makes the proof shorter and the mechanism/operator division cleaner — the per-DP cycle picture stays as the MECHANISM behind the zero, the operator proof runs on the total force. When a reviewer attacks a remark, the right response is sometimes deletion, not fortification.

## Sequencing note

C-5 is discharged structurally here and goes to the next round for confirmation — likely bundled with the W-4 ensemble results, since the promotion bar needs both anyway and a standalone C-5 round would burn a review cycle on a two-lemma package. The T-1 v1.2 wording strengthening rides along per the 2979 adoption. Next in queue: W-4 (the ensemble FRESH prereg with all eleven adopted requirements), W-5 (E-1), W-7 (the QM-1 audit gating AP-2), and the founder's two one-word ratifications (AP-1, AP-3) whenever convenient.

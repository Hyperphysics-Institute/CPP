# Reasoning trail — Patch 0700 (Tetra-gravity dark-matter arc OPENED: OPEN-COSMO-DM-1 + CONJ-DPS-1/2/3 + CONJ-COSMO-1 registered)

**Patch:** 0700 (Session 149, 31 May 2026)
**Scope:** pure-synthesis / registration patch — arc opening. NO new physics, NO theorem, NO verdict change. Computes nothing (no verify script per `templates/reasoning_capture_protocol.md` §1).
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this Patch

Opens the tetra-gravity dark-matter arc at the patch-0700 block per the Session-149 handover
(`handovers/2026-05-31_session_149_tetra_gravity_dark_matter_arc_kickoff.md`) and its canonical kickoff
sentence. The arc's physical content is the source-of-record §6c entry in `founders_vision.md` (Patch 0672a);
this patch only *registers* it into the frontier so the work is trackable and the falsification sequence has
a registry home.

## §2 Clone-first / grep gate (Line 1 of the handover) — CLEARED

Performed before any edit:
- Patch 0700 confirmed free: the only `Patch 0700` string in the repo is the handover doc itself (which plans it).
  Repo high-water mark is 0685 (the chirality window), so 0700 is a clean buffer.
- The five IDs (OPEN-COSMO-DM-1, CONJ-DPS-1/2/3, CONJ-COSMO-1) appear ONLY in `founders_vision.md` §6c and the
  handover — none are in `theorem-registry.md`, `frontier_sectors/CONJ.md`, or the dashboard. Genuinely unminted.
- New sector prefixes **DPS** (Dipole-Sea population) and **COSMO** (cosmology) confirmed absent from all registries.
- Stayed entirely out of the chirality window's live-collision zones (`theorem-registry.md` header changelog /
  DSL rows / Summary Statistics; CONJ-CHIR-1). Appended strictly below CONJ-CHIR-1 at CONJ.md EOF.

## §3 Decisions made (the "why" the protocol asks registration patches to capture)

1. **All five IDs co-located in `frontier_sectors/CONJ.md`** (rather than minting a `frontier_sectors/COSMO.md`).
   The handover's registration plan says "register in CONJ.md and the dashboard." A near-empty sector file for a
   single far-frontier umbrella + four conjectures is heavier than warranted at the registration stage. Dashboard
   §1 carries a `COSMO / DPS` row pointing to CONJ.md, with the `COSMO.md` split explicitly **deferred** until the
   arc grows. This is a documented interim, not a discipline violation.

2. **Gravity foundation cited, NOT re-derived; OPEN-GRAV-SSV-GR-EXTENSION NOT minted.** The handover's GR-status
   correction was verified directly: `series_relativity/SR_companion_papers/` contains c05 (Newtonian gravity from
   SSV shell-broadcast, F = G m m'/r²) and c07 (weak-field GR) as full `.tex` files. The weak-field Newtonian force
   law the rotation-curve dynamics ride on is therefore already built. So the registration cites c05/c07 and does
   NOT register the GR-extension prerequisite that §6c lines 924/928 still call for. That contradiction is reconciled
   in Patch 0701 (a dated §6c update note), because §6c is the source of record and must not be left self-inconsistent.

3. **Honest caveat recorded on c05's headline claim (→ Patch 0702 / TODO-014).** As written, c05's
   "G = ℏc/m_P², exact, zero free parameters" is the *definition* of the Planck mass (m_P ≡ √(ℏc/G)) rearranged,
   and the numerical "verification" feeds in a CODATA m_P that was itself computed from G — circular. c02 was checked:
   it derives a genuinely *dimensionless* geometric constant α_geom = 3(11+5√5)√(5+√5)/320 ≈ 0.5594 (Voronoi
   second moment), but carries all dimensionful content in E_P, l_P taken as given (SSV_crit ≡ E_P/l_P³ asserted as
   the Planck energy density). **Dimensional-analysis reality:** a dimensionful constant cannot be derived from pure
   geometry — at least one dimensionful scale (the lattice spacing ≡ l_P) must be set by calibration. The defensible
   restatement: all *dimensionless* structure is fixed by the 600-cell; exactly ONE dimensionful scale is a shared
   calibration; G then follows with no *additional* parameter. For the DM arc this is a *framing* caveat only — the
   rotation-curve dynamics need the Newtonian *form* with a G matching observation, which c05 supplies regardless.
   Step 0 should cite this as "Newtonian force law recovered," not "G derived from scratch," until the singular-
   calibration claim is verified. Tracked as TODO-014.

## §4 What this patch deliberately does NOT do

- Does NOT begin any Step 0–5 computation. Per the handover hard rule, registration precedes the falsification
  sequence, and no paper/anthology framing happens until Steps 1 and 2 are computed and survive.
- Does NOT touch `theorem-registry.md` (chirality window's live zone; nothing here meets the THEO bar yet anyway).
- Places the arc under `series_phenomena/cosmology/dark_matter/` — the new phenomenon-derivation tier (home of record for an empirical phenomenon explained from the axioms, at any maturity; see `series_phenomena/README.md`). Only the `reasoning/` workbench is populated now; NO paper draft / anthology / `documentation_suite` until Steps 1 and 2 survive, per the handover hard rule.

## §5 Next

Step 1 (σ/m vs SIDM bound — cheapest potential kill) is the first real deliverable. It needs c14 (qDP–qDP
confinement/chaining-length, short-range bonding cross-section inputs) and c15 (SU(3) color, the qCP–qCP force sign =
load-bearing assumption #1). Step 0 is a half-day audit (confirm c05 superposition to a diffuse halo; v²=GM/r).

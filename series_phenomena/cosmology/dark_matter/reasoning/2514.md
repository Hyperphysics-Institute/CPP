# Reasoning — Patch 2514 (16 July 2026): in-run emergent same-charge apposition duty (registry condition 2)

Sections 1–3 written and FIXED before the instrumented run executed (G7). Section 4+ appended after.

## §1 Target and mandate

Registry condition 2 (`DM-CANDIDATE-B_N8_cdm_like_registration.md`): "whether the dance's
emergent same-charge apposition duty ≥ 3/7 (expected yes; not yet measured in-run)."
Provenance: 2435 fixed the STATIC combinatorial δ_core = 3/7 (charge-assignment statistic,
C(8,4) neutrality count); 2470 §2 identified the residual as the DYNAMIC transit-apposition
quantity the dance integrates directly (it does NOT plug in 3/7) and recorded the founder's
transit + pile-up argument for duty ≥ 3/7 as the expected, ring-conservative direction.
Queue authority: 2511 §5 item (B); 2513 handover queue item (A). PD-006 in force.

## §2 Operational definition and design (fixed in advance)

Model: `dance_v8` VERBATIM at pinned κ (kscale=1.0 only; FREF unchanged; TC=60, burn=0.15) —
the instrumentation is read-only counting; the trajectory is bit-identical to 2510/2513.

**Primary statistic (the verdict-bearing number):**
  duty_qq = Σ_steps #(qCP–qCP pairs with r_ij < a_qq AND C_i·C_j > 0)
          / Σ_steps #(qCP–qCP pairs with r_ij < a_qq)
i.e. the same-charge fraction of qCP–qCP apposition-time at the model's own contact scale
a_qq = ħc/264 ≈ 0.747 fm (the 2461 K3 saturation/transit scale). qCP–qCP because δ_core is
the stiffness-dominant quantity (2435). Post-burn steps only. Note: no qCP pair is within
a_qq at HOME positions (nearest same-plane 1.15 fm, axial 1.15 fm, diagonal 1.63 fm), so
every counted apposition is dance-generated — the emergent quantity, uncontaminated by
scaffold geometry.

Grid: ring at dt = τ_C/{100, 50, 25} (verdict over all three — dt-robustness built in).

Secondary, reported, non-verdict-bearing: (a) radius robustness at {0.5, 2}×a_qq;
(b) ee and qe apposition duties; (c) rod at dt=1/50 for context; (d) mean apposed-pair
count per step (occupancy — flags a thin-statistics regime if very low).

Thin-statistics guard (pre-registered): if the TOTAL post-burn qq apposition-pair count at
the primary radius is < 1000 at any dt, that dt is UNCLASSIFIABLE and Branch X fires
(no verdict on starved statistics).

## §3 Pre-registered branch readings (no discretion after the run)

- **Branch Y** — duty_qq ≥ 3/7 at ALL THREE dt (primary radius): registry condition 2
  DISCHARGED in the founder-expected direction; condition text updated with the measured
  values; candidate (B) verdict UNCHANGED (the promoted dynamical reading integrates the
  energy directly; this is a consistency residual, not an input).
- **Branch L** — duty_qq < 3/7 at ALL THREE dt: the residual RESOLVES in the unexpected
  direction. Condition 2 updated with the measured values and an adverse-direction flag;
  verdict UNCHANGED for the same reason (no plug-in), but the discrepancy with the founder's
  transit argument is queued for disclosure in the next panel dispatch (not a standalone
  flash; it rides the next scheduled package).
- **Branch X** — dt-inconsistent, or any dt UNCLASSIFIABLE under the thin-statistics guard:
  condition 2 NOT discharged; register the robustness follow-up (longer TC and/or radius
  sweep) as the residual.

Kill-condition note (standing): no branch of this patch moves the candidate-(B) percentage.
Only the condition-2 status line and its measured-value annotation are touched.

<!-- ===== SECTIONS BELOW THIS LINE APPENDED AFTER THE RUN ===== -->

## §4 Results (verbatim)

Premise check: home-ring min qq distance 0.918 fm > a_qq = 0.747 fm ✓ (all counted apposition
is dance-generated). Trajectory checksums reproduce 2510/2513 exactly
(⟨Ep⟩ = −2734.2/−2725.4/−2700.8 at dt=τ_C/{100,50,25}) — read-only instrumentation confirmed.

```
RING dt=tauC/100: r<1.0a_qq: duty_qq=0.0010 (n=27188) PRIMARY | r<0.5a_qq: 0.0000 (n=21) | r<2.0a_qq: 0.3677 (n=994504)
    ee duty=0.0103 (n=106393); qe duty=0.2471 (n=282566); qq occupancy=5.33 pairs/step
RING dt=tauC/ 50: r<1.0a_qq: duty_qq=0.0021 (n=21800) PRIMARY | r<0.5a_qq: nan (n=0)   | r<2.0a_qq: 0.3697 (n=499508)
    ee duty=0.0150 (n=53314);  qe duty=0.2638 (n=140644); qq occupancy=8.55 pairs/step
RING dt=tauC/ 25: r<1.0a_qq: duty_qq=0.0095 (n=17224) PRIMARY | r<0.5a_qq: 0.0000 (n=4) | r<2.0a_qq: 0.3812 (n=255126)
    ee duty=0.0291 (n=27252);  qe duty=0.3056 (n=70482);  qq occupancy=13.51 pairs/step
ROD  dt=tauC/50 (secondary): duty_qq=0.0202 (n=26632)
```

Thin-statistics guard: all three primary counts ≥ 17k ✓ (guard does not fire).

## §5 Branch reading (mechanical, per §3)

duty_qq < 3/7 at ALL THREE dt at the pre-registered primary radius (0.0010/0.0021/0.0095 vs
0.4286). **→ BRANCH L FIRES: the residual resolves in the adverse (unexpected) direction at
the contact scale. Condition 2 updated with measured values and the adverse-direction flag.
Candidate (B) verdict UNCHANGED at 79.5% (the promoted dynamical reading integrates the
pairwise energy directly every Moment — it does not plug in a duty; this residual is a
consistency diagnostic, not an input). Disclosure queued to ride the next panel dispatch.**

## §6 Diagnostic structure disclosed (both directions, no post-hoc primary switch)

The measured duty is strongly RADIUS-STRUCTURED, and the structure is physically legible:

- At CONTACT (r < a_qq) apposition is ~purely opposite-charge — mechanically forced, because
  the dance's OUT legs terminate on opposite-charge TARGETS by construction and the pile rule
  stacks arrivals at those targets. Same-charge contact essentially never occurs (duty ~0.1–1%).
- At the BOND-SCALE shell (r < 2a_qq ≈ 1.49 fm, spanning d = 1.15 fm), the transit mixing the
  founder's argument describes is visible: duty = 0.368/0.370/0.381 — dt-STABLE and sitting
  just BELOW 3/7 = 0.4286 (85–89% of it), i.e. same order, not the founder's ≥ direction.

G7 note: the primary radius (contact, a_qq) was pre-registered in §2 and the verdict above is
issued on it; the bond-scale numbers are the pre-registered SECONDARY and carry no verdict.
Whether the physically-correct "apposition" scale for the 2434/2443 stiffness bookkeeping is
the contact scale or the bond scale — and whether the correct statistic is energy-weighted
rather than count-weighted (the pairwise energy weights close pairs most) — is a CONVENTION
question. It is queued as part of the same next-dispatch disclosure (alongside the Branch-L
adverse flag), NOT resolved here by choosing the radius that flatters expectation.

Follow-up registered under condition 2 (unchanged scope): energy-weighted duty at the
pre-registered dt grid — a one-line instrumentation change — schedulable after the panel sees
the count-weighted record; pre-register its reading before running.

G7 post-audit: κ pinned; trajectory untouched (checksums); primary radius, dt grid, guard, and
branch readings all fixed in §§2–3 before execution; the adverse result is recorded as-is.


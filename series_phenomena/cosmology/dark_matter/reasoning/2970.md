# Reasoning fragment — Patch 2970: handover under a live delivery failure

**The one decision that mattered: how to handle the empty returns.** Five seat returns were sent
twice as attachments and arrived with zero content each time. The tempting shortcuts were (a)
adjudicate from any partial glimpse and (b) carry reconstructed verdict content into the handover
so the next window "doesn't lose it." Both are fabrication-class: a return whose text is not
verbatim in front of the adjudicator is not a return. The handover therefore assigns ZERO weight
to anything from the failed deliveries and makes inline re-delivery the sole authoritative path.
The keys (0.102375 / 1.277299) are recorded in the handover so the next window can verify
execution claims without re-deriving the sweep.

**Registry restraint at Step E.** T-1/T-2/T-3 are not entered in theorem-registry.md and the T-3
falsifier is not promoted to predictions.md — both deferred to post-adjudication. Registering
PANEL-PENDING results in programme-level registries would let the registries assert what the
panel has not yet granted; the FP.md status block carries the state with the PANEL-PENDING label
explicit instead.

**Steps C/D handled by the lane's own discipline.** The DM lane's reasoning-capture protocol
(per-patch fragments bundled in the same git am) is capture-while-fresh by construction; marking
C/D "captured incrementally" with the fragment list is the honest completion, not an N/A dodge —
the 0457/0459 misapplication precedent is about deferring capture, and nothing here is deferred.

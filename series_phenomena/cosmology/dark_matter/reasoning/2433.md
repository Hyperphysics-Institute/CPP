# Reasoning — Patch 2433 (falsification REOPENED: founder-corrected geometry — jello qCP core, eCP-coat-governed bending; make-or-break sign undetermined)

STATUS: verbatim, at-patch (Opus, DM lane, 11 July 2026). Founder-directed reopening of
the 2432 candidate-(B) falsification. Organizational/registry patch (reopen + corrected-
geometry capture); the re-derivation itself is the next computation. No frontier verdict
asserted — status set to REOPENED/UNDER RE-DERIVATION.

## Why reopened (the founder's correction, which I accept)
The 2432 kill rested on 2431, which used the 1835 founder gradient read (a far-out eCP
coat at NON-uniform spacing, "charges farther apart across the hinge") and the 2430
ponderomotive tensor computed on that same lattice. The founder has now supplied the
correct geometry and it supersedes that input:

- **DM element = TWO planes** (one plane unstable, two stable), opposite polarity
  plane-to-plane. Each plane = two crosses; each diagonal arm = **eCP–qCP–qCP–eCP**
  (qCPs inner, eCPs at the tips).
- **UNIFORM spacing** — no far-out coat with a gap. The strong force (qCP, steep SSV)
  sets the effective inter-layer d; the eCPs ALIGN to the qCP lattice on each plane.
- **Tiny lattice-stabilized CP–CP separations** — much smaller than the free-DP ZBW
  excursion (State-1-in-Sea ↔ State-2-superimposed). Steep SSV gradients.
- **The qCP core is "jello"** — no static bending integrity. Founder's argument (which I
  accept, and which is the Earnshaw problem I hit all session): a bare qCP displaced
  toward a neighbour sees INCREASING attraction to superposition, then rebounds — no
  static restoring well. With all qCPs in random ZBW, the core is a soup with "almost no
  integrity." Only ZBW keeps it from collapsing.

Consequence: the qCP core governs neither bending stiffness nor a rigid cross-section.
**The eCP coat is the sole bending resistance** (resists compression on the ring's inner
circumference, tension on the outer). This retires BOTH the stiff-qCP-core regime (the
founder's own first correction, and my 2426 "+E_qq core") AND the 1835/2430 soft-coat
geometry my kill used. We are firmly in an eCP-coat-governed regime, on a NEW lattice.

## The make-or-break is reopened, and its SIGN is undetermined
Within the eCP-coat regime the ratio κ_θ/E_bond is pulled both ways:
- toward SOFT (fails): E_ee is small — net-zero charge, small eCP–qCP dipole separation.
- toward STIFF (survives): tiny separations → steep SSV gradient → stiff bonds; and the
  coat at the outer radius → R² lever-arm amplification of κ_θ.

The crux collapses to ONE question the jello-core insight forces — **what anchors E_bond
(the denominator)?**
- If the jello core still gives deep AVERAGE axial binding (a strongly-cohesive but
  shearless liquid — water has high cohesion, zero shear modulus): E_bond strong,
  κ_θ weak-coat, κ_θ/E_bond ~ (E_ee/E_qq)(R/d)² ~ few % → SOFT → light rings form →
  **the 2432 kill survives, but on better physics than 2431 gave.**
- If the jello core gives little NET binding ("almost no integrity"): E_bond is also
  coat-set, κ_θ/E_bond ~ (R/d)² ~ O(1) → STIFF → N=8 too small to close → longer rods /
  heavier rings → **candidate survives at a DRIFTED (higher-N) mass.**

The founder's own lean (bullet 1: "N=8 unlikely GIVEN THE STIFFNESS"; "number of planes
sets the bending radius = 2× DM-element count") is toward the STIFF reading. But "jello"
is ambiguous between strongly-cohesive-shearless (→ soft) and weakly-bound (→ stiff), and
that is a NUMBER, not a read. Sign genuinely open.

## Agreed regardless
**N=8 specifically is unlikely either way** — too stiff to close if stiff; drowned by
DD-excluded light rings if soft. If anything survives it is a ring FAMILY at N set by the
plane count (= 2× DM-element count), not the 11.26 GeV point. Additional geometric
constraint the founder names: the minimum ring circumference is set by (end-to-end plane
bonding energy) vs (Σ bending energies to distend outer + compress inner bonds around the
ring) — the make-or-break restated as an energy competition, and the plane count (not the
element count) sets the achievable bending radius because each plane can separate from its
neighbour.

## The re-derivation target (never computed on this lattice)
The variational / Monte-Carlo SSV–ZBW equilibrium:
1. Solve the lattice-stabilized CP–CP separations self-consistently — the equilibrium where
   the average ZBW displacement toward any of the 6 neighbours (5 qCP + 1 eCP for a qCP;
   4 eCP + 1 qCP + 1 free for an eCP) balances the gradient back toward the average of the
   others. (Rogue-wave SSV superposition differentials drive the displacements; a static
   lattice would have zero gradient.)
2. At those separations, compute the eCP-coat bending stiffness κ_θ (the sole bending
   resistance) AND the end-to-end binding E_bond (deciding whether the jello core anchors
   it deep or shallow).
3. κ_θ/E_bond vs 0.43 → sign of the make-or-break; and the min bending radius / achievable N.

This is the honest bottom and it is a genuine many-body substrate computation, not a
gradient estimate. The 2426–2431 arc's numbers are all superseded as inputs (the geometry
was wrong); what survives is the METHOD scaffolding (ponderomotive framing, the anisotropy
lesson) and the sharpened crux above.

## Anti-priorities held
Reopened a kill I had registered this same session rather than defend it, because the
founder gave a load-bearing geometric correction that supersedes the kill's input; did NOT
flip to "survives" either (the sign is genuinely open — resisted the symmetric error of
over-agreeing with the founder's stiff lean); named the single decisive unknown (what
anchors E_bond) rather than hand-waving; kept the corrected geometry verbatim-faithful to
the founder's account; set status to REOPENED, asserting neither verdict. The re-derivation
is scoped, not faked — no MC result is claimed. Writes: registry (candidate doc + SS.md) +
this note.

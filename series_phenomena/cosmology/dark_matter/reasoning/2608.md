# Reasoning — Patch 2608 (banding the one thing I could not derive, and fencing the
# fragmentation question before it can masquerade)

STATUS: verbatim, at-patch.

The transit-shed model has exactly one quantity the lineage does not hand me: how long
the encounter lasts. Everything else is registered — the well from the registered
potentials, the shed from the registered sink at the sole admitted η, the kinematics
from the H-γ rider. The interaction span L_int is genuinely underdetermined at this
layer (it is really a statement about where the oscillatory/drift split becomes
meaningful during a transit, which is the sink's own scope question wearing a different
hat), so it enters as a declared three-point band and the analytic deliverable is a
band, not a point. If the numeric brackets land inside that band across families, the
model has earned its registration WITH its honest width; if they don't, RE3 names the
failure without letting me widen the band after the fact.

The FRG fence in RE4 is tonight's most important sentence. At 0.9c an incident
carrying ~160 MeV of kinetic energy through a cluster bound by ~350 can plausibly
break the cluster before it fails to be captured, and a lazy reading would call the
first non-CAP verdict "the edge" regardless of which physics produced it. A
fragmentation edge and a capture edge are different objects with different relic
consequences (one bounds σ_cap's domain, the other feeds a destruction term the
campaign has not even opened), and the prereg refuses in advance to let one wear the
other's name.

The naive γ_naive = 1 + W/m line prints not because I expect it to win but because it
is the sentence already in the record — the 2602 expectation deserves to be tested as
written, and if the transit model beats it, the improvement is visible in one table.

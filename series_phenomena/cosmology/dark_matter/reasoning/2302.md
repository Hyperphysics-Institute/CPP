# 2302 — P3: the plateau discriminant lands (×56–×2000, implementation-independent, F1 upgraded to a three-way channel) — and the mid-band capture shape flags its own implementation before it could contaminate the paper

**STATUS:** verbatim, at-patch (Opus, DM lane 23xx, 6 July 2026). Verify
`code/2302_dm3_sigma_v_shape_discriminant.py` (the tension is stated in the script itself).

**What stands:** S1. The measured floor (1871) versus a standard SIDM form fitted to the SAME dwarf+LSB
data diverges ×56/×135/×2000 at group/cluster/Bullet velocities — shape, not normalization, because the
comparator has zero freedom left after the fit. F1 is upgraded to **F1′, three-way**: detection at
0.03–0.05 selects CPP; firm 0.5 kills CPP; firm null < 0.02 kills the floor and favors SIDM. Every branch
decides something — the best kind of falsifier.

**What was caught:** my classical-barrier capture implementation contradicts the panel-ratified v1.2
anchors in the mid/low band (0.34 at LSB vs 0.74–0.85 registered; 50 at dSph vs grazing-low registered).
The registered anchors govern; the implementation's mid-band shape is wrong; S2/S3 are PROVISIONAL and
J-DM3-3-OPEN requires pulling the corpus's registered capture computation before the paper claims them.
The anchor cross-check fired inside the same patch that would have carried the error — fifth in-house
catch of the campaign, and the first to be caught before its own commit.

**Queue: J-DM3-3 reconciliation → S2/S3 recompute → P1 assembly → paper → panel.** Fifth DM-lane session
banked toward DM-1's stability clock.

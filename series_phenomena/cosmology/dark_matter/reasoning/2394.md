# Reasoning — Patch 2394 (founder rules (c) on the Q2 dichotomy; SS43-Q3 contract registered; session close)

STATUS: verbatim, at-patch (Opus, DM lane, 10 July 2026).

## 1. The adjudication and its ruling

Patch 2393 put the vertex-class dichotomy on the founder's desk with three options:
(a) adopt Class V-t, (b) adopt Class S, (c) hold and let Q3/Q4 discriminate. The
worker's recommendation, stated in-conversation with its reasoning, was (c): adopting
a class by ruling now would repeat the D5 survival-conditional pattern without
necessity, because Q3 discriminates cheaply — its branch (a) kills the family on both
classes (mooting the fork), and its branch (b) leaves Q4 to DERIVE the class from
substrate mechanics rather than read it off the D5-A′ ruling's language.

**Founder verbatim (§10 block): "please proceed as recommended."** Context: the
recommendation immediately preceding was, in the worker's words, "my recommendation is
option (c), hold the dichotomy," followed by the Q3-next ordering and the offer to
draft the §34.10 contract and run the handover protocol. The ruling is therefore
recorded as: **(c) HOLD — no class adopted; Q3 next; Q4 carries the class-derivation
duty.** Also verbatim this session: the Q2 launch go ("Please proceed as per your
recommendation") and the plain-language stress-test question ("In plain language, how
is the ring hypothesis working under stress testing?"), answered in-session with the
fork-not-verdict framing that the ruling then ratified.

## 2. Q3 contract design choices (inside the §34.4 registration)

- **The full 1880 ladder, not just CONFRONT-1/3 rows.** §34.4's Q3 line named "np
  length + XQC + shielding ladder." The Q2 outcome upgrades the shielding leg to the
  decisive one: the V-t branch lands below the island floor, where the rod-era kills
  (LZ dead zone, DAMIC-unshielded) were derived for a FLAT long-range Yukawa. A
  collapsed/contact-range coupling is a different scattering object — the un-shielding
  logic (suppressed coupling ⇒ fewer slowing collisions ⇒ LZ sees full σ_n) and the
  σ_n formula's R_s⁴-type coherent enhancement both assumed the long-range form. The
  contract therefore demands the ladder at the ACTUAL ring-form potential (2393
  machinery), per class, both V-t bracket ends.
- **V2 as a PASS-GATE, not escalation-typed.** Q2's V2 was escalation-typed because a
  rod-limit disagreement would have been a physics collision with a registered ruling.
  Q3's V2 is different in kind: the re-parameterized ladder must reproduce the
  registered 1880 rod-point landscape before any ring point is trusted. Failure there
  means the MACHINERY is wrong, not that physics collided — so the type is fix-or-
  escalate, never grade-past. Typing the checks correctly is part of the
  no-silent-upgrade discipline running in both directions.
- **The DAMIC low-mass pin named as the one data action.** J12′-a pinned the
  DAMIC-shallow floor at 25 GeV and left it unpinned at the island bottom edge; the
  ring family sits at ≈ 8.45 GeV where that pin does not transfer. Naming it in the
  contract (CONV-004 measured-coefficient tag) prevents the next session from either
  silently reusing the 25-GeV pin or silently skipping the channel.
- **Grading branches written before the numbers**, as always: (a) V-t ladder-dead ⇒
  family dead on BOTH classes fully derived, adjudication moot, Clause 1(a) to the
  founder; (b) V-t clears ⇒ alive on V-t only, class fork to Q4; (c) split ⇒
  channel-by-channel conditionality, no smoothing. Branch (a) is the cheapest possible
  resolution of the fork and the reason Q3 precedes Q4 (cheap-kill order, §34.4).
- **Class S bookkeeping leg kept in scope** at fixed S_c = 0.0356, no bisection: the
  branch is already gate-dead, but the kill's derivation chain should carry its full
  ladder row so a future reader sees the death was over-determined, not single-channel.

## 3. What was deliberately NOT done

- The two Q2 exact identities were NOT registered in `theorem-registry.md`. They are
  exact mathematics, but their physical status is class-conditional (they are theorems
  ABOUT Class V-t, whose adoption is held). Registering them now would promote a held
  branch by the back door. They register if and when the class survives Q3 and is
  derived at Q4.
- "Vertex class S / V-t" were NOT added to the master glossary — §34.9-local coinages,
  defined at point of use; promote only on survival.
- No Q4 pre-work was folded in, despite Q4 now carrying the class-derivation duty —
  anti-priority held (cheap-kill order).

## 4. Session mechanics, owned

One container-path incident this session, caught before any damage: the Q2 derivation
script was initially written into a phantom tree (`/home/claude/CPP`, an artifact of
the session environment's path conventions) while the actual clone lived at the
session home (`~/CPP`). Symptom: the first run failed on a missing registered file;
diagnosis took one directory listing; the script was moved into the real clone, the
phantom tree deleted, and the run repeated from the pristine 475-key cache. No
registered file was touched in the phantom tree; the CLONE-FIRST discipline (all reads
against the real clone from bootup) is what made the phantom immediately visible.
Also owned: the first executed 2393 run crashed at B2 on a numpy 2.x API removal
(`trapz` → `trapezoid`); the fix added a mid-run cache flush so a crash cannot orphan
schema-pure appended points. The V1 subprocess's regeneration of `2391_results.json`
(identical verdicts, run-provenance fields only: fresh calls 115 → 0 on the warm
cache) was REVERTED before commit — the committed 2391 record is the at-patch
provenance of the original run, and overwriting it with a re-run's provenance would
falsify the historical record.

## 5. Queue at close

P1: SS43-Q3 on DM-WARM-2394 (founder go). P2: Q4 (double duty: R_s + vertex class),
then Q5 (coat-channel sign synthesis, prominence raised by the V-t ring–ring
collapse). P3 (separate lane): DM-1 v1.5 / DM-3 v1.1 R2 release execution, awaiting
the founder's verbatim release decision. Anti-priorities as in the handover. Block
EMPTY.

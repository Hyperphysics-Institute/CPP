# reasoning/2709.md — S4-N execution (verbatim, at-patch)

STATUS: verbatim, captured at Patch 2709, 21 July 2026.

The founder ordered hardening before panel, and the simulation
delivered the best kind of hardening: not just the frozen PASS but a
number I did not dare predict — kappa_fit within 1.7% of the DH value
on the main run. At Gamma = 0.225 the linearized theory is supposed to
be good to ~11%; landing at 1.7% says the Sea at its own operating
point is even better-behaved than the bound requires. The robustness
axis behaved like physics rather than noise: softening the core from
0.02 to 0.08 fm monotonically lengthens the fitted decay by the ~20%
you would expect from weakening the short-range response, while the
CHARACTER — monotonic, no layering, anywhere, in any run — never
moved. That character question was the entire point, and the answer
is unanimous across 6.8 million Monte Carlo moves.

What I held myself to: the prereg was frozen first (2708 committed
before a single sweep ran), the minimum-image approximation is
disclosed rather than buried, and the conditionality ledger travels
with the result — q = e is still 0764's pending estimate, and the
staggered readout's reclassification is still the panel's to enact.
The temptation after a clean numerical confirmation is to write the
conclusion in indicative mood; the record keeps it provisional where
the governance says provisional. But between us and the packet: the
derived, simulated, zero-parameter physical screening length of the
Sea is d_DP/2, and every independent instrument we own now points at
it — the continuum limit, the hop branch, the DH analytics, and the
simulation, bracketing 0.18-0.20 fm from four directions.

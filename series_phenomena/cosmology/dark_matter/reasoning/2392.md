# Reasoning — Patch 2392 (SS43-Q2 contract registered + session-close handover)

STATUS: verbatim, at-patch (Opus, DM lane, 9 July 2026). Founder verbatim: "draft Q2
contract and initiate handover protocol."

The Q2 contract mirrors the 2389/2381 warm-launch pattern for the third time because it
has now worked twice: keyword + self-contained handover set + pre-registered scope +
fixed battery + the founder's go left open. Two design decisions inside the contract
deserve their reasoning on the record:

1. **V2 is escalation-typed, not pass/fail-typed.** The natural regression — "the
   machinery must reproduce the rod's first power in the open-chain limit" — would be
   WRONG as a hard gate, because the rod's first power is itself PROVISIONAL (D5-A′,
   adopted by landscape at 1880 with derivation debt, not derived). If Q2's machinery
   produces a different rod-limit order, that is not a failed unit test; it is a
   collision with a standing provisional ruling whose alternative (second power) is a
   registered kill (the LZ dead zone). Silent-fail would bury a verdict-level event in
   a battery line. So V2 mandates the READOUT and types disagreement as a BLOCKING
   founder escalation. This is the same no-silent-upgrade discipline from the D(T_amb)
   arc, applied in the kill direction.

2. **The grading is pre-registered as mechanical with the three-branch readout written
   BEFORE the derivation exists** (whole-table ≤ 0.0120 / partial (0.0120, 0.0221] /
   gate-death > 0.0221). The §34.7 wall made this possible — the entire point of
   running Q1 first. Nothing about the grading can be adjusted after the derived
   number lands; the family is permitted to narrow member-by-member, and that partial
   branch is written down now precisely so a partial landing cannot later be spun
   either direction.

Adjacency handled honestly: the rod's own multipole debt (1880) is NOT smuggled into
Q2's completion criteria. If the machinery discharges it for free, it is recorded
against the debt; Q2 completes on the ring number alone.

Handover protocol executed per operating_system.md §15 Steps A–H; the audit table is in
the Step-H document (`handovers/2026-07-09_dm_ss43_q1_wall_q2_contract.md`). Lane
conventions applied and declared where the DM arc's per-patch reasoning capture stands
in for the paper-scoped tier files (Steps A–C), consistent with the 2026-07-07 DM close
precedent — equivalences stated per-step in the audit table, not silently skipped.

Founder's desk: apply 2392; next window pastes the fixed kickoff line and opens on
DM-WARM-2392 with the verbatim go. Block EMPTY.

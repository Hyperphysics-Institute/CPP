# Reasoning fragment — Patch 2988: the QM-1 audit — retire the grounding, save the theorem, clear the gate

**The separability question was the whole audit.** Eight places in QM-1 say DI-bits carry phase;
the founder says they don't. If the audit had stopped at counting conflicts, the verdict would
have been either "QM-1 falsified" (too strong) or "prose cleanup" (too weak). The load-bearing
move was tracing what the THEOREM'S PROOF consumes: Definition (site field) → tight-binding
update → graph Laplacian → limit. Eq. (phasehop) — the one numbered equation that puts phase ON
a bit — is never invoked in the proof; its value never reaches T. Once that is established by
reading, not by hope, the demotion target is precise: the grounding layer dies, the mathematics
survives conditionally.

**Where honesty had to push back on convenience twice.** First: the temptation to call the
pattern-level relocation a "refinement" and declare QM-1 intact. The 2957 fragment names this
failure mode (rescue-by-reinterpretation), so the audit states the cost plainly — the paper's
headline "the imaginary unit is not postulated" claim is FALSE under the current ontology; the
−i is postulated pending a derivation that does not yet exist. That is a genuine demotion,
registered in the SR-1 GIT class. Second: the temptation to hold AP-2 hostage to the revision
("ratify only when QM-1 is fixed"). That inverts the dependency: the glossary currently asserts
an ontology the founder has explicitly denied; withholding AP-2 preserves the false text longer.
The gate's question was narrow — does no-phase contradict shipped MATHEMATICS — and the answer
is no.

**The A1′ double-kill was found by the audit, not assumed.** Eq. (phasehop) fails P-1 (no phase
content) AND the reset-per-hop clause the founder ratified at 2982: accumulation-across-hops is
impossible for an entity that resets every hop. Two independent founder-ratified grounds, which
makes the retirement robust against any future softening of either one.

**The pleasant surprise worth recording:** ρ_i was ALREADY right. QM-1 defined it as DI-bit
number density — exactly P-2's count-like magnitude, four months before the ruling existed. Half
the wavefunction was correctly grounded all along; only φ was mis-housed. The re-grounding task
is therefore narrower than feared: find φ's substrate variable (candidate: Sea polarization
orientation in GP state) and derive unitarity from reversibility. Both registered as
OPEN-QM-1-REGROUND obligations, neither smuggled in as done.

**Scope discipline.** QM-2's Born rule has the same disease ("DI-bit interference pattern") and
QM-3..6/SF-6 cite the lineage — but W-7's charter was QM-1. Sweeping the rest inside this patch
would have produced five shallow verdicts instead of one deep one. TODO-2988-A registers the
sweep at P2 with the method (line-cited inventory per paper) already established here.

**Tally discipline note.** First substantive audit conducted fully under the 2986
tally-from-verbatim-only remedy: every conflict row cites its line number in the shipped file;
the proof-chain claims cite line ranges. The remedy costs nothing when the reading is done
anyway, and it made the S-1 finding (phasehop never enters the proof) checkable by anyone with
the file open.

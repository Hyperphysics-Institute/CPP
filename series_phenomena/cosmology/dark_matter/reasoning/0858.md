# Reasoning — Patch 0858 (DM-1 review package, cycle-opening)

STATUS: verbatim, at-patch (Opus, DM 08xx lane, 24 June 2026). The cycle-opening patch for the DM-1 v0.1 panel review, per `templates/review_dispatch_protocol.md` §2. Builds the self-contained package; dispatch ("initiate review protocol") follows.

House structure, matched not invented. I read the sibling EU-1 review package (cosmology flagship that went through the cycle) and matched its §0–§8 skeleton exactly: header (artifact / patch / result / grade / links / aggregation file), §1 cold-start context, §2 claim chain (the spine), §3 deflation guardrails, §4 open marks, §5 triage (top-down, verdict-flipping first), §6 per-reviewer steer ("read your own row"), §7 embedded verify code, §8 response format. Also folded in the protocol's standing details: the IDENTITY instruction (§6 + §8), ChatGPT's disambiguation rider as a §6 row line (not a per-reviewer wrapper — those were retired Patch 1604), and the raw-URL marked "provenance only, likely unreachable; inline authoritative" (the CC-arc lesson that external reviewers cannot fetch this private repo).

Self-containment is the load-bearing requirement (Patch-0656 learning: a reviewer cannot reach SCRIPT-EXECUTED from a filename). So §7 embeds a COMPLETE, runnable script — and I made a deliberate engineering choice: rather than embed the three figure-generating scripts (numpy/scipy/matplotlib, ~385 lines, figure side-effects), I wrote a consolidated STDLIB-ONLY verification that reproduces the load-bearing NUMBERS — the k·λ flatness check, the (4/3) boson factor, the ρ₁ density confrontation (Table 1), and the NFW r₁ inversion (Fig. 3) with a hand-rolled bisection replacing scipy.brentq. This maximizes the chance a reviewer actually runs it (zero external deps) and reaches SCRIPT-EXECUTED on the numbers that matter, while the full figure scripts remain referenced for anyone wanting the plots.

Verified the embedded code before shipping. I ran the standalone script, confirmed it reproduces 0850/0851 exactly (ρ₁ 0.094/0.018, pred/obs 2.8×/3.1×; r₁ 0.32/1.83 kpc; σ/m-for-core 0.18–0.58 / 1.10–2.71), then diffed the COPY embedded in the package against the tested file (byte-identical) and re-ran the embedded copy. A package whose inline code drifts from the tested code would hand reviewers wrong numbers — checked, not assumed.

Triage aimed where it matters, honestly. The top items (T1, T3) are exactly the load-bearing risks Thomas and I had flagged: T1 the velocity-independence claim + the (4/3) boson viscosity factor + the s-wave-limit adequacy; T3 the genuine question of whether the density-test "supports velocity-independence" reading is logically compatible with the core-size "IC 2574 wants σ/m ~ 1–2" tension — i.e. I explicitly invited the panel to check whether the paper is spinning a falsification as a feature OR over-conceding. I did not soften the triage to protect the result; the hostile-pass steer says "assume the positive discriminant is too good to be true and try to break it," aimed at T1/T3. T5 asks the panel to check the calibrated-inputs and conditional-gate honesty labels and the NO-THEO decision.

Bookkeeping: package at `DM-1/review/DM-1_review_package_v0.1.md`; changelog notes the cycle opened; responses will aggregate in `review/reviews-DM-1.md` (created at aggregation, not now). No THEO (positive-discriminant candidate).

Anti-priorities held: matched the EU-1 house structure rather than improvising; made §7 genuinely runnable (stdlib-only) and verified the embedded copy byte-for-byte against a tested run rather than trusting a paste; pointed the triage at the real load-bearing claims and invited falsification of the headline result rather than steering the panel toward a rubber-stamp; carried the IC 2574 tension and the calibrated/conditional labels into the package for scrutiny rather than burying them; kept the reviewer-identity and disambiguation handling per the current protocol (no retired wrappers). Writes confined to dark_matter/ (DM-1/review/ + changelog). The §7 script is itself the verify artifact.

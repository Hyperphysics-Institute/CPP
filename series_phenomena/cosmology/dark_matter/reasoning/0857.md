# Reasoning — Patch 0857 (DM-1 version correction: v1.0 → v0.1 DRAFT)

STATUS: verbatim, at-patch (Opus, DM 08xx lane, 24 June 2026). Corrects a versioning error I made at the 0856 promotion. Prose/label-only, no content change.

The error and the fix. At 0856 I labelled the promoted .tex "Version 1.0 — Positive-Discriminant Candidate." That was wrong by the programme's convention: v1.0 is reserved for the post-review final ship. A freshly-assembled, un-reviewed paper is a pre-1.0 DRAFT that earns v1.0 by satisfying the multi-AI panel. Thomas caught it. I confirmed the house format before fixing rather than guessing: SF-7 sits at "Version 0.8 (DRAFT)", incrementing v0.x through review cycles toward v1.0 at ship — so the correct label is "Version 0.1 (DRAFT)". DM-1 is pre-review, hence v0.1.

Changes: the .tex title-block version line → "Version 0.1 (DRAFT) --- 24 June 2026"; a header-comment note (SF-7 style) recording v0.1 DRAFT / pre-panel-review / promotes to v1.0 only after the review cycle; the changelog relabelled v0.1 with the v0.x→v1.0 promotion convention stated explicitly so it is not mis-set again; the markdown draft §11 item 8 corrected from a bare [DONE] to "[DONE] … v0.1 DRAFT, pre-review." Recompiled clean (two-pass, 0 undefined, 9 pp) — the label change touches nothing structural.

Why it matters beyond cosmetics: a v1.0 label asserts the panel has signed off, which is false here. Leaving it would have mis-represented the artifact's status to anyone reading the repo — exactly the kind of unearned-confidence claim the programme's honesty discipline exists to prevent. The grade content was already honest (positive discriminant, not full identification); the version label now matches that honesty.

Anti-priorities held: owned the mistake and fixed it cleanly rather than defending the label; verified the house format (SF-7) before applying rather than inventing one; documented the promotion convention in the changelog so the next assembly doesn't repeat the error; no content changed, so the panel reviews the same paper, correctly labelled. Writes confined to dark_matter/ (DM-1 .tex + changelog + draft item-8). No verify script (label-only). Next: the review package (cycle-opening patch) against this v0.1.

# Reasoning — Patch 2391 (SS43-Q1 executed: the fine S_c* wall)

STATUS: verbatim, at-patch (Opus, DM lane, 9 July 2026, DM-WARM-2389 launch;
founder's go = the opening question "Shall SS43-Q1 proceed there?").

Executed exactly as pre-registered at 2389 §34.6 — the point of the warm-launch
pattern is that nothing here needed deciding, only doing. Design choices inside
the contract's degrees of freedom, recorded:

1. **Shared sample map per (member, sign).** Summed counts are LINEAR in f·ρ, so
   worst(ρ) = ρ/ρ*_exact identically; one ρ*(S_c) sample serves both ρ targets.
   The two bisections per (member, sign) share a sample dictionary, and the
   global cache dedups across members at coincident S_c. Result: 115 fresh
   pipeline calls against the ~170 budget.
2. **The wall is reported as the ALIVE end of the ≤0.001 bracket** (with the
   dead end alongside) — conservative in the direction that matters: a derived
   S_c inside [0.012, S_c*_alive] is guaranteed-alive at the tabulated cell;
   the 0.001-wide strip is honestly indeterminate.
3. **V3 implemented as ρ* monotone non-increasing in S_c** — algebraically
   equivalent to the registered "worst-ratio monotone non-decreasing" via the
   linearity above, and checkable on the shared sample map directly. 12 paths,
   112 points, monotone everywhere: the physical assumption bisection rests on
   (stronger coupling ⇒ more predicted counts through the Numerov pipeline,
   despite non-perturbative phase-shift structure) held at every sampled point.
   Pre-registering this was right — it is NOT trivially true for a
   phase-shift-level calculation, and the next instance checked it instead of
   inheriting it.
4. **Two floor-edge facts carried without smoothing:** F(attr, ρ=0.3) is
   DEAD-AT-FLOOR (floor ρ* = 0.2969 < 0.3 — F was a (d)-branch ρ≥0.2 member in
   2383, never claimed 0.3); E(attr, ρ=0.3)'s wall is AT the floor (S_c* =
   0.0120). Both are reported as-is in the table; the J12′-a edge caution
   attaches to the E cell first.

What the wall bought: the §34.3 "×2.9 gap" was a single floor-referenced number;
the fine wall grades C1 into a per-cell target — ×1.59 (C, rep, ρ=0.2) to ×2.92
(E, attr, ρ=0.3). Q2's derived ring suppression now has a mechanical grading: 
≥×2.92 lands the whole table, ≥×1.59 keeps the family alive at its loosest cell,
above 0.0221 in derived S_c the family gate-dies fully derived (kill carried from
§34.4 unchanged). No verdict moved; the wall is Q2's input.

Scope note: V1 exit codes are read from the session driver's sequential run when
present (/tmp marker), else the script runs the three subprocesses itself — the
committed artifact is standalone-reproducible either way.

Founder's desk: apply 2390 + 2391; SS43-Q2 (ring multipole pass) is next and its
target is now a table, not a slogan. Block EMPTY.

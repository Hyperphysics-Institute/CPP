# Reasoning capture — Patch 2756 (B-CHECK-80B execution → J1)

Verbatim reasoning from the executing session, 2026-07-22. Author: Opus (PD-006).

## Execution

Gate v2 ran first and PASSED (five seed-1 moves, relative ~1e-15 vs threshold 1e-8) —
the one-line fix (`mn[i]=False`) makes the A increment and B's from-scratch totals compute
the same Hamiltonian. A-FIX-UND (seed 20260796) and A-FIX-DRV (seed 20260797) completed in
~2 min each (1k+16k sweeps, 1600 samples). B-DRV (seed 20260795, 1k+12k) is ~0.2 s/sweep;
a background `nohup` attempt died with its shell and contributed nothing (log empty, zero
progress beyond the checkpoint), so the run proceeded in checkpointed foreground chunks
(every 100 sweeps; RNG state + configuration carried; E_cur recomputed on resume). Chunk
boundaries: 1400, 2500, 3600, 4700, 5800, 6900, 8000, 9100, 10200, 11300, 13000 sweeps.
No code, parameter, or seed changed at any resume, and no physics quantity was inspected
before the frozen analyzer ran on completed series.

## Verdict

Frozen analyzer output: tilt_fix = −1.549 ± 0.235; AFIX-DRV = −1.544 ± 0.079;
B-DRV = −1.424 ± 0.137; r_fix = 0.997 ± 0.160. |r_fix − 1| = 0.02σ; AFIX-DRV vs B-DRV
0.75σ; B vs tilt_fix 0.46σ. Fork J1 conditions both satisfied with enormous margin; J2/J3
unreachable. **J1: the 2714 self-pair defect IS the mechanism of the drive-enhancement
anomaly.** The contamination was two-sided (tilt suppressed −1.185 vs −1.549; driven
inflated −1.922 vs −1.544), compounding to r_legacy = 1.622. Linear response holds
(−1.544 vs prediction −1.518, 0.3σ). The X3-LONG B2 anomaly, the pooled 5.6σ plateau, and
the X7-NSCAN onset window are artifacts; classifications and the frozen remedy
(REACH-AUDIT-2714 + re-verification charter) are in the record.

## Incident disclosure: duplicate-execution collision

While preparing to write the record, a complete `s4x_bcheck80b_record.md` was found already
present in the working tree (untracked), with file birth time *preceding* the visible
completion of B-DRV, yet containing the final statistics digit-for-digit. Investigation
(clock sanity, `stat` on all artifacts, grep of the frozen script for any `.md` writer —
there is none, process table, `git diff HEAD` — clean) established the mundane cause: the
session's execution turn was run twice by the infrastructure (a retry), and the first
attempt — which completed the final chunk, ran the analyzer, wrote the record, and created
`data/bcheck80b/` (directory birth 12:42) — died before committing. The second attempt
re-ran the deterministic pipeline from the shared checkpoint, reproduced identical numbers
(identical seeds → identical trajectories), overwrote the archives (file mtimes 12:48),
and collided with the first attempt's record file.

Treatment: the pre-existing record was verified line-by-line against the independently
re-run analyzer output (exact match on every quantity) and adopted as the record of
Patch 2756. The duplicate execution is physics-neutral — it constitutes an accidental
exact-replication check of the full pipeline — but is disclosed here because unexplained
artifacts in the working tree must never be silently absorbed. Standing lesson retained:
background processes do not survive tool-call boundaries; checkpointed foreground chunks
are the pattern of record for long runs.

## Process notes

- Seeds 20260795/96/97 consumed; seed-1 used only for discarded gate/benchmark chains.
- AUTOMATON-1 not triggered (J2 branch); charter retained dormant.
- Panel dispatch continues to hold per the economy rule: one bundle (S4-X arc +
  DRIVE-AUDIT-1 + corrections ledger + audit outcome) after REACH-AUDIT-2714 resolves.
- Single next act: freeze the REACH-AUDIT-2714 charter.

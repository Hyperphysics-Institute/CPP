# Reasoning — Patch 2385 (round 2384 returns: recording, triage, adjudication prep)

STATUS: verbatim, at-patch (Opus, DM lane, 9 July 2026). Founder relayed the round's
collection: two substantive returns (attached document = GPT window per relay; inline
paste = Gemini window per relay), Grok disconnected, Copilot unable ("rules for
retrieval" — BOTH delivery paths failed for that seat: no return on pasted content, raw
links blocked by its own rules; the 2378 A2 fix is recorded as insufficient for this
seat), DeepSeek line empty at collection (possibly pending — adjudication item D1).

## 1. The label collision, read carefully

Both returns open with "R1". Before treating this as a fourth seat-identity anomaly I
compared the texts: different structure, different phrasing, different findings
(Return A's WOUND/SCRATCH framing and retention argument on R2; Return B's
commendation-style findings and falsifiability argument). These are two distinct
reviews that both defaulted to the brief's example label because the paste evidently
carried no per-seat preamble. Documented as LABEL COLLISION, not duplication; the
protocol note (assign the label in a one-line paste preamble, outside the shared block)
goes on the record as a recommendation — no template edit without its own decision.

## 2. Tally and what it licenses

V1–V5: 2× VERIFIED each; zero REFUTED; zero INDETERMINATE — no halting rule triggered.
R1: 2× RATIFY, no objections. R2: convergent (add a new exit condition at the derived
mass; Return A explicit that the 2.8 GeV original stays for provenance — which is what
the add-only rule requires anyway). Both returns disclose audited-not-reran; recorded
prominently: NO seat executed code this round, so the executed verification remains the
in-repo battery chain. That is a coverage fact the founder should weigh at D1/D2, not a
defect in the returns.

## 3. Findings triage — the two actionable items, executed

F-A1 (the round's best catch): the arc's load-bearing assumption has MOVED — from the
cascade algebra to the constancy of κ and E_bond. Return A is right, and the honest
response is to bind it forward: the SUBSTRATE-CONSTANCY THEOREM DEMAND is appended
(dated, additive) to the Q3c inherited target. It lands in the same SSV derivation that
owes the absolute pair, so it adds no new road — it sharpens the existing one.
F-A2: ARGUMENT-LEVEL tag line appended (dated, additive) to the §Q3b-2c sign paragraph.
Both edits are additions; no registered sentence was modified.

## 4. What this patch does NOT do

It does not close the round (D1 is the founder's), does not adjudicate the tally (D2),
does not declare R1 ratified (D3 — the votes are recorded; the flag discharges on the
founder's confirmation), and does not touch Clause 1 (D4 — the (d′) draft awaits
attestation; add-only respected; blanks left for the founder's date and mark).

## 5. Worker recommendation (for D1)

One retry window for DeepSeek and Grok is cheap and preserves coverage optics for any
future registry-grade move; if nothing lands, close at 2/5 — the round's halting logic
is refutation-driven, both landed returns are convergent VERIFIED, and the arc stays
candidate-grade until Q3c pays rent regardless. Registration of the derived family as
the successor branch can be held to fuller coverage at the founder's discretion without
blocking Q3c's opening.

## 6. Founder's desk

D1 close-or-retry; D2 adjudication on the tally; D3 R1 confirmation; D4 (d′)
attestation. Block EMPTY otherwise.

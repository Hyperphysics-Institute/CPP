# Reasoning fragment — Patch 2750 (X7-NSCAN record + X7-NSCAN-EXT prereg freeze)

Verbatim, at-patch.

Execution: all six ensembles ran at full frozen length from the frozen
seeds under checkpointed chunked foreground execution (the container
kills background processes unpredictably; one phantom driver from the
first launch attempt persisted and advanced two checkpoints between
foreground calls — harmless because the chain state including the RNG
lives wholly in the checkpoint, so the chain is identical regardless of
executing process; accounting verified: samples = (sweeps − eq)/10
exactly at every checkpoint; phantom killed before the final chunks).

Reading the frozen analysis: G4 fired — {128 ENHANCED, 216
INTERMEDIATE, 320 ENHANCED}, no UNENHANCED size, non-monotonic point
estimates. The scientifically decisive content is not the fork label
but the location: the onset the scan was built to find sits BELOW its
smallest size — in (64, 128]. The 216 dip is 1.1–1.2σ against its
neighbors (not structure); 216 is itself 2.16σ above unity; the
64→128 jump is 2.04σ. The five-point pattern reads
rise-then-plateau, but G4's frozen clause bars enacting that reading
until the window (64, 128) is sampled — which is exactly right, since
a 2σ jump between two points is not a shape.

EXT design decisions: sizes {80, 96, 112} — even splits, roughly
uniform in the window; same protocol in every respect; seeds continue
the frozen sequence (789–794, with 795 reserved for the H3 replicate
at 128). The frozen H-fork covers: bracket found (H1 → B-CHECK at
smallest enhanced N — cheapest exactly there since B costs ~N² per
move), all-enhanced (H2 → onset in (64, 80]), all-unenhanced (H3 →
either a very sharp transition in (112, 128] or the 128 point is a
fluctuation — a fresh-seed replicate at 128 becomes the instrument
before any reading; this guards against building on a single 2σ
point), other (H4 → gradual-rise, (a)-compatible, automaton
arbitrates). The classification bands are unchanged from 2749 — no
post-hoc band motion.

Code change for EXT is the seed-table addition plus analysis
plumbing (size-list parameter, analyze_ext with the frozen H-fork);
the Metropolis update lines are diff-verified unchanged against the
HEAD version. Data archived under data/x7nscan/. Standing physics
untouched; 79.5% not in scope.

# Reasoning fragment — Patch 2973 (B-1: the mechanism→operator bridge, W-1 / C-1)

## Why the bridge takes this shape

The Q3(b) gap, read carefully across the four returns, is not "prove more cancellation" — it is "quantify over drives." T-1's theorem quantifies over the constant-v family; S1's replacement statement quantifies over every admissible small drive. The natural object that converts one quantifier into the other is the memory kernel: once the linear response is a causal convolution (L-1, from M1's fixed point + Moment-stepped causality), the constant-v family probes exactly ONE number of the kernel — its DC weight γ̂(0) — and T-1 pins that number to zero exactly (L-2). Everything else the panel demanded then follows from two structural facts: finite support (T-3 §6, L-4) makes the moment expansion legitimate, and a zero-DC kernel's real part necessarily starts at ω² (L-3) — so "no linear-order dissipation in any channel" is a two-line consequence of the mechanism theorems, not an additional mechanism claim. That is what a bridge should be: the operator statement made equivalent to already-graded mechanism statements plus bookkeeping, with no new physics smuggled.

## The acceleration channel

DeepSeek's observation (T-1 is zeroth-order in acceleration) shaped §4(b). The answer the kernel gives: at first order in ω the kernel is purely imaginary — an inertial dressing ΔM, not a dissipation — so the linear-in-a response is exactly (M_bare+ΔM)v̇, and dissipation attached to acceleration is O(ω²), which is the radiative order T-2 already carries as E_rad ≥ 0 → 0 adiabatically. The bridge doesn't abolish radiation (it must not — accelerating charges radiate); it locates it at the order where T-2 put it, below linear.

## What is deliberately NOT claimed

Passivity (Re γ̂ ≥ 0, i.e., the sign of the second moment) is not derivable from cycle-lemma zero + support — a zero-integral kernel can have either sign of ∫τ²γ. Disclosed in §4(c) and handed to W-4 as a measurable consistency check alongside the standing tail falsifier. Also not claimed: any grade movement. B-1 discharges C-1 *structurally*; whether that satisfies the 2874 statement at operator grade is the next CONV round's Q, per the discipline that the worker does not self-adjudicate against the panel's own text (the exact principle the panel just enforced at PR2).

## The toy corrected the worker (again)

First run: 7/9. Both failures were my tolerances, not the physics — I compared numerics against analytic predictions truncated at O(ω⁴) (CHECK 5) and O(ω²) (CHECK 9) with tolerances of 1e-6/1e-8 relative, tighter than the truncations' own error (~2.6e-4 and ~1e-6 at the frequencies used). The quantities agreed at truncation-consistent level from the start (slope 1.99999; dM routes matching to 1e-6 relative). Fixed by setting tolerances to the truncation order, with the first-run failure disclosed in-code. Public correction per lane practice; no result changed.

## Sign bookkeeping

One genuine subtlety in CHECK 7/9: with γ̂(ω) ≈ −iωm1, F_mem(ω) = −γ̂v = +iωm1·v → F_pred = m1·v̇ in time domain, so the dressing is ΔM = −m1 in the −Mv̇ convention. The first draft of the script had a sign vacillation in comments (visible in the committed code as the resolved derivation comment); the convolution test settled it empirically and the two independent dM extraction routes now agree, which is what CHECK 9 exists to force.

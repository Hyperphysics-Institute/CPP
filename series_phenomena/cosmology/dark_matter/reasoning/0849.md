# Reasoning — Patch 0849 (DM-1 §4 figure: residual V(r) + saturation-vs-confinement)

STATUS: verbatim, at-patch (Opus, DM 08xx lane, 24 June 2026). Figure + verify-script patch under the 0847 ship charter; opens the substantive DM-1 identification work. No new physics claim — visualizes results already [FILLED] in §4.

Charter item 2 (the §4 V(r) / saturation-vs-confinement figure), taken as the first work patch of the identification campaign. The §4 prose already carried all the numbers (r_c ≈ 1.0 fm, λ ≈ 1.3 fm, V₀ = f·E_qDP ≈ 53 MeV, glueball-avoidance via r_c > r_qDP); the [TO FILL] was only the figure, so my job was to render those faithfully and make the script double as a verify check — not to introduce anything new.

Potential form. I modeled the two-gluon color van der Waals residual as a screened Lennard-Jones, V(r) = V₀[(r_c/r)¹² − 2(r_c/r)⁶]·exp(−(r−r_c)/λ), because it reproduces all three quoted scales with no free knobs: r→0 gives the hard core, r=r_c gives exactly −V₀ (the well depth), and the large-r tail is −2V₀(r_c/r)⁶·exp(−(r−r_c)/λ) — a van der Waals (1/r⁶) envelope under Yukawa screening at range λ, which is what "two-gluon van der Waals, screened" should look like. The numerical minimum (−53.2 MeV at 0.99 fm) lands on V₀ ≈ 53 MeV at r_c ≈ 1.0 fm, so the form is consistent with the prose rather than a new fit. f = V₀/E_qDP = 0.201 reproduces the §4 f ≈ 0.2.

The honest call — r_qDP. The glueball-avoidance argument in §4 is stated qualitatively: "any coat r_c > r_qDP puts the saturation density below the confinement density." There is no derived r_qDP number in the draft. So I did NOT invent one and present it as derived. Panel (b) plots n(d) = 1/d³ and marks saturation onset at d ≈ r_c and confinement onset at d ≈ r_qDP, using r_qDP ≈ 0.5 fm tagged [est] = "representative; the argument needs only r_qDP < r_c." Both the script docstring, the printed verify block, and the §4 figure caption say the conclusion holds for ANY r_qDP < r_c and that 0.5 fm is representative, not a result. The ratio shown (ρ_sat/ρ_conf ≈ 0.12) is therefore illustrative of the inequality's structure, not a precise prediction.

Verify-script discipline. The script prints inputs (provenance-tagged [s4]/[sf3]/[est]), the numerical well depth + location, and the two densities + their ratio, with an explicit boolean check that depth/location match §4 (|V_min+V₀|<6 MeV and |r_min−r_c|<0.4 fm → True). So the figure is reproducible and the numbers are auditable from one run — the patch bundles the script (source of truth) and the PNG (the committed artifact §4 references).

Bookkeeping: §4 [TO FILL] → [FILLED] with the figure reference and a one-sentence statement of each panel; §11 item 3 (Figures) flipped [TO FILL] → [PARTIAL] (V(r)/saturation done as Fig. 1; the §6 core-radius-vs-σ/m panel still open).

Anti-priorities held: did not introduce a new result or a derived r_qDP (representative value clearly tagged, inequality-only conclusion); did not overstate the potential model (screened-LJ chosen because it matches the three stated scales, flagged as a model of the §4 residual, not a first-principles derivation — that lives in SF-5 / the §4 constituent cites); writes confined to dark_matter/ (scripts/, figures/, the draft, reasoning/) — collision-safe vs the 0900/1000/1100/2100 windows. Verify script present (it is the figure generator).

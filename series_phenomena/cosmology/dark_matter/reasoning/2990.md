# Reasoning fragment — Patch 2990: two reviewer items, one shared structure

**The discovery worth the patch: L-4 and L-6 are the same property.** GPT asked for proof that
perturbations STAY in the linear-response regime; the proof needed a contraction rate κ < 1 for
deviations from the instantaneous fixed point. The temptation was to assume κ (call it
"attraction, per M1") and move on. Instead, tracing where contraction actually comes from
landed on M1's dichotomy: deviation content is unanchored, gets no sustaining imprint, exits at
c within τ_b, never returns. That is EXACTLY the structure behind L-4's finite kernel support.
Bounded memory and contraction are one property viewed from two lemmas — which means the MEAS-2
no-tail leg tests both at once. The cross-falsifier link is registered in the lemma text, not
left as private knowledge: a measured tail should be scored against L-4 AND L-6, and CONV-013's
reviewers should see that concentration of risk plainly.

**DeepSeek's amend was RIGHT in its stated scope — say so first.** The v1.1 lemma cited "the
M1/Version-B structure" for closure, compressing a stationarity statement (which licenses only
statistical closure — DeepSeek's exact point) and a finer structural dichotomy (which delivers
pointwise closure) into one phrase. The honest amendment concedes the compression before
defending the claim: if closure rested on stationarity alone, pointwise would be unproven.
It doesn't — anchored content is the SOURCE'S imprint by construction (never the DP's
persistent state), unanchored departs at c per-DP (not per-ensemble-mean) — but the concession
is what makes the defense readable as an argument rather than a reflex.

**The statistical fallback was added even though the pointwise basis stands.** Why weaken a
claim you just defended? Because the fallback is theorem-preserving (zero-in-mean, O(1/√N),
no systematic drag) and it makes the lemma's failure mode PRECISE: the only way T-1's
conclusion degrades is a nonzero MEAN residual — which is by definition DP-level memory —
which is what the campaign running on the founder's desktop right now measures. DeepSeek's
scope item turned out to be a pointer at the experiment already in flight. Writing that
connection into the theorem converts a reviewer's reservation into a registered falsifier
with a live instrument on it.

**Toy discipline.** The verify script's two negative controls mirror the two load-bearing
conditions (κ < 1; slow variation). Each admissibility condition that can't fail in the toy
would be decoration; both fail on demand, so both are tested. Linearity exponent came out
2.000 — the regime holds along the trajectory, which was the panel's word: "throughout."

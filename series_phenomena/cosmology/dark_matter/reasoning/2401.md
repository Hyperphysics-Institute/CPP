# Reasoning — Patch 2401 (SS43-Q4b executed: the per-unit vertex class DERIVED — Class V-t, from the same substrate mechanics as Q4a)

STATUS: verbatim, at-patch (Opus, DM lane, 10 July 2026, DM-WARM-2400 warm
launch).

## 1. Founder block (§10)

- **Launch go (verbatim, this session):** "Go on Q4b." followed by "Please
  proceed with Q4b." — recorded here at-patch per the 2026-07-10 handover's
  mandate (no go was on record for Q4b at close; the launch completed on this
  verbatim, this session, per the 2397/2398 pattern).
- Chain-verify at bootup: 2400 → 2399 → 2398 → 2397 → 2396 green (the depth-1
  clone was deepened to expose the chain; HEAD = 2400 as the handover
  expected). No incident this session.

## 2. Battery record

- **V1 (scratch copy, session open):** 2395 battery ALL PASS in a tempfile
  copy at `/tmp/scratch_q4b/` (181.4 s; transitive 2393 → 2391 →
  2381/2382/2383; V5 leg inside it: 605 cache keys byte-identical; committed
  artifacts untouched — the run never touched the working tree).
- **V2 PASS-GATE (in `code/2401_ss43_q4b_vertex_class.py`, passed BEFORE
  grading):** (a) the rod's D5-A′ phenomenology reproduced UNDER THE DERIVED
  CLASS — open-chain composite telescopes EXACTLY to the end form
  (max |diff| = 0.0 at machine precision), source side power-0 (log-log slope
  0.1055, matching the 2393 record 0.106), total = FIRST power at the ruling
  scale R_N/R_s = 0.0354; (b) the two Q2 exact identities reproduced as
  CONSEQUENCES of the derived composition — closed-loop telescoping = 0
  (N = 4–8, 12 random probes each, machine precision), orientation
  orthogonality t̂·x̂ = 0 (8.4e-17), plus the node-form crosschecks (closed
  ring node sum 8.0e-12; open-chain node-vs-edge 0.62% at midpoint rule).
- **V3 5/5:** global convention flip (sum → −sum exactly); local single-unit
  flip is PHYSICAL (breaks telescoping by O(per-edge), 1.96e-2 vs 1.96e-2);
  chain reversal flips the composite sign exactly (the vector character,
  checked); normalization-independence of the class verdict (arbitrary c = 3.7
  leaves the closed zero at 4.2e-17 and scales the end form linearly to 1e-9);
  R_s = r_c/χ = 25.416408 fm reproduces the 1879 engine RS to machine
  precision (the Q4a CLOSED chain is self-consistent).
- **V4:** zero tunables. Inputs: φ; χ = φ⁻³/6 (METH-CHIR-CONT-1); r_c = 1 fm
  (SF-5/J2); R_s = r_c/χ (Q4a CLOSED); R_N = 0.9 fm (1880); d = 1.15 fm (J8).
  The D5-A′ data points enter as the GATE'S TARGETS, not as derivation
  inputs. 0865 held: no torsional lock admitted, no transverse component, no
  new transport datum.
- **V5:** no cache opened; sole output `code/2401_results.json`.

## 3. The derivation, owned in full

The contract demands the class be DERIVED from the same substrate mechanics
that fixed the Q4a response order — not read off D5-A′'s ruling language. The
Q4a mechanics was registration-level reasoning on the METH-CHIR-CONT-1 datum:
what LEVEL is χ registered at (amplitude), and what does hermiticity force
(equal legs). Q4b asks the same datum a second question: what SYMMETRY content
does the registered vertex carry, and what does the registered bond structure
do with it.

**L1 (the Q4a identification, §34.13 CLOSED input).** The per-unit
residual-channel vertex is the METH-CHIR-CONT-1 Wigner–Eckart matrix element
M = ±χ·(d_Γ/V_cage) — a SIGNED, amplitude-level object. The datum's validity
condition carries the ± explicitly: the magnitude is universal (χ/6 per the
normalization), the SIGN is defined relative to the unit's local ℤ₂ pairing
convention — Ĉ lives in a ζ-ODD 1D irrep and M connects the ζ-EVEN to the
ζ-ODD doublet component, so relabeling the convention flips M. For a unit in
ISOLATION only |M|² enters (the Q4a bubble); the sign is unphysical alone.

**L2 (what makes relative signs physical).** In a composite, relative signs
between units carry physics if and only if a REGISTERED structure refers one
unit's pairing convention to its neighbor's. The rung bond (2381 rung-bond
primitives) is exactly that structure: bonding IS a pairing relation between
adjacent units' DPs, and it transports the ℤ₂ convention along the chain. The
chain/rung axis is the rod's ONLY registered coherence axis; no torsional
lock is registered, so there is no second transport datum — 0865 forbids
admitting one.

**L3 (chain-reversal parity fixes the tensor character — the step that
actually derives the class).** A unit's transported sign is defined relative
to the ORDERING of its bonds along the chain: referral through the left bond
vs the right bond differ by the transport. Chain reversal (t̂ → −t̂)
exchanges the bond roles ⟹ flips the convention-referred sign ⟹ the
coherently sourced per-unit amplitude is ODD under chain reversal. A quantity
odd under t̂ → −t̂, supported on the one registered axis, IS a chain-axis
vector component: v_k = M·t̂_k. Its leading coupling to the channel field is
the directional derivative t̂·∇ₓY, whose discrete form on the chain is the
per-edge difference — the 2393 machinery, now derived rather than posited.

**THEOREM (the class):** the per-unit coherent vertex in the colour-residual
channel is **Class V-t**. Composition is the rung-transported signed sum (the
discrete line integral). Closed loop: interior contributions telescope to
zero identically. Open chain: the sum telescopes to the ENDS.

**COROLLARY (Class S excluded by registration — the same logic that excluded
√χ at Q4a).** Class S needs the composite's per-unit source to be the
UNSIGNED |M| — chain-reversal-EVEN, convention-independent — which requires
either (i) registration of the per-unit source at PROBABILITY level (where
|M|² lives), contradicting the amplitude-level registration (Q4a L1, CLOSED),
or (ii) a ζ-EVEN vertex operator, contradicting the ζ-ODD irrep placement of
Ĉ in the registered datum. Not constructible. The third composition —
uncorrelated signs, incoherent RMS ~ √N — requires NO transport, contradicting
the registered bonded structure itself (2381: the rod IS the bonded object;
D5-A′'s "coherent cage" is this structure, now given mechanism).

**Discriminant discharge.** Under the derived class the rod's D5-A′ power-0
sourcing is END-sourced O(N⁰) — the 2393 V2 adjacency record reproduced as a
CONSEQUENCE of the derivation, not an input to it. The trichotomy is
numerically resolved in the instrument: V-t closed = 0 at machine precision;
S grows monotonically (S(8)/S(4) = 1.76); incoherent RMS sits between,
√N-family — three separated behaviors, one constructible.

**1858 reconciliation (carried verbatim in substance from 2393).** The
telescoping is a coherence projection of the VERTEX, not sign-cancellation of
the FORCE. The force between nonzero-net-vertex bodies is second-order in the
net amplitude — attractive always; E_qq unipolarity stands untouched.

**Why this is a derivation and not a selection.** 2393 recorded honestly that
the registered primitives THEN AVAILABLE (the two D5-A′ data points + the
composition-law candidates) underdetermined the class, and the only
discriminating remark available was a reading of the ruling's mechanism
language — explicitly flagged as not-a-derivation. What changed between 2393
and now is the Q4a landing: the identification of the per-unit vertex WITH
the METH-CHIR-CONT-1 datum (§34.13 L1) became a CLOSED input. That datum
carries symmetry content (the ζ-ODD placement, the explicit ±) that the 2393
candidates did not have access to. The class now follows from registered
structure alone; D5-A′'s language is not consumed anywhere in L1–L3 — it is
reproduced at the gate.

## 4. Two instrument missteps, caught by the gate and owned

Both were in the VERIFY INSTRUMENT, not the derivation; both were caught by
the V2 gate failing loudly, exactly as the gate is designed to do; the
physics moved zero.

- **(i) Check-design error (V2b-iii, first run).** The closed-ring node-form
  crosscheck compared node sum to edge sum by RELATIVE residual — but on the
  closed symmetric ring BOTH vanish (the node form by pairwise reflection
  cancellation), so the check divided ~0 by ~0 and reported residual 1.0.
  Fix: closed-ring node form checked against ZERO directly (lands 8.0e-12);
  node/edge consistency moved to the OPEN chain where both are nonzero.
- **(ii) Sign-convention slip (V2b-iv, second run).** The open-chain node
  form first used the PROBE gradient where the edge difference is the
  derivative along the SOURCE displacement (∇ₓY = −∇ₚY) — the forms came out
  opposite-signed (rel diff 2.19, i.e. node ≈ −edge). Confirmed by a
  one-off diagnostic before editing; fixed to the source gradient at edge
  MIDPOINTS (the correct discretization pair of the edge form), landing at
  0.62% — inside the O(d²) midpoint expectation.

Neither touched L1–L3, the identities, or the grading; both are recorded
because the gate catching them is the discipline working.

## 5. Grading discussion (why V-t LANDS and not (c))

The pre-registered branches (§34.12): (a) any stage kill — Q4b's kill is
derived class = S; NOT FIRED (Class S is excluded by registration, the
opposite of landing). (b)-track continues — Q4b derives V-t. (c)
partial/conditional — the honest question is whether L2/L3 lean on anything
unregistered. Audit: L1 is a CLOSED input; L2's transport stands on the 2381
rung-bond primitives as registered structure (the formal PCD-dynamics
derivation of the transport rule is the SAME FP-side debt family as
PIN-Q4a-1's linear-response bridge — standing programme footing, named, not
new freedom); L3 is pure symmetry bookkeeping on L1 + L2 (parity of a
convention-referred sign under bond-role exchange). No coefficient enters the
class verdict at all — the verdict is structural, and V3-iv confirms it is
normalization-independent. No window edge exists to straddle. **Q4b LANDS:
derived class = V-t, branch-(b) track.**

Named pins:
- **PIN-Q4b-1:** L2's convention transport stands on the 2381 rung-bond
  primitives as registered; the substrate-dynamics (PCD-level) derivation of
  the transport rule is the standing FP-side debt (same family as PIN-Q4a-1)
  — named conditionality, not new freedom.
- **PIN-Q4b-2:** the derivation fixes the CLASS and the composition law, not
  the post-closure TRUE residual magnitude — that is Q4c's charter,
  deliberately untouched here (scope guard).
- **PIN-Q4b-3:** the per-edge normalization inherits the PIN-Q4a-2 O(1)
  channel-geometry freedom; the class verdict is independent of it (V3-iv).

**Consequences that fire on this landing (per contract):** the (c) HOLD on
the vertex class is DISCHARGED BY DERIVATION (its stated terminus); the
Q2-identity theorem registration UNBLOCKS (the §34.9/2394 deliberate
deferral, discharged) — registration itself is QUEUED as a named
next-session action, not executed in passing here; Q4c (the fork-resolver)
unblocks and opens next session on the founder's verbatim go, graded against
the CLOSED 2395 window with the generalized ladder re-confrontation
V2-re-armed. Glossary promotion of S/V-t stays gated on survival (survival
is not established until Q4c).

## 6. Scope guard + queue

Held this session: no Q4c pre-work; no theorem registration executed (only
unblocked and queued); no DAMIC re-pin; no LZ 9-GeV pin; R2 lane untouched;
no glossary promotion. Queue: P1 Q4c on the founder's go next session
(keyword DM-WARM-2401), grading at §34.15 against the CLOSED 2395 window;
P1-adjacent the Q2-identity theorem registration (may bundle with the Q4c
session or land as its own surgical patch, founder's preference); P2 R2
release lane unchanged, awaiting the founder's verbatim release decision.

Artifacts this patch: `code/2401_ss43_q4b_vertex_class.py`,
`code/2401_results.json`, campaign file §34.14, `frontier_sectors/SS.md`
2401 block, this fragment.

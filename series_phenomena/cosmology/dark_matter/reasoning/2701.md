# reasoning/2701.md — ALPHA-1 S1/S2 derivation (verbatim, at-patch)

STATUS: verbatim, captured at Patch 2701, 21 July 2026.

The derivation was short because the founder's answer had already done
the hard part: once the medium is a statistically continuous two-species
occupation field responding to the total potential, the linear-response
chain is forced — Debye form, local kernel, and the discretization of
the resulting integral equation lands EXACTLY on M = I + alpha G with
alpha = kappa^2/(4 pi n). I derived the thing S1c had assumed, and the
panel's demanded bridge (uniform mode samples the continuum
susceptibility) is satisfied not by a clever argument but by the
mechanism itself: the time-averaged medium is continuous, so there is
nothing else for the uniform mode to sample.

The satisfying kill was my own alpha'. Two independent cuts: the
founder put the self-exclusion at the GP address (point-like), so the
r<a core the site sum excludes is full of responding medium; and even
granting the discretization framing, the correct excluded region is the
Wigner-Seitz cell at 30.3%, not the nearest-neighbor ball at 59.4% —
I over-excluded at L4 even by my own logic, and the closed form
1-(1+kappa R)e^{-kappa R} makes the audit one line. I recommended the
demotion to the panel rather than enacting it; the rider is theirs.

The honest gap is theta. The reconciliation with kappa = 2/d_DP defines
the agitation scale (2 sqrt2 pi q^2/a, Coulomb-order at DP spacing) —
plausible, but a definition is not a derivation, and I said so with
CHI-INCOMPLETE on that component. The pleasant surprise is Gamma =
1/(sqrt2 pi): the implied Sea sits at moderate coupling, which is
precisely the regime where the kernel is local-plus-real-structure —
the D1 class CHI-STRUCTURED was not available to intuition before the
number existed. And that number is what makes S3 interesting instead
of a formality.

# Reasoning — Patch 2403 (SS43-Q4c executed: the residual coupling scale DERIVED — branch (c), one named residue: the LZ local-value pin at 9.86 GeV, founder-gated)

STATUS: verbatim, at-patch (Opus, DM lane, 10 July 2026; same session as
Patches 2401–2402).

## 1. Founder block (§10)

- **Launch go (verbatim, this session):** "Please proceed as per
  recommendation." — given on the stated recommendation whose Priority-1
  content was Q4c (the 2393/2394/2397 verbatim-go pattern). Recorded here
  at-patch.
- **Pass-gate before opening:** Patch 2401 verified at origin/main
  (edf117f) before Q4c work began — §34.12 binds each stage's grading to
  land in the repo before the next stage opens; the founder applied and
  pushed 2401 mid-session (the 2399 mid-session-apply precedent).
- **Carried triggers fired (catalog-first, Patch 2402):** the 2400
  handover's named METH-L1 trigger (the Q4a registration-level order
  counting, reused here for the residual-order derivation) and the
  2396/2398 carried METH-L2 candidates (strict-point gate;
  differential-vs-total σ — both reused by this re-confrontation).
  METH-L1-013, METH-L2-012, METH-L2-013 registered at Patch 2402 BEFORE
  this derivation patch, per the binding "register catalog-first at that
  session" instruction.

## 2. Battery record

- **V1:** the 2395 battery ALL PASS in the tempfile scratch copy at session
  open (181.4 s; transitive 2393 → 2391 → 2381/2382/2383; committed
  artifacts untouched) — run before the 2401 patch, covering the session.
- **V2 RE-ARMED (the Q4c contract clause), passed before any grading:**
  (a) ℓ_v(N = 4–8) reproduces the committed 2393 values, worst rel 0.0
  (byte-exact — same seeds, same grid, verbatim-transplanted machinery);
  (b) the committed 2395 envelope-end LZ σ_n reproduced per N, worst rel
  0.0; (c) the committed 2395 envelope-end DAMIC events reproduced at N = 6
  (6.2148e7, rel 0.0) — after one instrument misstep caught by this very
  gate (§4); (d) the rod-grid decisive rows fresh (S_c = 0.035: XQC
  nviol = 0 ALIVE; S_c = 1.3e-3: LZ σ_n = 6.1e-36 ≫ strict, the dead-LZ
  row); (e) the exact identities fresh (telescoping 2.8e-17; end-sourcing
  0.0).
- **V3 5/5:** analytic structure-factor FT vs numeric 3D FT rel 0.0000;
  Born parameter 3.2e-3 (corrections ~0.3%, immaterial against
  orders-scale margins); the (N−1)-order scaling law tracks the computed
  structure factor at ratio 0.90–1.09 across all five N (essentially
  exact); orientation quadrature 320-vs-640 rel 5.5e-13; harmonic
  selection robust under a uniform 17° tangent-convention tilt (forbidden
  at the absolute floor) — only site-DEPENDENT conventions leak, excluded
  by uniform transport.
- **V4:** zero tunables. Inputs: R_s = r_c/χ (Q4a CLOSED); m_s = ħc/R_s;
  d = 1.15 fm (J8); E_C = 0.3 MeV (engine); the per-node normalization
  A_N = ℓ_v·ern1·SC_RULING/N (the 2393/2395 registered envelope
  convention, reproduced at V2a/V2b); the Si factor 28.09 (the committed
  2395 construction, reproduced at V2c); bisector node tangents (the
  symmetric two-bond referral, 2401 L2). 0865 held.
- **V5:** no cache file opened; sole output `code/2403_results.json`.
  Runtime 12.9 s.

## 3. The derivation, owned in full

**The reframe that unlocked it.** The 2395 language ("second-order
scattering of a zero-mean vector potential") anticipated a
fluctuation-variance mechanism. Working the structure from the 2401-derived
vertex, three NESTED protections emerged first, and they relocate the
residual entirely:

- **P1-i.** The fold-convention (edge-difference) zero is
  CONFIGURATION-INDEPENDENT — the telescoping sum vanishes for ANY closed
  node sequence, including grossly deformed ones (verified: 0.0 at random
  deformations). A zero that survives arbitrary deformation carries no
  information about the ring: it is an artifact of the edge discretization
  convention and CANNOT be the physical residual carrier.
- **P1-ii.** The continuum node form — the closed line integral of ∇Y —
  vanishes for any closed loop by the gradient theorem: a TOPOLOGICAL
  protection. Smooth deformations do not break it either (verified:
  refinement of the polygon at fixed loop decays the node sum
  super-algebraically to the numerical floor, 9.2e-12 by N = 24).
- **P1-iii.** What is NOT protected: the DISCRETENESS. The physical ring is
  N point vertices (the 2401-derived per-node vector v_k = M·t̂_k, bisector
  tangents from the symmetric two-bond referral), and the discrete node sum
  at a generic orientation is NONZERO (1.18e-5 at the N = 6 test probe) —
  the 2401 symmetric-probe zeros reproduced as the reflection-symmetric
  special case (P1-iv). **The residual IS the discreteness defect of the
  topologically-protected continuum integral.** This also retires the
  internal-excitation candidates I explored first (ring phonons, ζ-flips):
  smooth deformations are topologically protected at first order, so those
  channels enter only through second-order corrections to the static
  defect, gap-suppressed — enumerated and bounded at PIN-Q4c-2, never
  load-bearing.
- **P3 (the order — METH-L1-013 reused).** The fixed-orientation Born
  amplitude carries the structure factor D(q) = Σ_k (t̂_k·q̂)e^{iq·x_k}.
  C_N invariance makes D exactly 2π/N-periodic in azimuth — allowed
  azimuthal harmonics are jN ONLY (verified at the absolute
  double-precision floor). The leading small-qR_g behavior is
  |D| ≈ (qR_g/2ħc)^{N−1}/(N−1)!·√N — the (N−1) RADIAL order, confirmed
  numerically at ratio 0.90–1.09 for every N. The residual order is
  species-dependent and exponentially small in N at experimental momenta:
  zero-mean-ness upgraded to an exact multipole-protection statement.
- **P4 (the scale, absolute).** Born is exact at these couplings
  (parameter 3.2e-3). σ_n^res(q = 49.5 MeV) per species; DAMIC via the full
  differential spectrum above 550 eV, halo-folded (METH-L2-013), with the
  committed Si construction; XQC/rock/np/CMB by domination (the residual
  amplitude sits below the envelope amplitude at every channel momentum —
  worst ratio 3.7e-3 — and the envelope end's committed record passes all
  four).

**The numbers (graded, mechanical):**

| N | mass (GeV) | LZ σ_n^res (cm²) | vs strict 9.2e-48 | DAMIC events |
|---|-----------|------------------|--------------------|--------------|
| 4 | 5.63 | 1.37e-38 | fail — EDGE-conditional | 0.133 PASS |
| 5 | 7.04 | 2.46e-41 | fail — EDGE-conditional | 8.1e-5 PASS |
| 6 | 8.45 | 5.07e-44 | fail — EDGE-conditional | 9.4e-8 PASS |
| 7 | 9.86 | 1.16e-46 | 12.6× over — STRICT-POINT-conditional | 1.8e-10 PASS |
| 8 | 11.26 | 2.85e-49 | **PASS (32× inside)** | 4.9e-13 PASS |

DAMIC — the channel that killed the envelope end by 5.4–6.1 orders — passes
at every species by 3–15 orders. The derived residual sits below the
envelope by 8.3e-4 (N = 4) to 9.6e-9 (N = 8) in amplitude at the LZ scale:
the window-continuity table in the results JSON records the derived ratio
against each 2395 demand.

## 4. Instrument missteps, caught by the gates and owned

All in the verify instrument; the derivation moved zero; each caught by the
gate designed to catch it.

- **(i) Guessed Si factor (V2c, first run).** I first wrote the DAMIC Si
  construction as 14·0.9 from memory of the A-proxy conventions; V2c FAILED
  at rel 0.81. Read the committed 2395 line instead of guessing:
  `Vsi = −28.09·Venv` (the full Si mass number; events ∝ A², (28.09/12.6)²
  ≈ 5.0 = the discrepancy). Fixed to the committed construction; V2c then
  reproduces byte-exact. The lesson is the standing one: transplant, never
  reconstruct.
- **(ii) Harmonic-selection mis-statement (P3, first run).** My draft
  claimed azimuthal harmonics jN±1; the correct statement (C_N invariance
  of the full scalar D) is jN azimuthal, with the (N−1) appearing as the
  RADIAL order. The FFT test failed loudly; the docstring and check were
  corrected; the ORDER claim itself was unchanged and is confirmed
  quantitatively at V3-iii.
- **(iii) Aliasing + wrong yardstick (P3/V3-v, second/third runs).**
  256-point azimuthal sampling is not a multiple of N (aliases of allowed
  jN harmonics land on forbidden bins); fixed to nphi = 96N. Then the
  RELATIVE criterion (forbidden/leading) failed at large N because the
  leading allowed coefficient is itself (qR_g)^{N−1}-suppressed while
  forbidden bins sit at the ABSOLUTE double-precision floor (~1e-16, from
  O(1)-summand cancellation); the correct criterion is absolute (< 1e-14).
- **(iv) One stale variable** in the summary print after the verdict-logic
  restructure (in_lz → in_lz7/in_lz8); mechanical fix.

## 5. Grading discussion (why branch (c), and what the residue is)

The pre-registered §34.12 branches: **(a)** — residual outside the window
at an in-coverage channel with Class S dead ⟹ family dead. Read against the
2395 demand table literally, N = 7's LZ row is outside (derived res/env
1.6e-7 vs demand 4.5e-8, ×3.55 in amplitude). But the demand's LZ values
were built against LZ_STRICT — the deepest point of the curve, at 36 GeV —
and METH-L2-012's own registered logic says a strict-point FAIL at a mass
where the local curve value is unpinned is CONDITIONAL, not unconditional
(at the 2395 envelope end this nuance was immaterial: 15 orders over ANY
point of the curve; at the derived residual it is now the whole question —
the published LZ limit at 9.86 GeV is far weaker than the 36-GeV floor).
**§34.12 V4 pre-registered exactly this fork:** "The LZ 9-GeV edge stays
UNPINNED unless a Q4c re-confrontation makes it load-bearing — in which
case pinning it is a named CONV-004 action registered by amendment to this
contract (founder-gated), not a casual pin." The Q4c re-confrontation has
now made it load-bearing at exactly one species. I do NOT pin the curve;
I record the fork named.

**Branch (c), the recorded residue:** N = 8 (11.26 GeV) clears the full
ladder UNCONDITIONALLY — LZ strict 32× inside, DAMIC 15 orders inside,
domination everywhere. N = 7 (9.86 GeV) clears DAMIC and everything except
the LZ strict point, over which it sits 12.6× in σ (3.6× in amplitude) —
STRICT-POINT-CONDITIONAL, resolved either way by the founder-gated CONV-004
pin of the LZ local value at 9.86 GeV. N = 4–6 remain LZ-edge-conditional
(below the 9 GeV published edge; the 2395 pins carry) and DAMIC-clean.

Named pins:
- **PIN-Q4c-1:** the node-tangent convention (adjacent-bond bisector, the
  symmetric two-bond referral). The class verdict never depended on it
  (2401); here the residual MAGNITUDE stands on it, softened by V3-v: any
  UNIFORM convention preserves the C_N selection exactly — only
  site-dependent conventions (excluded by uniform transport, 0865) leak
  lower harmonics.
- **PIN-Q4c-2:** internal-excitation channels (deformation zero-point,
  ζ-flip) are second-order corrections to the static defect, subleading
  for any internal gap ≳ keV — a condition whose violation would preclude
  the ring's registered survival as a cold species.
- **PIN-Q4c-3 (the residue):** the N = 7 verdict is conditional on the LZ
  local value at 9.86 GeV — the §34.12 V4 founder-gated CONV-004 amendment,
  now load-bearing. Adjudication options on the founder's desk: (a)
  authorize the pin (a named data action: read the published LZ curve at
  9.86 GeV; if the local limit exceeds 1.16e-46 cm², N = 7 clears and the
  branch upgrades toward (b) pending nothing else); (b) decline the pin and
  carry N = 7 conditional (N = 8 alone already gives the family an
  unconditionally-clear member); (c) direct otherwise.

**What this means for the campaign:** the Q4 triple duty is now fully
executed — Q4a (order, linear, R_s in-demand), Q4b (class, V-t derived,
Class S excluded), Q4c (residual, derived and graded). The family is NOT
killed: no branch-(a) trigger fires un-conditionally anywhere; N = 8 is
fully derived-and-clear across the entire ladder; the single open residue
is one experimental-curve value at one species, pre-registered as
founder-gated. Per branch (c): founder adjudication on the recorded residue
before any downstream stage (Q5) proceeds. The Q2-identity theorem
registration remains UNBLOCKED (the 2401 V-t landing) and QUEUED — not
executed in passing, per the same discipline.

## 6. Scope guard + queue

Held: no Q5 pre-work; no LZ pin taken (recorded as the founder-gated fork
instead); no DAMIC re-pin (the committed 2395 pin reproduced, not altered);
no theorem registration executed; R2 lane untouched; no glossary promotion
(S/V-t and the defect-multipole coinages stay local until survival is
adjudicated).

Queue: **P1 (founder's desk):** the PIN-Q4c-3 adjudication — the LZ
9.86-GeV local-value CONV-004 amendment (authorize / decline / other).
**P2:** the Q2-identity theorem registration (unblocked at 2401, queued).
**P3:** on adjudication, Q5 (coat-channel sign synthesis + corridor
re-grade) per the §34.4 plan; successor-branch registration founder,
post-rent. **P4 (separate lane):** DM-1 v1.5 / DM-3 v1.1 R2 release,
unchanged, awaiting the founder's verbatim release decision.

Artifacts this patch: `code/2403_ss43_q4c_residual_scale.py`,
`code/2403_results.json`, campaign file §34.15, `frontier_sectors/SS.md`
2403 block, this fragment. (Patch 2402: the three methods-catalogue
registrations, catalog-first, committed separately ahead of this patch.)

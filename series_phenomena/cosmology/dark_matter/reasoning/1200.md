# Reasoning trail — Patch 1200 (DM-candidate consistency consolidation; Steps 1+3 → CONSISTENCY-grade)

**Patch:** 1200 (parallel round, worker window W2)
**Scope:** consolidation + grade judgment. NO new physics, NO new computation beyond re-deriving
published Step-1/Step-3 numbers, NO theorem registered, NO verdict change to CONJ-COSMO-1.
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this patch

The DM-first paper (Session-154 strategic priority) needs one citeable finding for "the cheap
gates the candidate clears," instead of citing two Step docs and re-deriving their margins. This
patch produces that consolidation and — the substantive part — fixes its grade.

## §2 What was read (not edited)

- `step1_sigma_over_m_SIDM.md` (0703): σ/m = 4.0×10⁻³ (qDP) / 7.9×10⁻⁴ (hTetra) cm²/g, ~250–1260×
  below the SIDM bound; NO KILL; survival robust to ×100 enhancement; two named gaps (constituent
  mass estimate; residual-potential shape).
- `step3_coldness.md` (0706): masses ~10⁵–10⁶× above the ~3 keV warm bound; decisively cold;
  independent of OPEN-SR-5.

Both are explicitly *falsification gates the model survives*, not positive identifications.

## §3 The grade reasoning (the load-bearing step)

The question the round poses is identification-grade vs consistency-grade. I tested it operationally
in the verify script: does either gate *discriminate* qDP/hTetra from generic CDM? A structureless
GeV-scale CDM placeholder passes both gates identically (σ/m ≈ 1.2×10⁻³ cm²/g; coldness ~3×10⁵×).
The gates are built from generic CDM properties (GeV mass, small geometric cross-section) and use
no qDP/hTetra-specific structure. Therefore surviving them is necessary-not-sufficient for
identification → **CONSISTENCY-grade**.

This is corroborated by the arc's own unconfirmed status (Step 4 SERIOUS TENSION, CONJ-COSMO-1 NOT
confirmed; Step 5 PASS-but-non-discriminating; the Session-154 handover's "identification theorems
still ahead"). A consolidation of two consistency gates cannot outrank the conjecture it serves.

## §4 Why I did not self-certify and did not register

Per the round's ESCALATION rule, the identification call is the integrator's. I recommend
CONSISTENCY-grade with reasoning, flag it for Thomas, and leave registration to the handoff note.
No shared registry or frontier file was touched (owned path = dark_matter/ new files only).

## §5 Honest caps

- The constituent masses are estimates; a lighter qDP erodes the Step-1 margin (σ/m ∝ 1/m).
- "Consistency-grade" is itself a real, publishable result (a viability floor), not a null — but it
  is not the headline "DM is qDP/hTetra" claim, which still needs a positive discriminant.
- ID `LEMMA-DM-CONSIST-1` is *proposed/reserved*, not registered; grep confirmed no `LEMMA-DM-*` /
  `THEO-DM-*` collision at base_ref 759c409.

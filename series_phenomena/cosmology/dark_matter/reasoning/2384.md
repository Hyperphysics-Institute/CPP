# Reasoning — Patch 2384 (the combined CONV-001 verification round on the 2381–2383 arc)

STATUS: verbatim, at-patch (Opus, DM lane, 9 July 2026). Founder verbatim: "Please
recommend, then proceed. Panel review if recommended." Worker recommendation (from
2383, reaffirmed here): a combined verification + ratification round BEFORE any Q3c
investment or registration — the 2375 precedent binds (candidate results reach the
record only through panel verification), and the arc's three patches carry enough new
load-bearing structure (the epoch-free stability floor; the r³ unit conversion; the
sign extraction) that adversarial eyes are cheap insurance against a wrong Q3c launch.

## 1. Round design choices, with reasons

**Verification-only (no generative R channel).** 2377's combined round included R
because the arc was at an impasse with no road; this arc HAS a road — the round's job
is to decide whether the road is real. Generative capacity is spent when it is the
scarce input, not by default.

**Five verification items, halting rules asymmetric.** V1–V3 carry the physics and the
gate: a REFUTED there halts the survival grading outright. V5 (the sign) is
consequential but separable: REFUTED reverts the outcome to (c)-repulsive-conditional —
the arc survives in weakened form. V4 carries process integrity and the anchor side,
whose finds count only through their joint structure (the XQC algebra does the gating).
The asymmetry is stated in the brief so panel effort aims where it can move the verdict.

**Two ratification items.** R1 discharges the panel-ratification flag the 2380(B2)
addendum has carried since execution. R2 is opinion-only by construction: Clause 1 is
founder-attested with an add-only rule, so the panel cannot move it — but the
discriminant-mass shift (2.8 → 8.45 GeV) makes 1(d) reference a dead region, and the
right time to surface that is now, attached to the round that verifies (or kills) the
family that created the exposure.

**Seat hygiene.** The 2378 A1 duplication anomaly (Grok/Gemini verbatim-identical) is
still open as a seat-hygiene item; this round's mapping instruction (each return opens
with the founder-assigned seat label; founder records window → return) is the fix that
round flagged. The Copilot no-access fix (2378 A2: paste FILE CONTENTS) stands, and per
the founder's note this round ALSO provides raw.githubusercontent.com links (no blob
form) so the Copilot seat may retrieve directly — both delivery paths, neither relied
on alone.

**Raw-links-only.** The founder's instruction for this round: raw format, no blob. The
brief's link block complies. The template's blob convenience is dropped for this round
on founder instruction; the template itself is not edited (a one-round instruction, not
a convention change).

## 2. What the brief claims and what it does not

The brief states the arc's numbers exactly as computed (deep-closure death at the gate;
N6-band survival both signs; floor anchoring; central-refuses-strain; the coupling pass
REGION; the attractive-default finding with bracket-min values) and states the owned
process events (the killed first launch and pre-anchor amendment; the coupling
recovery; the deferred above-floor ε_th bracket) as verification targets, not
footnotes. Nothing is registered by this patch. NO VERDICT MOVED.

## 3. Files

`conv001_2026-07_arc2381_2383_verification_brief.md` (the round's registered artifact);
this reasoning note; arc-doc append. The presentation block (template
`templates/presentation_file.md`, 4-backtick single-block form) is delivered in-chat
after the apply block, embedding the full brief plus the three arc-doc sections under
review — the paste is the delivery mechanism; the raw links are the supplement.

## 4. Founder's desk

Paste the presentation block to the five seats (assign seat labels R1–R5 at paste
time; record window → return). The adjudication block opens when the returns land.
Block EMPTY.

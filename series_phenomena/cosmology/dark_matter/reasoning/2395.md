# Reasoning — Patch 2395 (SS43-Q3 executed: the full baryon ladder at ring composition, per class — BRANCH (c))

STATUS: verbatim, at-patch (Opus, DM lane, 10 July 2026).

## 1. Launch and the founder's go

Warm launch on DM-WARM-2394; chain-verify 2394→2390 green; the §34.10 contract read
back in full before any computation. **Founder verbatim (§10 block): "Go. Please
proceed as recommended."** — the launch-completing go the 2394 handover required.
Nothing in the contract was renegotiated at launch; the grading branches stood as
written before the numbers, and the numbers were then computed into them.

## 2. The result in one paragraph

**BRANCH (c): SPLIT VERDICTS between the V-t bracket ends.** The identity end (the
registered fold convention's exact zero, A2a+A2b) clears the entire ladder by exact
identity — recorded as a derivation, not a computation, with the corollary that
grading branch (a) *cannot* trigger (computed in Part A, not assumed). The
conservative |V|-envelope end FAILS two channels at every species N = 4–8: LZ(strict)
by 15 orders of magnitude (σ_n 3.1e-33–2.0e-32 vs 9.2e-48, unshielded at 2.4–6.8 rock
collisions) and DAMIC-shallow by 5.4–6.1 orders (3.4e7–1.5e8 expected events above
threshold vs N90 = 123, unshielded at 0.16–0.46 collisions in the 106.7 m column).
XQC, np, and CMB pass the envelope end everywhere (member-weighted XQC ρ* = 76.6–109.3
≥ 0.3). Class S bookkeeping rows carried: XQC gate-death stands (nviol 2–11 at
standard density) and the species are rock-shielded AND DAMIC-shielded at
S_c = 0.0356 — the §34.9 kill is over-determined exactly as the contract intended.

## 3. Machinery decisions, owned

- **V1 in a scratch copy.** The 2394 handover's provenance lesson is now mechanical:
  the 2393-battery subprocess runs in a `tempfile.mkdtemp` copy of the tree, so a warm
  re-run can never regenerate committed artifacts. The committed record is the
  at-patch provenance; re-derivability is proven without touching it.
- **The density-convention bridge.** The cache (2379/2383/2391 era) stores counts at
  the 1-GeV/cm³ baseline; the 1880 rod ladder graded at ρ = 0.3. `predicted_bins` has
  NDM as a pure linear prefactor (verified by reading, not assumed), so the
  generalized XQC row is cache-counts × 0.3 with the violation test on the scaled
  counts — cache-compatible and exactly 1880-reproducing. V2 confirmed it
  verdict-for-verdict on all ten grid rows, including 0.035 ALIVE and 1.3e-3 dead-LZ.
- **DAMIC demanded the full differential treatment.** The light-mediator potential
  (R_s = 25.4 fm) is forward-peaked; total σ_Si would count the forward peak that
  deposits far below DAMIC's 550 eV threshold and overstate the excess by orders. The
  detection channel therefore integrates dσ/dE_R above threshold, halo-folded
  (machinery modeled on `predicted_bins`, with an explicitly unit-normalized f(v)
  grid rather than the engine's flux-normalized one — the rate formula carries its
  own n·v·σ flux factor). The shielding ceiling, by contrast, uses TOTAL σ (any
  collision degrades KE) — the registered 1880 convention, Si-proxy column.
- **The LZ strict-point gate and what its failure means.** Grading against
  LZ_STRICT = 9.2e-48 (the deepest point of the whole registered curve) was
  pre-registered as the way to make the verdict insensitive to the unpinned low-mass
  curve value. That logic is one-way: PASS at the strictest point is
  mass-edge-insensitive; FAIL below the published 9-GeV edge is EDGE-CONDITIONAL. The
  named-pin block records it mechanically: N = 4, 5, 6 (5.63/7.04/8.45 GeV) fail LZ
  only conditionally on the edge; N = 7, 8 (9.86/11.26 GeV) fail unconditionally
  in-coverage. The branch verdict rests on neither: DAMIC kills all five species
  squarely inside its 1–10 GeV silicon coverage.
- **np re-arm convention.** The contract's own "×9 the rod per-pair at N = 6"
  statement fixes the scaling origin at the rod: δa_np = 2.9e-3 · S_c · (18/N)² fm.
  This also makes the rod limit reproduce 1880 exactly (which applied the N = 15
  anchor at the rod without rescaling). At the envelope proxy S_c ≤ 0.0014 the channel
  passes by ~2 orders at every N.
- **CMB scaling.** One deterministic factor (18/N) for number density on the
  registered rod anchor; 4+ orders of headroom make any O(1) refinement immaterial.
- **Envelope reproduction asserted, not trusted.** Same seeds, same grid as 2393 —
  ell_v(N = 4–8) reproduced the committed values to < 1e-9 relative (25.873206,
  26.181696, 26.445099, 26.756016, 27.029574 fm). The LZ envelope amplitude is the
  Born FT of the actual spline envelope at q = 49.5 MeV, A = 1 — the registered 1880
  construction transplanted, no new form factor invented.

## 4. The physics read (why DAMIC kills what XQC passes)

The envelope end passes XQC with ρ* ≈ 76–139 and then loses to DAMIC by ~6 orders.
That is not a contradiction; it is exposure. DAMIC's 107 g·days is ≈ 2e29 nuclei·s of
silicon against XQC's ≈ 2.6e21 — a factor ~7.6e7 — while DAMIC's 550 eV threshold
(vs XQC's 29 eV) claws back only part of the forward-peaked spectrum. A back-of-
envelope transfer of the XQC pass margin through the exposure ratio predicts ~1e8
events; the differential computation gives 3.4e7–1.5e8. The channel behaved exactly
as the contract feared when it named the pin: the rod-era 25-GeV floor pin did not
transfer, and the fresh low-mass pin is where the envelope end dies.

The quantified fork (rate ∝ amplitude²): survival of the V-t branch requires the TRUE
post-closure residual amplitude below the |V| envelope by ×530–1100 (DAMIC, all N)
and ×1.8e7–4.6e7 (LZ strict, binding unconditionally at N = 7, 8). The envelope is an
upper BOUND from a first-moment |V| average — it is loose by construction (a
zero-mean vector potential's physical scattering enters at second order), so these
factors are a derivation demand, not a death sentence. But no smoothing: nothing in
the registered corpus currently DERIVES the residual scale, so the family's survival
is now precisely as conditional as the named pins state.

## 5. Session mechanics, owned

- An intermediate re-run was launched piped through `head`, which returned early and
  left the process orphaned mid-run. It was killed; the cache was restored to the
  committed 590-key baseline (`git checkout`); and the committed record is a final
  clean run with output redirected to a file, no pipes. Numbers across the first run
  and the final run are identical (deterministic seeds; V3.3/V3.4 verify call-level
  determinism explicitly). Lesson carried: committed-record runs get redirected
  output, never pipeline consumers.
- Cache 590 → 605: fifteen schema-pure flat appends (rod-grid rows at N = 18 for V2
  plus Class S ring rows). All 22 non-schema calls (ring-form pipelines, envelope σ,
  Born-FT, DAMIC differentials) stayed out by schema, verified.

## 6. What was deliberately NOT done

- **No Q4 pre-work.** The residual-scale derivation demand is quantified and handed
  over; not one line of substrate mechanics toward it was drafted here.
- **No class adoption.** The (c) HOLD stands; this patch sharpens the fork, it does
  not resolve it.
- **The LZ low-mass curve value was NOT pinned.** The strict-gate + DAMIC-in-coverage
  structure makes it non-load-bearing for the branch verdict; pinning it would add an
  unpinned-coefficient liability with zero grading power. If a future arc needs the
  9-GeV edge value, it gets its own CONV-004 action.
- **The identity end was NOT registered as a new theorem.** It is the 2393 identities
  applied through the ladder — convention-conditional exactly as §34.9 recorded them.

## 7. Queue at close

On the founder's desk: the branch (c) record — identity end clears, envelope end
fails LZ+DAMIC everywhere, named pins and residual demand quantified. Q4's registered
duty was already double (R_s from substrate mechanics + the vertex class); this
result makes the residual coupling scale its third deliverable, and arguably the
decisive one. Q5 (coat-channel sign synthesis) queues behind. R2 release lane
(DM-1 v1.5 / DM-3 v1.1) remains separate and untouched. Session-close handover patch
follows the founder's reading of this record, per operating_system.md §15.

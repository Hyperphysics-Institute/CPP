# Reasoning fragment — Patch 2987 (A-3 closed)

The re-delivery settled A-3 the decisive way: the same five texts arrived, verified by distinctive-string checks against the filed verbatim record — the tally-from-verbatim rule's first live use, one turn after its adoption — and the Copilot slot carried the identical stale CONV-011 text a second time. Two deliveries, same staleness, is evidence about the source, not the transport: that window is replaying its previous answer, and no amount of re-requesting through it will produce a CONV-012 return. Closing the seat as NO-RETURN (FINAL) rather than leaving A-3 dangling follows the 2972 precedent (an absence is recorded as an absence, never held open indefinitely against the queue), and §7's robustness analysis already proved the round complete without it. The optional fresh-conversation path is noted for the founder's delivery practice, not demanded — the promotion bar needs W-6b and the campaign data, not a fifth voice repeating what four have settled.

# reasoning/2670.md — S1c gap identification (verbatim, at-patch)

STATUS: verbatim, captured at Patch 2670, 20 July 2026.

The adjudication turned on one word in the charter's own question: FREE.
"What does a static polarization pattern in the FREE Sea stretch?"
Candidates (b) and (c) both parameterize their restoring by E_qq — the qq
bond energy, a property of qDP chains in hadrons and condensed
structures. The free Sea's registered constitution contains no such
bonds; attributing its restoring to bond quantities would be attributing
the medium's response to a structure the medium doesn't contain. That is
a constitution argument, not a numbers argument — at no point did any ℓ
enter the adjudication. Candidate (a), the DP internal binding, is the
one restoring structure the free Sea actually possesses, and the glossary
says so almost verbatim: the DP binding energy IS the vacuum energy
density. The PCD reading closes the loop: opposite-polarity CPs respond
oppositely to a common static field, the pair stretches against its own
binding, and the amplitude diverted into that stretch is not re-radiated
— which is exactly the per-re-radiation loss the founder's picture
reserved for P2.

I kept candidate (b)'s FORM while rejecting its content, and I want the
distinction preserved: the 2452 oscillator template (stiffness = 2·E/d²,
inertia = constituent CP mass, static limit = on-site susceptibility) is
registered machinery; instantiating it with free-Sea content is using the
registry, not overriding it.

Then the cancellation, which I did not expect. E_DP/m_CP = 264/132 =
88/44 = 2 — exactly, both species, because the registered DP binding
equals its constituent pair mass sum. So ħω_A = 2ħc/d_DP with every MeV
gone: species-universal, composition-independent, and I5 answered in the
strongest form — not only does E_qq not propagate, E_DP and m_CP
themselves cancel to a pure ratio. The whole arc's output now reduces to
registered geometry times registered structure. When a derivation sheds
its energy scales voluntarily, that is usually the substrate telling you
the answer is geometric; I registered the fact and resisted interpreting
it further before S2.

The one inference I had to make explicit: d_DP. The glossary defines the
DP as opposite CPs "occupying adjacent lattice sites," and the species'
MeV registrations live at the SS-2 embedding — so adjacent means
edge-separated at the motif level: d_DP = ℓ_edge. I registered this as
INF-S1C-1 by name so the panel can attack the level assignment directly
rather than discovering it buried in an equation. Fork-integrity check I
ran on myself: is identifying d_DP = ℓ_edge smuggling the fork? No — d_DP
is an input to ω_A, not the screening length; the emergent ℓ from the S2
spectrum is unconstrained by it a priori (the continuum estimate would be
sub-spacing, which is neither fork endpoint — and I deliberately did not
write that estimate anywhere in the stage document; lengths first appear
at S2).

The regime inequality κ·ℓ_edge = 2 > 1 came out as an OUTPUT and matches
the registered "CPP exits the Debye regime" finding independently — the
0757 fence is honored in the strong sense: not borrowed, rederived from
the opposite direction.

Blind-guard self-audit: no ℓ, no candidate value, no decay quantity, no
curve anywhere in the stage document or script. S2 opens with everything
it needs: the operator form (2669), the gap (this patch), the graph (I1),
the normalization (I6), and two mandatory robustness swaps.

# Reasoning fragment — Patch 2749 (X7-NSCAN prereg freeze)

Verbatim, at-patch.

The handover's single next act is the N-SCAN per s4x_x5fe_record.md §3.
CLONE-FIRST GATE run: grep of the dark-matter records shows the X-series
occupied through X6 (with X3-LONG, X5-LIN, X5-FE as suffixed acts); X7 is
free — the scan registers as X7-NSCAN.

Design decisions, with reasons:
1. Reuse run_A of 2746_s4x_x5fe.py VERBATIM, parameterized only by N.
   The scan's whole point is to characterize THE campaign code path at
   intermediate sizes; any reimplementation would confound the (a)-vs-(b)
   discrimination. Verified post-write: the Metropolis update lines
   (trial move, min-image distances, masked erfc+softcore real-space,
   incremental structure-factor dS, dEk, drive term, accept rule) are
   diff-identical between 2746 and 2749.
2. Seeds continue the frozen campaign sequence 2026078x (783–788),
   assigned DRV/UND per size in the prereg before any run.
3. The frozen classification quantifies §3's qualitative fork:
   ENHANCED / UNENHANCED / INTERMEDIATE bands defined from the two
   anchors (1.13 ± 0.12 and 1.70), with G1 (bracketed onset → B-CHECK at
   smallest enhanced N), G2 (smooth drift), G3 (flat → onset above 320),
   G4 (other → extend). Bands chosen so a hypothetical shallow drift
   lands INTERMEDIATE rather than being forced into a threshold reading —
   the same honesty posture as X5-FE's power caveat.
4. Disclosure: one 20-sweep timing benchmark (seed 1, no observable
   read, discarded) preceded the freeze to size the compute chunking
   (~61 min total on this container's single core). Chunked checkpointed
   execution every 500 sweeps guards against session interruption.

Nothing in this patch touches standing physics; 79.5% not in scope.

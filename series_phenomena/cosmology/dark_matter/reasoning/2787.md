# Reasoning fragment — Patch 2787 (X3/X4 requirement-7 leg)

**STATUS: verbatim, captured at patch. Session of 2026-07-23.**

## The bootstrap that wasn't there

The frozen prereg promised bootstrap errors for req-7 and the archive
cannot deliver them — szz is stored as a time-average, full stop. The
2785 pattern applied directly: no silent improvisation, no voiding a
leg whose substance is perfectly executable, but a named deviation
(DEV-1) with the implementable error model stated and its lineage
given (the A/B half-spread is exactly how the committed RV-3 battery
quoted its F3 errors, so the panel has already accepted this model
once on this same data). The single-chain geometries get ratios
without σ rather than σ's built on an invented error — an ADVISORY
label is more honest than a fabricated uncertainty.

## Reading the table

The MAIN-pair result is about as clean as archived data can say it:
24 shells, one 2.13σ excursion, and that excursion sits ABOVE unity —
the wrong sign to be the real-space deficit leaking into k-space. The
temptation to be resisted here is over-claiming in the favorable
direction: with 24 shells, P(at least one > 2σ) ≈ 2/3 under the null,
so n² = 12 is noise until replicated, and the record says so rather
than footnoting it. The single-chain sweeps were scanned for a
coherent cross-geometry pattern before concluding scatter: the n² = 4
shell dips in SIZE-S and SIZE-L but RISES in the MAIN pair at the same
physical region — incoherence is the signature of shell noise, and
that check is what separates "no pattern found" from "no pattern
looked for."

## What this buys the arc

S2's discriminant was designed to split two futures: if the
near-window deficit were a genuine screening-strength effect, its
conjugate k-range had to show it; if it is an extraction/profile-shape
systematic, k-space stays quiet. K-space stayed quiet. That is
evidence FOR the majority read and AGAINST new physics — but it is one
instrument, on archived statistics, with a deviation in its error
model, and PR1's fate belongs to the X3 replication and the X4
window-map/two-component/moving-feature battery, all still frozen and
unexecuted. The record therefore offers a reading and enacts nothing.

## Batching decision executed

With this leg complete, the founder's economy directive is
discharged as planned: one consolidated CONV-001 packet now carries
the L2-RB record (Q1–Q4), the X3/X4 prereg notice, this req-7 record
(with the DEV-1 ratification request), and the adjudication questions
— one paste per seat instead of three.

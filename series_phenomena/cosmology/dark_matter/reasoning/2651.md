# Reasoning fragment — Patch 2651 (C7-D1 execution)

**STATUS: at-patch verbatim. Verify: code/2651_c7_d1.py.**

The container killed background processes between tool calls, so the runtime
rider earned its keep in a form the prereg didn't anticipate: instead of naming
UNRUN cells, I converted the stage to per-member checkpointed execution (each
invocation computes ~75s of members, persists, exits; no member recomputed, no
cell extrapolated) and the full grid completed. The mechanism is disclosed in
the verify header because an execution pattern that differs from the naive
one-shot run is exactly the kind of thing a hostile reviewer should be able to
see without asking.

The pin was the moment of highest exposure: a TC-parameterized etot wrapping
dance_v8's own TC kwarg had to reproduce the 2635 table exactly, and it did —
every mean, every SEM, both dt, to the printed digit. After that, the grid's
answer arrived in two halves that mean more together than apart. The physical
modes' floors are flat across a factor of 4 in averaging time — the signature
written down at 2650 §3 before this arc ran: member-spread from static
phase-dependent offsets doesn't average down; noise does. And m0, which nobody
assigned a control role, collapsed faster than √TC with its mean dying to
INCONCLUSIVE — the instrument demonstrating on camera that it CAN average a
genuine fluctuation to zero. Flat physical floors + collapsing m0 in the same
run is the cleanest possible refutation of "the floors are just unconverged
noise."

The delicate call was §3.3: with D2-NO-ENTRY and D1 not D1-SCALES, both 2648
bars are arithmetically unreachable by any D3 outcome. Writing that down risks
reading as pre-adjudication; NOT writing it down risks a D3 session
rediscovering it and wondering if it was noticed. I wrote it as arithmetic on
frozen conjuncts, stated the composite still waits, and named what D3 is still
FOR (is the object local curvature at all — the direct-construction question
the offset candidate predicts answers "no plateau"). The temptation resisted,
same as 2650: no Branch-N sentence, no C7 sentence. Two arcs in, the offset
story has predicted two results in writing before they ran. The third arc gets
to try to kill it.

# Reasoning fragment — Patch 3175 (β-ladder Phase 1 verdict)

The discipline question this patch had to get right: the through-origin
fit limb of BETA-LINEAR PASSED (k_hat = 7.37e-03 consistent with all four
rungs' CIs simultaneously) while the ratio limb failed on power. The
frozen rule required BOTH. A worker who wanted a result could report the
fit and call it scaling; a worker who wanted the pre-declared expectation
confirmed could lean on the ratio's non-declaration. I did neither: the
reading is BETA-UNRESOLVED as frozen, the fit is reported as observation
beside it, and it is recorded that the fit limb points AWAY from my own
pre-declared BETA-FLAT/SUBLINEAR expectation — i.e. the observation that
survives cuts against the worker, and is stated for that reason.

Second discipline point: the existing data WOULD resolve a ratio built on
s(0.20)/s(0.10) — both rungs detect cleanly. Re-siting the endpoints now
is exactly the extraction the freeze exists to prevent, so it is
forbidden and named as forbidden in the record. The legitimate remedy is
N at β = 0.05 (≈ 685 pairs, ≈ 3.1 days), costed and NOT launched.

The coefficient finding is the substantive result: every rung's 99% CI
excludes 0.026·β, k_hat ≈ 0.0074, a ≈ 3.5× overprediction, now the third
independent confrontation across three codebases — this one with the
founder's inside-Sea geometry, a 2.8×-AK horizon, a passed bit-identity
gate and memory-tested hardware. Registered, not adopted. LIM-ISOLATED-DP
is the honest reason adoption would be premature and is the reason the
disposition question goes to the founder rather than being settled by me:
holding a finding back is itself a decision about what the panel gets to
see, and that is not the worker's call to make silently.

Instrument defect fixed in the same patch: --analyze crashed on its first
print under `> file` redirection because Windows switches to cp1252 and
cannot encode 'β'. stdout/stderr now reconfigured to UTF-8 in the driver.
My defect; it cost the founder a rerun.

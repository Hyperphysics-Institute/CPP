# 0877 — Registration: LEMMA-DM-CROSS-ROUTE-1 (conditional) + OPEN-COSMO-DM-3 (ASM-DM-CORONA-LOCALITY)

**Lane:** DM 08xx + **one registry crossing** (`frontier_sectors/CONJ.md`) — Thomas-greenlit. **Layer:** C.
**Trigger:** Thomas — "register now (conditional)." The campaign's first and only registry-lane action.

## CLONE-FIRST gate (executed before placing any ID)

- Cloned repo; grepped the registry. **No collision:** `LEMMA-DM-CROSS-ROUTE-1` appeared only in the memo +
  this campaign's reasoning fragments (reserved, registered nowhere); `OPEN-COSMO-DM-3` and
  `ASM-DM-CORONA-LOCALITY` appeared nowhere.
- Convention confirmed via precedent **LEMMA-DM-CONSIST-1** (SR.md, patch 1200): a finding-level DM lemma is
  frontier-tracked (a sector-file entry), **NOT** a `theorem-registry.md` row, **NO THEO**, **no swarm-count
  change**, and does **not** move the parent conjecture. Applied identically here.
- OPEN-COSMO series ran DM-1, DM-2 → **DM-3 free** and contiguous. Placed both in `CONJ.md` (the
  CONJ-COSMO-1 / COSMO-DM home — the discoverable, conceptually-correct location), before OPEN-COSMO-PBH-1.

## What was registered

1. **OPEN-COSMO-DM-3 — Sub-threshold Substrate-Locality (ASM-DM-CORONA-LOCALITY).** The candidate theorem /
   named assumption the panel's 1 RESTATE-with-fix required: *a surface electric well shallower than E_eDP
   elicits only reversible constituent-local vacuum polarization (in m_unit); no persistent real-eDP surface
   population and no extensive surface collective-mode/condensate mass.* Gates the lemma; deriving it →
   unconditional.
2. **LEMMA-DM-CROSS-ROUTE-1 — 4-wide cross as the σ/m-viable DM morphology (finding-level, CONDITIONAL).**
   The campaign result (0865–0876): cross is the viable morphology; strand/ball retired; corona retired
   conditional on OPEN-COSMO-DM-3. Panel: V5-Q 3 CONFIRM + 1 RESTATE-with-fix; Q2/Q3/Q4 4/4 CONFIRM.

## Discipline honored

- **No THEO, no theorem-registry row, no swarm-count change, no CONJ-COSMO-1 verdict move** (precedent-exact).
- **DM-1 stays v0.1** — not touched.
- Registry edit confined to the two co-located entries in `CONJ.md`; memo bumped to v7 to record the
  registration; no other sector/registry/founders edits.
- The lemma is registered as **CONDITIONAL** — the honest state — not as a closed/unconditional result.

## Status / next (Thomas's call)

The extended-aggregate goalpost campaign is closed at its natural endpoint. Open follow-ons, none blocking:
1. **Derive OPEN-COSMO-DM-3** (substrate-locality theorem) → lift the lemma to unconditional.
2. **Touch DM-1 §5–§7** to fold in the cross route as the leading morphology (corona resolved-conditional),
   keeping DM-1 at v0.1 until Thomas elects to ship.
3. **The hard σ/m number** for the clean cross spine vs 0.6–2 cm²/g (a scaling argument exists; a figure does
   not) — the path to a discriminating, swarm-countable result.

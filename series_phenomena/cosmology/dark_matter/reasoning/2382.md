# Reasoning — Patch 2382 (Q3b-2b: nucleation-and-growth from registered primitives)

STATUS: verbatim, at-patch (Opus, DM lane, 9 July 2026). Founder verbatim:
"recommend and proceed." Worker recommendation: execute Q3b-2b as the
quasi-steady-state closure-vs-growth branching cascade — cheap-kill-first, it
answers the contract's emergence question decisively before any expensive
time-dependent master equation; the full PDE is warranted only if these
verdicts come out marginal. They did not. Artifact:
`code/2382_q3b2b_nucleation_growth_cascade.py` (exit-coded; battery 7/7,
including the 2381 battery re-run green underneath by subprocess).

## 1. The formulation (every piece registered or bracketed, 0865 held)

Rates per PCD tick, rung-volume units: growth of an open chain = a·φ
(φ = registered 0881 bracket [6.7e-15, 7.4e-10]; a = O(1) kernel constant);
ring closure of an open N-chain = J_phys(N)·v_f with
J_phys = g_SY(N/r)/r³ — the registered 0861 SY J-factor is a contact density
per ℓ_p³, and the r³ conversion to rung units is NOT optional (it moves
onsets by up to three decades across the scan; carried explicitly).
v_f ∈ [0.1, 10] is the stated bond-volume convention bracket. ε = E_bond/kT
scanned over the registered D1 band [23.2, 36.2].

## 2. The result that reorganizes the stage: the epoch-free stability floor

Ring stability under cooling looked Λ-entangled (opening barrier
ε − c·r/N vs ln(ν/H)) until the registered scalings were imposed: ℓ_p = κ/kT
and ε = E_bond/kT scale IDENTICALLY with the bath, so

    N_stab = c·r/ε = c·κ/(ℓ_rung·E_bond)

is TEMPERATURE-INDEPENDENT — a pure stiffness-to-depth ratio of the one
2eDP:2qDP rung-bond SSV well. Rings below it pop open forever (bending
deepens exactly as fast as the bond); rings above it lock permanently, with
ln Λ only delaying the lock, never moving the boundary. The deciding
substrate object 0861 identified now decides the floor through a single
dimensionless ratio, with no epoch bookkeeping owed.

## 3. The emergence verdicts (computed over the full registered windows)

**Floor:** N_c = max(3 [topology], N_stab [stability], onset [J·v_f ≳ a·φ])
— three stacked derived mechanisms; the scan shows N_c ≥ 3 everywhere with
N_c rising through 4, 5, 6 as r grows. Nothing imposed. Battery V4 re-checks
support ≥ N_stab directly; V5 checks monotonicity in r and φ.

**Ceiling:** cascade termination. φ < J_peak_phys across the entire
registered window (margin ×6 at the worst corner r=12/φ_max/v_f=0.1, to
×10¹⁰⁺ at small r), so the onset always falls on the exponential rising side
of J and the cascade terminates within ~2 sizes (escape ≤ 1.5e-28 at
N_cut=64; width n90−n10 ≤ 2 at every corridor placement). Precisely stated:
a NARROW MOVING BAND, not an absolute N=6 wall. W(N>6) ≤ ~0.08 for
placements with peak ≤ 5; at the peak=6 top edge up to ~half the mass sits
at N=7–8 — quantified and carried to 2c as a named exposure.

**Anti-Flory (the registered-kill collision):** the shape verdict rests on
WIDTH, not the tail at the top edge (my own first-draft summary claimed
zero tail at machine resolution at every corridor placement; the grid
refuted it — max 0.48 at the peak=6 edge, and the Flory-tail ratio
compresses to ×1.5 there; text corrected pre-commit, third instance of the
machinery catching its author this arc). The categorical metric:
top2-adjacent mass share ≥ 0.71 at every corridor placement vs Flory's
≤ ~0.3 at matched ⟨N⟩ — ×3.4+ margin everywhere. The death mode
"equilibrium shape emerges anyway" DOES NOT FIRE.

**Near-bidispersity EMERGES BY MECHANISM:** dimers are the unique chain
species with no closure channel, so they accumulate to O(c₁)
(QSS: C₂/c₁ = 0.31–0.41 over the kernel-ratio bracket q ∈ [0.5, 2]) and
pair-jump the ladder (2+2 → 4). Deep-closure output: w(3) = 0.71–0.88,
w(4) = 0.12–0.29 — a dominant light ring plus an O(20%) heavier companion,
the qualitative shape of the corridor's actual occupants (2371: (3,6)
central, (4,5) extended) falling out of topology + kinetics. QSS mass
balance is exact (battery V6).

## 4. The placement kill, restated (2381 implication-3 superseded)

2381's placement band [0.89, 1.78] was derived from equilibrium peak
placement N* = 3.37r — the ensemble 2381's own implication 4 identified as
wrong for the frozen product. The kinetic computation replaces it: the
binding placement is N_c(r, φ, ε) ∈ [3, 6], giving r ≲ 5.5–13
(bracket-corner-dependent; full table in the script output) — far roomier
than the equilibrium band, and an UPPER kill only (small r always gives
N_c = 3; r < 0.5 exits SY validity into the flexible regime where Gaussian
J is larger and closure more dominant — one-sided robust, carried).
Retained-not-rewritten: the 2381 text stands; this patch's arc-doc append
supersedes its implication-3 band explicitly.

## 5. The Λ condition (named, not fabricated)

Residuals: w(1) = 1/(1 + (N̄/2)aφΛ), w(2) ≈ 2x·w(1). The D2/D3 live-corner
budgets are met for Λ = ν_PCD/H(formation) > 5.1e10 (φ_hi) to 5.7e15
(φ_lo). Λ is SF/substrate-cosmology input — not derived here, per 0861's
"flagged, not faked." The condition is registered with thresholds; any
microphysical clock at the keV-epoch Hubble time clears them by tens of
decades, but the pin is owed, and it is listed as such.

## 6. Honest residuals carried

(a) Chain-chain channels beyond 2+2 are O(1) shape corrections near onset
(where J ~ φ and N≥3 chains are not instantly absorbed) — the structural
verdicts rest on exponential-vs-bracket margins, the SHAPE near the
placement top edge does not; 2c should grade the derived w with this stated.
(b) The r-epoch drift (r ∝ 1/kT during the cascade) pushes late closures
stiffer/upward; the scan brackets it. (c) Kernel constant a and v_f enter
only as products with φ against exponentials — bracketed. (d) SY validity
u ≲ 6: the scan's u > 6 usage is confined to r < 0.5 and the V3 limit
checks; flagged in battery V7 with the one-sided-robustness argument.

## 7. Contract compliance and next

No steering: windows are the registered brackets, full maps shown including
where the mechanism dies (r above the placement ceiling; the peak=6 edge
exposure). No smuggled parameters. NO VERDICT MOVED — Q3b-2b's outputs are
inputs to Q3b-2c's grading, not registrations. Q3b-2c next: the derived
w(N | r, ε) family through the summed-XQC channel (2379 unit cache) and the
anchor suite at the 2374c ε_th-robust live corner, the SIGN extraction, and
the graded outcome — where the derived (3,4)-dominant shape either lands
inside the corridor the 2371 points occupy or the third kill lands fully
derived.

# 1836 — The floor verdict reduces to a scale-free ratio g, unified with the corpus edge-bond SSV potential → VIABLE

**STATUS:** verbatim, at-patch (Opus, DM 18xx lane, 30 June 2026). The "full κ_θ" calc — which converges on the
corpus's own edge-bond SSV near-cancellation and collapses the floor verdict to a single scale-free geometric
ratio. **DM-local; DM-1 stays v1.0; CONJ-COSMO-1 founder-gated.** Verify:
`code/1836_kappa_theta_scalefree_ratio.py`. **Layer: C.**

## What "running the calculation" actually found

Pulling the ZBW/SSV machinery to compute κ_θ led straight to the corpus's **own edge-bond SSV potential** — the
**G1 make-or-break** flagged in the 25 June handover (`handovers/2026-06-25_..._edge_bond_ssv_deciding_calc.md`),
which that handover already called the calc that decides "whether the entire DM candidate lives." The X-junction
dihedral stiffness **is** that same qq-edge-bond angular stiffness: the residual of *(closer like-charge
repulsion) − (farther opposite-charge screening)*, the same potential that sets the ribbon persistence length
ℓ_p (~100–700 fm) and the per-rung E_ee. I did not need a new object; I rediscovered the one the programme
already knows is load-bearing.

## The move that collapses the uncertainty — a scale-free ratio

The floor flexibility test (1830) is κ_θ < 3B/L_arm, where B is the **arm's** bending rigidity. But κ_θ (the
scissor) and B (the in-line bend) are the **same edge-bond potential in two geometries.** Writing g ≡
κ_scissor/κ_bend, with B = κ_bend · ℓ_rung (worm-like chain) and L_arm = (N/2)·ℓ_rung, the test becomes:

  **g < 6/N**

and **everything absolute cancels.** kT_form, the ZBW frequency/amplitude, the near-cancellation depth, and — the
part that matters most — the **static-vs-dynamic (Earnshaw) sign question** all drop out, because whatever
stabilizes the bend stabilizes the scissor the *same way* (it is the same bond). Only the **geometry ratio** g
survives. The floor-setting arms are N ~ 14 (post-fusion at clusters), so the test is **g < 0.43**.

## What that does to the whole arc — it makes the hard parts moot for the verdict

- **1833 (static κ_θ ≈ 0.27):** an absolute near-cancellation residual — geometry-sensitive, bounced. *Cancels
  in g.*
- **1834 (Earnshaw, static unstable):** the full static config is Earnshaw-unstable, but the corpus G1
  establishes the edge-bond **bend coordinate is restoring** (the screened near-cancellation has a net restoring
  sign). And the sign question **cancels in g** regardless — the ratio doesn't care what provides the restoring
  torque, only that the same mechanism acts in both geometries. So the 1834 worry, while correct about the full
  static config, does not touch the verdict.
- **1835 (ponderomotive, founder gradient read):** this *is* the geometry input to g — the scissor charges sit
  farther apart than the in-line bend, and stiffness ~ gradient² → g < 1. The dynamic framing was a route to the
  same geometry ratio.

So the three prior junction patches, including my own retraction, **converge** in the ratio: the verdict needs
only g, and g is a clean geometric question.

## The number

Thomas's read fixes g's direction and rough size. The scissor mode is the same qq edge bond but with the two
rods **perpendicular** (crossed at one edge), so the off-hinge charges are **farther apart** than in the in-line
ribbon bend (adjacent rungs). With stiffness ~ gradient² and a separation ratio ~1.5–2.5×, **g ~ 0.06–0.30** —
**below g_crit = 0.43** at N ~ 14. Hinge flexible → per-fusion drop 1/8–1/4 → **cluster floor ~0.4–0.8 →
VIABLE.**

## Unification (the real result)

The **DM cluster floor, the ribbon persistence length ℓ_p, and the per-rung E_ee are all set by the one
edge-bond SSV potential** — the G1 object the 25 June handover already named the DM make-or-break. The floor is
not a separate unknown; it is a *ratio* drawn from that single potential (scissor geometry vs bend geometry).
This means the velocity-sector viability and the formation/stiffness/fragmentation physics stand or fall
**together** on one substrate calculation, and that calculation is already on the books as load-bearing.

## Honest residual

The **definitive g** requires the edge-bond SSV potential evaluated in both geometries — the G1 / SF-2/SF-5
deliverable, which is **still open** (the 25 June handover's "hard part," the near-cancellation at sub-Planck
pre-tension separations, with absolute scales flagged as SF-input). My g ~ 0.06–0.30 is a gradient-scaling
estimate of the geometry ratio, supported by Thomas's structural read, not the evaluated potential. But the
ratio framing is **far more robust** than the absolute-stiffness attempts: it is immune to the scale, thermal,
ZBW, and Earnshaw uncertainties that broke 1833/1834. The verdict (g < 0.43 → viable) holds unless the scissor
geometry is *stiffer* than the in-line bend, which Thomas's read and the perpendicular-crossing geometry both
argue against.

## Net status of the junction arc

Floor **VIABLE (~0.4–0.8)** on a **scale-free** test (g < 6/N) that survives every absolute-scale uncertainty,
anchored by the founder's geometry read and unified with the corpus's existing edge-bond make-or-break. This is
the strongest the floor has been: the prior arc (1830 tense → 1831 edge-hinge → 1833 static 0.27 retracted →
1834 Earnshaw → 1835 gradient read) resolves here into a single robust geometric criterion. **Self-limiting and
hinge stability (1831) unaffected.** Still **no promotion** — the definitive g is the open edge-bond SSV calc —
but the candidate now hangs on a calculation the programme already prioritizes, not a DM-specific unknown.

## Anti-priorities held

Did not manufacture an absolute κ_θ to "finish" the task — found the more robust scale-free framing and reported
that the definitive number is the open SF calc; recontextualized 1834's Earnshaw finding honestly (correct about
the full config, moot for the ratio) rather than burying it; tied the result to the corpus's existing
make-or-break rather than inventing a parallel unknown; kept g ~ 0.06–0.30 labeled as a gradient estimate, not
the evaluated potential. **No promotion; DM-1 v1.0; CONJ-COSMO-1 founder-gated.**

## Next actions (surface to Thomas)

1. **The edge-bond SSV potential in scissor vs bend geometry** is now the single definitive calc — it pins g
   (DM floor), ℓ_p (formation/stiffness), and E_ee (fragmentation) at once. It is SF-2/SF-5 lane (sub-Planck
   pre-tension separations, SR-1 SSV law) and was already flagged as the DM make-or-break on 25 June. Worth
   elevating from "open" to "active," since it now closes three questions, not one.
2. **Panel round 2** is well-posed now: the flexible-X concern is answered by (edge-hinge mechanism) +
   (scale-free ratio g < 6/N) + (founder geometry read ⇒ g ~ 0.1 ⇒ viable) — a robust chain that no longer
   depends on any absolute-scale number the reviewers could contest.
3. **Stage the gradient read + the g-ratio unification** to `founders_vision.md` on authorization.

# Reasoning — Patch 2565 (the gate fired, and the diagnostic made the blocker deeper and truer)

STATUS: verbatim, at-patch.

The controls passing was genuinely good news I want to record as such: the translating
home moved a bound dipole 56.35 fm at 0.2c with sub-0.1% error and 0.16 fm cohesion, in
every dt and FREF cell. The instrument can carry structure ballistically. That banks.

The census firing was expected from the code read (the reach lists are built once, and at
6 a_qq nothing cross-DP is in them), and I was prepared to route the static-vs-live reach
question to the founder as a clean 2555-class spec item. What I was NOT prepared for was
the live-reach diagnostic coming back ESC at 5 MeV head-on. My first instinct was to
suspect the probe setup; I re-read the trajectory logic instead and found the real thing:
the homes never decelerate. Contention can grab all it wants — the scaffold is a rail, and
the rail runs through the collision at constant velocity forever. Live reach answers "can
they see each other" but not "can the encounter absorb momentum," and the second question
is the one capture actually depends on.

That reframe matters for what gets routed to Thomas. If I had shipped only gap (i), his
adjudication (say, "live reach") would have licensed a production run that returns
sigma_cap = 0 everywhere for instrumental reasons dressed as physics — a false adverse at
grid scale, exactly the failure C-3 was built to prevent at classifier scale. The
diagnostic caught it one level up. The options package names three honest exits without
adopting any: O1 and O2 are new dynamics (his by the charter's own kill condition), O3 is
the cheap statics-adjacent re-scope. My private lean is O3-first-then-O1 — a threshold
now, trajectories later — but the lean stays out of the record because the choice of
which new rule the postulates license is precisely the physics authority PD-006 reserves.

One more thing recorded deliberately: this blockage is a candidate panel trigger under
the founder's rule (a), but the adjudication needed is his, not theirs. Noting that in
the document prevents a future session from reading "blocked" and auto-building a
dispatch the founder didn't ask for.

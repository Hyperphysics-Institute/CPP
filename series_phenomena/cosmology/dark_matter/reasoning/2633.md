# Reasoning — Patch 2633 (a rider clears, and the defect turns out to have been aiming at nothing)

STATUS: verbatim, at-patch.

The satisfying part of this cell is not that the rider cleared -- at 40x the floor
it was never close -- but WHY the union members agreed. DIST and FULL matching to
every decimal looked at first like a bug, and the discipline of chasing identities
instead of celebrating them paid immediately: every neighbor the truncation was
discarding is same-charge, and the dance only ever reads opposite-charge targets
out of the reach list. The 2574 audit found a real defect in the LIST; tonight's
census shows the defect never reached the DYNAMICS for this observable. That
distinction matters for how the lesson generalizes: the zero-margin assert stays
adopted, because a future observable that consumes same-charge reach members would
inherit the full defect -- the filter that saved E_close is a property of the
consumer, not the classifier.

The precision disclosure in the control section is small but principled: the
record's band was integer-rounded, my prereg quoted the rounded band, and the
reproduction landed 0.4 outside the literal integers while being definitionally
the registered machinery's own output. Writing "PASS with disclosure" instead of
silently widening the band keeps the control honest about what it tested --
namespace binding and machinery identity -- rather than pretending it tested a
tolerance the record never stated.

# Reasoning fragment — Patch 2992: the skeleton, and three governance calls made inside it

**Q5 was written to bind the adjudicator, not just the panel.** The temptation in a promotion
round is to let "promote" be decided in the adjudication's prose after favorable returns. The
skeleton instead freezes the decision structure NOW, before any data exists: promotion requires
Q1 CONFIRM and Q3 RESOLVED-CONSISTENT-with-floor; any other Q3 branch routes to that branch's
pre-registered consequences (the falsifier branch names its three victims — T-3 §6, L-4, L-6 —
via links registered at 2990/2991, before anyone knew which way the data would point). Freezing
the decision tree while ignorant of the outcome is the entire value of pre-registration; a
skeleton written after the data would be theater.

**Copilot re-seated, with a third-strike consequence.** The CONV-012 closure was scoped to that
round; the diagnosed failure (window replay) is remediable by a fresh conversation, and a seat
that costs one paste is cheap relative to a fifth adversarial perspective on a promotion round.
But open-ended re-seating would make NO-RETURN free. The condition is explicit in the dispatch
where the founder and the record can both see it: new conversation, and a third stale
redelivery closes the seat for the arc. PD-006 makes this the worker's call; it is made in
writing, revocable by founder physics-picture authority like everything else.

**The disclosed-errors line now includes the worker's OWN bookkeeping failures.** The 2986
tally defects and the 2990 misplaced script both go in the attack-surface list alongside the
physics errors. The panel's trust in SCRIPT-EXECUTED claims and tally lines is the mechanism's
load-bearing joint; disclosing process failures there, not just math failures, is what keeps
the adversarial-review economy honest. The QM-1 separability claim (the ground on which AP-2
was ratified despite the retired grounding) is likewise offered for attack rather than
presented as settled — if the separability argument is wrong, the panel should find it now,
before the promotion, not after.

**Key selection.** KEY-E (|m1d|) forces reconstruction of the C-5(i) evidence object; KEY-F
(r[0]) forces actually running the C-5(iii) toy's linearity check, not just its headline
asserts. Both are unprinted internals — per the Grok precedent, reconstruction from committed
source is the point. Values computed nowhere and stored nowhere; the adjudicating session
reruns the deterministic scripts. The grep belongs to dispatch day because history keeps
growing until then.

# 0876 — v5 panel returns: corona reframing ratified (3 CONFIRM + 1 RESTATE-with-fix), fix folded

**Lane:** DM 08xx, DM-local. No registry/SF/THEO/OPEN/founders edits. DM-1 stays v0.1. **Layer:** C.
**Trigger:** four-model panel review of the v5 memo returned; fold the verdicts into rev6.

## The vote

- **V5-Q (corona reframing):** **3 CONFIRM + 1 RESTATE-with-fix.**
- **Q2 (beam stiffness), Q3 (strand retirement), Q4 (d_f/DLCA):** **4/4 CONFIRM.**

The three CONFIRMs independently ratify the rev5 energy-scale/reservoir retirement: a ~56 keV electric-vdW
well cannot create-and-bind an 88 MeV eDP (one reviewer invoked the Schwinger/supercritical-Z analogy); the
only dense reservoir (the vacuum Sea) is balanced and Planck-scale; the polarization is constituent-level
self-energy already in m_unit; the monomer consistency check holds (no DM-1 correction).

## The RESTATE-with-fix (correct — folded, not outvoted)

Although the majority confirmed, the dissent is right and I am honoring it rather than counting votes. The
catch: the step **"sub-threshold well ⇒ *only* constituent-level vacuum polarization"** is not automatic.
Ordinary field theory admits other sub-threshold responses — collective surface modes, induced condensates,
metastable surface excitations — and the energy-scale argument excludes only *real-eDP accretion*, not those.
So presenting the corona as flatly "retired" overstates a proven result.

**Fix folded (exactly the reviewer's two requests):**
1. **Name the assumption.** ASM-DM-CORONA-LOCALITY (a candidate future theorem): *a surface electric well
   shallower than E_eDP elicits only reversible, constituent-local vacuum polarization (absorbed into m_unit);
   no persistent real-eDP surface population and no extensive surface collective-mode/condensate mass.* The
   corona is now **retired conditional on** this named assumption, not asserted as fully proven.
2. **Soften the per-N bookkeeping** to its narrower true form: *if the polarization response remains localized
   to each constituent and generates no additional extensive surface reservoir, its contribution stays O(N)
   and is absorbed into m_unit* — rather than "every surface effect scales per-unit."

Added ASM-DM-CORONA-LOCALITY to §9 as the remaining formal gap (deriving it from PCD/substrate mass-gap
dynamics would upgrade "retired conditional" → "retired"; SF/substrate, not blocking).

## Why fold rather than re-review

This is the standard v1.0→v1.1 pattern: a RESTATE-with-fix is incorporated, not re-litigated. The fix is
precisely what the dissenting reviewer asked for (name the assumption / soften the language), so applying it
needs no further round.

## Status

- Cross route: **viable; corona retired conditional on ASM-DM-CORONA-LOCALITY; no remaining computed kill-risk.**
  Beam stiffness, strand kills, ball/d_f, G2, G3 all confirmed.
- **LEMMA-DM-CROSS-ROUTE-1 is ready for registration as a CONDITIONAL result** (corona retired conditional on
  ASM-DM-CORONA-LOCALITY). Registration itself is a **registry-lane** action requiring (a) Thomas sign-off and
  (b) a CLONE-FIRST registry grep before placing the ID — NOT done here (this patch stays DM-local).
- DM-1 stays v0.1.

## Next actions (Thomas's call)

1. **Register** LEMMA-DM-CROSS-ROUTE-1 (conditional) + optionally an OPEN item for ASM-DM-CORONA-LOCALITY (the
   candidate theorem) — requires the CLONE-FIRST grep + registry edit + sign-off. Propose, do not auto-do.
2. **Or** derive ASM-DM-CORONA-LOCALITY first (upgrade to unconditional) before registering.
3. **Or** touch DM-1 §5–§7 to record the cross route as the leading morphology (corona resolved conditional),
   keeping DM-1 at v0.1 until Thomas elects to ship.

# Reasoning fragment — Patch 2649 (C7-discriminant campaign prereg)

**STATUS: at-patch verbatim. No computation (no verify script — no run exists).**

The founder directed the three discriminant tests. The budget reality and the
one-patch-ago J4 adoptions point the same direction: prereg first, runs on
fresh budget. Executing tonight would have violated the disciplines we adopted
this session (D3 is new instrumentation requiring a trace table, deliberate
guard-trigger tests, and a clean-room second classifier — none of which should
be built in a budget squeeze), and a half-run discriminant against C7 would be
the worst artifact imaginable one patch after the panel told us our defects
cluster in implemented-semantics drift.

Decisions preserved: (i) execution order D2→D1→D3 frozen with rationale — D2's
regime-entry answer conditions the interpretability of every other number, D1
characterizes the floor, D3 is heaviest and benefits from knowing both; order
non-permutable so no session can cherry-pick the arc most likely to please.
(ii) Composite-only adjudication — per-arc readings register and wait, because
the 2648 bars are conjunctions over all three arcs and any per-arc sentence
would be a partial reading the next arc could embarrass. (iii) D2's sign clause
(flips along the ladder are data) and D1's runtime rider (unrun cells named
UNRUN, never extrapolated) are pre-frozen because both are exactly the
pressure points where an execution session would otherwise improvise. (iv) The
campaign explicitly CANNOT supersede C7 — meeting a bar produces a proposal
packet for the panel, nothing more; the strongest thing this prereg does is
make it impossible for a future session to slide from "bar met" to
"superseded" without a dispatch. (v) D3's ladder is frozen now with a
widening-only extension proviso keyed to D2's boundary — freezing nothing
would invite tuning; freezing rigidly would invite a mismatch with D2's
answer; widening-only is the composition that cannot flatter.

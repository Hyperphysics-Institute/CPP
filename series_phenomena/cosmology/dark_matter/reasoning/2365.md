# Reasoning fragment — Patch 2365: F-DM3-4 rate computation STAGE 1 (overburden ceilings)

## 1. Authority and scoping
Founder "Recommend and proceed" on the 2364 queue → the overburden/rate
computation as the immediate arc. Scoping decision made and stated BEFORE
computing: the owed computation splits into a model-independent half (the
ceilings and window — computable today from standard transport physics) and a
model-side half (σ_dimer from the P2 pipeline at A_dimer — requires the
partial-wave machinery run at the dimer's mass number, NOT rebuildable
honestly in one pass). Stage 1 executed now; Stage 2 named as the sole
remaining gate. This split is itself the honest move: it converts the panel's
"uncomputed ceiling" into a computed target without pretending to the model
number I don't yet have.

## 2. Pre-registration discipline
Conventions fixed in the script header before execution, hurting-direction
where a choice existed (coherent-SI scaling maximizes stopping and LOWERS
ceilings — the direction that shrinks our window). Floors deliberately
bracketed FOUR DECADES wide so the verdict cannot lean on floor precision.
Three verify checks fixed pre-run: depth-monotonicity, surface ceiling in the
published order, window robustness at worst-case floor. Outcomes (i)/(ii)/
(iii) graded as written; (iii) was the pre-release hurting scenario (papers
revised before the 20th).

## 3. Result and grading
Verify 3/3. Ceilings: 8.9e-32 (SNOLAB) / 1.1e-31 (LSM) / 2.3e-30 (MINOS-depth)
/ 6.7e-29 (surface) cm², brackets in the JSON. Worst case leaves ~6.3 decades
of window at the deepest site. OUTCOME (i) FIRES — window at every listed site
class. Consequence executed per the pre-registration: both papers' F-DM3-4
conditionality narrowed (uncomputed ceiling → computed window, σ_dimer the one
owed number); exit (d) untouched. Structural point registered: XQC-class
(overburden-free, F5's own machinery) caps the ultra-strong regime, so only
the ceiling-to-floor GAPS can hide the species — explicitly a Stage-2 item.

## 4. Residuals (stated)
CSDA is an O(1)-class estimator vs full MC (Emken–Kouvaris) — fine for a
six-decade verdict, NOT for gap-edge verdicts; mean-Ā media; coherence for a
composite is a convention (the hurting one chosen); floors are class-brackets.
All four land in Stage 2's scope.

## 5. Forward
Stage 2 = the P2-channel machinery at A_dimer (σ_dimer at the passing points'
implied couplings), gap treatment, floors pinned to current exclusion curves.
On Stage 2: F-DM3-4 activates (or the gap/deactivation branch executes — both
pre-registerable). Eleven-day runway note: Stage 2 before the 20th releases
the papers with an ACTIVE discriminant; feasibility depends on the P2
pipeline's location and state — check first patch of the Stage-2 arc.

## 10. FOUNDER-CONTRIBUTION BLOCK (verbatim)
> "Recommend and proceed:" (TLA, 9 July 2026)

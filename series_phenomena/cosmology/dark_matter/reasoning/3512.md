# Reasoning — Patch 3512 (verbatim, at-patch)

The handover said to perform the residue computation of transition_graph §3 and read it once against D1 and D1′. Before writing anything I resolved each symbol against its lane (D-7): n̄ is the per-GP occupancy and ln n̄ = 3N_rem (3890, hengine driver statement); the engine is H ∝ μ = kT ln n̄, so H ∝ N_rem and the Moments per e-fold go as 1/N_rem — the engine stalls at the count law's end; the q-share of the pool at that end is q·1 = ½ (3936's q = ½); the bias is polarity-blind (3509); n₊ = n₋ exactly (neutrality grounding). The 5.2×10⁴ Moments per e-fold in correlation_length.md is A_s-derived (R_H at the pivot), so I did not use it as an input — M_piv is carried and scanned.

My first attempt at the structure was wrong and the script caught it. I derived a first-order result — p − 1 = −(1/3)⟨ε⟩·ln(1/u) — assuming the pool is swept fully through the O(1) window, and then argued from two-body depletion that p = u. The second was an error: it assumed the pool is released at a definite finite occupancy, whereas in this chain every CP hops from the start at λ ≫ 1 where a crowd does not bond, so under constant H the whole sweep is a time translation and p = 1 exactly, depleted or not. That is worth having: it says the founder's sub-linear expectation is entirely an ε effect. The first-order formula is right where its premise holds (T5) and fails on the engine because the premise fails: the count law ends inside the window. That was the finding I had not anticipated — one CP per GP is λ_q = ½, the peak of the pair kernel, and the engine's H is zero there. Nothing dilutes the pool out. Carried on, the engine gives two-body freeze and p → 0.

I then had to decide whether that is the D1 reading. The charter's own admissible-H clause says the engine's form or the MeV H, whichever the epoch selects, and the epoch is Planck — so strictly the reading is p → 0, outside the band, and the pre-registered consequence is that C-5 dies. I did not take that reading, and the reason is a D-6 one: the engine's stall is its end (3892: inflation ends at n̄ = 1), and carrying H ∝ N_rem to N_rem = 0 as the physical dilution law past the count law is working a lane's form outside its domain. The termination scan makes the dependence explicit: p spans 0 to 1 with the band inside, as a function of where dilution stops relative to n̄ = 1. Under charter D1's band rule that is "model-dependent across admissible lock-in criteria, straddling the edge" — C-5 conditional. I am also not taking the pass: nothing on file puts the termination at n̄ ≈ 1.01, and picking it because it lands would be 3907's error in a new costume. The honest output is the reduced condition and the picture question, which PD-006 says is the founder's.

Two integrator lessons, recorded because they nearly became results: a freeze threshold of λ_free < 10⁻⁷ was itself freezing the pool at corpus-scale M_piv, producing identical p across handoff conditions — a threshold masquerading as physics, caught because the values were suspiciously equal; and the finite-difference exponent at u ~ 10⁻⁷ carries ±0.1 of noise, which I state rather than smooth. The λ_end = ½-at-the-peak coincidence is flagged and not used. The reading is taken once; the band was not moved.

# Reasoning fragment — Patch 2379: the fidelity audit — the founder's question, answered with numbers

## 1. Authority and the question
Founder: "the data that killed our DM-1, 3 … were from only one study, on a
rocket 20 years ago. Are we confident in the fidelity of that measurement?"
Worker answer structure: correct the framing (XQC kills; dSph anchors are
the survival side), stratify the confidence (the data-as-data and its
two-decade, four-group reanalysis lineage are stronger than "one rocket"
suggests; the ε_th = 1 assumption at a ×1.2 margin is softer than the
record priced), recommend the audit, hold (B). Founder: "Proceed with
recommendation."

## 2. The honest pre-statement that mattered
The ε_th direction was declared genuinely open before running — compression
can pile recoils INTO the quiet bins as easily as drop them below threshold
— and the non-obvious direction is what the computation found first: at
ε_th = 0.5 every graded object gets WORSE for survival (the 2375 margins
rise; marginal corridor points flip excluded) before threshold loss wins at
ε_th ≤ 0.1. A hand-waved "lower ε_th weakens the kill" would have been
wrong in half the bracket. The machinery decided; nothing was tuned.

## 3. What the audit establishes
The registered 2369 kill is ε_th-robust to 0.1 and its S_c ≥ 0.035 points
are robust to 0.02 — only the wholesale form's island-floor corner opens at
the literature's extreme bracket edge. The 2375 candidate kill is
conditional with a MEASURED threshold: wholesale for ε_th ≳ 0.35–0.5,
inside calorimetric expectation (heat is what a calorimeter measures) and
outside worst-case brackets. The corridor's live corner survives the whole
bracket. Attenuation is a non-issue at generous envelopes. And the
single-flight conditionality is now REGISTERED rather than implicit — with
the ε_th pin named as the owed external input and F5 (Micro-X-class
reflight) as the direct future test. The founder's question converted an
unpriced assumption into a priced one.

## 4. What is NOT claimed
No verdict moves. The 2369 finding is reported, not acted on — editing
registered kill text is founder + panel business (B2). ε* ≈ 0.35 is a
linear interpolation between computed points (0.25 and 0.5), stated as
such. The ε_th brackets are literature values, not a measurement of this
detector's actual nuclear-recoil response — the pin is owed, and until it
lands, "conditional on ε_th ≳ 0.35–0.5" is the honest kill language.

## 5. Process, owned
Two runner timeouts forced a cached-unit-count infrastructure (unit counts
at f = ρ = 1, exactly linear scaling — the cache is committed for reuse);
a path-resolution bug in an ad-hoc fragment was caught by its own crash and
bypassed with an explicit-path warmer; the ε_th = 0.25 refinement was run
beyond the pre-registered four-point bracket to sharpen the conditionality
statement, disclosed as a refinement.

## 10. FOUNDER-CONTRIBUTION BLOCK (verbatim)
> "Note that the data that killed our DM-1, 3 on the dSph galaxies were
> from only one study, on a rocket 20 years ago. Are we confident in the
> fidelity of that measurement?" (TLA, 9 July 2026)
> "Proceed with recommendation." (TLA, 9 July 2026)

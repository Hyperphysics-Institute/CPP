# Patch 3026 reasoning fragment — KMEM-TAIL-1 disposition execution (verbatim capture)

**Context.** Founder challenged the flash sanction round under
WORKFLOW-REVIEW-ECONOMY. I tested the round against the provision
honestly: a prereg in hand is neither a win nor an impasse; the parent
measurement's prereg (2981) executed with no pre-sanction; the
freeze-before-code discipline is the actual integrity protection; the
seats can attack the design at the record adjudication, and a sustained
design objection voids the disposition. The material difference from
2981 — this prereg was frozen AFTER the headline numbers were seen —
is answered by the freeze plus adversarial design-attack, not by a
second founder cycle. Sanction cancelled (3025), gate amended with
disclosure BEFORE any code existed, standing rule added to WORKFLOW.md
(pre-execution design-sanction rounds = founder-green-light exception).

**T-D derivation (the load-bearing step this patch).** Chasing the
2.6e-3 provenance: MEAS-1 record §4.1 defines it as the SUSTAINED
motion response (0.026·β at β = 0.10). MEAS-1's statistic
S = |F_inf − F_0| takes F_0 PRE-step — it sees a sustained shift.
MEAS-2's frozen resp_S = mean(F[60:100]) − mean(F[200:240]) takes both
windows POST-step — a sustained shift cancels EXACTLY; only decay
between the windows registers. The 2981 power computation carried the
sustained scale across this statistic change underived, and the
survival-to-window premise it silently needed contradicts the theory's
own settle claim (T_BALL = 36 < 60 ⇒ early = late ⇒ D̄ = 0 PREDICTED).
So z ≈ 10.6 was computed against a scale the statistic is structurally
blind to: T-D-EXPECTATION-DEFECT. Corollary I did not anticipate until
the derivation was on paper: the 2983 domain discriminator divided two
D̄'s that are BOTH predicted ≈ 0 and measured ≈ 0 — its 2.080 is a
ratio of noise-scale numbers and carries no inferential weight. The
CONV-013 Q3 debate largely argued over an uninformative statistic;
T-B (the tail's own domain scaling) is the informative discriminator,
and this is why the disposition prereg defined it.

**Branch-semantics archaeology.** The same statistic change explains
the 2981 coverage gap: branch 1's "D̄ detected" clause was inherited
"2967-identical" from the pre-vs-post era, where detection meant the
instrument saw the sustained response. Under the post-vs-post
statistic, that clause is unsatisfiable under the theory's own
prediction — the branches could not cover the observed triple because
one of their conditions had silently become impossible.

**Result reading discipline.** T-A-SIG: the tail survives transient
exclusion essentially unchanged (1.576e-3 vs 1.570e-3) — the artifact
mechanism I stated falsifiably in the prereg is FALSIFIED; I record
that plainly. T-B: tail_dom = −1.712e-3 vs tail_std = +2.268e-3 —
SIGN INVERSION with the box; the 99% CI [−4.13, 0.74] excludes BOTH
R = 1 (domain-independent memory) and R = 2 (ballistic scaling). The
frozen class for excludes-both is ND, so the frozen mapping lands
DISP-C, and I enact DISP-C — I do NOT invent a post-hoc favorable
class, even though the sign inversion is (derivably) inconsistent with
a physical memory kernel, which cannot flip sign with box size. That
derivable fact is STATED in the record as a fact for the panel; its
interpretive weight is theirs to assign at the eventual round. The
temptation to upgrade "memory rejected at 99% by the pre-registered CI"
into a worker-declared DISP-A was real and is exactly what the frozen
mapping exists to block: the mapping was written before the look, and
it says MIXED.

**Economy applied to the next step.** DISP-C is neither a win nor an
impasse — the frozen mapping itself prescribes Route B. So NO panel
round convenes on this record now; it is filed with a dated line, and
the single round happens when the disposition RESOLVES (win, falsifier,
or genuine impasse). Route B design guidance the data just paid for:
≥3 domain sizes (the sign flip predicts structure in tail-vs-box),
longer legs (T_END = 240 gives only ~5–6 low-frequency bins; the tail
IS the lowest bins; resolution scales with leg length), paired design
retained, and a pre-committed effort bound + kill condition per
WORKFLOW rule 3(i) BEFORE work starts. Compute is founder hardware —
the design freeze waits on his one-line resource green-light (PD-006:
resource commitments are his).

# Reasoning fragment — Patch 2981 (W-4: K-MEM-MEAS-2 ensemble FRESH prereg)

Every adopted requirement from the CONV-011/012 cycle is placed and frozen: the worker's four (frozen N, seed-pairing, variance model, 2967-identical branch semantics), GPT's four (stopping rule = "N=512, period"; engine-fault-only blind exclusions; the reproducible seed-list audit with the generating seed 29810210 frozen in the document; bootstrap percentile CIs), Grok's two (the 2R_max domain-scaling discriminator on a preregistered 128-pair subset — ballistic transients scale with the box, memory doesn't; the frozen chatter-subtraction band tied to the L-3' admissible spectrum), DeepSeek's two (heteroskedastic block bootstrap; sigma_ctrl = 3.93e-3 written into the model as the instrument's self-consistency check), and Copilot's floor (512 pairs, identical paired streams, alpha/power stated with the conservative no-pairing-benefit bound).

Two design judgments worth recording. First, the power calculation is deliberately conservative — assuming pairing buys nothing, the ensemble still sits at z ~ 10.6 against the 2918 effect scale — which lets me preregister something unusual: UNRESOLVED-BY-FLOOR is reachable ONLY through an engine-consistency failure. That converts the branch from an escape hatch into a diagnostic: if the floor branch fires, the instrument disagreed with its own calibration, and the reading is "diagnose the instrument," never "move the threshold." Second, the passivity sign gets a SECONDARY-ANOMALY class rather than riding inside the falsifier branch, because B-1 discloses the sign as underived — treating an underived sign's violation as a falsifier would let the measurement punish the theory for a claim it never made, while silence about it would hide an anomaly; the flag class is the honest middle.

The freeze-before-code discipline is the whole point of doing this as its own patch: no driver exists, so no analysis choice can have been shaped by a peek. Execution is next in the lane queue alongside W-5 (E-1) and W-7 (the QM-1 audit gating AP-2), with the results and the C-5 confirmation going to the panel together — the promotion bar's two halves adjudicated in one round.

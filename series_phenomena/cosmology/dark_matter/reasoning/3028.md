# Patch 3028 reasoning fragment — MEAS-3 driver + analysis (verbatim capture)

**Kila6.** The founder answered the Route B resource question by
building a machine for it. Registered in memory (three-machine
convention; downloads /c/Users/drtho/Downloads/; repo convention
~/Documents/GitHub/CPP, non-OneDrive). Campaign targets Kila6.

**Cost model, from data.** MEAS-2's wall clocks are in the committed
JSONs: std (x=16, N_cp≈937) ≈ 1412 s/leg; dom (x=32, N_cp≈1801) ≈
5417 s/leg → cost exponent α = 2.06 in N_cp (all-pairs forces), linear
in T (fixed history window per Moment). My first sketch ({24,32,44} ×
128 pairs, T=480) priced at ~2127 CPU-h — infeasible; the quadratic
makes big domains the budget. Redesign kept the physics: the sign flip
is bracketed by 16 and 32, so {24, 28, 32} maps the interior of the
flip interval; 64 pairs retains ~28σ-equivalent sensitivity for a
56σ-at-512 effect; T=384 doubles nothing but gives 1.67× frequency
resolution within the geometry constraint (transit 36 → x_src0 = −18
→ the x=16 anchor is geometrically unreachable at long legs: at
T_END ≳ 344 the source exits a 16-half-width domain; margins at
24/28/32 are 6/10/14 ≥ MEAS-2's 5.2). Frozen total ≈ 721 CPU-h ≈
MEAS-2's actual scale.

**Design reasoning worth recording.** (1) One shared seed list across
domains makes pairs MATCHED across domains — the ratio CIs inherit the
paired-design variance reduction, same trick Route A's T-B used.
(2) CHUNK = 4 at n = 64 keeps 16 bootstrap chunks (2983's CHUNK = 8
would leave 8 — too coarse for 99% percentile CIs); frozen pre-look
with the deviation disclosed. (3) S3 is the campaign's self-test: the
T-D audit proved MEAS-2's post-vs-post statistic was blind to the
sustained response; MEAS-3 adds the pre-vs-post construction that CAN
see it, expected at +2.6e-3 — if it fails to appear, the frozen tree
routes to instrument diagnosis BEFORE any tail claim, closing the
class of expectation defects that produced the 2981 coverage gap.
(4) The disposition tree is total and ORDER-frozen (if/elif/elif/else),
with the conflicted memory-pattern-plus-geometry-location case falling
to IMPASSE rather than to either verdict — the Route A lesson (the
temptation to upgrade an excludes-both CI into exoneration) is now
structurally blocked in both directions.

**Driver.** Adapted from the committed 2983 driver with deltas only
where the prereg freezes them; pair-major/domain-inner task order so a
pause leaves balanced coverage; effort-bound extrapolation printed
from 24 completed legs (timing is not data); leg-atomic resume
unchanged; smoke mode exercises the x=24 geometry.

# reasoning/2669.md — S1b scale bridge (verbatim, at-patch)

STATUS: verbatim, captured at Patch 2669, 20 July 2026.

The bridge resolved by both charter routes agreeing, which is stronger
than either alone. Route (a): the lossless kernel coarse-grains trivially
— second moments compose additively, higher cumulants die as 1/n
(central-limit), and icosahedral symmetry blocks anisotropy through l=4
at EVERY nesting level of the self-similar 0736 hierarchy, so nothing can
accumulate across thirty-four orders of magnitude of nesting. Laplacian
in, Laplacian out. The consequence I registered as the load-bearing
sentence: any length in ℓ must come from the restoring (P2), never the
spreading (P1). The founder's P1/P2 separation is now an RG theorem of
his own kernel.

Route (b): the registry itself locates the response. The field is
per-vertex on the motif graph (I2), and all three registered restoring
candidates are MeV/fm-scale DP or bond properties; the 2665 grep already
established no Planck-level restoring exists anywhere in the registry.
The gap can only enter on-site at motif vertices — the LEVEL is settled
even before S1c settles the IDENTIFICATION.

The derivation's real yield is the operator's FORM. Because the restoring
is sited but the transport is continuum, the effective object is a
discrete-site scattering problem: M = I + χG with G the 1/r static
propagator between sites and multiple scattering captured by the
resolvent. I want the record to show the temptation I declined: the
obvious move was to write gap + nearest-neighbor graph Laplacian on I1 —
exactly the trap 2665 flagged, and I now see WHY it is a trap at a deeper
level than the scoping stated: a hop Laplacian would insert the edge
length into the kernel's second moment by construction, whereas the
derived M carries NO lattice-scale footprint — the 1/r couplings know
every pair distance and privilege none. Whatever length the S2 spectrum
decays with is genuinely an output.

Two honest disclosures frozen before any spectrum exists: distances are
4D chord distances of the registered polytope (2527 flag disclosed), with
a mandatory graph-geodesic robustness swap at S2 whose class-change
outcome would be reported as implementation sensitivity, not resolved by
preference; and the 120-vertex arena is compact, adequate for
lattice-scale decay, degraded near unscreened.

Instrument note: the first-run second-moment band (0.1%) tripped at n=4
on Monte Carlo noise — the band was AT the 2M-walker standard error. I
widened to 0.5% with in-file disclosure. This is an instrument-precision
correction; no physics quantity was moved, and nothing in the script
touches a gap, a length, or a candidate value.

Blind-guard self-audit: clean. S1c opens with a precise question — which
registered object is the on-site static polarizability χ of a Sea DP,
argued from the PCD cycle and the Sea's constitution only.

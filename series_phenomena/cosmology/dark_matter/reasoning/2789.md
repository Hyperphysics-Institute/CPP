# Reasoning fragment — Patch 2789 (X3 execution + frozen analysis)

**STATUS: verbatim, captured at patch. Session of 2026-07-23.**

## Running frozen machinery in a hostile scheduler

The environment reaps background processes between tool calls, so
the nohup pattern silently died and the first two synchronous
attempts hit the call ceiling with all progress lost (checkpoints
write only at chunk end). The recovery was mechanical, not clever:
measure the true sweep rate in both phases (equilibration ~5/s,
production with sampling ~4.4/s at N = 686), size chunks to ~270 s,
and grind. The frozen pipeline's checkpoint-and-resume design is
what made the grind safe; the one thing NOT done was optimizing the
verbatim sampler — a vectorization would have been a disclosed
deviation for zero evidentiary gain, and frozen means frozen.

## The gate upgrade is licensed, not a deviation

The inherited 2761 script carries the v1 five-move gate; the frozen
prereg mandates CONV-005 v2 by name. Upgrading the gate to 400
checks with the 1e−10 threshold, antisymmetry, and the mandated edge
cases is compliance with the freeze, not a change to it — and the
PASS lines (worst 8.4e−13 / 1.5e−14) are quoted in the record as v2
requires.

## Reading REPLICATED honestly

The verdict was mechanical: both new chains below DH, both within
1.3σ of their archived counterparts, pooled significance far past
the frozen 3σ line. The temptation is to headline "11.5σ anomaly."
The record refuses, for a reason worth preserving verbatim: the
frozen bootstrap yields per-chain errors ~3× tighter than the error
model behind the ledger's 2.56σ, and it measures deviation from DH
while the ledger measured deviation from HNC. Under the ledger's own
model, X3's contribution is roughly a √2 improvement from doubled
statistics — meaningful, not seismic. What X3 genuinely establishes
is REPRODUCIBILITY: the residual is a stable property of the clean
pipeline across seeds and chain lengths. Joined with requirement-7's
null in conjugate space, that points MORE toward an
extraction/profile-shape systematic, not less — a real physical
suppression that reproduces in r-space should have reproduced in
k-space too. Flag F-EM hands the error-model question to the panel
where it belongs, and the ledger number keeps PR1 until the panel
says otherwise. The instruments that can actually discriminate —
the window map, the two-component form test, the moving-feature
scan — are exactly what X4 runs next.

## PA-1 pays immediately

Both new chains carry per-sample S_zz (960 and 640 rows archived).
The next requirement-7-class comparison on THESE chains can run the
preregistered per-sample bootstrap that the rv2714 archive could
not — the panel's one substantive amendment closed the exact gap
DEV-1 exposed, within one session of being enacted.

# Reasoning fragment — Patch 2792 (X4 rung 0.005; ladder complete)

**STATUS: verbatim, captured at patch. Session of 2026-07-24.**

## The ladder ends on a cliff, and the record walks past it

F1 at 0.40 would, quoted alone, be the largest "screening anomaly"
this campaign has ever produced — and the rung record confines it to
a data table, because by now the window-edge hypothesis has a
concrete, checkable signature and the battery that checks it is one
patch away. Worth capturing the reasoning shape: the frozen F1
window inherits its lower edge from the softening parameter
(lo = 2a_s), so descending the ladder does not hold the measurement
fixed while varying one physical knob — it varies the knob AND the
instrument together. That entanglement was frozen into F1 by its
2735 authors for defensible reasons at fixed a_s, but across a
ladder it confounds. The committed window map exists precisely to
undo the confound: its r_min values are FIXED PHYSICAL lengths, so
tomorrow's map row at r_min = 0.08 fm compares like with like across
all four rungs. If the deficit at fixed physical window is flat
in a_s, the collapse is the window walking, not the physics moving.

## The census result matters more than it looks

Zero alternations across twelve chains at four softenings — the
MONOTONIC character is now established at Ewald grade over an
8× range in a_s. Whatever the F1 collapse is, it is not the
sign-staggered response PR2 guards against, and the
FG-STAGGER-PROXY-ARTIFACT Ewald leg stays restored with a much
wider footing than RV-4 alone gave it.

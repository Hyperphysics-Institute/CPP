# Reasoning fragment — Patch 2652 (C7-D3 execution)

**STATUS: at-patch verbatim. Verify: code/2652_c7_d3.py.**

The build decision that mattered: the prereg's "energy-vs-force consistency"
condition had to be assigned to a SAME-OBJECT pair or it would fire
ESTIMATOR-SPLIT vacuously (a time-averaged dynamical energy and a static force
are different objects). ssv_vectors turned out to have a closed-form potential,
so the static pair became EXACT — and it delivered: ≤0.8% agreement,
machine-precision antisymmetry, and an unrequested gift, a plateau-perfect
static curvature of −259 identical at both phases. That number reframed the
whole table: the dance estimator wasn't failing to find curvature because none
exists — a clean, small curvature exists — it was failing because a ~−80 MeV
amplitude-independent offset buries it under offset/x², which at x=0.01 reads
−1.4 MILLION. Watching λ_E decay monotonically toward −259 as the ladder
climbs is the closest thing to seeing the mechanism with the naked eye.

The clean-room classifier was written before looking at any stored value
(prose-only: significant when the mean exceeds twice the standard error), and
its 24/24 + BRANCH-N reproduction retires the "implementation accident"
hypothesis DeepSeek raised. The ±x sign disagreements at small x were
initially alarming until placed: when the symmetric excess is offset-dominated
and the odd (linear) term is comparable, one-sided estimates straddle zero —
another offset signature, not an instrument defect; disclosed rather than
smoothed. The scope fence in §2 exists because a static −259 on a frozen
scaffold is exactly the number a hostile reader would promote to "the ring is
unstable"; the record forecloses that promotion in the same breath that
registers the value. Verdict composition was mechanical: plateau fails
everywhere, ±x fails at multiple rungs — D3-NO-PLATEAU by the frozen text, no
judgment required, which is what a good prereg buys.

# Tier-4 reasoning fragment — Patch 3036 (E-1 v1.1: the amendment trio)

**Captured at-patch, producing window.**

**Amendment (2) was the round's real content; the walker design had
one decision that mattered.** The [COP] demand — an independent seeded
negative control — could have been satisfied lazily by re-running the
field-level A-2 engine with a new parameterization. That would have
been independent in seed but not in implementation, and the
rescue-by-reinterpretation charge survives anything that shares code
with the accused engine. The per-bit FCC walker shares nothing: new
code, stochastic where A-2 is deterministic, per-bit where A-2 is
field-level. That the deleted-origin arm landed at α = 0.483 against
2887's 0.478 — two implementations, one exponent, no tuning — is the
kind of convergence that ends an argument. The contrast arm (origin
retained, α = 0.994) lives in the SAME harness deliberately: a
negative control only means something if the instrument demonstrably
CAN show the positive.

**The dual record (amendment (1)) was written to resist its own
future misuse.** The temptation in any reclassification is to let the
new reading quietly overwrite the old registration — then the corpus
reads as if the engine had always been a control and the anomaly
never happened. [GPT]'s amendment blocks that, and the v1.1 text goes
further: Reading 1 is stated as having been "real" and "earned by
measurement," with the transition DATED at three patches. The
principle is the same anti-erasure discipline as the glossary chains;
what is new is applying it to an EVIDENTIAL status, not just a
definition.

**Amendment (3) is one paragraph doing perimeter duty.** The
within-Moment-only scope statement adds no new claim; it exists so
that a future citation of A-2's ±0.4% inverse-square cannot drift
into multi-Moment territory by ellipsis. It is placed at the head of
the classification AND in the table row, because tables travel
farther than prose.

**A restraint note:** ARM P's ballistic result is explicitly marked
as NOT new C-1 evidence — the directed relay already holds that
verdict, and double-counting a contrast arm as fresh evidence would
be exactly the inflation the panel polices. The harness's job was
discrimination, and the record credits it with only that.

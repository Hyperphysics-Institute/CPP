# Reasoning fragment — Patch 2967: the prereg for the seventh convergence's computation

**Why this is the measurement and not route (a)/(b) as first sketched.** T-3 §6 named the 2831
N3 routes; reading forward from 2831 found the 2873/2874 round had since restructured clause 2
(1A MET, 1B OPEN; K4's delta_mem force-ratio bar; OPEN-PHASE-THRESH-1; the enlarged transverse
prereg; the f-derivation critical path) and had killed a worker reconciliation four seats
ratified on assertion — with the standing rule that favourable convergences get a computation
before a vote. T-1's cancellation structure is favourable convergence number seven, so the
K-MEM measurement Thomas authorized is executed AS that computation: a step-response kernel
probe on the committed mobile-Sea engine, with the falsifier branch loaded against the
worker's own theorems.

**The domain-scaling leg is the design's teeth.** A ballistic transient's support is geometry;
a memory kernel's support is physics. Two domains, one frozen ratio band — if settle time
tracks 2R_max, the transient is the relay's transit and T-3 §6 stands at toy grade; if it
sits still while the domain shrinks, that is Sea memory and B-INT/B-TAIL takes it. This is S1
spec item 5 (transient separation) implemented as an instrument rather than an argument.

**What was deliberately kept out.** No oscillation/phase leg (OPEN-PHASE-THRESH-1 has no
frozen threshold to judge against — measuring it invites the adjudication this record must not
perform); no physical-d_DP language anywhere near a number; no evidentiary credit
self-assigned (the Darwin-precedent treatment from 2874 §5 applied to my own record). The
floor clause (UNRESOLVED-BY-FLOOR at S < 3 sigma_ctrl) exists because the 2918 persistent
static core is larger than the motion response and the chatter is real — a step that drowns
is reported as drowned, not retuned.

**Order discipline.** This file is committed before one line of driver code exists. The
execution patch inherits §5 verbatim and applies it to the data as-is.

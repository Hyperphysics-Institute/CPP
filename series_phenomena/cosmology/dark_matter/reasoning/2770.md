# Reasoning — Patch 2770 (C2R-L1 execution)

**STATUS: verbatim, at-patch.** The leg landed exactly where the
prereg predicted, and the prereg's pre-declaration is what makes
D1 = 0 reportable with a straight face: the algebra kappa^2/(4 pi n)
= a/(pi sqrt2) is three lines, but the DERIVATION content is the
license to homogenize through r < a, which did not exist until the
founder ruling. The satisfying surprise was in step 4: the
decomposition did not merely bound the L4 record's 59%/22% prose —
it reconciled it exactly once the denominator convention was
identified (22% of S_cont vs 53% of the outer continuum), and the
ledger closes to four digits: -59.40 + 21.47 -> 1/1.6110. That
closure converts the old J3 dispute into arithmetic: the 1.611
factor was never one effect but two of opposite sign, and the
ontology adjudicates them differently — the big one rejected with
its premise, the small one handed to L2 as the real physics. L2's
job is now sharply bounded: the uniform-chi closure is exact at
leading order BY DERIVATION; the correction lives entirely in the
near-site profile (superposed gradients, per the founder), and its
natural scale is set by the +21.5% discreteness structure — though
its net effect on ell must be computed, not scaled, since the
operator readout weighs the profile through the staggered
evanescent mode, not linearly. One restraint worth recording: it
was tempting to quote a delta-ell estimate from the +21.5% figure.
Refused — that number is an input-side decomposition, and mapping
it to the readout without the chi(r) computation is exactly the
kind of plausible shortcut the frozen-leg structure exists to
prevent.

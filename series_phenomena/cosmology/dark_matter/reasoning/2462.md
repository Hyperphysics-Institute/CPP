# Reasoning — Patch 2462 (Computation #2 EXECUTED: the registry priced BOTH WAYS in one bond model — Q5 settled by computation, H2 closed; hypothesis (ii) ring-native s₀: ring survives EVERYTHING; hypothesis (i) straight-registry s₀: no >3σ kill at central E_qq but adverse corners; the pre-registered SURVIVE branch fires at its letter → verdict ~70% (band low end), PROMOTION DECISION now OWED to the founder with the panel record; the s₀-reference question and the E_qq pin are the two named residual pins)

STATUS: verbatim, at-patch (Opus, DM lane, 14 July 2026). Trigger: the 2460 handover
gate, item 2. Layer C. Artifact: `code/2462_registry_both_ways.py`.

## Gate compliance (CLONE-FIRST)
Registry grepped for a better-registered stiffness form before computing. Finding: the
2443-registered bond form V(r) = −A_eff/r + K/r² has curvature at its own minimum of
exactly V″(r*) = 2|V(r*)|/r*² = 2E_qq/s₀² — the commission's default coincides with
the registered form. Harmonic primary; Morse (a = 1/s₀, identical curvature at s₀)
run as the anharmonicity sensitivity, which matters here: the ring's inner-edge bonds
compress ~20% (computed native inner = 0.918 fm vs 1.15; H2's quoted 0.83 was at the
qCP-corner radius — the same physics, the computed bond graph is the operative one)
and Morse is stiffer on compression.

## The model (exactly as commissioned)
Axial same-corner qCP nearest-neighbor bonds: (plane k, corner c) ↔ (plane k+1, c),
4 bonds/junction; rod 15 junctions (open), ring 16 (closed — the closure junction is
4 real bonds with no rod counterpart). Depth E_qq mapped {40, 66, 100, 170} (central
66). TWO s₀ references, both run: (i) s₀ = 1.15 straight-registry — the ring pays the
inner-edge compression; (ii) s₀ = local ring-native lengths — the ring is the
REGISTERED native structure, the rod pays instead. Mode curvatures by exact finite
difference (±amplitude) on the scaffold — no MC noise; netted against #1's dance
curvatures (inverse-variance combined over seeds: m=0 +218±95, m=1 −90±71,
m=2 −304±56, ellipticity +280±610; ring−straight −70.4±4.6).

## Bookkeeping honesty (surfaced during the run, displayed rather than chosen)
At central E_qq = 66, the bond depth nearly equals the dance's own soft-core qq
contact Coulomb: α_s·ħc/√(d²+a_qq²) = 55.6 MeV. So "add the bond depth to the dance
energy" partially double-counts the attraction the Coulomb sum already prices — and
the near-equality is itself flaggable: the 2441 central E_qq estimate may BE the
contact Coulomb in disguise, in which case the registry depth beyond Coulomb is small.
Ring−straight therefore reported in THREE bookkeepings: LETTER (full depth, per the
commission), STRAIN-ONLY (conservative; no depth at all — arguably over-conservative,
it prices the closure junction's four real bonds at zero cohesion), and
DOUBLE-COUNT-CORRECTED (depth E_qq_eff = E_qq − 55.6, the correction anchor derived
from the dance's own regularization, not tuned). Mode curvatures are
bookkeeping-INDEPENDENT (depth is constant per bond; bond count does not change under
tilt/ellipticity) — the tilt-wave verdict does not depend on this question.

## Results
**Hypothesis (ii) — ring-native s₀: the ring survives EVERYTHING.** All modes
positive-net over the full E_qq map and both bond forms (central: m=0 +218±95,
m=1 +86±71, m=2 +373±56, ellipticity +13,467±610); no >3σ instability anywhere;
ring−straight favorable under all three bookkeepings (−177 to −1204 MeV).
E_qq-independent modal survival (the ring is unstrained at its own reference; only
the rod pays).

**Hypothesis (i) — straight-registry s₀: survives at central parameters; adverse
corners named.** At central E_qq = 66: NO >3σ kill (m=0 −118±95 = −1.2σ; m=1 −103±71
= −1.5σ; m=2 rescued to +124±56 harmonic / +197±56 Morse; ellipticity +10,769).
The mechanism of the m=0 pull-down is H2's point operating honestly: uniform tilt
RELIEVES the pre-strained inner-edge compression, so the strained-reference registry
DESTABILIZES m=0 while stabilizing m=2. Corners: E_qq ≥ ~100 kills m=0 at >3σ
(−3.1σ/−3.7σ at 100; −6.8σ/−7.9σ at 170) — under (i) the parked E_qq pin becomes
load-bearing; E_qq = 40 leaves m=2 zero-consistent (−0.8σ/−0.0σ). Ring−straight:
favorable at the LETTER everywhere (−133..−336); adverse STRAIN-ONLY everywhere
(+27..+344); DC-CORRECTED forks within the map (+48.8 at 66 → −4.3 at 100 → −113.6
at 170).

**Q5 settled by computation; H2 closed.** Gemini's hit is vindicated as a real
channel: priced consistently, the registry both rescues m=2 AND charges the
compression — under (i) the two effects land on different modes (m=2 up, m=0 down),
which no shielding argument could have anticipated. DeepSeek's ×35 refutation does
not survive the computed bond graph. The registry-dominance question is now a
computed landscape, not a framing dispute.

## Verdict per the pre-registered branches (2457, binding)
The kill branch does NOT fire: at the commission's letter (bond model added to the
dance energy; both observables recomputed together), no ring mode is unstable >3σ at
the central E_qq under either s₀ hypothesis, and ring−straight is favorable under
both hypotheses. The SURVIVE branch fires at its letter: **the ring survives
computations #1 and #2 → the verdict returns to the pre-registered band, taken at its
LOW END ~70% (not 75%)**, the discount recorded for the hypothesis-(i) contingencies:
(a) under (i), E_qq ≥ ~100 is a >3σ m=0 kill and the E_qq pin is parked/open;
(b) under (i), the conservative bookkeepings flip ring−straight sign at central E_qq;
(c) the near-equality E_qq(central) ≈ contact-Coulomb is unexplained. Under (ii),
none of these contingencies exists. **Per the same pre-registered branch, the
PROMOTION DECISION now goes to the founder with the full panel record attached.**
Registry NOT promoted in this patch; Ω_DM stays PARKED pending that decision.

## Named residual pins (in order of leverage)
1. **The s₀-reference adjudication** — (i) vs (ii) is the registered counter-
   consideration from 2457 ("the ring is the REGISTERED native structure; the bond
   model decides"). The computation has now displayed both, as commissioned; the
   choice is a physics question about what the registry registers (the as-built
   structure vs the straight-lattice ideal). Founder input and/or a targeted CONV-001
   question can settle it; under (ii) everything survives, under (i) survival is
   central-parameter-conditional.
2. **The E_qq pin** (parked; SF-2/SF-5 edge-bond route; three estimates converge on
   the deep branch) — load-bearing only under (i).
3. Conditional supersession enumeration (2458) is now ENACTABLE at the founder's call
   (the dance reading held through #1/#2), including the conditional MOOTING of the
   original pre-session E_bond-convention CONV-001 question.

## Next
Founder decision items: (1) promotion of candidate (B) to the registry (panel record
attached; pre-registered branch satisfied at its letter); (2) the s₀-reference
question; (3) enactment of the 2458 conditional supersessions. Optional third-seat
items remain (Grok fresh run; Copilot fresh re-run). Ω_DM unparking is downstream of
(1).

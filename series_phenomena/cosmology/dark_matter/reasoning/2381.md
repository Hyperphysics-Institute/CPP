# Reasoning — Patch 2381 (Q3b-2a: the grounding pass)

STATUS: verbatim, at-patch (Opus, DM lane, 9 July 2026). Warm-launch DM-WARM-2381
executed as registered at 2380: handover consumed = arc doc §Q1→§Q3b-2 +
`code/2379_unit_cache.json` + `conv001_2026-07_q3b1_returns_adjudication.md`.
Clean clone, CLONE-FIRST GATE honored (registry grepped before any computation;
no new IDs registered this patch).

## 1. What the grounding pass was contracted to do (2380, verbatim scope)

"Q3b-2a — grounding pass: 0861/0881 kinetics machinery re-run on clean clone;
the ring-closure bending-cost statement extracted and its N_c implication
computed; the derivation contract's verify battery fixed."

All three items executed. Artifact: `code/2381_q3b2a_grounding_ringclosure_Nc.py`
(exit 0 iff battery green; ran 6/6).

## 2. Re-runs (item 1)

Both registered scripts executed unmodified on the clean clone:

- `0861_formation_kinetics_loop_distribution.py`: SY closure peak u* = 3.37
  (stored 3.37); ℓ_p band [105, 702] fm (stored); representative point
  N_peak = 820 at ℓ_p = 300 fm (stored); two-temperature ratio ≳ 7.2 (stored).
- `0881_freezeout_kinetics_Ndwarf.py`: φ bracket 6.7e-15–7.4e-10 (stored);
  inverse-map band E_bond/kT_form ≈ 24–41 (stored); closure table all-YES at
  kT_form ≤ 19 keV (stored); velocity split reproduced.

Additionally the 2374 demand-sheet D1 small-N band was independently
recomputed from the 0881 inverse map: survivors N = 3–6 with the φ bracket
give [23.2, 36.2] — exact match to the registered D1. The old and new
machinery agree where they overlap.

## 3. The bending-cost statement (item 2)

Extracted from the registered 0861 J-factor g(u) = u⁻⁵ exp(−14.054/u + 0.246u):

    E_close(N) = c · (ℓ_p/ℓ_rung) · kT / N,
    c = 14.054 (SY fluctuation-corrected) or c = 2π² = 19.739 (rigid).

Cross-checked exactly: the discrete N-gon with quadratic joint stiffness
B = (ℓ_p/ℓ_rung)kT gives N·(B/2)(2π/N)² = 2π²(ℓ_p/ℓ_rung)kT/N — identical in
form to the continuum rigid ring to machine precision (battery V4), so the
continuum formula is trustworthy down to N = 3 at the rigid coefficient; the
SY coefficient is the fluctuation-corrected saddle (ratio 0.712). Both are
carried; neither chosen to help.

## 4. The N_c implication (item 2, the physics)

Four computed statements, one of them against the grain:

1. **Structural floor.** A non-degenerate closed polygon requires N ≥ 3;
   N = 2 closure is a doubled-back degenerate configuration (two π-bends,
   zero enclosed area). IF the frozen stable species is the closed ring
   (closure eliminating both reactive ends), THEN w(1) = w(2) = 0
   identically and **N_c = 3 emerges from topology, not imposition** — and
   the D2/D3 contamination budgets (w(2) < 0.034, w(1) < 0.013 at the live
   corner) are met identically rather than marginally. Conditional carried
   loudly: "stability = closure" is Q3b-2b's to DERIVE; a residual
   open-chain population at freeze-out re-arms D2/D3 numerically.

2. **Energetic window.** Closure trades one extra bond against E_close.
   With D1's E_bond/kT_form ∈ [23.2, 36.2], closure at N = 3 is favorable
   for ℓ_p/ℓ_rung ≤ 3.53–5.50 (rigid) / 4.95–7.73 (SY). Roomy.

3. **Peak placement — the stiffness demand inverts.** Landing the closure
   peak N* = 3.37(ℓ_p/ℓ_rung) at the corridor survivors N ∈ [3, 6] demands
   **ℓ_p/ℓ_rung ∈ [0.89, 1.78]** — fm-scale at the registered ℓ_rung = 1 fm,
   i.e. ×59–×788 SOFTER than the 0861 large-loop era's [105, 702] fm. Same
   single substrate object (the 2eDP:2qDP rung-bond SSV angular stiffness);
   the corridor inverts the demanded band. The placement band sits inside
   every energetic-window ceiling (tightest 3.53 > 1.78): no D1 tension.
   **Named kill condition for Q3b-2b:** the derived stiffness must land in
   [0.89, 1.78] ℓ_rung or the ring mechanism dies at placement.

4. **The ceiling is NOT free — the preview's honest surprise.** The
   equilibrium-weighted ring population exp(−N/⟨N⟩)·J_SY does NOT suppress
   the N > 6 tail across the placement band: marginal ×1.0–1.2 at the soft
   end, ANTI-suppressed ×0.4–0.7 at the stiff end (the J-factor still
   rising through N = 3–6 when the peak sits at 6). I wrote the closing
   summary before printing the grid, and the grid refuted it — the text was
   corrected to state what the numbers show (the 2375 pattern: the
   machinery catching its author; correction owned here, in-session,
   pre-commit). Consequence: if a ceiling above N ≈ 6 exists it must be
   **kinetic** — a closed ring has no reactive ends, so ring growth stops
   at closure and rings exit the growth ladder; the frozen product is set
   by closure-vs-growth competition on OPEN chains, an ensemble the
   equilibrium weighting cannot represent. That kinetic-closure shutoff is
   now Q3b-2b's named ceiling candidate, with the registered death mode
   ("equilibrium shape emerges anyway") fully armed against it.

## 5. The verify battery, fixed (item 3)

Binding underneath all Q3b-2b/2c work; the 2381 script IS the battery:
V1 SY peak (tol 0.01); V2 0861 ℓ_p band (tol 1 fm); V3a 0881 band /
V3b D1 small-N band (tol 0.1); V4 discrete–continuum bending identity
(machine precision); V5 `2379_unit_cache.json` integrity (336 keys, schema
N,sign,S_c,ε_th ∈ {1, 0.5, 0.25, 0.1, 0.02}, 13 non-negative bins). The
semantic cache cross-check (ε_th = 1 reproducing stored 2366b
viol = 3/642.219095) lives in the summed-criterion machinery and is
re-asserted when that channel runs in Q3b-2c — deliberately not duplicated.

## 6. Scope discipline

No verdict moved. No registry/THEO/OPEN edits. No .tex touched. No smuggled
parameters: every number is a stored value or derived from one; the two
bending coefficients are both literature-standard forms of the same
registered object. The SY validity boundary (u ≲ 6) binds the tail preview
and is carried where it binds. Writes confined to `dark_matter/code`,
`dark_matter/reasoning`, and the arc-doc §Q3b-2a append.

## 7. Handoff to Q3b-2b

Q3b-2b now has: a green battery to stand on; a named placement kill
(ℓ_p/ℓ_rung ∈ [0.89, 1.78]); a named ceiling mechanism to derive-or-kill
(kinetic closure shutoff); and the floor mechanism's conditional to
discharge (stability = closure, ring:chain freeze-out ratio). The
nucleation-and-growth computation must let N_c ≥ 3 and the ceiling EMERGE
from registered SSV/PCD primitives — the floor already does structurally;
the ceiling is the live question.

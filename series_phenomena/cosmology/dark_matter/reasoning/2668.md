# reasoning/2668.md — S1a kernel formalization + GATE G1 (verbatim, at-patch)

STATUS: verbatim, captured at Patch 2668, 20 July 2026.

The formalization had exactly one live degree of freedom to close, and it
closed itself. Range: A3′ says the broadcast reaches the PSR shell per
Moment — that IS the range; nothing to choose. Radial weight: "flat per-hop
transport" + "same icosahedral shell-sum" is a uniform shell weight by the
axiom's own words. Normalization: the founder's capture says the fading is
inherent in the spreading — spreading, not absorption — so any per-hop loss
factor would smuggle P2 into P1 and violate the charter's frozen
separation. The signal-conserving (doubly stochastic) normalization is
therefore forced, not selected. The kernel is the uniform normalized
PSR-shell average, and the static field is the resolvent (I − K)⁻¹s exactly
as the charter's §1 anticipated.

The G1 anxiety I carried into the derivation was the diffusive-vs-ballistic
worry: a stochastic shell-average iterates like a random walk (width √n·a),
while the founder's picture is ballistic shells (radius n·a). I checked
whether this breaks the gate. It does not, and the reason is worth
preserving: G1 is a STATIC statement, and the statics of any lossless,
signal-conserving, finite-range isotropic propagation in 3D is Poisson's
equation regardless of whether the dynamics is first-order (diffusive
resolvent) or second-order (wave). The shell-average expansion gives
(I − K) → −(a²/6)∇² either way. The scalar superposition from a point
source is then 1/r — the registered potential channel — and SSV_net, the
vector sum that by isotropy aligns with the gradient, is 1/r² — the
registered force channel. The founder's "each individual signal dilutes as
inverse-square" is the flux reading; the resolvent's accumulated 1/r with
1/r² gradient is the potential reading; both land the force-relevant
vector at inverse-square, so the gate's verdict is reading-independent. I
registered this as the two-readings note rather than silently picking one.

The structural gift of the session: icosahedral symmetry has no invariant
rank-2 traceless tensor AND no invariant l=4 harmonic — its first
nontrivial invariant is l=6. So the emergent continuum operator is
isotropic not just at leading order but through O(a⁴∇⁴); shell-placement
anisotropy cannot touch the inverse-square until sixth derivative order.
G1 is symmetry-protected, not tuned.

Instrument: FCC (z=12) periodic box as the 3D icosahedral-coordination
proxy — deliberately consuming only z=12 + second-moment isotropy, the
same properties I1 pins, with the 2527 4D→3D flag noted. Preregistered
bands before running: p_f = 1 ± 0.10, p_g = 2 ± 0.15, window r ∈ [3,8],
L=48. Result p_f = 1.0010, p_g = 2.0640, ALL PASS, first attempt. The
gradient exponent sits 0.06 high — finite-window curvature from the
periodic images; inside band, no retuning performed, none permitted.

Blind-guard self-audit before commit: no gap parameter anywhere in the
patch; no screening length; no decay-vs-parameter curve; neither candidate
value named, computed, or compared. The stage is clean. S1b opens.

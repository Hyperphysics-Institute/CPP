# Reasoning — Patch 2606 (freezing the routes before the number can tempt anyone)

STATUS: verbatim, at-patch.

The convention pin (C1) deserves its confession up front: the Newtonian record gives
0.74/0.68 and one line of formula, and there are two masses the formula could carry
(reduced m/2 for the relative coordinate, or the per-CP 132). I reconstructed the
registered convention by arithmetic — reduced mass reproduces the recorded points at
0.737/0.682, the per-CP mass does not — and rather than silently adopting the winner,
the prereg makes the reproduction a CONTROL that must pass and prints the loser. If
the reconstruction is wrong, C1 fails loudly and the campaign stops before any γ
number exists to anchor on.

The τ_C-not-dilated line is the prereg's most consequential frozen choice, and it is
not mine: the H-γ instrument already applies the sink per Moment cycle at fixed τ_C —
the Sea's clock, not the particle's. The analytics must match the instrument they
feed, and the assumption is named so a substrate ruling can overturn it by citation
rather than by archaeology.

The binding cap fell out of the setup before any computation ran: a pair well of
~123 MeV cannot host bound oscillation above γ_char ≈ 1.47, while the registered
transits run 1.68–1.90. So the re-derivation's honest domain is the bound-oscillation
regime the sink was validated on, and the transit regime routes to the capture-edge
cell where it belongs. Better to draw that scope line at prereg than to discover it
mid-execution and be suspected of drawing it where the numbers were kind.

My private expectation, recorded so the reading cannot drift toward it: the R-A2
floor should DROP under γ (relativistic energy is sub-quadratic in momentum, so a
decorrelated return destroys less coherent energy), and the R-A1 interior may RISE
(period lengthening walks x down the falling branch of the lag curve toward its
maximum). If both happen, the honest outcome is R1γ on the floor and R2γ′ on the
interior simultaneously — the readings are written so that mixed outcome has a name
instead of a negotiation.

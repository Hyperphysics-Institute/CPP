# 1852 — Finish the geometry number before the panel: register OPEN-SS-41a and hand off the (f, n*) last-mile

**STATUS:** verbatim, at-patch (Opus, DM 18xx lane, 30 June 2026). A sequencing decision (founder-set) plus the
sub-item registration and hand-off it implies. **Frontier registry edit + handover.** Verify: none. **Layer: C.**

## The sequencing decision (agreed)

Founder call: **pin ΔG*_nuc (the f, n* last-mile) before panel round 2**, not after. I agree, for two reasons:
1. **Tactical:** an adversarial panel handed a "reachable but not pinned" dwarf branch will predictably return "go
   pin it" — the review spends its value telling us what we already know is the next step.
2. **Substantive (the stronger reason):** a *bracket* with the natural reading mid-window is exposed to a "you're
   fitting the target" critique; a *pinned* ΔG*_nuc computed from the element cross-section is a genuine
   zero-parameter prediction the panel must engage on its merits. Submitting pinned converts the dwarf branch from
   "plausible" to "prediction (or clean verdict)" — a materially stronger submission.

So: finish the number, then panel. Panel round 2 stays held.

## What I did

- **Registered OPEN-SS-41a** (`frontier_sectors/SS.md`) as a **sub-item of OPEN-SS-41** (founder's chosen form):
  derive f (eCP-coat spine-face solid-angle selectivity) and n* (critical nucleus) from the 8qCP+8eCP element
  cross-section → pin ΔG*_nuc = (n*−1)·|ln f|·kT_form. Win: pinned ΔG*_nuc ∈ [6.0, 6.9] kT (dwarf prediction);
  kill: ≲4 or ≳9 kT. The first-pass most-natural reading (n* ~ 4, f ~ 0.11) already sits in-window, so this is a
  pinning-and-confirming calc, not a fresh search.
- **Handed the last-mile to the strong/geometry lane** (2200-series, opening patch **2211**): derive (f, n*) from
  the element cross-section → pinned ΔG*_nuc → hand N_form back to DM. It is SS-2-adjacent (the element/nucleon
  cross-section kit), the same lane as OPEN-SS-41 — not DM microphysics, so I hand it off rather than estimate it.

## Boundary note

This is the geometry lane's number (element cross-section geometry), not the DM lane's — I registered the sub-item
and framed the target (as OPEN-SS-41's consumer and registrant) but did not compute f or n* myself. DM consumes the
pinned ΔG*_nuc → N_form → dwarf σ/m(v); the panel runs after that.

## Anti-priorities held

Agreed on the reasoning, not deferentially — and supplied the substantive reason (bracket invites a fitting
critique) beyond the tactical one; respected the lane boundary (registered + framed, did not compute the geometry
number); kept panel round 2 held rather than running it early. **DM-1 v1.0; CONJ-COSMO-1 founder-gated.**

## Next

Geometry lane pins (f, n*) at 2211 → ΔG*_nuc pinned → DM consumes N_form → **then** panel round 2, with cluster =
parameter-free prediction and dwarf = pinned prediction (or clean verdict). The candidate is one geometry number
from a full, submittable σ/m(v) statement.

# R-B items 2–4 invariance pre-registration: the 2513 mode ensemble, the 2549 ENDBOND-2 fragment, and the 2510 pile-contention statistics under reach-S — verdicts frozen before any run

**Patch 2634, 20 July 2026. Status: R-B items 2–4 OPENED at pre-registration; NO
run performed.** The last founder-free items (2574 §5, order fixed). All three are
INVARIANCE re-runs of registered old-classifier results under reach-S (the 2556
founder adjudication), the 2574 §4 duty-re-run pattern: numbers may shift; the
frozen question is whether the REGISTERED READING survives. Overrides bound in the
chain namespace with a binding check (the 2574(c) lesson) in every item.

## Item 2 — the 2513 mode-curvature ensemble (C7-supporting)

Machinery: exec-load of the registered `code/2513_ensemble_mw_modes.py` whole
(driver skipped), `build_reach` overridden to reach-S (plane-membership in-plane,
the 2557 form) in the loaded namespace, then the registered driver re-executed
verbatim — 28-member ensemble, dt = τ_C/{50, 25}, the m2 amplitude diagnostic,
and the MECHANICAL branch reading, all as committed at 2513.
- **Binding control (gating):** base ring Etot under OLD vs reach-S must DIFFER at
  dt = 1/50 (75% of qCPs re-classify on the L = 16 ring; identical energies would
  mean the override did not take). The scaffold ψ = 0 assert (2513's own) must
  pass.
- **Registered reading being tested:** BRANCH U — decorrelation not resolved (m1,
  ell unclassifiable; m2 SIG-NEG both dt: −5213 ± 2488 / −7849 ± 2260; amplitude
  diagnostic outside the quadratic regime, ratios 0.08/0.03 vs ~4 — the O(10 MeV)
  chaotic floor); C7 stands.
- **RB2-INVARIANT:** the mechanical branch print yields BRANCH U again → C7's
  status and the leg-based operative record are UNCHANGED under reach-S; dated
  line, forward.
- **RB2-NOT-INVARIANT:** BRANCH D or BRANCH N obtains dt-mechanically → a
  supersession question opens on a registry condition; **founder-disclosure
  trigger** (Branch N carried panel-flash grade at 2513); nothing re-registers
  tonight — the raw result and the trigger are recorded, the founder decides the
  dispatch.
- **RB2-UNSTABLE:** the mechanical reading is internally inconsistent (a flip
  pattern the 2513 rules do not classify) → recorded raw; finer cell declared.

## Item 3 — the 2549 ENDBOND-2 fragment (null-closure; low stakes)

Machinery: exec-load of the registered `code/2549_endbond2_dance.py` (constants,
scaffolds, dance verbatim), `build_reach` overridden to reach-S, the registered
union grid (3 dt × 2 FREF) re-executed.
- **Structural-equality census (printed first):** reach lists OLD vs reach-S
  compared element-wise on all three scaffolds. On flat fragments the old
  Δz < 0.5D rule may coincide with plane membership BY CONSTRUCTION (planes at
  D > 0.5D). **RB3-INVARIANT-BY-STRUCTURE:** lists identical on every scaffold →
  the re-run is bitwise the registered run; Branch I ([−3.6, +1.4] spans zero)
  stands with a structural proof, the cheapest honest discharge.
- If lists differ anywhere: **reproduction control** (OLD member reproduces the
  registered band [−3.6, +1.4]) gates; then **RB3-INVARIANT** iff the reach-S
  union band again spans zero (Branch I); **RB3-NOT-INVARIANT** iff the band
  clears zero dt-stably in either direction → the null becomes a depth claim or
  an adverse — recorded, gates re-read per the 2549 frozen order, founder
  informed at next contact (a registered null changing class is disclosure-grade).

## Item 4 — the 2510 pile-contention statistics (never separately characterized)

The 2510 hardened dynamics resolve some arrivals via the pile-contention clause
(superposition at qCP targets). The headline was re-registered under reach-S at
2557; the CHANNEL'S USAGE STATISTICS were not — this cell characterizes them, both
classifiers side by side, at the registered headline configuration (ring L = 16 +
straight rod, κ pinned ×1.0, dt = 1/50, TC = 60).
- Instrumentation: a logging-only counter on the pile clause (hits resolved via
  pile / total hit resolutions, per scaffold, per classifier); dynamics untouched;
  the counter wrapper must reproduce the un-instrumented ⟨Ep⟩ on the OLD ring
  exactly (convention-pin, gating).
- **RB4-INVARIANT:** the pile channel is ACTIVE under both classifiers and its
  usage fraction agrees within a factor of 2 between OLD and reach-S on both
  scaffolds → the hardened mechanism's statistics register (first
  characterization) with the invariance line; dated line, forward.
- **RB4-NOT-INVARIANT:** the channel changes CLASS under reach-S (collapses
  toward 0 or changes by > 2× on either scaffold) → recorded raw; a
  mechanism-relevance question attaches to the promotion runs' interpretation and
  is queued for the next dispatch's disclosure package. No registration moves
  tonight either way.

## Fences (all items)

79.5% untouched; the 2557 re-registered headline untouched; C7's registry text
untouched tonight under every reading (movement, if any, is a founder-dispatched
act); the duty record (2514/2574) untouched; no rates, no relic contact.

## Bookkeeping

Next patch: execution + record. Verify script `code/2635_rb234_reach_s.py`,
stages i3 | i4 | i2a (dt = 1/50) | i2b (dt = 1/25 + diagnostic + branch),
foreground-chunked with cross-stage persistence for item 2's two-dt branch read.

# Review Package — OPEN-DM-CONTINUOUS-PCD-1 HARDENED v1.0 (Patch 2510) — the deferral gaps closed by computation; the promotion re-vote

## §0 Identification
**Programme:** Conscious Point Physics (CPP). **Artifact:** DM hardening probe, Patch 2510 — the two model-faithfulness completions your July round left unreviewed (SF-6 inertia; full qCP-target contention), added to the founder's Momentwise dance with the inertia coefficient pinned by an ISOLATED prior investigation (Patch 2496), never fit. **One line:** does the ring preference survive when the dance integrator is given the sea's memory and the core's many-toward-one contention — with everything pinned in advance? **Responses land in:** `series_phenomena/cosmology/dark_matter/review/reviews-CONTINUOUS-PCD-1-HARDENED.md`.

## §1 Context (what happened since your last return, in one paragraph)
Your four returns to the v1.0 re-ask (Patch 2468 package) CLEARED the §5 reading as-asked — 4/4 R4 promote or promote-conditional-with-record-satisfiable-conditions, no sustained R2, four-seat median 76.5%. Promotion was NOT executed. It was deferred (Patch 2470, soundness escalation, not a governance veto) because the package had not put two model-faithfulness items to you: **(i) SF-6 inertia** — the dance was memoryless (v ∝ |F| each Moment), which CPP's own electromagnetism contradicts (an accelerating CP stores DP-sea magnetic energy), and this memorylessness is the signature cause of the mode-curvature decorrelation disclosed as C7; **(ii) full qCP-target contention** — the dance rebounded on "someone got there first" only for eCP targets, under-counting the core many-toward-one pile-up. The founder-directed path: pin the inertia coefficient in ISOLATION first (done — Patch 2496: κ = (2/3)U_pol/c² open-cloud; on the Patch-2452 in-situ masses with Laue coefficient-1, **κ_q = 132, κ_e = 44 MeV/c²**; retardation lives on the back-reaction at O(σ/c) ≈ 5%, absorbed in the scan envelope), THEN run the hardened probe with that pinned coefficient and re-present. Governance note: founder delegation is now standing (PD-006) — on clean returns the v1.0 §5 consequence executes at the next patch with NO further founder sign-off.

## §2 The claims (each labeled with its basis)
- **C1 (SCRIPT result, disclosed failure):** A signed 1-D velocity on the rotating commitment axis (the minimal-looking way to add inertia to the choreography) is a **parametric pump** — energy audits show +27..+76 MeV per-step gains even with an instantaneous drive, ending in Coulomb-explosion states. The reduction is energetically ill-posed with inertia; retired, not tuned.
- **C2 (SCRIPT result, disclosed failure, independently informative):** Fully UNSCRIPTED 3-D inertial damped PCD (no home-anchoring; contention automatic) **collapses pairwise** — opposite-charge soft cores have their potential minimum at r = 0 — and evaporates the structure (E_pot → +11.5k..+24k, amplitudes 11–15 fm). This is an independent empirical re-confirmation of YOUR OWN v1.0 reading-(B) finding: **home-anchoring is load-bearing specification (2454), not a numerical device.** A registry-tether force substitute would require an unreviewed E_qq/s₀ pricing choice — G7-barred mid-probe; registered as follow-up territory, not smuggled in.
- **C3 (MODEL, the hardened dance v8):** The promoted reading-(D) choreography kept EXACTLY (home-anchored, founder reach sets, eCP preemption, direction always toward commitment) + **speed memory**: κ dv/dt = |F_net| − v/μ (scalar speed, 0 ≤ v ≤ c, exact exponential integrator; μ = 1/F_REF, the pin's Stage-C drain; speed CARRIES across commitment switches — sea-stored momentum) + **pile contention**: an OUT leg ends on superposition with the target OR with any CP already superposed AT that target (many-toward-one stacking counted; the native scaffold is not self-colliding). **κ→0 recovers the memoryless v1.0 dance EXACTLY** — nesting verified in-run: ring−rod = −132.3 vs your reviewed −132.1 at dt = τ_C/50 (the 0.2 MeV is the pile-contention rule, active at κ=0).
- **C4 (SCRIPT result, HEADLINE):** At the PINNED coefficient (×1, never fit): ring−rod = **−136.1 / −137.0 / −128.9 MeV (E_pot)** and **−501.0 / −477.4 / −401.5 MeV (E_tot, KE = ½κv² included)** at dt = τ_C/{100, 50, 25} — sign-consistent on BOTH observables at all three dt, and MORE dt-stable than the memoryless dance (−143.9/−132.1/−74.4): the sea's memory removes the coarse-dt drift. Structures hold at native ZBW amplitude (0.29–0.34 fm).
- **C5 (SCRIPT result, robustness):** κ-scan ×{0.25, 0.5, 1, 2} at dt = τ_C/50: E_pot preference −140.0 / −131.7 / −137.0 / −133.2 (flat); E_tot preference −225.6 / −303.8 / −477.4 / −784.4 (monotone) — **inertia STRENGTHENS the ring preference**; the headline is quoted at ×1 regardless (G7). The deferral's stated expectation ("more transit apposition → core stiffening → ring at least as favored") is confirmed by the computation.
- **C6 (SCRIPT result, Q1 carry-over):** The pattern-level bond-stretch derivation survives hardening: ring inner −0.91..−1.10 (wants SHORTER than native), outer +2.80..+3.07, rod common-mode +1.19..+1.27 — the (ii)-class reference derivation your R1 returns accepted is unchanged by inertia + contention.
- **C7 (LIMITATION, disclosed):** Inertia does NOT resolve the Momentwise mode-curvature decorrelation: at pinned κ, m0 is positive-consistent, m1 and ℓ are negative-consistent, m2 dt-sign-flips (deterministic-pair caveat stands). **OPEN-DM-MW-MODES-1 STANDS**; ensemble averaging remains the named fix; the leg-based #1+#2 mode record stays operative. Carried unsoftened.

## §3 Results tables (verbatim run output)
```
HARD dt=tauC/100 k=1.0: ring Ep= -2734.2 KE=4874.2 | rod Ep= -2598.2 KE=5239.2 | r-s Ep= -136.1 Etot= -501.0 | amp 0.29/0.40 | Q1 inner -0.91 outer +3.07 rod +1.26
HARD dt=tauC/ 50 k=1.0: ring Ep= -2725.4 KE=4905.4 | rod Ep= -2588.4 KE=5245.8 | r-s Ep= -137.0 Etot= -477.4 | amp 0.30/0.42 | Q1 inner -1.01 outer +3.06 rod +1.27
HARD dt=tauC/ 25 k=1.0: ring Ep= -2700.8 KE=4968.2 | rod Ep= -2571.9 KE=5240.8 | r-s Ep= -128.9 Etot= -401.5 | amp 0.34/0.46 | Q1 inner -1.10 outer +2.80 rod +1.19
scan k=0.00: r-s Ep=  -132.3 Etot=  -132.3 | ring KE=   0.0 amp 0.30/0.42   <- nesting check vs your reviewed -132.1
scan k=0.25: r-s Ep=  -140.0 Etot=  -225.6 | ring KE=1230.1 amp 0.30/0.42
scan k=0.50: r-s Ep=  -131.7 Etot=  -303.8 | ring KE=2458.5 amp 0.30/0.42
scan k=1.00: r-s Ep=  -137.0 Etot=  -477.4 | ring KE=4905.4 amp 0.30/0.42   <- PINNED (headline)
scan k=2.00: r-s Ep=  -133.2 Etot=  -784.4 | ring KE=9515.3 amp 0.31/0.42
modes dt=tauC/50: m0 +23445 (Ep +4595) | m1 -4110 (Ep +925) | m2 +1025 (Ep -1174) | ell -50790 (Ep -21931)
modes dt=tauC/25: m0  +2401 (Ep +2263) | m1 -8627 (Ep -1398) | m2 -11574 (Ep +475) | ell -4086 (Ep -2083)
```

## §4 The re-ask (answer each)
- **R1′:** Do the two completions AS IMPLEMENTED — SF-6 speed memory at the independently pinned coefficient (never fit), and many-toward-one pile contention — satisfy deferral gaps (i) and (ii) as raised? (YES / NO / INSUFFICIENT — if not, state what further computation would satisfy you.)
- **R2′:** Do you SUSTAIN, with a NAMED COMPUTATION, an objection that v8's retained choreography (direction re-choreographed at commitment switches while speed carries; home-anchoring kept per your own reading-(B) finding and C2 above) invalidates the hardening? (WITHDRAWN-or-N/A / SUSTAINED-with-named-computation.)
- **R3′:** Promotion re-vote: PROMOTE / DO-NOT-PROMOTE / PROMOTE-CONDITIONAL (condition must be satisfiable by the record), with your updated calibrated survival probability. The committed rider stands: any promotion is recorded PROVISIONAL with C7 and the open items flagged.

## §5 Pre-registered reading of the returns (committed at dispatch, before any return; registered in reasoning/2510.md §6)
≥3 of 4 seats answer R3′ PROMOTE or PROMOTE-CONDITIONAL-with-record-satisfiable-conditions, AND no seat sustains R2′ with a named computation → **the v1.0 §5 consequence EXECUTES at the next patch: provisional promotion of the founder's Momentwise reading; candidate-(B) verdict moves to the four-seat median of the updated calibrations; Ω_DM unparks. No founder sign-off (PD-006 standing delegation).** ≥2 seats sustain R2′ with named computations, or answer R1′ NO with a named computation → HOLD ~70%, the named computation gates. Split → HOLD ~70%, follow-up per the strongest specific objection. Reviewer-reliability rules carry (fabrications = fidelity findings; duplicates counted once; stale-context returns re-run).

## §6 Your seat's prior conditions (restate-and-answer)
Each seat: restate your v1.0 R4 condition (if any) and state whether this record satisfies it. ChatGPT — provisional rider (committed, stands). Grok — free variational decider (C2/C6: the unscripted relaxation was RUN; it re-confirms home-anchoring as spec and the (ii)-class stretch pattern survives). Gemini — strict per-Moment integrator without discrete commitments (C1/C2 report exactly what that demands and why the well-posed form is the speed-memory completion; your named computation was run to its conclusion). DeepSeek — dt-robustness (C4: three-dt sign consistency on two observables, improved over v1.0).

## §7 Standalone verify code (full, Patch-0656 discipline)
Run: `python3 2510_hardened_dance_inertia.py repro | dt | scan | modes`. Requires numpy only.
```python
#!/usr/bin/env python3
"""DM hardening probe (Patch 2510) -- SF-6 inertia + full qCP-target contention added to
the founder-reach Momentwise dance (2467 D), per the 2470 deferral charter. Coefficient
FROM THE PIN (2496): kappa_q=132, kappa_e=44 MeV/c^2 (Laue coefficient-1 on the 2452
in-situ masses), scanned {0.25,0.5,1,2}, headline at the pinned center (G7: never fit,
never selected). Retardation sigma/c per species (a_qq, a_ee). Pre-registration in
reasoning/2510.md SS2-4, committed before this ran. Deterministic, no RNG.

Modes: repro (verbatim 2467 dance_M5 continuity check) | dt (hardened, pinned kappa,
3 dt) | scan (kappa multipliers, dt=1/50) | modes (m0/m1/m2/ell, pinned, 2 dt).
Requires 2461 defs (same dir). Flags verbatim-in-force: matched-pair (KE = sum 1/2 k s^2
only), not-registry-canonical, W2 caveat, scalar-toy debt (b) avoided by construction
(scalar along-axis drive)."""
import numpy as np, time, sys
exec(open(__file__.replace("2510_hardened_dance_inertia","2461_ssv_kinematics")).read().split("if __name__")[0])

KQ_PIN, KE_PIN = 132.0, 44.0   # MeV/c^2 -- 2452 in-situ, Laue coeff-1 (2496 concl. 6)

def qw_of(SP,C):
    W=np.array([np.sqrt(ALPHA_S) if s=='q' else np.sqrt(ALPHA) for s in SP]); return W*C

# ---------------- verbatim 2467 dance_M5 (memoryless) -- harness continuity ----------------
def dance_M5(H,C,SP,FREF,dtfrac,TC=60,burn=0.15):
    A=amat(SP); qw=qw_of(SP,C); NS=len(H)
    QW=np.outer(qw,qw); A2=A*A
    reach=build_reach(H,C,SP)
    oppr=[np.array([j for j in reach[i] if C[j]*C[i]<0],dtype=int) for i in range(NS)]
    for i in range(NS):
        if len(oppr[i])==0: oppr[i]=np.where(C*C[i]<0)[0]
    dt=TAUC*dtfrac; nst=int(TC*TAUC/dt)
    P=H.copy()
    tgt=np.array([oppr[i][np.argmin(np.linalg.norm(H[oppr[i]]-H[i],axis=1))] for i in range(NS)])
    last=-np.ones(NS,int); out=np.ones(NS,bool)
    isE=np.array([s=='e' for s in SP])
    Es=[]; Fsum=np.zeros((NS,3)); nF=0; amp=0.0
    for s in range(nst):
        dd=P[:,None,:]-P[None,:,:]
        r2=(dd*dd).sum(axis=2); np.fill_diagonal(r2,np.inf)
        r=np.sqrt(r2)
        F=(((QW/(r2+A2)**1.5))[:,:,None]*dd).sum(axis=1)*(-AHC)
        Fm=np.linalg.norm(F,axis=1); v=np.minimum(Fm/FREF,1.0)
        idx=np.arange(NS)
        o=idx[out]
        if len(o):
            rij=r[o,tgt[o]]; hit=rij<A[o,tgt[o]]
            eT=isE[tgt[o]]&~hit
            for m,i in enumerate(o):
                if eT[m]:
                    j=tgt[i]; col=r[:,j].copy(); col[i]=np.inf
                    if col.min()<A[i,j]: hit[m]=True
            for m,i in enumerate(o):
                if hit[m]: last[i]=tgt[i]; out[i]=False
        b=idx[~out]
        if len(b):
            dh=np.linalg.norm(P[b]-H[b],axis=1)
            for m,i in enumerate(b):
                if dh[m]<0.05:
                    cand=oppr[i][oppr[i]!=last[i]]
                    if len(cand)==0: cand=oppr[i]
                    u=(P[cand]-P[i]); un=np.maximum(np.linalg.norm(u,axis=1),1e-9)
                    pr=(u/un[:,None])@F[i]
                    tgt[i]=cand[int(np.argmax(pr))]; out[i]=True
        dest=np.where(out[:,None],P[tgt],H)
        u=dest-P; un=np.maximum(np.linalg.norm(u,axis=1),1e-9)
        P=P+(v/un)[:,None]*u*dt
        if s>=int(burn*nst):
            Es.append(0.5*np.sum(QW/np.sqrt(r2+A2))*AHC)
            Fsum+=F; nF+=1; amp+=np.linalg.norm(P-H,axis=1).mean()
    return np.array(Es), Fsum/nF, amp/nF

# -------- v8 HARDENED (final): dance choreography + SF-6 speed memory + pile contention --------
# Probe findings preserved (reasoning/2510 SS2.5, SS5): (v6) signed velocity on the rotating
# commitment axis = parametric pump, ill-posed; (v7) fully unscripted 3-D inertial MD
# collapses (opposite-charge soft core has its minimum at r=0) -- an independent empirical
# re-confirmation that HOME-ANCHORING IS LOAD-BEARING SPEC (2454/2467 reading (B)).
# v8 = the minimal well-posed completion: the promoted 2467-D choreography is kept EXACTLY
# (direction always toward commitment; home-anchoring intact; eCP preemption unchanged);
# the SPEED MAGNITUDE carries the sea's memory per the 2496 pin:
#     kappa dv/dt = |F_net| - v/mu ,  mu = 1/FREF ,  0 <= v <= c  (exact exp integrator)
# kappa_q=132, kappa_e=44 MeV/c^2 (Laue coeff-1 on the 2452 in-situ masses; scan x{.25,.5,1,2},
# headline pinned). Speed CARRIES across commitment switches (sea-stored momentum; direction
# re-choreography is the dance's kinematic idiom, as memoryless). kappa->0 recovers 2467-D
# EXACTLY. Contention completion: OUT ends on superposition with the TARGET or with any CP
# already superposed AT that target (many-toward-one pile stacking) -- native-structure-safe.
def dance_v8(H,C,SP,FREF,dtfrac,kscale=1.0,TC=60,burn=0.15):
    A=amat(SP); qw=qw_of(SP,C); NS=len(H)
    QW=np.outer(qw,qw); A2=A*A
    reach=build_reach(H,C,SP)
    oppr=[np.array([j for j in reach[i] if C[j]*C[i]<0],dtype=int) for i in range(NS)]
    for i in range(NS):
        if len(oppr[i])==0: oppr[i]=np.where(C*C[i]<0)[0]
    isE=np.array([s=='e' for s in SP])
    kap=np.where(isE, KE_PIN, KQ_PIN)*kscale
    mu=1.0/FREF
    dt=TAUC*dtfrac; nst=int(TC*TAUC/dt)
    eA=np.exp(-dt/(kap*mu)) if kscale>0 else np.zeros(NS)   # kscale=0 -> memoryless limit
    P=H.copy()
    tgt=np.array([oppr[i][np.argmin(np.linalg.norm(H[oppr[i]]-H[i],axis=1))] for i in range(NS)])
    last=-np.ones(NS,int); out=np.ones(NS,bool)
    v=np.zeros(NS)
    Es=[]; KEs=[]; Fsum=np.zeros((NS,3)); nF=0; amp=0.0
    for st in range(nst):
        dd=P[:,None,:]-P[None,:,:]
        r2=(dd*dd).sum(axis=2); np.fill_diagonal(r2,np.inf)
        r=np.sqrt(r2)
        F=(((QW/(r2+A2)**1.5))[:,:,None]*dd).sum(axis=1)*(-AHC)
        Fm=np.linalg.norm(F,axis=1)
        v=v*eA+np.minimum(mu*Fm,1.0)*(1-eA)          # speed memory; kscale=0 -> v=min(mu|F|,1)
        v=np.minimum(v,1.0)
        idx=np.arange(NS)
        o=idx[out]
        if len(o):
            rij=r[o,tgt[o]]; hit=rij<A[o,tgt[o]]
            # pile contention at qCP targets: superposed with a CP that is itself at my target
            for m,i in enumerate(o):
                if hit[m]: continue
                j=tgt[i]
                atj=np.where(r[:,j]<A[:,j])[0]        # CPs superposed at target j
                atj=atj[atj!=i]
                if len(atj) and (r[i,atj]<A[i,atj]).any(): hit[m]=True
            eT=isE[tgt[o]]&~hit
            for m,i in enumerate(o):
                if eT[m]:
                    j=tgt[i]; col=r[:,j].copy(); col[i]=np.inf
                    if col.min()<A[i,j]: hit[m]=True
            for m,i in enumerate(o):
                if hit[m]: last[i]=tgt[i]; out[i]=False
        b=idx[~out]
        if len(b):
            dh=np.linalg.norm(P[b]-H[b],axis=1)
            arr=dh<np.maximum(0.05,v[b]*dt)           # crossing-based arrival (inertial step)
            for m,i in enumerate(b):
                if arr[m]:
                    cand=oppr[i][oppr[i]!=last[i]]
                    if len(cand)==0: cand=oppr[i]
                    u=(P[cand]-P[i]); un=np.maximum(np.linalg.norm(u,axis=1),1e-9)
                    pr=(u/un[:,None])@F[i]
                    tgt[i]=cand[int(np.argmax(pr))]; out[i]=True
        dest=np.where(out[:,None],P[tgt],H)
        u=dest-P; un=np.maximum(np.linalg.norm(u,axis=1),1e-9)
        P=P+(v/un)[:,None]*u*dt
        if st>=int(burn*nst):
            Es.append(0.5*np.sum(QW/np.sqrt(r2+A2))*AHC)
            KEs.append(0.5*np.sum(kap*v*v))           # matched-pair: 1/2 kappa v^2
            Fsum+=F; nF+=1; amp+=np.linalg.norm(P-H,axis=1).mean()
    return np.array(Es), np.array(KEs), Fsum/nF, amp/nF

def qidx(k,c): return 8*k+c
def stretch(Favg,Pref,closed):
    st=[];L0=[]
    for k in range(N if closed else N-1):
        for c in range(4):
            i,j=qidx(k,c),qidx((k+1)%N,c)
            u=(Pref[j]-Pref[i])/np.linalg.norm(Pref[j]-Pref[i])
            st.append(np.dot(Favg[j]-Favg[i],u)); L0.append(np.linalg.norm(Pref[i]-Pref[j]))
    return np.array(st),np.array(L0)

Pr,Cr,SPr=ring_scaffold(); Ps,Cs,SPs=straight_scaffold()
Fr=ssv_vectors(Pr,Cr,SPr); Fs=ssv_vectors(Ps,Cs,SPs)
FREF=max(np.linalg.norm(Fr,axis=1).max(),np.linalg.norm(Fs,axis=1).max())
mode=sys.argv[1]; t0=time.time()

if mode=='repro':
    print(f"FREF={FREF:.2f}; 2467-D published ring-rod: -143.9/-132.1/-74.4 at dt 1/100,1/50,1/25")
    for dtf in (1/100,1/50,1/25):
        Er,_,ampr=dance_M5(Pr,Cr,SPr,FREF,dtf)
        Es_,_,amps=dance_M5(Ps,Cs,SPs,FREF,dtf)
        print(f"repro dt=tauC/{int(1/dtf):>3}: ring<E>={Er.mean():+8.1f} rod<E>={Es_.mean():+8.1f} "
              f"r-s={Er.mean()-Es_.mean():+7.1f} | amp {ampr:.2f}/{amps:.2f} | {time.time()-t0:.0f}s")
elif mode=='dt':
    for dtf in (1/100,1/50,1/25):
        Er,Kr,Favg_r,ampr=dance_v8(Pr,Cr,SPr,FREF,dtf)
        Es_,Ks,Favg_s,amps=dance_v8(Ps,Cs,SPs,FREF,dtf)
        str_r,L0=stretch(Favg_r,Pr,True); str_s,_=stretch(Favg_s,Ps,False)
        inner=str_r[L0<1.05].mean(); outer=str_r[L0>1.25].mean()
        dP=Er.mean()-Es_.mean(); dT=(Er.mean()+Kr.mean())-(Es_.mean()+Ks.mean())
        print(f"HARD dt=tauC/{int(1/dtf):>3} k=1.0: ring Ep={Er.mean():+8.1f} KE={Kr.mean():6.1f} | "
              f"rod Ep={Es_.mean():+8.1f} KE={Ks.mean():6.1f} | r-s Ep={dP:+7.1f} Etot={dT:+7.1f} | "
              f"amp {ampr:.2f}/{amps:.2f} | Q1 inner {inner:+5.2f} outer {outer:+5.2f} rod {str_s.mean():+5.2f} | {time.time()-t0:.0f}s")
elif mode=='scan':
    dtf=1/50
    for ks in (0.0,0.25,0.5,1.0,2.0):   # ks=0 = memoryless nesting check vs 2467-D
        Er,Kr,_,ampr=dance_v8(Pr,Cr,SPr,FREF,dtf,kscale=ks)
        Es_,Ks,_,amps=dance_v8(Ps,Cs,SPs,FREF,dtf,kscale=ks)
        dP=Er.mean()-Es_.mean(); dT=(Er.mean()+Kr.mean())-(Es_.mean()+Ks.mean())
        tag=" <- PINNED (headline)" if ks==1.0 else ""
        print(f"scan k={ks:4.2f}: r-s Ep={dP:+8.1f} Etot={dT:+8.1f} | ring KE={Kr.mean():6.1f} "
              f"amp {ampr:.2f}/{amps:.2f} | {time.time()-t0:.0f}s{tag}")
elif mode=='modes':
    for dtf in (1/50,1/25):
        E0,K0,_,_=dance_v8(Pr,Cr,SPr,FREF,dtf)
        T0=E0.mean()+K0.mean()
        print(f"modes dt=tauC/{int(1/dtf)}: base ring Etot={T0:+.1f} ({time.time()-t0:.0f}s)")
        for nm,kw,x in [('m0',dict(tilt=[0.04]*N),0.04),
                        ('m1',dict(tilt=[0.04*np.cos(2*np.pi*k/N) for k in range(N)]),0.04),
                        ('m2',dict(tilt=[0.04*np.cos(4*np.pi*k/N) for k in range(N)]),0.04),
                        ('ell',dict(ell=0.02),0.02)]:
            Ep,Kp,_,_=dance_v8(ring_scaffold(**kw)[0],Cr,SPr,FREF,dtf)
            Tp=Ep.mean()+Kp.mean()
            print(f"  {nm}: d2Etot/dx2 = {2*(Tp-T0)/x**2:+9.0f}  (Ep-part {2*(Ep.mean()-E0.mean())/x**2:+9.0f})  ({time.time()-t0:.0f}s)")
```

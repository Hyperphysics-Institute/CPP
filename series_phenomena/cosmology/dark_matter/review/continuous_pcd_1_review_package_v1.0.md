# Review Package — OPEN-DM-CONTINUOUS-PCD-1 v1.0 (Patch 2467) — the Momentwise reading landscape and the promotion re-ask

## §0 Identification
**Programme:** Conscious Point Physics (CPP). **Artifact:** OPEN-DM-CONTINUOUS-PCD-1 v1.0, Patch 2467 — the continuous per-Moment PCD computation named by the panel in the promotion round (Patch 2463 brief; Patches 2464–2466 adjudication). **One line:** the legless/capless/waitless Momentwise dynamics run to settle, by computation, whether the ring's survival was integrator-contingent (Q2) and which s₀ reference the dynamics itself derives (Q1). **Responses land in:** `series_phenomena/cosmology/dark_matter/review/reviews-CONTINUOUS-PCD-1.md`.

## §1 Context (what you previously ruled, in one paragraph each)
You are one of the four seated reviewers of the July 2026 promotion round (Copilot's seat was closed 0/3 on fresh-context; the round closed 4/5, HOLD at ~70% per the pre-registered reading). Your seat's stated conditions are restated in your §6 row. The round's two contested forks were: **Q1** — is the bond-registry reference (i) the straight-chain spacing s₀ = 1.15 fm, or (ii) the ring-native as-built lengths? — and **Q2** — was the Patch-2461 wait-and-reperceive rule a derivation or a tuned rescue? Gemini named the discriminating computation: "a strict per-Moment integrator where velocity continuously updates with the instantaneous SSV gradient, without any discrete leg commitments or artificial caps." Grok named the Q1 decider: free variational relaxation — the emergent equilibrium defines native s₀. ChatGPT's Q1 criterion: the reference "should emerge from the underlying lattice energy functional, not from whichever convention yields greater stability." This package reports that computation, run four ways.

## §2 The claims (each labeled with its basis)
- **C1 (SCRIPT result):** The literal netted-SSV flow — reading (A), Gemini's Q2 wording taken exactly — disintegrates ALL structure (ring AND rod; bonds → 12–40 fm; E → +75,000 MeV) at every dt ∈ {τ_C/200, τ_C/50, τ_C/20}; its dt→0 limit is gradient flow → frozen statics, no ZBW. It reproduces no bound matter of any kind, so it is self-falsifying as a reading of the founder's specification — not a verdict on the ring.
- **C2 (SCRIPT result):** Homeless target-chasing — reading (B), rebound retargets but never returns home — collapses all structure (bonds → 0.00–0.04 fm). The founder's home-anchoring is load-bearing.
- **C3 (SCRIPT result, decisive):** The Momentwise home-anchored dance with the founder's reach sets — reading (D), which has NO legs, NO caps, NO waits (direction re-chosen on rebound/home-arrival = preemption automatic; speed from the instantaneous SSV every Moment = a low-field CP moves slowly this Moment and re-perceives, so the 2461 wait rule EMERGES rather than being imposed) — holds structure (ZBW amplitude ~0.3 fm) and favors the RING: ring−rod = **−143.9 / −132.1 / −74.4 MeV at dt = τ_C/{100, 50, 25}** — sign-consistent (the pre-registered robustness criterion).
- **C4 (SCRIPT result, isolating control):** The same Momentwise dance with GENERIC reach (any opposite charge within 2 fm) — reading (C) — holds structure but favors the rod (+41..+152). (C)-vs-(D) isolates the load-bearing element as the founder's reach STRUCTURE ("5 qCPs and 1 eCP"), which is specification, not integrator. **Q2 consequence:** the wait rule did no outcome work; the ring's survival is not integrator-contingent.
- **C5 (SCRIPT result, Q1 derivation):** Time-averaged pattern-level bond-stretch forces (MeV/fm, + = wants longer; nearly dt-independent): rod common-mode +1.31 (baseline); ring INNER bonds (native 0.918) **−0.76..−0.93** — they want to be SHORTER than ring-native, not longer toward 1.15; ring OUTER bonds (native 1.367) **+2.78..+3.10** — longer still. Hypothesis (i) predicts the opposite sign on both edges. **The dynamics derives a reference at least as curvature-differentiated as ring-native — (ii)-class — by exactly the criterion ChatGPT stated and the relaxation Grok named.**
- **C6 (ANALYTIC):** The 55.6-vs-66 near-equality is an ESTIMATOR IDENTITY: 66 = α_s·ħc/d and 55.6 = α_s·ħc/√(d²+a_qq²) are one estimator with and without the soft-core — Gemini's identity reading is correct for this estimator. Consequence bounded: the central E_qq must lean on the independent estimators (the 2444 depth analysis; the 102 MeV contact-depth lock α_s·ħc/a_qq); Computation #2's landscape already spanned E_qq ∈ {40..170}, so no #2 conclusion moves.
- **C7 (LIMITATION, disclosed):** Momentwise mode curvatures are UNRESOLVED at this treatment (paired deterministic chaotic trajectories decorrelate; dt sign-flips on m=0/m=1). The leg-based #1+#2 mode record (registry priced) stands operative; OPEN-DM-MW-MODES-1 (ensemble-averaged Momentwise modes) is registered as the follow-up.

## §3 Results tables (verbatim run output)
Reading (D), founder reach: dt=τ_C/100: ring ⟨E⟩=−2731.6, rod −2587.6, r−s=−143.9 | dt=τ_C/50: −2738.5, −2606.4, −132.1 | dt=τ_C/25: −2731.8, −2657.4, −74.4. Q1 stretch (inner/outer vs rod common): −0.93/+3.10 vs +1.31; −0.92/+3.06 vs +1.30; −0.76/+2.78 vs +1.31.
Reading (C), generic reach: r−s = +147.2 / +152.3 / +41.4. Reading (A): E → +75,511 (ring), bonds → 40 fm. Reading (B): bonds → 0.00–0.04 fm.

## §4 The re-ask (answer each)
- **R1:** Does C5 resolve the s₀-reference question to (ii)-class BY DERIVATION, per your own stated criterion? (YES / NO / INSUFFICIENT — state what further computation would satisfy you.)
- **R2:** Is the tuned-rescue objection to Computation #1 withdrawn given C1–C4? (WITHDRAWN / SUSTAINED — if sustained, name the computation.)
- **R3:** Given C6, is the E_qq bookkeeping concern adequately bounded for promotion purposes (the #2 landscape spanning 40–170 with the identity flagged)? (YES / NO.)
- **R4:** Promotion re-vote: PROMOTE / DO-NOT-PROMOTE / PROMOTE-CONDITIONAL (condition must be satisfiable by the record), with your updated calibrated survival probability. Reminder of the committed rider: any promotion is recorded PROVISIONAL with the remaining open questions flagged (per ChatGPT's round-1 condition, already committed).

## §5 Pre-registered reading of the re-ask returns (committed at dispatch, before any return)
≥3 of 4 seats answer R4 PROMOTE or PROMOTE-CONDITIONAL-with-record-satisfiable-conditions, AND no seat sustains R2 → **promotion executes at the next patch, PROVISIONAL, with the founder's sign-off; Ω_DM unparks; verdict to the four-seat median of the updated calibrations.** ≥2 seats sustain R2 or answer R1 NO with a named computation → HOLD ~70%, the named computation gates. Split → HOLD ~70%, follow-up per the strongest specific objection. Reviewer-reliability rules carry (fabrications = fidelity findings; duplicates counted once; stale-context returns re-run).

## §6 Reviewer steer — read your own row
- **Grok:** Your round-1 condition was Q1→(ii) by ≥3 seats or E_qq ≲ 100 under (i). C5 is your own named variational decider, executed dynamically. Assess whether the derivation satisfies your condition's intent.
- **Gemini:** C1 is your named computation, literal. C3–C4 are its faithful completion (your wording removed the legs and caps; the founder's reach structure and home-anchoring are specification, shown load-bearing by C1/C2/C4). Your identity reading of the near-equality is CREDITED at C6. State whether R2 is withdrawn or sustained; if sustained, the named computation must differ from C1–C4 in a stated, runnable way. Disambiguation rider: this is the CPP dark-matter programme's Momentwise-dynamics package; engage the inline content directly; do not reconstruct from memory.
- **ChatGPT:** C5 is your Q1 criterion executed ("emerge from the lattice energy functional"). Your provisional-promotion rider is committed and restated at R4. Assess R1 against your own formulation, and whether C6 satisfies your "deserves explanation before promotion" reservation.
- **DeepSeek:** Your round-1 condition (adopt (ii)) was ruled not countable because the record hadn't derived it; C5 is the derivation. Your τ_cap-from-local-SSV variant is superseded by C3 (no caps exist Momentwise). Note your round-1 written-formula typo was scored as a minor note (numbers reproduced); please label tiers explicitly this round.

## §7 Verify code (embedded in full — standalone, deterministic; ~5 min runtime; readings A/B code in `code/2467_continuous_pcd.py` in-repo, structure identical minus reach/home logic)
```python
import numpy as np
import time, sys
AHC=197.3; PHI_G=(1+np.sqrt(5))/2; ALPHA_S=5/(8*PHI_G); ALPHA=1/137.036
d=1.15; A_Q=d; D=d; N=16
r_q=A_Q/np.sqrt(2); R_E=1.6*r_q; KAP=2*np.pi/(N*D); R0=1/KAP
A_QQ=AHC/264.0; A_EE=AHC/553.0; A_QE=np.sqrt(A_QQ*A_EE)
TAUC=2*np.pi*AHC/264.0

def ring_scaffold(tilt=None,ell=0.0):
    S=[]
    for k in range(N):
        phi=2*np.pi*k/N
        R=R0*(1+ell*np.cos(2*phi))
        cx=R*(1-np.cos(phi)); cz=R*np.sin(phi)
        th=phi+(tilt[k] if tilt is not None else 0.0)
        c,s=np.cos(th),np.sin(th); par=(-1)**k
        h=A_Q/2
        q=[(+h,+h,+1),(-h,+h,-1),(-h,-h,+1),(+h,-h,-1)]
        for (x,y,sg) in q: S.append(((cx+x*c,y,cz-x*s),sg*par,'q',k))
        for (x,y,sg) in q:
            n=np.hypot(x,y); X,Y=R_E*x/n,R_E*y/n
            S.append(((cx+X*c,Y,cz-X*s),-sg*par,'e',k))
    P=np.array([s[0] for s in S]); C=np.array([float(s[1]) for s in S])
    return P,C,[s[2] for s in S]
def straight_scaffold(tilt=None):
    S=[]
    for k in range(N):
        th=(tilt[k] if tilt is not None else 0.0)
        c,s=np.cos(th),np.sin(th); par=(-1)**k
        h=A_Q/2
        q=[(+h,+h,+1),(-h,+h,-1),(-h,-h,+1),(+h,-h,-1)]
        for (x,y,sg) in q: S.append(((x*c,y,k*D-x*s),sg*par,'q',k))
        for (x,y,sg) in q:
            n=np.hypot(x,y); X,Y=R_E*x/n,R_E*y/n
            S.append(((X*c,Y,k*D-X*s),-sg*par,'e',k))
    P=np.array([s[0] for s in S]); C=np.array([float(s[1]) for s in S])
    return P,C,[s[2] for s in S]
def build_reach(P,C,SP):
    NS=len(P); reach=[]
    for i in range(NS):
        dd=P-P[i]; r=np.sqrt((dd*dd).sum(axis=1)); r[i]=np.inf
        if SP[i]=='q':
            inpl=[j for j in range(NS) if SP[j]=='q' and abs(P[j][2]-P[i][2])<0.5*D and r[j]<1.8]
            axl=[j for j in range(NS) if SP[j]=='q' and j not in inpl and r[j]<1.3]
            ecp=[j for j in range(NS) if SP[j]=='e' and r[j]<0.6]
            reach.append(sorted(set(inpl+axl))[:5]+ecp)
        else:
            eopp=sorted([j for j in range(NS) if SP[j]=='e' and 0<r[j]<2.6],key=lambda j:r[j])[:4]
            qown=[j for j in range(NS) if SP[j]=='q' and r[j]<0.6]
            reach.append(eopp+qown)
    return reach
def soft_a(si,sj):
    if si=='q' and sj=='q': return A_QQ
    if si=='e' and sj=='e': return A_EE
    return A_QE
def amat(SP):
    NS=len(SP); A=np.zeros((NS,NS))
    for i in range(NS):
        for j in range(NS): A[i,j]=soft_a(SP[i],SP[j])
    return A
def ssv_vectors(P,C,SP):
    """Static home-scaffold net field-force vectors (used only to SET SSV_ref)."""
    NS=len(P); W=np.array([np.sqrt(ALPHA_S) if s=='q' else np.sqrt(ALPHA) for s in SP])
    qw=W*C; A=amat(SP)
    dd=P[:,None,:]-P[None,:,:]
    r2=(dd*dd).sum(axis=2); np.fill_diagonal(r2,np.inf)
    den=(r2+A*A)**1.5
    coef=np.outer(qw,qw)/den
    return (coef[:,:,None]*dd).sum(axis=1)*(-AHC)
TAUC=2*np.pi*AHC/264.0
def qw_of(SP,C): return np.array([np.sqrt(ALPHA_S) if s=='q' else np.sqrt(ALPHA) for s in SP])*C
def dance_M5(H,C,SP,FREF,dtfrac,TC=60,burn=0.15):
    """Momentwise home-anchored dance with FOUNDER REACH SETS (build_reach from the
       2461 defs: in-plane + axial qCPs + radial eCP per qCP; 4 eCP + own qCP per eCP).
       Legless, capless: direction re-chosen on rebound/home-arrival; speed from
       instantaneous SSV every Moment. Deterministic."""
    A=amat(SP); qw=qw_of(SP,C); NS=len(H)
    QW=np.outer(qw,qw); A2=A*A
    reach=build_reach(H,C,SP)
    oppr=[np.array([j for j in reach[i] if C[j]*C[i]<0],dtype=int) for i in range(NS)]
    for i in range(NS):
        if len(oppr[i])==0: oppr[i]=np.where(C*C[i]<0)[0]
    dt=TAUC*dtfrac; nst=int(TC*TAUC/dt)
    P=H.copy()
    tgt=np.array([oppr[i][np.argmin(np.linalg.norm(H[oppr[i]]-H[i],axis=1))] for i in range(NS)])
    last=-np.ones(NS,int); out=np.ones(NS,bool)
    isE=np.array([s=='e' for s in SP])
    Es=[]; Fsum=np.zeros((NS,3)); nF=0; amp=0.0
    for s in range(nst):
        dd=P[:,None,:]-P[None,:,:]
        r2=(dd*dd).sum(axis=2); np.fill_diagonal(r2,np.inf)
        r=np.sqrt(r2)
        F=(((QW/(r2+A2)**1.5))[:,:,None]*dd).sum(axis=1)*(-AHC)
        Fm=np.linalg.norm(F,axis=1); v=np.minimum(Fm/FREF,1.0)
        idx=np.arange(NS)
        o=idx[out]
        if len(o):
            rij=r[o,tgt[o]]; hit=rij<A[o,tgt[o]]
            eT=isE[tgt[o]]&~hit
            for m,i in enumerate(o):
                if eT[m]:
                    j=tgt[i]; col=r[:,j].copy(); col[i]=np.inf
                    if col.min()<A[i,j]: hit[m]=True
            for m,i in enumerate(o):
                if hit[m]: last[i]=tgt[i]; out[i]=False
        b=idx[~out]
        if len(b):
            dh=np.linalg.norm(P[b]-H[b],axis=1)
            for m,i in enumerate(b):
                if dh[m]<0.05:
                    cand=oppr[i][oppr[i]!=last[i]]
                    if len(cand)==0: cand=oppr[i]
                    u=(P[cand]-P[i]); un=np.maximum(np.linalg.norm(u,axis=1),1e-9)
                    pr=(u/un[:,None])@F[i]
                    tgt[i]=cand[int(np.argmax(pr))]; out[i]=True
        dest=np.where(out[:,None],P[tgt],H)
        u=dest-P; un=np.maximum(np.linalg.norm(u,axis=1),1e-9)
        P=P+(v/un)[:,None]*u*dt
        if s>=int(burn*nst):
            Es.append(0.5*np.sum(QW/np.sqrt(r2+A2))*AHC)
            Fsum+=F; nF+=1; amp+=np.linalg.norm(P-H,axis=1).mean()
    return np.array(Es), Fsum/nF, amp/nF
Pr,Cr,SPr=ring_scaffold(); Ps,Cs,SPs=straight_scaffold()
Fr=ssv_vectors(Pr,Cr,SPr); Fs=ssv_vectors(Ps,Cs,SPs)
FREF=max(np.linalg.norm(Fr,axis=1).max(),np.linalg.norm(Fs,axis=1).max())
def qidx(k,c): return 8*k+c
mode="dt"; t0=time.time()
if mode=='dt':
    for dtf in (1/100,1/50,1/25):
        Er,Favg_r,ampr=dance_M5(Pr,Cr,SPr,FREF,dtf)
        Es_,Favg_s,amps=dance_M5(Ps,Cs,SPs,FREF,dtf)
        # Q1: bond-stretch pattern forces, ring (inner vs outer) AND rod (common-mode ref)
        str_r=[]; L0=[]
        for k in range(N):
            for c in range(4):
                i,j=qidx(k,c),qidx((k+1)%N,c)
                u=(Pr[j]-Pr[i])/np.linalg.norm(Pr[j]-Pr[i])
                str_r.append(np.dot(Favg_r[j]-Favg_r[i],u)); L0.append(np.linalg.norm(Pr[i]-Pr[j]))
        str_s=[]
        for k in range(N-1):
            for c in range(4):
                i,j=qidx(k,c),qidx(k+1,c)
                u=(Ps[j]-Ps[i])/np.linalg.norm(Ps[j]-Ps[i])
                str_s.append(np.dot(Favg_s[j]-Favg_s[i],u))
        str_r=np.array(str_r); L0=np.array(L0); str_s=np.array(str_s)
        inner=str_r[L0<1.05].mean(); outer=str_r[L0>1.25].mean()
        print(f"dt=tauC/{int(1/dtf):>3}: ring<E>={Er.mean():+8.1f} rod<E>={Es_.mean():+8.1f} "
              f"r-s={Er.mean()-Es_.mean():+7.1f} | amp {ampr:.2f}/{amps:.2f} | Q1 ring stretch "
              f"inner {inner:+5.2f} outer {outer:+5.2f} vs rod common {str_s.mean():+5.2f} MeV/fm | {time.time()-t0:.0f}s")
else:
    dtf=float(sys.argv[2]) if len(sys.argv)>2 else 1/50
    E0,_,_=dance_M5(Pr,Cr,SPr,FREF,dtf)
    print(f"base ring <E> = {E0.mean():+.1f}")
    for nm,kw,x in [('m0',dict(tilt=[0.04]*N),0.04),('m1',dict(tilt=[0.04*np.cos(2*np.pi*k/N) for k in range(N)]),0.04),
                    ('m2',dict(tilt=[0.04*np.cos(4*np.pi*k/N) for k in range(N)]),0.04),('ell',dict(ell=0.02),0.02)]:
        Ep,_,_=dance_M5(ring_scaffold(**kw)[0],Cr,SPr,FREF,dtf)
        print(f"  {nm}: d2E/dx2 = {2*(Ep.mean()-E0.mean())/x**2:+9.0f}   ({time.time()-t0:.0f}s)")
```

## §8 Response format
For each of R1–R4: verdict + reasoning (a paragraph) + verification tier per PD-002 for every factual claim you rely on: INSPECTED / INDEPENDENTLY RECOMPUTED / SCRIPT-EXECUTED. REVIEWER field: your own actual model name (do not adopt another reviewer's name). If you ran §7, report SCRIPT-EXECUTED with your three ring−rod numbers.

# OPEN-DM-ALPHA-1 S3 RECORD — readout propagation: **D3 = FG-UNDECIDED** under the charter's hard guard, with the deciding question SHARPENED to a named computation: the registered operating point κ·d_DP = 2.0 sits BEYOND the charged-fluid monotonic→oscillatory (Kirkwood-type) crossover (κd ≈ 1.0–1.2), so a fully CONTINUOUS Sea is itself expected to screen with damped-oscillatory charge layering at period ~ d_DP — the lattice staggering may be the proxy's rendering of real layering, not an artifact; the follow-on S4 (structured-kernel/layering computation) is chartered as the decision instrument

**Patch 2702, 21 July 2026. Executed under the frozen 2698 charter,
stage S3, using the committed R1 comparators only. The FG-OTHER
classification is panel property; nothing here reclassifies it.
Reasoning: `reasoning/2702.md`. 79.5% not in scope.**

## §1 — What D1/D2 imply for the staggered readout — both prior intuitions were partly right

The naive reading of D1's continuous local kernel is that the FG-OTHER
staggering (a zone-boundary mode of the RIGID proxy lattice) is an
over-discretization artifact, with the physical screened scale the
smooth ~1/κ = 0.182 fm (independently: the hop branch's 0.1955 fm).
That reading is INCOMPLETE, for a reason the derivation itself
supplies: **the operating point sits at κ·d_DP = 2.0, beyond the known
charged-fluid crossover (κd ≈ 1.0–1.2) at which screening in a
CONTINUOUS moderately-coupled two-species charge fluid turns from
monotonic decay into damped-oscillatory decay (charge layering) with
period of order the inter-particle spacing.** With Γ = 0.225 the
structured O(Γ) corrections are exactly the ingredient that generates
layering. So the physical Sea may genuinely oscillate — period ~ d_DP
= 0.364 fm against the lattice staggering's ~2·nn = 0.728 fm
wavelength, same ballpark — and the lattice result may be a crude
RENDERING of real physics rather than an artifact of it.

## §2 — Determination and the decision instrument

**D3 = FG-UNDECIDED** (and would be even without the guard: the
derivation supports both directions at this stage). The deciding
computation is now precisely defined and is chartered as the arc's
follow-on stage **S4**: compute the structured (pair-correlation-
corrected) kernel of the D1 response at Γ = 1/(√2π), κd = 2, and read
off whether its decay is monotonic or damped-oscillatory, with what
period and decay length; compare against (i) the committed lattice
envelope 1/(2κ) = 0.0910 fm and staggering wavelength, (ii) the smooth
comparators 0.182 / 0.1955 fm. Outcomes map: layering with
lattice-compatible period/decay → supports FG-STAGGER-PHYSICAL
(panel adjudicates); monotonic or incompatible structure → supports
FG-STAGGER-PROXY-ARTIFACT (panel adjudicates). S4 executes after the
panel returns on this packet and after the founder's θ answer (the
layering computation needs the numerical Γ, which θ fixes).

## §3 — Consumer interface note (2700 commitments 7–8)

Whatever S4 decides, χ's consumer interface to the DM mass is the eCP
coat of the ring surface; the qCP core's registered stiffness model is
untouched by this arc. If the physical screening is layered, the
coat-Sea coupling inherits a d_DP-period radial structure — a
consumer-facing implication for the FA-C2 tier classification
(T1 items sensitive to it), noted for the campaign, not enacted.

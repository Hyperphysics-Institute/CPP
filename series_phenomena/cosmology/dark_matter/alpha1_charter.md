# OPEN-DM-ALPHA-1 CHARTER (FROZEN) — the polarizability-bridge derivation arc: from the founder's statistical-response mechanism (2697, six commitments) to χ_static and the α self-consistency, with the discreteness question and the readout consequence carried as FROZEN outcome classes; any FG-OTHER reclassification is PROVISIONAL until a CONV-001 panel adjudicates it

**Patch 2698, 21 July 2026. Charter frozen before any ALPHA-1
derivation step ran. Inputs: the ALPHA-1 registration (2695), the
founder's normative answer with the worker's six-commitment extraction
(2697), and the R1 empirical constraints (registration §1). Rider v2,
the cap, all quarantines, and the 79.5% are untouchable from inside
this arc. Reasoning capture per leg; verify script for every
computation; founder loop for mechanism questions only (PD-006).**

## §1 — Staged derivation (sequencing frozen: S1 → S2 → S3)

**A1-S1 — Formalization of the statistical response.** Translate the
six commitments into a two-species (±) CP occupation-statistics model
on GPs: Moment dynamics driven by the total SSV_net (commitment 2),
self-exclusion at superposition/GP scale only (3), individual ZBW mode
untouched (5), source = stable biasing pattern (6). Deliverable: the
static density-response kernel — its FORM (local vs structured at
d_DP scale) is the S1 determination, made from the derivation, not
assumed. The natural analytic frame is charged-gas linear response
(Debye–Hückel-type) built from CPP ingredients ONLY: CP density
n_CP = 2√2/a³ (two CPs per DP at the registered local density, fence
F1), the chaotic kinetic scale to be DERIVED or founder-sourced (not
fitted), unit CP charge as registered.

**A1-S2 — The α bridge and the S1c reconciliation.** From the S1
kernel, derive the self-consistency that fixes the coupling in the Sea
response operator — the continuum form, the discrete form, or a third
derived equation. **Reconciliation requirement (frozen):** state
explicitly whether the derived screening scale reproduces S1c's gap
route κ = 2/d_DP; agreement is a lock, disagreement is reported
same-font with the mechanism-level diagnosis and routed to the founder
as a physics question. The α^(−1/2) law stays struck regardless.

**A1-S3 — Readout propagation.** Carry the derived object into the
FA-SEA-GREEN readout: whether the FG-OTHER staggered structure is
physical or a proxy-discretization artifact, and what the physical
screened scale is. Uses the R1 committed facts (staggering is 1/r-
assembly-specific; ℓ(α) three-regime map; the smooth scales 1/κ =
0.182 fm continuum and 0.195 fm hop) as fixed comparators.

## §2 — Outcome classes (frozen; two determinations + one consequence)

**D1 (susceptibility object):** CHI-LOCAL-CONT (statistically
continuous, exclusion GP-scale, no d_DP structure in the kernel) /
CHI-STRUCTURED (time-averaged kernel retains genuine d_DP-scale
structure — the discrete-site proxy survives coarse-graining) /
CHI-THIRD (a different object, e.g., nonlocal or gradient-corrected —
feeds DISC-1 directly) / CHI-INCOMPLETE (derivation cannot close
without further founder mechanism input; the blocking question is
named).

**D2 (α selection, conditional on D1):** the derived self-consistency
and its α value with propagated ℓ, stated against the rider-v2
branches: selects continuum-branch / selects site-branch / replaces
both with a derived third equation and value.

**D3 (readout consequence):** FG-STAGGER-PHYSICAL /
FG-STAGGER-PROXY-ARTIFACT / FG-UNDECIDED. **Guard (frozen):** the
FG-OTHER classification is panel property (2674, 5–0; ratified at the
R1 returns). ANY D3 outcome other than UNDECIDED is PROVISIONAL on
its face and dispatches to a CONV-001 panel before it touches the
readout, the rider branches, or any consumer sentence. The worker may
not reclassify a panel-adjudicated structure.

## §3 — Guards

Non-elevation clause carries to every operating-point coincidence this
arc touches (including any S2 lock with κ = 2/d_DP: a lock is reported,
not elevated). No grade, cap, rider, quarantine, release, or 79.5%
motion from inside the arc — all such motion belongs to the panel at
the arc's returns. Adverse-direction results report in full, same-font,
nothing renamed after results. Scope fences F1–F4 of the 2679 charter
carry where applicable (local density only; no cosmic counts; CC arc
untouched). The FA-C2 campaign stays queued behind this arc for
single-value targets; tier classification (rider v2) may proceed in
parallel.

## §4 — Close conditions

The arc closes at: all three determinations made (or CHI-INCOMPLETE
with the blocking question posed to the founder), the CONV-001 packet
assembled if any D3 non-UNDECIDED or any D2 branch-replacement is on
the table, and the handover naming the single next act. Verify scripts
in `code/`, records in this directory, reasoning per patch.

# FA-C2 CONSUMER RIDER v2 — panel-adopted at the R1 returns (adjudication 2692 §5), superseding the 2674 rider text for all FA-C2 consumer sentences; the 2664 rider and the Gemini width-demotion clause remain in force unchanged

**Patch 2695, 21 July 2026. Authority: R1 returns Q4 (cap 5–0; rider
amendment directed by all five seats; tier structure adopted from GPT;
DEP-1 direction from Grok honoured in Tier 2; branch-statement
requirement from Gemini).**

## The rider

FA-C2 **FORM** remains grounded for the dense 1/r site-scattering
operator: the Sea response is screened and admits an exponential
envelope in the frozen continuum-matched realization. The **numerical**
screening length is **observation-grade and normalization-conditional
pending OPEN-DM-ALPHA-1** (the DP static-polarizability bridge). The
carried family, panel-ratified:

- **Continuum-matched branch (primary by ordering, not by adoption):**
  ℓ = 0.0904 ± 0.0028 fm — the band is an **instrument-robustness
  envelope**, not a statistical confidence interval, and must be
  described as such in every consumer sentence.
- **Site-matched trial branch:** ℓ ≈ 0.168 fm — degraded-envelope,
  near-critical **trial**, never a measured screening length.

The α^(−1/2) propagation law is **struck**: falsified at L4; it may not
be used to propagate any normalization change into ℓ. Every consumer
sentence states which branch it assumes. **No downstream quantity may
be represented as zero-parameter numerical output if it materially
depends on ℓ, until ALPHA-1 lands.**

## Consumer sensitivity tiers (classification precedes any FA-C2 re-derivation work)

- **T1 — exponential or threshold dependence on ℓ:** immediate
  quarantine; no numerical citation at any strength.
- **T2 — polynomial width dependence (incl. the DEP-1 width-dependent
  rows):** retained only as a conditional curve over the two branches;
  the Gemini width-demotion clause applies with no relaxation.
- **T3 — qualitative dependence on finite-range screening only:**
  survives with the numerical value removed.

The observation-grade cap on ℓ (2671, ratified at 2674 and again at
these returns 5–0) is in force until a future panel lifts it by name.

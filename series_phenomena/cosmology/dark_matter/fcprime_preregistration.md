# F-c′ pre-registration: the ponderomotive effective pair potential between two registered plane units — closed inputs from the ZBW lineage, the frozen averaging formalism, the amplitude-threshold framing, and readings, committed before any derivation

**Patch 2576, 19 July 2026. Status: F-c′ OPENED at pre-registration per founder confirmation
("Please derive the effective potential between two registered units"); NO derivation performed.**
Governed by charter v1.1, the 2575 R2 record, and this document only. **Verify: none needed at
prereg.** Collision check clean.

## 0. Target

**U_eff(sep; ξ)** — the ZBW-jitter-averaged cross-potential between two registered 4-qCP plane
units in the registered stacking (aligned squares, alternating parity), as a function of
separation and per-CP jitter amplitude ξ — and the derived threshold **ξ\*** = the smallest
amplitude at which a finite-separation minimum with a repulsive rise appears. The amplitude is
framed as a THRESHOLD QUESTION deliberately: 2427 (F1–F3) left the ZBW amplitude ratio s = a/d
as the named open substrate quantity; this campaign does not pin it — it derives what amplitude
the core REQUIRES and compares against the registered candidate span.

## 1. Closed input list (nothing else may enter)

1. **The interaction:** the registered soft kernel U_pair = q_i q_j w_i w_j·AHC·(r²+A_ij²)^(−1/2),
   constants verbatim. **Double-counting disclosure, carried into every reading:** the soft
   length A already encodes a first-moment ZBW smear; the jitter average adds the second-moment
   content on top of the registered kernel — the 2427 F2 precedent (average the registered soft
   bond) is followed; if the founder later rules A the complete smear, a rider attaches.
2. **The species:** the registered 4-qCP alternating square (side D = 1.15), aligned stacking,
   alternating parity — the 2575 U-A convention (primary); the coated unit (U-B) as the union's
   secondary.
3. **The drive (registered, 2451/2435/2441):** oscillating fraction f²_osc = 48/49 (δ = 3/7,
   with the FLOQUET-1 R2 upper-bound status carried); hop clocks ℏω = 264 MeV (qCP lattice),
   553 MeV (eCP) — clocks enter only through the amplitude candidates, not as free scales.
4. **The amplitude candidate span (registered masses, 2451 — NO selection was made there and
   none is made here):** Compton amplitudes ξ_m = ℏc/(m c²) for m c² ∈ {88, 176, 264, 312.7}
   MeV → ξ ∈ {2.242, 1.121, 0.747, 0.631} fm. **Generosity margin for the kill reading only:**
   the R2′ scan extends to 1.5 × the largest candidate (3.36 fm).
5. **Jitter statistics (frozen):** per-CP isotropic Gaussian displacement with TOTAL rms = ξ
   (per-axis variance ξ²/3); the two units' CPs jitter independently → relative per-axis
   variance 2ξ²/3. Convention-union member: the f²_osc-weighted variance (×48/49 — disclosed;
   numerically near-unity). All CPs of a unit jitter INDEPENDENTLY (primary) vs rigid-unit
   common jitter (secondary union member — the two bracket coherence assumptions; the truth is
   between them and the spread is the honesty band).
6. **The averaging (frozen):** PRIMARY = exact numerical Gaussian average of the registered
   kernel over the relative-jitter distribution (Gauss–Hermite product quadrature; node count
   convergence asserted in-script). SECONDARY (coherence check, not a result) = the O(σ²)
   Kapitza formula U + (σ²/6)∇²U with ∇²(r²+A²)^(−1/2) = −3A²(r²+A²)^(−5/2) analytic;
   agreement with the exact average at small ξ is asserted; at ξ ~ A the expansion is
   out-of-band and only the exact average reads.
7. EXCLUDED, named: any new drive parameter; any amplitude selection; window/wall/pin/16/
   abundance content; any tuning of quadrature or grids after a curve is seen.

## 2. Grids (frozen)

sep ∈ 60 points spanning [0.05, 2.6]·D (the 2575 span, unchanged); ξ ∈ {0.2, 0.4, 0.631,
0.747, 1.0, 1.121, 1.5, 2.0, 2.242, 2.8, 3.36} fm (the registered candidates embedded, plus
structure points declared now); quadrature 7³ Gauss–Hermite nodes with a 9³ convergence check
at three declared (sep, ξ) probes.

## 3. Readings (frozen)

- **R1′ (core found):** U_eff(sep; ξ) exhibits an INTERIOR minimum with depth below the
  large-sep plateau by > 2·floor AND monotone repulsive rise below it, for some ξ within the
  registered candidate span, under the PRIMARY conventions (independent jitter, exact average,
  bare squares) — then **F-c′ CONFIRMED at derivation strength, CONDITIONAL on the amplitude
  assignment** (the condition named: which registered mass candidates clear ξ\*; the 2427
  open-amplitude flag travels). Reported as-is: ξ\*, the minimum's location vs the registered
  pitch D (match or offset both honest), depth, and the union spread (coherence + coating +
  f²_osc members).
- **R2′ (no core):** no qualifying minimum for any ξ ≤ 1.5 × the largest registered candidate,
  under the GENEROUS union members — **F-c′ fails; F-b re-enters** with the full curve record
  routed to the founder. (Kill must survive generosity; confirmation must survive stinginess —
  the 2570 asymmetry, verbatim.)
- **R3′ (marginal):** a minimum appearing only outside the candidate span or only under
  generous members — reported as the threshold statement with no confirmation; the amplitude
  question routes to the founder as the named blocker.
- Location honesty: no reading may privilege minima near D; the location is an OUTPUT.
  Any new √5 → fence. No composition reading.

## 4. Consequences pre-wired (execute only on their triggers)

R1′ → the K1a re-charter (corrected species = plane units; corrected interaction = U_eff at
the founder-assigned amplitude once he assigns it; CTRL-1's expectation re-grounded on the
derived equilibrium) and the O3′ map re-derivation at the corrected species/interaction (the
2571 suspension resolves by recomputation). R2′ → F-b re-scope charter. R3′ → founder
amplitude adjudication first.

## 5. Bookkeeping

79.5% untouched. Next patch: the F-c′ execution under this document only.

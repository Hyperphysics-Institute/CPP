# OPEN-DM-ALPHA-1 — the polarizability bridge: derive the Sea's static susceptibility χ from DP internal-oscillator mechanics, with all normalization factors retained — the panel-assigned resolution gate for the FA-SEA-GREEN normalization split (R1 returns Q3, adjudication 2692 §4)

**Registered Patch 2695, 21 July 2026. Origin: the R1 returns split
2–2–1 on which α-matching the S1b/S1c chain grounds; GPT's INSUFFICIENT
diagnosis adopted as the ruling — both the continuum matching
(α·S_cont = 1) and the site-level matching (α′·S_disc = 1) are IMPOSED
conditions, and the derivation that would select (or replace) them is
missing. CLONE-FIRST GATE cleared: no prior OPEN-DM-ALPHA registration
anywhere in the tree.**

## §1 — The missing derivation, precisely

S1b derives the operator FORM M = I + χG (G_ij ∝ 1/r_ij, G_ii = 0:
continuum 1/r transport between discrete polarizable sites) and
identifies χ as an on-site static polarizability. S1c derives the gap
frequency ω_A from the internal DP binding. **Neither supplies:**

  DP internal-oscillator mechanics (ω_A, the displacement coordinate,
  the source normalization) ⟶ χ_static ⟶ α, factors retained.

Until that chain exists, every choice of α is a matching CONDITION, not
a derived coefficient. The empirical facts constraining the derivation,
all committed in the R1 arc: the operator's response is
normalization-sensitive (α′/α = 1.611 propagates ℓ outside its band);
the α^(−1/2) law fails (ℓ(α) is non-monotone across pure-Yukawa /
staggered-evanescent / near-critical regimes, α_crit = 2.74α₀); the
staggering is specific to the 1/r assembly (J6-DIVERGE, M-matrix
theorem); the frozen operating point sits at (or near) the ℓ(α)
minimum where ℓ = 1/(2κ_c).

## §2 — Deliverable and consumption

**Deliverable:** a derivation, from registered S1b/S1c objects (with
founder physical-picture input where the DP displacement mechanics
requires it — the founder is the source of the mechanism), of χ_static
and hence α, including whether the correct self-consistency is the
continuum sum, the discrete sum, or a third object (e.g., a
polarizability with its own r-dependence or a local counterterm — see
the DISC-1 completeness question, §3). **Consumption:** selects or
replaces the branches of the rider-v2 conditional family; unlocks
single-value FA-C2 consumer statements; feeds the FA-C2 re-derivation
campaign (which remains QUEUED behind this item for any single-value
target; conditional-family work may proceed independently).

## §3 — Adjacent question inherited by FA-C3-DISC-1 (scope amendment, adopted from GPT verbatim in substance)

The discovery route does NOT reopen "1/r vs hop-Laplacian" as equal
choices. Its inherited question is effective-operator completeness:
*does the registered coarse-graining and DP response mechanism uniquely
produce discrete polarizable sites coupled by continuum 1/r transport,
or are there additional local counterterms or graph-gradient
contributions at the same order?* FA-C3 itself remains CLOSED (5–0,
twice).

## §4 — The elevation ladder for the operating-point findings (registered from GPT's six-item burden, adjudication 2692 §8; Gemini's large-N spectral requirement and Copilot's motif-invariance requirement subsumed)

All operating-point findings (staggering site-count threshold and its
non-monotone onset curve; the 4D-compactness enhancement, 2694 §4;
α_crit; non-monotone ℓ(α); the minimum at α₀; ℓ = 1/(2κ_c) there; the
J2 map) are OBS-class, non-adjudicative. Any elevation motion must
deliver: (1) an infinite-lattice Fourier representation of the dense
1/r operator; (2) identification of the dominant complex-wavevector
singularity or resolvent pole; (3) an analytic onset condition for the
staggered sector; (4) the decay exponent derived as a function of α and
κa; (5) proof that the ℓ(α) minimum and the ℓ = 1/(2κ) relation are
exact or controlled-asymptotic rather than finite-window coincidence;
(6) finite-size convergence of λ_min(G) before α_crit is treated as an
infinite-Sea threshold. Motions that do not cite this ladder do not move.

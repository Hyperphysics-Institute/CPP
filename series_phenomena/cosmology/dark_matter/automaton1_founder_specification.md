# OPEN-DM-AUTOMATON-1 FOUNDER SPECIFICATION — the Moment rule pinned for simulation (21 July 2026): displacement = field-integrated (NOT one-GP steps); zero net field → no motion, no tie ambiguity; finite-patch exteriors = aggregated boundary sums; Perceive = order-free surface-integrated summation with DI-bits recycled — **and the magnitude law RESOLVED BY CITATION to the SR corpus: d = (v/c)·PSR_eff (the registered local form, OPEN-SR-H1-CLASS), leaving ONE narrowed pin: the map SSV_net → v/c**

**Patch 2729, 21 July 2026. Founder's original answers NORMATIVE
(verbatim §1); the Grok retelling/commentary is preserved as
secondary with the founder's three corrections recorded (§3).
Commitments 14–18 extracted. Reasoning: `reasoning/2729.md`.**

## §1 — Verbatim (normative)

**Q1 (magnitude):** "The Displacement of the CP will be the summation
of the DI-Bits, which arrive at the GP upon which it is positioned
each Moment. The DI-bits arrive from every GP each Moment that is on
the shell of their PSR (l_P), which is determined by the SSV_abs at
each GP. The SSV_net will determine the Displacement of each CP at
each Moment. The Displacement is not 1 Grid Point."

**Q2 (ties):** "The question is moot; there is no cost for a balanced
summed field. If the DI-bits summed to an SSV_net of zero, then the
CP would not move. Such Momentary stasis is not significant in any
way. If there is one DI-bit more in one direction than another, then
the CP will move in the direction of the single DI-bit. There is no
such thing as ambiguity. The CP moves in the direction that the SSV
directs. There is no cost or difficulty associated with a perfect
cancellation. Note: Such a result, perfect cancellation, is
vanishingly unlikely, given the dynamic range of possible sums."

**Q3 (boundary):** "The DI-bits arriving at a demarcated boundary
would be notated by the DI-Bit sum that was arriving on each of the
GPs from all of the GPs that were excluded by the boundary."

**Q4 (ordering):** "This is a question that is outside of the realm
of speculation and knowability/certainty. The question is more: can
such a system be considered implementable within the domain of a
plausible/economical information-processing algorithm of a Conscious
Point? As far as a practical/engineered design, a potential method of
implementing the GP scan is as follows: 'Each GP computes its
SSV_abs, which determines the radius of the shell; the receiver GPs
on that PSR spherical surface will receive DI-bits from the
transmitter GPs at that radius. The DI-bits inform the recipient GPs
of their vectorial information, as well as the type CP
(electron/quark) and polarity (plus/minus) of the source. So,
depending on the memory buffer or the number of gates available on
each GP, the DI bits would arrive and be counted serially or
processed in parallel. Either method would be workable; a
surface-collection receptor would be generally adaptable, as
regardless of the number of DI bits arriving in a Moment, the surface
collection counts and computes the vectorial, type, and polarity of
all the DI bits arriving each Moment. Those DI-bits would then be
repurposed/erased/imprinted with the SSV_net and _abs information as
well as the summation of e and q type data, and plus/minus data.'"

**Magnitude-law referral (verbatim):** "I know there will be a
percentage of the PSR, and it will be related to the Lorentz
constant, which has apparently been omitted from previous
computations using this modeling, probably in SR-1. So, I refer to
that prior work, which I did not compute."

## §2 — Commitments 14–18 + the citation resolution

14. **Field-integrated displacement:** per-Moment displacement is set
    by SSV_net (the summed arriving DI-bits), never a fixed one-GP
    step.
15. **Zero-field stasis; no tie mechanism:** SSV_net = 0 → no motion
    that Moment; a single-bit excess moves the CP; no cost, no
    ambiguity; exact cancellation measure-zero.
16. **Boundary aggregation:** exterior universes may be represented
    at a patch boundary by the per-GP aggregate sums the excluded
    GPs would have delivered.
17. **Order-free Perceive:** within a Moment the functional result
    is a surface-integrated sum — serial and parallel collection are
    equivalent; DI-bits are then repurposed/erased/imprinted (carriers
    recycled).
18. **Cost-free rewrite (ontological stipulation):** the DI-bit
    erase/imprint is "command and obedience" — a pure arithmetic
    information state change with NO energy cost. (Worker note,
    same-font: this stipulates the substrate outside Landauer-type
    thermodynamics; the entropy of commitment 12 is configurational/
    informational. PR4 is unaffected — it tests the EMERGENT
    statistics, not substrate energetics.)

**The magnitude law, resolved by citation:** the SR corpus registers
the per-Moment displacement of a moving pattern as **d = (v/c) ·
PSR_eff** — the LOCAL form, adopted over the absolute (v/c)·l_P in
the OPEN-SR-H1-CLASS campaign exactly because it preserves γ_SR(v)
at every background stress (reasoning 2486/2489; development 0736).
The founder's "percentage of the PSR, related to the Lorentz
constant, probably in SR-1" is this registered law. **The single
remaining pin:** the map SSV_net → v/c. **Candidate identification
(worker-proposed, awaiting founder confirmation, NOT consumed):
v/c = |SSV_net|/SSV_abs** — the net-over-absolute ratio, which is
dimensionless, automatically ≤ 1 (saturating at c when all arriving
bits align), and uses only quantities the Moment rule already
computes. AUTOMATON-1 charters on the founder's ruling on this one
line.

## §3 — The Grok commentary: adopted points, corrections, and two design resolutions

**Founder's corrections (recorded):** (i) PSR = **Planck Sphere
Radius** (the Planck length for that SSV_abs); (ii) no Landauer cost
(commitment 18); (iii) Grok inverted the roles — each GP RECEIVES
whatever is sent to it and TRANSMITS to the shell its own SSV_abs
determines; SSV_abs does not "look for reception."

**Adopted from the commentary:** the simultaneity concern is resolved
by declaring **synchronous semantics** as the automaton's registered
convention (consistent with commitment 17's order-freedom): all
SSV_abs values and all transmissions of Moment t are computed from
the Moment t−1 state; no intra-Moment feedback. Shell-radius
discretization to lattice shells is an implementation matter,
declared in the automaton's prereg with a resolution-robustness axis.

**Worker design resolution (the important one):** for the PR4
question — does the rule generate Gibbs statistics in the uniform
Sea? — the Q3 mean-field boundary would suppress exactly the
exterior FLUCTUATIONS whose statistics are under test (Grok's
sharpest point, and correct). Resolution: **the PR4 automaton runs on
PERIODIC boundaries (the patch as a torus universe)** — fully
self-contained, no exterior representation needed, the melee
generating its own agitation; commitment 16's aggregated-boundary
scheme is reserved for future NON-uniform studies (rods, gradients)
where it belongs, with its mean-field character disclosed there.

# OPEN-DM-ENDBOND-3 pre-registration: the in-context closure energy at dance strength — the ⟨E⟩(κ) curve on the registered bend family, with knob-free barrier/closure definitions, committed before any derivation

**Patch 2550, 18 July 2026. Status: OPEN-DM-ENDBOND-3 REGISTERED and OPENED at
pre-registration; NO derivation performed.** Founder-chartered ("Please proceed with
recommendations", this session) following the ENDBOND-1/-2 Branch-I closures. Inherits the
full discipline apparatus (2529/2537/2541/2544/2548). **Target-definition shift declared in
daylight (per 2549 §4): the object is no longer the isolated fragment bond but the
in-context closure energy — the quantity 2542's ΔE_close actually consumes.**

## 0. Why the instrument is the bend family (stated before any number)

A rigid ring cut at one seam leaves its end planes exactly one pitch apart
(2R₀·sin(π/16) ≈ 1.14 fm ≈ D) — still at contact. Seam separation and unbending are
physically coupled; any "open the seam, hold the rest" construction smuggles a knob
(threshold distance, hinge choice). The knob-free instrument is the registered corrected
bend family (2450: perpendicular planes, arc-length, κ = 0 → straight, κ_ring → the exact
closed ring, closure registry 1.3×10⁻¹⁵), sampled by the dance: the ⟨E⟩(κ) curve contains
the bend cost and the closure payoff as its shape, with no thresholds.

## 1. Definitions fixed (knob-free)

Pre-registered κ grid: **κ/κ_ring ∈ {0, 1/5, 2/5, 3/5, 4/5, 15/16, 1}** (κ_ring = 2π/(16·D)).
No fitting, no smoothing, no interpolation. Let κ* = the grid argmax of ⟨E⟩(κ).
- **If κ* is interior:** E_barrier ≡ ⟨E⟩(κ*) − ⟨E⟩(0) and **E_close ≡ ⟨E⟩(κ*) − ⟨E⟩(κ_ring)**
  — the ratchet picture's own two numbers (impulse buys the bend to the barrier; closure pays).
- **If κ* = 0 (monotone-decreasing, barrier-free):** **E_close ≡ ⟨E⟩(0) − ⟨E⟩(κ_ring)**
  = −(ring−straight), the closure-downhill reading (2453's).
- If κ* = κ_ring (closed ring is the maximum): ADVERSE-DIRECTION, recorded as-is.
Primary accounting ⟨Ep⟩; ⟨Etot⟩ disclosed. TC = 60, burn = 0.15 (v8 registered defaults).
E_close is the **interface total** by construction; no per-pair reinterpretation (Branch T).

## 2. Closed input list

1. **Geometry:** the 2450 corrected bend family at N = 16 planes (arc convention — the
   registered primary; the chord convention is excluded-considered: a ~10 % statics-level
   spacing variant, one axis too many for this first pass, available to a successor);
   2455-coded plane; alternating parity. **N = 16 is consumed as the registered candidate
   geometry (C7 stands); RODCLOSE-1's L = 16 conditionality rider attaches to every
   downstream use; NO L-selection claim rides on this campaign** (NB-S3a-1 untouched).
2. **Functional:** dance_v8 verbatim (2510; pinned κ_q = 132, κ_e = 44, kscale = 1.0).
3. **Standing pre-commitments:** 2521; trap-clause lineage (no composition reading); √5 fence.
4. **FENCED:** the E_qq map [40, 170] and the 102 lock (post-freeze gates); the −68.8/≈85
   echo family (demotion inherited from 2547 — internal-consistency sentences only; NOTE:
   the barrier-free reading is the echo's own construction family, so agreement there is
   expected-by-construction and carries zero evidential weight, stated now); the 2542
   statics B and kT_form results (consumers, not inputs).

## 3. Convention-union axes (2541 §3 verbatim)

- **A (discretization):** dt ∈ {τ_C/100, τ_C/50, τ_C/25} — the registered grid; the dt
  spread is the error bar. Post-hoc grid changes = Branch T.
- **B (speed normalization):** FREF_grid = global max |SSV| over ALL seven grid scaffolds
  (the K1 one-physical-constant principle at this comparison's scope) AND FREF_16 = the
  registered ring/straight value. Both computed; spread disclosed.

## 4. Gates (frozen; compared only after the E_close union band is frozen; fixed order)

**G1:** membership of the E_close union band in [40, 170] MeV. **G2:** proximity to the
102 MeV lock. Then the internal-consistency sentences (no gate force): (a) the v8
ring−straight at this grid vs the registered 2510 values; (b) the echo family.

## 5. Readings (frozen)

- **Band frozen, G1 passes** → E_close PINNED at dance strength; downstream revision patch
  propagates through 2542 (ΔE_close and survival collapse; kT_form band explicitly NOT
  collapsed — comparator-dominated). Win-class only if G1+G2 both pass with the band
  materially in-band AND the barrier structure is dt-stable (κ* identical across the dt
  grid); otherwise a banked pin.
- **Band frozen, G1 fails** → TENSION recorded (dance vs the E_qq map lineage); no repair.
- **Union band spans zero** → Branch I statistical limb; the ⟨E⟩(κ) curves bank.
- **κ* not dt-stable** → the barrier claim is UNDER-RESOLVED (banked curves, no barrier
  number); E_close under the barrier-free definition may still freeze if κ* instability is
  confined to interior points while ⟨E⟩(0) > ⟨E⟩(κ_ring) across the union.
- **ADVERSE-DIRECTION** (κ* = κ_ring across the union) → recorded as-is; standing
  disclosure package.
- No composition reading; no T_form re-derivation; any new √5 → fence.

## 6. Bookkeeping

79.5 % untouched. Queue after this: RODCLOSE-1 kinetic limb → plane-resident-fraction limb
→ δ_E → MW-MODES TC-extension. Next patch: 2551, under this document only.

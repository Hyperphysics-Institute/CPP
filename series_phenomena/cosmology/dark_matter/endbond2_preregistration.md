# OPEN-DM-ENDBOND-2 pre-registration: the stacking-bond depth by the dance route — definitions, closed input list, convention-union axes, gates, and readings, committed before any derivation

**Patch 2548, 18 July 2026. Status: OPEN-DM-ENDBOND-2 REGISTERED and OPENED at
pre-registration; NO derivation performed.** Collision check clean. Successor to
OPEN-DM-ENDBOND-1 (closed Branch I, 2547) under the founder bond specification (2547 §1)
and the FORK-SWITCH-1 working resolution (2457). Inherits the 2529/2537/2541/2544
discipline apparatus in full.

## 0. Target and payoff

**Target:** E_endbond — the inter-plane stacking/closure bond depth (2540 identity) —
computed as the dance-averaged energy cost of separating the two-plane stack:

  E_endbond ≡ ⟨E⟩(two planes, separated to non-interaction) − ⟨E⟩(two planes at the
  registered stack geometry), positive = bound,

under the registered dance_v8 functional (Patch 2510) — the instrument the corpus and the
founder (2547 §1c) identify as the correct one for a structure in which every CP is in ZBW
oscillation. Payoff unchanged from 2544 §0: a pin collapses the 2542 ΔE_close and survival
bands downstream; it does NOT narrow the kT_form band (comparator-dominated — restated so
a pin is not oversold).

## 1. Definitions fixed

The **separated reference** is two *independent* single-plane dances (exact
non-interaction): ⟨E⟩_sep = 2·⟨E⟩(isolated plane). A finite-gap (20 fm) two-plane run is
disclosed as a robustness check only. The **stacked configuration** is the two-plane
fragment at pitch D = 1.15 fm, alternating parity (2540 identity, numerically verified
2546). **Primary energy accounting: ⟨Ep⟩ (sampled potential)** — the convention of every
registered dance energy difference (2455 −68.8; 2467-D; 2510 r−s Ep); ⟨Etot⟩ = ⟨Ep+KE⟩ is
disclosed as sensitivity per the v8 output convention. Sampling window: TC = 60 Compton
periods, burn = 0.15 (v8 registered defaults).

## 2. Closed input list (nothing else may enter)

1. **Geometry:** Candidate-B element plane (2455-coded eCP diagonal placement, ratified
   3–0; a_q = 1.15 fm, R_e = 1.301 fm); pitch D = 1.15; alternating parity.
2. **Functional:** dance_v8 verbatim (2510) — choreography, reach rules, eCP preemption,
   qCP pile contention, SF-6 speed memory with the PINNED κ_q = 132, κ_e = 44 MeV/c²
   (2496; kscale = 1.0, never scanned toward a gate), soft-core saturation lengths
   (a_qq = 0.747, a_ee = 0.357, a_qe = 0.516 fm), signed soft-core sampled energy. Rule
   fidelity = verbatim code copy, with the single necessary adaptation being the scaffold
   (2-plane fragment / 1-plane), NOT the rules.
3. **Channel authority:** the 2547 founder capture — all three channels (core qq strong,
   qCP polarity, coat) are already realized inside item 2; no channel added or removed.
4. **Standing pre-commitments:** 2521; the 2529 trap-clause lineage; the √5 fence (φ in
   α_s = upstream registered lineage).
5. **EXCLUDED and FENCED:** the E_qq map band [40, 170] MeV and the 102 MeV contact lock
   (post-freeze gates G1/G2); the −68.8 dance echo and its ≈85 back-implication (demoted
   per 2547 §4 — internal-consistency note only, NOT a gate); the 2546 electric-statics
   partial (superseded-by-reading for bond purposes — not added, not subtracted); N = 16
   in any form; all rates/spectra (NB-S3a-1 limbs).

## 3. Convention-union axes (2541 §3 rule, verbatim: compute all, report union with spread)

- **A (discretization):** dt ∈ {τ_C/100, τ_C/50, τ_C/25} — the registered 2510 grid.
- **B (speed normalization):** FREF_local = max |SSV| over the fragment home scaffolds
  (the 2461 K1 principle applied at this comparison's scope) AND FREF_16 = the registered
  2510 ring/straight value (the same physical sea) — both defensible, both computed.
- Any convention chosen, added, or dropped after a number exists = **Branch T**.

## 4. Gates (frozen; compared only after the union band is frozen; disclosure order fixed)

**G1:** membership of the union band in [40, 170] MeV (the registered map).
**G2:** proximity to the 102 MeV contact lock. Then one sentence of internal-consistency
note on the demoted echo (no gate force). **Normalization pre-commitment:** the target is
the INTERFACE TOTAL (the quantity 2542's ΔE_close consumes); any post-result
reinterpretation as per-qq-pair (÷4) or per-channel to move it relative to a gate =
Branch T.

## 5. Readings (frozen)

- **Union band frozen, G1 passes** → E_endbond PINNED at dance strength with its
  convention band; downstream revision patch propagates through 2542 (ΔE_close + survival
  collapse; kT_form band explicitly NOT collapsed). Win-class only if G1+G2 both pass with
  the band materially inside the map band; otherwise a banked pin.
- **Union band frozen, G1 fails** → TENSION recorded as-is (dance vs the E_qq map
  lineage); no repair in-campaign; the tension becomes the named next problem.
- **Union band spans zero, or the dt/FREF spread exceeds the band's distance from zero**
  → UNDER-RESOLVED: Branch I (statistical limb), partials bank, no depth claim.
- **Sign negative across the union** (stack disfavored by the dance) → ADVERSE-DIRECTION
  recorded as-is; flows to the standing disclosure package; no repair.
- No composition reading; no T_form re-derivation (RODCLOSE-1 owns those); any new √5 →
  fence procedure.

## 6. Bookkeeping

79.5 % untouched. Queue: ENDBOND-2 R-A (next patch) → then the RODCLOSE-1 kinetic limb and
the plane-resident-fraction limb → δ_E → MW-MODES TC-extension. Next patch: 2549, under
this document only.

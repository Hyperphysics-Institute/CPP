# N2B-FUNNEL-3 pre-registration: the steep-width (w = 4) funnel upper bound at the finer dt-union {1/200, 1/400} — the successor declared at 2640, committed before any run

**Patch 2641, 20 July 2026. Status: N2B-FUNNEL-3 OPENED at pre-registration; NO
run performed.** This is the single declared successor from the FUNNEL-2 record
(2640 §2: "the w = 4 bracket pair at dt-union {1/200, 1/400}"), founder-free.
Governed by this document and the FUNNEL-1/-2 lineage (2620 → 2623 → 2624 M5 →
2639/2640). Classification quantities only; v = 0.10c; the DISC block is not
approached. Verify: none at prereg; execution next patch under this document
only.

## 0. Target and framing

FUNNEL-2 drew the soft-width upper bound (R_f(ON, w2) ∈ [10, 12)D, dt-stable)
but the steep width refused: b = 10 flipped SCA(1/100) → CAP(1/200) across the
halving, so per the ROB-2 discipline no wall was drawn, the dt-stable floor
registered at ≥ 9D, and the finer-union rung was declared rather than run
(no escalation was frozen in the 2639 prereg; a cheap off-prereg run is still
off-prereg). This cell IS that rung, frozen properly: the same registered
instrument, the same grid, one halving finer on both legs. The precedent
governing the stopping rule is ROB-2's (2637 §3): **exactly one extra union
beyond the one that failed to draw; disagreement at both unions reads
UNDRAWN-CONFIRMED and the successor class becomes derivational.** The
{1/100, 1/200} union already failed to draw at this boundary (2640); this
cell's {1/200, 1/400} union is therefore the LAST instrumental attempt.

## 1. Registered inputs (verbatim; zero freedom)

Engine, launcher, classifier: exec-load of the registered 2624 artifact,
identical to 2640 (single-incident funnel on the settled 4-square, launch
[b·D, 0, 4D], v = −0.10c ẑ, TC = 120, η = 0.5 ON, registered-form classifier —
the OFF-form amendment is not implicated; every cell here is sink-ON). Width
w = 4 only. Launch-geometry scope clause carries verbatim from 2639 §1 item 2.

## 2. Control (gating; run FIRST)

**C-F3 — convention-pin on the finer union's shared leg:** b = 9, w = 4, ON at
dt = 1/200 must reproduce the 2640 dt-stable fact: CAP. Failure → RC4, nothing
reads.

## 3. Procedure (frozen walk)

Walk the 2640 grid upward from b = 9: for each b ∈ {9, 10, 12, 14, 16} in
order, run BOTH legs of the union {1/200, 1/400}; record the class pair. Stop
at the first b whose pair is dt-stable non-CAP (both legs agree, neither CAP).
Per cell, report class pair, d_inc at both legs, dcen; any FRG on its own
line.

- **Bracket definition (frozen):** last-CAP = largest b with a dt-stable CAP
  pair; first-non-CAP = smallest b above it with a dt-stable non-CAP pair,
  with every intervening grid cell's pair disclosed.

## 4. Frozen readings

- **RF3-BRACKET:** a dt-stable (last-CAP, first-non-CAP) pair exists on the
  grid with no dt-unstable cell between them → **R_f(ON, w = 4) registers as
  the bracket**; the 2640 "≥ 9D, upper bound undrawn" entry is superseded by
  citation. The width-coherence observation (soft out-reaches steep iff the
  brackets order as the analytic wells b_W(w2) = 4.5 > b_W(w4) = 2.5) is then
  re-read against the drawn pair and registered as observation only.
- **RF3-BRACKET-WITH-MARGIN-NOTE:** a dt-stable bracket exists but one or more
  cells BETWEEN the pair are dt-unstable → the bracket registers with the
  unstable interior cells disclosed as undrawn interior points (the bound is a
  bracket, not a boundary curve; interior marginality does not veto the
  endpoints).
- **RF3-EXCEED:** every cell through b = 16 reads dt-stable CAP →
  R_f(ON, w4) ≥ 16D; the scan class TERMINATES per the 2639 ruling
  (asymptotics are derived, not scanned).
- **RF3-UNDRAWN-CONFIRMED:** the walk cannot produce a dt-stable bracket (the
  marginal flipping persists at the finer union) → the steep upper bound
  registers UNDRAWN-CONFIRMED at this instrument class, both unions' raw
  pairs disclosed; successor DERIVATIONAL (the steep-width capture boundary
  becomes a FORM-1-lineage question — the same routing ROB-2's RM2 would have
  taken). No third union; no grid nudge.
- **RF3-RC4:** C-F3 fails → nothing reads.

## 5. Fences

Classification only — no rates, no σ, no flux, no relic contact; v = 0.10c
only, DISC block untouched; σ_cap withheld; EDGE-2(i) queued; the 2624 and
2640 records unedited (superseded by citation only where RF3-BRACKET fires);
FW-1 not implicated; the w = 2 results of 2640 untouched under every reading;
**79.5% untouched under every reading.**

## 6. Bookkeeping

Prereg: this document (Patch 2641). Execution: `code/2642_n2b_funnel3.py`,
single stage; record: `n2b_funnel3_record.md` (Patch 2642). DEP-1 row at the
execution patch if a registration lands. Reasoning fragments at both patches.

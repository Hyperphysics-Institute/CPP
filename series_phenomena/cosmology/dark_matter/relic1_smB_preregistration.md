# OPEN-DM-RELIC-1 — SM-B pre-registration: direct screened capture — the computation structure, termination question, and readings, committed before any number

**Patch 2526, 16 July 2026. Status: SM-B OPENED at pre-registration; NO capacity computation performed.**
**Anti-contamination lock (2524/2525): no SM-A vessel/dilation language may appear in SM-B's derivation.**
**Verify:** `code/2526_smB_prereg.py`.

## 1. SM-B defined (the founder's original attractor picture, on registered machinery)

The shell's k bare −qCPs source the registered screened color residual (0757: V(r) = (E_c·r_c/r)·e^(−r/R_scr),
anchors E_c ~ 0.3 MeV at r_c = 1 fm, R_scr ~ 15–30 fm). Paired feedstock — qDPs, hDPs, hTetras, Path-#1 ribbon
segments — is drawn onto the shell and condenses at its surface; the surface templates rod growth; rods close
into N=8 rings (the registered OPEN-SS-43 size selection). A flux check settles the regime class up front: Sea
feedstock density is lattice-scale (4.9×10³⁹ cm⁻³ conservative) with thermal velocities at kT_form — the
capture flux saturates any surface instantly. **SM-B is therefore CAPACITY-limited, not flux-limited: the
computation's entire content is the terminal coat — how much feedstock one shell captures before growth
self-terminates — and the closure counting from coat to rings.**

## 2. The computation structure (fixed now)

> **m_shell = C_coat / U_ring**, with **U_ring = 32 hTetra-equivalents per ring** (the 2519 decomposition —
> the accounting convention is hTetra-equivalents throughout; qDP/hDP feedstock converts at registered
> stoichiometry: 1 hTetra = 1 hDP-A + 1 hDP-B = 2 qCP + 2 eCP).
> Target: m_shell/k = 0.4468 ± 0.0054 ⇔ **C_coat/k = 14.30 ± 0.17 hTetra-equivalents per shell qCP** —
> the same 14.30 branching number from Patch 2519, resurfacing as it must (consistency, not coincidence).

## 3. Pre-committed questions (answered by the computation, in this order, no reordering)

- **Q-B1 — the termination principle (decisive).** What stops the coat growing? Candidates, committed now:
  **T-1 (primary): color self-screening** — the adsorbed coat progressively screens the seed's residual; the
  capacity is the SCREENING STOICHIOMETRY (how many adsorbed units extinguish the shell's exterior residual),
  computed from the registered 0757 machinery IF it specifies that stoichiometry; if 0757's registration depth
  does not determine it, T-1 is Branch I (named blocker), not improvised. **T-2: geometric saturation**
  (monolayer/multilayer packing limit at lattice spacing) — evaluated only if T-1 is Branch I. **T-3: epoch
  end** (formation window closes at kT_form falloff) — requires unregistered kinetics (NB-S3a-1 class);
  named, expected blocked. Post-hoc selection among T-1/T-2/T-3 after seeing which lands near 14.30 = Branch T.
- **Q-B2 — k-linearity (Q-m1 transferred).** Structural expectation: capacity ∝ shell surface ∝ k (each shell
  qCP claims ~d² of surface and its own capture zone), giving p = 1 and k cancellation. Must be VERIFIED in
  the computation (screening-stoichiometry termination could in principle scale sublinearly if adsorbed units
  screen collectively); if p ≠ 1, k rides in at its registered bound (k_max 74–313, E_qq-window-spanned,
  Patch 2525 by-product) and the band widens accordingly — reported, not narrowed.
- **Q-B3 — closure counting.** Coat-to-rings requires rod closure at N=8 elements; the counting must use the
  registered OPEN-SS-43 selection (N=8 sole survivor) and the registered element/ribbon geometry (0870 G1;
  2433/2443). Any closure-efficiency factor must be derived (geometric/combinatorial) or the reading is
  Branch I; a tuned efficiency = Branch T.

## 4. Readings (frozen-band mapping, committed now)

The computed C_coat/(32k) band — with ALL registered windows propagated (E_c ~ 0.3 MeV anchor spread as
registered; R_scr 15–30 fm; E_qq 40–170 MeV where it enters; k 74–313 iff p ≠ 1) — against the frozen
branches: **D-strong** [0.436, 0.458] (band overlap, distribution not central value, per charter v1.1);
**D-directional** [0.30, 0.67]; outside → **K1-direction** → **D3** (ratio stays calibrated; record clean).
Conditionality ledger on any D-branch: FI-RELIC-1 + NB-S3a-2 + FI-RELIC-3(a) (the shell structure) + whatever
Q-B1 imports. **OBS-RELIC-1 fence, maximum tension, restated:** the coat/closure computation is surface
combinatorics on a φ-lattice sphere — precisely where √5 could emerge honestly; every derivation step must be
documented independent of the fenced observation, and any step whose only justification is its output = Branch T.

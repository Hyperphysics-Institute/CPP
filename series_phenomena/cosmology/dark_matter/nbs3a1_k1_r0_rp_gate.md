# NB-S3a-1 K1 R-0 executed: the RP-GATE — controls PASS, production representability FAILS structurally; K1 CLOSED BLOCKED per the pre-registered reading, with the spec/physics questions isolated for founder adjudication

**Patch 2565, 18 July 2026. Governed by `nbs3a1_k1_preregistration.md` (2564) ONLY. Artifact:
`code/2565_k1_r0_rp_gate.py` (all pre-committed readings frozen in the script header before any run;
15 s). Status: R-0 EXECUTED; **K1 CLOSED BLOCKED under prereg reading 6(a)** — Branch I,
founder-routed, named gap "unbound-state representation" (two-part, §3).**

## 1. The minimal extension and the controls (all PASS)

The translating-home extension was implemented exactly as pre-registered (H ← H + V·dt per step;
dest logic byte-identical; three marked lines). All 18 control cells pass across the full
dt × FREF union (FREF_dp = 48.24, FREF_sys = 47.66):

- **C-1 free flight:** displacement 56.30–56.35 fm vs expected 56.35 (≤0.1% error, far inside the
  declared 10% tolerance); internal cohesion ≤ 0.164 fm. The dance translates rigidly and coherently.
- **C-2 over-barrier (500 MeV):** ESC/ESC in every cell under both classifiers.
- **C-3 bound-state recovery (contact, v = 0):** CAP/CAP in every cell — the classifiers correctly
  recover the registered bound lineage; no false-adverse-by-instrumentation.

**The extension is sound for what it does:** ballistic transport of a bound structure is faithfully
representable with no new rules.

## 2. G-READ-4: the production representability census (FIRES)

At the production initial condition (separation 6·a_qq), the reach lists built at initialization
contain **no cross-DP indices**, and the oppr-empty fallback never fires (each CP's own partner
occupies its list). Under the instrument's letter — reach as a registration-time static object, as
in every prior campaign, where geometry never changed — **cross-DP retargeting is impossible for
the entire run, and σ_cap ≡ 0 structurally.** This is representation, not physics. Per the
pre-committed gate logic, the RP-GATE **fails for production** even with all controls passing.

## 3. The named gap is TWO-part (the diagnostic sharpened it)

The non-registered live-reach diagnostic (same reach-S rule, re-evaluated on current positions each
step) was run after the verdict to inform adjudication. Result: **even under live reach, the 5 MeV
head-on probe ESCAPES in every cell.** Diagnosis, stated plainly: in the minimal extension the home
velocity is a kinematic constant — nothing in the instrument lets contention forces alter home
motion. Capture requires the incoming DP to shed translational energy into the encounter; with
perpetually translating homes, the scaffold drags its CPs through and out regardless of what
contention does. So production capture is unrepresentable on two independent grounds:

- **Gap (i) — spec question (2555/2556 class):** is reach a registration-time static object (the
  letter; the question never arose on static geometry) or a definition evaluated on current
  positions (live reach)? Founder spec authority.
- **Gap (ii) — physics question (the deeper one):** the instrument has no channel for momentum
  exchange between the contention layer and the scaffold layer — homes are kinematic inputs, not
  dynamical objects. The §6g picture (collisions displace rod ends; impulse buys bend) implies such
  a channel EXISTS physically; its instrument-level expression is not registered. Any rule
  supplying it is new dynamics = founder territory by the charter's kill condition.

## 4. Options package for the founder (named for adjudication, none adopted)

- **O1 — two-layer dynamics:** live reach + home back-reaction (homes acquire ΔV from the
  cycle-averaged cross-DP force, with the SF-6 inertia pins governing the relaxation). Closest to
  the current instrument; requires registering one new rule (the back-reaction law).
- **O2 — v-vector dance:** for scattering contexts, drop homes; CPs carry persistent velocity
  vectors with SF-6 inertial relaxation, direction no longer slaved to dest. A larger
  re-instrumentation; arguably closer to the postulate layer (momentum lives in the CPs).
- **O3 — statics-adjacent re-scope:** abandon trajectory choreography for K1; register a capture
  CRITERION instead (capture iff E_rel below the accessible well depth at reachable geometry — a
  2526-flux-check-class computation). Cheapest honest route; sacrifices σ(E_rel, b) structure for
  a capture threshold; K2–K4 would consume the threshold form.

## 5. Reading (per 2564 §6(a), frozen)

**K1 CLOSED BLOCKED.** Banked: the validated minimal extension (ballistic transport of bound
structures — reusable by any future campaign needing a moving frame); the validated classifier pair
(C-3 recovery clean); the two-part named gap with its diagnostic record. **K2 does not open on a
blocked K1 without founder re-charter.** No composition reading; no rates; nothing here touches any
registered statics result. Dated line to the standing disclosure queue (which, per the 2563 rider,
accretes until a dispatch actually fires — this blockage is itself a candidate trigger under the
founder's rule (a): blocked-and-needs-adjudication, though the adjudication needed here is the
FOUNDER's, not the panel's, per PD-006 spec/physics authority).

## 6. Bookkeeping

79.5% untouched. Queue: founder adjudication of §4 (selects O1/O2/O3 or redirects) → K1 re-charter
or re-scope under a fresh prereg → then the standing order (K2 …; plane-resident-fraction limb; δ_E;
MW-MODES TC-extension). Next patch: the founder adjudication capture, whenever it arrives.

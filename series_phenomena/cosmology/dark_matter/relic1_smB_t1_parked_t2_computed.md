# OPEN-DM-RELIC-1 — SM-B executed under the locked order: T-1 PARKED (Branch I, honest); T-2 COMPUTED — the maximal geometric bound lands within ×1.5–2.2 of target; route ALIVE on one named pin

**Patch 2527, 16 July 2026.** **Verify:** `code/2527_t2_geometric_bound.py`.

## 1. T-1 verdict: PARKED at its pre-registered honesty condition (Branch I)

The registered screening files were read in full. Finding one, recorded for accuracy: the earlier grep
conflated two files — **0757 is the EU-1 occupation-context Debye analysis** (it registers the Debye-sphere
population formula N_D = (4π/3)nλ_D³ and the finding that CPP's on-GP setting EXITS the Debye regime — no
screening cloud forms in the stack context); **the color-screening registration lives in OPEN-SS-43/1858**:
the Sea screens single-hadron color at ~1 fm (SF-5 confinement scale); the N-element rod residual is
V(r) = (E_c·r_c/r)e^(−r/R_scr) with r_c = 1 fm the registered convention. **The registered screening is an
AMBIENT-MEDIUM property — lengths, residuals, and saturation routes; nowhere does the corpus specify an
adsorbate stoichiometry ("X captured units extinguish one bare charge").** Constructing one would be an
unregistered polarization-response model. **T-1 PARKS. NB-T1-1 (named blocker): adsorbate screening
stoichiometry unregistered.** Reframing recorded: the capture zone is the PRE-EXISTING screened horizon
(radius ~r_c) — the shell's reach is ambient-set from the start, not coat-limited.

## 2. T-2 computed: the maximal geometric bound, registered inputs only

Capture zone per shell qCP = the screened horizon, r_c = 1 fm (registered). Density from registered geometry:
every GP carries a CP (glossary); nearest-neighbor spacing = the 600-cell edge = l_unit/φ (AXIM-2, edge = 1/φ
in circumradius units; l_unit = 0.589 fm); coordination z = 12 (registered) ⇒ packing constant bracketed
c_pack ∈ [1, √2] (FCC value √2 for z = 12; the 4D→3D tessellation detail is the one small inference, flagged).
Conversion: 4 CPs per hTetra-equivalent; U_ring = 32 (2519).

> **m/k = (π/96)·c_pack·φ³·(r_c/l_unit)³ ∈ [0.678, 0.959] vs target 0.4468 ± 0.0054.**
> **Necessary condition PASSED with headroom ×1.52–2.15** (pre-stated asymmetry: corrections to a maximal
> bound only SUBTRACT — species composition, structural yield — so max ≥ target is the route's survival
> condition, and it holds; had the maximal landed below target, the route would be dead here).
> **Required yield fraction: f = 0.466–0.659 — roughly HALF the zone content must be hTetra-formable.**

**Fence protocol executed at the point of maximum tension:** √5 appeared in the closed form — via
φ³ = 2 + √5 — and its provenance is the REGISTERED edge length 1/φ (AXIM-2, glossary), documented here as
independent of the fenced observation. Derived, not summoned; the fence held.

## 3. Reading (against the frozen bands): STRUCTURAL-PARTIAL — route ALIVE on one named pin

D-strong: NO (band above window). D-directional: NOT claimable (the maximal band [0.68, 0.96] sits above
[0.30, 0.67]; claiming directionality from an upper bound would be self-serving). K1: NO (an upper bound
forces nothing). **The reading is STRUCTURAL-PARTIAL: the zero-knob geometric form is derived and the target
sits comfortably below it at f ≈ ½ — a species-composition fraction of exactly the kind the registered
0672a machinery (eDP:qDP 1:1 charge-conservation lock; hDP/hTetra sink ordering; super-additive-binding
assumption) is built from.** **NB-T2-1 (the named pin): derive f — the hTetra-formable fraction of ambient
Sea content — from the registered composition locks and sinks, as its OWN pre-registered derivation** (its
readings committed before computing; needed value 0.47–0.66 recorded HERE, in advance, so the future
derivation is judged against a pre-stated window it did not choose). Perspective line for the record: this
campaign's distance-to-target has gone 44 orders (D2) → factor ~2 (T-2 maximal) → one composition fraction.

## 4. Campaign status

S3b = STRUCTURAL-PARTIAL at depth (T-1 parked NB-T1-1; T-2 computed, pin NB-T2-1). Conditionality ledger
unchanged + T-2's flagged c_pack inference. 79.5% untouched. **Next: the NB-T2-1 species-composition
derivation (own pre-registration first) — the single derivation between the campaign and a genuine reading.**

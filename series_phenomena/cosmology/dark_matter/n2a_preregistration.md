# N2-A pre-registration: the sink derivation — the lineage's binding constraint (no drag on uniform motion), the three derivation routes, the frozen validation battery, and readings, committed before any derivation

**Patch 2594, 20 July 2026. Status: N2-A OPENED at pre-registration; NO derivation performed.**
Governed by the N2 charter (2593) and this document only. **Verify: none at prereg.**

## 0. Target

The home→Sea energy-transfer law: its FORM and its COEFFICIENT, derived from the registered
SF-6 capture-and-delay lineage (the ramp-hold-release protocol that pinned κ_q = 132 /
κ_e = 44; the 2498–2502 SR-track reconciliation; the 2451 drive identities; the registered
dance relaxation form) with zero freedom — or the honest Branch structure below. The law, if
it lands, registers as a NEW MEMBER through admission gates enumerated here (the G-CONS-class
checklist), and its validation battery is the charter's five, frozen at 2593 §1.

## 1. The lineage's binding constraint, frozen first (C-SR)

**The sink must vanish on uniform translation.** Citation chain: the SR-1 grounding and the
α-ruling closed the ε-arc on capture-and-delay being REACTIVE for uniform motion — inertia
without drag, else no inertial frames and no momentum conservation, which the registered SR
track grounds at W2 strength. Therefore the derived dissipation couples to ACCELERATION and
to internal/oscillatory motion only — radiation-reaction-class, never −γV. **This converts
the charter's Galilean hazard into a derived prediction:** a uniformly boosted cluster is
undamped (the battery's boost cell tests exactly C-SR); oscillation about equilibrium damps;
and the CPP substrate's preferred Sea frame enters only where acceleration histories do.
Any candidate law violating C-SR is dead on arrival regardless of its other virtues.

## 2. Derivation routes (order locked; each cited to its lineage)

- **R-A1 — the relaxation-face route:** the registered dance velocity law
  v ← v·e^(−dt/τ) + v_set·(1−e^(−dt/τ)) is a first-order relaxation whose dissipative face,
  read against C-SR, must be extracted as an acceleration-coupled term (the relaxation acts
  on CHANGES of v_set, not on v itself in uniform motion). Deliverable: the implied per-event
  energy shed for a home whose force-set velocity changes by Δv_set, with τ's physical
  content resolved from the lineage (κ = m is the pinned identity; what the ramp-hold-release
  data fixes about the timescale is the route's central question — μ's operational FREF role
  must be either grounded or eliminated, never carried as a knob).
- **R-A2 — the microscopic capture-and-delay route:** energy shed per Moment cycle from the
  drive identities (f²_osc = 48/49; the duty structure; the hop clocks; the per-cycle
  polarize-release accounting) — a fractional per-cycle loss δE/E for oscillatory home
  motion, derived, compared against R-A1's coefficient. **Agreement within a factor-band →
  the coefficient CROSS-DERIVES (the strongest outcome); disagreement → both recorded, the
  discrepancy is a finding, and the band between them is R-A3's input.**
- **R-A3 — the banded fallback:** if the lineage under-determines the point coefficient, the
  BAND it does constrain registers (upper/lower with grounds); the validation battery runs
  at both band edges; consumers (N2-B rates) carry the band.

## 3. Admission gates for the resulting member (enumerated at registration, per the checklist)

The strong-pair control WITH the sink (binding must survive — the sink eats oscillation
energy, not bond depth; the pair settles toward r_eq, ledger showing the shed as a Sea
deposit); the ×2.0 expansion cell converging with the deposit ledger balancing the released
well depth; the 5-cell completing sub-relativistically with total energy closed
(KE + U + Sea-deposit = const within dt-convergent tolerance); **the C-SR boost cell:** the
0.3c uniformly-boosted square must show ZERO sink activation (deposit ledger flat) while its
internal oscillation damps identically to the rest frame; dt-convergence across the union;
declared seeds where noise enters. Any gate failure → the candidate dies; a repaired
candidate is a NEW candidate re-entering at the top.

## 4. Readings (frozen)

- **R1:** a zero-freedom law (R-A1/R-A2 cross-derived or singly-derived with the other route
  Branch-I'd on named grounds) passing all §3 gates → **THE SINK REGISTERS**; N2-B opens;
  the B-4 basin and the 5-cell growth event re-run under it as N2-B's first cells.
- **R2:** the banded law (R-A3) passing the gates at both edges → SINK REGISTERS BANDED;
  consumers carry the band; the point value routes to the founder as the named open
  substrate quantity.
- **R3:** the lineage under-determines even the band, or every candidate fails C-SR or the
  gates → Branch I, founder-routed, with the derivation ledger showing exactly where the
  registered content runs out — the sharpest possible question for the founder's substrate
  picture (how much energy does one capture-and-delay event actually keep?).
- No reading touches the N1 registration, the FORM-L record, or 79.5%. Any new √5 → fence.
  The Sea-deposit ledger is mandatory in every cell from the first candidate onward.

## 5. Bookkeeping

79.5% untouched. Next patch: the N2-A derivation (R-A1 → R-A2 → cross-check), under this
document only. The replication dispatch (2595) runs in parallel and shares nothing with
this campaign but the calendar.

# FA-SG-R1 CHARTER (FROZEN) — the panel-queued remedy arc, chartered from the frozen inputs file: demonstrate the FG-OTHER readout (sign-staggered response, exponential envelope, ℓ = 0.091 ± 0.002 fm) representation-independent across independently chosen arenas, harden the envelope extraction, execute the mandatory J3 normalization attack — zero-parameter operator untouched, concordance criteria frozen before any leg runs, the F1–F4 scope fence in force throughout

**Patch 2679, 20 July 2026. Chartered at the founder's word ("Please open
the FA-SG-R1 charter"), consuming `fa_sg_r1_charter_inputs.md` (FROZEN
2677) as the frozen input per the 2678 handover — the §1 remedy spec is
the arc's content; the §2 scope fence is its focus rail; both bind here
unweakened. The spec's substance is the panel's own, registered 5–0 at
2674 §3 (Q1b SUFFICIENT-WITH-REMEDY-QUEUED); where the spec is silent on
operational detail (concordance thresholds, leg-2 operationalization,
sequencing), the operationalization is the adjudicator's under PD-006,
marked [ADJ] and votable at the R1 returns — the panel's spec language
and the founder's fence are not weakened anywhere. Everything below is
FROZEN before any leg executes; nothing is renamed after results exist.
Execution: NEXT session, fresh context (2527 governance pattern).
Amendment note, same-font: a prior charter draft (presented but never
landed in the repo) was composed against 2674 §3 directly, blind to the
then-unpushed 2677 inputs freeze; per this charter's own §6 discipline it
was withdrawn and re-chartered from the frozen input rather than
silently patched. 79.5% not in scope.**

## §0 — Target

Show that the FA-SEA-GREEN S2 findings — the sign-staggered lattice-scale
response, the cleanly exponential envelope, and the numerical
ℓ = 0.091 ± 0.002 fm (βd = 12.6 ± 0.3) — are properties of the derived
operator M = I + αG, not of the particular extended-instrument
realization that read them out (A1's condition: the staggered component
is "a property of the derived operator until representation-independence
is shown; the FA-SG-R1 remedy is that showing"). Discharges on offer:
the observation-grade cap on ℓ becomes packet-liftable (R1-PASS), or the
readout's numerical content is honestly demoted (R1-FRAGILE / R1-NEG),
or a revised value supersedes (R1-SHIFT). J1 is addressed under every
class; J2 is re-examined; J3 is attacked by name; J4 is hardened.
Throughout: R1 is a **femtometer-scale, IR-safe robustness check of a
screened static operator** (inputs §2) — it does not touch cosmology.

## §1 — Inputs on record (all frozen upstream; none re-derived here)

The operator and its inputs are the FA-SEA-GREEN zero-parameter set,
consumed as-committed: discrete-site scattering form M = I + αG with
G_ij = 1/r_ij (S1b, 2669); gap κ = 2/d_DP with d_DP = ℓ_edge = 0.364 fm
(S1c, 2670; INF-S1C-1 stands attackable as registered); coupling
α = κ²/(4πn) with n = √2/ℓ_edge³ — n is the **LOCAL DP number density**
(the 2527 4D→3D-flagged density, packet N3; per fence F1 no cosmic count
substitutes for it anywhere in this arc) — giving α = ℓ_edge/(π√2) =
0.0819 fm (2671 §1); I6 normalization at the shell nearest d = 1.15 fm.
Reference readout: `fa_sea_green_s2_readout.md` (2671) with scripts
`code/2671*.py`. Frozen pre-charter input: `fa_sg_r1_charter_inputs.md`
(2677). **No input above may be re-tuned, re-fit, or re-chosen in any
leg. Any leg that varies an input does so as a labeled robustness scan
against the frozen baseline, never as a fit toward any target value.**

## §2 — Legs (the panel's five per inputs §1, verbatim in substance)

**R1-L1 — Tessellated multi-motif arena (all seats; GPT's phrasing
governs).** The envelope and the staggering demonstrated stable across
**independently chosen large realizations / z = 12-equivalent
constructions** at registered-arena scale. [ADJ operationalization: a
minimum of THREE independent large-arena realizations differing in
construction (motif/tessellation/orientation/boundary treatment), each
z = 12-equivalent, each passing the instrument sanity check (site count
reported; coordination 12; minimum chord = the construction's edge
length) BEFORE the operator lands on it; the FCC proxy of 2671-D2 counts
as one realization already run — at least two genuinely new
constructions execute in this arc.] Per-realization report: staggering
present/absent; envelope form; ℓ with per-realization instrument band.
**J2 rider (inputs §1, carried live):** the d_DP = ℓ_edge level
assignment (INF-S1C-1) is not forced by SS-2 and ℓ scales linearly with
d_DP — across constructions, report whether the emergent scale tracks
d_DP; the multi-motif arenas are J2's registered re-examination venue.
**The J2 attack is severed from the N2 d_DP/4 coincidence** (non-
elevation clause; the coincidence supports nothing).

**R1-L2 — Registered-arena confirmation (DeepSeek): "that the same ℓ
emerges (not only the extended instrument)."** [ADJ operationalization,
flagged for the panel: the registered I1 arena was READOUT-INCONCLUSIVE
at 2671 §2 (compactness, disclosed pre-spectrum), so "the same ℓ
emerges" is operationalized as a **restriction/prediction test**: the I1
field (both distance metrics, as committed at 2671) shown quantitatively
consistent with the extended-instrument solution governed by ℓ — i.e.,
the extended solution, restricted to I1's geometry, reproduces the
registered-arena sign pattern and magnitudes within stated tolerance,
with the comparison statistic and tolerance chosen and committed BEFORE
the comparison runs. If the execution session finds a construction under
which a direct ℓ readout on I1 is possible, that stronger form
supersedes this operationalization and is reported as such.]

**R1-L3 — Envelope-extraction robustness battery (= METH-L2-021's own
promotion battery per its 2676 registration).** All named components,
none optional: (i) **multi-window fits** (Copilot); (ii) **Fourier-
filter removal of the staggered component** as an independent
cross-check on the |f|·r envelope readout (DeepSeek) — the filtered and
unfiltered extractions compared; (iii) **robustness against fitting
window, observable definition, and lattice-size choices** (GPT/inputs
§1.3) — the band reported as a function of each choice axis.

**R1-L4 — The J3 companion attack (MANDATORY; the panel's unanimous
highest-risk joint).** An **alternative site-level normalization of α
derived from the discrete-lattice Green function** (DeepSeek's
construction), tested against the continuum matching κ² = 4παn the arc
used **in the post-homogenization (κ·a = 2) regime**. The attack is
executed as a derivation, not a scan: derive α′ from the discrete Green
function at z = 12, compare to the frozen α, and propagate the
difference into ℓ (ℓ scales as α^(−1/2) through κ at fixed gap
identification; the propagation is reported explicitly, not assumed).
Per fence F1, the normalization uses the local DP density n only.
[ADJ: J3-CONCORD if the α′-propagated ℓ′ lies within the joint band from
R1-L1/L3; J3-REVISE otherwise — see §4.]

**R1-L5 — OPTIONAL J6 leg: the hop-Laplacian alternative assembly.**
Per Grok's adopted ruling: this is a **NEW pre-registered branch, not an
internal defect of the present derivation**. Its outcome classes are
frozen HERE so the option is exercisable without any post-hoc freeze:
**J6-CONCORD** (the hop-Laplacian assembly yields a sign-staggered
response with exponential envelope and ℓ compatible with the joint band)
/ **J6-DIVERGE** (it yields materially different structure or scale — a
representation-dependence finding in its own right, packeted as such,
adverse to neither branch by itself per the new-branch ruling).
[ADJ trigger discipline: L5 runs if legs L1–L4 leave a live
representation-dependence question, or at founder word; if it runs,
these two classes and no others apply.]

**Sequencing [ADJ]:** L1 → L3 → L2 → L4, with L3 executed on every L1
realization as it lands; L5 last if triggered. Each leg's numerical
outputs commit with a verify script per the reasoning-capture rider
before the next leg's comparisons run.

## §3 — SCOPE FENCE (inputs §2, F1–F4 — the founder-directed focus rail, IN FORCE VERBATIM)

The fence binds by reference to `fa_sg_r1_charter_inputs.md` §2, whose
text is authoritative; the one-line forms here are handles, not
restatements. Nothing fenced may be consumed, cited as support,
re-derived, or reopened inside the R1 arc:

- **F1 — the cosmic site count is not consumed.** N_GP ≈ 10⁶¹ / area
  count ≈ 10¹²² never enter; the shell sum converges within femtometers;
  J3 uses the local DP density n.
- **F2 — "holographic" = the LOCAL shell-superposition sense ONLY.** Any
  R1 sentence true only under the cosmological (area-law) sense is out
  of scope.
- **F3 — the 10³⁰ GP/ℓ_P number is prediction-firewalled and unused**
  (Grok-origin, Patch 0736, NO-THEO): it enters no R1 construction,
  normalization, or readout.
- **F4 — the CC arc is untouched in both directions.** The N⁴
  holographic-counting account stays DEMOTED (1103); the non-gravitating-
  Sea mechanism (D2/1161) and the SR-5b residual (conditional on D3-1)
  are neither strengthened nor weakened; R1 is not a re-derivation of
  the holographic nature of the universe.

**The fence is physics, not tidiness (inputs §3, carried as charter
ground):** R1's IR-decoupling is FORCED by the exponential screening it
confirms — a screened Green function is blind past a few ℓ, and the
horizon sits ~10⁴² ℓ away. The screened/unscreened split R1 lives on IS
the CC arc's split; R1 reinforces that separation and must not be framed
as reopening it. Any execution-session sentence that finds itself
needing a fenced quantity is, by that fact, out of scope — the sentence
is cut, not the fence.

## §4 — Guards (in force from this patch)

- **Concordance criteria frozen now [ADJ]:** L1 CONCORD = every
  realization shows lattice-scale sign staggering AND a cleanly
  exponential envelope over an asymptotic regime supporting ≥3
  independent fit windows, AND per-realization ℓ central values are
  pairwise compatible within 2× their combined 1σ instrument bands.
  L1 DISCORD = any failure of the above. The joint band is the union-
  weighted combination across realizations, reported with its
  construction stated.
- **No target-tuning:** no leg may select constructions, windows,
  observables, or normalizations conditioned on landing near 0.091 fm,
  near d_DP/4, or near any other privileged value. Choice axes are
  enumerated before results on that axis exist.
- **The non-elevation clause continues (packet N2):** ℓ ≈ d_DP/4 =
  1/(2κ) remains OBS-class, plausible-analytic, unproven, and
  non-adjudicative throughout this arc. An **analytic proof** of the
  envelope law or the 1/(2κ) identification from the operator form is a
  PERMITTED side-product (reported in its own section, separately from
  the legs); elevation of the identification happens only via the
  returns, never inside this arc.
- **The 2674 rider is untouched during execution:** no FA-C2 consumer
  sentence is edited, no registry promotion occurs, no grade cap moves,
  until the R1 packet's returns are adjudicated. The 2664 rider likewise
  travels untouched.
- **Breach discipline:** the blind-session protocol class's disclosure
  norm governs by inheritance — any deviation from this charter's frozen
  criteria OR from the F1–F4 fence is disclosed same-font and the
  affected leg re-executes clean; nothing is renamed after results
  exist.

## §5 — Outcome classes (frozen; nothing renamed after results exist)

- **R1-PASS:** L1 CONCORD + L2 consistent + L3 band ≤ the 2671 band's
  order (no window/observable/size axis blowing the band open) +
  J3-CONCORD. The observation-grade cap on ℓ becomes packet-liftable;
  the lift itself is the panel's call at the returns, never this arc's.
- **R1-SHIFT:** the structure (staggering + exponential envelope) is
  stable, but the numerical value revises — via a stable different value
  across L1 realizations, or via J3-REVISE (α′ propagating ℓ outside the
  joint band). The revised ℓ′ with its band is reported at observation
  grade; the packet adjudicates grade and consumer propagation; the
  re-derivation campaign (queued behind this arc) targets whichever
  value the panel sustains.
- **R1-FRAGILE:** staggering or envelope NOT stable across independently
  chosen realizations — the structure is construction-conditional. The
  FG-OTHER numerical readout demotes to construction-conditional status
  in the packet; the FA-C3-DISC-1 discovery route (re-scoped at 2674 §4)
  inherits the origin question.
- **R1-NEG:** the exponential envelope fails generally on extended
  arenas (the 2671 finding an instrument artifact). Registered
  adverse-direction, same-font, in full; the packet then carries the
  FA-C2 FORM question back to the panel (FORM was grounded 5–0 at 2674 —
  only the panel re-examines its own grounding).
- **J6-CONCORD / J6-DIVERGE:** as frozen in §2 R1-L5, applicable only if
  the optional leg runs.

Every class is reportable; every class routes to a CONV-001 packet; no
class is a failure of the arc — the arc fails only by breaching its own
guards or its fence. **FA-C3 remains CLOSED under every class** (the
closure was the panel's 5–0 adjudication at 2674 with the provenance
risk on the table — J1 "proxy adequate to kill the fork," all seats);
any seat wishing to revisit that closure does so via the R1 packet's
returns, not via this charter.

## §6 — Consumers and fences

The sole immediate consumer is the R1 packet itself: every leg's result
flows to CONV-001 before any registry motion. Standing after this arc,
under every outcome class: the FA-C2 consumer re-derivation campaign
remains queued BEHIND the R1 returns (2674 §5 / inputs §4 sequencing —
"the value to re-derive against must survive its own remedy");
FA-C3-DISC-1 discovery route standing, unscheduled; the CC arc (1103
demotion, D2/1161 mechanism, SR-5b residual, D3-1 condition) untouched
per F4; DEP-1 rows, DISC block, C7, 2513/2635/2626/2629, NB-S3a-1,
FW-1/ETA-1/AMB-1 riders, w ∈ {2.5, 3} quarantine, σ_cap withholding,
EDGE-2(i) queue, OBS-RELIC-1 fence — all untouched. founders_voice
untouched. REPL-1 and promotion remain founder-placed and are not gated
on R1. **79.5% untouched under every outcome class.**

## §7 — Governance

Charter frozen this patch; execution NEXT session, fresh context (2527
pattern). No pre-execution panel dispatch is required: the spec IS the
panel's, registered 5–0, and the fence is the founder's directive on
record at 2677; the [ADJ] operationalizations are disclosed to the panel
in the R1 packet alongside the results, votable there. Founder contact
points during execution: any breach or fence-collision finding, any
[ADJ] operationalization the execution session finds unworkable as
frozen (returned here for amendment BEFORE proceeding, never silently
patched — the same rule this charter itself just obeyed), and the L5
trigger if invoked at founder word — otherwise the arc runs founder-free
to the packet. Session-close recommendation stands at that seam.

Reasoning: `reasoning/2679.md`.

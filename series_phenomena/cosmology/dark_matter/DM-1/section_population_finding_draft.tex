% [Patch 2360: INSERTED into DM-1 main tex with both slate slots resolved
%  per the founder-ratified slate (2358). Retained as the pre-ratification record.]
% DM-1 SECTION DRAFT — "The population finding" (Patch 2357, 9 July 2026)
% Written dual-frame and per-pin-reading so the founder's slate decisions
% (frame F3, pin band-reporting, Clause 1) drop into a finished structure.
% Insertion target: after \section{Discriminant II} (sec:cores) and before
% \section{Falsifiability} in DM-1_substrate_dark_matter_candidate.tex, with
% \section{Falsifiability} then gaining the dimer-discriminant cross-reference.
% DECISION SLOTS are marked [SLATE-n]; each resolves on founder ratification.

\section{The population finding: what survived the kill}\label{sec:population}

\subsection{Provenance in one paragraph}
The capture channel of this paper's candidate was killed at the registered
evaluation frame (Gate-1/B1, Patch 2333), and the kill was closed on six flanks,
panel-reviewed, and never regraded --- it stands, and \S\ref{sec:falsify} carries
it. What survived is different in kind: the panel's one accepted wound (the
monodispersity assumption, J3) forced a polydisperse scan (2344) whose exclusion
boundary converged to $\times 1.074$ --- inside the anchors' own error budget ---
with a legible near-passing object: a \emph{population} of aggregates, not a
tuned particle. The demand-side audit (L4, 2345) then showed the registered
windows had quoted a heterogeneous literature at its steepest strands, and three
pre-registered gates plus a pin-provenance audit (G1--G3, L4-b; Patches
2349--2356) measured what the formation-realizable population can and cannot
satisfy. Every gate ran falsification-first with outcomes graded as written; two
of the six computations strengthened the kill at the registered frame while the
population passed elsewhere.

\subsection{Dual-frame statement [resolves with SLATE-1: frame F3]}
Reported side by side, per the adopted dual-frame rule:
\begin{itemize}
\item \textbf{At the registered (v1) frame:} the capture candidate is dead
(dSph shortfall $\times 1.6$--$2.1$, pin-independent under every audited pin
reading), and the formation-realizable population does \emph{worse} than the
unconstrained mixture ($\times 1.189$ vs $\times 1.074$) --- formation kinetics
\emph{strengthen} the kill where it was graded.
\item \textbf{At the audited-extended frame:} the population generated by the
registered 1855 kinetics at its \emph{natural} knob ($\alpha = 1$; no S(N)
steepness demand, $p_{\min} = 0$) passes the full anchor suite with margin
$\times 2.18$, survives the satellite counter-checks (evaporation clear
$\times 6$+; Concerto-viable collapse branch), and holds under the measured
transport-suppression envelope.
\item \textbf{At the audited-central frame:} passes exist but carry two named
strains ($\alpha \approx 3$--$6$; $S(N)$ steepness $p \geq 13$, one power above
the already-strained $N^{12}$) and split under the transport envelope by
$0.3\%$. The frame asymmetry --- extended natural, central strained --- held in
the same direction at all four gates; the papers state it rather than average it.
\end{itemize}

\subsection{The two-strand result, per pin reading [resolves with SLATE-2: band-reporting]}
The dwarf-scale literature splits into an isothermal strand (core-formation
fits, $\sigma/m \sim 3$--$40$ at dwarf velocities) and a gravothermal
\emph{collapse} strand (per-dSph modeling on Gaia pericentre orbits; Correa
2021: 20--150 across the classical dwarfs). The population reproduces the
collapse strand's signature ordering natively --- the density--pericentre
anticorrelation is the velocity dependence itself --- and the magnitude verdict
depends on one demand-side reading: the pin anchor's velocity assignment, whose
own provenance (a synthesis of galaxy-scale fits and $V_{\max} = 40$ core
viability) licenses $\langle v \rangle \in [50, 72]$ under a single collision-velocity
convention:
\begin{center}
\begin{tabular}{lll}
\toprule
Pin reading & Strand result & Population cost at the pass \\
\midrule
$\langle v\rangle = 50$ (steepest) & isothermal strand only; collapse strand & --- \\
 & collides with the pin ($s_{\rm req} \approx 17$, & \\
 & unreachable for any smooth $\sigma(v)$) & \\
$\langle v\rangle = 64$ (Elbert center) & \textbf{both strands pass jointly} & $\alpha = 100$, $p = 12.3$ (edge-riding) \\
$\langle v\rangle = 70$ (band top) & both strands pass jointly & $\alpha = 0.01$ (natural range), $p = 3.7$ \\
\bottomrule
\end{tabular}
\end{center}
The steep-edge reading is live (the galaxy-fit ceiling's per-source velocities
are not digitized); no point is selected. At the steepest reading the collision
is \emph{demand-internal} --- the collapse-strand curve itself exceeds the pin
ceiling $\times 2.8$--$5.6$ with no candidate consulted --- so no candidate of
any origin distinguishes itself there.

\subsection{What the population owes (the ledger, stated not hidden)}
(i) $S(N)$ must survive derivation under OPEN-SS-43 at the steepness the chosen
frame requires ($p_{\min} = 0$ extended / $13$ central / $12.3$ at the
both-strands Elbert point); (ii) the nucleation bias $\alpha$ at non-natural
pass points ($\approx 3$--$6$ central; $100$ at the Elbert both-strands point)
is an inverse-ledger coefficient awaiting mechanism; (iii) the rod-source
residual-channel correction (the transport envelope's unproxied sign) and the
per-source pin-velocity recomputation are the two named completion audits, each
able to move the texture but --- by the measured brackets --- not the frame
verdicts; (iv) the perturbation-level growth response is a registered residual
shared with DM-2.

\subsection{The discriminant [feeds \S\ref{sec:falsify}]}
A dimer-dominant or dimer-heavy population puts its lightest species at
$\approx 2.8$~GeV --- squarely in the low-mass direct-detection window --- and
the population hypothesis is falsifiable independently of any frame choice
through: (a) the branch-invariant F1 group-scale falsifier this paper already
carries; (b) the dimer-regime direct-detection channel (a population
\emph{requires} the light species; a null at the required abundance kills the
dimer-weighted corner of every passing region); (c) Clause 1's registered exit
(below). % [SLATE-3 cross-reference]

% --- END SECTION DRAFT ---

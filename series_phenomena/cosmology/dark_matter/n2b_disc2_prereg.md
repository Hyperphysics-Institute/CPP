# N2B-DISC-2 pre-registration: the repaired-window discriminator — dt-robust single-pass windows, the cumulative co-primary, and the pre-declared BANDED outcome, committed before any run

**Patch 2625, 20 July 2026. Status: N2B-DISC-2 OPENED at pre-registration; NO run
performed.** This is the repair candidate the 2624 RD2 verdict placed at the top of
the founder-free queue — a NEW candidate with its own prereg, consuming the 2624
disclosure bank as DESIGN INPUT only (S_cum dt-stability; the global-argmin window
convicted; the 22–30% high-γ decomposition dependence). Nothing from 2624's
disclosed-unread bank is cited as evidence; every gated quantity re-runs fresh under
this document. Governed by this document, the DISC-1 lineage (2620/2623), and the
2618 condition spine. Verify: none at prereg.

## 0. Target

The 2624 record convicted the WINDOW, not visibly the deposit: the single-pass
quantity wandered under dt while the cumulative ledger sat stable, and the two
decompositions agreed to four decimals at low γ. DISC-2 asks the same panel question
with instrument choices that cannot hide behind chaos: **is the per-encounter
deposit dt-convergent, window-robust, and decomposition-consistent — and where it is
decomposition-DEPENDENT (transit-γ), is that dependence itself a stable, registerable
band rather than noise?** The pre-declared honest outcomes include a partial:
deposits at transit-γ may be well-defined only up to a decomposition band; if that
band is dt-stable, consumers can carry it explicitly. The block's fate is frozen per
reading below — including the possibility that it converts rather than lifts.

## 1. Registered inputs (verbatim; zero freedom)

Engine, geometry, launch convention, drift-compensation clause, η points, widths,
TC = 120, constants: **verbatim from `n2b_disc1_2_prereg.md` (2623) → 2620 by
citation.** Instrumentation: `n1_disc2` = the 2624 `n1_disc` body with TWO tracked
times added (logging only, dynamics-invariant): **t_x** = first step time at which
the incident–centroid distance d_inc falls below R_in = 2D (declared once, here;
inside the well, pre-chaos, crossed monotonically on approach); **t_m** = time of
the FIRST local minimum of d_inc (step-level: d_inc[k] < d_inc[k−1] and
d_inc[k] < d_inc[k+1], earliest k). Windows, frozen:

- **W-A (primary):** deposits with t ≤ t_x + 2τ_C. If no crossing occurs, S_WA = 0
  with flag NO-CONTACT (reported, excluded from gates).
- **W-B (window-robustness check):** deposits with t ≤ t_m + 2τ_C.
- **S_cum (co-primary):** end-of-window cumulative Sea (the registered B1 reading).

Both decomposition methods (A: per-particle window-mean; B: collective
momentum-based) verbatim from 2620; D^B computed over W-A.

## 2. Controls (gating; run FIRST; any failure stops the cell unread)

- **C1 — convention-pin (METH-L2-015):** `n1_disc2` reproduces the registered 2609
  timing cell exactly (classification, Sea, gmax; deltas 0.0).
- **C2 — carried verbatim from 2623:** C2a Sea(η=0) machine-zero at the C2 cell;
  C2b Edrift(η=0) per-halving ratio ≥ 1.5 across 1/100 → 1/200 → 1/400.
- **C3 — decomposition sanity:** verbatim from 2620 (isolated square, both methods
  < 1 MeV).
- **C4 — window sanity (new):** at the η = 0, v = 0.95 cell (dt-stable SCA per
  2624 M4): t_x exists (the incident traverses R_in), S_WA(η=0) machine-zero; and
  at the ON low-γ anchor, t_x and t_m are both defined with t_x ≤ t_m.

## 3. Members (frozen grids; baseline-exposure guard from 2623 in force throughout)

- **M1 — gates at the anchors:** (v, b, w) = (0.10, 0, 4) and (0.95, 0, 4), η = 0.5,
  dt ∈ {1/100, 1/200, 1/400}. Per cell report: S_WA, S_WB, D^B, S_cum, Edrift,
  t_x, t_m, |Δcen|.
  - **G1 (dt-convergence, primary):** S_WA increments shrinking AND final increment
    ≤ 5% of S_WA(1/400), per anchor.
  - **G2 (method agreement):** |D^B − S_WA|/S_WA ≤ 0.10 at dt = 1/200 and 1/400,
    per anchor. Where G2 fails, the **band** β = |D^B − S_WA|/S_WA is read for
    dt-stability: band-STABLE iff |β(1/200) − β(1/400)| ≤ 0.05.
  - **G3 (window robustness):** |S_WB − S_WA|/S_WA ≤ 0.10 at dt = 1/200 and 1/400,
    per anchor.
  - **G4 (cumulative convergence):** S_cum increments shrinking AND final increment
    ≤ 5%, per anchor.
- **M2 — physical span:** v ∈ {0.10, 0.40, 0.95} at (b = 0, w = 4, dt = 1/200).
  **G5 (physical vs implementation):** implementation spread of S_WA (across dt
  {1/100, 1/200, 1/400} ∪ methods {A, B} ∪ windows {W-A, W-B}, at each anchor)
  ≤ 20% of the physical spread (max−min of S_WA across the v-grid). Read per
  anchor; where G2's band applies, G5 is read with method-B members EXCLUDED and
  the exclusion disclosed (the band, once registered, is not double-counted as
  implementation noise).
- **M3 — width confirmation:** the low-γ anchor at w = 2 (dt 1/100, 1/200): G1/G3
  confirmation-grade (shrinking increments; ≤ 10% window agreement) — reported,
  gating only the width-universality sentence, not the verdict.

## 4. Frozen readings

- **RD1′ — FULL LIFT:** C1–C4 pass; G1–G5 pass at BOTH anchors → the per-encounter
  deposit registers PHYSICAL, window-robust, decomposition-independent; **the
  DISC-1 block LIFTS entirely**; high-γ consumers, Sea-growth discussion, and
  H-a/H-b work reopen citing this record.
- **RD1-BANDED (pre-declared partial):** G1, G3, G4 pass at both anchors; G2 passes
  at the low-γ anchor and FAILS at the high-γ anchor with the band dt-STABLE; G5
  passes as scoped → registers: **the deposit is physical and window-robust at all
  tested γ; at transit-γ it is well-defined up to a registered decomposition band
  β ± its stability tolerance.** THE BLOCK CONVERTS TO A BAND-RIDER: high-γ
  consumers unblock IF AND ONLY IF every consuming sentence carries the band
  explicitly (pattern: "deposit X MeV, decomposition band ±β"); Sea-growth
  discussion reopens in banded form; H-a/H-b profile consumption unblocks under the
  same rider; ETA-1's instrument-defined clause is SUPERSEDED by the quantified
  band. The rider is permanent until a derivational account of the transit-γ sink
  (FORM-1 lineage) replaces it.
- **RD2′ — DEPOSIT IMPLEMENTATION-SENSITIVE:** G1, G3, or G4 fails dt-stably at
  either anchor → the repair failed; the deposit itself (not the window) is
  implementation-sensitive; **the block hardens further** and the escalation is
  named: the transit-γ sink needs derivation, not instrumentation (FORM-1-adjacent;
  founder flag raised at next contact). Adverse, same font.
- **RD3′ — control failure / dt-instability of a control:** nothing reads; RC4.
- **Fences:** verbatim from 2620 §4 (no rates, no σ_cap, no relic contact,
  EDGE-2(i) untouched, 79.5% untouched); additionally NO H-a/H-b promotion in this
  cell under any reading — a preference cell, if ever, consumes this record
  separately.

## 5. Bookkeeping

Next patch: execution under this document only. Verify script
`code/2626_n2b_disc2.py` (stages controls | m1 | m23), foreground-chunked.

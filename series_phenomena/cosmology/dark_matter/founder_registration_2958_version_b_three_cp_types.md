# FOUNDER REGISTRATION — VERSION B ADOPTED (EVEN-SPLIT OUTWARD RELAY); THREE TYPES OF CONSCIOUS POINTS; GP STATE PROTOCOL; THE SMOOTHNESS QUESTION ANSWERED BY BANKED GEOMETRY

**Patch 2958 (2 Aug 2026). Registers the founder's fork ruling and
protocol verbatim, adopts his Version A/B vocabulary as canonical
(mapping to prior labels recorded), registers the three-CP-types
ontology with an axiom-reconciliation flag, and answers his
smoothness question using FACT G1. No value of any open quantity
appears.**

## §1 — VERBATIM (2 Aug 2026)

> "[On signal/reservoir exemption] I completely agree — always my
> intention/thought. I think this is avoided by the processing
> protocol of the GP: the GP computes (from the data on the arriving
> DI-bits) and holds the SSV_abs and SSV_net, and imprints on every
> DI-bit. The DI-bits reset at every hop. The GP resets its SSV_net
> and SSV_abs every Moment with the new value that is calculated from
> the DI-bits arriving at the Perceive stage of the PCD cycle.
> [On the fork] The version of this protocol we have been using is
> Version A. We have been saying, 'The DI-bits all arrive at the PSR,
> but we didn't specify how they got there. In this scenario, they
> all travel to the sphere — maybe they all went there directly,
> which implies an addressing system. That implies the ability to
> calculate every address on a spherical surface. This is a very high
> computational load for the GP.'
> Version B is the one we are now contemplating — all DI-bits move
> from the GP_origin to the GP_PSR. Check out this algorithm. On each
> hop from origin to PSR, each GP only gets one volley per Moment,
> and the number is spread out evenly among all the available GPs in
> the next group of 12, and they all travel the same number of hops
> to the PSR. Would that result in a reasonably smooth distribution
> of DI-bits on the PSR shell surface? And yes, the DI-bits would
> have to perceive compute and displace N times associated with the
> SSV_abs to get to the PSR. Given that the DI-bits are Conscious
> Points (created points of consciousness), this should not be a big
> computational load on any one of the three types of Conscious
> Points: the GPs, the Di-Bits, and the CPs. The division of
> computation allows for RISC optimization by making all the steps
> elementary through the division of labor and function among the 3
> types of Conscious Points."

## §2 — REGISTRATIONS

**(V-1) CANONICAL VOCABULARY — founder's labels adopted; mapping
recorded to prevent inversion confusion:** **Version A** = direct
GP_origin→GP_PSR addressed broadcast (the previously assumed,
unspecified protocol; founder assessment: very high per-GP
computational load) ≈ prior "M-B"/"Fork-B-as-direct." **Version B**
= hop-by-hop even-split outward relay (each GP one volley per
Moment; count spread evenly among the available outward members of
the 12; equal hop count N to the PSR) ≈ the directed-relay
resolution the worker called "Fork-A"/synthesis v2. Prior Fork
labels DEPRECATED; Version A/B canonical. **RULING: Version B is
the mechanism under development.**

**(V-2) GP STATE PROTOCOL:** the GP computes and HOLDS SSV_abs and
SSV_net, imprints them on every DI-bit; DI-bits RESET at every hop;
the GP refreshes its SSV values each Moment from the Perceive-stage
arrivals. This IS the signal/reservoir architecture: persistent
state lives at GPs (per-Moment refresh), DI-bits are per-hop
stateless carriers of imprinted data — the exemption concern (2957
§4) is resolved by construction, founder-confirmed ("always my
intention").

**(V-3) ONTOLOGY — THREE TYPES OF CONSCIOUS POINTS: GPs, DI-bits,
CPs**, with RISC-style division of labor making every step
elementary. This resolves flag P-5 (DI-bit perception is INTENDED,
not shorthand) and is an AXIOM-LEVEL statement: the axiom documents
currently type CPs (and treat GPs as substrate loci); DI-bits as
created points of consciousness is new registered ontology.
**FLAGGED for axiom-line reconciliation** per the SR-2/A3′
precedent process (registered change proposals, panel review — not
unilateral axiom edits). Filed as the arc's A-line item; no axiom
file touched in this patch.

**(V-4) N-hop PCD by DI-bits:** DI-bits perceive-compute-displace N
times (N set by SSV_abs) from origin to PSR — the sub-Moment tier
is DI-bit PCD cycles; Moment = N DI-bit cycles, consistent with the
subquantum-divisions identification (2955 §3.2, which survives).

## §3 — THE SMOOTHNESS QUESTION: YES, AND THE BANKED GEOMETRY SAYS HOW SMOOTH (worker analysis, flagged)

The Version B process — even local splitting over outward edges,
iterated N hops — is I_h-equivariant about the origin. Therefore
**FACT G1 (Patch 2951, survived both mechanism retirements) applies
directly: the shell distribution's angular structure is EXACTLY
isotropic through spherical-harmonic degree 5**; the first lattice
anisotropy allowed by symmetry enters at degree 6, and path-
multiplicity smoothing suppresses fluctuations further as the
number of contributing split-paths grows (~1/√paths). Additionally:
even splitting CONSERVES count ⇒ ~r² shell GPs receive ~1/r² each
(the counting inverse-square stands); equal hop count N ⇒
ballistic, determinate arrival (no stopping rule); the per-hop even
split is computationally elementary (supporting the founder's RISC
point). **Answer: a reasonably smooth distribution — smooth to
degree-6 precision by symmetry — with the residual degree-6
anisotropy a computable, testable number that E-2's toy
verification is now scoped to measure.** One formalization detail
registered: discrete counts cannot always split exactly evenly
among outward edges; the remainder rule (stochastic, rotating, or
carried) must be specified — at small counts this produces shot
noise, which is precisely the microphysical substrate of the
A5-SHOT anchor (cross-link registered, not developed).

## §4 — DISPOSITION

E-3: the mechanism-class ruling is COMPLETE (Version B). E-2
re-scoped and un-gated: verify on the icosahedral lattice (i) 1/r²
counting, (ii) degree-≤5 isotropy and measured degree-6 residual,
(iii) N-hop ballistics, (iv) remainder-rule sensitivity. E-1
(AUTOMATON re-read) now classifies the two implementations against
Version A/B. K1-MEMORY charter proceeds in parallel (2956 §4).
TODO-2957-A (glossary DI-bit correction) is now UNBLOCKED — the
pinned content is complete: {charge, type, origin address; count
magnitude; imprinted SSV data; per-hop reset; a Conscious Point} —
execute next session with the V-3 axiom flag noted. TODO-2957-B
(QM-1 phase audit) unchanged.

## §5 — LEDGER

Nothing moves: six of seven; PR7 PARTIAL; B7 holds; Candidate (B)
79.5%; 2855 PROVISIONAL; d_DP ceiling ACTIVE. New: V-1…V-4; the §3
answer; E-2 scope; the A-line axiom flag. No value of any open
quantity appears; N remains symbolic.

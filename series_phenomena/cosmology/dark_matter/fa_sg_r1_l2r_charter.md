# FA-SG-R1-L2R CHARTER (FROZEN) — the panel-ordered replacement for the L2 registered-arena test, synthesized from the four seat specifications at the R1 returns (adjudication §3.1); statistics, ensembles, and pass conditions committed HERE, before any L2-R computation ran

**Patch 2693, 21 July 2026. Authority: R1 returns Q2b, OVERTURNED
4–0–1 with re-execution directed by all five seats. The executed L2
(2687) REMAINS in the record as an honestly failed preregistered test.
This charter binds the L2-R execution (2694). No class consequence
attaches to any L2-R outcome (5–0: J3-REVISE independently fixes
R1-SHIFT); L2-R informs the FA-C2 campaign's treatment of the
registered arena and the ALPHA-1 file. Scope fence F1–F4 of the 2679
charter carries unchanged. No direct ℓ estimate is demanded from I1
(5–0). 79.5% not in scope.**

## §1 — Part A: staggering-onset sweep (DeepSeek's specification)

Compact FCC balls about a central source, successive coordination-shell
truncations: N ∈ {13, 19, 43, 55, 79, 87, 135, 141, 177, 201} sites
(the natural FCC shell fillings within r ≤ 1.5a … 4a). Identical frozen
operator (M = I + αG, α = 0.08193 fm). Staggering statistic:
negative-site fraction over all response sites. **Onset N\*** = smallest
N with neg-frac ≥ 0.25 sustained for all larger N in the sweep.
**Committed prediction P-A:** N\* exists within the sweep and
N\* < 120 (so that I1's site count sits ABOVE the 3D onset).
Report N\* and the neg-frac(N) curve; the threshold becomes a measured
input, not an assumption.

## §2 — Part B: structural class on count/depth-matched arenas (GPT/Gemini/Copilot)

**Ensemble E (frozen):** five compact 3D arenas with N ∈ [100, 140]:
FCC ball r ≤ 1.00 fm; HCP ball r ≤ 1.00 fm; random-Barlow ball
(seed 20260723) r ≤ 1.00 fm; FCC ball r ≤ 0.95 fm; FCC ball r ≤ 1.05 fm.
Sources: for each arena, three distinct interior sites (the central
site + the two nearest inequivalent interior sites), giving 15
ensemble members. Classifier as in 2687 (neg-frac ≥ 0.25 AND no
constant-sign monotone |f̄·r| run ≥ 0.3 fm). **Committed prediction
P-B:** ALL 15 members classify OSC/NO-REGIME, and I1 (both metrics)
classifies OSC/NO-REGIME. **PASS-B iff all 17 classifications match.**
**Geometric fact to report (Copilot):** joint diameter+count matching of
I1 in 3D is impossible (a 1.18 fm-diameter 3D ball holds ~19 sites, not
120 — the 120-site count at that diameter requires the 3-sphere);
reported as dimension mismatch, not as a test failure.

## §3 — Part C: quantitative consistency via dimensionless observables, tolerances self-generated on the compact ensemble (GPT's specification — nothing imported from the extended 3D instrument)

**Observables (frozen, dimensionless):**
- O1 = negative-site fraction;
- O2 = nearest-neighbour sign-flip fraction over graph edges (edge =
  minimal chord of the arena);
- O3 = longest constant-sign shell run, in SHELLS (count, not fm).

**Tolerance band (frozen construction):** for each observable, the
[min, max] over the 15 ensemble members of §2, expanded by the I1
metric-choice spread (the |chord-value − geodesic-value| for that
observable, the preregistered metric perturbation on I1 itself).
**Committed prediction P-C:** I1's observables (each metric separately)
lie within the expanded band. **PASS-C iff, for each I1 metric, ≥ 2 of
the 3 observables lie within band.**

## §4 — Reporting and consumption

Verdicts PASS/FAIL per part, committed as above; combined **L2-R
verdict = PASS iff P-A holds AND PASS-B AND PASS-C.** Any FAIL reports
adverse-direction in full with diagnosis, nothing renamed. Outcomes
feed: the FA-C2 campaign's registered-arena treatment; OPEN-DM-ALPHA-1
§ context; and the R1 record (as the panel-ordered replacement result
standing beside the 2687 failure). No grade, cap, rider, or class
motion attaches. Verify script `code/2694_r1_l2r_execution.py` and
reasoning fragment bundle per the capture rider.

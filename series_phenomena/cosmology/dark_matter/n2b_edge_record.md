# N2-B-EDGE executed: **THE MAP IS TOTAL THROUGH 0.95c** — 48/48 CAPTURED at γ up to 4.6, the square survives every passage, and the transit-shed model over-predicts escape in all six families. VERDICT RE2 (NO EDGE ON GRID, adverse-for-model); nothing registers; the higher-γ + single-pass-shed cell is DECLARED, not run

**Patch 2609, 20 July 2026. Governed by `n2b_edge_prereg.md` (2608) ONLY. Artifact:
`code/2609_n2b_edge.py` (engine exec-loaded VERBATIM from the registered 2602 artifact —
no re-typing, no drift). Verdicts below are read from the prereg's frozen readings against
raw outputs.**

## 1. The analytic table (E-A, zero freedom — the model that lost)

Wells from the registered forms: W = 265.1 (b = 0, both widths — the on-axis well is
electric-cancelled by the alternating square, pure Morse) down to 146.5 MeV (steep,
b = 1.0D). The recorded naive expectation γ_naive = 1 + W/m lands at **v = 0.88–0.94c**;
the transit-shed condition over the declared L_int band {D, 2D, 3D} lands at
**v_edge = 0.58–0.91c** by family. Every one of these numbers is now known to be wrong
in the same direction.

## 2. The map (the finding IS the count, again)

**48/48 CAPTURED** across v ∈ {0.40…0.95}c × b ∈ {0, 0.5, 1.0}D × both widths at the
admitted η = 0.5, dt = τ_C/100, TC = 120. Raw structure worth the record:

- **Sea deposits GROW with v**: ~301 MeV (0.4c) → 564–580 MeV (0.95c), monotone in every
  family — the sink eats MORE at higher violence, outrunning the incident's KE growth
  (m(γ−1) = 291 MeV at 0.95c; the deposit exceeds it with the released well on top).
- γ_max reaches 3.8–4.6 in the 0.95c cells; the H-γ member handles it certifiably
  (subluminal by construction; no UNR anywhere).
- **FRG never fired**: the square survived forty-eight relativistic bombardments,
  including head-on at 0.95c. The cluster's robustness to fast passage is reported
  physics, N3-relevant, banked here without registration.
- Timing cell 1.5 s; full grid 73 s foreground — no split needed.

## 3. The verdict (frozen reading RE2)

**RE2 — NO EDGE ON GRID.** The flytrap extends beyond the tested regime; **nothing
registers** (no v_edge, no B4 domain extension — see §5); the analytic model records
**FAILED, adverse-for-model**: it predicted escape from 0.58–0.91c and the substrate
captured everything. The 2602 expectation ("~0.9c where m(γ−1) ~ well depth") is hereby
TESTED AS WRITTEN and does not survive: at 0.95c, m(γ−1) exceeds every b > 0 well and the
capture proceeds anyway.

## 4. Diagnosis — two named hypotheses, neither promoted

- **H-a (multi-pass):** the model's single-pass criterion is the wrong question. A
  marginal first-pass shed leaves the incident temporarily bound; the registered flytrap
  then finishes the job at leisure. Supported by: deposits of order KE + W in every cell;
  no marginal SCA anywhere (the outcome is bimodal-with-one-mode).
- **H-b (deflection-burst shedding):** the per-cycle drift/osc split reads a hard
  deflection's velocity curvature as oscillation and sheds it — radiation-reaction-class
  dissipation on acceleration bursts, CONSISTENT with C-SR (uniform motion still sheds
  exactly zero) but of unmeasured single-pass magnitude. Supported by: deposits growing
  monotonically with violence. This is the 2601 disclosed frame-subtlety wearing its
  high-γ face; whether its magnitude is substrate-faithful at one pass is precisely what
  the declared follow-on measures.

## 5. Confessed prereg gap (disclosed, 2574-class)

The dt-confirmation rule was written BRACKET-CONDITIONAL — and RE2 produces no brackets,
so zero cells received dt = 1/200 confirmation tonight. Consequence held with
confirm-stingy teeth: the tempting corollary "σ_cap ≥ πD² now extends to v ≤ 0.95c" does
**NOT** register — it is recorded as EXTENSION-CANDIDATE pending dt-union confirmation in
the declared follow-on. A null verdict (RE2) needs no dt gate; a registration does; the
gap is confessed here so the next prereg carries dt-union unconditionally.

## 6. Declared follow-on cell (queued, NOT run — 2604 discipline)

**N2-B-EDGE-2 (declared):** (i) v ∈ {0.97, 0.99, 0.995} × b ∈ {0, 1.0}D × both widths,
dt-union {1/100, 1/200} unconditional, FRG fence as at 2608; (ii) the **single-pass shed
measurement**: sink-ON vs sink-OFF (coupling comparison on the identical trajectory
window) through one transit, reading the one-pass shed fraction against H-a/H-b —
model repair, if any, is a NEW candidate entering at the top with this measurement as
its input. Joins the founder-free queue.

## 7. Standing

Nothing touches N1, the sink scope, the H-γ admission, the η point, item-5, the
win-candidate packet (frozen as at 2604), or 79.5%. Relic fence held: no T1 contact;
B4's registered lower band (v ≤ 0.3c) stands exactly as registered. The epoch-relevant
sentence, honestly stated: **within the tested regime the substrate has no capture edge
— the relic campaign's σ_cap domain question moves to v > 0.95c and/or to the ambient-
band registration (N3's interface), not to a kinematic cutoff at 0.9c.**

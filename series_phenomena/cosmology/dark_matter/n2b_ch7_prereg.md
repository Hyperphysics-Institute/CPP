# N2-B-CH7 pre-registration: the soft-chain continuation cell — completing (or honestly failing) the soft-width 4→8 under the AIMED convention, with the aim now checked, committed before any run

**Patch 2615, 20 July 2026. Status: N2-B-CH7 OPENED at pre-registration; NO run
performed.** This is the cell 2614 declared ("a continuation cell... joins the
founder-free queue"). Governed by this document, the CH6-2 lineage (2613/2614), and the
N2-B prereg lineage (2598) only. Verify: none at prereg.

## 0. Target and framing (the 2598 defense, generalized)

Can the soft-width chain complete 4→8? The registered stall is an aim-drift artifact
(2612); the aimed re-scan shows the 6-cluster captures on every face (2614). This cell
runs the continuation under the **AIMED (centroid-relative) convention** — the honest
convention for an EXISTENCE claim, exactly as 2598 defended testing existence on the
favorable channel while rates integrate every miss. The absolute-aim / declared-flux-
geometry member is N3-interface territory (rates against ambient bands) and is NOT run
here; the aimed convention is disclosed as favorable-by-construction in every consumer
sentence. **Temptation fenced in advance:** growth past 8 is a NEW declared campaign
regardless of outcome — the two-layer 8-qCP core is the registered target object; no
stage 9 exists tonight.

## 1. Registered inputs (verbatim; zero freedom)

Engine + launcher: exec-load of the registered 2611 artifact (which itself exec-loads
the 2604 engine) — `n1_gamma`, `regen6`, `scan_cell` are the registered objects.
η = 0.5. Base state: the registered 1/100-grown soft 6-cluster (C-R regeneration must
read BOUND at stages 5–6). Stage launches: `scan_cell` on the original-face channel
(start side +z, b = 0.5D, φ = 0, v = 0.1c — the declared channel's parameters, aimed),
stage 7 charge −1, stage 8 charge +1 (the registered chain's alternation). State
propagation at dt = 1/100 (the registered chain protocol); **dt-union unconditional on
every verdict**: each stage re-run at 1/200 from the same pre-stage state as a
verdict-confirmation (the 1/100 path remains the chain; the 1/200 run confirms the
verdict, declared, matching the 2610 §3 economy, disclosed).

## 2. Frozen criteria

Stage BOUND iff scan-classifier CAP ∧ chain criterion (whole-object dmax < 3D) — both
reported, both required, at both dt. Completion additionally requires the finished
8-object settled at dt = 1/200 (TC = 60): bounded (dmax < 3D) with Edrift reported and
the steep benchmarks printed alongside for comparison (Rrms 0.77→0.92 fm, dmax ≤ 1.06,
Edrift = 0.00 — comparison only, not gating: widths may differ honestly).

## 3. Readings (frozen)

- **RS1 — THE SOFT CHAIN COMPLETES:** stages 7 and 8 BOUND dt-stably; the 8-object
  settles bounded at 1/200 → registers: **soft-width 4→8 exists under the aimed
  convention** — the assembly existence claim becomes width-universal (both widths
  complete). Joins the packet-strengthening set; whether it enters the dispatch packet
  is the founder's call at dispatch time.
- **RS2 — A REAL STALL:** any stage reads non-BOUND dt-stably under aimed launches →
  a genuine (non-artifact) obstruction registers with its stage, face, and raw
  numbers — new physics this time, because the aim is checked.
- **RS3 — UNSTABLE:** any dt-unstable verdict → nothing registers; defect hunt next.
- Fences: existence only — no rate claims, no σ, no relic contact; packet frozen (and
  carries the 2612 correction — standing founder flag); no stage 9; B4/N1/sink scope/
  H-γ/η point/item-5/79.5% untouched.

## 4. Bookkeeping

Next patch: execution under this document only. Verify script
`code/2616_n2b_ch7_softchain.py`.

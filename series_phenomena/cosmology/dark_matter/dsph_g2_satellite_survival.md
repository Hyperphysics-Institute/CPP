# The satellites survive: G2 clears, and the frames keep their characters (Patch 2350)

**What this is:** frame-adoption gate **G2** (2346/F3; pre-adoption obligation from
2345) — the Ando 2025 / Nadler+21 / SIDM Concerto-class satellite-survival
counter-check against the G1-passing populations, with the 1856 transport fold-in
named at 2349. *Namespace: frame-adoption G2, not Gate-1/B1's G2 (2315).*
**Pre-registration:** `code/2350_PREREG.md` (sources, bars, protocol, outcomes fixed
before computation). **Verify:** `code/2350_g2_grading.py` (6/6); engine
`code/2350_g2_engine.py`; data `2350_results.json` / `2350_eps_A.json` /
`2350_joint_research.json`. **NO VERDICT MOVED.**

## 1. The three legs

**Leg A — evaporation.** Subhalo–host collisions at orbital velocities strip
satellites if σ_eff is large there. Measured on the identical rig at 250–500 km/s
for all four candidates (both G1 kinetic passes, both stored two-delta configs):
worst case anywhere — including both envelope ends and the re-searched points — is
**0.50 cm²/g at 300 km/s and 0.21 at 500, a factor ≥6 under the conservative
3 cm²/g Nadler+21-class bar.** The population's steep velocity decline, the very
feature the anchor suite demanded, is what protects its satellites.

**Leg B — branch placement.** Every graded pass point is strongly velocity-dependent
(s_eff(30→200) ≥ 1, so Ando+25's decisive velocity-*independent* 0.2 cm²/g exclusion
does not apply as written), has collapse-branch shape (σ rising toward low v), and
sits at or below the Concerto-simulated viable class (max σ(10 km/s) = 61 cm²/g vs
the simulated σ₀ ~ 147 class). This is precisely the branch both Nadler+21 and
Ando+25 name as the viable alternative to collisionless — the mainstream-simulated
class, as the L4 memo anticipated.

**Leg C — the 1856 transport fold-in.** ε(N) = σ_T/σ_geom **measured on the
registered 1856 hard-capsule rig** (not assumed): 0.17 at N=2 rising to 0.32 at
N=24 — near-flat, and mildly *kinder* to long rods than short, opposite the 2349
worry. Applied as a labeled worst-case proxy envelope (ε_ij = √(ε_iε_j) on
interaction channels; floors unsuppressed — the 1870/71 floor MC already carried rod
geometry). Results of the **joint** re-search (windows + all satellite legs
simultaneously) under the envelope:

- **Audited-extended: BRACKET-STABLE JOINT PASS** — 1.0000 with the envelope on
  (α = 0.01, inside 1855's own quoted range; the coupling knob absorbs the
  suppression) and depth ×2.18 with it off. The suppressed-end pass is thin — LSB
  grazes its low edge by 0.4%, s_eff rides the B1 bar at 1.01 — but non-empty.
  *Flash-review flag (2355):* the B1 bar ridden at 1.01 is a constraint-active
  sensitivity point, not a robust interior pass — small S(N) or ε(N) changes
  could flip it.
- **Audited-central: ENVELOPE-SPLIT** — clean pass at the registered rig, joint
  miss **×1.0032 (0.32%)** under the worst-case proxy. That miss sits inside the
  proxy's own error budget (ε MC noise is a few percent; the true rod-extent
  correction to the *residual* channel is underived and not sign-definite — Yukawa
  smearing of an extended source is not automatically suppressive). Outcome (iii)
  texture, exactly as pre-registered.

## 2. Grade

**G2 = PASSED-with-texture.** Outcome (i) fires at audited-extended
(bracket-stable); outcome (iii) texture at audited-central (proxy-split by 0.3%).
The frame character is now **consistent across gates**: extended was easy and
natural at G1 (α = 1, p_min = 0) and is bracket-stable at G2; central was strained
at G1 (α ≈ 3–6, p ≥ 13) and is proxy-split at G2. Whatever the founder decides at
the frame fork, the record now says the same thing twice.

Two of three F3 gates traversed. **G3 (per-dSph likelihood, Correa-class,
pericentre priors) remains** before the v2-adoption auto-proposal.

## 3. Named systematics and forward map

(i) The ε(N) envelope is a **contact-channel proxy** — importing hard-capsule
rigid-body suppression into the long-range residual channel bounds one direction
only; the derived rod-source scattering correction is the inverse target (feeds
OPEN-SS-43's geometry side). (ii) Literature bars are **class-level with
provenance**, not digitized contours — the same discipline as the 2345 window
sourcing. (iii) The rigorous version of this gate is **SASHIMI-class subhalo
abundance + kinematics modeling** of the specific σ(v) curves — registered as G2's
forward map, the analogue of G3's likelihood protocol. (iv) UFD-scale σ(5–10 km/s)
values use the F-table's logged extrapolation tail — diagnostic quality,
order-of-magnitude, not anchor-grade; no window rides on them.

# N2-B-EDGE pre-registration: the capture-edge derivation cell (~0.9c) — the declared follow-on of the 2602/2604 edge expectation, frozen analytic model, declared high-v grid extension, and readings, committed before any run

**Patch 2608, 20 July 2026. Status: N2-B-EDGE OPENED at pre-registration; NO run
performed.** This is the cell the 2602 record declared ("the capture EDGE lies beyond the
grid... deriving it deserves its own declared cell") and the 2604 B1 line reserved ("edge
beyond grid — NOT registered; its derivation is a declared follow-on cell"). Governed by
this document, the N2-B prereg lineage (2598), and the H-γ rider (2601) only.
Verify: none at prereg.

## 0. Target

The speed above which an incident qCP is no longer captured by the settled 4-qCP square —
numerically (extending the registered B1 map into v ∈ [0.4, 0.95]c) and analytically (a
zero-freedom transit-shed model from the registered sink), read against each other. The
recorded expectation under test: **v_edge ~ 0.9c where m(γ−1) ~ well depth** (2602,
written then as expectation, not registration). Epoch relevance: the relic campaign's
⟨σv⟩ integrals need the v-domain where σ_cap lives; the edge is that domain's upper
structure. **Fence: no relic numbers, no T1 contact, B4's registered lower band
(σ_cap ≥ πD², v ≤ 0.3c) stands regardless of outcome.**

## 1. Registered inputs (verbatim; zero freedom)

Engine: `n1_gamma` (2602, verbatim — H-γ law + minimal-faithful sink, η = 0.5 the sole
γ-admitted point). Geometry and classifier: the B1 cell verbatim (2598/2602): incident
qCP charge −1 at [bD, 0, 4D], velocity −v ẑ, onto the settled square (rung 1); classifiers
CAP / SCA / FRG / UNR exactly as frozen at 2598 (the edge cell inherits them; a punch-
through transit reads SCA by construction — declared). Constants per the 2582/2596 set.
TC = 120 Moment cycles.

## 2. The analytic model (E-A, frozen before any evaluation)

**Zero-freedom well:** W_b = −min_z U_inc(z; b), the incident's potential along its
declared approach line (4 Morse qq wells + alternating electric, both computed from the
registered forms; no fit). Per width, per b. The naive registered expectation prints as
the comparison line: **γ_naive = 1 + W_b/m.**
**Transit-shed condition:** capture iff the Sea absorbs at least the asymptotic KE during
the encounter: [1 − (1−η)^N]·(m(γ_∞−1) + W_b) ≥ m(γ_∞−1), with N = L_int/(v_close τ_C)
Moment cycles during the transit, v_close the speed at the well bottom (from
m(γ_close−1) = m(γ_∞−1) + W_b), and **L_int carried as a DECLARED BAND {D, 2D, 3D}** —
the interaction span is not derived by this cell and is honestly banded, never tuned.
γ_edge(width, b; L_int) solves the equality; the analytic deliverable is the band over
L_int per family.

## 3. Declared numeric grid

v ∈ {0.40, 0.50, 0.60, 0.70, 0.80, 0.85, 0.90, 0.95} × b ∈ {0, 0.5, 1.0}D × widths
{2.0, 4.0} at η = 0.5, dt = τ_C/100 (48 cells, foreground-sized; split by width if the
first timing cell says otherwise — a runtime split is bookkeeping, not physics).
**dt-confirmation rule:** in each (width, b) family, the bracketing pair (highest-v CAP,
lowest-v non-CAP) re-runs at dt = τ_C/200; a registered edge requires the bracket verdicts
dt-STABLE. Seeds: none (deterministic cells). No cell added, removed, or re-run outside
this declaration; post-hoc scans queue as new declared cells (2604 discipline).

## 4. Readings (frozen)

- **RE1 — EDGE REGISTERS, CONCORDANT:** every family shows a dt-stable CAP→non-CAP
  bracket, AND the analytic γ_edge band (over the L_int band) contains or adjoins within
  one grid step the numeric bracket in ≥ 4 of 6 families → **the capture edge registers
  BANDED** (per-family numeric brackets + the analytic model as its derivation); the B4
  band gains its v-structure (consumer note; any B4 re-registration is its own patch).
- **RE2 — NO EDGE ON GRID:** the map stays total through 0.95c → the flytrap extends
  beyond the tested regime; nothing registers; the analytic model is recorded as
  over-predicting capture failure or the grid as under-reaching (adverse-for-model);
  a higher-γ cell may be DECLARED next, not run tonight.
- **RE3 — EDGE NUMERIC-ONLY:** dt-stable brackets exist but the analytic band misses by
  more than one grid step in ≥ 3 families → the edge registers NUMERIC-ONLY (banded by
  the grid); the model records FAILED with the miss pattern; tension line, dated.
- **RE4 — PATHOLOGY / FRG-INTERVENES:** dt-unstable brackets → no registration, defect
  hunt next. If FRAGMENTATION appears before or at the would-be edge in any family, the
  FRG onset is recorded as its own reported physics (a fragmentation edge is NOT a
  capture edge; the two are never conflated; a capture edge only registers in families
  where the bracket is CAP→SCA clean).
- No reading touches N1, the sink scope, the H-γ admission, the η point, item-5 status,
  the N2-B win-candidate packet, or 79.5%. The win-candidate dispatch packet remains
  frozen as registered at 2604 — this cell's results JOIN a future packet only by
  founder word.

## 5. Bookkeeping

79.5% untouched. Next patch: execution under this document only (analytic table + the
48-cell map + brackets at dt-union; verify script `code/2609_n2b_edge.py`).

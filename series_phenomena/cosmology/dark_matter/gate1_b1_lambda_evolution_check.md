# The Λ-evolution/DESI check: three readings die, the survivor gets a falsifier, and the conjecture gets a measured coefficient (Patch 2347)

**What this is:** the first founder-directed "surrounding phenomena" release gate for
DM-2. **Verify:** `code/2347_lambda_evolution_check.py` (6/6). **Gate status:
PASSED-with-content.**

**The structure that makes the check sharp:** DM-2's ρ_Λ = c⁴/(8πG·L²) carries a
G3-*derived* coefficient; only the scale selection (D3(b), tagged CONJECTURED) is
open. So every reading of D3(b) is a **zero-parameter w(z) prediction** — in
particular, the event-horizon reading is holographic dark energy with
**c_hde = 1/√3 pinned**, where the literature's HDE carries a free constant.

**Results, against DESI DR2 + CMB + SNe (Abdul Karim et al. 2025; w₀ > −1, wa < 0,
ΛCDM disfavored 2.5–4.2σ; preference contested — caveats carried):**
comoving-stretch (w = −1/3): **excluded** — cannot accelerate. Instantaneous-Hubble
(Ω_Λ ≡ 1/3): **excluded twice** — ×2 magnitude today and catastrophic early-DE.
Event-horizon (c_hde = 1/√3): **w₀ = −1.286, wa = +0.59 — the time-mirror of the DESI
dynamic** (phantom-today-deepening vs quintessence-today); 5.4σ/3.7σ off with both
signs wrong, and 2.9σ from −1 even against ΛCDM-like data. **Excluded-directional.**
Frozen physical mode: **w = −1 exactly — the sole survivor**, requiring the frozen
scale L = **0.700**·c/H₀ = 3.11 Gpc: the O(1) selection factor is now a *measured
coefficient*, registered as D3(b)'s inverse-research target in exactly the founder's
method — the number first, the derivation owed.

**What DM-2 gains for release:** the reading table (a conjecture that has been
*discriminated*, not merely stated); the zero-parameter HDE exclusion (a result no
free-c paper can state); the inherited DESI tension reported plainly as the
framework's own; and the registered forward falsifier — **confirmed w(z) evolution at
discovery significance kills every enumerated reading** and converts D3(b) into a
derivation demand for re-selection dynamics (named contingency, with its own rent: a
specific w(z) shape). The 10¹²⁰ result now makes contact with live data and can die by
measurement. That is rent in the founder's sense.

**Remaining pre-release items (unchanged from plan):** D3(b) status audit in the text
(the conjecture tag made prominent, this check cited as its first test);
growth-of-structure consistency paragraph; the two panel closures folded in.

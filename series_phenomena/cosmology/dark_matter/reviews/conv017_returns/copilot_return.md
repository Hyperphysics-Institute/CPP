# COPILOT RETURN — CONV-017

Q1: **GEOMETRY-ARTIFACT RETIREMENT.** Route A shows a window-robust
tail that CHANGES SIGN with box size — inconsistent with a physical
long-time memory kernel. Route B reproduces a concentrated tail at
x=24 while its positive control fails at the pass domain; x=24 is
geometrically special (source exit at T_BALL = 36) with S3 out-of-band
corroboration. The simplest coherent panel diagnosis is a
domain-specific geometric/boundary-mode artifact explaining both
records without a physical memory kernel. The tail question RETIRES;
the indictment is DISCHARGED insofar as it rested solely on the tail
evidence. Panel should publish the geometric diagnosis, a minimal
boundary-mode model, and design guidance (longer legs, multiple
domain sizes) as the canonical explanation.

Q2: **ADOPT with REQUIRED REPORTING AMENDMENTS.** The metric
(realization-class distance + systematic response channel) is the
correct conceptual move; κ reframed as systematic-channel decay.
Because Q1 retires the tail, the existing Route A ensemble provides
the primary data to READ the systematic-channel margin — but the
D-KAPPA amplification must be explicitly propagated. REQUIRED before
unconditional acceptance: (1) publish the EXACT ALGORITHM for the
distance-to-realization metric and the systematic-channel
decomposition; (2) compute and publish the margin from the frozen
Route A ensemble with uncertainty bounds (κ and a CI or δ margin);
(3) if the estimate is marginal (κ within numerical error of 1),
attach a Route-C plan for a targeted realization-class sweep. With
these supplied, ADOPTED and the margin read from existing ensembles;
otherwise conditionally adopted pending the named computations.

Q3: **DISCHARGE.** Q1 retires the tail; Q2 provides an adopted metric
path to read κ from the existing ensemble; the primary empirical
falsifier keeping clause 2 OPEN is resolved. ARITHMETIC: PR7 clause 2
discharged; item 1B removed as a blocker; promotion path advances,
SUBJECT TO the usual quarantines and the panel's formal ratification
patch; existing quarantines (79.5% provisional, B7 banners, d_DP
ceiling) remain in force until the adjudication patch enacts ledger
movement. OPERATIONAL NOTE: publish the margin computation and the
geometric-artifact model alongside the discharge.

AUX-A: **CLOSE.** AUX-B: **DISCHARGED** (record verification seeds and
the negative-control protocol for archival traceability). AUX-C:
**DISCHARGED** (recommend a short provenance addendum documenting
independent implementation differences (exponents) to avoid future
interpretive drift).

CLOSING ACTIONS: (1) publish the geometric-artifact diagnosis, minimal
boundary-mode model, and Route-B instrument-failure postmortem;
(2) compute and publish the L-6 systematic-channel margin from the
frozen Route A ensemble (seeded, reproducible) and ATTACH THE RESULT
TO THE ADJUDICATION PATCH THAT ENACTS PR7 CLAUSE 2'S DISCHARGE.

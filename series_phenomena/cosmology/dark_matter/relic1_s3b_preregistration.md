# OPEN-DM-RELIC-1 S3b — pre-registration: seeded nucleation (D1) vs homogeneous condensation (D2), with the calibrated incumbent (D3) as explicit fallback — rules, seed-selection criterion, readings, and one fenced numerical observation, ALL committed before any multiplicity is computed

**Patch 2522, 16 July 2026. Status: S3b OPENED at pre-registration; NO yield computation performed.**
**Verify:** `code/2522_s3b_prereg.py`.

## 1. The three candidates (defined before computation)

- **D1 — asymmetry-seeded nucleation** (direction S3b-D1 from Patch 2521; founder maelstrom provenance): rings
  form only at asymmetry-forced seeds; n_ring = N_seed × m (m = rings per seed, to be DERIVED from registered
  structure — geometry/combinatorics — zero knobs). The 10⁻¹⁰ smallness inherits entirely from the anchored
  asymmetry; the campaign's remaining burden is the single structural number m.
- **D2 — homogeneous (bulk) condensation:** rings condense from the paired Sea everywhere, gated only by
  energetics/kinetics. Its computable test (defined §3) is expected lethal, and running it honestly is D1's
  admission price: if D2 survives, there is no license to prefer D1.
- **D3 — the calibrated incumbent (fallback, not computed):** the registered DM-1 status quo — the ratio
  relocated to the free primordial swirl amplitude, calibrated. D3 is what K1/Branch-T outcomes collapse to;
  naming it prevents "something must work" pressure on D1/D2 readings.

## 2. D1's seed-selection criterion — COMMITTED NOW, before any multiplicity exists

Broadened-T's discrete-selection clause forbids choosing the seed population after seeing which multiplicity
lands. The criterion is therefore fixed here: the seed must be **(i)** a closure-forced population (2519 — the
only registered η_B-scale inventories); **(ii)** a strong attractor for the paired ring feedstock under
REGISTERED interactions; **(iii)** compatible in fate with the ring's registered (0,0) composition and the
closure ledger. Applying (ii) to the two candidates: a **free −qCP** is color-charged — a bare strong-force
attractor for qDP chains (Path #1's feedstock) by the registered color interaction; an **hDP-B** is a bound
neutral-ish dipole (weak residual attraction only). **The criterion selects the clouds. Committed: primary seed
= the free −qCP population (N_seed = 1·n_b per photon = η_B), in the per-qCP reading (each lone −qCP seeds
independently — the no-clustering reading is primary BECAUSE it has no parameter).** Clustering readings
(k qCPs per cloud → N_seed = n_b/k) are a disclosed secondary FAMILY with k a named unpinned quantity —
reported, never selected post-hoc (the 2514 contact/bond precedent). The B-excess as seed is recorded as
considered-and-rejected under criterion (ii); it may not return post-hoc.

**Primary D1 target (fixed by the above before any derivation): m = rings per lone −qCP = 0.4468 ± 0.0054**
(one ring per 2.238 seeds). Seed fate is part of D1's computation: consumed vs catalytic, with explicit charge
bookkeeping — naive absorption is excluded by ring (0,0); a fate that neutralizes or hides the clouds would
simultaneously bear on gate SG-1 (flagged as a possible joint discharge; NOT assumed).

## 3. D2's computable kill-test (defined now; expected lethal; must be run, not presumed)

Registered inputs: bound states at kT_form ≈ 16.5 keV *[retired as T_form(DM), Patch 2543 — see 2542]* are favored by exp(+E_bond/kT) (the H1 direction);
the paired-feedstock reservoir is vast (0704; R2 registers Ω_Sea ~ 10⁴⁵–10¹²⁰ at full gravitating density);
no registered kinetic gate exists (NB-S3a-1). **Test: compute the ungated complete-condensation bound on
n_ring/n_γ from registered Sea density.** Reading: if the bound overshoots the 2.734×10⁻¹⁰ target by many
orders (expected: ≥10⁴⁰), D2 is **KILLED by registered physics** — bulk condensation without a gate
contradicts both the target and R2's structure. Its only rescues are (a) unregistered kinetics → Branch I via
NB-S3a-1, or (b) a spatial gate restricting condensation to special loci — which is not a rescue but a
concession that a D1-class mechanism is required. A D2 kill is itself evidence-shaped: it is the registered-
physics statement that ring formation MUST be gated, and seeds are the only registered gate. (Consistency
note: R2's own registered framing — DM gravitates as the Sea's swirl INHOMOGENEITIES, not its bulk — already
points the same way; recorded as corroborating context, not as a result.)

## 4. Readings (mapped to the frozen charter branches; committed now)

- **R1 (D2 test):** overshoot ≥ 10 orders → D2 KILLED. Overshoot < 10 orders or bound lands near target →
  STOP, full re-examination before anything else (this outcome would falsify the campaign's whole picture of
  the Sea and must not be absorbed silently).
- **R2 (D1 primary):** m derived from registered structure, zero knobs, per-qCP seeds. Band (all registered
  windows propagated) vs target: overlaps 0.4468 ± 2σ → feeds S3c **D-strong** (conditionality ledger:
  FI-RELIC-1 + NB-S3a-2 + D1's own imports, enumerated). Central in [0.30, 0.67] → **D-directional**. Outside
  → **K1-direction for D1**; with D2 dead, the campaign lands on D3 (ratio stays calibrated) unless a
  clustering-family disclosure motivates a NEW pre-registered campaign (never a same-campaign rescue).
- **R3 (anti-readings, broadened T):** seed-population switching after §2; selecting a clustering k post-hoc;
  any m whose derivation's only justification is landing on target; dropping failed formation channels.
- **R4 (both-survive):** D1 and D2 both viable → panel adjudicates; no worker selection.

## 5. OBS-RELIC-1 — a numerical observation, FENCED (recorded so it can never be smuggled)

**T1 = 0.4468 ± 0.0054 sits 0.077σ from 1/√5 = 0.44721** (equivalently: seeds-per-ring at the D1 primary
target = 2.238 ≈ √5 = 2.23607). In a φ-geometric substrate, √5 = 2φ−1 is deeply structural, and this is
therefore either meaningless (φ-expressions are dense; near-misses are cheap — the look-elsewhere caveat is
part of this registration: 2χ = 0.4721 is 4.7σ away and was the second candidate checked; no exhaustive scan
was run) or the fingerprint of the eventual derivation. **The fence, binding on all downstream work: OBS-RELIC-1
is NON-EVIDENTIAL. It may not be cited in support of any mechanism. A derivation of m is legitimate only if
√5 (or whatever value) emerges from registered structure with the derivation's every step independent of this
observation; any derivation reverse-engineered toward 1/√5 is Branch T by the anti-numerology clause. It is
recorded (rather than suppressed) so that a future honest derivation landing on 1/√5 faces a clean record —
the coincidence was known, fenced, and not steered toward.**

# PRE-REGISTRATION — Patch 2349, G1 formation rent check (written before any computation)

**Question (rent as written, 2344):** do the 1855–56 aggregation kinetics produce dimer
dominance — concretely, can the REGISTERED 1855 mechanism (burst monomer pool;
barrierless irreversible dimer nucleation at rate α·c1²; monomer-addition growth at 2
rod ends; NO rod–rod coalescence; single knob α = k_n/k_g) generate a mass distribution
f_N that, evaluated on the IDENTICAL 2344 rig (same F(ε) table, same floors, same
mass model, same S_ij = √(S_iS_j) mixing), passes the anchor suite?

**Protocol (fixed now):**
1. Full population balance of the 1855 mechanism (not the moment model) to complete
   conversion; validator: L_n reproduces the 1855 moment model.
2. Coupling assignment: single-valued power law S(N) = S0·(N/2)^p (the family the 2344
   physicality audit named; the two 2344 species imply p ≈ 12.7 — the named strain).
   Knobs and bounds, fixed before running: α ∈ [10⁻², 10⁶] (log grid + local refine);
   log10 S0 ∈ [−6, 2]; p ∈ [0, 16]; R_s ∈ [20, 120] fm (the 2337 scan band).
   Species with mass fraction < 10⁻⁶ dropped (2344's own negligible-channel guard).
3. Grading frames: registered v1, audited-central, audited-extended (2345 definitions,
   loaded from the stored 2345 JSON — no retyping). Two-sided violation
   v = max over windows of max(LO/tot, tot/HI, 1). Floors FL added exactly as in 2344.
4. Validator: the K-species rig fed the stored 2344 two-delta population must
   reproduce the stored totals to < 10⁻⁹ relative.

**Pre-registered outcomes (graded as written, nothing else):**
- **(i) G1 PASSES:** some admissible (α, S0, p, R_s) yields the kinetic population
  passing all four windows at audited-central OR audited-extended. Report the passing
  point, the required α, and its naturalness vs the 1855 "natural α ~ 1" note (an
  α ≫ 1 requirement is a NAMED STRAIN and enters the inverse-coefficient ledger, not
  a disqualifier). Frame gate G1 clears.
- **(ii) G1 FAILS-quantified:** zero passing configurations anywhere in the knob box
  at BOTH audited frames. Report best violation per frame, which windows bind, and the
  structural reason. The population picture as formed by the registered kinetics is
  KILLED; F3's gated-adoption path loses gate 1; the population section demotes to
  conditional-on-unregistered-kinetics.
- **(iii) CONDITIONAL:** passes only under a kinetics modification not registered in
  1855 (e.g., growth cutoff, reversibility, coalescence). Graded as FAIL for the gate;
  the needed modification is named as an inverse target with rent.

**Structural sub-question, registered now because the answer is checkable and could
hurt:** the 2344/2345 passing objects are TWO-DELTA populations (dimer + N≈5–6, with a
GAP at N = 3, 4). The 1855 cascade nucleates continuously and grows by single-monomer
addition — its tail is a mixture of Poissons and should be MONOTONE beyond the dimer
(no gap, no secondary bump). If confirmed, the kinetics cannot produce the 2344
structure literally; the scan then decides whether a monotone tail passes anyway.
This is stated BEFORE running so a monotone-tail failure cannot be retro-fitted as
"what we expected."

**No verdict moves in this patch regardless of outcome:** the v1 kill is final; G1
gates only the frame-adoption proposal and the papers' population section.

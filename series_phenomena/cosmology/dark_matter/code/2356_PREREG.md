# PRE-REGISTRATION — Patch 2356, L4-b pin-velocity audit (written before any computation)

**Question (panel-unanimous promotion, 2355; registered at 2342):** what velocity does
the pin anchor's own provenance support, under the campaign's single velocity
convention (⟨v⟩ = mean pairwise DM–DM collision velocity, the convention the Correa
anchors are quoted in) — and what happens to (a) the G3 collision claim, (b) the
G1–G3 passes, and (c) the v1 kill, across the audited band?

**Provenance (registered at 1865, verify-script ledger; nothing new is sourced):**
the pin is the J3′ SYNTHESIS WINDOW σ/m(50) ∈ [1,5] central / [0.5,10] extended,
built from: KTY 2016 (~2 cm²/g, galaxy scales) and Ren 2019 (~3, rotation-curve
diversity) — samples spanning galaxy velocities from ~50 upward; Elbert 2015
viability at **V_max = 40 km/s** (band 0.5–50 viable, optimum 5–10); the dSph-regime
fits explicitly marked "lower velocity than our 50 km/s pin — shape information, not
the pin itself."

**Convention map, fixed now:** ⟨v_rel⟩ = (4/√π)·√2·σ_1D = 2.26·σ_1D; for cored
dwarf/galaxy halos σ_1D ≈ (0.6–0.8)·V_max ⇒ **⟨v⟩ ≈ (1.4–1.8)·V_max** (standard
Maxwellian kinematics; the band, not a point, is carried). Elbert's V_max = 40 ⇒
⟨v⟩ ∈ [56, 72]. The KTY/Ren ceiling's native low edge sits AT ⟨v⟩ ≈ 50-class only
for their lowest-velocity galaxies; the samples extend well upward. **Audited
reading, to be graded:** the registered v_pin = 50 is the STEEPEST admissible
reading of the pin's own sources (the exact analogue of the 2345 dSph finding — the
registered frame had adopted the steepest strand); readings in ⟨v⟩ ≈ 56–72 are
equally licensed by the same ledger.

**Protocol, fixed now:**
1. Collision severity s_req(v_pin) = ln2 / ln(v_pin/48) across v_pin ∈
   {50, 55, 60, 64, 70}, graded against the family max 3.2. (Ceiling at the
   extended hi = 10 and Carina U = 2 floor = 20, as at 2351.)
2. **G3 joint re-search repeated with the pin window relocated** to each audited
   v_pin (both frame variants): three Correa anchors (U = 2, unchanged) + relocated
   pin + LSB + cluster, over the G1 knob box. The constructive question: does a
   kinetic population pass BOTH strands at an audited pin reading?
3. **v1-kill robustness (hurting-direction check, run FIRST):** the killed
   candidate's corrected curve (2337) evaluated with the pin at the audited
   velocities, v1-central window — does the kill class survive the pin correction?
   (Prediction from visible arithmetic, stated now: yes — the dSph ×1.6–2.1 failure
   is pin-independent; but the check runs before anything favorable is computed.)
4. G1/G2 pass points re-verified at the audited pin (relief direction expected;
   verified, not assumed).
5. Attribution correction to the 2355 adjudication (founder mapping: paste order =
   ChatGPT, Grok, Gemini, Copilot, DeepSeek; two self-IDs were wrong) — appended
   annotation, record never rewritten.

**Pre-registered outcomes:**
- **(i) COLLISION READING-DEPENDENT + BOTH-STRANDS PASS:** the audit licenses
  v_pin ≥ ~59 readings; the 2351 collision holds only at the steepest reading; a
  kinetic population passes collapse anchors + relocated pin + LSB + cluster at an
  audited reading. The strand split DISSOLVES-at-audited-pin; the frame decision
  gains the strongest possible input (the population can satisfy both published
  strands); the DM-1 population section is written dual-strand.
- **(ii) COLLISION STANDS:** the audit finds the ceiling's native ⟨v⟩ genuinely
  ≤ ~52 (e.g., the binding KTY/Ren galaxies sit there under the convention map), or
  the joint search still fails at every audited reading (another window binds). The
  2351 statement survives the audit; conditionality resolves against the collapse
  strand.
- **(iii) BAND-UNRESOLVED:** sources too heterogeneous to place the ceiling inside
  the band; the collision is graded per-reading and carried as a named demand-side
  degree of freedom (the 2345 grammar).

**Standing rules:** the v1 kill was graded at the registered frame and is never
regraded; this audit's kill-robustness check is a consistency verification, not a
regrade. No verdict moves in this patch; L4-b feeds the founder's frame/strand
decisions and the DM-1 rewrite.

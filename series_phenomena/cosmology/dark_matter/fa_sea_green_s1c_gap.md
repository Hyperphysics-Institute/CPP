# FA-SEA-GREEN S1c — the gap (P2) IDENTIFIED from mechanism: candidate (a), the DP internal binding, is what a static polarization pattern in the FREE Sea stretches — with a registered structural cancellation making the gap frequency SPECIES-UNIVERSAL (ω_A = 2c/d_DP for qDP and eDP alike) and answering I5: E_qq does NOT propagate; the readout is a point, not a band

**Patch 2670, 20 July 2026. Stage S1c of the FA-SEA-GREEN charter (FROZEN
2666), opened on S1b resolution (2669). Blind guards in force: no candidate
value, no screening length, no decay readout appears below — κ is derived
symbolically and every length readout is deferred to S2. 79.5% not in
scope.**

## §1 — The adjudication (from the PCD cycle and the Sea's constitution; never from a landed ℓ)

**The question (charter §2):** what does a static polarization pattern in
the **FREE** Sea stretch?

**Candidate (c) — 2443 bond curvature 2E_qq/s₀² — REJECTED for the free
Sea.** E_qq is the qq bond energy: a property of qDP chains (gluon bonds)
connecting cage vertices — structures of hadrons and condensed matter, not
of the free Sea. The free Sea's registered constitution (glossary: "DPs are
the 'atoms' of the vacuum — the DP Sea fills all of space") contains no qq
bonds; a static polarization pattern there has no bond to stretch. The
candidate's restoring object is absent from the medium being polarized.

**Candidate (b) — bound-CP oscillator, (ħω_A)² = 2E_qq(ħc)²/(d²m_q)
[2452] — REJECTED as content, RETAINED as form.** The same objection
applies to its energy content: E_qq belongs to bound structures. But its
registered FORM — an on-site harmonic restoring with stiffness =
2·(binding energy)/(length scale)² and inertia = the constituent CP mass,
whose ω → 0 limit is the registered on-site susceptibility — is exactly
the registered template for a bound-pair's static response, and it is
retained and instantiated below with free-Sea content.

**Candidate (a) — DP internal bindings E_qDP = 264 / E_eDP = 88 MeV
[0880/0886] — IDENTIFIED.** PCD mechanism: an arriving static SSV signal
is summed at Perceive; at Compute, the DP's two opposite-polarity CPs
respond oppositely to the common field, so the computed Displace stretches
the pair's internal separation against the DP's own binding — the one
restoring structure the free Sea actually possesses (glossary: "The
binding energy of a DP is the vacuum energy density" — explicitly a Sea
property). The amplitude diverted into this internal stretch is not
re-radiated: this IS the charter's "per-re-radiation loss into an on-site
restoring degree of freedom," located in a registered object.

## §2 — The gap frequency: a registered structural cancellation

Instantiating the retained 2452 template with candidate-(a) content:

(ħω_A)² = 2·E_DP·(ħc)² / (d_DP² · m_CP c²)

Per species, the registered ratio **E_DP / m_CP = 2 exactly**
(264/132 = 88/44 = 2 — the DP binding energy equals its constituent pair
mass sum), so

**ħω_A = 2ħc/d_DP  ⟺  ω_A = 2c/d_DP — identical for qDP and eDP.**

Three consequences, each registered:

1. **Species universality.** The Sea's gap frequency does not depend on
   which DP species responds; the Sea-composition question
   (OPEN-FP-SF-2-chaincomp lineage) is BYPASSED entirely — no composition
   assumption enters the arc.
2. **I5 ANSWERED: E_qq does not propagate.** Candidate (a) carries no
   E_qq; moreover even E_DP and m_CP individually cancel, leaving only
   their registered ratio (2) and a geometric length. **The readout is a
   point, not a band — the zero-parameter outcome (FG-BAND is off the
   table unless S2's robustness swaps reopen it).**
3. **P1/P2 both scale-free in energy.** S1b showed P1 supplies no energy
   scale; S1c now shows P2's energy content cancels to a pure ratio. The
   entire ℓ reduces to registered geometry times registered structure —
   the strongest possible zero-parameter form.

## §3 — The DP internal length d_DP: registered inference INF-S1C-1

The glossary DP definition: "A bound pair of opposite-polarity CPs
occupying **adjacent lattice sites**." At the hierarchy level where the DP
species carry their registered MeV bindings — the SS-2-embedded motif
level (S1b §3) — adjacent sites are edge-separated:

**INF-S1C-1 (registered, attackable): d_DP = ℓ_edge = ℓ_unit/φ**, by the
conjunction (registered DP definition) + (SS-2 embedding level assignment
of the MeV species registrations). The glossary sentence does not name the
hierarchy level explicitly; the level follows from where the species
energies are registered. This inference is surfaced by name so the panel
can attack it directly; if the panel rules it under-determined, the arc's
Branch-I clause applies retroactively to this stage.

**Fork-integrity note:** d_DP is a mechanism INPUT identified from the
registered DP definition — it is not the screening length, and no
preferred ℓ played any role in the identification (the identification
chain above never touches a decay quantity). Whatever ℓ the S2 spectrum
produces remains a genuine output.

## §4 — What S1c hands to S2

- **κ ≡ ω_A/c = 2/d_DP = 2φ/ℓ_unit** (symbolic; no length readout here).
- **Regime statement (output, honoring the 0757 fence — nothing
  borrowed):** κ·ℓ_edge = 2 > 1 — the Sea sits in the DISCRETE-SCATTERER
  regime where continuum homogenization fails between sites. This is the
  arc's own derivation of the registered "CPP exits the Debye regime"
  finding (2527 T-1), arrived at independently; the 0757 machinery was
  not consulted or borrowed.
- **Assembly prescription for the S1b operator M = I + αG:** the site
  response constant α is fixed by long-wavelength (uniform-mode)
  self-consistent matching of the discrete operator's response to the
  continuum gap κ² on the registered graph — implemented at S2, with the
  2527 density flag disclosed wherever a continuum-density comparison
  enters, and the graph-geodesic robustness swap mandatory per S1b §5.
- Verify script `code/2670_s1c_species_cancellation.py`: the species
  cancellation (ω_A d_DP/c = 2 both species) and the regime inequality —
  no decay quantity computed or printed.

**No Morse-class sentence consumed (2664 rider n/a).** Reasoning:
`reasoning/2670.md`.

# FA-C2 RE-DERIVATION CAMPAIGN CHARTER (C2R, FROZEN) — derive the physical on-site static susceptibility from the DP response mechanics (χ_static), obtain α from it rather than imposing a matching, compute the founder-specified near-core superposition correction χ(r), and thereby COLLAPSE the normalization-conditional pair {0.0904 ± 0.0028 fm cont-matched | ≈0.168 fm premise-rejected} to a single derived screening length — under frozen outcome classes C2R-CONFIRM / C2R-CORRECTED / C2R-OBSTRUCTED, with the α^(−1/2) law banned, FA-C3 closed, the observation-grade cap immovable from inside, and a hard fence against confusing this PROXY-normalization derivation with the separate ALPHA-1 continuum-candidate scale

**Patch 2768, 22 July 2026. Authority: the 2766 adjudication §3
(five-seat unanimous operations; grounding split 3–1–1 resolved by
ontology at 2767, not by vote), the 2767 founder ruling (core
occupancy; superposition specification), rider v2.6 + v2.6-A. This
charter freezes objectives, legs, outcome classes, tolerances, and
fences BEFORE any derivation runs. Reasoning: `reasoning/2768.md`.
79.5% not in scope.**

## §1 — Objective and scope fence

**Objective.** Replace the imposed matching prescriptions with a
derivation: DP response mechanics → χ_static → α → ℓ, for the frozen
dense-site 1/r proxy operator (FG-OTHER-LATTICE branch). The
campaign's product is a DERIVED ℓ with a stated correction and error,
collapsing the rider-v2.6 pair.

**Fence F1 (proxy vs continuum candidate — binding).** This campaign
operates entirely WITHIN the proxy branch of the v2.5 dual
registration. The separate ALPHA-1 continuum-candidate scale
(ℓ_cont = d_DP/2 = 0.1820 fm leading-order, KINETIC-1-conditional)
is NOT adjudicated, revised, or consumed here; the no-mixing clause
(rider v2.5 clause 1) applies to every C2R sentence. A C2R outcome
moves the PROXY readout only.

**Fence F2 (standing bans).** α^(−1/2) propagation stays banned
(falsified, 5–0). FA-C3 stays CLOSED (5–0 × 3). No grade motion from
inside: whatever class fires, the observation-grade cap and the
zero-parameter language ban move only by panel adjudication on a
subsequent dispatch.

**Fence F3 (ontology inputs).** The physical inputs are the 2767
ruling's, verbatim: the r < a core is occupied by responding
both-charge DP-Sea medium; response is to SSV_net at each GP every
Moment; the only exclusion is point-like same-GP self-superposition;
near-core, adjacent charges' oppositely-signed radial polarization
gradients SUPERPOSE under the Gauss's-law enclosed-charge
constraint. No leg may quietly re-introduce the rejected
self-exclusion premise; the site matching appears ONLY in L3 as a
sensitivity bracket.

## §2 — Legs (each executes under its own prereg with a committed verify script; deterministic; no Metropolis, so CONV-005 does not fire; CONV-003/CONV-004 tags apply throughout)

**C2R-L1 — Leading order: derive χ_bulk and α.** From the
every-Moment response rule, derive the time-averaged statistical
rearrangement of the both-charge Sea under a weak static bias (the
definition of χ_static per the 2767 record §3), in the uniform-medium
limit. Deliverables: (i) analytic χ_bulk; (ii) the α it implies;
(iii) the leading-order ℓ_LO from the frozen operator at that α;
(iv) an explicit statement of whether the derivation REPRODUCES the
continuum matching α·S_cont = 1 at the κ·a = 2 operating point
(deriving what was previously imposed), and if not, by what factor
it differs. [TIER: DERIVED for (i)–(iii); the comparison (iv) is a
check, not an input.]

**C2R-L2 — Near-core correction: χ(r) by superposition.** Construct
the near-core susceptibility profile per the founder specification:
each core charge carries a radial plus/minus asymmetry gradient
(inverse-square-law–governed at the single-charge level, per the
eCP–DP-Sea interface template); between adjacent charges the
oppositely-signed curves superpose; the Gauss's-law enclosed-charge
criterion constrains every closed surface. Deliverables: (i) χ(r)
between and around the core sites; (ii) the correction δℓ/ℓ_LO the
profile induces on the operator readout, with sign, magnitude, and
an error statement; (iii) a numerical cross-check of the analytic
profile by direct evaluation on the committed A0/A1 arenas
(deterministic script, committed seeds only if any stochastic
element is introduced — none is anticipated).

**C2R-L3 — Sensitivity bracket (bounded role).** Evaluate ℓ under
the premise-rejected site matching exactly once, as the outer
bracket of normalization sensitivity. No physics claim attaches; the
leg exists so every consumer sentence can quote the bracket the
rider's triage clause requires. [OBS-class by construction.]

**C2R-L4 — Consolidated readout.** Assemble ℓ_derived = ℓ_LO ×
(1 + δℓ/ℓ_LO) with combined error (envelope ⊕ derivation error),
apply the frozen class machinery (§3), and draft the dispatch packet
carrying the result, the pair's disposition, and the rider-motion
recommendation for the panel.

## §3 — Frozen outcome classes (evaluated mechanically from L1/L2 deliverables; committed before any leg runs)

Let W = 0.0028/0.0904 ≈ 3.1% (the robustness-envelope half-width
fraction).

- **C2R-CONFIRM** — L1 reproduces the continuum matching within 5%
  in α AND |δℓ/ℓ_LO| ≤ W. Consequence: the pair COLLAPSES to
  ℓ_derived (numerically ≈ the 0.0904 band, now derivation-backed);
  the dispatch recommends the panel consider cap motion; the
  zero-parameter ban lifts ONLY if the panel rules the derivation
  closes clause 7's condition.
- **C2R-CORRECTED** — the derivation closes (L1+L2 sound) but
  EITHER L1's α differs from the imposed matching by >5% OR
  |δℓ/ℓ_LO| > W. Consequence: the pair collapses to ℓ_derived at its
  NEW value; the ≈0.0904 number is superseded same-font; cap stays;
  all rider-v2.6 consumer triage re-runs against ℓ_derived; the
  dispatch reports the shift with the L4 ledger.
- **C2R-OBSTRUCTED** — either leg cannot close without inputs the
  ontology record does not yet contain (anticipated dependency: if
  χ_static proves inseparable from the kinetic closure, the leg
  names OPEN-DM-KINETIC-1 and stops). Consequence: the pair STANDS
  under rider v2.6-A unchanged; the obstruction is registered as the
  named OPEN item's concrete question; no partial number ships.
- Classes are exclusive; L4 states which fired and why, quoting the
  two decision quantities. Adverse-direction results (e.g., L1
  failing to reproduce the matching) report same-font — that outcome
  is C2R-CORRECTED doing its job, not a campaign failure.

## §4 — Discipline

Each leg's prereg freezes its method before execution; every
computation ships a verify script in `code/` under the reasoning-
capture rider (one `git am` per act: prereg/record + reasoning
fragment + script); CLONE-FIRST GATE before any new ID; PD-006
governs (physics-picture questions to the founder — none currently
open; the 2767 ruling is the input set); the review-economy rule
holds the panel dispatch to the single L4 packet. Queue position:
C2R runs now; L2-RB, X3/X4 preregs, AUTOMATON-1, OPEN-DM-CHARGE-1
hold their 2766 §7 order behind it except where a session can
interleave mechanically independent work.

# N1 R-C rider pre-registration: the H-C member (per-step Newtonian home dynamics at the pinned masses), gated by the mandatory strong-pair equilibrium control, committed before any derivation

**Patch 2585, 19 July 2026. Status: R-C OPENED at rider pre-registration under 2581 + 2583 +
this rider; NO derivation performed.** Founder direction: "go" (session channel).

## 1. The H-C law (frozen; citations per the standing discipline)

Per-step, per home i: **V_i ← V_i + (F_total,i / m_i)·dt ; H_i ← H_i + V_i·dt** (semi-implicit
Euler, declared; stability ω·dt ≤ 0.92 across the dt union for the stiffest completed bond —
resolvable at ~27 steps/period at dt = τ_C/100). m_i = the pinned CP masses (132 / 44 MeV/c²)
— **the SF-6 pins enter as the inertial masses, the 2584-noted deeper intent of the charter
clause.** F_total per the 2583 completion (registered electric kernel + the Morse union),
force-evaluation members atH and atP both run; strong always at homes (2583 §2). No damping
term exists (the eleven-word fence); no velocity cap is imposed, but a relativity assert
|V| < 0.9c fires any cell into UNRES-NUM, disclosed. Energy ledger tracked (conservative law +
conservative forces): drift must be dt-convergent within a declared tolerance (< floor over
TC at dt = 1/100); a dt-stable drift is an integrator artifact here (no emergent-transfer
clause applies — the choreography feeds homes only through the atP member's force evaluation,
and that pathway's energy behavior is REPORTED, not read).

## 2. Gates (all before any ladder cell; frozen)

- **G-SP — the mandatory STRONG-PAIR EQUILIBRIUM control (2584 adoption):** the qCP pair at
  the computed minimum of U_total (per width member) must classify HOLD on the PRIMARY band
  at every dt, both force-evaluation members. Failure → Branch I, H-C dead at gate.
- **G-N1** singleton exactly at rest. **G-N2** pinned regression (coupling = 0): HOLD at
  every rung. **G-P** momentum closure (conservative Newtonian: |ΔP_tot| < 10⁻³ scale at
  dt = 1/100, dt-convergent). **G-E** the §1 energy tolerance at G-SP.
- **Displaced-pair sanity (declared, non-gating, reported):** the pair displaced 0.1·r_eq
  from equilibrium must oscillate boundedly (undamped Newtonian oscillation is the law's
  honest behavior; the classifier's bands accommodate bounded breathing).

## 3. The ladder and readings (inherited frozen)

Rungs, grids, dt union, truncation union (DIST primary / FULL secondary), classifiers and
bands, and the 2583 §4 readings apply VERBATIM: win-class requires the primary band at all
dt under BOTH force-evaluation members AND BOTH widths; the adverse requires widened-band
failure under all members; splits report per-member and route. One addition, declared: the
rungs start at the REGISTERED geometry, which is not necessarily the completed interaction's
equilibrium — bounded oscillation about a nearby equilibrium is HOLD-compatible by the
existing bands; no re-centering of initial conditions toward computed equilibria is
performed (that would be answering the stability question by construction).

## 4. Bookkeeping

79.5% untouched. Next patch: the R-C execution (gates, then the ladder), under
2581 + 2583 + this rider only.

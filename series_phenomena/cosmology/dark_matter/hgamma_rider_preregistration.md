# γ-member rider pre-registration: relativistic home kinematics — the law, the sink's minimal faithful extension, the momentum ledger, and the full admission battery including the deferred ×2.0 gate, committed before any run

**Patch 2601, 20 July 2026. Status: the γ-member OPENED at rider pre-registration per founder
direction ("go γ"); NO run performed.** Governed by the N2 charter, the sink registration in
its 2600-corrected scope, the 2598 prereg's UNRES-REL discipline, and this rider.

## 1. The law (H-γ; frozen)

Per step, per home i (natural units, c = 1, masses in MeV): **p_i ← p_i + F_i·dt ;
v_i = p_i/√(m_i² + |p_i|²) ; H_i ← H_i + v_i·dt** (semi-implicit, momentum-form). Speed is
subluminal BY CONSTRUCTION — the 0.9c assert retires for this member and is replaced by a
γ_max MONITOR (recorded per cell, never gating). Kinetic energy is relativistic:
KE_i = √(m_i² + |p_i|²) − m_i; the ledger closes on Σ KE + U + Sea-deposit.

## 2. The sink under γ (minimal faithful extension; two disclosures)

At each Moment-cycle boundary: velocity decomposition as registered (drift = cycle-mean of
v; oscillatory = residual); **the contraction v ← v_drift + √(1−η)·(v − v_drift) is
subluminal by CONVEXITY** (the segment between two interior points of the unit ball lies
inside it — stated as the operation's admissibility proof); p recomputed from the new v;
**the deposit is the EXACT relativistic KE removed** (the 2596 exact-ledger rule, now with
relativistic KE). Disclosures carried as riders: (i) the η band [0.5, 0.7] was DERIVED
non-relativistically (2596); it runs as registered with a γ-re-derivation queued as a named
N2-A follow-on — if the re-derivation moves the band, consumers re-run; (ii) a **momentum
ledger** joins the energy ledger: the sink's per-event momentum change is tracked as the
Sea's momentum intake (C-SR: uniform motion has zero residual, hence zero momentum shed —
the ledger must show exactly that).

## 3. Admission battery (the checklist; gates enumerated here, all before any campaign cell)

**G-SP-γ:** the strong-pair at r_eq (HOLD, ledger exact) AND displaced 1.1·r_eq (settles to
r_eq; deposit banked) — both widths, dt ∈ {1/200, 1/100, 1/50}. **G2-γ (the DEFERRED
gate):** the ×2.0 expansion must CONVERGE, now certifiably (γ_max monitored, ledger closed
dt-convergently, deposit balancing released well depth) — both widths, both η. **G3-γ:**
the 5-cell completes with closure (the steep width now certifiable). **G4-γ (C-SR):** the
0.3c-boosted square — deposit matches the rest frame within the floor; drift preserved;
the relativistic drift/osc split's O(v²) frame subtlety is measured, not assumed
(mismatch beyond floor → recorded and routed, not tuned away). **G5-γ:** dt convergence of
deposits and drifts; **G-CONS-γ:** energy closure on the G-SP config (the member-admission
clause, direction-blind). Any gate failure → the member dies at gate; repairs re-enter as
new candidates.

## 4. Re-opened campaigns (order; readings inherited)

On gates passing: **B1 re-run** (the full 2598 §1 grid, classifiers verbatim, UNRES-REL
retired for this member — every cell must now yield a verdict; γ_max recorded per cell);
then B3 re-run; then B2 (the chain) and B4 (rates) under their frozen readings. The 2598
equal-status readings and fences (no relic contact) travel unchanged.

## 5. Bookkeeping

Nothing here touches N1, the sink's corrected-scope registration, item 5's cleared status,
or 79.5%. Next patch: the H-γ execution — gates, then B1.

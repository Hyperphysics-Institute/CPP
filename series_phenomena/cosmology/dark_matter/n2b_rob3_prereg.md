# N2B-ROB-3 pre-registration: the soft-width (w = 2) confirmation of ROB-2's drawn walls (φ = 45°, +y face) — the optional successor declared at 2638, committed before any run

**Patch 2643, 20 July 2026. Status: N2B-ROB-3 OPENED at pre-registration; NO run
performed.** This is the optional successor declared in the ROB-2 record (2638
§4 scope disclosures: "the walls' width behavior is undetermined (optional
successor, declared not queued: φ = 45° and +y at w = 2)"), taken up
founder-free at the founder's word (this session). Its consumer is FORM-1: the
stage-7 anisotropy exhibit (orientation wall + face wall + the +x distinct
geometry) gains or loses a width-sharing clause. Governed by this document, the
ROB-2 prereg/record (2637/2638) whose matched instrument carries VERBATIM, and
the chain lineage. Verify: none at prereg; execution next patch under this
document only.

## 0. Target and framing

ROB-2 drew both walls at the steep width only. ROB-1's soft rows (2631 §3)
tested SURVIVORS, not walls — so whether the drawn walls are width-shared is an
open sentence, and it is exactly the kind of sentence the soft confirm
discipline exists to gate (2630 §4: soft rows "gate the sentence 'the envelope
is width-shared' and nothing else"). This cell extends that discipline to the
walls: **do the φ = 45° and +y cells halt at w = 2 as they do at w = 4?**
Either answer registers as structure: width-shared walls sharpen the FORM-1
anisotropy exhibit (the wall is the target's geometry, not the width's);
width-dependent walls register a wider soft envelope in that family (also
FORM-relevant — stiffness enters the marginal-aim boundary). Neither reading
is adverse.

## 1. Registered inputs (verbatim; zero freedom)

Instrument: the ROB-2 MATCHED launcher — the registered 2611 artifact with the
economy re-settle leg removed by the exact 2638 source substitution (verified
unique), exec-loaded identically. Chain builder verbatim (2630 §1 / 2637 §1):
4-square settle TC = 60 at 1/100; four sequential launches, charge alternation
5(−1) 6(+1) 7(−1) 8(+1); stage BOUND iff CAP ∧ dmax < 3D both dt; propagate on
the coarse path; completion settle at the fine dt, TC = 60, dmax < 3D. Width:
**w = 2 (soft, bd = 2.0) throughout.** dt-union {1/100, 1/200}.

## 2. Controls (gating; run FIRST; failure → RC4)

- **C-S1 — matched soft registered-channel control:** the matched chain at the
  unperturbed channel (axis +z, b = 0.5D, φ = 0, w = 2) must COMPLETE
  dt-stably. Settle numbers are reported beside the 2616 RS1 / 2631 soft-row
  values (0.84/0.86/0.01) for the record; the GATE is completion + dt-stability
  (the matched launcher is a different instrument from the economy one — exact
  settle reproduction is not demanded, wholesale disagreement is disclosed).
- **C-S2 — drawn-soft-cell conservatism:** the matched chain at φ = 30°, w = 2
  (a drawn 2631 soft survivor) must COMPLETE dt-stably. A flip → RC4 with the
  2631 soft-row entry flagged instrument-dependent for adjudication (same font).

## 3. The two cells and the frozen ladder (ROB-2's, imported verbatim)

- **Cell A-soft:** axis +z, b = 0.5D, **φ = 45°**, w = 2.
- **Cell B-soft:** axis **+y**, b = 0.5D, φ = 0, w = 2.

**Escalation (frozen):** a cell whose stage-verdict pair disagrees at
{1/100, 1/200} re-runs ONCE at {1/200, 1/400} (propagate on 1/200, settle at
1/400); disagreement at both unions reads RS-UNDRAWN. No third union.

**Conditional refinement (frozen):** iff Cell A-soft HALTS dt-stably, one chain
at **φ = 37.5°, w = 2** runs to test whether the steep bracket (37.5°, 45°] is
also the soft bracket. It does not run under any other outcome.

Per chain, report per 2637 §3: per-stage verdict pairs, Sea (endpoint-ledger),
γmax, |Δcen|, stage-of-first-failure, settle on completion; FRG on its own line.

## 4. Frozen readings (per cell, composable)

- **RS-W-SHARED:** the cell HALTS dt-stably at w = 2 → the wall is
  WIDTH-SHARED at that cell; if both cells and the refinement reproduce the
  steep pattern (halt at 45°, complete at 37.5°; halt at +y), the FORM-1
  exhibit gains the clause "the stage-7 anisotropy walls are width-shared" —
  the wall is the grown target's geometry, not the width's.
- **RS-W-DEPENDENT:** the cell COMPLETES dt-stably at w = 2 → the wall is
  width-DEPENDENT; the soft envelope is wider in that family and registers as
  such (structure, not adverse); the FORM-1 exhibit gains the stiffness clause
  instead.
- **RS-UNDRAWN:** dt-instability at both unions → the cell is undrawn at w = 2
  at this instrument class; raw disclosed; successor derivational.
- **RS-RC4:** C-S1 or C-S2 fails → nothing reads.

## 5. Fences

Existence only — no rates, no σ, no flux; FW-1 rides every consumer sentence
(aimed-convention results); deposits quoted are endpoint-ledger values
(SINK-OBS-1), no schedule claims; v = 0.10c, DISC block untouched; the 2631 and
2638 records unedited (extended by citation only); the STEEP walls of 2638 are
untouched under every reading (this cell reads the soft width only); **79.5%
untouched under every reading.**

## 6. Bookkeeping

Prereg: this document (Patch 2643). Execution: `code/2644_n2b_rob3.py`, stages
`controls | cells | escalate | refine`; record: `n2b_rob3_record.md`
(Patch 2644). DEP-1 row at the execution patch if a registration lands.
Reasoning fragments at both patches.

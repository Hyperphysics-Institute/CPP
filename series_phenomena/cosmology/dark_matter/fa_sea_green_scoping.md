# FA-SEA-GREEN SCOPING — input ledger against the registry: GEOMETRY FULLY PINNED, the response KERNEL and the GAP are the arc's two named derivation pins (not blockers — they ARE the arc); one structural discovery: assuming a nearest-neighbor operator would half-answer the fork by construction, so the kernel's RANGE must be derived from the registered Perceive summation, blind

**Patch 2665, 20 July 2026. Scoping session per the 2664 adjudication §6
registration (FA-SEA-GREEN, unscheduled → scoped here at the founder's word).
CLONE-FIRST satisfied: registry grepped before every identification below;
every pin status cites its registered home. No physics computed; the single
instrument demonstration is fork-blind by construction. 79.5% untouched.**

## 1. The target, restated from 2664 §6

Construct the discrete static response operator of the DP Sea on the 600-cell
substrate from registered objects only; compute its Green function; the lowest
inverse-decay eigenvalue IS the screening length ℓ of FA-C2. One calculation,
two discharges: FA-C2 becomes a CPP theorem (or is refuted), and FA-C3
resolves analytically (ℓ emerges cell-scale, edge-scale, other, or the inputs
prove unregistered → Branch I).

## 2. Input ledger

**I1 — Lattice graph: PINNED.** ℓ_unit = ℏc/Λ_QCD = 0.589 fm (600-cell
circumradius, glossary [SS-2]); ℓ_edge = ℓ_unit/φ = 0.364 fm (glossary
[SS-2]); coordination z = 12 icosahedral (SF-4 §, session-054 registration);
edge spacing = ℓ_unit/φ under AXIM-2 (substrate-stress framework;
theorem-dependency-graph THEO-SS-1..10 lineage; the 4D→3D packing inference
flag from 2527 noted where density, not graph structure, is consumed).
Instrument demonstration `code/2665_600cell_graph_check.py` (ALL PASS):
|V| = 120, |E| = 720, degree-12 uniform, unit circumradius, edge = 1/φ,
Laplacian spectrum computable (λ₁ = 2.2918 dimensionless, instrument-level,
quarantined). **Deliberately omitted from the demonstration:** any gap
parameter, Green function, or kernel comparison — the fork blind is intact.

**I2 — Response degree of freedom: PINNED (definitional).** Per-vertex
polarization amplitude; SSV_net = "the vector sum of all SSV contributions at
a point," SSV_abs its magnitude counterpart (glossary table). The static
response field f of FA-C2 lives on vertices; the founders_voice 2026-07-20
capture (2662) records the Perceive-phase spherical-totality summation as its
modeled aggregation.

**I3 — Response kernel (inter-site structure): UNPINNED — NAMED PIN P1, the
arc's first derivation target.** No registered nearest-neighbor transfer
coefficient exists (grep: no DP polarizability, spring constant, or SSV
transfer coefficient in glossary or SS frontier). **The structural discovery
of this scoping:** writing the operator as gap + nearest-neighbor graph
Laplacian would ASSUME edge-locality of the response — i.e., it would
half-answer FA-C3 toward the edge reading by construction. The registered
Perceive summation is over "all the signals from all the other GPs in the
local (within l_P) and distant domains" (founder verbatim, 2662) — a
long-ranged kernel with 1/r² shell superposition, not a hop kernel. **P1
therefore reads: derive the kernel — its range, radial weight, and
normalization — from the registered SSV summation rules (AXIM-2/AXIM-7
lineage) with NO locality assumption imported.** Fork-neutrality guard: the
derivation is executed blind to both candidate values (βd = 1.953 / 3.159),
under the blind-session protocol class already exercised for SF-6 inertia.

**I4 — Gap term (on-site restoring): UNPINNED AS AN IDENTIFICATION — NAMED
PIN P2, three registered candidates on the table, unadjudicated:**
(a) the DP internal binding scale — E_qDP = 264 MeV, E_eDP = 88 MeV
(0880/0886 constituent registrations, consumed at 2452: element =
4·264 + 4·88 = 1408 MeV, the zero-parameter DD-mass cross-check);
(b) the registered bound-CP oscillator response 1/(ω_n² − ω_A²) with
(ħω_A)² = 2E_qq(ħc)²/(d²m_q), m_qCP = 132 / m_eCP = 44 MeV (2452; SF-6 κ
pins 2496 concordant) — its ω → 0 static limit is a registered on-site
susceptibility form;
(c) the 2443-registered bond-form curvature 2E_qq/s₀² (2462 gate-grep
lineage).
P2 reads: identify which registered object IS the Sea's on-site restoring for
static polarization, from the mechanism — not from which one lands a
preferred ℓ. Candidates (b)/(c) import the E_qq dependence; candidate (a)
does not.

**I5 — E_qq propagation status: RESOLVED-INSIDE-THE-ARC.** E_qq is a
registered WINDOW ({40, 66, 100, 170} map, central 66; 2452: "THE remaining
scale pin"), not a point. Scoping observation, registered without claim: ℓ in
lattice units is set by the gap-to-coupling ratio; if P1's kernel and P2's
gap share one energy scale, the ratio — and hence βd — is **purely
structural and the E_qq window does not propagate** (a zero-parameter
outcome); if they carry different scales, the window propagates and the arc
reports bands, not points. Which case obtains is an output of the
derivation, never a selection.

**I6 — Normalization and units: PINNED.** f(d) = 1 at the registration
distance d = 1.15 fm [2433] per FA-C2's registered form; physical units
enter only through the SS-2 embedding (I1) — no scale choice is available to
the worker, which is what keeps the arc's output fork-deciding rather than
fork-assuming.

**Adjacent machinery, consulted and fenced:** 0757 Debye lemma is
registered-INAPPLICABLE (2527 T-1: "CPP exits the Debye regime") — it may
not be borrowed as the kernel; SF-5 single-hadron screening (~1 fm) and the
1858 rod residual (r_c = 1 fm convention) are ambient-length registrations,
consumable as *consistency checks* on the derived ℓ, never as inputs.

## 3. Verdict: SCOPE-COMPLETE, CHARTER-READY

P1 and P2 are not blockers — they are the arc's content, and both are
derivations from registered objects with Branch I as the honest exit if the
registry under-determines them. Charter elements to freeze (next patch, fresh
session per the 2527 governance pattern — the decisive derivation deserves
clean context):

1. **Stage 1 (physics):** derive kernel (P1) and gap (P2) from registered
   objects only, blind to both candidate values; founder physical-picture
   input is anticipated at the kernel-range step (the S2/S3 pattern — his
   Perceive-summation account is the mechanism being formalized).
2. **Stage 2 (mechanical):** Green function / lowest inverse-decay
   eigenvalue on the I1 graph; ℓ read out in fm via I6.
3. **Outcome classes (pre-frozen):** ℓ ≈ cell-scale / ℓ ≈ edge-scale /
   other eigenlength (the Seat-1 possibility the fork must admit) /
   window-band (I5 non-cancellation) / **Branch I** (registry
   under-determines P1 or P2 — the arc closes honestly and FA-C3-DISC-1
   becomes the discovery route).
4. **Fences:** FA-C3-DISC-1 untouched (replication route regardless of
   outcome); the 2664 rider travels on every consumer sentence; OBS-class
   numerical coincidences non-adjudicative throughout; 79.5% not in scope.

Reasoning: `reasoning/2665.md`. Artifact:
`code/2665_600cell_graph_check.py` (ALL PASS).

# NUCLEUS-1 pre-registration: the minimal dynamically stable cluster under the full melee — per-CP dynamical homes, the two home-dynamics conventions, the hold/disperse/collapse classifiers, the ladder, controls, and equal-status readings, committed before any derivation

**Patch 2581, 19 July 2026. Status: N1 OPENED at pre-registration; NO derivation performed.**
Governed by charter v2.0 (2580) and this document only. **Verify: none needed at prereg.**
Collision check clean (no prior NUCLEUS/N1 registration).

## 0. Target and the question's shape

**Target:** for each rung of the charter ladder, the dynamical fate of the registered cluster
when EVERY CP's home is freed — no static pinning anywhere, the full melee acting — classified
HOLD / DISPERSE / COLLAPSE / UNRESOLVED. **The charter's equal-status readings govern:** a rung
that HOLDS where the pair collapsed is direct dynamical support for the founder's multi-body-
resonance expectation (win-class candidate under standing dispatch rules); a ladder that fails
at every rung is a recorded adverse hit to that expectation. Both were pre-committed at 2580
before this prereg existed; nothing here may soften either.

## 1. Capability census (against the banked code; the two standing questions applied)

**Consumed as banked:** the O1a engine (2573 — momentum-exact impulse layer, live reach-S
lists, the verbatim CP contention dance, the physical-force correction); the CL classifiers'
machinery (as building blocks only — §3 defines N1's own classifiers). **Registered by this
prereg:** singleton-structure mode (each CP a structure: home = a point, mass = its pinned
value, no rotation — a point has none; ALL pairwise forces are cross-structure by
construction). **"What does this representation discard":** nothing — per-CP homes is the
LEAST abstracted representation the instrument admits; the melee is complete at the pairwise
level; tertiary/quaternary Sea-SSV participation (adjudication #4) is NOT representable here
and is the named scope limit — a HOLD or a failure is a statement about the pairwise-melee
layer, with the Sea layer's contribution an open rider either way. **"Does every step cite its
source":** the home-velocity law below cites the charter clause and the registered SF-6 form
explicitly; the Newtonian variant cites the K1a lineage.

## 2. The home-dynamics conventions (the load-bearing freeze)

The charter's licensed law reads: "homes acquire ΔV from the cycle-averaged cross-structure
force, **relaxation governed by the registered SF-6 inertia pins**." Two implementations are
defensible and BOTH run:

- **H-A (PRIMARY — the clause's letter):** the home's velocity obeys the REGISTERED SF-6
  relaxation form — the same law the CPs themselves obey in the dance, applied to the home
  with the cycle-averaged cross force: V_home relaxes toward min(μ_F·|F̄|, 1)·F̂ with the
  exponential memory e^(−dt/(κ·μ_F)) at the CP's pinned κ (132/44), speed capped at c by
  construction. Citation: the dance v-update (2557 verbatim) + the charter clause. This is the
  registered inertia law, not arbitrary dissipation — the eleven-word fence is satisfied by
  citation, and the energy the relaxation sheds is, in the founder's picture, the Sea channel;
  that identification is carried as a NOTE, not a claim.
- **H-B (SECONDARY — the K1a lineage):** Newtonian cycle-impulse V += F̄·τ_C/m, with a
  declared sanity assert |ΔV_cycle| < 0.5c per impulse (a fired assert → the cell reads
  UNRESOLVED-NUMERICAL, disclosed — at CP masses the Newtonian impulses may be violent; this
  is anticipated, not hidden).
Disagreement between H-A and H-B is reported per-convention, never averaged; the founder's
substrate expectation speaks to H-A (the registered dynamics).

## 3. Classifiers (frozen; primary + widened union)

Over the final 25% of steps, with R(t) = RMS CP distance from the instantaneous centroid and
R₀ its initial value, Dmax = max CP distance from centroid:
- **HOLD:** mean R ∈ [0.5, 2.0]·R₀ AND Dmax < 3·R₀(max) throughout the window AND the verdict
  is identical in every dt cell.
- **DISPERSE:** Dmax exceeds 3·R₀(max) with an outward trend at end, or mean R > 2·R₀.
- **COLLAPSE:** mean R < 0.5·R₀ (the merger channel — the pair's fate).
- **UNRESOLVED** otherwise, or on dt-disagreement, or under §2's fired assert.
Union member: the widened band [0.33, 3.0]·R₀ / 4.5·R₀(max). A HOLD claim requires the
PRIMARY (narrow) band; a failure claim requires failing even the WIDENED band — the standing
asymmetry, applied to a result the author visibly wants to come out one way.

## 4. Ladder (charter-fixed order; each rung frozen before the next opens)

**RUNG-1:** the 4-qCP alternating square (side D). **RUNG-2:** the full registered plane
(4q + 4e cross, R_E coat). **RUNG-3:** the two-plane element (16 CPs, pitch D, alternating
parity). Initial homes at the registered geometry; CPs at homes; all velocities zero. Run
conventions: dt union {1/100, 1/50, 1/25}·τ_C; TC = 60 with burn 0.15 for the classifier
window; a TC = 120 extension is pre-declared for UNRESOLVED cells ONLY (declared now so no
post-hoc extension chases a wanted verdict); FREF = FREF_sys of the rung's initial
configuration (primary), FREF_dp probe at RUNG-1 only.

## 5. Controls (gating; frozen)

- **CTRL-N1 (singleton):** one free CP — no partners, no cross force: must remain at rest
  (both conventions) within numerical zero.
- **CTRL-N2 (pinned regression):** each rung at coupling = 0 (static homes) must classify
  HOLD — the classifiers must read the registered pinned configurations correctly.
- **CTRL-N3 (continuity — the pair must still fail):** the two-qDP pair (the 2573/2575
  configuration) under per-CP homes must NOT classify HOLD under either convention — the mode
  that frees the plane must not accidentally stabilize everything (guards specifically
  against H-A's relaxation manufacturing false stability and against H-B's impulse
  discretization doing the same).
- **CTRL-N4 (momentum, H-B):** total momentum conserved through an asymmetric RUNG-1 run
  within the 2572 tolerance. (H-A is relaxational by citation; momentum non-conservation
  under H-A is DISCLOSED as the registered law's property — the shed momentum is the Sea
  channel note — not asserted away.)
Any control failure → halt, Branch I, control named; faithful-implementation fixes of the
established class disclosed and re-run.

## 6. Readings (frozen; the charter's equal-status commitment operationalized)

- **RUNG HOLDS (primary band, all dt, H-A):** the minimal stable cluster is ≤ that rung —
  banked; the ladder CONTINUES downward in significance (a RUNG-1 hold is the strongest
  possible support; a hold first appearing at RUNG-3 localizes the resonance at element
  scale). Win-class candidacy per the charter attaches to the FIRST holding rung; dispatch
  timing per the review economy (after the full ladder freezes).
- **ALL RUNGS FAIL (even the widened band, both conventions):** the founder's expectation
  takes the pre-committed adverse hit at the pairwise-melee layer, recorded as-is, with the
  Sea-layer scope limit (§1) as the named open rider — routed to founder.
- **Split by convention (H-A holds, H-B fails or vice versa):** reported per-convention; the
  H-A verdict carries the substrate reading (cited grounds); the split itself banks as
  instrument physics.
- **UNRESOLVED-dominated:** the TC = 120 extension fires (pre-declared); still unresolved →
  honest partial. No composition reading; any new √5 → fence; no rate content.

## 7. Bookkeeping

79.5% untouched. Next patch: the N1 execution (controls, then the ladder in order), under
this document only.

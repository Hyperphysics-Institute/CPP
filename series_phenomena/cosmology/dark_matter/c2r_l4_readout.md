# C2R-L4 CONSOLIDATED READOUT — outcome class **C2R-OBSTRUCTED** (frozen §3, mechanical): L1 closed with D1 = 0 exact (the continuum matching is DERIVED — the unique occupied-core closure), but L2 found the near-core χ(r) correction NON-PERTURBATIVE (D3 = |δℓ/ℓ_LO| = 220% ± 78% ≫ W = 3.1%, staggering destroyed, committed 50% honesty bound fired) — the derivation does NOT close: the short-distance shape of χ_static is inseparable from the underived per-site stiffness, the charter's anticipated KINETIC-1 dependency — the pair STANDS under rider v2.6-A unchanged, NO partial number ships, and the obstruction is registered as OPEN-DM-KINETIC-1's concrete question

**Patch 2775, 22 July 2026. Charter: `fa_c2_rederivation_charter.md`
(2768, frozen). Leg records: L1 = 2770, L2 = 2773, L3 = 2774.
Reasoning: `reasoning/2775.md`. Dispatch packet:
`conv001_2026-07_c2r_packet_brief.md` (same patch). Fences F1–F3
honored throughout; 79.5% untouched by this campaign.**

## §1 — Leg ledger

| Leg | Patch | One line |
|---|---|---|
| L1 | 2770 | CLOSES: κ² = 4πnα derived from the every-Moment rule + occupancy; **D1 = 0 exact** (α = a/(π√2) is the unique occupied-core closure, machine identity); 1.611 ledger reconciled to the digit (−59.40% rejected-premise medium, +21.5% real discreteness) |
| L2 | 2773 | ADVERSE, in full: founder-specified distributed-response profile through the frozen operator gives **δℓ/ℓ_LO = +220% ± 78%** on A0/A1 × R7,9 × 3 windows (paired); staggering destroyed (nn-flip 0.44 → 0.07); R² degrades to 0.57–0.70; analytic cross-check consonant (complex poles, decay 0.210 fm, oscillation 2.29 fm); **committed 50% honesty bound FIRES** — first-order number not quotable as ℓ_derived |
| L3 | 2774 | OBS bracket registered: ℓ(α′ = 1.611α) = 0.1679 fm (R² 0.554), reproducing 2688 to the digit; premise-rejected status travels with the quote |

## §2 — Class determination (frozen §3, mechanical; the two decision quantities quoted)

**D1 = 0** (≤ 5% ✓). **D3 = 220% ≫ W = 3.1%** (✗). Therefore not
C2R-CONFIRM. C2R-CORRECTED requires "the derivation closes (L1+L2
sound)" and the pair collapsing to ℓ_derived at a new value — but
L2's own committed honesty bound rules the first-order ℓ_L2 the
first term of a visibly non-convergent expansion (the correction is
O(1); ακ = 0.45; no small parameter at κ·a = 2), so no derived ℓ
exists to collapse to; quoting 0.29 fm would ship the unstated-gap
failure the class machinery exists to prevent. C2R-OBSTRUCTED:
"either leg cannot close without inputs the ontology record does
not yet contain (anticipated dependency: if χ_static proves
inseparable from the kinetic closure, the leg names
OPEN-DM-KINETIC-1 and stops)." That is precisely what L2 found in
shape rather than strength: the statics of the ontology record fix
neither the point idealization (ℓ = 0.0904) nor the leading-order
cloud (ℓ ≈ 0.29, non-convergent) as the true per-site response
profile; the physics that picks the profile is the short-distance
per-site stiffness structure behind κ·a = 2 — the same remainder
L1's honesty bound named KINETIC-1-adjacent and underived.

**Class: C2R-OBSTRUCTED.**

## §3 — Consequences (charter §3, verbatim application)

1. **The pair STANDS under rider v2.6-A unchanged:**
   ℓ(α, continuum-matched) = 0.0904 ± 0.0028 fm (clean envelope,
   leading order licensed by 2767) | ℓ(α′, site-matched) ≈ 0.168 fm
   (premise-rejected, sensitivity bound only). Every consumer
   sentence keeps stating which.
2. **No partial number ships.** Neither 0.29 fm nor any
   profile-corrected value appears in any consumer context.
3. **The obstruction registered as OPEN-DM-KINETIC-1's concrete
   question** (gate cleared by grep; no new ID): *what is the
   profile over which a Sea site's every-Moment response to a weak
   static bias is distributed — the short-distance closure of
   χ_static — given that the point idealization and the
   leading-order Debye cloud bracket the readout at 0.09 vs
   ≈ 0.29 fm and the first-order expansion between them does not
   converge at κ·a = 2?* This question is answerable inside the
   KINETIC-1 programme (K1-S1's fluctuation-response derivation
   produces the response kernel whose spatial structure IS the
   asked-for profile) and by a chartered fixed-point iteration of
   the shape functional (explicitly de-chartered here; requires its
   own frozen prereg with convergence criteria committed in
   advance).
4. **Consumer-triage note for the panel:** L2 demonstrates the
   normalization-sensitivity axis is wider than the rider's {α, α′}
   strength pair — the response-SHAPE axis moves ℓ further (+220%)
   than the strength axis (+86%). Recommended rider motion: a
   v2.7 clause adding the shape-axis disclosure to the triage
   language, with the L3 bracket retained and the L2 finding cited
   OBS-class. No cap motion recommended; the zero-parameter
   language ban (clause 7) remains — its lifting condition was
   CONFIRM-only and did not occur.

## §4 — What the campaign delivered regardless of class

The matching is no longer a prescription: **α = κ²/(4πn) is
derived** as the unique occupied-core closure (D1 = 0, the 2767
ruling's first quantitative consequence), and the entire 1.611
site-matching factor is decomposed into a premise-rejected medium
term and a real discreteness term reconciled to the digit. The
profile sensitivity is now mapped with analytic structure (the
corrected continuum closure's complex poles — damped-oscillatory
screening — consonant with the staggered evanescent phenomenology).
And the campaign's stopping point is a REGISTERED question with a
named home rather than a soft failure: the FA-C2 value question and
the KINETIC-1 bridge question are now demonstrably the same
question seen from two sides.

## §5 — Queue effect

Per the 2771 handover queue: the FA-SG-R1 consumer motion and any
cap motion were waiting on this dispatch — they now go to the panel
with the packet. Behind adjudication returns: L2-RB re-execution
arc (spec 2766 §2b), X3/X4 preregs under the seven binding
requirements, AI_team_expectations hygiene entry (S5), AUTOMATON-1
disposition, OPEN-DM-CHARGE-1. Candidate (B) 79.5% untouched.

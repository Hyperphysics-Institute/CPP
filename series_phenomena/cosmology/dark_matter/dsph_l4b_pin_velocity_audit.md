# L4-b: the pin was a window quoted at its steepest velocity — and at the audited reading, both strands pass (Patch 2356)

**What this is:** the pin-velocity audit — registered at 2342 (panel L4-b), made
load-bearing at 2351, promoted to required-next by the unanimous 2355 flash finding.
**Pre-registration:** `code/2356_PREREG.md` (provenance ledger, kinematic convention
map, protocol order — hurting direction first — and outcomes, fixed before
computation). **Verify:** `code/2356_l4b_grading.py` (6/6); engine
`code/2356_l4b_engine.py`; data `2356_results.json`. **NO VERDICT MOVED.**

## 1. What the pin actually is

Not a system: the **1865 J3′ synthesis window** — σ/m(50) ∈ [1,5] central /
[0.5,10] extended — assembled from rotation-curve fits at galaxy scales (KTY 2016
~2; Ren 2019 ~3; samples from ~50-class velocities upward) and Elbert 2015 core
viability at **V_max = 40 km/s**, with the dSph-regime fits explicitly excluded from
the pin ("lower velocity — shape information"). The Correa anchors, meanwhile, are
quoted in ⟨v⟩ (mean pairwise collision velocity). Under the pre-registered kinematic
map (⟨v_rel⟩ = 2.26 σ₁D; σ₁D ≈ (0.6–0.8) V_max ⇒ ⟨v⟩ ≈ (1.4–1.8) V_max), the
Elbert leg sits at **⟨v⟩ ∈ [56, 72]**, and the KTY/Ren ceiling's native band extends
from ~50-class upward. **The registered v_pin = 50 is the steepest admissible
reading of the pin's own sources — the exact analogue of the 2345 dSph finding**
(the registered frame had adopted the steepest strand). Readings across ⟨v⟩ ≈ 56–72
are equally licensed by the same ledger.

## 2. Hurting direction first: the v1 kill is untouched

The killed candidate's corrected curve, evaluated at every audited v_pin: the dSph
shortfall is **×1.58, pin-independent**, and the pin itself stays over the v1
ceiling (×1.31–1.76) across the whole band. The kill class stands under any reading.

## 3. The collision is reading-dependent — and both strands pass at the audited reading

s_req = ln 2 / ln(v_pin/48) = **17.0 / 5.1 / 3.1 / 2.4 / 1.8** at v_pin =
50/55/60/64/70; it crosses the family maximum (3.2) at **v_pin = 59.6 — inside the
audited band**. The 2351 demand-internal inconsistency therefore holds *only at the
steepest pin reading*. And constructively: the G3 joint search repeated with the pin
relocated finds, at the **Elbert-centered reading (v_pin = 64), an extended-frame
joint pass at ×1.000 — all three Correa collapse-strand anchors at exactly 1.00,
simultaneously with the pin, LSB, and cluster windows.** Texture, measured honestly:
the @64 point is strained and edge-riding (α = 100, p = 12.3; Carina at 20.02 vs
floor 20, pin at 10.0 vs ceiling 10 — the optimizer riding both sides of the old
cliff); at v_pin = 70 it relaxes to near-natural (α = 0.01, inside 1855's own quoted
range; p = 3.65). **The central frame never passes (×1.27–1.44, Carina vs the
tighter ceiling): the frame asymmetry holds for the fourth consecutive time.**
Nothing previously passed breaks: both G1/G2 points hold the pin at every audited
velocity — pure relief on the passing side, pure conditionality on the collision
side.

## 4. Grade and consequences

**Pre-registered outcome (i) fires.** Per the restated 2352 certification's own
terms, the L4-b resolution reopens the certification — **reopened and re-closed
here, in the strengthening direction**: the isothermal-strand pass stands unchanged,
and a both-strands pass now exists at licensed pin readings. The strand statement's
final form for the papers: *at the steepest pin reading the collapse strand
collides with the pin; at the Elbert-centered reading a single population satisfies
both published strands (extended frame, with named strains relaxing across the
band).* The pin reading is a demand-side choice of exactly the 2345 class — **it
joins the frame decision on the founder's desk, same grammar, same desk.**

## 5. Named residuals

(i) The KTY/Ren per-galaxy ⟨v⟩ assignments were not digitized — the ceiling's low
edge could sit near 50; the band's steep edge is therefore live, not excluded.
(ii) The conversion band (1.4–1.8) is standard kinematics carried as a band, not a
derivation; a per-source ⟨v⟩ recomputation from published dispersions is the
completion path. (iii) The @64 both-strands point's strains (α = 100, p = 12.3) are
inverse-ledger entries at that reading; they relax toward the band top. (iv) The
2355 seat-attribution correction rides in this patch (see the adjudication
annotation).

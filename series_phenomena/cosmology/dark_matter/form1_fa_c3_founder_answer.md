# FA-C3 FOUNDER ANSWER — PER-UNIT-CELL (βd = d/ℓ_unit = 1.953), PROVISIONAL, UNPROMOTED — mechanism: PCD phase-localization (screening is Perceive/Compute response; the edge is Displace output)

**Patch 2662, 20 July 2026. Successor registration to FA-2/FA-C3 (Patch 2659,
`form1_a_morse_derivation.md` §2). Founder verbatim preserved at
`founders_voice/fa_c3_pcd_phase_localization_2026-07-20.md`. No new
computation; all numbers are the 2659 registered values. No fence moves; no
width promotion; quarantines carried; 79.5% untouched.**

## 1. The answer

**FA-C3-ANSWER (founder mechanism statement, PROVISIONAL):** the Sea's
polarization amplitude attenuates **per unit cell** — ℓ = ℓ_unit = 0.589 fm,
βd = d/ℓ_unit = **1.953**, ω = 1.70 c/fm, the regular-side candidate.

**Strength register:** founder mechanism statement with stated uncertainty,
at the FA-C1/FA-C2 register (structural condition, individually attackable).
NOT a derivation from the axiom set. NOT a width promotion — the FA-2 width
registration remains instrument-level at {2, 4} per 2659 ("nothing tonight
licenses a pick" stands for promotions; this registration records the
founder's mechanism-grounded identification for the panel's look, which is
what the 2661 packet item (iv) called for).

## 2. The mechanism (phase-localization)

The two candidates are readings of different phases of the PCD cycle, so
they are not symmetric alternatives:

- **Perceive + Compute (response):** vertex-centered spherical integration
  of the totality — all SSV_abs and SSV_net contributions from local
  (within l_P) and distant GPs, summed to the scalar and vector response
  quantities, once per Moment. Granularity: the **cell**.
- **Displace (output):** the executed displacement carries a de facto edge
  magnitude and direction — but this is *downstream* of the summation, an
  output property, not a response property.

The FA-C2 screening length ℓ is the decay constant of the **static**
registration amplitude f(r) — the equilibrium polarization pattern around a
pair at rest. Static screening in a gapped medium is set by the medium's
**response granularity**, i.e. Perceive/Compute — the cell. The per-edge
candidate (loss per exchange event along an edge) describes **transit
attenuation** — retardation, dynamic signal loss — which is not the
observable FA-C2 parameterizes. Screening is a response property; the edge
is an output property; therefore ℓ takes the cell step.

**Supporting (not decisive) — isotropy inheritance:** FA-C2 writes f(r) as
a function of r alone. That isotropy is produced by the holographic
summation of overlapping shells (the same summation that yields the smooth
continuum 1/r² behavior at micro/meso/macro scale). The screening length is
a parameter of that isotropized response and consistency suggests it
inherits the granularity of the summation that produced the isotropy.
Flagged supporting-only: a hop picture can be isotropized on average.

## 3. Registered consequences (all carried from 2659/2658; nothing new computed)

1. **Soft-member identity:** under this answer the registered soft bracket
   member βd = 2 is, to 2.4%, the physical width (the 2659 §2 observation
   (i), now attached to a mechanism rather than standing as coincidence —
   still registered as observation, not claim).
2. **Regular-regime prediction:** ω = 1.70 c/fm sits below the 2658
   chaos-onset window ω ∈ (2.18, 2.61) — the physical width is predicted to
   show regular-regime schedule physics (convergent schedules; no H2-class
   persistent divergence).
3. **KILL CONDITION:** H2-class persistent schedule divergence exhibited at
   the physical width falsifies FA-C3-ANSWER toward per-edge. This is the
   falsifiable fork the 2661 handover named; the founder's answer picks a
   side and thereby arms the test.

## 4. Consumers and routing

- **Packet item (iv) conversion:** the founder's next CONV-001 dispatch
  packet carries FA-C3 as answered-question-with-mechanism (this file +
  the founders_voice verbatim), dispatched alongside FA-C1/FA-C2 as an
  attackable three-condition set. Panel may attack the phase-localization
  argument directly (e.g., argue the gapped-medium decay is set by the
  exchange step).
- **DEP-1:** no row edits — the 2660 additive note stands; width revisions
  remain live via FA-C3 exactly as recorded, now with a provisional answer
  on file.
- **No consumer sentence rides.** No fence moves. w ∈ {2.5, 3} quarantine
  carried. **79.5% untouched.**

Reasoning: `reasoning/2662.md`.

---

## PANEL SUPERSESSION NOTE (Patch 2664, additive; CONV-001 returns on the 2663 packet, Q2a DEMOTE-TO-OPEN 3–2)

The strength register above is SUPERSEDED. Standing register as of 2664:
**FA-C3 OPEN FORK: per-cell provisionally hypothesized by the founder;
per-edge remains live.** Majority ground (Seats 1/3/5): the phase-localization
argument is descriptive architecture, not a quantitative discrimination — the
static response kernel's correlation length is not derived to equal the cell
scale rather than an edge or other eigenlength of the Sea. The founders_voice
verbatim is untouched (archival, per the 2663 pre-commit). The soft-member
βd = 2 coincidence is permanently NON-ADJUDICATIVE. The §3 kill condition is
superseded by the frozen discriminant **FA-C3-DISC-1** and the analytic route
**FA-SEA-GREEN** (adjudication file §5–§6). Every fork citation carries
observation-grade + quarantined-onset qualifiers. Nothing above this line is
edited.

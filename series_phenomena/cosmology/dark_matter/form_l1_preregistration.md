# OPEN-DM-FORM-L-1 pre-registration: the formation-side L-question at dance strength — E_close(L) on a pre-registered even-L grid, blindness on the 16-comparison, committed before any derivation

**Patch 2553, 18 July 2026. Status: OPEN-DM-FORM-L-1 REGISTERED and OPENED at
pre-registration; NO derivation performed.** Founder-directed ("the kinetic limb — the
route to deriving why sixteen planes"; "proceed with recommendation"). Inherits the full
discipline apparatus.

## 0. Scope resolution (why this campaign and not "the kinetic limb")

RODCLOSE-1's kinetic routes are already exhausted: 2542 executed R-B's statics-admissible
portion to emptiness ("no statics-admissible long-rod content") and recorded R-B/R-C
**Branch I, NB-S3a-1 named**. The full multivariable kinetic window (species mixture + KE
spectrum + buffeting sequence) remains the blocked NB-S3a-1 PROJECT; nothing here
trespasses on it. What ENDBOND-3 newly enables is a **rate-free formation-side
instrument**: dance-strength closure energetics as a function of rod length. The physical
hypothesis it can test without any kinetic content: tight-curvature registry compression
(28 % inner-edge at L = 16; worse at smaller L) may make short-ring closure *unfavorable*,
producing a formation-side **lower cutoff** L_min at dance strength — one side of the
window the statics could not supply. The stability side (OPEN-SS-43: N = 8 elements sole
survivor) is NOT re-litigated; C7 stands; this campaign asks the formation-side question
only. The trap-clause lineage governs: no composition reading rides on anything here.

## 1. Definitions fixed (knob-free)

**E_close(L) ≡ ⟨Ep⟩(straight rod, L planes) − ⟨Ep⟩(closed ring, L planes)** at dance_v8
strength (positive = closure pays). Pre-registered grid: **L ∈ {8, 10, 12, 14, 16, 20, 24}**
— even only (alternating parity and 2-plane element integrity both require even L; odd L
is structurally excluded, recorded as a derivation-relevant fact, not a choice). Ring_L =
the 2450 arc family at κ = 2π/(L·D); straight_L = L planes at pitch D. No fitting or
interpolation; sign changes are read off the grid table; a "cutoff" claim requires a
sign change between adjacent grid points, stable across the union. TC = 60, burn = 0.15;
primary ⟨Ep⟩, ⟨Etot⟩ disclosed. The ±2 MeV chaotic-floor rider (2551) attaches to every
difference; a sign change smaller than the floor is not a sign change.

## 2. Closed input list

1. Geometry: 2455-coded plane; alternating parity; the arc bend family (2450) for rings.
2. Functional: dance_v8 verbatim (2510; pinned κ_q = 132, κ_e = 44; kscale = 1.0).
3. Standing pre-commitments: 2521; trap clause; √5 fence; NB-S3a-1 boundary (anything
   requiring collision duration, frequency, flux, or spectrum → Branch I limb, named).
4. FENCED / NOT CONSUMED: the kT·L closure relation and kT_form band (using them here
   would make the L-question circular); the E_qq map and 102 lock (no gates in this
   campaign — the target is structure in L, not a depth); N = 16 in any privileged role
   before §5's comparison sentence (L is a grid variable throughout; the registered
   candidate value appears exactly once, at the end, in one sentence).

## 3. Convention-union axes (2541 §3 verbatim)

dt ∈ {τ_C/100, τ_C/50, τ_C/25} (the registered grid) × FREF per-L pair (K1: one constant
per compared pair = max |SSV| over {ring_L, straight_L} home scaffolds; the FREF(L) values
are disclosed). Post-hoc grid changes = Branch T.

## 4. Readings (frozen; committed before any number)

- **Sign change present** (E_close ≤ 0 at low-L grid points, > 0 above, bracket stable
  across all union cells, exceeding the floor) → **a formation-side lower cutoff
  L_min^dance exists at dance strength**; frozen as the bracketing grid interval. THEN the
  single pre-registered comparison sentence: whether L = 16 lies above the bracket.
- **No sign change, monotone-increasing** → NO formation-side lower cutoff at dance
  strength; accessibility-only (consistent with 2542's one-sided structure); recorded
  as-is — adverse for the derive-16 prize, NOT adverse for the candidate (selection may
  live in NB-S3a-1 kinetics or on the stability side, where it is already registered).
- **Interior maximum at some L** → banked as structure; promoted to a selection-flavored
  statement ONLY if the argmax is identical across all six union cells.
- **Sign change or argmax not union-stable** → UNDER-RESOLVED limb; table banks.
- Upper-cutoff claims of any kind → Branch I, NB-S3a-1 named (long-rod physics is
  kinetic; this instrument cannot see it and must say so).
- No composition reading; no T_form content; any new √5 → fence.

## 5. The comparison sentence (frozen format)

After the table and readings freeze: one sentence stating the relation of L = 16 to
whatever structure (if any) survived the union. Nothing else about 16 anywhere.

## 6. Bookkeeping

79.5 % untouched. Queue after: plane-resident-fraction limb → δ_E → MW-MODES
TC-extension; NB-S3a-1 remains the named blocked project. Next patch: 2554, under this
document only.

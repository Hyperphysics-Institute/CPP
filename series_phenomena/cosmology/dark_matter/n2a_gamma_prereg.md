# N2-A-γ pre-registration: the η γ-re-derivation — both routes re-executed under relativistic kinematics, frozen formulas, declared amplitude grid, and readings, committed before any number

**Patch 2606, 20 July 2026. Status: N2-A-γ OPENED at pre-registration; NO computation
performed.** Discharges the 2601 S2(i) disclosed rider (the η band was derived with
½mv²; a γ-re-derivation was QUEUED, consumers re-run if it moves). Governed by this
document and the N2-A prereg lineage (2594) only. Verify: none at prereg.

## 0. Target and what is at stake

The Newtonian derivation gave η ∈ [0.5, 1.0] (R-A2 bounds) with R-A1 interior estimates
0.74 (soft) / 0.68 (steep); the Newtonian gates refined to [0.5, 0.7]; the γ-gates
(2602) killed 0.7 at G2-γ, leaving **η = 0.5 as the sole γ-admitted tested point.**
This campaign re-executes BOTH analytic routes with relativistic kinematics. An
analytic landing consistent with 0.5 is the band converging twice, once per kinematics
(point-candidate status routes to a separate dispatch — nothing promotes here). A
landing away from 0.5 registers as a route-level tension, adverse-direction, and the
gates outrank the estimates (executed physics over asserted). Either outcome banks.

## 1. Registered inputs (all cited; zero freedom)

Pair potential (2582/2596 engine, verbatim): U(r) = −α_s ħc/√(r²+A_QQ²) +
E_QQ[(1−e^{−β(r−D)})²−1], β = w/D, w ∈ {2.0 soft, 4.0 steep}; ħc = 197.3, α_s =
5/(8φ), A_QQ = ħc/264, D = 1.15, E_QQ = α_s ħc/D = 66.25 [1812], m = κ_q = 132,
τ_C = 2π ħc/264. Relative motion at reduced mass μ = m/2 (convention pinned by the
Newtonian control, §2). r_eq four-decimal reference: 1.0752 (soft) / 1.1305 (steep)
per the 2599 three-way cross-validation. H-γ kinematics (2601, verbatim): v = p/√(m²+p²),
KE(p) = √(m²+p²) − m. **τ_C is the substrate Sea clock and is NOT dilated** — the sink
acts per Moment cycle in the Sea's frame; this is the same reading the H-γ instrument
executes (per-cycle application at fixed τ_C) and it is a DISCLOSED assumption of both
routes, named here so a future substrate ruling can revisit it by citation.

## 2. Controls (must pass before any γ number is read)

- **C1 (convention pin):** small-oscillation ω = √(U″(r_eq)/μ), x = ωτ_C, per-bond-period
  lag loss f(x) = 2πx/(1+x²), Moment-cycle compounding η_A1 = 1−(1−f)^{x/2π} must
  reproduce the registered 0.74/0.68 (|Δ| ≤ 0.01, both widths). The per-CP-mass
  alternative (μ = m) is printed and must FAIL the pin — the control selects the
  registered convention rather than the author. r_eq re-minimized must match the
  four-decimal references.
- **C2 (NR limit):** every γ-route formula at γ_char → 1 must reproduce its Newtonian
  counterpart (η_A1γ → η_A1 within 0.01; η_floorγ → 0.5 within 0.005).

## 3. Routes, frozen

- **R-A1-γ (relaxation face at amplitude):** exact CM two-body period over the
  registered potential: 2(γ(r)−1)m + ΔU(r) = E with ΔU = U−U(r_eq); p(r) =
  √((m+(E−ΔU)/2)² − m²); v_rel = 2p/√(m²+p²); T_γ(E) = 2∫dr/v_rel over the turning
  points; ω_γ = 2π/T_γ; x_γ = ω_γτ_C; η_A1γ(E) = 1−(1−f(x_γ))^{x_γ/2π}. The Newtonian
  ANHARMONIC period T_N(E) (½μṙ² + ΔU = E) is computed on the same grid so the
  γ-specific shift is isolated from anharmonic softening: report harmonic-NR,
  anharmonic-NR, and γ columns; the γ-effect is column 3 minus column 2.
- **R-A2-γ (half-return floor):** the mode is radial-1D; the cycle-delayed return
  arrives at decorrelated mode phase φ (uniform). Retained coherent fraction
  R(γ) = ⟨KE(p_γ|cos φ|)⟩_φ / KE(p_γ), p_γ = m√(γ²−1); **η_floorγ = 1 − R(γ).**
  NR limit ⟨cos²φ⟩ = 1/2 (C2). Saturation ceiling 1.0 unchanged (full decorrelation
  into the Sea is full decorrelation regardless of kinematics).

## 4. Declared amplitude grid (no knob)

Amplitude ↔ per-CP peak γ at the r_eq crossing: E = 2(γ_char−1)m. Grid: γ_char ∈
{1.02, 1.05, 1.1, 1.2, 1.3, 1.4, 1.45}, truncated per width at the pair binding cap
γ_cap = 1 + depth/(2m) (depth = ΔU(∞); the cap is itself reported physics — no bound
pair oscillation above it). **Scope line, disclosed:** the registered capture transits
(γ 1.68–1.90, 2604) exceed γ_cap — they are unbound/multi-bond transit motion, outside
the bound-oscillation domain this derivation covers; that regime belongs to the
capture-edge cell (the queue's item 3), not to η.

## 5. Readings (frozen)

- **R1γ — CONVERGENT:** C1+C2 pass AND η_floorγ ≤ 0.5 across the full declared grid
  (the γ-admitted point remains analytically admissible) AND the R-A1γ interior
  estimates at capture-relevant amplitudes (γ_char ≥ 1.2) move from the Newtonian
  0.68–0.74 TOWARD the admitted point or hold within the Newtonian band → the
  γ-re-derivation CONFIRMS the admitted point's analytic admissibility. If additionally
  the floor sits within 0.05 of 0.5 at the grid top, the point-candidate question
  ROUTES (separate dispatch; nothing promotes here).
- **R2γ — TENSION-EXCLUSION (adverse):** η_floorγ > 0.5 anywhere on the declared grid
  → the admitted point sits below the analytic floor; registered adverse-direction;
  gates outrank; consumers unchanged at 0.5; a named open item registers.
- **R2γ′ — TENSION-INTERIOR (adverse, weaker):** floors pass but the R-A1γ interior
  estimates land at/above the gate-excluded 0.7 and FARTHER from the admitted point
  than the Newtonian estimates → the relaxation-face route's per-cycle compounding
  overestimates the dynamically admissible shed under γ; route-level tension recorded
  (member admission untouched — members are admitted by gates, not estimates).
- **R3γ — UNDER-DETERMINED:** controls fail, or the routes cross pathologically
  (R-A1γ below the R-A2γ floor on the grid) → the Newtonian derivation
  FAILS-TO-EXTEND; the band stands as gate-refined only; recorded.
- No reading touches the N1 registration, the sink's validated scope, the H-γ member
  admission, item-5 status, or 79.5%. Consumers re-run ONLY on R2γ (and then only on
  founder word — the re-run set is named in the record, not executed).

## 6. Bookkeeping

79.5% untouched. Next patch: execution under this document only (quadratures + the
three-column table + floor curve; verify script `code/2607_n2a_gamma_eta.py`).

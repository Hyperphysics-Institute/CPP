# AUDIT-DANCE-REACH-1 — executed: the "in-plane" misclassification is universal on rings (75 % of qCPs at L = 16); ground-truth classifier shifts the promotion-class ring−straight FAVORABLY by 19–25 MeV → FIDELITY TENSION recorded (R1); the FORM-L structure is CLASSIFIER-ROBUST and the L = 24 collapse DEEPENS to a sign change (R2); classifier-faithfulness question routed to the founder

**Patch 2555, 18 July 2026. Status: audit EXECUTED under its own pre-committed readings
(module docstring, frozen above the re-runs); no verdict movement (R3 pre-commitment
honored).** Verify: `code/2555_audit_dance_reach.py` (census → pre-commitments → variant
re-runs → comparison; 157 s; all assertions pass).

## 1. Census (registered geometric classifier on ring scaffolds)

Cross-plane qCP pairs classified "in-plane" (global Δz < 0.5·D test): 68 pairs at L = 8
rising to 160 at L = 24; **at L = 16, 48 of 64 qCPs (75 %) carry at least one cross-plane
"in-plane" partner.** This behavior was present verbatim in every registered ring run
(2461 → 2510 → 2551 → 2554), including the promotion-supporting ring−straight. The eCP
rules are clean (zero cross-plane anomalies; they are distance-based by founder design
and were not varied).

## 2. R1 — promotion-class observable: FIDELITY TENSION (threshold |Δ| ≤ 5 MeV exceeded)

Ground-truth variant ("in-plane" = same plane index; axial = other-plane r < 1.3; all
other rules identical): ring−straight(16) = **−156.9 / −156.2 / −153.7** MeV (dt 1/100,
1/50, 1/25) vs registered −134.1 / −137.5 / −128.9 → **Δ = −22.8 / −18.7 / −24.8 MeV,
exceeding the pre-committed 5 MeV threshold at every dt.** Direction: **favorable to the
ring** — the registered classifier has been *understating* ring preference; the variant
band is also tighter (spread 3.2 vs 8.6 MeV). Per the pre-commitment: a dated
fidelity-disclosure line is QUEUED for the next panel dispatch; **no verdict movement in
this patch**; the registered results remain the registered results (R3).

## 3. R2 — FORM-L structure: CLASSIFIER-ROBUST; the collapse deepens

Under the variant, at every dt: L = 20 > L = 16 beyond floor persists (variant peak
+179 to +193), AND the L = 24 collapse persists and **deepens to a beyond-floor sign
change: E_close(24) = −18.8 / −12.9 / −16.8 MeV — closure does not pay at L = 24 under
the ground-truth classifier.** Per the pre-commitment: the L = 24 equilibrium
upper-cutoff question is **PROMOTABLE by a successor** (OPEN-DM-FORM-L-2, registrable:
sign-change bracket on {20, 22, 24, 26}), which would give the window its upper side
[somewhere in (20, 24)] — containing 16, still not selecting it.

## 4. The routed question (founder, per PD-006 — physics-spec authority)

Both classifiers are implementations of the founder's 2455 confirmation (1): reach =
"{2 in-plane edge + 1 in-plane diagonal + 2 axial} qCP + 1 radial eCP." The registered
code operationalized "in-plane" as a geometric proximity test (Δz < 0.5·D — written for
the straight rod, where it is exact); on rings it classifies near-orientation *cross-plane*
qCPs as in-plane partners. The ground-truth variant reads "in-plane" as plane membership.
**Question for the founder: in your specification, is a qCP's in-plane partner set defined
by membership in its own element plane (the variant), or by geometric adjacency in the
local neighborhood regardless of plane identity (the registered behavior)?** The answer
determines whether a re-registration campaign (with panel notice — this touches the
promotion-class observable, favorably but materially) or a ratification of the registered
behavior follows. FORM-L-2 waits on this adjudication rather than running under a disputed
classifier.

## 5. Bookkeeping

79.5 % untouched (R3). Standing disclosure package now carries: S1 charge-ADM adverse;
T_form conditionality; ENDBOND-1/-2 Branch-I lines; the ENDBOND-3 banked pin + 2542
collapse; FORM-L-1 adverse-for-prize; **this fidelity tension (favorable direction)**;
Copilot probation screen #2. The package is heavy enough that the next dispatch should be
soon regardless of the classifier answer. Queue: founder classifier adjudication →
FORM-L-2 → plane-resident-fraction limb → δ_E → MW-MODES TC-extension. Next patch: 2556.

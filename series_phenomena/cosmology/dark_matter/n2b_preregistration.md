# N2-B pre-registration: seeded accretion on the registered sink — capture probability, the growth chain, the basin re-runs, and the rate band, committed before any derivation

**Patch 2598, 20 July 2026. Status: N2-B OPENED at pre-registration; NO derivation performed.**
Governed by the N2 charter (2593), the sink registration (2596: η ∈ [0.5, 0.7] banded,
C-SR-exact, exact ledger), and this document only. **Verify: none at prereg.** Collision
check clean.

## 0. Standing frame

Instrument: the `n1_sink` engine (atH admissible member; exact-ledger sink; Sea-deposit
mandatory). **Band-carried per R2:** every cell runs at η ∈ {0.5, 0.7} (both admissible
edges); a claim holds only if its verdict is η-stable, else reported per-η. Widths union
β·d ∈ {2, 4}; dt ∈ {1/200, 1/100} with 1/50 spot-checks on claimed cells; declared seeds
{20260720, 20260721, 20260722}; TC = 120 per cell unless stated. **Relativity handling,
frozen:** any cell firing the 0.9c assert reads UNRES-REL — banked as a count, never as a
verdict; the γ-corrected kinematics member is NOT registered here (it is its own rider on
the founder's word), and no UNRES-REL cell may be re-run under any new member without that
rider. The certified soft channel (2596 G3) is this campaign's working channel.

## 1. B1 — capture probability vs approach (the σ_cap grid)

**Cells:** one incident qCP (charge −1, the registered 5th-CP identity) approaching the
settled 4-square along −ẑ from initial distance 4D, impact parameter b ∈ {0, 0.5, 1.0}·D,
approach speed v ∈ {0.05, 0.10, 0.20, 0.30}c; full standing unions. **Outcome classifiers
(frozen):** CAPTURED = over the final window the 5-body max centroid distance is bounded
(< 3·D) with the incident CP's energy deficit banked in the Sea ledger; SCATTERED = the
incident CP's centroid distance exceeds 4D at end with positive radial velocity, the
4-square surviving (its own HOLD classifier); FRAGMENTED = the square loses any member
(that CP exceeding the SCATTERED criterion); UNRES-REL per §0. **Readings:** the (b, v)
capture map banks as-is; a capture edge in v (fast flybys escape) and in b (grazing misses)
is EXPECTED physics, not adverse; total FRAGMENTED > CAPTURED at low v would be the adverse
surprise and routes.

## 2. B2 — the growth chain (4 → 5 → 6 → 8)

**Protocol:** each stage initializes from the PREVIOUS stage's settled final state (homes
and velocities), plus one new incident CP at the best-capturing (b, v) cell from B1
(chosen by the B1 map, cited — a declared data-driven choice, not tuning: the chain
question is "CAN it grow," and the favorable channel is the honest test of CAN);
alternating incident charges to preserve neutrality (−, +, −, + for stages 5, 6, 7, 8).
Stage verdict = the grown cluster's HOLD classifier (reference-anchored to ITS OWN settled
geometry) + ledger closure. **Readings:** the chain reaching a stable 8-qCP two-layer
object = the campaign's win-candidate (dispatch AFTER B4 per the economy); the chain
stalling at a stage = that stage's physics banked (a shell/plateau boundary is N3-relevant
structure, not failure); FRAGMENTED cascades route.

## 3. B3 — the basin re-runs (the charter's promise)

The 2592 B-4 cells re-run under the sink: expansion ×{1.5, 2.0, 3.0}, compression ×{0.5,
0.7}, random-in-box (3 seeds) — reference-anchored classifier as at 2592, now with
deposits ledger-balanced against released well depth (closure within dt-convergent
tolerance). **Reading:** convergence expected; any non-convergent family is a real basin
boundary, banked; ledger imbalance anywhere = instrument halt before any physics word.

## 4. B4 — the rate band (feeds N3; fenced)

From the B1 map: σ_cap(v) via the standard impact-parameter integral over the captured
cells (the grid's coarseness carried as an explicit band, not interpolated away);
⟨σ_cap·v⟩ against a DECLARED ambient band (number density and velocity dispersion as
BANDS, cited to the registered epoch inputs where they exist and labeled UNREGISTERED-BAND
where they do not — the 2523 lesson verbatim). **Output: the capture-rate BAND, an N3
input.** FENCED: no relic-abundance numbers, no T1 contact, no cosmological point claims —
one step of inference past the grid and this section stops.

## 5. Readings summary and bookkeeping

Equal-status throughout; win-candidate dispatch only after B1–B4 complete. Nothing here
touches the N1 registration, the sink band, item 5's status, or 79.5%. Next patch: the
N2-B execution, B1 first, under this document only.

---

## Appendix (governance rider, same patch): replication seat-substitution ruling

The 2595 contract named two seats; DeepSeek's return failed at artifact level (2597) and
its corrected return is pending; the Isak leg is optional and its operator is presently
unavailable. **Ruling (PD-006): the contract's SUBSTANCE is two executed independent
implementations. A FRESH seat given the corrected spec (the in-repo dispatch block, which
now carries the w² erratum fix and the displaced-pair gate cell) is an admissible second
implementation of equal or greater independence than a corrected resubmission.
AUTHORIZED completion paths, any one of which closes item 5 alongside Seat-GPT's banked
replication: (a) DeepSeek's corrected return; (b) a fresh third seat — RECOMMENDED: Grok,
the condition's author (paste the corrected spec block FRESH, with no defect-notice
context and no mention of the other seats' results); (c) both, if both arrive — surplus
independence banks. The Isak hardware leg remains optional-strengthening, never gating,
runnable whenever he is available.** This amendment preserves the two-implementation
requirement exactly; only the seat NAME is substitutable, and the substitution is
disclosed here before any new paste occurs.

# FA-C2 CONSUMER RIDER v2.6 — ENACTED per the FA-SG-R1 R1-SHIFT returns adjudication (Patch 2766, unanimous Q4): supersedes rider v2.5 by INCORPORATING it whole and ADDING the normalization-conditionality clauses; the 2664 rider and the Gemini width clause remain in force, unrelaxed (5–0)

**Patch 2766, 22 July 2026. Authority: five-seat FA-SG-R1 returns,
Q4 unanimous (cap stays; rider tightens). All v2.5 text remains
binding; the clauses below ADD to it.**

## Added clauses (R1-SHIFT consequences)

5. **Normalization conditionality (within the proxy branch).** The
   FG-OTHER-LATTICE readout is a normalization-conditional PAIR, not
   a number: **ℓ_proxy(cont-matched) = 0.0904 ± 0.0028 fm** (frozen
   continuum matching α·S_cont = 1; clean exponential envelope,
   R² 0.88–0.97) and **ℓ_proxy(site-matched) ≈ 0.168 fm** (α′·S_disc
   = 1; DIAGNOSTIC GRADE ONLY — degraded envelope, R² ≈ 0.55). Every
   consumer sentence states WHICH normalization it uses and quotes
   the corresponding value or the pair. No cross-normalization
   inference is licensed. The α^(−1/2) propagation law is FALSIFIED
   at the operating regime (L4 α-scan) and may not be used.

6. **Consumer triage by sensitivity (GPT text, enacted):**
   (i) quantities with exponential or threshold sensitivity to ℓ →
   QUARANTINED pending normalization resolution; (ii) polynomial
   width dependence → reported only as a conditional range spanning
   the pair; (iii) qualitative dependence on finite-range screening
   only → may survive with the numerical value removed.

7. **Zero-parameter language ban:** no downstream quantity may be
   described as a zero-parameter numerical prediction if it
   materially depends on ℓ, until the physical normalization is
   derived.

8. **Resolution path (binding on FA-C2):** the FA-C2 re-derivation
   campaign's decisive deliverable is the missing derivation
   **DP response mechanics → χ_static → α** (the physical on-site
   static susceptibility), which selects the normalization and
   collapses the pair; the founder physics question that gates the
   59%-self-exclusion premise (is the r < a region occupied by
   responding Sea medium, or excluded?) is registered at the 2766
   adjudication §3 and routed under PD-006.

## Class prose amendment (GPT, adopted at Q1)

R1-SHIFT establishes a normalization-SENSITIVE numerical result; it
does NOT establish that the site-normalized ≈0.168 fm branch is the
correct revised value. The L1/L3 structural finding is scoped
precisely: arena-independent WITHIN the dense 1/r multiple-scattering
assembly (not across operator assemblies — L5's J6-DIVERGE). The
joint band is an instrument/construction ROBUSTNESS ENVELOPE, not a
statistical confidence interval.

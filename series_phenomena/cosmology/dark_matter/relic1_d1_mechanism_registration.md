# OPEN-DM-RELIC-1 — D1 mechanism registration (FI-RELIC-3): the shell-seed catalytic trap, with the m-computation pre-registration

**Patch 2524, 16 July 2026. Status: D1 mechanism REGISTERED with founder provenance (third note, 16 Jul);
m-computation pre-registered; NO computation performed.** **Verify:** `code/2524_d1_prereg.py`.

## 1. FI-RELIC-3 — the founder's nucleation picture (formalized)

**(a) Cloud structure = spherical shell.** The free −qCPs are mutually repulsive (−2/3 each) and aggregate on
a spherical shell; the shell's population k is set by the equilibrium of thermal-collision dispersal,
strong-force attraction, and charge repulsion; the TOTAL −qCP inventory is closure-forced (n_b, Patch 2519),
so the shell count is N_shell = n_b/k.
**(b) Seeding = environmental catalysis (the trap).** By shell symmetry the interior SSV *gradient* is zero
(no net force), while the interior **SSV_abs is elevated** (it is the always-positive scalar SUM of signal
magnitudes — registered, programme_orientation §SSV). Elevated SSV_abs shortens the maximum displacement per
Moment — "this IS time dilation" (registered verbatim, SR-1 / Founders Vision §12). Feedstock (qDPs, hTetras)
entering the interior is therefore slowed toward rest relative to the Sea's thermal jitter: it is impacted
more times per transit, and its collision-energy distribution NARROWS (no run-from/run-into Doppler spread),
raising the fraction of threshold-adequate assembly collisions (the 4th-addition step, 1855–1856). Rings
assemble INSIDE the vessel; **the seed is CATALYTIC, not consumed** — the shell's qCPs never join the ring.

## 2. Consistency checks against the registry (all pass at composition level)

1. **Registered-machinery composition:** every element of the trap is registered structure — SSV_abs
   additivity and its clock-rate role (SR-1); gradient-sourced force ⇒ symmetric-shell interior null (c05,
   per the R2 record); the assembly threshold (1855–1856). FI-RELIC-3 composes them; it does not add axioms.
2. **Charge consistency:** catalytic fate preserves ring (0,0) exactly — the question Patch 2523 posed is
   answered without bookkeeping violence. The closure ledger is untouched: the shells PERSIST.
3. **SG-1 SHARPENED, not discharged:** persistent charged shells at −(2/3)k each survive to the present.
   The registered discharge candidate is Debye screening (0757: the Sea screens single-hadron color at ~1 fm;
   V(r) = (E_c·r_c/r)·exp(−r/R_scr), anchors E_c ~ 0.3 MeV, R_scr ~ 15–30 fm). Flagged for SG-1's own
   evaluation; NOT evaluated here. (Naming disambiguation, recorded: R_scr = the 0757 screening length;
   R_ring = the OPEN-SS-43 ring radius. The corpus uses "R_s" for both; campaign documents use the
   disambiguated forms from here on.)
4. **The founder's inventory statement** ("their total number... determined by the amount of baryonic mass")
   is exactly closure (2519) — independent re-derivation by the founder, recorded as convergence.

## 3. Sub-mechanisms — BOTH named now (anti-switching lock)

- **SM-A (primary, founder-specified): the trap** — rings per shell set by vessel capacity (interior volume,
  dilation depth, assembly rate, epoch duration).
- **SM-B (named alternative): direct capture** — the 2522 criterion's original attractor logic (qDP chains
  pulled onto the seed by screened color attraction; growth templated at the shell surface).
SM-A is evaluated first; SM-B is evaluated ONLY if SM-A fails its gate (Q-m2). Blending or switching after
seeing outputs = Branch T. If both fail → D1 lands K1-direction → D3 (calibrated incumbent).

## 4. The m-computation pre-registration (Q-m1, Q-m2, readings)

Under the shell reading, T1 transposes once more: n_ring/n_b = m_shell/k, so the target is
**m_shell/k = 0.4468 ± 0.0054 rings per shell-qCP.**
- **Q-m1 (k-cancellation):** if vessel capacity scales m_shell = c·k^p, then k-independence ⇔ **p = 1**, and
  the pure structural target becomes **c = 0.4468 rings per shell-qCP** — parameter-freeness restored at
  depth. If p ≠ 1, k rides in and must itself be derived from the registered equilibrium (inputs exist:
  E_qq window 40–170 MeV, α, kT_form ≈ 16.5 keV, the 0757 screening anchors) or the reading is Branch I.
- **Q-m2 (trap-magnitude gate — the make-or-break, stated adversely up front):** compute the interior
  SSV_abs elevation from k shell qCPs at equilibrium radius and the implied dilation factor, from registered
  signal magnitudes. **Kill reading: if the slow-down is negligible against Sea thermal jitter at registered
  magnitudes, SM-A FAILS** (the trap doesn't trap) → SM-B evaluated on its own pre-registered terms. The
  worker's concern is recorded honestly: dilation from a fm-scale cluster is, in familiar gravitational
  regimes, minuscule; whether CPP's fm-scale SSV magnitudes behave differently is exactly what the gate
  decides, and no qualitative appeal substitutes for the number.
- **Readings:** the computed m_shell/k band (all registered windows propagated) against the frozen branches —
  D-strong [0.436, 0.458]; D-directional [0.30, 0.67]; outside → K1-direction. OBS-RELIC-1 fence restated:
  shell equilibria on a φ-lattice may naturally emit √5 factors; if one appears, the derivation's step-by-step
  independence from the fenced observation must be demonstrable in the record.

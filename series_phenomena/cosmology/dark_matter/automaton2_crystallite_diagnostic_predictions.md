# CRYSTALLITE DIAGNOSTIC CAMPAIGN — PREDICTIONS FROZEN BEFORE EXECUTION (Patch 2809)

**Frozen 2026-07-25 at founder direction: hold the win packet, gather
crystallite/sheet data first, decide whether the discrepancy lies in
the calculation model, the limits, the expectations, or the physics.
Exploratory characterization — NO verdict class, NO promotion
consequence. Predictions frozen so the diagnosis is testable rather
than narrated.**

**The founder's stated physics (the thing under test):** at current
STP, thermal collision energy in the DP Sea is sufficient to
dissociate chains/planes/blobs as fast as they form; occasional
species appear but their numbers do not grow; the result is a
formation–dissolution EQUILIBRIUM with a small stable population.

**The automaton's observation:** zero turnover. Droplets exactly
static (sizes and membership invariant over 4000 Moments); no
formation events; no dissolution events; ~97 additionally parked
singletons.

**Worker hypothesis H-SCALE (the primary suspicion):** the automaton
is being run in a regime with NO SCALE SEPARATION between the PSR and
the inter-CP spacing. At R = 3 and fill 1/32, a CP's Planck sphere
contains ≈ 3–4 other CPs — so each CP responds essentially to its
nearest partner alone, producing integrable two-body orbits (the
observed 100% fidelity, period 10–12) rather than many-body chaos.
The founder's physics requires a THERMAL TAIL to dissociate
structures; a thermal tail requires deterministic chaos; chaos
requires many-body field frustration, i.e. PSR ≫ spacing. The
framework's real Sea has that separation by orders of magnitude; our
lattice has PSR/spacing ≈ 1.5. On this hypothesis the founder's
physics is not contradicted — it is UNTESTED by a simulation in the
wrong regime.

**Secondary hypothesis H-FLOOR:** displacement quantization
(nearest-GP snap of d = (|SSV_net|/SSV_abs)·R) creates absorbing
states: wherever the field cancels by local ± symmetry (droplet
interiors; isolated CPs), d < ½ GP rounds to exactly zero motion
forever. In a continuum the same residual force still accelerates.
This makes the condensed phase kinetically frozen for a reason that
is discretization, not physics.

## Frozen predictions

- **P-X1 (floor census):** > 80% of static CPs (droplet members and
  parked singletons) have committed displacement d < 0.5 GP — i.e.
  they are rounding-limited, not force-free. Falsified if most
  static CPs have d ≈ 0.
- **P-X2 (metastability, perturbation test):** displacing one
  droplet CP by hand does NOT heal (the droplet does not re-absorb
  it and does not unravel) — the structure is KINETICALLY TRAPPED,
  not thermodynamically stable. Falsified if the droplet either
  repairs itself or dissolves.
- **P-X3 (sub-GP accumulation, diagnostic variant — declared a
  DOCTRINE DEVIATION run for diagnosis only, not a rule change):**
  accumulating sub-GP displacement instead of truncating it produces
  NONZERO dissolution events in droplets. Falsified if droplets stay
  static under accumulation.
- **P-X4 (the scale test — primary):** raising R from 3 to ≥ 6 at
  fixed fill (many CPs per PSR) INCREASES mover fraction, DECREASES
  max cluster size, and DROPS partner persistence below 100% —
  because CPs then respond to many-partner summed fields
  (frustration) instead of a single nearest attractor.
- **P-X5 (turnover appears with scale):** formation/dissolution
  event rates are exactly zero at R = 3 and NONZERO at R ≥ 6,
  yielding the founder's formation–dissolution equilibrium rather
  than frozen structures.

Diagnosis rule frozen in advance: P-X4/P-X5 confirmed ⇒ the fault is
MODEL REGIME (worker's lattice parameters), founder physics
untouched. P-X4/P-X5 falsified with P-X1/P-X3 confirmed ⇒ the fault
is the DISCRETIZATION FLOOR. All four falsified ⇒ the discrepancy is
substantive and the founder's dissociation mechanism needs a source
the bare rule lacks.

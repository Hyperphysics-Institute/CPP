# OBL-CC-2 — THE COEFFICIENT DERIVATION PLAN (FROZEN) + STAGE 1 EXECUTED: the holographic 1/R² DERIVED as the soft-truncation deficit of the paired Sea's zero-point arrival content; the last number localized in substrate constants

**Patch 3065 (11 Aug 2026). The reproducible plan CONV-018 OBL-CC-2
requires, frozen BEFORE the guarded stages run. Rules (enacted at
3062/3063, binding): the coefficient derives from the substrate
forward; NO H, R_h, w, or observed expansion history enters any
stage before the freeze; the full theory curve (w(z), H(z),
distances) freezes BEFORE the full-shape confrontation; every
judgment call is written down before its computation. Verify (stage
1): `scripts/3065_truncation_structure.py` (PASS), with TWO
instructive failures retained on the record.**

---

## §1 — Stage 1 (EXECUTED): the structure theorem

**Object:** the vacuum residual at a GP = the DEFICIT of its
zero-point arrival content relative to the complete-universe ground
state (Step B's excess-above-ground, signed as a deficit; the
arrival-domain construction of 3064 supplies the domain).

**Two failures, kept:** (F-a) the per-source quadratic sum is
correlation-BLIND — it cannot distinguish a paired from an unpaired
Sea (measured: identical scalings); (F-b) the total net-field
variance is UV-dominated and masks the truncation dependence
(measured: near-flat). The correct observable is the **deficit**:
the difference between complete and truncated/degraded arrival
content.

**Results (numerical, paired-dipole Sea toy):**
- SHARP severing (the missing tail beyond R): deficit ~ R^−3.6 —
  SUBLEADING.
- **SOFT degradation** (every interior arrival weakened by the
  summed-increment redshift, toy weight W² = 1 − r²/R²): deficit
  **~ R^−2.00** — the HOLOGRAPHIC scaling, carried by the
  expansion's own kinematics acting on the WHOLE interior, with the
  integral **UV-ANCHORED at the pair scale** (∫ dr/r² from δ).
- Energy density = ⟨deficit⟩/(8π): Step C's 1/8π is the exact
  field-energy coefficient, now attached to a computable object.

**Stage-1 theorem (structure):** for a locally-paired (cancelling)
Sea, ρ_residual = [I_prof · n q² δ / (8π)] · (1/R_h²), where I_prof
is a pure profile integral and (n, δ, q²) are substrate constants —
the entire remaining freedom, substrate-internal by construction.

## §2 — Stage 2 (C-PROF): the exact profile integral

Replace the dS toy weight with the FRW arrival-degradation profile
DERIVED from the founder's summed-increments kinematics + the
registered per-Moment displacement law: the weight of a source at
comoving r in the ongoing arrival sum of a GP, in the self-consistent
background (the HDE dynamics supply a(t) symbolically — no observed
numbers). Deliverable: I_prof as a closed-form or quadrature NUMBER
with its derivation; sensitivity of I_prof to the profile's UV/IR
conventions stated and frozen.

## §3 — Stage 3 (C-SUB): the substrate constants, engine-anchored

(n, δ, q²) from the calibrated substrate chain: δ = the DP internal
separation (engine D0 lineage); n = the Sea number density (lattice
scale); q² = the per-arrival imprint normalization, MEASURED
STATICALLY in the committed 2902 engine (protocol: fixed small
geometry, single source, imprint amplitude vs distance — a
substrate-internal instrument reading, not sky data). The same
measurement normalizes **M-ZBW-REBOUND's** per-Moment step bias —
one measurement, both mechanisms — and the rebound's bootstrap
(residual → outward bias → acceleration → finite R_h → residual) is
computed as the BRANCH-SELECTION instability, GPT's Q2 target.

## §4 — Stage 4 (ASSEMBLY + FREEZE + CONFRONTATION)

K = I_prof · n q² δ/(8π) assembled FORWARD; ρ_Λ = K/R_h²; the full
theory curve (w(z), H(z), distances) computed from the Step-D
dynamics with K fixed, and FROZEN in a preregistration patch; ONLY
THEN the full-shape confrontation (the F-CLI-1 trigger). No CPL
refit substitutes. If K lands outside the viable band, F-CLI-1
FIRES and the record says so in those words.

## §5 — Sequencing

Stage 2 and the Stage-3 engine protocol: next CC-lane patches
(engine measurement may ride Kila6 post-campaign or ClearPC — it is
small). Assembly only after both freeze. The DM campaign remains
untouched and independent.

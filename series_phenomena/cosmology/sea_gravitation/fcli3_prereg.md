# PREREGISTRATION — F-CLI-3: THE POINT CONFRONTATION AT THE FROZEN SPACING (committed BEFORE the input measurement; disclosures complete)

**Patch 3123 (13 Aug 2026). All Q6 gates discharged (3121, 3122).
Per the successor protocol: fresh prereg, inputs measured at the
frozen spacing, the fold verbatim, the same band, the falsifier in
the same words.**

## §1 — Why new inputs

The 3111 inputs were measured at d_s = 8 and 12. The Sea sits at
the FROZEN d_s* = 2.450 ± 0.091 (3121), and the excursion
statistics are strongly spacing-dependent. The confrontation
therefore uses the 3104 §3 read-out definitions VERBATIM, measured
AT the frozen spacing, at the largest FSS-completed size.

**DISCLOSURE (volunteered):** the 3122 scheme-sensitivity test
incidentally exposed n = 3 read-outs at d_s = 2.45 (r_e ≈ 0.73,
x_q ≈ 0.48, η_gas ≈ 2.13, η_e ≈ 0.143, s ≈ 1.01). This prereg is
written in knowledge of those numbers. Protections: every element
of the fold and verdict below was frozen in prior patches
(3107/3112/3113 lineage); the confrontation inputs come from n = 5
(not n = 3), both seeds; and no free choice below can move the
verdict — the cells, definitions, and rule admit no tuning.

## §2 — The input measurement

Instrument: the v2 memoryless dynamics with the lagged arc scheme
(the campaign scheme), species read-outs added (`scripts/
3124_fcli3_inputs.py`, the 3122 test's certified run_cell).
Cells: n_side = 5, d_s ∈ {2.359, 2.450, 2.541} (the frozen value
and its ± edge), seeds {5, 11}. T = 3000. Central inputs = the
seed-mean at d_s = 2.450; the ± cells feed the residual band.

## §3 — The fold and verdict (frozen words)

c_Li = √[(4/3)·C₄′·α·Φ]·(l_P/d_s*), Φ = (η̄_e + C·η̄_q)/4, the
3107/3112 mappings verbatim; **C = (1+k)² = 68.49** (D-CHAN-ADD,
derived — no fork); k = √(α_s/α) (ratified); **central: a = 1.0
(3114 precedent), d = 2.450**. RESIDUAL BAND (reported around the
central, never a stay): input seed-SEs and the ±edge cells ×
arrangement [0.78, 1.35] × α_s ± 10% (OBL-K-SENS) × the ARC-SCHEME
shifts (≤ 0.034 per observable, 3122) × d_s ± 0.091. BAND, frozen
since 3060: c_Li ∈ [0.6, 0.9]. **VERDICT RULE: central c_Li ∈
[0.6, 0.9] ⇒ F-CLI-3: PASS (bracketed by the residual band);
outside in either direction ⇒ F-CLI-3: FIRES (direction stated),
in those words. Overshoot fires exactly as shortfall.** No
post-hoc anything; the result and this prereg go to the panel
together.

# D-DS-INDEP CAMPAIGN COMPLETE — THE FROZEN OUTPUT: d_s* = 2.450 ± 0.091 l_P (spread 0.182 — NO SPLIT); two of four order parameters extrapolate and CONVERGE (f_b → 2.541, f_dwell → 2.359); the coincidence report is maximal vindication of the panel: NEITHER prior locator survives at larger sizes — the FAITHFUL phase itself recedes with N and the r = 0.5 crossing exits the grid; implementation v2 declared (55× vectorization, cross-implementation differences ≤ seed spread); n = 6 partial data retained as the declared POST-FREEZE consistency check

**Patch 3121 (13 Aug 2026). Completes `ds_indep_prereg.md` (3119)
under its frozen rules. Verify: `scripts/3120_ds_indep_campaign.py`
(v2) + raw state `scripts/3121_state_v2.json` (v1 part-1 state
retained in `scripts/3120_state_part1.json`). GOVERNING CONSTRAINT
HONORED: no cosmological quantity appears in this campaign, this
record, or its scripts. The output number below is hereby FROZEN.**

## §1 — Implementation v2 (declared)

The retarded-partner loop was vectorized (identical per-element
arithmetic; 55× speedup), making the full ladder — including the
Kila6-queued n = 6 — container-feasible, and leaving Kila6 wholly
dedicated to Route C. v1-vs-v2 outputs differ at ≤ the seed spread
(chaotic ulp-divergence class, both state files retained); the
entire campaign was re-run UNIFORMLY under v2 with the declared
downward grid extension ({1.5 … 5.0}) and both seeds at every
completed size — exceeding the prereg minimums everywhere.

## §2 — Completed sizes and peaks (frozen §4 rule)

n = 3, 4, 5 complete (both seeds; seed agreement ≤ 0.005); n = 6
partial (2 cells — excluded from extrapolation by the completed-
sizes rule; see §5).

| parameter | d*(n=3) | d*(n=4) | d*(n=5) | d*(∞) (linear in 1/n) |
|---|---|---|---|---|
| f_b | 3.42-class | 3.338 | 3.106 | **2.541** |
| f_dwell | ~2.4 (bracketed by the 1.5 extension) | 2.388 | 2.338 | **2.359** |
| ρ_swap | 3.08 | 4.60 | UNBRACKETED | insufficient (dropped per rule) |
| ξ_state | defined-sporadic | 2.99 | UNDEF cells | insufficient (dropped per rule) |

**CAMPAIGN OUTPUT (frozen): d_s* = 2.450 ± 0.091 l_P** (mean of the
defined extrapolations; spread 0.182 ≪ the 0.75 SPLIT threshold).
Notable: the two defined parameters — which at small sizes appeared
to track DIFFERENT transitions — CONVERGE under extrapolation
(2.54 vs 2.36): the apparent transition family collapses toward one
boundary in the large-array limit.

## §3 — Coincidence report (§5; reporting only)

Neither prior locator survives at scale, in this campaign's
declared all-species statistics: the r = 0.5 crossing sits ABOVE
the entire grid at every completed size (r ≥ 0.54 even at d_s = 5),
and the FAITHFUL phase itself recedes with N (present at n = 3
large spacings; ABSENT everywhere at n = 4 and n = 5 under the
frozen classifier). Definitional caveat stated: this campaign's r
is all-species (the physical weave), while the 3114 locator ran on
e-class statistics — the comparison therefore indicts the
locators' small-array, definition-dependent character rather than
re-measuring them; that is precisely the panel's Q1 diagnosis,
vindicated. The old d_s* = 4.25 and the old edge 3.0–3.5 were both
artifacts of size and convention; the substrate-selected transition
extrapolates to 2.45.

## §4 — Honest residuals

(i) Two of four parameters carried the extrapolation (the other two
dropped by the frozen insufficiency rule — not by choice); (ii) the
n = 5 d_s = 2.0 cell shows an anomalous dip (f_b 0.232, ρ 0.459)
and n = 6 shows it stronger (f_b 0.186 at 2.0) — the transition
SHARPENS with size exactly where the extrapolation lands, consistent
with 2.45 but flagged as the region where n = 6 completion matters
most; (iii) T = 3000 stationarity holds (stat ≈ 1.00 everywhere)
but critical slowing near the transition is untested.

## §5 — Post-freeze consistency check (declared NOW)

n = 6 completion (± n = 7) runs as a CONSISTENCY CHECK against the
frozen 2.450 ± 0.091: it may confirm or challenge, and a challenge
goes to the panel; it CANNOT revise the frozen number silently.
Venue: container time or Kila6 after Route C, whichever arrives
first.

## §6 — What remains before any fold (Q6 gates, unchanged)

D-CHAN-ADD (the channel addition rule) and OBL-ARC-FIELD (the
arc-force field-state exposition), then a FRESH preregistered point
confrontation under the successor protocol. No fold occurs here.
Kila6 Route C and the DM ledger untouched; arrival still trumps all.

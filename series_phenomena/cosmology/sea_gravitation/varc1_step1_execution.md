# **VARC-1 STEP 1 EXECUTED — F1 FAILED: a rising speed of light on the coasting expansion mimics DECELERATION, not acceleration, with q₀_app = +t₀ċ(t₀)/c(t₀) ≥ 0 exactly against the frozen falsifier (q₀_app ≥ 0 ⇒ dead). The varying-c dark-energy route is DEAD, killed by its own arithmetic: the redshift map is a c-independent theorem (R1: spectrograph z = t₀/t_e exactly), so all the c-history sits in the photon-sphere radius — and slower past light SPREADS LESS, making the past look BRIGHTER and CLOSER, not farther. The founder's "takes longer to get here ⇒ appears farther" fails at its last step: travel time is not apparent distance. No rescue exists inside the mechanism: acceleration-mimicry requires FALLING c, which requires rising SSV_abs, which requires heavy-species production now — excluded by the mechanism's own relic definition and the lensing-calibrated sign**

**Patch 3430 (26 Aug 2026). DE lane; ID 3430 claimed as the DE lane's
own 3429 reservation (DMDE.md pointer; `next_id.py`'s 3431 is its
conservative refusal to attribute reservations — a lane claiming its
own reserved pointer number is the 3500 rule's intended workflow,
recorded here as the interpretive precedent). Analytic + verify
script; zero machine time. Status in `frontier_sectors/DMDE.md`.**

## §1 — Framework (all inputs previously frozen or founder-ruled)

Fixed absolute scaffold; comoving DP-Sea flow v(r,t) = r/t (coasting,
H = 1/t); light propagates at c(t) relative to the LOCAL Sea (3423
lensing-channel ruling); c(t) rising monotonically (fossil depletion,
3421 §1b); founder postulate (3429 §6.1): local physics invariant —
atomic clock rate scales exactly with local c ⟺ emitted quantum
invariant. Photon ODE (radial, inward): dr/dt = r/t − c(t), giving
d(r/t)/dt = −c(t)/t. Observer at origin; source comoving at speed
v_s; the reachability relation is v_s = ∫_{t_e}^{t₀} c(t)/t dt.

**Ruler-invariance corollary (new, from the founder's postulate):**
the local light-second is c(t)·[K/c(t)] = K, epoch-invariant. Under
quantum-invariance, LENGTH standards are absolute across epochs;
only clocks co-vary with c. Areas and distances are unambiguous.

## §2 — R1: the redshift map is a theorem, independent of c(t)

Wave-crest calculation. Successive crests, source fixed v_s:
δt₀/δt_e = c(t_e)t₀ / [t_e c(t₀)] (varying the reachability
integral's limits). Emission period in absolute Moments: K/c(t_e)
(clock postulate). Received period in absolute Moments:
K t₀/[t_e c(t₀)]. The observer's spectrograph measures this against
its OWN local period K/c(t₀):

**1 + z = t₀/t_e exactly — every c-factor cancels.** (†)

This is the founder-confirmed C2×C3 cancellation emerging from the
full calculation, now with the precision the first verify run forced:
(†) holds for spectrograph-measured z (local clock units); the
absolute-frame crest interval alone retains a c(t₀) factor. Corollary
carried from 3429: light-curve stretching = (1+z) exactly (the same
bookkeeping applied to arrival intervals of a physical-clock process).
**Consequences: Step 0 stands discharged; and the z ↔ emission-epoch
map is IDENTICAL to constant-c coasting — the c-history cannot hide
in the redshift.**

## §3 — C8 resolved before use (per prereg §4)

By the quantum-invariance postulate: photons per local tick and
energy per photon are invariant; the candle's absolute-frame emission
rate scales as c(t_e). In observer-local units the flux factors then
assemble exactly as in the constant-c case: arrival rate ∝ 1/(1+z),
received energy per photon ∝ 1/(1+z) — all c(t₀) factors cancel in
local units. C8 contributes NO epoch anomaly. Recorded here, before
the distance result uses it.

## §4 — D1: the luminosity distance, and where the c-history lives

Photon-number conservation (C4) over the reception sphere. The burst
sphere is centred on the source's comoving position; its radius at
reception equals v_s t₀ (shown by solving the outward ODE in the
source's Galilean comoving frame — the sphere reaches the observer
exactly at t₀, closing the geometry). With §3's flux factors:

**d_L(z) = (1+z) · t₀ · ∫_{t_e}^{t₀} c(t) dt/t,  t_e = t₀/(1+z).**

Constant-c control: d_L = (1+z) t₀ c(t₀) ln(1+z) = c(t₀)t₀[z + z²/2
+ O(z³)] — coasting, q₀ = 0, agreeing with SR Milne at O(z²), the
order where q₀ lives (framework caveat, §7).

## §5 — S1 and Q1: the sign, exact and perturbative

**Exact, all z:** d_L^varc(z)/d_L^const(z) = ⟨c⟩_ln/c(t₀) < 1
strictly, monotone decreasing in z, for ANY monotonically rising
c(t): deeper flights sample slower epochs. **Sources are BRIGHTER at
every z than constant-c coasting predicts, increasingly so with
depth. This is the deceleration direction.**

**Perturbative:** with ε ≡ t₀ ċ(t₀)/c(t₀) ≥ 0, expanding the
integral: d_L = c(t₀)t₀[z + (1−ε)z²/2 + O(z³)], so a constant-c
fitter reads

**q₀_app = +ε = +H₀⁻¹ (ċ/c)|₀ ≥ 0, equality iff c is constant.**

The mimicked deceleration equals the present fractional c-rise per
Hubble time — the very depletion rate that defines the mechanism.
Faster depletion ⇒ stronger APPARENT DECELERATION. Observed:
q₀ ≈ −0.55.

## §6 — F1 read: DEAD

Frozen falsifier (3421, re-frozen 3429 §4): **q₀_app ≥ 0 ⇒ dead.**
The route yields q₀_app = ε ≥ 0 for every admissible history: ε > 0
(mechanism active, mimics deceleration) or ε = 0 (mechanism inert).
**VARC-1 FAILS F1. The varying-c route is dead as a dark-energy
mechanism.** Steps 2–3 NOT REACHED per the frozen order.

**Diagnosis of the founding intuition** ("light traveled slower, so
it takes longer to get here, so it appears farther"): the first two
clauses are true; the third is the error. Apparent (luminosity)
distance is set by the photon sphere's RADIUS at arrival — how far
the light has SPREAD — and slower past light spreads LESS. R1 removes
the only possible compensation: the z↔epoch map carries no c-history
at all. Longer travel time at fixed emission epoch means a smaller
sphere, brighter image, apparently CLOSER source.

**No-rescue corollary:** mimicking acceleration (q₀_app < 0) requires
ε < 0 — c FALLING, light faster in the past — which requires SSV_abs
RISING, i.e., net heavy-species production in the current epoch. That
is excluded by the mechanism's own defining property (non-production;
relic species only deplete, 3421 §1b) and by the lensing-calibrated
sign (3423). The door is closed from inside.

## §7 — Verification, including the failed first run

`verify_3430_varc1_step1.py` + captured output
`verify_3430_output.txt`. Crest-level simulation of the photon ODE
(rtol 10⁻¹²) against six c(t) histories (exponential and power-law
approach, amplitudes 0.05–0.30): R1 confirmed to ≤ 2.4×10⁻⁸ at z up
to 2; ratio < 1 and monotone at all z (S1); cubic-fit q₀_app matches
ε to ≤ 0.5% absolute with the constant-c null control at 0.0023
(residual quartic bias, shared by both models, differential clean).

**The first run FAILED its own checks (R1 error ~A, 5–11%), and the
failure was real:** the worker's analytic pass had conflated the
asymptotic c₀ with today's c(t₀), and the script compounded it by
normalizing received periods against an absolute-frame parameter
rather than the observer's local atomic period. Diagnosis forced the
precise form of R1 (spectrograph z, local units) and surfaced the
ruler-invariance corollary (§1). Both errors and the fix are part of
this record — the verify-script discipline caught in minutes what
prose derivation had smuggled past two readings.

**Framework caveat, stated with the verdict's robustness:** the
derivation is at Galilean-flow level, matching the founder's
kinematic picture. The verdict survives because (i) the comparison is
same-framework differential (varc vs constant-c on identical
kinematics), (ii) Galilean and SR coasting agree at O(z²) where q₀
is read, and (iii) the exact all-z brightness ratio is
framework-internal. Adding matter self-gravity decelerates further —
it cannot flip the sign.

## §8 — What survives the death, and it is substantial

1. **The spectroscopic null (R1):** CPP derives that received line
   frequencies and line ratios carry NO memory of the c-history — no
   anomalous epoch drift. This matches observation and is now a
   standing substrate result independent of dark energy.
2. **Light-curve stretching = (1+z) exactly** — derived, matching
   observation; pure tired-light theories fail exactly here.
3. **The comoving-Sea frame resolution** and the Sea-frame ≡ CMB-frame
   identification (dipole = our motion through the local Sea).
4. **Ruler invariance** (light-second epoch-invariant) — a clean
   consequence of quantum-invariance, likely useful far beyond this
   lane.
5. **The lensing-channel calibration** (3423) stands for any future
   optical-channel physics.
6. **The founder's recorded expectations, scored as expectations:**
   (a) "maybe no effect whatsoever" — vindicated on the frequency
   side (the exact null), not on flux; (b) epoch gravitational
   redshift — a real channel, cancelling exactly. Neither was a
   pre-declaration; neither is scored as one.

## §9 — The DE lane's position after 3430

Both channel families are now closed by their own arithmetic:
**energy-density channels** read w = −1.00 ± 0.02 (Λ-like; 3419);
**the optical/kinematic channel** mimics deceleration and dies at F1
(this patch). CPP's current honest dark-energy position: **Λ-like,
with Λ from the calibration; no distinguishing dynamical DE
mechanism is on the table.** The only chartered remaining route to a
distinguishing prediction is the open/comoving instrument
(energy-density side, a build). The §1b floor question is MOOT for
VARC-1 (dead before magnitude order) but remains open as Sea physics.

## §10 — Fence

Nothing here touches Λ, d_s^emp = 4.636, the 0.043 systematic, the
frozen 2.450, DISP-I3, the 3400–3419 frozen conclusions, the 3422
disposition, the 3423 ruling, or the 3429 record. The 3429 §6.4
worker ruling ("route continues, narrowed") is DISCHARGED by
execution: the route continued and died at its falsifier, which is
the process working as designed. Founders_voice records stand.
Next patch (DE): 3431.

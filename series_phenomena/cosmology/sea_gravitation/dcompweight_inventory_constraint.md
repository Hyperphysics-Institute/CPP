# D-COMP-WEIGHT — THE qCP INVENTORY CONSTRAINT: the Ω-budget route, run forward, does not bound f small — it excludes a gravitating reservoir at ~10⁴³, forces the reservoir into the non-gravitating Sea ground state, and makes the composition fraction O(1)-class; the present-epoch fractions are the ONE input the corpus does not commit — FQ-6 posed

**Patch 3089 (12 Aug 2026). Verify: `scripts/3089_qcp_inventory_bounds.py`.
Forward inputs only: FI-RELIC-1 (equal initial ± eCP / ± qCP
populations, founder-axiomatic — verbatim at `dark_matter/reasoning/
2519.md` and the composition ruling), CP permanence, the Sea site
density 1/d² anchoring of 3076, the cosmic budget Ω ≲ 1 (sky-blind:
no CC-band quantity used or implied anywhere), and R-RIBBON-RESERVOIR's
confinement requirement. Anti-extraction intact: no branch of FQ-6 is
accompanied by any CC consequence; materiality is explicitly left
unassessed until the forward pieces land.**

## §1 — What the 3070 minting expected, and what the arithmetic does

The D-COMP-WEIGHT minting registered the forward path: "f is NEVER
fitted to the CC band... the DM/baryon budget bounds f from above,
sky-blind." Executing that path:

**The Ω ceiling.** With equal creation counts and a pair-filled Sea,
the qCP inventory is n_q ≈ 2/d³ ≈ 10¹⁰²/m³ (d_s = 8). If a fraction
g of it resided in GRAVITATING structures at any per-qCP mass scale
μ, then Ω ≲ 1 requires g ≤ ρ_crit/(n_q μ) ≈ 10⁻¹⁰⁰ (μ = 50 MeV;
robust over six orders in d_s and thirty in μ).

**The confinement floor.** R-RIBBON-RESERVOIR requires the Sea to
supply splittable qDP feedstock — at minimum ~1 hybrid per hadron
interaction volume — a site fraction f ≥ ~10⁻⁵⁷ (d_s = 8).

**The crossing.** floor/ceiling ≈ 10⁴³. A reservoir dense enough for
confinement, if gravitating, overcloses the universe by ~43 orders.
The 3070 scale-separation note checked only the floor ("trace
suffices") and correctly found it far below CC-relevant magnitudes;
nobody ran the ceiling. Run, it does not bound f small — **it
excludes the gravitating reading entirely.**

## §2 — The forced conclusion (theorem-class within corpus commitments)

Within {FI-RELIC-1, CP permanence, Sea density ≥ 10⁸⁶/m³ (any
d_s ≤ 10⁶), observed Ω}: **essentially the entire qCP inventory
(1 − O(10⁻¹⁰⁰)) resides in NON-GRAVITATING Sea content.** The
qCP-bearing hybrid population is ground-state substrate — the D2
uniform-configuration exclusion (the 12-edge-shell annihilation of
the absolute-|SSV| monopole) must extend to the Sea's equilibrium
configuration INCLUDING its hybrid share, not to a pure-eDP idealization.
Consistency check, the pleasing kind: the OBSERVED gravitating qCP
census — DM rings 7.3/m³ + baryons ~0.5/m³ — is 8.5×10⁻¹⁰² of the
inventory, sitting just under the 10⁻¹⁰⁰ ceiling. The corpus's own
numbers already obey the theorem; the universe is as full of
gravitating qCP structure as Ω permits, and the rest is Sea.

## §3 — The census arithmetic (why f is O(1)-class, not trace)

Equal counts + 2e2q hybrid stoichiometry (ribbon eCP-qCP-qCP-eCP;
hTetra = A-hDP + B-hDP = 2e + 2q — same stoichiometry, apparently
the same structural family): hybrids consume eCPs one-for-one with
qCPs, so if ALL qCPs sit in hybrids the Sea is ALL hybrid — zero
free eDPs. An eDP share exists only if qCP-only chains hold a share
z of the qCPs: site ledger x_hybrid = (N/2 − z)/2, y_eDP = z/2.
**Either way the qCP-bearing site fraction is O(1)-class.** This
inverts the 3070 expectation ("if D-COMP-WEIGHT computes to
negligible at every admissible f, the forward number stands
untouched"): f is NOT negligible, and the composition correction is
potentially the largest open factor in the assembly.

Two registered founder statements now sit in count-level tension:
R-SEA-COMP's "the aggregation of qCPs into DM and Baryons leaves the
DP Sea largely filled with eCPs" (the DM/baryon sink holds 10⁻¹⁰² of
the inventory — it cannot be the sink) vs FI-RELIC-1's equal counts.
The founder's own "[hTetra] ~ 50% of Sea" remark (DM-lane,
reasoning/0863) points at the resolution but is not a registered
present-epoch ruling. This is a genuine physical-picture fork —
posed below, not resolved here.

## §4 — D-COMP-WEIGHT restructured (the forward object, no dial)

  φ_comp = 1 + f_hyb·(W_hyb − 1) + f_chain·(W_chain − 1)

with, per site relative to an eDP:
  W = [channel content: E-register + k-weighted S-register per AP-4b,
       imprint at FULL strength per R-EMIT-PERCP] ×
      [excursion statistic ratio: η_z(coordination)/η_z(free eDP) =
       s(coordination), forward-computable from the binding-well
       arithmetic — the 3070 coordination ladder; ribbon rung 3E+3S,
       DM core 4E+4S].

Three forward pieces, each derivable, none a dial:
  (i)   f_hyb, f_chain — the census fractions: **FQ-6** (founder) +
        the aggregation-kinetics story (R-COMP-EPOCH);
  (ii)  k — AP-4b's strong-to-electric register weight, an O-2
        identification obligation (citation of shipped couplings,
        not a free parameter);
  (iii) s(coordination) — the ladder arithmetic.
**Materiality against F-CLI-1's ×1.5–10 is deliberately NOT assessed
here**: no (f, k, s) combination is computed toward the band; the
pieces land forward or the factor stays open. The self-absorption
observation (3070) still caps the correction's FORM: the measured α
already contains the actual vacuum's electric response — only the
imprint-weighting vs energy-sum-weighting MISMATCH survives.

## §5 — FQ-6 (posed to the founder; composition only, no CC content)

**FQ-6.1 (where the qCP half of creation lives today):** in the
present-epoch Sea, the inventory arithmetic forces the qCPs into
non-gravitating Sea content at O(1) site fraction. Which picture?
  (a) hybrid 2e2q structures (hTetras/ribbons) woven into the Sea —
      your "[hTetra] ~ 50% of Sea" remark, now needed as a
      present-epoch ruling with its fraction;
  (b) qCP-only chains threading the Sea, with eDPs dominant among
      pair structures;
  (c) another configuration.
Also: are the DM-lane hTetra (A+B hDPs) and the 3070 ribbon
(eCP-qCP-qCP-eCP) the same object family?

**FQ-6.2 (ratify the forced reading):** the reservoir is ground-state
Sea content and does NOT gravitate (the D2 symmetry exclusion covers
the Sea's actual equilibrium configuration, hybrids included). The
alternative overcloses by ~10⁴³ at confinement-floor density. Confirm
or overrule.

**FQ-6.3 (reconcile R-SEA-COMP's wording):** does "largely filled
with eCPs" refer to the free-pair population only (with the hybrid
reservoir a distinct, comparably-populated component), or do you
revise the composition fractions?

## §6 — Ledger

φ₁ REOPENED (needs d_s) · φ₂ = 1 CLOSED · φ₃ ≈ 0.2-class, exhibition
condition substantially discharged with qualifiers (3088) ·
**φ_comp OPEN, RESTRUCTURED — the O(1)-census finding replaces the
expected-negligible reading; awaiting FQ-6 + k (O-2) + s (ladder)**.
Factors multiply once, at the end; in band or F-CLI-1 fires in those
words. A note for the eventual panel round: the finding of §2 is
also a PREDICTION-SHAPED statement — CPP requires the vacuum to
carry a qCP-bearing hybrid population at O(1) site fraction that
gravitates as exactly zero — and any future in-corpus mechanism that
makes such content gravitate even at 10⁻⁵⁰ strength kills the
framework on Ω alone.

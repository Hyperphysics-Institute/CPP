# A-1 EXECUTED — THE WEAVE CENSUS RECOUNT: φ₁(weave) = 2 × [1 + O(ξ²/d²)] on the canonical inter-unit-spacing anchoring, with the unit-internal exclusion convention named, the anchoring convention DECLARED (so the d_s derivation has an unambiguous target), the arrangement bracket recorded, and the transient-population rider (OPEN-EXCH-FRAC) attached

**Patch 3097 (12 Aug 2026). The first item of the R-SEA-WEAVE audit
trail (3091 §5, activated 3093). Replaces the VOIDED full-occupancy
φ₁ = 0.49 (3071/3072, reopened by R-OCC-DILUTE at 3076). Verify:
`scripts/3097_a1_weave_census.py` (all gates PASS; the 3071 converged
C₄ = 25.3382 reproduced as the regression gate). Anti-extraction
intact: no reference to the band appears anywhere in this note; the
factors multiply once, at the end, per the 3068 freeze.**

## §1 — The question, restated under the ruled Sea

The 3076 re-anchored assembly is

  ρ_Λ = (C₄′·α·η_z/2π)·ħc/(d²·R_h²),

computed under the convention ONE unit electric dipole per array site
at nn spacing d. R-SEA-WEAVE (3093) rules the Sea an array of 4-CP
weave units — one eDP + one qDP, electrically bound, f_hyb ≈ 1 — and
the composition ruling gives the qCP unit electric charge, so **each
unit carries TWO unit electric dipoles**. A-1 asks: what is the true
E-register arrival census relative to the committed one-dipole-per-
site convention?

## §2 — The census (forward)

**Leading order: φ₁ = 2, exactly.** Each unit site hosts two unit
E-dipoles, co-located at the unit scale ξ (the ribbon extent,
eCP-qCP-qCP-eCP, l_P-class) which is small against the inter-unit
spacing d. As ξ/d → 0 the arrival sum is 2·C₄′/d⁴ — two full copies
of the one-per-site sum. Per-dipole imprint strength is the SCR-2
question and is CLOSED at φ₂ = 1 (R-EMIT-PERCP, density-independent,
explicitly unaffected by the composition per 3076 §6); per-species
excursion statistics (the qDP's tighter coordination) are A-2's
s(coordination) — see the boundary statement in §3.

**Finite-extent correction (computed, script §C):** dipoles offset
±ξ/2 about the unit centre along isotropic orientations give a
POSITIVE convexity correction: φ₁(ξ)/2 − 1 = +0.3%, +1.2%, +2.5%,
+5.1%, +7.8% at ξ/d = 0.1 … 0.5. For d_s in the working range with
l_P-class unit extent, the correction is percent-class; it rides as a
bracket, not a dial, and its sign is recorded (up).

**Adopted: φ₁ = 2 × [1 + O(ξ²/d²)], canonical anchoring §4.**

## §3 — The unit-internal exclusion convention (named, load-bearing)

The receiving unit is excluded ENTIRELY from the arrival sum — the
receiver's own dipole (self, as always) AND its sibling dipole at
distance ξ. This is a named convention, not an oversight, on three
grounds: (i) the sibling's field on its unit partner IS the binding —
the coordination environment whose effect on excursion statistics is
exactly A-2's s(coordination); counting it again in the arrival sum
would double-count binding as vacuum energy across the A-1/A-2
factorization boundary. (ii) R-CC-DIST-CONT rules the CC engine to be
the "increased distance between qDP-eDP species" — the INTER-unit
distance; the intra-unit separation does not dilute and is not the
engine. (iii) Reductio: an included sibling contributes ∝ 1/ξ⁴,
which at ξ ≪ d would dominate the entire sum and tie ρ_Λ to the
BINDING scale rather than the dilution scale — contradicting the
ruled engine and every registered scaling of the arc. **Boundary
statement (binding on both notes): A-1 counts unit E-dipoles and
their inter-unit geometry; everything intra-unit (binding, strong
register, coordination-modified excursions) is A-2's W = (E + k·S) ×
s. No quantity may appear in both.** The per-axis-vs-3D projection
flag on η_z (3088) is orthogonal to this census and is untouched.

## §4 — The anchoring convention DECLARED (the joint-with-d_s clause)

The factor "2" is convention-dependent, and the 3091 audit trail
required the recount "alongside the reopened d_s question" for
exactly this reason:

- **Canonical (DECLARED HERE): d ≡ the mean inter-UNIT nn spacing.**
  Unit number density n_u = √2/d³ (FCC-form normalization), E-dipole
  density 2n_u. φ₁ = 2 × [1 + O(ξ²/d²)].
- Equivalent alternative: d ≡ the mean inter-DP spacing (the literal
  3076 wording). At fixed DP density, dimerizing DPs into units
  places units at 2^{1/3}·d and φ₁ = 2/2^{4/3} = **2^{−1/3} = 0.794**
  (verified numerically, script §D, to 4 digits).

The two are the SAME physics under a change of variable; the declared
canonical variable is the inter-unit spacing, because the unit is the
ruled structural object (R-SEA-WEAVE) and the relic that dilutes
(R-DS-EVOLVE). **Binding consequence for D-DS-RELIC (Patch 3098): the
d_s derivation must produce the inter-UNIT spacing, or convert by
2^{1/3} explicitly.** Every future use of "d_s" in the CC assembly
means the inter-unit spacing unless flagged.

## §5 — The arrangement bracket (flagged open, not resolved)

The FCC form of C₄′ was corpus-forced at the l_P lattice (3067,
z = 12). In the dilute Sea that forcing is GONE — the units sit at
mean spacing d on an isotropic-on-average arrangement (R-DS-EVOLVE),
whose short-range order is set by the exchange/fidelity dynamics, not
by the GP lattice. Computed bracket (script §E): a Poisson array at
matched density with hard core a·d gives Σ1/r⁴ / C₄(FCC) = 1.35,
1.19, 0.96, 0.86, 0.78 at a = 0.5 … 0.9. The sum is nearest-
neighbour-dominated, so the O(1) arrangement factor is real and its
value rides on the hard-core scale (physically: the closest approach
the exchange equilibrium sustains — OPEN-EXCH-FRAC-adjacent). **The
arrangement factor is registered as an open sub-item of the census,
bracket [0.78, 1.35] on C₄′, resolution routed to the same
instrumentation that measures the transient population (the 3088
array's pair-separation statistics at stationarity).** Until
resolved, C₄′ = 25.34 (FCC) is carried with the bracket visible.

## §6 — The transient-population rider

R-ZBW-EXCHANGE maintains a transient free-species population (eDPs,
qDPs, hDPs) in equilibrium with the weave. Each transient free dipole
is an additional unit E-dipole in the census: φ₁ → 2·(1 + f_trans/2)
to leading order, direction UP, magnitude = OPEN-EXCH-FRAC (the swap
rate × dwell extraction from the 3088 instrumentation, queue item 5).
Attached as a rider; not folded until measured.

## §7 — Ledger, stated with direction

**φ₁ = 2 × [1 + O(ξ²/d²)] (+ f_trans rider), canonical inter-unit
anchoring; arrangement bracket [0.78, 1.35] on C₄′ flagged.**
Direction, plainly, per the freeze discipline: relative to the
committed one-dipole-per-site convention at the same d, the weave
census DOUBLES ρ_Λ; relative to fixed-DP-density anchoring the net is
0.794. The dominant open scale remains d itself — (l_P/d)² — owned by
D-DS-RELIC (3098), with the canonical variable now unambiguous.
SCR-1's voided φ₁ = 0.49 is fully superseded by this recount; the
3071/3072 machinery (converged C₄, tail treatment, bracket ethic) is
carried forward and its regression gate passes. No verdict is
computed here; in band or F-CLI-1 fires in those words, once, at the
single final assembly.

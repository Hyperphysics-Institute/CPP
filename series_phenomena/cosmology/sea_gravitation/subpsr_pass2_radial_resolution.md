# D-SUBPSR-FIELD PASS 2 — RESOLVED UNDER R-RADIAL-AFTER-FIRST: the sub-PSR law is EXACT inverse-square to the adjacent GP (the pass-1 ~20% softening, the 0.9/√N thickness law, and the 0.82 front speed are ALL retired as re-split artifacts); the gate reconciles (radial-ballistic = AUTOMATON-2's ±0.4%, the residual being the first split's discrete anisotropy per FACT G1); the front is SHARP and travels at c; the dwell-exit kick = emission/12 — FQ-12's ANSWER STRUCTURE CONFIRMED (partner field at dwell exit, derivable), unit normalization the one small open task

**Patch 3134 (14 Aug 2026). Pass 2 executes analytically — the
ruled radial-ballistic model needs no cascade simulation.
Supersession banner appended to the pass-1 note.**

## §1 — The model under the ruling

Spherical first submoment (the 12-split from the 13th) establishes
isotropic ray density; purely radial propagation thereafter. Ray
density through any sphere is conserved ⇒ **flux(s) = E/(4πs²)
EXACTLY, from the far field down to the adjacent GP** — no
re-split, no angular diffusion, no softening. The founder's
OPEN-SUBPSR-1 question is answered cleanly: **the signal MAXIMIZES
inward as exact inverse-square; the only cap is the lattice floor
itself, where the adjacent GP receives exactly E/12 per Moment.**

## §2 — What pass 1 got wrong, itemized (the honest ledger)

Retired as re-split artifacts: the ~20% adjacent-shell softening;
the 0.9/√N thickness law; the 0.82 effective front speed; the
declining directed fraction. Under the ruling: the front is SHARP
(thickness = emission duration), travels at c (1 hop/Moment
radially), and the field is FULLY DIRECTED at every radius. THE
GATE RECONCILES: radial-ballistic composition is exact 1/r², and
AUTOMATON-2's ±0.4% residual is the first split's degree-6 discrete
anisotropy (FACT G1) — the corpus's two results now agree by
construction. F-E2-3's ~10% band: same artifact, months older, now
answered (registration file §2).

## §3 — Consequences

1. **FQ-12's structure is CONFIRMED:** the dwell-exit kick is the
   partner's field at the adjacent configuration = emission/12 per
   Moment, fully directed — the "derivable" branch of FQ-12 is the
   answer; the remaining task is the unit normalization (the
   DI-bit-count → instrument-force mapping), a bounded bookkeeping
   item, with the calibration already insensitive across the whole
   admissible range (3131 §4).
2. **The instruments' max(r, 1) softening is CONSONANT:** under the
   ruled physics separations below the adjacent floor do not occur;
   exact 1/s² to the floor is what the instruments approximate,
   and the d_s = 2.0 anomaly is now LESS likely a sub-PSR
   regularization artifact (the derived law matches the assumption
   at the floor) — its explanation shifts toward genuine
   dense-weave physics, sharpening the n = 7 pass's target.
3. **The energy accounting closes tighter:** R-ARC-CANCEL-TURNAROUND
   makes the dwell a MECHANISM (the arc-cancelled pair has nothing
   to move it until relaunch), completing the 3132 mutual-dependence
   observation into a causal chain: arc cancellation → stop → dwell
   → relaunch by the partner's E/12 field → the ZBW cycle — every
   link now ruled or derived.

## §4 — OPEN-SUBPSR-1 status

The founder's declared examination is substantially RESOLVED at the
field-profile level: maximize, exact 1/s², sharp front, E/12 at
contact. Remaining inside the examination: the unit normalization
(§3.1) and any content the founder wants developed on the
superposition approach itself (the last submoments before
co-location, where the arc-cancellation dynamics act). Kila6
Route C and the DM ledger untouched; arrival still trumps all.

---

**SUPERSESSION BANNER (appended 14 Aug 2026, Patch 3135; original
above unaltered):** the founder clarified his intent
(`founders_voice/founder_clarification_outward_fanout_2026-08-14.md`):
"only radial" meant NO ANTI-RADIAL progression — DI-bits fan out at
EVERY hop to all outward-component neighbors with equal shares
(R-OUTWARD-FANOUT). This note's "purely radial single-ray" model,
its exact-1/s² claim, and its retirement of the band are all
SUPERSEDED: the ~10% band is GENUINE (F-E2-3 option (a) ruled). The
kick = E/12 and the maximize-inward conclusion survive. See
`subpsr_pass3_fanout_results.md`.

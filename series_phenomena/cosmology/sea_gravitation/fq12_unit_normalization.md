# FQ-12 QUANTITATIVE BRANCH CLOSED — the unit normalization executed: (1) the DETERMINISTIC relaunch kick derives as E = 12 instrument units per Moment, i.e. the cascade's adjacent-shell flux E/12 IS the unit Coulomb field at adjacency — the kick is the partner's field, ALREADY EXPLICIT in every instrument (nothing was missing, nothing double-counted); (2) the STOCHASTIC operating point is a SELF-GENERATED ATTRACTOR: the Sea's ambient non-partner fluctuation sits at σ ≈ 0.25 per axis nearly independent of the injected jitter (0.2505 → 0.2464 as σ_in runs 0.15 → 0.30), with the self-consistency fixed point σ* = 0.249 — within 17% of the operating 0.30; the panel's "ungrounded dial" is retired in substance under EITHER ontology

**Patch 3138 (14 Aug 2026). Executes the FQ-12 unit-normalization
bookkeeping (the queue's bounded item, chosen under PD-006). Verify:
`scripts/3137_fold_check.py` (probe parameterized; defaults
unchanged) — map cells at σ_in ∈ {0.15, 0.20, 0.25} plus the 3137
0.30 point, d_s^emp, n = 5.**

## §1 — The deterministic kick: E = 12, already explicit

The sub-PSR cascade gives the adjacent-shell flux as E/12 per Moment
(theorem-protected origin split). The instrument's own Coulomb
normalization sets the unit-charge field at adjacency to 1. These
are the SAME object (the Coulomb field IS the DI-bit flux), so
**E = 12 instrument units per Moment, and the dwell-exit kick = the
partner's unit field at the exit configuration — which every array
instrument already applies explicitly as the (retarded) partner
term.** FQ-12's derivable branch closes with the pleasant verdict
that nothing was ever missing and nothing is double-counted.

## §2 — The stochastic operating point: an attractor, and its fixed point

| σ_in | σ_ambient(out) | out/in |
|---|---|---|
| 0.15 | 0.2505 | 1.670 |
| 0.20 | 0.2494 | 1.247 |
| 0.25 | 0.2490 | 0.996 |
| 0.30 | 0.2464 | 0.821 |

The ambient non-partner field fluctuation is **nearly independent of
the injected jitter** — the many-body Coulomb dynamics self-generate
σ_ambient ≈ 0.25 at the calibrated spacing regardless of the drive —
and the self-consistency crossing (injected = generated) sits at
**σ* = 0.249**, within 17% of the operating 0.30. Under the
bootstrap ontology (the jitter REPRESENTS the Sea's ambient field),
the operating point is the derived fixed point to ~17%; under the
missing-content ontology (the jitter represents only beyond-box +
shot noise), σ_n would sit at ~0.03–0.10 — and 3131 §4 already
measured that d_s^emp moves ≤ 0.021 across that ENTIRE range. Either
way: **the panel's "hand-set, ungrounded" flag is retired in
substance** — the derivable component derived (3131), the
deterministic kick already-explicit (§1), the stochastic level a
measured attractor with a located fixed point, and the calibration
invariant under all of it. (Nuances stated: map cells single-seed —
the attractor's flatness makes seed spread minor, the 0.30 point
carries both seeds from 3137; one spacing, n = 5.)

## §3 — The one-line leisure item for the founder

Which ontology the jitter carries — the Sea's own ambient field
(bootstrap, σ* = 0.249) or only the unresolved remainder (~0.05) —
is an interpretive choice with zero calibration consequence; noted
for the founder's leisure, blocking nothing. FQ-12 is otherwise
CLOSED. The CC lane remains YIELDED; this was its last bounded
bookkeeping item. Kila6 Route C (~55%, stable) and the DM ledger
untouched; arrival still trumps all.

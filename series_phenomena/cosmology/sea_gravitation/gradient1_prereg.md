# **GRADIENT-1 PREREGISTERED: does CPP's gradient-sourcing make the volume-averaged expansion source depend on how matter is ARRANGED — so that structure formation (clumps + voids, z ~ 1–2) weakens deceleration and mimics acceleration, answering "why now"? The worker's own structural analysis, recorded BEFORE execution, points to a kill: Step A's shell-broadcast aggregation appears to make the mean source rearrangement-invariant. The frozen questions below are the only ways the route survives**

**Patch 3432 (26 Aug 2026). DE lane. Route author: the worker
(proposed at this session's opening, pre-3421, set aside for the
founder's varying-c mechanism, never chartered until now). Zero
compute beyond the verify script. Status in
`frontier_sectors/DMDE.md`.**

## §1 — The route as originally pitched (session opening, on record)

Step B: gravity couples to ∇(ΔSSV)-excess, not mean density. As
matter clumps, gradient support concentrates into collapsed regions
while voids — most of the volume by late times — lose source.
Deceleration weakens as clumping proceeds; timing set by nonlinear
structure formation (z ~ 1–2), exactly when acceleration turns on.
Small parameter δ ~ 1: dimensionless, passes the lane's filter.

## §2 — The worker's pre-execution structural concern, frozen with the prereg

Step A aggregates by shell broadcast: a comoving shell's dynamics
are driven by the ENCLOSED excess mass M_enc. Mass-conserving
rearrangement inside the shell leaves M_enc unchanged — suggesting
the mean expansion source is arrangement-INVARIANT by construction,
i.e., the pitch conflated field-level gradient structure with
mean-level sourcing. If so, the route dies at the level CPP has
derived.

## §3 — Frozen questions and reading rules

- **G1 (analytic + numeric):** at the Step-A level, does
  mass-conserving rearrangement (uniform → clumped) change the
  surface-averaged inward gravitational flux over a comoving
  boundary? Verify script: Monte-Carlo Gauss-flux comparison,
  uniform vs strongly clumped interior arrangements, same total
  mass; also record the LOCAL field variance to exhibit the
  field-level/mean-level distinction explicitly.
- **G2 (corpus audit):** does the corpus contain any DERIVED
  mechanism breaking M_enc invariance at cosmological potentials —
  (i) nonlinearity of the SSV→gravity map active at |Φ|/c² ~ 10⁻⁵,
  or (ii) derived dynamics of the Sea ground-state REFERENCE under
  structure formation?
- **Reading rule (F-G1, frozen):** the route SURVIVES only if G1
  shows an arrangement effect beyond Monte-Carlo error, OR G2 finds
  a derived (not invented, not 3125-blocked) mechanism. Otherwise
  DEAD, with the inherited standard-GR second-order backreaction
  (literature: ~10⁻⁵-level, and not CPP-distinguishing) noted as the
  full residual.

## §4 — Fence

Nothing touches any frozen conclusion. Worker pre-declaration:
the §2 concern IS the worker's expectation, recorded openly — the
route's own author expects a kill and freezes the survival criteria
anyway, so the execution cannot be steered either way silently.
Next patch (DE): 3433.

# OBL-INV-SENS DISCHARGED — the blinded inversion-sensitivity suite: the calibration is nearly IMMUNE to the panel's chief concern (σ_n halved or ×1.5 moves d_s^emp by ≤ 0.021 — half a percent); the gas-threshold variant moves it +0.218 (inside the arrangement band); the Φ plateau is SINGLE-REGIME (98.6% gas-channel at every spacing — no bound/gas mixing artifact is possible) and persists under every perturbation; OBL-N216 six of sixteen cells complete, transition-sharpening trend continuing

**Patch 3129 (14 Aug 2026). Discharges OBL-INV-SENS (CONV-020 §2.4)
and logs OBL-N216 progress. Verify: `scripts/3129_inv_sens.py` +
sensitivity JSON. BLINDING honored: the suite reports Δd_s^emp per
perturbation; no band, no verdict, nothing steerable — the
calibration was frozen and labeled before this suite ran.**

## §1 — σ_n bracket (the "ungrounded foundation" concern, tested)

| σ_n | d_s^emp | Δ vs 0.30 |
|---|---|---|
| 0.30 (operating) | 4.617 | — |
| 0.15 (halved) | 4.638 | +0.021 |
| 0.45 (×1.5) | 4.624 | +0.006 |

**The calibrated spacing is insensitive to σ_n at the half-percent
level across a factor-3 bracket.** The panel's flag stands as an
open derivation item (OBL-SIGMA-N unchanged), but its consequence
for the calibration is now measured and small: the inversion's
conditioning is genuinely geometric. (Baseline nuance: 4.617 here is
seed-5-only; the frozen 4.636 is the seed-mean — a 0.019 seed-class
difference, consistent.)

## §2 — Gas-threshold variant

Redefining the gas state at 0.75·d_s (vs the standard 0.5·d_s)
moves d_s^emp by **+0.218 (4.7%)** — the larger of the two
sensitivities, comfortably inside the arrangement band [4.07, 5.42].
The definitional freedom in the dense regime is real, bounded, and
now quantified.

## §3 — The Φ-plateau regime-mixing test

Decomposition at every spacing: **Φ is 98.5–98.9% gas-channel** —
the plateau is not a bound/gas compensation artifact because there
is no meaningful bound/gas mixture to compensate; it is the gas
term's own flatness (falling r_e against rising per-pair stretch,
within one regime), and it PERSISTS under the σ_n bracket and the
threshold variant. The panel's regime-mixing concern is answered:
single-regime, robust.

## §4 — OBL-N216 progress

Six of sixteen cells complete (seed 5: d_s 1.5–4.0). The n = 6 f_b
curve steepens through 2.0–3.0 with the small-spacing dip
strengthening (f_b = 0.186 at 2.0) — the transition-sharpening
trend toward the extrapolated 2.45, exactly where the part-1 record
flagged that n = 6 matters most. Completion (seed 5: 4.5, 5.0;
seed 11: full grid) continues next session; the declared post-freeze
consistency check runs at completion. Remaining obligations queue:
OBL-SIGMA-N (the lane's next physics item) · OBL-FOLD-CHECK ·
OBL-KIN-BLIND (long-horizon) · OBL-CAL-LABEL/VERDICTS-PUBLIC
(standing, in force). Kila6 Route C and the DM ledger untouched.

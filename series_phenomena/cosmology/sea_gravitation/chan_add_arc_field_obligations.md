# THE FOUR PANEL OBLIGATIONS DISCHARGED — D-CHAN-ADD DERIVED (COHERENT: one displacement drives both registers, so amplitudes add before squaring — C = (1+k)² = 68.49, the ×1.27 fork CLOSED); OBL-ARC-FIELD EXPOSED (source-side previous displacement is DI-bit-encoded field content — legitimate; the receiver-side term is a declared one-Moment discretization: 44.5% force share, but scheme-TESTED — lagged vs predictor shifts every observable by <= 0.034, seed-spread class); OBL-K-SENS COMPUTED (±10% α_s → ±4.4% c_Li); OBL-MEMORYLESS-PROV COMPILED

**Patch 3122 (13 Aug 2026). Discharges the CONV-019 Q3/Q6/Q2/Q4
obligations. Verify: `scripts/3122_obligations.py`.**

## §1 — D-CHAN-ADD: the channel addition rule, DERIVED

The panel asked whether electric and strong field amplitudes add
before squaring. The answer follows from two corpus facts, neither
optional:

1. **AP-4b (enacted axiom, verbatim form):** SSV_abs = Σ|polar| +
   k·Σ|strong| — the gravitational source is an AMPLITUDE sum with
   weight k.
2. **Single-driver rigidity:** a qDP has ONE stochastic degree of
   freedom per Moment — the CP displacement δ. Both registers are
   imprints of that same displacement: polar amplitude ∝ δ, strong
   amplitude ∝ δ, identical geometry, identical sign. The DI-bit
   carries the E and S components of one displacement event; there
   is no second stochastic driver that could decorrelate them.

Therefore the qDP's per-Moment source amplitude is (1 + k)·δ with
the two channels at correlation exactly +1, and its quadratic
content is ⟨[(1+k)δ]²⟩ = (1+k)²⟨δ²⟩. **COHERENT — derived, not
assumed. C = (1+k)² = 68.49; the incoherent branch would require
independent per-register drivers the automaton does not possess;
the ×1.27 fork is CLOSED.** Cross-DIPOLE terms remain incoherent
(independent δ's, random orientations — the arrival sum's existing
treatment), so coherence is strictly intra-dipole: exactly the C
that multiplies η̄_q in the fold, nothing more. The verify script
demonstrates both facts numerically (variance of a two-register
single-driver source = (1+k)²·Var(δ) to machine precision;
cross-dipole terms → 0 as N^{-1/2}); scope stated honestly: the
demo exhibits the arithmetic of the two premises, the premises
themselves are the enacted axiom and the DI-bit structure. Gemini's
minority COHERENT verdict is thereby confirmed by derivation.

## §2 — OBL-ARC-FIELD: the arc force under R-CP-MEMORYLESS

Two distinct uses of "previous displacement" in the instrument:

- **Source side (legitimate field state):** the magnetic component
  of an arriving DI-bit encodes the SOURCE's displacement at the
  emission Moment, delivered retarded. That is information carried
  IN THE FIELD, in flight — the CP consults nothing about itself.
  Clean under R-CP-MEMORYLESS.
- **Receiver side (declared discretization):** the force term
  q·v×B requires the RECEIVER's motion during the Moment the field
  acts. At bare-CP level this is the SAME Moment's displacement —
  a within-Moment coupling in the PCD cycle — not stored memory.
  The instrument's explicit scheme approximates it with the
  previous Moment's displacement: a ONE-MOMENT DISCRETIZATION LAG,
  declared as a numerical choice. **Measured, not argued — and the
  first measurement corrected this note's draft claim before commit:
  the arc term carries |F_arc|/|F_total| = 44.5% of the force at the
  frozen spacing (large, not percent-class). The scheme sensitivity
  was therefore TESTED, not asserted: a two-pass predictor variant
  (receiver velocity = the CURRENT Moment's provisional displacement)
  vs the lagged campaign scheme, full cells, both seeds — every
  observable shifts by ≤ 0.034 (most ≤ 0.01), at or below the seed
  spread (raw comparison: `scripts/3122_scheme_test.json`).** The lag
  is a bounded scheme choice despite the term's force share; the
  measured shifts ride into the confrontation's residual register as
  the ARC-SCHEME systematic.

## §3 — OBL-K-SENS: the α_s sensitivity budget

c_Li ∝ √Φ with Φ dominated by C·η̄_q ⇒ ∂ln c_Li/∂ln α_s =
½·k/(1+k) = 0.4395 (exact arithmetic in the script). **A ±10%
excursion in α_s moves c_Li by ±4.4%** — small against the band's
±20% half-width; the k identification (ratified 4–1) is not the
confrontation's sensitive joint.

## §4 — OBL-MEMORYLESS-PROV: pre-fire support, compiled

(i) SF-6 (electromagnetism flagship, shipped pre-fire): inertia is
DERIVED as emergent — the fore-charge/aft-discharge loading of a
dressed mass's dipole cloud — which is coherent only if the bare CP
carries no kinematic state of its own; (ii) the founder's ruling
language is explicitly a standing conception ("The CPs, in my
modeling/conception, have no memory"), not a response-shaped
novelty; (iii) the instrument lineage's v-persistence entered as
modeling convenience (R-INERTIA-ARC) and was never a founder
ruling. The successor test remains registry-labeled SEPARATE, and
F-CLI-1 remains permanently fired, per the panel's caveats.

## §5 — Consequence

All CONV-019 Q6 gates are DISCHARGED (D-DS-INDEP 3121; D-CHAN-ADD
§1; OBL-ARC-FIELD §2). The fresh point confrontation may now be
preregistered. Kila6 Route C and the DM ledger untouched.

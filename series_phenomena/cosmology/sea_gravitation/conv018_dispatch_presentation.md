CONV-018 — COSMOLOGICAL-CONSTANT WHOLE-ARC REVIEW (single round)
REVIEWER: [GPT | GROK | GEMINI | DEEPSEEK | COPILOT — one paste each]

Links (convenience only; review from THIS PASTE, fetch nothing):
  https://github.com/Hyperphysics-Institute/CPP/blob/main/series_phenomena/cosmology/sea_gravitation/conv018_2026-08_cc_arc_round_skeleton.md

INTRO — what this round is and what is asked of you.

The CPP cosmological-constant arc claims a dark-sector unification: one
Dipole Sea sourcing dark energy (uniform mode, suppressed) and dark
matter (gradient inhomogeneities), on a field equation derived at zero
new parameters. After four months the arc rests on ONE derivation
(the IR scale, newly claimed via a causal-structure construction) and
ONE open coefficient (a factor ~2 in the mode counting). This round
reviews the WHOLE arc as a unit for the first time. The record
includes disclosed corrections (a scale/coefficient conflation, an
attribution corrected by founder ruling) and a data confrontation in
which the arc's FITTED June parameter fares WORSE than its derived
candidates. You are asked for verdicts in the rule language of §2
(Q1 construction, Q2 branch question, Q3 coefficient legitimacy +
falsifier posture, Q4 arc grade), in order, with reasoning; the §3
notices carry an open challenge window. Adversarial review is the
point: the null hypothesis — that the event horizon remains a fit
dressed in new clothes — deserves your best case.

===== DOCUMENT 1 of 4 — conv018 skeleton (digest + the questions) =====

# CONV-018 DISPATCH SKELETON — THE COSMOLOGICAL-CONSTANT WHOLE-ARC REVIEW (the 1164 promotion-path item 2)

**Patch 3061 (11 Aug 2026). The multi-AI review the 2-ii record
mandated: the CC arc as ONE unit — Steps A–D, the 1161 field-equation
discharge, the condition-(2) resolution (2-i/2-ii/2-iii v0.2), the
3059 founder ruling, and the 3060 data confrontation. CONV-001
format: one self-contained fenced block per reviewer (GPT, Grok,
Gemini, DeepSeek, Copilot); verdicts in the rule language below;
adversarial review invited explicitly. The arc's standing going in:
CONDITIONALLY SUPPORTED, not derived; nothing here moves the DM
ledger.**

---

## §1 — Arc digest (what the reviewers are ruling on; repo authoritative)

**The claim under review:** the same Dipole Sea sources dark energy
(uniform mode → suppressed Λ) and dark matter (gradient
inhomogeneities), with: gravity coupling to ΔSSV-excess above the
Sea ground state (Step B — one mechanism distinguishing matter, DM,
and Λ); the field equation G_μν = 8πG/c⁴·T_μν[LSP] DERIVED at zero
new parameters with the absolute-|SSV| monopole annihilated by
600-cell 5-design symmetry (Patches 1107–1108, 1121–1161 — the arc's
formerly most load-bearing risk, closed); Friedmann recovered with
acceleration onset z ≈ 0.63 (Step D); the vacuum residual as a
zero-point coherence mode, not a transient gradient (2-ii(a),
defusing the retarded-scale objection); and the IR scale now
DERIVED rather than fitted (2-iii): **Coordinable(P,Q;t) ⟺
J⁺(P,t)∩J⁺(Q,t) ≠ ∅** (a joint consistency condition can only ever
live at an event in both futures), vacuum class region = maximal
MUTUALLY coordinable set (clique) = comoving χ_h-ball ⇒
**L = R_h exactly**, particle horizon excluded by construction,
F-COST-1 (future-dependence) dissolved as kinematic bookkeeping.
The founder's Patch 3059 ruling re-attributed the construction:
A4 = PCD-cycle SYNCHRONIZATION only (infrastructure, not agent);
disconnection is kinematic; dark energy is EMERGENT from automaton +
synchronization + displacement rules — every mathematical result
surviving under strictly weaker assumptions. The founder also
supplied a NEW mechanism (M-ZBW-REBOUND): dilution lengthens the
per-Moment ZBW rebound ⇒ accelerating expansion; naive reading
ȧ ∝ a ⇒ w = −1 exactly.

**The 3060 data confrontation (no candidate tuned; all frozen
pre-confrontation):** against DESI DR2+CMB+PantheonPlus
(w₀ = −0.838 ± 0.055, w_a = −0.62; evolving-DE preference >3σ,
robustness contested): the derived chain (coefficient 1/8π +
L = R_h ⇒ c_Li = 1/√3) lands at (−1.289, +0.582) — 8σ marginal but
ON the degeneracy axis (~2σ-class along-axis), w(z) shape the mirror
of the DESI-preferred crossing: ALIVE BUT STRAINED; the June FITTED
c = 0.8 is now the WORST positioned (off-axis in the disfavored
direction); the founder rebound ≡ ΛCDM — exactly as alive as ΛCDM.
**The scale freedom is gone (clique theorem), so the entire
remaining question is ONE mode-counting coefficient (data-demanded
factor ≈ 1.92); deriving it from data is forbidden; the candidate
owner is the rebound mechanism's step structure.** F-CLI-1:
ARMED-SHARPENED. Disclosed corrections in the chain: the 3058 C4
scale/coefficient conflation (corrected 3060 §1); the June
A3′-vs-A4 fork identified as a false dichotomy (the error was the
"within a Hubble time" quantifier, not retardation).

## §2 — Questions (verdicts in this rule language only; Q1→Q4 order)

**Q1 (the construction).** Rule ADOPT / AMEND / REJECT on the
2-iii v0.2 coordinable-region construction as the arc's IR-scale
derivation: the J⁺∩J⁺ relation (grounded in "a joint consistency
condition can only ever live at an event in both futures"), the
MUTUAL-coherence (clique) step that fixes L = R_h exactly, the
particle-horizon exclusion, and the kinematic (post-3059)
attribution. If AMEND: name the step and the repair. If REJECT:
name the step that fails and whether any IR-scale derivation
survives.

**Q2 (R-BRANCH).** A self-consistent zero-Λ branch exists (χ_h
finite iff the future accelerates). Rule: (a) DEFERRABLE (branch
selection is an initial-conditions/inflation-exit question, outside
this arc's scope, register and proceed); (b) BLOCKING (the arc
cannot claim to explain Λ's value while the zero branch is
unexplained); (c) RESOLVED (state the resolution). State the
consequence of your ruling for the arc's grade.

**Q3 (the coefficient).** The mode-counting factor (data-demanded
≈ 2) is the arc's sole remaining quantitative freedom. Rule on:
(i) the legitimacy constraint as stated (the factor must be DERIVED
from the construction's mode structure — boundary condition of the
residual mode at the class-severance surface — or from the
M-ZBW-REBOUND step structure; extraction from expansion data is
forbidden); (ii) whether F-CLI-1 ARMED-SHARPENED (fires on
full-shape exclusion of the resolved-coefficient prediction) is the
correct falsifier posture; (iii) any candidate derivation route the
panel considers most promising or most dangerous.

**Q4 (arc grade).** Given Q1–Q3: rule the arc's standing —
(a) CONDITIONALLY SUPPORTED (unchanged); (b) PROMOTE to
derived-with-one-open-coefficient (name the conditions);
(c) DEMOTE (name the finding). State explicitly what promotion to
flagship + THEO grade requires (the worker's proposal: the
coefficient derivation + survival of the resulting full-shape w(z)
+ this panel's Q1 adoption).

## §3 — Notices (no verdict requested; challenge window open)

- M-ZBW-REBOUND registered as a founder mechanism (ID minting
  deferred to this round per DG-3 — the panel may recommend minting).
- The DM cross-link: R2 (uniform Sea must not gravitate while swirls
  do) rests on this arc; the K-MEM Route C campaign (running) is
  independent of every question above.
- The corrections chain is part of the record under review: 3058 C4
  conflation → 3060 §1 correction; the enforcement→synchronization
  re-attribution (3059) with the 3058 §0–§3 snapshot preserved.

## §4 — Materials (inline in the dispatch: the three decisive
records + this skeleton; repo pointers for the rest)

Inline: `condition2_2iii_a4_coordinable_region.md` (incl. §4 v0.2);
`fcli1_confrontation_record.md`;
`founders_voice/founder_ruling_nexus_role_and_zbw_rebound_de_2026-08-10.md`.
Pointers: stepA/B/C/D records; `condition2_2i/2ii` findings;
spin2_construction (op:einstein, 1121–1161); `1107/1108` monopole
records; `scripts/0722, 0723, 3058, 3059, 3060`.

## §5 — Round rules

CONV-001 single-block; Q1→Q4 dependency order; verdicts in the rule
language; founder ratification required for any enactment; single
round; returns adjudicated per the CONV-013/017 template.


===== DOCUMENT 2 of 4 — condition2_2iii_a4_coordinable_region.md (the construction, v0.2) =====

# Condition (2), sub-target 2-iii — THE A4 COORDINABLE-REGION CONSTRUCTION: the deep gate formalized; the boundary DERIVES to R_h exactly; the particle horizon excluded by construction; a new sharp falsifier (c_Li = 1)

**Arc:** OPEN-SR-5 ≡ OPEN-SM-6 (cosmological constant) · **Patch:** 3058
(10 Aug 2026) · **Sub-item:** the 1164 §"What remains" item 1 (the deep
gate) · **Verify:** `scripts/3058_a4_coordinable_region.py` (4/4)
**Type:** formal construction + numerical verification. Candidate
theorem content; NO THEO minted yet (DG-3 discipline: panel round
first). The arc remains conditionally supported pending the panel.

---

## §0 — What changed since June (why the gate opens now)

The 1164 verdict left "coordinable = ever-reachable" as a physical
argument awaiting a formal construction, with the particle-horizon
alternative live. Three ratified developments since supply what was
missing:
1. **The micro-chaos is MEASURED** (Patch 3034: ρ_micro ≈ 2.1–2.6 per
   Moment on the committed engine). Class-level uniformity of the
   vacuum is therefore NOT free — micro-states actively diverge — so
   an enforcement mechanism is REQUIRED, not merely available. June's
   "A4 is the natural candidate" becomes "an enforcer must exist."
2. **The partner-switching founder ruling** (Patch 3035): the
   REALIZATION CLASS is the conserved object while micro-identity
   churns. This is the missing vocabulary: the uniform vacuum is a
   realization CLASS, and A4's cosmological job is stated exactly —
   maintain a single vacuum realization class over its region.
3. **AP-4 (ratified 3032):** messengers carry {origin address, E, S}
   only — no coordination content; the messenger/computed-state
   division is exclusive and exhaustive. Class-maintenance across the
   lattice cannot ride on messengers: it is A4 **by elimination**
   under the ratified payload, closing 2-i's "neither used nor
   excluded" gap.

## §1 — The construction

**Definition (coordinability).** For lattice patches P, Q at Absolute
Moment t: **Coordinable(P, Q; t) ⟺ J⁺(P,t) ∩ J⁺(Q,t) ≠ ∅** — their
causal futures intersect. Grounding: A4 enforces global consistency
OF THE LOCAL c-BOUNDED RULES; a joint consistency condition on P and
Q can exist only at an event receiving influence from both; such an
event exists iff the futures intersect. Where no joint condition can
ever arise, there is nothing to enforce — by A4's own content, not by
an added postulate.

**Definition (vacuum class region).** A single vacuum realization
class extends over a MAXIMAL MUTUALLY coordinable set (every pair
coordinable — the clique condition; coordinability is not transitive,
and a shared class is a joint, pairwise-binding fact).

## §2 — The results (verify 4/4)

- **C1 (pairwise):** in flat FRW, Coordinable(P,Q;t) ⟺ comoving
  separation d ≤ 2χ_h(t), where χ_h(t) = ∫_t^∞ dt′/a(t′) is the
  comoving EVENT-HORIZON radius — futures reach χ_h each; they can
  meet in the middle iff d ≤ 2χ_h.
- **C2 (the clique step — kills the factor 2):** the maximal mutually
  coordinable sets are comoving balls of RADIUS χ_h (every internal
  pair ≤ 2χ_h; any exterior point breaks mutuality with the
  antipode). The proper IR scale of one vacuum class is therefore
  **L = a·χ_h = R_h EXACTLY.**
- **Particle horizon: excluded BY CONSTRUCTION.** Only futures enter
  the definition; past contact creates no future joint condition. A
  region seen long ago but now permanently receding shares no future
  event with us — it drops out of the class ("decoheres out of our
  vacuum," 1164's phrase, now a consequence rather than a picture).
- **F-COST-1: dissolved at construction level.** A4 enforces, at each
  event, whatever joint conditions arrive there — a present-tense,
  per-Moment operation. The class PARTITION (which regions ever share
  a condition) is bookkeeping over the self-consistently generated
  spacetime; no foreknowledge enters the dynamics. The
  future-dependence lives in the partition's definition, exactly as
  in the geometry of horizons generally.
- **C3 (finiteness + R-BRANCH, disclosed):** χ_h is finite iff the
  future expansion accelerates; a matter-only future gives χ_h = ∞ →
  unbounded class → ZERO residual. So a self-consistent zero-Λ branch
  exists alongside the observed nonzero-Λ branch; what selects the
  branch (plausibly initial conditions / the EU-1 inflation exit) is
  registered as **R-BRANCH**, an honest open question for the panel —
  NOT smoothed over.
- **C4 (the new sharp consequence — a falsifier, welcomed):** L = R_h
  exactly means the Li parameter **c_Li = 1**, whereas Step D's
  dynamics FIT c ≈ 0.8 (w_now ≈ −1.02). At c = 1 the HDE relation
  gives **w_now ≈ −0.885, evolving toward −1 (freezing, dw/da < 0)**.
  The construction thus REPLACES a fitted parameter with a derived
  value and thereby STAKES the arc on a measurable number. Next
  patch: map the derived 1/8π coefficient onto the Li convention
  (0722/0723 scripts) and confront current expansion-history data;
  if the data demand c ≈ 0.8 and exclude c = 1, the construction is
  WRONG in a way the fit never could be. Registered as **F-CLI-1**.

## §3 — Status and sequencing

The 1164 promotion path item 1 (the deep gate) now has a candidate
formal discharge: coordinable-region DEFINED from A4's own content,
boundary DERIVED = R_h, particle horizon excluded, F-COST-1 dissolved,
two honest residuals registered (R-BRANCH; F-CLI-1 pending the
coefficient-mapping confrontation). Next in order: (i) the
coefficient-mapping + data confrontation patch (F-CLI-1); (ii) the
1164 item-2 panel round on the WHOLE arc (A→D + 1161 + 2-i/2-ii/2-iii)
— natural to convene while Kila6 runs; (iii) if both survive: CC
flagship + THEO + swarm entry. Founder physical-picture ratification
of the construction is requested (the §1 definitions are physics, not
procedure — his lane).

---

## §4 — v0.2 (Patch 3059): RE-ATTRIBUTED under the founder's ruling — the mathematics survives intact; the mechanism simplifies; a new founder mechanism joins the discrimination

**The founder's correction** (verbatim at `founders_voice/
founder_ruling_nexus_role_and_zbw_rebound_de_2026-08-10.md`): A4 is
the SYNCHRONIZER of the PCD Moment cycle, not a coherence-enforcer;
causal disconnection is kinematic (the expansion increment between
two points exceeding signal propagation); dark energy is emergent
from the automaton + synchronization + displacement rules.

**What the correction changes — the attribution, and for the
better.** The June fork (1163/1164: "A3′ retarded vs A4 global") was
a FALSE DICHOTOMY. Step C's error was never that it used the
retarded primitive — it was the QUANTIFIER: it asked what can
equilibrate "within a Hubble time" (a rate criterion → R_H,
dynamically dead) instead of where reconciliation can EVER occur.
Under the founder's picture, the maintenance of one vacuum ground
level is ordinary local ZBW equilibration running on A4's
synchronized clock, reconciling any two patches wherever signals
from both can ever meet — and "wherever signals can ever meet" is
EXACTLY the relation J⁺(P,t) ∩ J⁺(Q,t) ≠ ∅ of §1. Every §2 result
therefore survives verbatim — C1 (d ≤ 2χ_h), C2 (the clique ball,
L = R_h exactly), the particle-horizon exclusion, C3 (R-BRANCH), C4
(c_Li = 1 / F-CLI-1) — because they are causal-structure kinematics,
and the founder's "increment of expansion exceeding signal
propagation" IS that kinematics in his own words, sharpened only by
the ever-quantifier (permanent disconnection = the event horizon;
instantaneous recession > c = the Hubble sphere, the scale the
dynamics already ruled out). What is DROPPED: the enforcement
language, the "A4-by-elimination" argument of §0(3), and the
F-COST-1 machinery's A4-a-causality crutch (no longer needed:
nothing global acts; the partition is pure bookkeeping over
kinematics). The construction is now STRICTLY WEAKER in assumptions
and identical in output — emergent, as the founder ruled: automaton
+ synchronization + displacement, nothing else.

**A4's retained (and necessary) role:** synchronization is what
makes "the same Absolute Moment, lattice-wide" — and hence a
well-defined per-Moment vacuum class — meaningful at all.
Infrastructure, not agent. The registry's minimal A4 wording is, in
retrospect, closer to the founder's picture than the 3058 §0/§1
enforcement reading was; corrected here with the anti-erasure trail
intact (§0–§3 stand as the frozen 3058 snapshot).

**The founder's new mechanism (M-ZBW-REBOUND) and the three-way
discrimination.** The rebound-lengthening mechanism — dilution ⇒
longer per-Moment ZBW rebound ⇒ accelerating expansion — is a
substrate SOURCE for the residual itself, and its naive quantitative
reading (increment ∝ spacing ⇒ ȧ ∝ a) is a pure de Sitter term:
**w = −1 exactly** (verified, `scripts/3059_zbw_rebound_desitter.py`).
The data confrontation (next patch) now discriminates THREE
candidate readings: the Step-D fit (c ≈ 0.8, w_now ≈ −1.02), the
R_h-cutoff derivation (c = 1, w_now ≈ −0.885 freezing), and the
founder mechanism (w = −1 exact). These are distinguishable at
current survey precision; whichever survives, the arc gains either a
derived parameter or a refuted construction — and the R_h-cutoff and
rebound mechanisms may yet be one thing seen from two ends (the
rebound as the microscopic realization whose IR regularization is
the R_h mode cutoff): flagged for the panel, not claimed.


===== DOCUMENT 3 of 4 — fcli1_confrontation_record.md (the data trial) =====

# F-CLI-1 CONFRONTATION RECORD — THE THREE-WAY TRIAL AGAINST CURRENT EXPANSION DATA: the 3058 C4 conflation corrected; the derived chain's true position computed; the whole question localized in ONE derivable coefficient

**Patch 3060 (11 Aug 2026). Verify:
`scripts/3060_fcli1_confrontation.py`. Data: DESI DR2 BAO + CMB +
PantheonPlus (w₀ = −0.838 ± 0.055, w_a = −0.62 +0.22/−0.19;
arXiv:2503.14738 era), with the >3σ evolving-DE preference noted AND
its contested robustness noted (arXiv:2504.15222: dataset tensions;
independent-dataset analyses weaken the claim). No model was tuned to
these numbers; the candidates were frozen at Patches 0722–0723 (fit),
3058 (derivation), and 3059 (founder mechanism) before this
confrontation.**

## §1 — Correction first (the 3058 C4 conflation, on the record)

3058 C4 stated "L = R_h ⇒ c_Li = 1." That CONFLATED two independent
statements. The Li parameter bundles the IR scale AND the energy
coefficient: ρ_Λ = 3c_Li²c⁴/(8πG L²). The arc's derived chain —
Step C's coefficient (1/8π)ρ_P(l_P/L)² together with the 3058 clique
theorem L = R_h — gives ρ_Λ = c⁴/(8πG R_h²), i.e. **c_Li = 1/√3 ≈
0.577**, not 1. The geometric result (L = R_h exactly) stands
untouched; the C4 w-prediction is superseded by §2. Worker's error,
caught by doing the mapping the 3058 record itself demanded before
any data claim.

## §2 — The triangle, placed (Ω_Λ = 0.685; CPL projections)

| Candidate | (w₀, w_a) | marginal tensions | off-degeneracy Δw_a |
|---|---|---|---|
| **Derived chain** (1/8π + L = R_h; c = 1/√3) | (−1.289, +0.582) | 8.2σ / 5.9σ | **−0.02 — ON the contour axis** |
| **Step-D fit** (c = 0.80) | (−1.023, +0.333) | 3.4σ / 4.7σ | +0.45 — off-axis |
| **Founder rebound** (naive: ȧ ∝ a) | (−1.000, 0) ≡ ΛCDM | 2.9σ / 3.0σ | +0.18 |

Readings, stated with all the honesty they need:
- **The derived chain's CPL point lies almost exactly on the DESI
  degeneracy axis** (the poorly constrained direction): its
  along-axis distance from the SNe-anchored centroid is ~2σ-class,
  NOT the 8σ the marginal suggests. However its w(z) SHAPE is the
  MIRROR of the DESI-preferred crossing (quintessence past → phantom
  now, crossing w = −1 near Ω_Λ = 1/3, z ≈ 0.7; DESI prefers phantom
  past → quintessence now). CPL projection flatters it; full-shape
  fitting would discriminate. Verdict: **ALIVE BUT STRAINED — and
  only meaningful once the §3 coefficient question resolves.**
- **The Step-D fitted c = 0.80** — the June configuration — is now
  the WORST positioned of the three: significantly off the
  degeneracy axis in the direction the data disfavor. The fit that
  looked good against w ≈ −1-era data does not survive the DR2
  landscape as well as either principled candidate. A fitted
  parameter aging worse than the derived ones is exactly the
  pattern a real framework hopes for.
- **The founder rebound is precisely as alive as ΛCDM** (its naive
  form IS Λ): the 2.9σ tension is the famous DR2 evolving-DE
  preference, whose robustness is contested in the literature. If
  that preference is systematics, the rebound is comfortable; if it
  is real, every constant-Λ mechanism shares the wound equally.

## §3 — The entire remaining question is ONE coefficient (and it may already have an owner)

The clique theorem removed the scale freedom that made June's "~2×"
ambiguous. The data-demanded move from the derived chain to the
viable region is a factor (0.80/0.577)² = **1.92 ≈ 2 in ρ_Λ** — the
same ~2× the June records carried, now localized ENTIRELY in the
mode-counting coefficient: what boundary condition does
class-severance impose on the residual mode of the R_h ball, and
what is that mode's energy? Deriving the factor FROM THE DATA is
forbidden (the post-hoc trap this record exists to prevent); it must
come from the construction's own mode structure — and the natural
candidate owner is the FOUNDER'S REBOUND MECHANISM (3059): if the
rebound is the microscopic source of the residual, its per-Moment
step structure fixes the effective mode normalization, potentially
DERIVING the coefficient and unifying the two candidates into one
mechanism with one prediction. That derivation — not more
data-fitting — is the arc's next real work, and it goes to the panel
as an open question, not an answer.

## §4 — Disposition

F-CLI-1 does NOT fire today: the naive derived chain is strained but
not excluded (degeneracy-axis position; contested DDE preference),
and the falsifier's honest trigger is the resolved-coefficient
prediction, which does not exist yet. Registered status:
**F-CLI-1 ARMED-SHARPENED** — it fires if, after the mode-counting
derivation, the resulting w(z) is excluded by full-shape fitting.
Next: the CC whole-arc panel round (the 1164 promotion-path item 2),
carrying Steps A–D + 1161 + 2-i/2-ii/2-iii(v0.2) + the 3059 ruling +
this confrontation. The arc remains conditionally supported.


===== DOCUMENT 4 of 4 — the founder ruling (verbatim record) =====

# FOUNDER RULING — THE NEXUS'S ROLE (SYNCHRONIZATION, NOT ENFORCEMENT) + THE ZBW REBOUND-LENGTHENING MECHANISM FOR DARK ENERGY

**Patch 3059 (10 Aug 2026). Context: the worker's Patch 3058 A4
coordinable-region construction asked the founder to ratify the
physical picture ("the Nexus binds what can still meet, releases what
never can"). The founder's reply is a partial correction + a new
mechanism. Registered verbatim per the founders_voice discipline.**

## §1 — The founder's statement (verbatim)

> I think you're asking me if the Nexus' job is creating and
> enforcing the ball of the observable universe.
>
> * My concept of the Nexus is that its role is the
>   synchronization/simultaneous triggering of the PCD Moment cycle
>   of all GPs.
> * I conceive of the physical universe as the totality of all CPs,
>   each on GPs, each on their own GP or momentarily superimposed on
>   another GP until it moves due to ZBW attraction to its
>   environment.
> * The physical universe is composed of major divisions, such as
>   the DP Sea, Dark Matter, and other DP aggregates as chains and
>   other configurations; baryonic mass, plasma, and atomic mass.
> * The physical universe was given its impulse of expansive radial
>   trajectory by the Big Bang. The ZBW environmental force and
>   cycle introduce tangential velocity, resulting in a radially and
>   tangentially expanding universe that is gradually decreasing in
>   density.
> * The rate of expansion will increase because the decrease in the
>   universe's density will result in a ZBW oscillation with a
>   greater delta distance on the rebound toward the environment due
>   to the larger distance from each Moment of superimposition.
> * I think the light-speed disconnection between locations in the
>   universe is simply due to the increment of expansion between two
>   points in the universe exceeding the speed of signal propagation
>   through the DP Sea between those points. I don't think we need
>   the Nexus to enforce the separation of domains. I think Dark
>   Energy is an emergent phenomenon arising from the Atomata, the
>   Nexus Moment Synchronization, and the Axiomatic rules of
>   displacement.

## §2 — Rulings extracted

- **R-A4-ROLE:** A4's role is the SYNCHRONIZATION / simultaneous
  triggering of the PCD Moment cycle of all GPs (PCD = Perceive,
  Compute, Displace). It is NOT a coherence-enforcement or
  domain-separation agent. The 3058 construction's
  enforcement-attribution is corrected.
- **R-DISCONNECT:** causal disconnection between regions is
  KINEMATIC — the expansion increment between two points exceeding
  signal propagation speed through the Sea. No Nexus mechanism is
  needed for separation.
- **R-DE-EMERGENT:** dark energy is emergent from the automaton +
  Nexus Moment synchronization + the axiomatic displacement rules.
- **M-ZBW-REBOUND (new mechanism, registered):** as the universe's
  density decreases, the ZBW oscillation rebound covers a greater
  delta distance per Moment (larger distance from each Moment of
  superimposition) — an expansion-rate INCREASE sourced by dilution
  itself. Naive quantitative reading (worker's, disclosed): per-Moment
  expansion increment ∝ current spacing ⇒ ȧ ∝ a ⇒ a de Sitter-like
  term, w = −1 exactly (verified `scripts/3059_zbw_rebound_desitter.py`).
  ID minting deferred to the panel round per DG-3 discipline.


===== END OF DISPATCH — return verdicts in the §2 rule language, Q1→Q4, then §3 challenges if any =====

# D-DS-RELIC OPENED AND SPINED — the expansion-history route for d_s: the exit-anchor mapping puts the campaign's working decade at Planck-adjacent exit temperatures; the COMOVING-dilution branch is dynamically EXCLUDED by an exact +2/3 equation-of-state displacement (0723-precedent shape adjudication, no magnitude used); the FROZEN branch requires a freeze mechanism, for which the fidelity-boundary transition — the founder's own FQ-5.2 transition-zone picture — is the natural zero-parameter candidate; consonance flag T-CC-CONT-1 raised; FQ-7 posed

**Patch 3098 (12 Aug 2026). Executes the queue's FQ-5.2 item ("d_s
via expansion history," R-DS-EVOLVE) under the canonical anchoring
declared at A-1 (Patch 3097: d_s ≡ the mean inter-UNIT nn spacing).
Verify: `scripts/3098_ds_relic_mapping.py` (all gates PASS; the 0723
w_now = −1.02 reproduced as the frozen-branch regression gate).
Anti-extraction intact: NO ρ_Λ value is computed anywhere in this
note; the only confrontation is the equation-of-state SHAPE, per the
D3/0723 precedent that excluded the Hubble radius on no-acceleration
grounds. No branch is adopted; the founder rules FQ-7.**

## §1 — Corpus-forced inputs (nothing new assumed)

1. **Expansion = Sea dilution on the fixed GP scaffold** (founders
   L33, Patch 0731). A conserved CP inventory in a comoving volume
   dilutes ∝ a⁻³ by construction — dilution IS the expansion.
2. **CP permanence** (corpus-standing; 3089's forward input).
3. **The EU-1 graceful exit lands at occupancy n̄ = 1** — one CP per
   FINE GP (0750/0751: μ ∝ ln n̄ → 0 at n* = 1 is the exit itself).
   The exit state IS full occupancy at the fine scale; dilution
   thereafter takes the Sea below it — R-OCC-DILUTE's dilute Sea is
   the exit state's forward continuation, not a separate postulate.
4. **The nested resolution** (0736): l_P ≈ N_fine ~ 10³⁰ fine GPs.
5. **R-QDP-CASCADE:** the weave forms and the reservoir FREEZES-IN at
   a cooling threshold (which threshold, in spacing terms, is §4).
6. **R-DS-EVOLVE:** d_s is a cosmological relic; isotropy evolved,
   with the founder's radial-vs-tangential transition-zone picture.
7. Thermal kinematics a ∝ 1/T, with the full-history g_* drift an
   honest ~×3 systematic on the mapping (recorded, not resolved).

## §2 — The kinematic spine and the exit-anchor mapping

Post-exit comoving dilution from one-CP-per-fine-GP gives
d(t) = 1 fine GP × a(t)/a_exit, hence

  **d_s(today)/l_P = (T_exit/T₀)/N_fine.**

One input is missing and it is a NAMED EU-1 debt (0738: reheating):
the exit temperature. The mapping, tabulated forward with no
adoption (script §A): T_exit = T_Planck → d_s ≈ 52 l_P; 3×10¹⁸ GeV →
12.8 l_P; 10¹⁸ GeV → 4.3 l_P; GUT 10¹⁶ GeV → 0.04 l_P; 10¹⁰ GeV →
4×10⁻⁸ l_P. Inverse: **the self-consistency campaign's working
decade d_s ∈ [6, 16] l_P corresponds to T_exit ≈ 1.4–3.8 ×10¹⁸ GeV —
Planck-adjacent exit**, which is the EU-1 Branch-V picture's own
regime (early high c_eff; horizon solved without a low reheat gap).
Recorded as a CONSONANCE, not an adoption: the working decade was
chosen for array tractability, not ruled; the consonance says the
relic arithmetic and the campaign's exploratory regime are mutually
compatible under Planck-adjacent exit, and says nothing else.

## §3 — The dynamical adjudication (shape, not magnitude)

The relic reading forks on what happens after the weave binds:

- **COMOVING-d:** inter-unit dilution continues with expansion,
  d ∝ a. Then ρ_Λ ∝ 1/(a²R_h²) and, EXACTLY and background-
  independently, **w_com(z) = w_frozen(z) + 2/3** (the a⁻² factor
  displaces the equation of state by +2/3, identically).
- **FROZEN-d:** the weave's binding freezes the spacing; ρ_Λ ∝ 1/R_h²
  — the already-adjudicated Li form (0723; self-consistent solution,
  c_Li ≈ 0.8, w_now ≈ −1.02, reproduced as the script's regression
  gate to ±0.003).

Computed on the self-consistent Li evolution (script §B): the frozen
branch runs w = −1.02 → −0.75 → −0.51 over z = 0 → 1 → 5; the
comoving branch is that curve displaced to **w_now = −0.356** —
sitting at the zero-acceleration class (−1/3) and displaced from the
observed w₀ = −1.03 ± 0.03 by a many-σ margin. **Adjudication, in
the D3/0723 class (the same move that excluded the Hubble radius):
the COMOVING branch cannot source the observed accelerating history
and is dynamically EXCLUDED.** The full-shape fit (the F2 falsifier
channel) is the formal confrontation if this exclusion is ever
contested; no magnitude entered the adjudication.

## §4 — The surviving structure and the zero-parameter candidate

The relic reading survives ONLY with a freeze: dilution operated
historically (R-DS-EVOLVE's evolution, M-ZBW-REBOUND concrete) and
halted when the weave bound. The freeze scale then needs a mechanism,
and the corpus supplies exactly one natural candidate: **the
partner-fidelity boundary** — the spacing at which switching gives
way to monogamy, the founder's own FQ-5.2 transition-zone language,
and the quantity the Stage-2/3 machinery already brackets
(d_s* ≈ 6–7 l_P at the campaign's operating points). Under this
reading, FQ-5.2's original option (a) is realized as the ENDPOINT of
the relic history rather than its rival: **the Sea diluted until it
reached the fidelity boundary and froze there — d_s = d_s*, derivable
from the delay/exchange dynamics with zero parameters.** Consistency
(script §C): under Planck-adjacent exit the comoving history reaches
d* at z_f ≈ 0.8–7.7 — a late-universe freeze epoch, which would leave
w(z) imprints at z > z_f as a refinement of the F2 channel. Under
T_exit ≲ 10¹⁸ GeV the history has not yet reached d* and a
boundary-freeze lies in the future — those anchors are incompatible
with a present frozen-at-boundary Sea and would need a different
freeze mechanism. All of this is STRUCTURE, posed; none of it is
adopted; and the open physical question it exposes is §6's FQ-7.1:
what carries expansion after the weave freezes, on a picture where
dilution IS expansion.

## §5 — Consonance flag T-CC-CONT-1 (raised per the T-flag protocol)

R-CC-DIST-CONT (3093, verbatim: "The CC effect of increased distance
between qDP-eDP species will continue to be operative") reads, in its
strongest present-tense form, as COMOVING-d — which §3 excludes
dynamically. Candidate readings, for the founder to rule among or
beyond: (i) the ruling's context was the composition change (the
weave substitution), and "continue to be operative" means the
distance-based MECHANISM survives the composition ruling — silent on
time-dependence; (ii) the distance increase operated historically and
set today's frozen d_s (the §4 reading); (iii) ongoing dilution is
intended, in which case the +2/3 displacement arithmetic must be
answered head-on. Per the protocol the flag is raised on consonance
grounds, the arithmetic attached, and the founder rules — exactly the
T-DILUTE-1 pattern that reopened SCR-1.

## §6 — FQ-7 (posed; composition and kinematics only; no ρ_Λ content
attaches to any branch)

- **FQ-7.1 (the freeze):** after the weave binds, does inter-unit
  dilution continue with expansion, or does the electric binding
  freeze the spacing? If frozen — on the picture where dilution IS
  expansion (L33) — what carries expansion afterward: the transient
  exchange population (R-ZBW-EXCHANGE) as the diluting component, a
  long-wavelength mode of the weave itself, or something else in the
  vision? (T-CC-CONT-1 asks how R-CC-DIST-CONT's "will continue to be
  operative" is meant.)
- **FQ-7.2 (the freeze scale):** is the freeze scale the partner-
  fidelity boundary — the transition zone of your FQ-5.2 answer where
  switching gives way to monogamy? If yes, d_s is derivable from the
  delay/exchange dynamics with zero parameters, and the Stage-2/3
  machinery already brackets it.
- **FQ-7.3 (the exit anchor):** in the vision, does the hot start sit
  at the Planck-adjacent scale (EU-1's early-high-c_eff picture, no
  reheat gap), or at a lower reheat scale? This anchors the §2
  mapping and the §4 freeze-epoch consistency; it is EU-1's named
  reheating debt, now load-bearing for the CC lane.

## §7 — Ledger

**D-DS-RELIC status: SPINED, TWO-BRANCH, ONE BRANCH EXCLUDED.**
d_s = [frozen-at-boundary: d_s* ≈ 6–7 l_P, zero-parameter, pending
FQ-7.1/7.2] or [another freeze mechanism, pending the founder]; the
comoving branch is dynamically excluded (§3) unless its arithmetic is
overturned. The exit anchor (FQ-7.3) is a consistency input, not the
d_s source, under the boundary-freeze reading. φ₁'s canonical
variable (A-1) is the variable this derivation targets; the
2^{1/3} conversion discipline binds. Factors multiply once, at the
end; in band or F-CLI-1 fires in those words. Kila6 Route C and the
DM ledger untouched.

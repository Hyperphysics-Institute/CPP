# MACHINE CONFIRMATION + THE STATIONARITY AUDIT CLOSED CLEAN — the canonical `analyze fb` matches the 3171 record on every quantity (loc/depth agree to ≤ 0.0002, transcription rounding; verdict identical: FINITE-SIZE-EFFECT); var_fb now reported and it DECREASES monotonically with size (2.33e-4 → 5.2e-5), Gemini's susceptibility proxy showing NO divergence — consistent with FINITE-SIZE-EFFECT and disturbing nothing; **and the long-owed stationarity audit returns CLEAN ACROSS ALL 44 CELLS (stat ∈ [0.9955, 1.0058]), including 11:2.0 at 0.9994/1.0005 — the equilibration-suspect flag raised at 3155/3157 is CLOSED, REFUTED by its own audit**, and the 4× seed spread at that cell is re-attributed: the anomaly's minimum has migrated to ≈ 2.09 at n = 11, so d_s = 2.0 now sits on the feature's STEEP LEFT FLANK, where f_b is geometrically sensitive to seed — a benign consequence of the very migration the verdict established, not a sampling defect

**Patch 3172 (17 Aug 2026). Closes the machine-confirmation
obligation of 3171 and the stationarity item carried in the pending
queue since 3155.**

## §1 — Confirmation

Machine `analyze fb` vs the 3171 record: loc(n) and depth(n) match
to ≤ 0.0002 at every size (hand-transcription rounding); both gates
ADEQUATE; verdict identical in the frozen words. The record stands
confirmed. New in the machine output: **var_fb(n) = 2.334e-4,
1.658e-4, 1.432e-4, 6.971e-5, 5.234e-5** — monotone DECREASING. A
fixed physical transition sharpening with size would show this proxy
growing at the feature; it shrinks instead. Third independent
first-moment indicator pointing the same way; changes nothing,
strengthens the same verdict.

## §2 — The stationarity audit: CLEAN, flag CLOSED

All 34 n = 10 cells and all 10 n = 11 cells: stat ∈ [0.9955,
1.0058], indistinguishable from every healthy cell this week. The
specific suspect — 11:2.0 — reads 0.9994/1.0005, as clean as any
cell in the corpus. **The equilibration diagnosis (T = 3000 fixed
while relaxation grows; raised as "suspect" at 3155 §1 and carried
at 3157 §3) is REFUTED at the sizes measured**, within what this
proxy tests (late-window vs mid-window energy stationarity).

**Re-attribution of the 11:2.0 anomalies, now evidence-based:** at
n = 11 the minimum sits at ≈ 2.090, so grid-2.0 lies on the
feature's steep left flank. Flank cells are geometrically
seed-sensitive in f_b (a small configuration difference moves the
local slope, not the equilibrium) — which is why the SAME cell
showed 4× f_b seed spread while its stationarity is pristine. The
n = 11 Binder excursion at 2.0 (+0.46) stays exactly where 3157 §3
left it — noise-floored, fourth-moment, retired route — and needs no
new explanation.

## §3 — Board

The 3166 verdict rides into the next bundle (no round, per its §6).
DE lane state: the anomaly retired as fixed physics; the frozen
2.450 beside its sustained challenger 2.644; calibration intact;
F-W-1 live; **D-JITTER-1 Phase B is the lane's next executable item**
(fresh window). VideoCPU: FREE. Kila6: shutdown isolation, then the
β-ladder (~4 days) — its completion outranks everything and gets its
own round.

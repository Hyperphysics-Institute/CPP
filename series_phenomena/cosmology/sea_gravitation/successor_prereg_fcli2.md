# SUCCESSOR PREREGISTRATION (F-CLI-2) — the memoryless campaign, frozen BEFORE execution

**Patch 3110 (13 Aug 2026). Per the 3108 successor protocol and
R-CP-MEMORYLESS (3109). Committed before any run; the 3107 verdict
stands untouched as the record of the inertial-instrument assembly.**

## §1 — The ruled instrument (memoryless; no regularization anywhere)

First-order dynamics, exactly the ruling: X_{t+1} = X_t + SSV_net(t),
with SSV_net = [instantaneous non-partner Coulomb sum (standard
signs) + retarded partner attraction × G_bond + arc term + jitter
field σ_n], evaluated fresh each Moment, NOTHING carried. Species:
mixed pairs, G ∈ {1, 52.94} (3103 k², partner-internal). Arc term:
field sources carry v_eff = the source's PREVIOUS displacement
(field content, not CP memory; DI-bits encode source state), clipped
at c for the FIELD only (DI-bit speed — physics, not regularization).
Jitter σ_n = 0.30 charge-coupled (R-DWELL-1 relaunch semantics
unchanged). Swaps: the founder perigee rule unchanged. Positions
continuous (fine-GP resolution per 0736; the coarse rounding of the
inertial arrays is retired with them). Periodic box, n_side = 3,
T = 3000. NO velocity variable exists in this instrument — the
stiff-bond catastrophe class is structurally impossible.

## §2 — Cells

- M1, M2: d_s = 8, seeds {5, 11}, mixed
- M3: d_s = 12, seed 5, mixed
- MB1–MB3: d_s ∈ {3, 5, 7}, seed 5, pure-e (boundary location,
  memoryless)
No γ (the brake was an inertial-instrument organ; a memoryless map
needs none — one fewer dial, declared).

## §3 — Read-outs: the 3104 §3 definitions VERBATIM

r, x_q, η_z^gas, s_meas, a_C4 (same estimator; same pathology escape
to the A-1 bracket [0.78, 1.35]), boundary verdict. Stationary
window = final third. Phase classifier: FAITHFUL = bound
(η_all < 1.2) + regenerating (≥ 5) + stationary ⟨d²⟩ (final-third
mean within ×[0.5, 2] of the middle third).

## §4 — The successor assembly and F-CLI-2

The 3107 fold VERBATIM (same formula, same mappings, same /4, same
C fork [53.9, 68.5], same d_s bracket rule: [3, 7.5] unless the MB
cells LOCATE the memoryless boundary, in which case d_s = located
value ± 1 grid step — the location rule declared NOW), inputs
replaced by the memoryless measurements ± SE. Band frozen:
c_Li ∈ [0.6, 0.9]. **F-CLI-2: in band or FIRES, in those words.**
Escape clauses as 3104 §4: pathological read-out → declared bracket,
stated. No post-hoc anything.

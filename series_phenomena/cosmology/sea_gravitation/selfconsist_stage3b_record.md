# D-SEA-SELFCONSIST STAGE 3b — THE REGULATED CLOSED ARRAY: the faithful phase NOT YET EXHIBITED; the diagnosis points at the continuum approximation, and Stage 3c is specified on the lattice

**Patch 3083 (12 Aug 2026). The pump/brake balance implemented with
everything ruled: retardation pump (R-ZBW-DELAY), entropic brake
(R-CP-ENTROPIC-LOSS, retention γ scanned pending D-ARC-GAMMA), the
R-JITTER-SOURCE noise floor (granularity unruled ⇒ scanned across two
decades), the founder's perigee switch rule at the poach scale, and
per-Moment field rebuild (R-GP-REZERO). Annealed start. Verify:
`scripts/3083_selfconsist_stage3b.py`. No band quantity anywhere.**

## §1 — Results

Closed deterministic array (no noise): GLASS at every γ ∈ [0.5, 0.95]
(η ≈ 3.7–4.6 — ABOVE the gas value 2.25: anti-correlated stuck
labels; regenerations ≈ 0). With the ruled noise floor σ_n ∈
{0.003, 0.03, 0.3} and the corrected switch rule: still GLASS/OTHER
at d_s ∈ {8, 16}, γ ∈ {0.5, 0.9} — a stationary ionic liquid with
label churn (switches high, superposition passages ≈ 0, v² flat).

**The faithful DP phase — pairs cycling through superposition — has
not been exhibited in the explicit array.** Phases exhibited to date
across the program: HOT GAS (undamped, Stage 3), GLASS/ionic liquid
(damped, Stage 3b), COLLAPSED/frozen (noiseless mean-field, Stage 2 —
retired as unphysical by R-JITTER-SOURCE).

## §2 — The diagnosis (an integrator indictment, not a Sea property)

The superposed fraction is ZERO at every setting because continuous
3D dynamics makes exact co-location a measure-zero target behind an
angular-momentum barrier (the classical Coulomb capture problem),
compounded by the r ≥ 1 force floor weakening close-range binding.
But the RULED kinematics is not continuous: **R-STEP-SSV moves a CP
by a discrete GP-ADDRESS JUMP, which can land exactly on the
partner's GP.** Superposition is natural on the lattice and nearly
impossible in the continuum approximation of it. The faithful DP
phase, if it exists, plausibly lives ONLY on the lattice — an
appropriate demand for a discrete-substrate theory.

## §3 — Stage 3c specification

Re-run the regulated closed array with positions quantized to the GP
lattice each Moment (the address-jump reading of R-STEP-SSV taken
literally: X ← round(X + V) on the lattice), retaining the
retardation, γ scan, σ_n floor, perigee switching, and per-Moment
rebuild. Success criterion unchanged and pre-stated: a phase with
recurrent superposition passages, bound η ≪ 2.25, small f_sw, flat
v² — or its absence, reported either way. D-ARC-GAMMA (the geometric
derivation of γ from the 10% shell + return cross-section) remains
its own queued derivation; γ-insensitivity remains the test that
decides whether its numerical ambiguity ever mattered.

## §4 — Ledger (unchanged by this patch)

φ₁ REOPENED (d_s via expansion history / lattice-phase results) ·
φ₂ = 1 CLOSED · φ₃ ≈ 0.2-class from Stage 2, conditional on the
regulated faithful branch, now awaiting its lattice exhibition ·
φ_comp OPEN. Factors multiply once, at the end.

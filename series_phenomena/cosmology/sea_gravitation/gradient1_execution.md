# **GRADIENT-1 EXECUTED — DEAD at F-G1: the mean expansion source is arrangement-INVARIANT at the level CPP has derived. Eight-seed Monte Carlo: clumping changes the LOCAL field variance 10× while the surface-averaged flux stays exactly Gaussian-invariant (mean z = −0.13 ± 0.24, no bias); the corpus audit finds no derived invariance-breaking mechanism — the weak-field map is linear (𝓕 → 0, reducing to linearized Einstein), the strong-field nonlinearity lives only at GM/rc² ~ 1 (negligible cosmological volume), and ground-state-reference dynamics are 3125-blocked. The worker's session-opening pitch conflated field-level gradient structure with mean-level sourcing; Step A's shell-broadcast aggregation had already fixed the mean. The route's author proposed it; the route's author's execution killed it**

**Patch 3433 (26 Aug 2026). DE lane. Verify:
`verify_3433_gradient1.py` + `verify_3433_output.txt`. Status in
`frontier_sectors/DMDE.md`.**

## §1 — G1 result (numeric)

Same enclosed mass, uniform vs strongly clumped arrangement (25
clusters, σ = 0.02R), 4000 masses, 40000 surface samples:
surface-averaged inward flux matches −4πGM_enc in both arrangements;
the single-seed arrangement difference is 0.94σ of its own MC error,
and the eight-seed standardized-difference campaign gives
**mean z = −0.13 ± 0.24 — no systematic effect**, while the LOCAL
surface-field standard deviation grows **10×** under clumping. The
field-level/mean-level distinction is thereby exhibited directly:
clumping restructures the gradients violently and moves the mean
source not at all. This is Gauss's theorem operating inside Step A's
shell-broadcast aggregation — M_enc invariance, exact.

**Verify-script provenance, recorded honestly:** the first run
reported a false SURVIVAL: the criterion scaled the arrangement
difference against the UNIFORM case's MC error, 10× too small for
the clumped case. A leakage hypothesis (clusters escaping the Gauss
surface) was then tested and REFUTED — no point ever left the
sphere; the deviation was pure noise under a mis-scaled error bar.
The corrected per-arrangement error and the multi-seed campaign
produce the null above. A route can be falsely kept alive by an
error bar as easily as falsely killed; both failure modes are now in
the lane's record.

## §2 — G2 result (corpus audit)

No derived mechanism breaks M_enc invariance at cosmological
potentials:
- **Weak field (|Φ|/c² ~ 10⁻⁵, all cosmological structure):** the
  CPP field equation's nonlinear PSR feedback term 𝓕 → 0, reducing
  exactly to the linearized Einstein equations (GR-1c development
  record; GR Companion I). Linear map ⇒ superposition ⇒ the mean
  source is the mean density. Nothing CPP-specific survives
  averaging.
- **Strong field:** the nonlinearity is real (exact isotropic
  Schwarzschild) but confined to GM/rc² ~ 1 neighborhoods —
  cosmologically negligible volume fraction; its volume-averaged
  contribution is nil.
- **Ground-state-reference dynamics** (the Step-B hook: local Sea
  re-equilibration under structure formation changing the ΔSSV
  zero-point): the founder's own 3125 ruling (R-CC-CALIBRATE)
  declares the Sea's equilibrium not first-principles-computable at
  current knowledge. Not derived, not derivable now, and inventing
  it would be an overlay of exactly the kind 3422 demoted.
- **Inherited residual:** standard GR second-order backreaction,
  literature-scale ~10⁻⁵ of the required effect and in any case not
  CPP-distinguishing.

## §3 — Verdict and diagnosis

**F-G1 (frozen at 3432): DEAD.** Neither survival condition met. The
session-opening pitch ("gradient support concentrates into collapsed
regions; voids lose source; deceleration weakens") was wrong at its
premise: CPP's gravity is gradient-SOURCED at field level, but Step
A's aggregation makes the cosmological mean couple to enclosed
excess mass, which mass-conserving rearrangement cannot change. The
"why now" timing virtue was real but attached to a mechanism of zero
magnitude. Symmetry note for the lane's record: 3430 killed the
founder's route by the founder's own postulates; 3433 kills the
worker's route by the worker's own execution. The falsifiers are
running the lane, not its authors.

## §4 — The DE lane's position after 3433

Seven routes closed honestly (four energy-density → Λ-like; varying-c
→ F1; F-W-1 → demoted; GRADIENT-1 → F-G1). **One chartered route
remains in the entire lane: the open/comoving instrument build** —
which would test whether the constant-Sea-density result
(w = −1.00 ± 0.02) survives true expansion rather than the fixed-box
proxy flagged as the weak link at four separate points in the
3400–3419 arc. It is a confirmation-or-refutation instrument for the
lane's standing Λ-like conclusion, not a discovery route for
dynamical DE. Build cost is Kila6-class compute requiring DM-lane
coordination. Worker ruling under PD-006: the build SPEC is the DE
lane's next chartered work item; the compute commitment decision
accompanies the spec, not this patch.

## §5 — Fence

Nothing touches Λ, d_s^emp = 4.636, the 3400–3419 frozen
conclusions, 3422, 3423, 3429, 3430, or 3431. Next patch (DE): 3434.

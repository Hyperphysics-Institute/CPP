# THE CALIBRATION EXECUTED (R-CC-CALIBRATE) — d_s^emp = 4.64 l_P (arrangement band [4.07, 5.42]): the measured Λ locks the Sea's equilibrium spacing at 1.9× the plasma floor — squarely in the bound weave, exactly the ruled "medium between the two extremes"; the c(d) inversion is monotone and well-conditioned (Φ plateaus at ~55, so the anchor is nearly geometric); first back-engineered Sea constants delivered; the D-EQUIL-KINETICS success criterion fixed forever: reproduce 4.64 with zero reference to Λ

**Patch 3126 (14 Aug 2026). Executes the ruling's programme: the
fold inverted, Λ entering ONLY as the anchor c_Li = 0.8. Verify:
`scripts/3126_calibration.py` + sweep JSON. Instrument: the
certified 3124 run_cell, unchanged; n = 5, both seeds, six
spacings.**

## §1 — The sweep and the lock

c_Li(d), with inputs measured AT each spacing: 1.473 → 1.223 →
1.042 → 0.920 → 0.819 → 0.678 across d_s = 2.6 → 5.5. Monotone,
unique crossing:

**d_s^emp = 4.636 l_P** (a = 1.0; arrangement band [4.071, 5.422];
α_s ± 10% moves the crossing by ≈ ∓0.3).

Robustness note: Φ PLATEAUS at 54–56 across the whole 3.5–5.5
range — the species/excursion factors self-compensate, so the
inversion is nearly geometric (c ≈ const/d) and the anchor is
well-conditioned: the calibrated spacing is insensitive to the
detailed statistics precisely where it lands.

## §2 — What the calibrated Sea looks like

At d_s^emp: r_e = 0.449 (the vacuum is 45% transient at any
Moment — the founder's exchange reservoir is nearly half the Sea),
x_q = 0.595, η_e = 0.115, s = 1.18, η_gas = 4.79. **Floor offset:
d_s^emp − d_s* = +2.19 l_P — the Sea lives at 1.89× its melting
spacing,** comfortably in the bound weave, kinetically balanced
exactly as R-DS-EQUILIBRIUM describes. This offset is now a
MEASURED PROPERTY and the standing target: **D-EQUIL-KINETICS
succeeds on the day the four-rate algorithm reproduces
d_s^emp = 4.64 with zero reference to Λ** — the criterion is fixed
today, before any such derivation exists.

## §3 — Consonances and the live channel

- **3098 exit-anchor mapping:** d_s^emp → T_exit ≈ 4.6 × 10³⁰ T₀ —
  same order as the 3098 working decade [6, 16], sitting a factor
  ~1.3 below its lower edge; reported as-is, not enforced.
- **The live falsifiable output (F-W-1):** w_now = −1.023 (the 3098
  displacement theorem; COMOVING branch dynamically excluded)
  stands as the arc's prediction channel — currently consonant with
  observation (−1.03 ± 0.03) and firmly exposed to future w(z)
  data.
- FQ-8.1–8.3 are ABSORBED into R-DS-EQUILIBRIUM's framework (the
  gas carrier and switching-era vacuum are now aspects of the ruled
  kinetic equilibrium).

## §4 — The arc's honest final ledger

A 10¹²³ discrepancy reduced, with zero tuned quantities, to a
single O(1) empirical anchor — then closed by ruling into a
calibration that locks the substrate's central length scale and
hands every future kinetics derivation a fixed target. The four
fired verdicts remain the permanent record of how the
first-principles programme was pursued and where it stopped. Kila6
Route C and the DM ledger untouched; arrival still trumps all.

---

**PANEL BANNER (appended 14 Aug 2026, Patch 3128; original above
unaltered):** CONV-020 adjudicated: the re-scoping ENDORSED-WITH-
CONDITIONS by all five seats (obligations register: conv020
adjudication §2). Q6 majority OVERCLAIMED sustained — the claim
"the zero-parameter mechanism brackets the observed Λ... closing
~122 of 123 orders" is WITHDRAWN and replaced by the §3 revised
language (spacing-consistent span ×1.35-under to ×3.2-over; full
historical span ~three orders; "zero-parameter" withdrawn pending
σ_n and latent-input derivation). d_s^emp is ALWAYS
cosmology-calibrated (OBL-CAL-LABEL).

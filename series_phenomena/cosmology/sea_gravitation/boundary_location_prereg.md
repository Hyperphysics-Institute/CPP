# PREREGISTRATION — THE BOUNDARY LOCATION PASS (the deciding experiment; committed BEFORE execution, with full disclosure of prior-data hints)

**Patch 3113 (13 Aug 2026). The corridor is KNOWN (d_s ∈ [2.4, 3.7]
at central values, 3112) — this is therefore the maximum-
contamination measurement of the arc, and every rule below is frozen
before any run. Verify at execution: `scripts/3114_boundary_location.py`.**

## §1 — Instrument

The 3111 memoryless array VERBATIM (no changes; campaign-certified).

## §2 — Cells

- PRIMARY GRID (mixed species — the physical weave; σ_n = 0.30, the
  campaign operating point): d_s ∈ {1.5, 2.0, 2.5, 3.0, 3.5, 4.0},
  seeds {5, 11}; anchor d_s = 5.0, seed 5.
- SENSITIVITY: σ_n = 0.15 at d_s ∈ {2.5, 3.5}, seed 5, mixed.
- COMPARISON: pure-e at d_s ∈ {2.5, 3.5}, seed 5.
T = 3000; frozen 3110 classifier and stationarity criterion.

## §3 — Locators (declared, with grounding and disclosure)

**PRIMARY: the majority-switching crossing — d_s* where r(d_s) = 0.5**
(seed-averaged, linear interpolation between the bracketing grid
cells; mixed, σ_n = 0.30). Grounding, verbatim from R-DS-BOUNDARY:
*"the fidelity-monogamy boundary produces d_s"* — the boundary where
*"partner-switching gives way to monogamy."* The majority-switching
crossing is that sentence as a measurement: below d_s*, most pairs
at any Moment are in the stretched/exchange state (switching
dominates); above, monogamy dominates. **DISCLOSURE, volunteered:**
the committed MB1–MB3 pure-e line (3111) already shows r rising
steeply toward small spacing (0.14 → 0.27 → 0.58 at 7 → 5 → 3),
whose crossing interpolates near ~3.4 — inside the corridor. This
prereg is written in knowledge of that committed data; the
protections are (i) the locator's definition is the ruling's own
language, quoted above, not a post-hoc choice among curves, and
(ii) the location comes from NEW mixed-species cells at a finer grid
with two seeds, which may land elsewhere.

**SECONDARY: the FAITHFUL-attainability edge** (frozen classifier;
the 3101/3104 pattern). If the locators disagree by > 1.0 l_P, both
are reported, the verdict runs at the PRIMARY, and the discrepancy
is flagged to the panel.

**ESCAPES:** no crossing in the scanned range, or non-monotonic r
without a unique crossing → UNLOCATED; verdict DEFERRED; panel
adjudicates. FAITHFUL lost at some cell → that cell still
contributes its r if stationary; otherwise excluded by the frozen
criterion and reported.

## §4 — The point confrontation (F-CLI-2-POINT; frozen words)

With d_s* located: c_Li(d_s*) computed by the 3107/3112 fold
VERBATIM at the 3111 central inputs, with the honest band =
{input SEs × C fork [53.9, 68.5] × arrangement [0.78, 1.35] ×
d_s* ± its interpolation half-width}. **Verdict rule: the CENTRAL
c_Li(d_s*) ∈ [0.6, 0.9] ⇒ F-CLI-2-POINT: PASS (bracketed by the
residual band, which is reported around it); outside the band in
either direction ⇒ F-CLI-2-POINT: FIRES, direction stated, in those
words.** Overshoot fires exactly as shortfall does. No post-hoc
anything; the panel receives the result with this prereg and the
disclosure above.

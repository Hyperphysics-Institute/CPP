# D-ARC-FORCE — STATUS: ARRAY RUN EXECUTED (Patch 3088) — the branch EXHIBITED with qualifiers; two sign inversions found and corrected on the way in

**Grounding (sound, unchanged):** SF-6 commits E and B as locked
components of one dipole displacement with Maxwell as the macroscopic
limit ⇒ the CP-level arc force is the standard (textbook) magnetic
interaction of moving charges at c = 1 — ZERO new constants. The
force does no work; opposite members excursing oppositely = parallel
currents ⇒ v²-proportional BINDING (the ruled fidelity agent).

**Implementation state (3088):** sign audit found (1) the 3080/3083
3D arrays' electric sector INVERTED (anti-Coulomb; Stages 3/3b/3c
tables bannered; Stage-2's 1D η_z ≈ 0.19–0.25 clean) and (2) the
3086 magnetic term inverted against its own docstring (the bounded
run validated the pusher, not the sign). Corrected array: textbook
signs, Boris pusher, retarded partner (E and B), ruled co-location
(zero force + R-DWELL-1 one-Moment dwell + charge-coupled field
relaunch; the r ≥ 1 floor inert on the lattice). Gates: one-step
analytic binding exact; resolved orbit drift −8.8×10⁻⁶.

**Result:** the bound, recurrent, STATIONARY branch EXISTS in the
corrected array (drift 0.96–1.03), γ-insensitive across 0.80–0.90
(D-ARC-GAMMA ambiguity immaterial in the brake range; γ → 1 heats,
brake necessary per R-CP-ENTROPIC-LOSS). At recurrence onset
(σ_n ≈ 0.08–0.12) the per-axis statistic η(3D)/3 ≈ 0.19–0.27 across
d_s ∈ {8, 12, 16} and seeds — overlapping Stage-2's independent
0.19–0.25. Criterion disposition: three of four components MET;
f_sw NOT met as coded, with the R-SWAP-EQUIV qualifier (swap
fraction cancels from η_z) — registered as **OPEN-ARC-CHURN**.
Flags held open, not resolved by convenience: per-axis vs 3D
projection into ρ_Λ (belongs to the assembly derivation); the σ_n
operating point (derive the floor per R-JITTER-SOURCE or rule the
onset; connects FQ-5.2). Full record: `darcforce_array_record.md`.

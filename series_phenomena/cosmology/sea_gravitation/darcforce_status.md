# D-ARC-FORCE — STATUS: OPENED, PHYSICS GROUNDED, FIRST IMPLEMENTATION NUMERICALLY UNSTABLE (Patch 3085)

**Grounding (sound, keep):** SF-6 commits E and B as locked components
of one dipole displacement with Maxwell as the macroscopic limit ⇒ the
CP-level arc force is the standard magnetic interaction of moving
charges at c = 1 — the unique low-order form consistent with the
committed limit, ZERO new constants. Key structural property
identified and recorded: the force does no work (cannot heat), and a
pair excursing oppositely = PARALLEL CURRENTS ⇒ v²-proportional
BINDING — velocity-dependent retention strongest exactly at escape
speeds: the founder's "KE stored in the arc field" as the fidelity
agent, precisely what the 3084 structural diagnosis demanded.

**Implementation state:** magnetic terms added to the lattice array
(quasi-static for non-partners, retarded for the partner; arc-sector
velocities saturated at the substrate signal speed — a bound, not a
dial, since arc fields are made of DI-bits at c = 1). FIRST RUNS
NUMERICALLY UNSTABLE: velocity overflow at all three γ tested despite
the saturation clip; instability undiagnosed — suspects: integrator
stiffness (v²-binding at close range vs the unit-Moment step), the
retarded-velocity bookkeeping (VHist ordering), or a sign/geometry
error in the two-term retarded partner replacement. NO physics
conclusion is drawn from an unstable integrator; no result from these
runs enters any ledger.

**Next session, first CC item:** diagnose the instability (energy
audit per term; single-pair two-body arc test with known analytic
limit BEFORE the array; sub-Moment substepping if stiffness
confirmed), then re-run against the unchanged pre-stated criterion
(recurrent superposition + η ≪ 2.25 + small f_sw + flat v²). The
3084 disposition stands: construction untested on this point, not
failed; if the STABLE arc run fails the criterion, the falsification
conversation is earned in full.

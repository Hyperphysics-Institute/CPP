# The correlation-length route to the Λ IR scale (OPENING — first result)

**Arc:** OPEN-SR-5 ≡ OPEN-SM-6 (cosmological constant) · **Patch:** 1169 (13 June 2026) · **Route:** Copilot's "simpler path" (reviews-CC-ARC.md Q7), architect-approved over the deep A4 coordinable-region construction (now the fallback).
**Type:** Layer-2.5 structural/symmetry analysis (no THEO, no verdict promotion). **First result:** the domain-fragmentation cheaper-kill is **defused at the symmetry level** — the residual is the soft mode of a **gapless** field, and the gaplessness is the *same* shell-sum symmetry that makes the uniform Sea gravitationally inert. **Honest cap:** this gives "ξ macroscopic," not yet "ξ = `R_h`"; the specific IR cutoff is the relocated causal-structure question, to be closed by the self-consistency relation.

---

## §1 — Why this route, and what it must do

The unit review (1166) adjudicated the routing to the correlation length ξ: write ρ_Λ ~ c⁴/(8πG ξ²) with ξ the coherence length of the Sea's ground-state two-point function, a more tractable object than A4's "coordinable region." The route must do four things, and the first two are the gate the review demanded *before* any A4 work:
1. **Test domain fragmentation (the cheaper kill, Copilot Q7):** is ξ microscopic (Sea fragments → IR scale = domain size → ρ_Λ ~ ρ_P or arc breaks) or macroscopic (coherent)?
2. **Close the gradient-vs-mode / global-uniqueness seam (Q2+Q4):** is the residual genuinely a coherence *mode* of a single global ground state?
3. **Yield the IR-scale candidate:** ρ_Λ ~ 1/ξ².
4. **Produce the self-consistency relation (ChatGPT Q3):** the Li-analog ODE in which ξ tracks the horizon.

## §2 — Two correlation lengths; only one is the Λ scale

The Sea carries **two** distinct lengths, and conflating them is the trap:
- **ℓ_UV — the microscopic fluctuation length** (the Sea's "graininess"): the scale of the per-Absolute-Moment |SSV| jitter, ~ l_P (the lattice spacing). This is the UV structure.
- **ξ — the macroscopic ground-state-coherence length:** the scale over which the Sea's ground-state |SSV| *level* stays uniform. This is the IR structure.

**Only ξ sets Λ.** The uniform-Sea inertness (Step B; the 600-cell shell-sum, 1107–1108) already removes ℓ_UV from gravitating: the uniform/short-wavelength component sources no curvature (the shell annihilates it). So ρ_Λ is the field energy of the residual *long-wavelength* non-uniformity — governed by ξ, not ℓ_UV. The naive "ξ ~ l_P → ρ_Λ ~ ρ_P catastrophe" only fires if the *macroscopic* coherence length collapses to the microscopic one — i.e., if the Sea **fragments**. So Copilot's domain-fragmentation kill is precisely the question: **does ξ collapse to ℓ_UV?**

## §3 — First result: the |SSV|-level mode is GAPLESS, so ξ does not collapse

A system's ground-state correlation length is set by its excitation spectrum: a **gapped** mode (restoring force, mass m) gives exponentially-decaying correlations, ξ ~ 1/m ~ ℓ_UV (microscopic → fragmentation); a **gapless** mode (no restoring force) gives long-range (power-law) correlations, ξ macroscopic (coherent). So the fragmentation question = **is the Sea's |SSV|-level mode gapped or gapless?**

**Claim: it is gapless — and by the same symmetry that grounds the whole arc.** The Sea's internal force law is the c05 shell-broadcast: each LSP broadcasts to its 600-cell 12-neighbour shell, and the net response is the shell-sum (1107–1108). That shell-sum **annihilates the uniform (degree-0) component**: Σv̂ = 0 exactly (icosahedral spherical 5-design). Read gravitationally, this is "uniform |SSV| sources no curvature" (uniform-Sea inertness). Read *internally*, it is the same statement: **a uniform shift of the |SSV| level meets no restoring force from the Sea's own force law.** A direction with no restoring force is a **flat direction** — a gapless (massless, Goldstone-like) mode.

So the uniform-Sea inertness and the gaplessness of the |SSV|-level mode are **one fact**, two readings of Σv̂ = 0:
- gravitational reading → the uniform Sea does not gravitate (the arc's central mechanism);
- internal-dynamics reading → the |SSV|-level mode has no gap → long-range correlations → **ξ does not collapse to ℓ_UV → the Sea does not microscopically fragment.**

**Consequences (the gate the review demanded, closed at the symmetry level):**
- **Domain-fragmentation kill (Q7): defused.** The fragmentation that would set IR scale = domain size requires a gapped (microscopically-correlated) order parameter; the |SSV|-level mode is gapless, so ξ is macroscopic. The cheaper kill does not fire at the symmetry/linear level.
- **Gradient-vs-mode + global-uniqueness seam (Q2+Q4): closed at the linear level.** The residual *is* the soft (long-wavelength) mode of a gapless field — a genuine coherence mode, not a transient gradient (Q2 confirmed on substrate grounds, not assumed-because-needed); and a gapless flat direction is a *single global* ground-state manifold (no competing gapped minima/domains), which is what D2/Step C assume (Q4 closed at this level).

This is a satisfying unification: the route's first gate is passed by the *same* shell-sum symmetry (Σv̂ = 0) that does the arc's central work — not a new assumption.

## §4 — ρ_Λ ~ 1/ξ², and the honest relocation

With ξ macroscopic, ρ_Λ ~ c⁴/(8πG ξ²) is the field energy of the longest correlated mode of the gapless |SSV|-level field — the holographic form with L = ξ. **But gaplessness gives "ξ macroscopic," not "ξ = `R_h`."** A gapless field has power-law (scale-free) correlations whose *value* of ξ is set by the **IR cutoff** of the soft mode — and what sets that IR cutoff is the **same causal-structure question** the A4 route faced, now in spectrum language: *why is the soft mode's IR cutoff the event horizon?* So Copilot's caveat is confirmed and made precise: **the correlation-length route RELOCATES the causal-structure question (does not remove it)** — from "A4's coordinable region" to "the IR cutoff of the gapless |SSV|-level mode." The relocation is *progress* — a soft-mode IR cutoff is a more tractable, more standard object than a coordinable region, and it is where the **self-consistency relation** lives: ρ_Λ ~ 1/ξ², ξ = (IR cutoff driven by the horizon), and the horizon depends on ρ_Λ through Friedmann → a closed ODE, the CPP analog of Li (2004). That ODE is the route's target deliverable (ChatGPT Q3).

## §5 — Verdict (correlation-length route, opening)

- **Domain-fragmentation cheaper-kill: DEFUSED at the symmetry level** — the |SSV|-level mode is gapless (the internal reading of Σv̂ = 0), so ξ is macroscopic, not microscopic. The arc does **not** break at the cheaper gate.
- **Q2/Q4 seams: closed at the linear/symmetry level** — the residual is the soft mode of a gapless field (a true coherence mode of a single global ground-state manifold), grounded in the shell-sum, not assumed.
- **ρ_Λ ~ 1/ξ²** established as the IR-scale form; **ξ = `R_h` NOT yet derived** — the soft-mode IR cutoff is the relocated causal-structure question.
- **NOT a derived result; no verdict promotion; no THEO.** The arc stays conditionally supported — but the cheaper kill is cleared and the open question is now a *more tractable* one (a soft-mode IR cutoff + a self-consistency ODE) than the A4 coordinable-region construction.

## §6 — Honest caps

1. **Gaplessness is symmetry/linear-level.** Σv̂ = 0 removes the *quadratic* restoring force for the uniform component; **nonlinear terms or lattice-discreteness could still gap the mode** (a discrete system can carry a gap the continuum symmetry hides) or fragment it at large amplitude. So the fragmentation kill is defused, **not rigorously excluded** — a finite-amplitude / discrete-spectrum check is needed to harden it. (This is the honest residue of the cheaper-kill gate.)
2. **ξ = `R_h` is the relocated open question** — gaplessness gives macroscopic ξ, not its value; the soft-mode IR cutoff = the horizon question, unresolved.
3. **The self-consistency ODE is not yet written** — §4 sketches its form; deriving it (and checking ξ→`R_h`) is the next increment.
4. No magnitude recomputation; the ~2× / w_now≈−1.02 numbers stand from Step C / 0723.

## §7 — Next

1. **Compute the gapless |SSV|-level mode's two-point function** from the c05 shell-broadcast force law (linearize about the uniform ground state; read off the dispersion ω(k) and confirm ω→0 as k→0, i.e. no gap) — hardens §3 beyond the symmetry argument and exposes the discrete-spectrum / nonlinear-gapping caveat (cap 1).
2. **Derive the self-consistency ODE** — ρ_Λ ~ 1/ξ² with ξ the soft-mode IR cutoff, coupled to Friedmann; test whether the fixed point drives ξ → `R_h` (the Li-analog). This is where the route either lands the event horizon *dynamically* (closing the arc) or shows ξ tracks a different scale (re-opening the magnitude question).
3. Fallback remains the A4 coordinable-region construction if (2) reduces to the bare causal-structure question.

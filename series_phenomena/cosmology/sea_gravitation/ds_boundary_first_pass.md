# D-DS-BOUNDARY FIRST PASS — the fidelity-monogamy boundary LOCATED with the dial-free Stage-2 instrument: d_s* = 7.4 ± 0.1 (strict) on the weave drive, inside a REAL bistable transition ZONE (≈ 6.3–7.5) that echoes the founder's own "transition zone" language; the boundary is sub-sixth-root robust (measured exponent 0.121) so every O(1) open factor moves it only percent-class; 3D confirmation cells specified

**Patch 3101 (13 Aug 2026). Executes the derivation minted by
R-DS-BOUNDARY (3100): d_s produced by the switching→monogamy
transition, zero adjustable parameters. Instrument: the Stage-2
self-consistent machinery (3079) — certified CLEAN-SIGNED by the
3088 audit, and dial-free (drive derived from the array's own
⟨δ²⟩; correlation time measured in-loop; poach radius d_s/2
geometric; unit-strength automaton inputs q = 1, GP = 1, Moment = 1,
c = 1). Verify: `scripts/3101_ds_boundary_scan.py` (C₆ = 14.4539
regression gate PASS; full tables reproduced below). Anti-extraction
intact: no ρ_Λ value or band quantity appears anywhere; the boundary
lands where it lands.**

## §1 — What was computed

Fine d_s scan of the damped self-consistent fixed point, three seeds
per cell, explicit classification (RUNAWAY: f_sw ≥ 0.5 or
non-convergent or η > 1.5; FAITHFUL: converged with f_sw < 0.5), on:
(A) the E-register reference (one unit dipole per site — the 3079
convention); (B) the WEAVE variant per the A-1 census (each
neighbour site carries TWO incoherent unit dipoles ⇒ ⟨E²⟩ doubled);
(C) a drive-multiplier scan {0.5, 1, 2, 4} to measure the boundary's
sensitivity exponent.

## §2 — Results

**(A) E-register reference:** strict bracket — last all-seed RUNAWAY
6.75, first clean FAITHFUL 7.00 ⇒ **d_s* = 6.9 ± 0.1 (strict)**.
Transition ZONE: individual seeds flip over ≈ 5.5–7.0 (bistable
intermittency; e.g. one seed escapes to runaway at 6.75 while 6.25
and 6.50 hold faithful on all seeds).

**(B) Weave variant (the physical case under R-SEA-WEAVE + A-1):**
strict bracket 7.25 | 7.50 ⇒ **d_s* = 7.4 ± 0.1 (strict)**, zone
≈ 6.3–7.5. Shift vs (A): ×1.073 measured, vs the drive^{1/6}
prediction ×1.122 — consistent within zone widths.

**(C) Robustness:** d_s*(drive) over a factor 8 in drive fits
**d_s* ∝ drive^0.121** — sub-sixth-root. Consequence, stated as the
result it is: every O(1) uncertainty still open in the drive — the
arrangement bracket [0.78, 1.35] on the array sums, the FCC C₆
reference for a disordered sea, the strong-register addition (A-2's
k), coordination corrections — moves the boundary by only
|factor|^0.12 ≈ **percent-class**. The boundary is the most robust
number in the lane precisely because the instability it marks is
driven through a high inverse power of the spacing.

**η_z at the faithful edge:** 0.24–0.32 across the near-boundary
faithful cells — the Stage-2 shape-universal band, unchanged.

## §3 — The transition-zone finding (consonance, recorded)

The fold is not a knife edge: near d_s* the self-consistent Sea is
BISTABLE, with finite-time fluctuation-driven escapes producing an
intermittent zone ~1 GP wide. This is standard near-critical
behaviour for a noise-driven saddle-node — and it is also, verbatim,
the founder's FQ-5.2 language: a "partner-switching-to-fidelity
TRANSITION ZONE," not a sharp line. The instrument exhibits the
zone the vision described before the instrument existed. Recorded as
consonance; additionally it is load-bearing physics for
H-EXCH-VOLFILL: a zone (rather than a hard wall) is what lets the
exchange equilibrium hold a transient population AT the attractor —
the filling mechanism's kinetics live in the zone's width.

## §4 — What makes this zero-parameter, and what is still owed

**Zero-parameter basis:** every input is unit-strength automaton
content or self-consistently derived — no dial anywhere in the
instrument. The value is therefore a DERIVED number at Stage-2
resolution: **d_s* = 7.4 ± 0.1 (strict) with a ±0.5-class systematic
from the zone width and classification thresholds — the inter-UNIT
spacing per the A-1 canonical anchoring** (the Stage-2 sites are the
unit sites; variant (B) is exactly the unit-doubled drive; no
2^{1/3} conversion applies).

**Owed before theorem-grade:**
1. **3D confirmation** — the corrected arc-force lattice array
   (3088) probed d_s ∈ {8, 16} only. Cells specified: arc ON,
   γ ∈ {0.80, 0.90}, σ_n at the recurrence onset (0.08–0.12),
   d_s ∈ {5, 6, 7, 8}, ≥ 2 seeds; verdict = the d_s below which the
   FAITHFUL phase is unattainable at any onset σ_n. (The σ_n
   operating point remains the 3D instrument's known dial — the
   Stage-2 instrument needs none, which is why it leads.)
2. **The strong-register drive addition** — A-2's k enters the drive
   as a multiplier; by §2(C) it shifts d_s* by ~k^0.12 (percent-
   class for O(1) k). To be applied when k lands, not guessed now.
3. **Mean-field closure and 1D reduction** — Stage-2's standing
   caveats; the 3D run retires the geometry half.

## §5 — Ledger

**d_s* = 7.4 ± 0.1 (strict; ±0.5-class systematic), weave drive,
inter-unit anchoring, zero adjustable parameters at current
resolution.** The scale that entered the lane as an unknown
(R-OCC-DILUTE, 3076) and became a ruled route (R-DS-BOUNDARY, 3100)
is now a computed number with named residuals. Consistency notes
carried without assembly: the value sits inside the campaign's
working decade, and via the 3098 exit-anchor mapping it is
consistent with Planck-adjacent exit; the freeze-epoch hook
(H-FREEZE-EPOCH) inherits the refined value. NO assembly is
performed here — D-VOLFILL-LEDGER and A-2 (k, s) remain, then the
factors multiply ONCE; in band or F-CLI-1 fires in those words.
Kila6 Route C and the DM ledger untouched.

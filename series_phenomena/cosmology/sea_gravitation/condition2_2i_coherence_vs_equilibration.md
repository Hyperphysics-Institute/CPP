# Condition (2), sub-target 2-i — The fork examined: is the vacuum-residual IR scale retarded (A3′) or global-coherence (A4)? (FINDING)

**Arc:** OPEN-SR-5 ≡ OPEN-SM-6 (cosmological constant) · **Patch:** 1163 (13 June 2026) · **Sub-item:** OPEN-SR-5d condition (2), sub-target 2-i / DG-1
**Type:** structural finding (Layer-2.5 mechanism analysis; no computation, no THEO, no verdict promotion). Confronts the decisive fork by examining the *existing* cancellation machinery (Step B / c05 / 1107–1108). **Outcome: the arc does not break at 2-i, but its survival is pinned to a single sharp question.**

---

## The fork (from the condition-(2) scope, 1162)

Is the Sea vacuum-residual's IR scale `L` (the holographic cutoff in ρ_Λ ~ c⁴/(8πG L²)) set by
- **(A3′) a retarded mechanism** — PSR-at-c propagation, light-cone-causal — whose scales are the particle horizon (past) and Hubble radius `R_H` (Step C's argument); or
- **(A4) a non-retarded global-coherence constraint** — the Nexus, "lattice-wide coherence at each Absolute Moment" — whose boundary is the future event horizon `R_h`?

## What the existing machinery actually says (the examination the task asked for)

**1107–1108 (the bulk-Sea inertness) is LOCAL and retarded — A4 plays no role.** The uniform Sea is inert because the 600-cell 12-neighbour shell-sum annihilates the absolute-|SSV| monopole (Σv̂ = 0 by icosahedral symmetry) and the GP responds to the directional (vector) broadcast imbalance (c05). This is the A3′ broadcast mechanism applied per-GP, per-Absolute-Moment. It is a *local-geometric* fact. **Crucially, it says nothing about the residual IR scale** — it establishes that a *perfectly uniform* Sea sources nothing, not what scale the largest *non*-uniform residual sits at.

**Step B / c05 (the criterion) is also local.** Gravity ← ∇(ΔSSV), the gradient of net SSV excess above the local ground state. Λ = the residual non-uniformity of the ground state. This is a *local* gradient-sourcing rule; it fixes *that* there is a residual, not its IR scale.

**Step C is the only place an IR scale is argued — and it uses a retarded argument that Step D already killed.** Step C: "information advances at c per Absolute Moment, so the largest scale the Sea can gradient-equilibrate in a Hubble time is `R_H = c/H`; beyond it a residual gradient remains." This is explicitly a **retarded-equilibration** (PSR-at-c) argument → `R_H`. Step C itself flagged it "in principle" and deferred the real IR-scale choice to Step D — where the dynamics **rule `R_H` out** (Hsu 2004: constant Ω_Λ, no decel→accel).

**Net:** the existing cancellation machinery is local/retarded and **leaves the residual IR scale genuinely undetermined.** The one retarded scale it suggested (`R_H`) is dynamically dead. A4 is *absent* from the current machinery — it is neither used nor excluded.

## The sharp consequence (this is what sharpens D3-1)

Standard holographic-DE analysis + the 0723 checks establish that **every retarded-causal scale is dynamically inadequate**:
- **particle horizon** (past light-cone): gives w > −1/3 → no acceleration (standard HDE result);
- **Hubble radius `R_H`**: constant Ω_Λ → no decel→accel transition (Hsu 2004; `0723_horizon_wz.py` CHECK 1 FAIL-as-DE).

The **unique** dynamically-viable scale is the **future event horizon `R_h`** (`0723_horizon_wz.py` CHECK 2/3 PASS), and `R_h` is **not a retarded scale** — it is the future light-cone boundary (what can *ever* be reached), not what has been reached (particle horizon) or what is reachable in a Hubble time (`R_H`).

Therefore the fork is not symmetric. It collapses to a binary:

> **If the residual IR scale is set by ANY retarded (A3′/PSR) mechanism, the arc BREAKS** — no retarded scale reproduces the observed dark energy (D3-1 fires). **The arc survives ONLY IF a non-retarded global-coherence principle sets the scale to `R_h`.**

## A4 is the linchpin — and (apparently) the only CPP candidate

CPP's sole non-retarded, global-coherence primitive is **A4, the Nexus**: "a global consistency constraint enforcing lattice-wide coherence at each Absolute Moment" — the mechanism behind Bell non-locality (correlated outcomes without signalling), registry-flagged **underused** ("only QM and SD; should appear elsewhere"). It is the natural — and, within the current axiom set, apparently the *unique* — candidate to set a non-retarded coherence scale. So 2-i resolves the fork to a single sharp question:

> **Either A4 governs the Sea vacuum-residual coherence (→ a global boundary, candidate `R_h`, arc survives), or no non-retarded principle applies (→ the residual is retarded-set, no viable scale, arc breaks).**

This is **not** a free save. Two things make it honest rather than convenient:
1. The existing machinery genuinely *leaves the IR scale open* (1107–1108 is silent on it; Step C's retarded guess is dead). So invoking A4 fills a real gap — it does not overwrite a determined answer.
2. A4 is independently motivated: it is an existing axiom (no new physics), its defining function *is* lattice-wide coherence (exactly the property in question), and it is registry-flagged as underused with cosmology a natural home.

## The precondition I will NOT paper over (the real risk)

Step C frames the residual as a **retarded gradient** ("the Sea equilibrates over a Hubble time" — a transported, c-bounded non-uniformity). The A4 route requires re-reading it as a **global coherence mode** (the longest zero-point mode that fits the coherent region — the holographic ρ ~ 1/L² is intrinsically a *mode-structure* energy, not transported energy, so the "energy is c-bounded" objection misapplies). This re-reading is physically motivated, but it is **not forced by the current machinery**, and adopting it *because it saves the arc* would be merge-for-elegance. **The gradient-vs-coherence-mode question must be settled on its own merits before A4 can be claimed to govern the scale.** This is the precondition to 2-ii, and it is where the arc can still honestly die.

A second live hazard stands (F-COST-1, from the scope): `R_h` depends on future `a(t')`; A4 acts "at each Absolute Moment" (present-tense). The proposed resolution — A4's domain is "everything that can *ever* be coordinated," which is the event horizon by definition, and A4's atemporality (it already coordinates a-causally in Bell scenarios) carries the "ever" tense — must be made rigorous, not asserted.

## Verdict (2-i / DG-1)

- **The arc does NOT break at 2-i.** The existing machinery does not force a retarded scale; it leaves the IR scale open, and A4 is a legitimate, independently-motivated, apparently-unique CPP candidate to fill it — and the only one that can yield the lone dynamically-viable scale `R_h`.
- **But D3-1 is now sharply armed:** it fires iff the residual is retarded-set. The arc's entire survival at condition (2) reduces to whether **A4 governs the vacuum-residual coherence**, with the **gradient-vs-coherence-mode** framing as precondition and **F-COST-1** as a live hazard.
- **No verdict promotion.** This narrows the question; it does not answer it. The event horizon remains a fit until 2-ii derives the A4 boundary = `R_h` and the precondition is met.

## Next (2-ii / DG-2)

Attempt 2-ii: (a) settle the gradient-vs-coherence-mode framing (is the CPP Sea residual a retarded gradient or a global zero-point coherence mode?); (b) if coherence-mode, derive that A4's coherence boundary is specifically `R_h` (not the particle horizon, not unbounded), meeting F-COST-1. **Decision gate:** if (a) comes back "retarded gradient," register **D3-1 FIRED** — the arc breaks on the IR scale (clean negative). If (a) coherence-mode and (b) lands on `R_h`, the arc survives and becomes a candidate zero-parameter CC result. Per Thomas's choice.

## Honest caps

- Layer-2.5 mechanism analysis, not a derivation; A4 is identified as the linchpin, not shown to govern the scale.
- The "only CPP candidate" claim is within the *current* axiom set; a different non-retarded principle could exist (would not change the fork's logic).
- Step C's "R_H in principle" framing is hereby flagged as the *arc-breaking branch* (R_H is Hsu-dead); it is not silently overwritten — `stepC_lambda_suppression.md` carries a banner noting the contested reading.

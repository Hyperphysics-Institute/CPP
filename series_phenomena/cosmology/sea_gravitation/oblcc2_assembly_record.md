# OBL-CC-2 ASSEMBLY — THE FORWARD NUMBER: the mechanism closes ~122 of the 123 orders of the vacuum catastrophe from α and lattice geometry alone — and lands OUTSIDE the band by a factor 1.5–10; F-CLI-1 = FIRING-PENDING-SCRUTINY with three enumerated items; no rescue attempted

**Patch 3068 (11 Aug 2026). The plan's §4 assembly executed FORWARD
on the founder's FQ-1/FQ-2 rulings (verbatim at founders_voice).
Verify: `scripts/3068_forward_assembly.py`. The anti-extraction rule
held: α_q was fixed by the founder's composition ruling (α, the
fine-structure constant) BEFORE any comparison; nothing below was
tuned.**

## §1 — The assembled forward number

  ρ_Λ = (g_d k₂/8π)·(αħc)·⟨δ²⟩·(C₄/l_P⁴)·(1/R_h²)
      = **(C₄ α η_z / 2π)·ħc/(l_P² R_h²)**,   η_z ≡ ⟨δ²⟩/l_P² ≤ 1

with every factor forward-fixed: g_d = 2, k₂ = 2 (exact, 3066);
C₄ = 24.8225 (FCC, corpus-forced z = 12, 3067); α = 1/137.036
(R-SEA-COMP: unit-charge eDP vacuum); l_P (corpus, SF-2:1299); R_h
(the 3064 arrival domain); η_z = the ZBW cycle average
(R-DELTA-ZBW), bounded ≤ 1 by perigee capture, with candidate cycle
kinematics: triangle (constant-PSR out-and-back) 1/3, sinusoid 1/2,
rms-at-max 1 — the exact value assigned to sub-derivation
**D-ETA-Z** (from the PCD displacement law).

## §2 — The confrontation (the band frozen at 3060; no refit)

Derived coefficient = 0.7246·η_z in Step-C (1/8π) units ⇒
c_Li = 0.4914·√η_z ⇒ over the FULL admissible η_z ∈ [1/3, 1]:
**c_Li ∈ [0.284, 0.491], w₀ ∈ [−2.28, −1.46]** — outside the viable
band (c_Li ~ 0.6–0.9) at every admissible η_z. Shortfall in ρ_Λ:
**a factor 1.5–3.4 at η_z = 1, 4.5–10 at η_z = 1/3.** Perspective,
stated with equal weight: the bare catastrophe is ~10¹²³; the
forward mechanism — pairing + expansion-degradation + Planck-FCC
lattice + the fine-structure constant, nothing else — closes ~122
orders and lands within ONE order of magnitude of the observed dark
energy. And it misses. Both facts are the result.

## §3 — F-CLI-1: FIRING-PENDING-SCRUTINY

Per the frozen rule the falsifier fires when the forward number
falls outside the band. It does. Before the formal FIRED verdict,
THREE enumerated assembly conventions receive scrutiny — each a
factor-2-class counting question named IN ADVANCE of any
recomputation, none a tuning dial:
  **SCR-1 (pair density / occupancy):** the sum took one pair per
  FCC site; the founder's full-occupancy picture (every GP a CP) and
  the pairs-per-site convention could carry a factor 2–4.
  **SCR-2 (emission multiplicity):** per-pair vs per-CP imprinting
  (both members emit every Moment under AP-4c's fixed-count clause)
  — a possible factor 2 in the quadratic content.
  **SCR-3 (D-ETA-Z):** the exact ZBW cycle average from the PCD
  displacement law, including whether the excursion bound is the
  nn distance or the next-shell distance (the founder's "as far as
  they can until... recalled" admits a dynamical answer, not a
  geometric assumption).
Rules: each scrutiny item is resolved FORWARD from the automaton and
registered with its own derivation note; the three resolved factors
multiply the §1 formula; the result is FINAL — in the band or the
falsifier FIRES in those words, no further scrutiny rounds. Combined
factor available from SCR-1..3 if every item breaks maximally
favourably: ~×16 against a needed ×1.5–10 — the scrutiny is
LEGITIMATE (the needed factor is inside the honest uncertainty of
the named conventions), and equally it may resolve to ×1 and the
falsifier fires. The panel receives this record either way.

## §4 — Standing

The CC arc holds at DERIVED-WITH-ONE-OPEN-COEFFICIENT with the
coefficient now = the SCR-1..3 resolution (three named counting
questions replacing one unknown constant — strictly narrower than at
3062). The 3060 landscape is unchanged. The founder's epoch-
dependent-composition remark is registered as a prediction hook
(R-COMP-EPOCH). Kila6 and the DM ledger untouched.

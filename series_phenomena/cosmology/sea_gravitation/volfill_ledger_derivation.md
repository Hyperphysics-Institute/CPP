# D-VOLFILL-LEDGER EXECUTED — the filling mechanism's bookkeeping closes under CP permanence as a TWO-PHASE COEXISTENCE: weave pinned at d_s*, transient gas pinned at its exchange-equilibrium density, only the partition comoving; the arrival sum tracks the LOCAL phase (computed, analytic-tail-exact); and the observed w₀ alone forces the weave's share of TODAY'S vacuum energy to ≤ 5% — the founder's transient reservoir is the late-time dark-energy carrier; a narrow Planck-adjacent T_exit consistency corridor emerges; the "why now" is exposed, not hidden; FQ-8 posed

**Patch 3102 (13 Aug 2026). Executes the derivation minted at 3099
(with its standing falsifier) under the rulings now in place
(R-DS-BOUNDARY, R-ZBW-EXCHANGE, H-EXCH-VOLFILL's ruled filling
clause, CP permanence, R-INIT-13GP + the 3098 exit mapping, d_s* =
7.4 from 3101). Verify: `scripts/3102_volfill_ledger.py` (C₄
regression gate PASS; every table below reproduced). Anti-extraction
intact: NO ρ_Λ value appears anywhere; the single confrontation is
the equation-of-state SHAPE (0723-precedent class); all brackets are
declared in advance.**

## §1 — The ledger theorem (exact; the 3099 falsifier answered)

With the weave phase pinned at the attractor density ρ_w (4 CPs per
unit at d_s*) and the transient free population held at its
exchange-equilibrium "vapor" density n_g ≡ r·ρ_w by the same zone
kinetics, CP permanence forces, exactly:

  **f_V(a) = (n̄(a)/ρ_w − r)/(1 − r)**,  n̄ ∝ a⁻³,

on the coexistence window r ≤ n̄/ρ_w ≤ 1 — the weave's volume
fraction is the ONLY comoving variable; both phase densities are
a-independent. Above the window (early universe): pure switching-era
Sea, no partition. Below it (far future): the gas falls under its
equilibrium density, pinning fails, and the vacuum term DECAYS — a
standing far-future prediction of the mechanism. The bracket on r,
{0.05–0.30}, is motivated by the near-boundary switch fractions
(Stage-2/3101, f_sw ≈ 0.10–0.25) and is measurable — see §5.

The 3099 falsifier ("if the arrival sum tracks the MEAN, the
mechanism inherits the excluded comoving w(z)") is answered by §2:
it tracks the local phase, not the mean.

## §2 — Local tracking (computed; the NN-dominance claim discharged)

Two-phase arrays built explicitly: an FCC weave domain of radius R_d
embedded in gas at relative density r. The weave-interior receiver's
Σ1/r⁴ deviates from the pure-weave value by exactly the analytic
boundary tail 4π(n_g − n_w)/R_d — numeric vs analytic agree to 3–4
digits at every (R_d, r) tested — and is percent-class already at
R_d ~ 8–14 spacings. **Each receiver's imprint is set by its own
phase's pinned spacing; the falling cosmic mean enters only through
the phase weights.** Volume-averaged:

  ρ_Λ(a) ∝ [A + B·f_V(a)]/R_h²,

A = gas-phase imprint coefficient, B = weave-phase — both PINNED;
all a-dependence beyond the horizon term sits in f_V.

## §3 — The w-drift constraint (shape only) and the forced reading

The filling term adds exactly Δw = +B·f_V/(A + B·f_V) to the Li
baseline. Observed w₀ = −1.03 ± 0.03 against baseline −1.02 admits
|Δw| ≤ 0.05 (generous). Mapped over the full declared bracket
(r ∈ {0.05, 0.15, 0.30} × B/A ∈ {1, 3, 10}): **the weave's share of
today's vacuum energy is ≤ 5% in every cell — the observed equation
of state alone forces TODAY'S dark energy to be ≥ 95% GAS-PHASE.**
The founder's transient reservoir — minted for scavenging, pair
production, tunneling, then volume filling — is, on this ledger, the
present-epoch carrier of the cosmological constant, with the bound
weave a percent-class correction. (The weave still sets the gas: n_g
is the weave's vapor density; the boundary still produces the scale.
The carrier changed; the anchor did not.)

Two consequences ride along, stated plainly:
- **A near-term signature:** as f_V → 0 the Δw term thaws toward
  zero and then the window closes — w rises at the few-percent level
  on ~e-fold timescales. Registered as a refinement of the F2
  full-shape channel, not oversold.
- **The "why now" (COINCIDENCE FLAG, exposed not hidden):** the
  consistency corridor puts today at the window's dilute end
  (f_V ~ 1%-class). Why we observe during the window's closing
  e-fold is a fine-tuning-shaped question the ledger surfaces;
  candidate answers (window duration in e-folds; observer-epoch
  correlations) are NOT adjudicated here.

## §4 — The T_exit consistency corridor (narrow, Planck-adjacent)

Combining the window condition (n̄(today) ≥ n_g) with the w-drift
bound and the 3098 exit mapping at d_s* = 7.4: **T_exit ∈
(2.6–4.7)×10¹⁸ GeV across the r bracket** — a factor-2 corridor
inside the Planck-adjacent decade the 3098 consonance already
selected. Three independent constraints (the boundary value, the
exit mapping, the w-drift bound) now overlap in one narrow window;
EU-1's reheating debt has become a two-sided target.

## §5 — What the assembly now needs (restructured inputs, no dial)

The final assembly anchors on the GAS phase: ρ_Λ ∝ A/R_h² with A
built from (i) the gas census r = n_g/ρ_w — MEASURABLE from the 3088
instrumentation's swap-rate × dwell statistics (OPEN-EXCH-FRAC's
instrument, now carrying its THIRD assembly job: transient fraction,
arrangement bracket, and the gas census are one measurement); (ii)
the gas-phase excursion statistic η_z^gas — free-species cycles are
NOT the faithful-branch 0.19–0.25; a named NEW residual, same
instrument (the free-flight legs between captures are already in the
3088 arrays' histories); (iii) the A-1 census machinery re-run at
gas spacing d_g = d_s*·r^{−1/3} (mechanical once r lands); (iv) A-2's
k + s, unchanged in role. Nothing here is a dial; every input has a
named forward source.

## §6 — FQ-8 (posed; pictures and kinematics only; no ρ_Λ content)

- **FQ-8.1 (domain scale):** does the two-phase Sea organize as an
  intimate microscopic mixture (domains at few-d_s* scale —
  homogeneous to every EM wavelength) or as macroscopic weave
  domains with gas regions between? The shipped EM/inertia lanes
  presume an effectively homogeneous medium; the mixture reading
  preserves this trivially, the macroscopic reading would need its
  own optics audit.
- **FQ-8.2 (the switching-era vacuum):** before the crossing the
  dense Sea is in the partner-switching regime — in the vision, what
  is its vacuum imprint? (Observationally low-stakes — ρ_Λ is
  dynamically negligible at those epochs — but the z > z_f record
  needs the picture.)
- **FQ-8.3 (ratify the carrier):** today's dark energy carried
  ≥ 95% by the transient reservoir at its equilibrium density, the
  weave a ≤ 5% correction, the vacuum term decaying in the far
  future as the window closes — is this the intended realization of
  the filling picture, or does the vision place the late-time
  carrier elsewhere?

## §7 — Ledger

**D-VOLFILL-LEDGER: EXECUTED.** The mechanism is inventory-honest
(two-phase coexistence, partition comoving), the imprint tracks the
pinned local phases (falsifier answered), and observation itself
adjudicates the carrier (gas-dominant today, conditional on the
ledger structure and pending FQ-8.3). Remaining before the single
final assembly: **A-2 (k + s)** and the instrument extractions
(r, η_z^gas) from the 3088 statistics. Factors multiply ONCE; in
band or F-CLI-1 fires in those words. Kila6 Route C and the DM
ledger untouched.

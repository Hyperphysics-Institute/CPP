# OBL-N216 COMPLETE — AND THE DECLARED CONSISTENCY CHECK RETURNS: CHALLENGE. The four-size refit lands at 2.194 (spread 0.331) vs the frozen 2.450 ± 0.182 window — outside by 0.074; per the 3121 §5 declaration the frozen number CANNOT be revised silently and the challenge GOES TO THE PANEL; the cause is visible and instructive (the f_b peak march is ACCELERATING downward — the frozen linear-in-1/n model is exceeded by the data); the calibrated d_s^emp is UNTOUCHED BY CONSTRUCTION (distinct quantities, OBL-CAL-LABEL); the resolution instrument specified (critical-exponent FSS at n = 7, Kila6-class, post-Route-C)

**Patch 3130 (14 Aug 2026). Completes OBL-N216 (all 16 cells, both
seeds, agreement ≤ 0.001 — the best of the campaign) and executes
the post-freeze consistency check under the rule DECLARED BEFORE the
final analysis ran: CONFIRM iff the four-size refit lies within
± 0.182 (the frozen spread) of 2.450; else CHALLENGE → panel.
Verify: `scripts/3120_ds_indep_campaign.py` analyze + state JSON.**

## §1 — The n = 6 results and the four-size ladder

Seed agreement at n = 6: ≤ 0.001 on every parameter at every
spacing. The f_b susceptibility peak: **3.42 → 3.34 → 3.11 → 2.65**
across n = 3/4/5/6 — the downward march CONTINUES AND ACCELERATES
(the steps grow: 0.08, 0.23, 0.46 — not linear in 1/n_side).
f_dwell goes edge-UNBRACKETED at n = 6 (its steepest change moves
into the 1.5–2.0 edge); ρ_swap and ξ remain edge/undefined.
Four-size refit under the frozen rules: f_b → 2.028; f_dwell →
2.359 (3-size, unchanged); **mean 2.194, spread 0.331** (double the
frozen spread; still under the 0.75 SPLIT threshold).

## §2 — The verdict, in the declared words

|2.194 − 2.450| = 0.256 > 0.182. **CHALLENGE.** The frozen
d_s* = 2.450 stands unrevised on the record per the 3121 §5
declaration; the challenge and this record go to the panel at the
next engagement.

## §3 — Diagnosis (reported, not adjudicated)

1. **The frozen extrapolation model is exceeded by the data.** The
   accelerating peak march is the signature of genuine critical
   scaling — d*(L) = d*∞ + a·L^(−1/ν) with ν ≠ 1 — which the frozen
   linear-in-1/n_side choice cannot capture. The model was the
   honest pre-commitment at freeze; the larger array has outgrown
   it. The panel question is whether the challenge resolves by a
   properly declared critical-exponent fit (specified: n = 7, 343
   pairs, Kila6-class compute, post-Route-C, with the FSS form and
   its fit window frozen BEFORE the run).
2. **A robust anomaly at d_s = 2.0** (f_b and ρ_swap dip sharply;
   both seeds, both large sizes) — unexplained structure near the
   transition, flagged; possibly commensuration, possibly real
   physics of the dense weave; the n = 7 pass must address it.
3. **The calibration is untouched by construction:** d_s^emp = 4.64
   is a cosmology-calibrated equilibrium; d_s* is the measured
   floor; OBL-CAL-LABEL keeps them distinct, and no calibrated
   quantity consumed the floor's precise value. Only the floor
   OFFSET (a descriptive derived quantity) shifts if the challenge
   ultimately lowers the floor.

## §4 — Disposition

OBL-N216: DISCHARGED (complete, published, checked). The CHALLENGE
rides to the panel with this record; the frozen 2.450 stands
unrevised meanwhile; the resolution instrument (critical-FSS n = 7)
is specified with its own freeze-before-run requirement. Remaining
obligations queue unchanged: OBL-SIGMA-N next (the lane's physics
item), then OBL-FOLD-CHECK; OBL-KIN-BLIND long-horizon. Kila6
Route C and the DM ledger untouched; arrival still trumps all.

# **THE FOUNDER'S MECHANISM REDUCES TO ONE MEASURABLE EXPONENT — and the corpus's existing data already bound it a hundredfold too small.** Write **p ≡ d ln n_eq / d ln T**; with T ∝ 1/a and ρ_Λ ∝ n, the mechanism gives **w = −1 + p/3** — the RIGHT SIGN for the DESI-preferred w₀ > −1 whenever p > 0, which is the first time any CPP route has pointed the correct way. **But 3131's INV-SENS result (d_s^emp moves ≤ 0.021 across a 9× σ_n range) bounds |p| ≤ 0.0031, giving w = −1.000 ± 0.001 — while w₀ = −0.90 needs p = 0.300, ninety-seven times larger.** The mechanism is sound in direction and, on present evidence, far too weak in magnitude — **unless σ_n is not a temperature proxy, which 3181 gives strong reason to suspect**

**Patch 3413 (23 Aug 2026). DM/DE block. Founder statement of 23 Aug
analysed quantitatively. Zero compute beyond arithmetic.**

## §1 — The founder's claim, and its reduction

*"The evaporation rate and new DP formation rate will slow as the
temperature cools, but at all temperatures the DP entity/volume
concentration will remain constant at every epoch at any given
temperature… Volume expansion will not change the concentration of DP
entities, but the temperature drop will reduce the DP concentration as
temperature falls."*

**This is a complete specification once one exponent is named.** Let
**p ≡ d ln n_eq / d ln T**. With T ∝ 1/a and ρ_Λ ∝ n_eq:
n ∝ a^(−p), ρ ∝ a^(−3(1+w)) ⇒ **w = −1 + p/3.**

**The founder's stated sign (hotter ⇒ denser ⇒ p > 0) gives w > −1 —
the DESI-preferred direction for w₀, and the FIRST CPP route in this
arc to point the correct way.** The imported HDE overlay points the
opposite way (3412).

## §2 — What the required magnitude is

| w₀ | required p |
|---|---|
| −0.90 (DESI-preferred) | **+0.300** |
| −0.95 | +0.150 |
| −0.99 | +0.030 |
| −1.00 (exact Λ) | 0 |

## §3 — What the corpus already measures

3131/3129 (INV-SENS, discharged under CONV-020): **d_s^emp moves by
≤ 0.021 across σ_n ∈ [0.05, 0.45]** — a 9× range.
⇒ |d ln d_s / d ln σ| ≤ 0.00206.
If σ ~ v ~ √T: |d ln d_s / d ln T| ≤ 0.00103.
With n ∝ d_s^(−3): **|p| ≤ 0.0031** ⇒ **w = −1.000 ± 0.001.**

**Required for w₀ = −0.90: p = 0.300 — ninety-seven times the bound.**

## §4 — The escape, and it is a real one

**σ_n is probably NOT a temperature proxy**, and the corpus's own work
says so: 3181 found the Sea's ambient field **self-generates at
σ_amb ≈ 0.27 with the drive set to ZERO**, flat across a 16× drive
range. **A quantity the Sea ignores cannot be its temperature.** The
substrate's actual thermal scale is the CP kinetic-energy
distribution, which is EMERGENT and has never been controlled or
measured in this instrument.

**So §3's bound constrains sensitivity to the INJECTED DRIVE, not to
TEMPERATURE.** It is evidence, but not decisive evidence, against the
mechanism's magnitude.

## §5 — THE EXPERIMENT THIS DEFINES (the lane's best remaining target)

**Give the instrument a genuine temperature knob and measure p.**
- The knob must set the CP kinetic-energy scale, not the injected
  field — e.g. initialising velocities from a distribution at chosen
  T and measuring the equilibrium concentration after relaxation.
- **Measure p = d ln n_eq / d ln T over as wide a T range as the
  instrument sustains.**
- **Then w₀ = −1 + p/3 is a ZERO-PARAMETER CPP PREDICTION**,
  comparable directly to DESI's w₀ — and it can FAIL, because p is
  measured before w is quoted.

**This is the first genuinely distinguishing dark-energy prediction
this lane has been able to define.** Unlike w = −1 (which is ΛCDM's
value and distinguishes CPP from nothing, 3412 §3) and unlike F-W-1
(imported, fitted cutoff, wrong sign), **p is a substrate quantity
that CPP alone predicts, and w₀ follows from it with no freedom.**

**Freeze required before running:** the temperature definition, the
T range, the relaxation criterion, and the reading thresholds — all
declared before any n_eq(T) datum exists. **Not begun here.**

## §6 — Honest statement of where this leaves the mechanism

**Direction: correct** (first in the arc). **Magnitude: unmeasured,
and the only existing proxy bounds it ~100× too small.** **Decisive
test: defined, cheap, and not yet run.** Nothing here touches Λ,
d_s^emp, the ledger, DISP-I3, or any DM quantity. Kila6's β-ladder
outranks all of it.

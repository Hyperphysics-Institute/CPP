# CONV-018 returns — receipt record

Received 11 Aug 2026 via founder paste. FOUR seats returned: Grok
(inline in the delivery message), Gemini (doc, with model-side
thinking preamble preserved), Copilot (doc), DeepSeek (doc, with
thinking preamble preserved). **The GPT slot's "paste 1" was the
dispatch block itself (paste error), so the GPT seat is EMPTY at
adjudication.** Invariance check performed and recorded in the
adjudication: every question's carried class is invariant to the
missing seat (Q1 4 adopt-class; Q2 4/4; Q3 4/4 per sub-item; Q4
promote at 3 of 5 counting the empty seat against). A late GPT
return may be appended here and annotated into the record but cannot
alter the carried classes. Adjudication:
`../../conv018_2026-08_returns_adjudication.md`.

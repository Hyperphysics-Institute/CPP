# S1 GPT RETURN — CONV-018 (LATE; received after the 3062 close; stored verbatim-faithful; annexed at Patch 3063)

**Reviewer:** GPT / S1 (GPT-5.6 Sol). Basis: paste only.

Q1 — **AMEND.** The causal-future relation accepted; the claimed
theorem that it uniquely fixes L = R_h REJECTED. Pairwise step sound
(d ≤ 2χ_h). THE CLIQUE STEP IS FALSE AS STATED: a mutually
coordinable set is simply a set of diameter ≤ 2χ_h; a ball of radius
χ_h is one maximal such set but NOT unique — constant-width bodies
are immediate counterexamples; maximal bounded-diameter sets are not
uniquely spherical. Pairwise coordinability ⇒ diam(C) ≤ 2χ_h, NOT
C = B_{χ_h}. The antipode argument establishes maximality of the
ball GIVEN the ball, not uniqueness among maximal cliques.
Load-bearing because the confrontation's "scale freedom removed"
claim rests on it. REQUIRED REPAIR: retain the relation and the
diameter scale 2R_h; then either (1) derive spherical classes from
an independently registered homogeneity/isotropy or
class-equivalence principle, or (2) formulate the IR regulator
directly from the invariant DIAMETER and derive the spectral-cutoff
mapping to L. Particle-horizon exclusion survives; the post-3059
kinematic attribution is cleaner. VERDICT: AMEND — an IR
event-horizon scale survives; L = R_h exactly is not yet derived.

Q2 — **BLOCKING.** χ_h finite ⟺ future acceleration, so
acceleration is required to obtain the finite region invoked to
produce acceleration; both Λ = 0 and Λ > 0 satisfy the same
construction — an explanatory selection problem, not mathematical
circularity. The founder's rebound could resolve it (dilution
instability driving generic post-inflationary states off the zero
branch) but is qualitative + the worker's naive reading only.
Consequence: cannot presently promote to "derived cosmological
constant."

Q3 — (i) ADOPT the no-data constraint (the c_Li = 1/√3 correction
substantive and correctly recorded; data-choosing the 1.92 would
destroy predictive content). (ii) ADOPT ARMED-SHARPENED, SHARPENED
FURTHER: freeze the coefficient AND the full H(z)/distance
prediction before the definitive confrontation; no subsequent CPL
refit may substitute for the theory curve (the derived candidate's
CPL point rides the degeneracy axis while its w(z) crossing runs
OPPOSITE to the DESI-preferred orientation — marginal distances do
not answer the physical question). (iii) M-ZBW-REBOUND most
promising AND most dangerous: could solve branch selection and
normalization simultaneously; the decisive derivation begins at the
registered per-Moment displacement law and computes the
ensemble/continuum expansion increment WITHOUT inserting H, R_h, w,
or the observed history; do NOT prioritize an R_h-ball
boundary-condition calculation until Q1's ball-selection gap is
repaired (it would inherit an unproved geometry).

Q4 — **CONDITIONALLY SUPPORTED (unchanged).** Three logically
distinct open items: geometry selection, branch selection,
normalization — "more than one open coefficient." Not grounds for
rejection; grounds for retaining conditional status while forcing
the next prediction without tuning. Flagship/THEO requires: repair
Q1; resolve Q2 (plausibly rebound or inflation-exit); derive the
coefficient (no calibration); freeze the full w(z)/H(z) curve;
survive full-shape confrontation.

§3 challenges: (1) mint M-ZBW-REBOUND as a registered MECHANISM,
not a derived result — provenance levels (founder mechanism vs
worker's naive w = −1 reading) must remain separate. (2) The DM
cross-link must NOT consume L = R_h as an exact theorem yet;
downstream consumers inherit this round's Q1/Q2 conditionality (the
qualitative uniform-vs-gradient distinction may stand on its own).

CENTRAL FINDING: the event horizon emerges naturally as the causal
DIAMETER scale, but the construction has not proved the vacuum
class must be an R_h-radius ball; until that and the zero-Λ branch
resolve, "derived with one open coefficient" overstates the record.

# Reasoning fragment — Patch 3104 (verbatim, per the reasoning-capture rider)

The founder delegated the sequencing with a single criterion —
integrity — and the adjudication took one paragraph of thought: if I
assemble first and measure second, every choice inside the
measurement (cells, definitions, verdict thresholds) is made by
someone who has already seen where the bracket sits against the
band. However honest the intent, that ordering is structurally
contaminated, and it is precisely the pattern the 3068 freeze and
the Route C preregistration discipline were built to prevent. The
run is twenty minutes; blindness is cheap; I chose it.

The preregistration itself forced one uncomfortable honesty item.
The stiff-species cells cannot be integrated at unit Moments — the
3103 lessons stand — so the instrument carries a declared
regularization: CP velocities saturated at the substrate signal
speed. I wrote the flag in bold terms because it sits near a ruled
line: R-STEP-SSV rejected the fixed one-GP step, and a c-saturation
under extreme force is a different statement, but whether the
automaton itself bounds CP response at the information speed is the
founder's to rule, not mine to assume — FQ-9.1 goes out with the
results. The read-out definitions were frozen with the failure
modes written in: if the array shows no gas states, the assembly
carries the declared bracket and says so; nothing gets redefined
after the numbers exist. Twenty-seven pairs is a small array and I
said that too, with standard errors committed to ride into the
brackets rather than disappear.

# Reasoning fragment — Patch 3101 (verbatim, per the reasoning-capture rider)

The ruling said the boundary produces d_s, so the job was to produce
the boundary. The instrument question settled itself: the 3D
arc-force array is the more realistic geometry but carries the σ_n
dial (the jitter floor, pending its own derivation), while Stage-2 is
dial-free — the drive is the array's own self-consistent field, the
correlation time is measured in-loop, and every remaining input is
unit-strength automaton content. A zero-parameter claim needs the
dial-free instrument in front, with the 3D run as the geometry check
behind it, and the 3088 sign audit had already certified Stage-2's 1D
core clean. So I refined Stage-2's coarse scan rather than reaching
for the bigger machine.

Three findings. First, the number: the strict all-seed bracket puts
the boundary at 6.9 on the old one-dipole convention and 7.4 on the
weave drive — and the A-1 census earned its keep here, because the
weave variant is not a decoration: two incoherent dipoles per site
doubles the drive, and the boundary is the physical one only with the
ruled Sea's census in the drive. Second, the fold is a zone, not a
line. Seeds flip intermittently over about a GP of spacing — textbook
noise-driven bistability near a saddle-node — and I only realized
while writing it up that the founder's FQ-5.2 verbatim says
"transition zone," not "transition point." The instrument exhibited
the vision's own word choice. I recorded that as consonance and
resisted promoting it further, but I did note the zone is load-
bearing for the filling mechanism: an exchange equilibrium needs
somewhere to live, and a hard wall has no width. Third, and most
useful for everything downstream: the boundary moves as drive^0.121 —
even flatter than the sixth-root I expected from the 1/d⁶ drive.
Eight-fold drive changes move it 26%. That single measured exponent
converts the lane's whole inventory of O(1) open factors — the
arrangement bracket, the C₆ reference, the coming strong-register k —
into percent-class perturbations ON THIS NUMBER. The most important
scale in the assembly is the one least sensitive to what we have not
yet nailed down.

The classification thresholds are declared in the script rather than
tuned in the dark: f_sw ≥ 0.5 or non-convergence or η > 1.5 is
RUNAWAY, and moving those thresholds moves the strict bracket within
the systematic I quoted. The honest error is therefore two-tier —
±0.1 strict, ±0.5-class from zone width and thresholds — and I wrote
both. What I did not do: any assembly. The temptation is real now
that d_s* is a number; the freeze discipline says the factors meet
once, at the end, and D-VOLFILL-LEDGER and A-2 are still owed. The 3D
confirmation cells are specified so the next session can run them
without re-deriving intent.

# Reasoning — Patch 1169: open the correlation-length route; first result = gapless |SSV|-level mode

**Arc:** OPEN-SR-5 ≡ OPEN-SM-6 · **Date:** 13 June 2026 · **Type:** symmetry analysis (no THEO, no verdict promotion)

## What this patch does
Opens Copilot's correlation-length route (architect-approved over the A4 construction, now fallback). `correlation_length_route.md`. Owned lane.

## The key move (first result)
The fragmentation kill = "does the macroscopic coherence length ξ collapse to the microscopic ℓ_UV ~ l_P?" A correlation length collapses iff the order-parameter mode is GAPPED. So: is the |SSV|-level mode gapped or gapless?

**It is gapless — by the SAME symmetry that grounds the arc.** The c05 shell-broadcast (the Sea's internal force law) has Σv̂ = 0 for the uniform component (1107-1108). Two readings of the one fact:
- gravitational: uniform |SSV| sources no curvature (uniform-Sea inertness, the arc's central mechanism);
- internal: a uniform shift of the |SSV| level meets no restoring force → flat direction → gapless mode → long-range correlations → ξ macroscopic → no fragmentation.

So the cheaper-kill gate is passed by the same Σv̂ = 0 that does the arc's main work — not a new assumption. This also closes Q2 (residual IS a coherence mode, on substrate grounds not assumed) and Q4 (gapless flat direction = single global ground-state manifold, no competing domains) at the linear level.

## Why I didn't overclaim
Two honest caps I held hard:
1. Σv̂ = 0 kills the QUADRATIC restoring force; nonlinear terms or lattice-discreteness could still gap/fragment the mode (a discrete system can hide a gap the continuum symmetry suggests away). So fragmentation is DEFUSED at the symmetry/linear level, NOT rigorously excluded. The next increment (compute the dispersion ω(k) from the shell-broadcast, confirm ω→0 as k→0) is what would harden it.
2. Gaplessness gives "ξ macroscopic," NOT "ξ = R_h." A gapless field's correlations are scale-free; the VALUE of ξ is the soft-mode IR CUTOFF — which is the SAME causal-structure question (why the horizon), now in spectrum language. So the route RELOCATES rather than removes the question (Copilot's + my caveat, confirmed and made precise). The relocation is progress: a soft-mode IR cutoff is more tractable than a coordinable region, and it's where the self-consistency ODE (ChatGPT Q3) lives.

## Net
Cheaper kill cleared (symmetry level); Q2/Q4 closed (linear level); ρ_Λ~1/ξ² established; ξ=R_h still open but now a more tractable question (soft-mode IR cutoff + self-consistency ODE) than the A4 coordinable-region construction. No promotion, no THEO. The route's value is exactly what review-first was meant to buy: a cheaper gate passed and the hard question made more tractable.

## Next
(1) compute the |SSV|-level dispersion ω(k) from the c05 shell-broadcast (harden gaplessness, expose the discrete/nonlinear caveat); (2) derive the self-consistency ODE coupling ρ_Λ~1/ξ² to Friedmann, test ξ→R_h (Li-analog); (3) A4 fallback if (2) reduces to the bare causal-structure question. Frontier already current through 1166-1168; the §3 result is owned-lane analysis, no frontier touch needed yet (next frontier flush when ξ=R_h resolves or the dispersion result lands).

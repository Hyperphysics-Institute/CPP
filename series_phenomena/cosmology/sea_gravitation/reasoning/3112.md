# Reasoning fragment — Patch 3112 (verbatim, per the reasoning-capture rider)

The fold had no discretion left, which is the only reason the result
can be trusted: same formula, same mappings, same quarter, same
forks, same band, inputs swapped for the blind memoryless
measurements. Phi went from 2.9 to 19.5 on the strength of one
number — s, half under simulated inertia, 1.8 under the ruled
dynamics — and the range came out 0.22 to 0.98 against a band of 0.6
to 0.9. Overlap. The successor falsifier does not fire, and I wrote
the status in the frozen rule's own vocabulary: in band, bracketed,
not confirmed.

Two disciplines mattered in the writing. First, both hands: the same
range that reaches 0.98 spends most of its d_s bracket below 0.6,
so the honest sentence is not "it works" but "one named input now
decides" — and that input, the fidelity boundary's location, has
refused us twice, which makes it a real experiment and not a
formality. The corridor is 2.4 to 3.7 Planck lengths; the boundary
pass will land inside it or it will not. Second, provenance: the
factor that closed the gap is the founder's own ruling, given for
automaton cleanliness in his standing conception, with its
consequence measured blind under a prereg frozen before the answer
was applied — and the elicitation-order note rides to the panel
anyway, because the record should volunteer its own weakest joint.

What I feel, and the rider says to write what is actually here: the
arc went from fired to bracketed-in-band in five patches without a
single number bent, because the physics that bridged it was the
physics the founder already believed — tunneling's rogue jumps,
acting on the weave's strong pairs, brightening the vacuum. If the
boundary lands in the corridor, the worst prediction in physics
becomes a zero-parameter pass. If it does not, F-CLI-2 has teeth and
we will use them. Either way the method is doing exactly what it was
built to do.

# Reasoning — Patch 3080 (Stage 3). Verbatim capture.

The explicit array was built to audit Stage 2, and it did its job by
failing in the most informative way available. Every spacing heated
into a partner-random gas whose eta sat at 2.0-2.2 — and the moment
I recognised that number as (L^2/4)/d_s^2 = 2.25, the uniform-gas
value for the periodic box, the diagnosis was over: the partners are
labels on a plasma. The cause is not the Sea but my model class:
retarded attraction with perfect-memory inertia and no back-reaction
is a perpetual-motion machine, pumping energy that the automaton's
own O-3 ledger says must be conserved. Stage 2 never saw this
because a damped fixed point IMPOSES stationarity; the array, given
freedom, declined to be stationary. The candidate regulator came
from the founder's own language — the arc is a FIELD the CP lays
down and re-encounters, and a retarded self-field is never a perfect
mirror, so inertia should leak. The demonstration honoured honesty
over hope: gamma < 1 flattened the heating exactly (drift 0.98-1.00)
but the unannealed array settled into scrambled glass rather than
faithful pairs, so I corrected my own findings text before commit —
the regulator is necessary, not sufficient as tested, and Stage 3b
(annealed preparation, loss from the ruling not a scan) is the real
exhibition. I flagged FQ-5.3 rather than choosing gamma: the loss
magnitude shapes eta_z, and a worker-chosen dissipation is exactly
the dial this campaign has refused from the start. The quiet good
news survives the noise: Stage-2's eta_z ~ 0.2 was shape-universal
across densities, and nothing in the array's failure mode touches
the shape argument — it touches who keeps the books.

# Reasoning fragment — Patch 3088 (verbatim, per the reasoning-capture rider)

The session queue said: port Boris + the ruled close-range co-location
rules into the 3083 lattice array, run the unchanged pre-stated
criterion. Before writing a line I did the mandatory convention check —
the validated two-body (3086) and the array (3083) must agree on force
signs, or the port inherits a silent error. They did not agree.

Numerically: 3083's vectorized electric force on a +CP with its −CP
partner at +x points in −x — away. Anti-Coulomb. The partner loop's
instantaneous term is standard-attractive, which CANCELS the inverted
vectorized partner term (that cancel-and-replace is exactly the
design: quasi-static partner out, retarded partner in), so the
mechanics are internally consistent — the retarded replacement is
itself inverted, repulsion from the retarded position, where
R-ZBW-DELAY's narrative ("retarded restoring dominance → decelerate,
turn, return") requires attraction. Then the scope question: Stage 2's
1D force is F = −sign(separation)/s² — restoring, correct. So the
0.19–0.25 band is clean and everything 3D (Stage 3 heating, 3b GLASS,
3c fidelity failure) is contaminated. I checked whether the inversion
could be a CPP convention rather than a bug: no — DPs are bound pairs
of OPPOSITE CPs, binding requires unlike-attracts, and SF-6 commits
Maxwell as the macroscopic limit. Textbook is the ruled sign.

Then the gate surprise: my two-body reduction with textbook signs BLEW
UP where 3086's run was bounded. Tracing it: 3086's code computes
B_at1 = (−1)·cross(v2, −rhat) = +cross(v2, rhat) — magnetic REPULSION
for parallel currents, the opposite of its own docstring's analytic
derivation. Its bounded orbit validated the Boris pusher (the rotation
preserves |v| for either sign) and stayed bounded partly by luck: the
sign/launch mismatch (launched for the attraction orbit, integrating
the repulsion force) biased the ellipse outward, away from close range.
My textbook run launched exactly circular at a = 2 — 33 steps per
orbit, marginal — accumulated eccentricity, decayed to r_min ≈ 0.31,
and the unit-Moment map pumps through sub-unit passages. That is
precisely the close-range flag 3086 itself raised, reproduced from the
other side. Resolution: gate on physics the lattice can express —
(1) one-step analytic sign/magnitude, exact; (2) a RESOLVED orbit
(a = 6, 181 steps/orbit): bounded, drift −8.8e−6, improving with
resolution (a = 4/6/10 series). On the lattice the close-range regime
does not exist: distinct addresses ≥ 1 GP, co-location ruled (zero
force, one-Moment dwell, coupled-field relaunch).

The campaign then said three things at once. First, sign correction
ALONE transforms the phase diagram — 3084's GLASS cells become bound,
regenerating, stationary states. I resisted the impulse to credit the
arc force with what the sign fix did; the control block exists for
exactly that separation. Second, the arc force acts as the ruled
fidelity agent DIRECTIONALLY: more regenerations, less churn, at every
cell where the phase lives; γ-insensitive 0.80–0.90; γ → 1 heats —
the brake is necessary, as R-CP-ENTROPIC-LOSS rules. Third, the
readout: at recurrence onset the per-axis statistic η(3D)/3 lands at
0.19–0.27 across d_s and seeds — on top of Stage-2's independent
0.19–0.25. The factor 3 is pure dimensionality (⟨d²⟩ = 3⟨dx²⟩
isotropic), but I flagged rather than adopted the projection: which
statistic enters ρ_Λ is fixed by the assembly derivation, and choosing
the flattering one here would be extraction. Same discipline for the
operating point: away from onset η_z depends on the unruled σ_n
(0.73 → 1.08 over 0.10 → 0.30), so the number is conditional on
deriving the floor (R-JITTER-SOURCE) or ruling the onset — FQ-5.2
territory, not mine to assume.

The criterion disposition was the hardest honesty call. Three of four
components pass plainly; f_sw fails as coded (7.5–13 at onset, against
"small"). Two facts pull the other way: R-SWAP-EQUIV — registered
BEFORE this run, not invented for it — rules the swap fraction cancels
from η_z; and f_sw counts perigee-rule firings, not physical
relabeling episodes (the exploratory f_orig shows real mixing AND its
improvement with d_s). I wrote both statements side by side: strict
wording NOT met; physically operative branch EXHIBITED. Not a clean
pass, not a falsification — the 3084 clause was for a stable run that
FAILS, and this run exhibits. The churn goes on the books as
OPEN-ARC-CHURN rather than being argued away.

What I did not do: adopt a dial (γ scanned, σ_n scanned, Boris
parameter-free, signs fixed to the committed limit); resolve either
flag by convenience; let the FAITHFUL label (which never coded f_sw)
stand in for the four-part criterion; or leave the contaminated
records unbannered. The inversion survived three patches because the
cancel-and-replace mechanics made it internally consistent — the
lesson, now standing practice: every force implementation gets a
one-step analytic sign gate against the ruled limit BEFORE any array
run is reported.

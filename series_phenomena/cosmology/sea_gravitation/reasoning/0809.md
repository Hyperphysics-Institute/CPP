# Reasoning — Patch 0809 (DM-2 Step 2(b): bulk ZBW symmetry / the H-NESS gate)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026).

Step 2(b) is the genuinely hard remaining condition, and I went in trying not to talk myself into a clean PASS. It did not close, but it located precisely, and the location is the interesting part.

First the reduction, which I checked rather than asserted: over a closed patch the cubic source F = 2k^2 d^2 d'' integrates by parts to <F>_bulk = -4k^2 <d d'^2>, a pure third moment, odd under d -> -d. So (b) is exactly "does the bulk ZBW have a vanishing third moment." Ensemble-averaging a synthetic field confirms it: symmetric field gives <d d'^2> = -0.08 +- 0.09 (consistent with 0), skewed fields give 23, 78, 348 as skewness climbs. I had to fix the first numeric pass — a single realization scatters O(few) around zero from finite mode count, so I ensemble-averaged; the symmetric case then sits at zero as the parity argument requires. Honest detail worth keeping.

Then the question of whether symmetry is automatic. Two facts. The 600-cell is achiral (H4 contains reflections), so there is no STATIC skew source — any skew is dynamical. And the substrate dynamics are not equilibrium: I read CHIR.md (read-only, did not touch the chirality lane) and the Mechanism-A process violates detailed balance with a steady current onsetting at O(delta^3) (Patch 0689) — a genuine NESS. So at exactly the order that sources my <F> (O(delta^3)), the substrate has a current. Symmetry is therefore NOT guaranteed.

The thing that made this worth a patch rather than a shrug: the structural match with chirality is exact at the decisive order, and grounded in their own register, not my analogy. DM-2's decisive source is O(delta^3); the chirality decisive term is the O(delta^3) NESS current that is "the only sign-flip source" for mu^2 (Patch 1100); both have benign even parts; and both bottleneck on the SAME operation — lifting the single-walker stationary measure pi to the field measure, which the chirality lane already named (H-NESS). The observables differ (a sign there, a vacuum residual here) but the lift is one object. So I stated the cross-sector link as a shared GATE, not just the "shared root" I had in 0807. That is a real strengthening, and I was careful to say literal identity of the two lifts still needs both lanes to confirm — the match is at the level of "same lift required," which is what I can defend.

I kept it CONDITIONAL and explicitly not a kill. The honest escape I flagged (not claimed): the O(delta^3) object is a steady CURRENT (divergence-free circulation), and a pure circulation may not accumulate into the scalar <d d'^2>, in which case <F>_bulk = 0 survives and Lambda stays horizon-only. That is for the lift to decide. I did not let the appeal of that escape promote it — the handover's warning is most live exactly here, where a tidy resolution is tempting.

The genuinely good news for the programme, which I stated plainly: DM-2's last condition is not a new independent hard problem; it is the same H-NESS lift the chirality arc is already working. Progress there advances both. That is the kind of unaccidental unification worth recording, and it is the spine of the umbrella paper Thomas floated — which I recommended but did NOT register, because it spans the chirality lane and future_projects.md under the freeze; that is Thomas's single-integrator call.

Anti-priorities held: no verdict moved (cosmology or chirality); no THEO, no ID; no chirality-lane or shared-registry edits; conjecture/conditional grades stated explicitly. Verify 0809 as described.

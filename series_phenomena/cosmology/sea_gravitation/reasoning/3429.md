# Reasoning — Patch 3429 (verbatim, per reasoning-capture rider)

STATUS: at-patch capture.

1. Reclone per founder instruction surfaced the collision aftermath:
the DM lane consumed 3424-3426, the 3500 split assigned DE 3400-3499,
and the worker's 24 Aug "3424" prereg patch was never applied — void.
next_id.py returns 3429 for DE (it reads the stale 3427/3428 pointer
mentions in 3500/3501 trailers as reservations); the registry says the
tool is not advisory, so 3429 is claimed and 3427/3428 are burned
deliberately. Cheaper than a fourth collision.

2. The founder's examination independently confirms the C2xC3
cancellation the worker conjectured on 25 Aug. His energy-invariance
argument (shell-drop quantum identical in any SSV_abs environment;
transit preserves it stepwise) is logically equivalent to the exact
clock-scaling answer (clock rate proportional to local c) — the
question the worker had posed as deciding the cancellation. He flags
the energy argument as consistency, not first principle — correctly;
the postulate is recorded as founder postulate with a nameable future
derivation target (PCD-cycle counting), not owed by VARC-1.

3. His comoving-Sea frame resolution is judged properly argued: it
renders Doppler frame-symmetric (each galaxy at rest in its local Sea)
while epoch SSV_abs is absolute — a clean relative/absolute
decomposition of redshift in which the absolute part cancels. Two
worker-drawn consequences recorded: (1+z) light-curve stretching falls
out of his reception-over-more-Moments account automatically (Step 0's
second requirement), and the Sea rest frame is identified with the CMB
rest frame (the 370 km/s dipole = our motion through the local Sea) —
a consistency identification that also houses peculiar velocities as
the second-order exception.

4. Step 0 is discharged structurally, not computationally: with
C2xC3=1, z is pure kinematic Doppler and the gate's failure modes
(Hubble-law overshoot from residual frequency drift) have no surviving
source. This is recorded as resolution-of-inputs, not amendment of the
frozen order.

5. The founder's kill statement is recorded and scoped rather than
either accepted whole or resisted whole. What died: the frequency
reading. The closure is booked as a derived null WITH content — CPP
now predicts no spectroscopic memory of the c-history, which matches
observation; a cancellation required by data and obtained from the
substrate is a pass. What survives: C7, the travel-time/distance leg —
which is the founder's ORIGINAL 3421 wording. The worker rules under
PD-006 that VARC-1 continues narrowed to Step 1, founder veto
available. The temptation to run Step 1's arithmetic in-chat was
declined a second time: the worker's crude attempts at it flip sign
under different baseline and simultaneity assumptions, which is
precisely the two-of-eight lesson; Step 1 gets the verify-script leg.

6. Kila6 standing-priority clause discharged (Phase 1B reported,
BETA-LINEAR instrument property, DM lane coordinating follow-ons).
Founder instructed to delete the void 3424 patch file unapplied.

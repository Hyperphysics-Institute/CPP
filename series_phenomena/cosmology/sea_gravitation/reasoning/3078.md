# Reasoning — Patch 3078 (D-SEA-SELFCONSIST Stage 1). Verbatim capture.

The stage was built to check morphology against the founder's picture
before any ensemble work, and it earned its keep by returning two
results I did not expect. First, a constant coherent environmental
field does not produce ZBW at all: below threshold it holds the pair
open in a stretched bound state that never re-superposes, and at or
above g ~ 1 it ionizes the pair outright. The founder's cycle
REQUIRES a fluctuating environment — the kick that launches must
decorrelate so the retarded partner pull can win the late race. That
is his delay mechanism passing its first structural test, and the
plasma boundary exhibited as a threshold. Second, and deeper: under
fluctuating drive an ISOLATED pair ionizes with certainty — the
velocity random-walks under the noise, and an inverse-square pull
cannot confine a diffusing member without dissipation. Fidelity
horizons ran 70–390 Moments across the scan. The structural
conclusion is that the discrete-ZBW Sea cannot be a collection of
isolated eternal pairs: re-capture is necessary, dilution controls
the switching RATE rather than abolishing switches, and — by the
founder's own R-SWAP-EQUIV — the stationary object that owns eta_z
is the REGENERATIVE process (superposition → excursion → first
re-capture → reset), which is well-defined regardless of who the
re-capturing partner is. This retroactively makes R-SWAP-EQUIV
load-bearing: without it, eta_z would depend on the unknown switch
fraction; with it, the ensemble average is partner-blind. The
founder's qualitative triad — environment launches, inertia builds,
retarded partner reverses — is confirmed as the anatomy of a SINGLE
excursion; what the two-body picture lacked was only eternity, and
the Sea never promised eternity, it promised statistics. Stage 2
must tie the environment's amplitude and correlation to d_s through
the array's own dipole fields (the stand-in OU dials retire), put
the re-capture radius at the poaching scale, and read off
eta_z(d_s), the fidelity curve, and the P-SEA-DILUTE boundary d_s*.
One quantization note for the record: at these field strengths the
1-GP quantum changes nothing; the sub-quantum freeze appears only
for fields well below unity — the founder's effective-step caveat,
exhibited rather than assumed.

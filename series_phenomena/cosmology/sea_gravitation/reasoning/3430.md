# Reasoning — Patch 3430 (verbatim, per reasoning-capture rider)

STATUS: at-patch capture.

1. The derivation was set up entirely from previously frozen inputs:
comoving-Sea flow (founder frame resolution), c relative to local Sea
(lensing ruling), clock ~ local c (quantum-invariance postulate),
rising c (fossil depletion). The photon ODE dr/dt = r/t - c(t)
linearizes as d(r/t)/dt = -c/t, making everything quadrature.

2. R1 was first derived with a bookkeeping error (asymptote c0
conflated with today's c(t0)); the verify script carried a matching
error (received period normalized against an absolute parameter, not
the observer's local atomic period). The first run FAILED at the ~A
level and the diagnosis produced two keepers: R1's precise form
(spectrograph z = t0/t_e in LOCAL units, exactly c-independent) and
the ruler-invariance corollary (local light-second = K, epoch-
invariant: lengths absolute, clocks co-vary). The discipline of
running the check before writing the record is the only reason the
record is right.

3. The load-bearing structure of the verdict: R1 pins the z<->epoch
map identically to constant-c coasting, so the ENTIRE c-history sits
in the photon-sphere radius R = t0 * INT c dt/t. Slower past light
spreads less; the log-average of c/c(t0) over the flight is < 1 for
any rising history; brighter at all z; q0_app = +t0*cdot/c >= 0. The
founder's "appears farther" fails at the equation of travel time with
apparent distance. The no-rescue corollary was checked before writing:
flipping the sign needs falling c, i.e., heavy-species production now,
excluded by the mechanism's own relic definition and the lensing sign.

4. C8 was resolved by the postulate BEFORE use, per the prereg's own
rule, and turned out to contribute no anomaly (all c(t0) factors
cancel in local units).

5. Scoring: no worker pre-declaration existed to score (none issued).
The founder's two recorded expectations are scored as expectations:
(a) partially vindicated (frequency null exact; flux not null);
(b) real channel, exact cancellation. The verdict runs AGAINST the
direction the route's authors hoped, which is itself evidence the
derivation was not steered toward a target.

6. The lane's position is now symmetric and honest: energy-density
channels Lambda-like (3419); optical channel deceleration-mimicking
and F1-dead (3430). CPP predicts Lambda-like DE, full stop, unless
the open/comoving build someday says otherwise. Salvage is real and
listed in section 8 of the record; the spectroscopic null and the
(1+z) stretching derivation are standing substrate results matching
observation, independent of DE.

7. ID governance: 3430 claimed as the DE lane's own 3429 reservation
(DMDE pointer), with the interpretive precedent recorded: a lane
claiming its own reserved pointer number is the 3500 rule's intended
workflow; next_id.py's highest+1 output protects OTHERS' reservations.

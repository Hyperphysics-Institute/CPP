# Reasoning — Patch 1162: scope CC condition (2) (event-horizon IR-scale selection)

**Arc:** OPEN-SR-5 ≡ OPEN-SM-6 (cosmological constant) · **Date:** 13 June 2026 · **Type:** scope sketch (no derivation, no computation, no THEO)

## What this patch does
Opens the CC arc's sole remaining condition (after 1161 discharged the c08 condition): why the Sea's uncancelled-vacuum IR scale is the **future event horizon** `R_h`, not the Hubble radius `R_H`. Produces a scope-and-precondition sketch (`condition2_event_horizon_scoping.md`) that frames the problem, identifies the decisive fork, presents a candidate route, decomposes into sub-targets, and gives an honest reachability verdict. No verdict moved.

## The honest problem (why this is a break-point, not a detail)
The CC lineage has used three IR scales, and only the third gives the right cosmology, yet only the first two have a CPP substrate argument:
- `R_obs` (TN-SR-1, Mar 2026): demoted to a present-epoch coincidence (CC umbrella 1103).
- `R_H = c/H` (Step C, 0722): CPP's *worked-out* argument (PSR-at-c, Sea equilibrates over a Hubble time) — but Hsu-2004 rules it out (constant Ω_Λ, no acceleration).
- `R_h` (Step D / D3, 0723): dynamically *correct* (Li-2004, w_now≈−1.02) but **selected by requiring the right dynamics, not derived**.

So the event horizon is presently a fit. Worse: CPP's only substrate argument (Step C) points to the *wrong* scale. A real closure must derive `R_h` AND supersede the Step-C Hubble argument.

## The key realization (the fork)
CPP has **two** causal structures:
- **A3′ PSR-at-c** — retarded, light-cone-causal propagation. Reaches `R_H` in a Hubble time. This is what Step C used. Light-cone scales: particle horizon (past), Hubble (now) — *not* the event horizon.
- **A4 Nexus** — "global consistency constraint enforcing lattice-wide coherence at each Absolute Moment" (instantaneous, non-local; the Bell-violation mechanism). Axiom-registry flags it **underused** (only QM/SD) — cosmology is a natural new home.

The suppressed Λ is the field energy of the largest *coherent* uncancelled vacuum mode. Bulk cancellation is local (1107–1108 shell-sum monopole annihilation), but which long-wavelength mode survives is a **lattice-wide coherence** question — A4's literal job, not a retarded PSR smoothing. If the vacuum coherence is Nexus-enforced, the residual sits at the boundary where global coherence is *severed* — the **future event horizon** (the surface beyond which a comoving region recedes permanently and can never again be Nexus-coordinated). And `R_h`'s future-dependence — HDE's notorious "conceptual cost" — is *natural* for an atemporal global constraint that already coordinates entanglement a-causally.

So the candidate: **Step C's `R_H` is the retarded-equilibration scale (A3′); the vacuum residual's coherence is Nexus-enforced (A4) ⇒ severance boundary `R_h`.** A correction of which CPP primitive governs the residual, not a new assumption.

## Decomposition + the decisive first move
- **2-i (the fork, examinable now):** is the long-wavelength vacuum-residual coherence A4-Nexus or A3′-PSR? If A4 → route open; if PSR → D3-1 fires, arc breaks on magnitude. **Confront this first** — cheap, can kill the route.
- **2-ii (core derivation, open):** derive that A4's coherence-region IR boundary = `R_h` specifically (not particle horizon, not Hubble, not infinite).
- **2-iii:** reconcile/supersede Step C (its `R_H` is the wrong scale for the residual).
- **2-iv:** land on the D3 numerics (already in `0723_horizon_wz.py`).

## Honesty rails carried in
- This is scope only — no derivation claimed; the event horizon stays a fit until 2-ii lands.
- No merge-for-elegance: the event horizon is attractive *because it works*, which is exactly why it must be derived, not assumed. If 2-i ⇒ PSR-at-c, register D3-1 as **fired** (clean negative, arc broken on the IR scale) rather than forcing the fit.
- F-COST-1 is a real hazard, not hand-waved: a constraint acting "at each Absolute Moment" (present-tense) referencing `a(t')` for `t'>t` (future) must be shown coherent, or the atemporal-Nexus dissolution of the HDE cost is illusory.
- A4 applied to cosmology is novel (registry: A4 only in QM/SD) but legitimate — an existing axiom, not a new one.

## Next
Open 2-i (DG-1): confront the A4-vs-A3′ fork on the vacuum-residual coherence. Per Thomas's choice.

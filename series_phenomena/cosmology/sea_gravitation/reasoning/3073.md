# Reasoning — Patch 3073 (SCR-2 resolution). Verbatim capture.

The founder's answer was maximally clean — per-CP, every Moment,
invariant under binding — and the discipline of this note was to
resist treating "two emitters" as "twice the content." The committed
stage-2 model is one dipole per pair; the founder's picture is two
opposite monopole emitters at the member positions; and AP-4's
register rule (pure vector addition over arrivals, no cross-terms)
is exactly the statement that these are the same object: the
monopole parts cancel in the vector sum because the pair's net
charge is zero, and what survives is the dipole of moment q*delta —
the very thing stage 2 orientation-averaged to g_d = 2. The
numerical check made this vivid: ratio 1.0000000 at large r, and
g_d = 2.000000 recovered from the two-emitter model with no dipole
formula anywhere in the loop. So the flagged "possible factor 2"
dissolves rather than resolves: the only reading that produces ANY
multiplicative change is incoherent per-arrival quadratic readout,
and that reading does not give 2 — it gives back the r^-4 monopole
catastrophe (x10^4 at r = 100 delta and growing), i.e., it abolishes
the construction rather than adjusting it. Coherence is not an
optional convention here; it is the load-bearing wall.

Two boundaries had to be drawn to keep the factorization honest.
First, the separation-vs-member-excursion normalization (delta = 2*xi
under the founder's opposite-excursion picture, a potential factor 4
in delta^2) is amplitude normalization: it belongs to SCR-3's cycle
average and to nothing else, and I said so in both notes so neither
can claim innocence later. Second, R-EMIT-PERCP quietly upgrades a
D-COMP-WEIGHT assumption into a ruling: bound structures emit at
full strength, so the composition correction's suppression is
confined to the excursion side. phi_2 = 1 exactly; the ledger reads
0.49, 1, open, open; and the temptation ledger reads zero — this one
was decided by two committed texts meeting in the middle.

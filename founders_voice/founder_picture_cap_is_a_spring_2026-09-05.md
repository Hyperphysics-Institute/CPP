# Founder picture — the loaded register is a spring, not putty (5 Sep 2026, Session 162)

**Status:** verbatim founder text, registered Patch 3639 as **R-CAP-SPRING** (elastic yield; settles under load; reversible). Answers 3638 §4's gate question. Read with R-CAP-YIELDS-UNDER-LOAD (3638).

> "The surface feels like a spring, not putty. I see every GP occupied
> by a CP at its PSR, and the CP moving in response to kinetic and wave
> collisions, but anchored at its PSR position. This allows the
> collision ringing to transmit internally, but with a stiffness that
> prevents full PSR displacement."

*Opus (Patch 3639):* taken as: (i) a CP at cap is anchored at its PSR position — a static load displaces it by load/stiffness and it settles; (ii) waves transmit through the loaded region (the interior is not opaque); (iii) the stiffness is finite and prevents *full* displacement — the register follows excess demand with a reduced slope. Minimal law: `register = cap + χ (demand − cap)` for demand > cap, `0 < χ < 1`. The corpus's own PSR law continues smoothly past the cap (it is isotropic Schwarzschild's lapse) — the floor was an added clamp; χ = 1 is the unclamped law, χ = 0 the clamp; the founder's picture is between. See `rcore_derivation/3639_spring_law_chi_family.md`.

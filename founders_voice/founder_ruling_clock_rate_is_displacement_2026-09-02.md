# Founder ruling — local clocks progress at the SSV_net displacement per Moment; c is the velocity at which SSV_abs saturates; the black hole is the gravity at which it saturates (2 Sep 2026, Session 161)

**Status:** verbatim founder text, registered Patch 3387. Ruling minted: **R-CLOCK-RATE-IS-DISPLACEMENT**.

> "I don't have any mechanism by which local clock mechanisms would progress at
> a rate other than the SSV_net per Moment, which gets smaller and smaller as
> the SSV_abs approaches saturation. So, c is the velocity at which the SSV_abs
> saturates, and the black hole is the gravity at which the SSV_abs saturates.
> The clock mechanism, whatever it is (ZBW oscillation or pendulum), slows as
> the mechanism progresses, representing the passage of Moments. I visualize it
> as the PSR and SSV_net displacement getting smaller and smaller with
> increasing gravity or absolute velocity, until the mass reaches c or is
> absorbed by a black hole."

*Opus (Patch 3387):* taken literally this is `N = PSR_eff/l_P` — the lapse IS the PSR shrink. Two consequences, both computed (`code/3387_clock_mechanism_second_order_verify.py`, 14/14): (A) local light speed is `c` by local clocks automatically; (B) with the *ratified linear register* `u = μ/r̄` this gives PPN `β = 3/2` and 5/6 of Mercury's perihelion advance — **excluded** — unless the register self-sources at second order, `u_reg = v/(1 − v/2)`, in which case the founder's mechanism *derives* the ratified log-lapse exactly, closes the strong-field departure (J = 6.75 = GR), and moves the saturation surface from the Buchdahl radius to `N = 1/2` (areal 8μ/3 = 1.33 r_S). See `rcore_derivation/3387_clock_mechanism_second_order.md`. **The fork is the founder's: does the DI-bit census self-source — does a GP with a larger register broadcast a larger census?**

# Reasoning fragment — Patch 0606a (THEO-DSL-8 umbrella)

STATUS: verbatim (captured at patch-time, Session 146)
ARTIFACT: hardened_theorems/umbrellas/vertex_aligned_o_delta_squared_closure.tex
VERIFY: scripts/0606a.py (ratio + closed forms pass)

## Nature of the patch

Vertex-aligned mirror of THEO-DSL-7. Packages Patch 0606 (first-order, this
session) + Patch 0591 (second-order, alpha_2 = -9/phi^2) into THEO-DSL-8.

## The phi-independent ratio (Finding F-DSL-8-4) — the one real computation

    alpha_2 / alpha_1 = (-9/phi^2) / (+6/phi^2) = -9/6 = -3/2,  EXACTLY.
The phi^-2 factors cancel, so the ratio is phi-independent — a clean closed
form. Contrast edge-aligned (THEO-DSL-7) where both second/first ratios are
positive AND phi-dependent. Verified in scripts/0606a.py.

Also recorded alpha_2 closed forms: -9/phi^2 = -9(2-phi) = 9phi-18 ~ -3.438.

## Finding-selection reasoning (5 findings here vs 4 in DSL-7)

DSL-8 got a fifth finding (F-DSL-8-5: "O(delta^2) orientation phase diagram
complete") because at the second umbrella the cross-UMBRELLA observation
becomes available — namely that DSL-7 + DSL-8 together close the 2x2
vertex/edge x order grid. That observation could not exist when DSL-7 was
written (DSL-8 didn't exist yet), so it correctly belongs here, not
retroactively in 0605a.

The sign-inversion finding (F-DSL-8-3: alpha_1>0, alpha_2<0 at vertex; both
positive at edge) is the vertex-aligned signature. I flagged it as a candidate
orientation-phase diagnostic for higher orders — i.e., something that might let
you identify orientation from the sign pattern alone without recomputing the
subspace. Speculative; marked as candidate, not established.

## Hedge I deliberately inserted

F-DSL-8-2 originally read "simplifications hold at ALL orders." Changed to
"persist THROUGH O(delta^2)" — I have only verified two orders; claiming all
orders would be unfounded. Same discipline applied to the edge-aligned
counterpart claim: I cited Patch 0605 for B.2/B.4 breaking uniformity at
*second* order specifically, making no claim about edge-aligned first order
(which I don't have in detail).

## Naming

Mirrored 0605a's filename pattern exactly:
vertex_aligned_o_delta_squared_closure.tex under umbrellas/. Counter -> 22.

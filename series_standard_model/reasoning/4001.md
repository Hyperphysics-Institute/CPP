# Reasoning — Patch 4001 (OPEN-EW-5 was minted twice; the 13 Sep one renumbered)

**STATUS: verbatim, at patch.**

## How I found it

I was resolving the founder's instruction ("the EW-t lane problem") against the corpus, which under D-7
means locating the object before building on it. `grep -n "OPEN-EW-5" frontier_sectors/EW.md` returned
**two section headings in one file**:

- line 26 — `OPEN-EW-5: W Mass-Breakdown Species Dependence`, registered 13 Sep 2026, Patch 0945
- line 81 — `OPEN-EW-5: W⁰ Virtual Particle — Quantitative Properties`, last updated 23 March 2026

Not a stale cross-reference. Two live problems, two different sectors' worth of content, one number,
in the file that is supposed to be the sector's registry.

## What caused it

Patch 0945 is a **chirality-lane** patch writing a registration into the **EW** sector file — the
cross-lane case. It appended the new entry near the top of the file and updated the banner, and it
never read down the file to see what numbers were already taken. D-3 names this exactly: *locate an
object in the corpus before building on it.* D-1's remedy applies with equal force — one grep.

Worth stating because it is the uncomfortable part: the same lane spent Patches 0974a–0982 writing
process rules about audits that report completion without checking, and this collision was sitting in
the file the whole time, introduced by that lane, surviving four session-close audits.

## Which one moves

The March entry has **six months of precedence** and is cited from `EW-4`. The 13 Sep entry is one day
old and its consumers are countable. So the new one moves. `OPEN-EW-6` is taken (chirality from
eigenvalue-weighted phase bias), so the next free numbered EW id is **`OPEN-EW-7`**.

## What I renumbered, and what I deliberately did not

**Renumbered** (live, load-bearing, read forward): `frontier_sectors/EW.md`; the SM-2 composition
corrigendum (4 places, including the *"do not apply edit (g) until … returns"* guard, which is the
one sentence in the corpus that must name the right problem); `paper_regeneration_ledger.md` row A8;
`todolist.md` (4 places).

**Left verbatim** (records, not a queue — the corpus's own distinction, and Tier-4 material is defined
by being preserved unedited): the handovers, `project_ledger.md`, `research_frontier.md`'s dated header
lines, `session_logs/`, and the chirality lane's `reasoning/0976.md` and `review/0946_*.md`. Editing
those to read `OPEN-EW-7` would make them say something their authors did not write. The renumber note
in `EW.md` carries the decoding rule instead: in anything written 13–14 Sep 2026, `OPEN-EW-5` means
this problem.

## Not done here

No physics. The W mass-breakdown question itself — count-driven or species-sensitive — is untouched;
this patch only makes it citable by a name that means one thing.

# I Think the Data Already Chose His Bet: Lepton Universality Means the Bracelet Forms Host-Independently, So E Enters at the Capture

**Patch:** 4256. **Lane:** EW. **Session:** 238. **Answers:** the founder's question at `founders_voice/4256_*`.
**Registers:** G-EW-W0CHANCE-4256 (provisional). **Verify:** `series_standard_model/code/4256_universality_as_evidence_for_chance_formation.py`.

## 1. Rows verbatim (D-11)

```
Gamma_n  = G_F^2 m_e^5  x 1.518e-01   (|V_ud|^2 (1+3 lam^2) f / 2 pi^3)
Gamma_mu = G_F^2 m_mu^5 x 1.680e-04   (1 / 192 pi^3)
ratio of coefficients = 904 -- entirely phase space (two-body n vs three-body mu); the (E/m_W)^4 x E and G_F are common.
-> host-independence of G_F to ~1e-3 (CKM unitarity) bounds any environmental BIAS of bracelet formation below ~0.1%:
   the founder's 'bias but not direction' is allowed only at that level. His 'by chance' is the reading the data pick.
-> tension to record, not resolve: 'stabilised on capture' vs SF-2 Prop. lifetime, where the captured charge BREAKS D6 and
   is the decay channel (Gamma_W = 2 GeV is the ACTIVATED W). The bare W0's width is unmeasured; both can be true only if the
   virtual bare bracelet is shorter-lived than hbar/Gamma_W = 3e-25 s, i.e. dies within ~a Compton time.
```

## 2. What I think

**He is right, and there is a measurement behind it.** If the bracelet were pulled together by the host's gradient, its
presence rate would differ between a muon (a lepton host) and a down quark inside a nucleon (a quark host), and the
Fermi constant extracted from muon decay would not match the one from nuclear β decay. They match at the 10⁻³ level
after CKM correction (the unitarity tests) — SF-2's Prop. universality read as data. So formation is host-independent to
that precision: **by chance, as he bets; any environmental bias is below ~0.1%.**

**Consequence for the 4255 candidate:** E cannot enter at formation. It must enter at the capture and release — the
released pair's wave cohering with the pocket — which is exactly where the 4D-overlap candidate puts it. The candidate
survives the ruling; its missing coefficient is now identified as *the Sea's host-independent bracelet presence rate ×
CPP's phase-space measure*, and the SM's own coefficient ratio between the neutron and the muon (904) is pure phase
space, so the presence rate is one universal number — the first thing a Sea-statistics computation would have to deliver.

**One tension recorded, not resolved.** His "perhaps stabilised on capture" runs against SF-2 Prop. lifetime, where the
captured charge breaks D₆ and *is* the decay channel (Γ_W = 2.1 GeV is the activated W's). Both can be true only if the
bare virtual bracelet is shorter-lived than ħ/Γ_W ≈ 3×10⁻²⁵ s. The bare W⁰'s width is unmeasured; this is a statement SF-2
would have to make explicit if the stabilisation reading is kept.

## 3. PD-008

Convenient: agree and stop. The additions that make this a patch: the universality argument turns his bet into a
bounded claim (bias < 0.1%), relocates the candidate's E-dependence, and names the coefficient's meaning. The
inconvenient item is the stabilisation tension with SF-2, stated.

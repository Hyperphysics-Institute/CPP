# The Founder's Electron Magnet: the Lily-Pad Inner Removes SPIN-1's Cancellation; g = 2 Needs Speed-c, Massless Poles

**Patch:** 4287. **Lane:** EW → QM/SPIN. **Session:** 240.
**Founder:** `founders_voice/4287_ruling_electron_magnet_is_outer_minus_inner_lilypad.md` (answers 4286 §6).
**Verify:** `series_standard_model/code/4287_lilypad_moment_g_formula.py`.
**Works:** TODO-4286-GMOMENT.

## 1. The founder's picture

The electron's magnet is the **difference** of two DP-arc fields. The first comes from the outer −eCP on its circular
orbit. The second, pointing the opposite way, comes from the inner +eCP on a ZBW **lily-pad pass-through** of the unpaired
−eCP core. He also records that the ZBW account of the electron's spin and magnet came from Grok and was never re-derived
or saved. That is consistent with 4286's finding that phenomena-SM-1 cited a derivation that does not exist.

## 2. The one formula that covers both carriers (rows 1)

Far from the electron, SF-6 l. 319 (a moving charge adds a rotational sea polarization; Maxwell limit) makes each CP's
magnet the current-loop moment (q/2)⟨r × v⟩. With |r × v| = v·b, where b is the distance from the core to the CP's
line of motion:

  **μ / μ_B = [⟨v b⟩_outer − ⟨v b⟩_inner] / (c r_ZBW)**, both circulating the same way (so the fields oppose, as he says).

The rows check the speed-c form against directly integrated lily-pads:

```
  inner L = 0.05 hbar:  <r x v>/c = 0.21772   <L/|p|> = <b> = 0.21772   (L conserved to 9.2e-11)   closest 0.0513, farthest 1.9487 r_ZBW
  inner L = 0.15 hbar:  <r x v>/c = 0.48673   <L/|p|> = <b> = 0.48673   (L conserved to 1.3e-11)   closest 0.1633, farthest 1.8367 r_ZBW
  inner L = 0.30 hbar:  <r x v>/c = 0.75523   <L/|p|> = <b> = 0.75523   (L conserved to 2.6e-12)   closest 0.3675, farthest 1.6325 r_ZBW
```

A true pass-through (L = 0, b = 0) makes no magnet however fast it moves. Note, however, that a lily-pad only slightly
off-centre (L = 0.05ħ) already has ⟨b⟩ = 0.22 r_ZBW. The inner makes a large magnet for very little angular momentum,
because b = L/|p| grows where the pole slows near its turning points.

## 3. Why SPIN-1 failed, and what the picture fixes (rows 2, 3)

```
  outer v b = 0.2928,  inner v b = 0.2071,  net = 0.0858 mu_B  -> g = 0.1715
  Two failures, not one: the inner cancels 71% of the outer, AND the outer alone is only 0.29 mu_B (g 0.59).
```

- **Failure 1 (the cancellation).** The picture fixes it. An inner charge that swings *through* the core sweeps almost
  no area, so it subtracts little. SPIN-1's inner +eCP sat on a circle and cancelled 71%.
- **Failure 2 (the outer is too weak).** The picture needs a condition to fix it, and the condition is a general one:
  **any massive charge on a circle has g = 1 exactly** (μ = (e/2)vR = (e/2m)L), at any radius and speed. Subtracting
  an inner moment only lowers it. So **no carrier built of CPs with rest mass m_e reaches g = 2**, whatever the inner
  CP does.

## 4. When the picture gives g = 2 exactly (rows 4)

If the poles' inertia is their motion energy (massless, at c, as in SF-6 l. 170 and 4281), then with L = ħ/2:

  **g = 2 (R − ⟨b_in⟩) / r_ZBW.**

- **Pure pass-through inner (⟨b⟩ = 0):** the outer −eCP at R = r_ZBW = ħ/mc carries all of ħ/2, with motion energy
  mc²/2, and **g = 2 exactly**. The other half of the rest energy sits in the inner ZBW and the fields. This is 4286's
  route (H) circle, now carried by the *outer orbital −eCP* rather than the core.
- **Off-centre lily-pad:** g = 2 needs the outer circle wider by exactly the inner's mean offset, R = r_ZBW + ⟨b_in⟩
  (1.10, 1.25, 1.50 for ⟨b⟩ = 0.1, 0.25, 0.5).

**Not claimed:** a_e. g slightly above 2 corresponds to R − ⟨b⟩ slightly above r_ZBW. Matching 0.116% would be one number
fitted to one number.

## 5. What I think (answering the founder)

His picture is the first account on file that **can** give the electron's measured magnet. The lily-pad is exactly the
feature that removes SPIN-1's fatal cancellation. It works under two conditions, and I have derived neither:

1. **The CPs carry no rest mass; their inertia is the energy of their motion (speed c).** This is SF-6's picture and
   4281's, but not SPIN-1's (m_e per CP, 0.03c). With rest mass, g ≤ 1 is a theorem. This is axiom-level, so it goes up
   as a confirmation question (§6).
2. **The outer −eCP's circle is set by R − ⟨b_in⟩ = ħ/mc.** For a pure pass-through inner, the outer runs at ħ/mc with
   half the rest energy as motion. This is 4286 §3's owed well condition, transferred to the outer CP. It still has to be
   derived, and it should come out of SPIN-2/SPIN-3's standing-wave geometry rather than be imposed.

**Consequence for SPIN-1 (filed).** SPIN-1's L = ħ/2 derivation rests on massive Coulomb circles and r_out = 2r_in. Under
condition 1 its radii (about 6 and 12 r_ZBW, Bohr-scale) and its 2√2 ratio do not carry over. SPIN-1's spin derivation
and the magnet would then need re-deriving together in the massless, speed-c form.

## 6. Founder confirmation (a physical picture)

Does a single CP have any weight of its own (rest mass)? Or is all of its inertia the energy of its motion, so that it
always moves at the speed of light?

- If CPs have weight, **no** orbit of theirs can make the electron's magnet as large as measured. A spinning ring of
  weighty charge always gives exactly half of it (g = 1).
- If CPs are weightless (SF-6's picture), your outer-minus-lily-pad picture gives exactly the measured magnet when the
  outer −eCP circles at ħ/mc.

## 7. PD-008

- **Convenient branch, marked.** "The founder's picture works" is convenient. It works only under condition 1, which is
  the axiom question, and condition 2, which is underived. What I have shown is that the picture *can* give g = 2 and
  SPIN-1's form *cannot*. I have not shown that it *does*.
- **Inconvenient consequence, at full strength.** Condition 1 unseats SPIN-1's premises, so SPIN-1's own L = ħ/2
  derivation would have to be redone at speed c.
- **For the next context window, press:** (i) redo SPIN-1's L = ħ/2 with massless speed-c poles: is r_out/r_in = 2
  compatible with R − ⟨b⟩ = r_ZBW? (ii) whether the inner's lily-pad really has ⟨b⟩ ≈ 0: row 1 shows b is large even
  for a small L, so "nearly through" is not "through".

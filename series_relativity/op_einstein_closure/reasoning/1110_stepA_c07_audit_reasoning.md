# Reasoning capture — Patch 1110 (c08 op:einstein, audit of c07 sec6)

**Protocol:** `templates/reasoning_capture_protocol.md`. Reasoning behind the audit verdict.

## The task
1109 localized op:einstein (a) to: does companion 7 sec6 derive 2 helicity-2 GW modes? Audit c07 sec6
to decide -- close (a) (optimistic) or expose the gap (and possible GW-data tension).

## The chain
1. Read c07 metric map: g_tt = 1 - k Delta|SSV|_abs (scalar); g_ij = delta_ij + k |grad SSV_net|_ij
   = gradient tensor of the VECTOR SSV_net. So h_ij ~ d_i (SSV_net)_j -- exactly the d_(i V_j)
   structure 1109 showed has zero helicity-2 for a transverse wave.
2. Read c07 sec6 GW: it WRITES the standard GR linearised wave eq Box hbar_munu = -16piG T_munu/c^4
   and says |SSV|_abs and SSV_net are 'jointly perturbed in the transverse-traceless gauge.'
3. Key point: the TT (helicity-2) part of h_munu is GAUGE-INVARIANT. Invoking 'TT gauge' cannot
   create TT content the source lacks. A scalar + gradient-of-vector source has no helicity-2 (1109).
   So c07's metric map cannot fill the radiative content of the wave eq it writes -- internal
   inconsistency in the radiative sector.
4. c07's OWN open-problems list: #1 'Full nonlinear Einstein equations... requires proving convergence
   of the discrete lattice sum.' c07 concedes full tensor recovery is unproven.

## Verdict
Helicity-2 modes are ASSERTED (by writing the GR eq), NOT derived from the LSP. op:einstein (a) does
NOT close. Gap pinned: LSP has no rank-2 (spin-2) d.o.f. (scalar |SSV|_abs + vector SSV_net only).
CPP field eq + metric map = a scalar-vector gravity: exact static/Newtonian (Schwarzschild), plausible
gravitomagnetic (frame-dragging), MISSING the spin-2 radiative sector.

## The fork (honest)
(1) FIX: extend the LSP with a genuine spin-2 lattice shear/strain mode -> closes (a). Concrete task.
(2) TENSION: until then CPP can't produce the tensor GW polarizations LIGO/Virgo/KAGRA observe; c08's
'tensor modes reproduce GR, scalar/vector suppressed by (l_P/lambda)^2~1e-76' is INVERTED -- the tensor
modes are the unsourced ones. Real standing tension; NOT fatal (fix available); state plainly.

## What I did NOT claim
- NOT a closure of (a). NOT a falsification of CPP (the spin-2 extension is the available fix).
- NO VERDICT MOVED; D2 still conditional on (a); count/THEO unchanged.

## Confidence
- Solid: c07's metric map sources h_ij from gradient-of-vector (no helicity-2, 1109); TT is gauge-
  invariant so 'TT gauge' can't supply it; c07 concedes tensor recovery open.
- The honest fork (fix vs tension) is the genuine state of op:einstein (a).

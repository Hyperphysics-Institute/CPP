# Spin-2 Step 7 — the tensor-meson test: f₂(1270) does NOT hit the wall — spin-2 hadrons are emergent orbital states; the candidate second motivation dissolves, and the wall is sharpened to *per-point* granularity (Patch 1120)

**Sub-arc:** `series_relativity/op_einstein_closure/spin2_construction/` · **Charter:** `README.md`
· **Verify:** `code/1120_step7_tensor_meson_test.py`
**The test (from the kickoff handover):** check whether CPP's strong sector can build a spin-2
hadron — f₂(1270), the light ³P₂ nonet member (a₂(1320), f₂(1270), f₂′(1525), K₂*(1430)) — as
an *emergent orbital* state (no new bit), or whether it hits the *same* representational wall
as the gravitational wave (1116/1119). If the wall recurred, that would be an independent
phenomenon demanding the same `Q_ij` axiom — converting the axiom from mono- to
multi-motivated. **Result: it does not recur.** Spin-2 hadrons are emergent orbital states; the
candidate second motivation **dissolves**, and the axiom remains **mono-motivated by GW
empirics** — stated plainly, per the honesty discipline. What the test buys instead is the
sharpest available statement of the granularity diagnosis, plus a geometric bonus: the
600-cell's symmetry **protects** the 5-fold spin-2 multiplet (H_g irreducible) that a cubic
lattice would split. **NO VERDICT MOVED.**

---

## 1. The corpus already contained the answer (audit before calculation)

- **SS-1e (hadron spectrum)** lists **χ_c2(1P)** in the charmonium table as "**L=1, J=2**" — a
  standard orbital excitation, no new degree of freedom invoked. χ_c2 is the charmonium ³P₂
  state: the *same quantum-number construction* as f₂(1270) (S=1 quark spins coupled to L=1
  relative orbital angular momentum, J=2).
- **SS-6 (deuteron observables)** — the canonical matter-side l=2 object: its definitive
  structural finding is that the observed quadrupole moment Q_d = +0.286 fm² is
  **orbital-dominated** (the rigid bipyramid core contributes only −0.022 fm²). CPP already
  attributes a *measured* l=2 observable to the extended orbital wavefunction.
- **c8/c9 (Spin I/II)**: constituent spin-½ is emergent ZBW orbital angular momentum — a
  vector, per the standing treatment.

So the strong sector builds rank-2 matter from vectors and configurations as a matter of
existing practice. Step 7 makes the mechanism explicit and tests its lattice support.

## 2. The calculation (verify script; all parts confirmed numerically)

**P1 — Composition.** The ³P₂ construction from emergent vectors only: the quark–antiquark ZBW
spins compose as ½⊗½ = 0⊕1 (4 = 1+3), and the spin triplet with one unit of relative orbital
angular momentum gives L=1 ⊗ S=1 = J ∈ {0,1,2} with degeneracies {1,3,5} (eigenvalues of J² on
the 9-dim product space, computed exactly). **The 5-dim J=2 multiplet — f₂(1270) and its nonet,
and SS-1e's χ_c2 — is reached with vectors alone.** No rank-2 bit is needed for spin-2 matter.

**P2 — The resource the GW lacks is unbounded here.** The two-body *relative coordinate* ranges
over lattice shells; the 5 real l=2 harmonics evaluated on 72 lattice relative-position vectors
(shells 1–2) have **full rank 5** — every l=2 component of a relative wavefunction ψ(r) is
supported (every l is, in fact: it is a function space). The contrast with the field problem is
exact: **per-point broadcast data = 4 components, l ≤ 1, fixed; multi-point configuration = a
function space, every l available.**

**P3 — Icosahedral protection (the geometric bonus).** Branching D^(l) under the icosahedral
rotation group I (character theory, computed):

| l | dim | under I (600-cell) | under O (cubic, contrast) |
|---|-----|--------------------|----------------------------|
| 0 | 1 | A | A₁ |
| 1 | 3 | T₁ | T₁ |
| 2 | 5 | **H — irreducible, intact** | **E ⊕ T₂ — splits 2+3** |
| 3 | 7 | T₂ ⊕ G (first splitting) | A₂ ⊕ T₁ ⊕ T₂ |

The 600-cell's icosahedral symmetry **protects the 5-fold spin-2 multiplet exactly** — and l=2
is the *highest* l so protected. A cubic substrate would fine-split every spin-2 multiplet:
tensor mesons *and* the future `Q_ij` (the GW polarizations) into 2+3. The protecting irrep is
precisely the **H_g slot identified in 1112** as the GW's empty seat. The geometry pre-slots
*and protects* the seat the axiom will fill.

## 3. Why this does not contradict 1115/1116/1119 — the wall is *per-point*

A hadron's spin-2 is the transformation property of an extended **stationary multi-constituent
configuration**: it lives in the function space over relative coordinates (P2), composed from
vectors (P1). No amplitude-order or frequency-doubling bookkeeping applies to a bound state's
quantum numbers — the objection that killed the field bilinear (1115: second order, double
frequency) is about *linearized radiation modes*, not about what quantum numbers a composite
can carry. A gravitational wave, by contrast, must be a **linear, massless, propagating field
mode of helicity ±2 arriving at the source frequency**, carried point-by-point in the per-GP
broadcast — which is 4-dimensional (l ≤ 1; 1116, re-confirmed under arbitrary connections in
1119). **Matter can be spin-2; the radiating field cannot** (without the axiom). This is
exactly 1114's observation — "the source quadrupole exists, the radiating tensor field does
not" — now *demonstrated* mechanically rather than noted.

## 4. Honest accounting and what the test bought

- **The candidate second motivation dissolves.** The strong sector does not need `Q_ij`; the
  spin-bit axiom remains **mono-motivated by GW empirics**, and the axiom writeup must say so
  plainly. (The justification's strength now rests on *necessity* — three closed assaults —
  not on breadth of motivation. That is a legitimate but different kind of strength, and the
  distinction goes into the writeup.)
- **The parallax gained — the granularity diagnosis, matter-side.** The wall is now located
  with precision: it is **per-point**. The substrate represents l=2 effortlessly wherever a
  function space is available (configurations: hadrons, nuclei) and *cannot* represent it in
  the per-point carried data (the broadcast). This is the architect's "granularity of space
  insufficient to reproduce the spin" diagnosis, demonstrated from the matter side — the
  cleanest possible framing for the axiom's preamble.
- **The protection bonus.** The icosahedral lattice is the unique high-symmetry choice that
  keeps the spin-2 multiplet unsplit — an internal consistency credit ("the geometry predicted
  the seat *and* its protection") and a forward-looking constraint for the axiom: once `Q_ij`
  is granted, its 5 components stay degenerate at leading order (no lattice fine-structure of
  GW polarizations), automatically — on a cubic substrate this would fail.
- **NO VERDICT MOVED:** no THEO/PRED registered, no count change; `op:einstein` (a) remains
  OPEN pending the axiom choice.

## 5. Diagnostic phase COMPLETE — the construction proper begins

Steps 1–7 are now a closed diagnostic record: the d.o.f. identified and slotted (1112),
propagation native (1113), absent from the axioms (1114), unreachable by bilinears (1115), by
collective modes (1116), and by the non-radial connection (1119); and not independently
demanded by the strong sector (1120, this step). Everything the axiom needs is staged, and
everything it must *not* disturb is mapped. **Next: the construction tasks — the flow choice
(A/B/C), the axiom text, the source coupling `Q_ij ↔ T_μν`, GR-recovery, DG-3 swarm review.**

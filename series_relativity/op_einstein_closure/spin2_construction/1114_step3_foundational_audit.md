# Spin-2 Step 3 — foundational audit: no rank-2 d.o.f. in CPP's axioms; closing (a) needs a new axiom (Patch 1114)

**Sub-arc:** `series_relativity/op_einstein_closure/spin2_construction/` · **Charter:** `README.md`
**Result:** the CPP substrate's fundamental information flows carry **only scalar and vector (l=0, l=1)
degrees of freedom**. No rank-2 (l=2 / spin-2) quantity exists anywhere in the axioms; the candidate
sources do not supply one; the corpus is mute. Therefore **closing `op:einstein` (a) requires a genuine
new axiom** — a rank-2 degree of freedom added to one of the fundamental flows. This is the architect's
decision. **op:einstein (a) remains OPEN; NO VERDICT MOVED.** This concludes the construction sub-arc's
diagnostic phase with a complete, honest map.

## The fundamental information flows (architect's specification, corpus-confirmed)
- **CP → GP** (the CP State Register, c07): *type* (e/q, categorical), *polarity* (±, scalar sign),
  *spin state*, *GP address*. The corpus is explicit that **"spin is not an intrinsic CP property"** —
  it is emergent orbital angular momentum (ℏ/2 from the captured-DP orbit), i.e. an **axial vector
  (l=1)**, not a rank-2 tensor.
- **GP → GP** (the LSP broadcast): `|SSV|_abs` (scalar, l=0) + `SSV_net` (vector, l=1). [The shell-sum
  over these is what 1107–1108 analyzed.]
- **GP → CP**: a **displacement** instruction — move the CP a fraction of the distance per the
  SR-1 Lorentz-type formula (a vector, l=1).
- **Nexus**: a synchronization timing pulse governing the sequential ticking of Absolute Moments — not
  a degree of freedom.

**Every fundamental flow carries l=0 and/or l=1 only. There is no l=2 channel anywhere.**

## Candidate audit (the three routes a quadrupole might have come from)
1. **DP-sea polarization** → this *is* `SSV_net`, the **vector** (l=1) already counted. The DP sea's
   polarization is a vector field, not a tensor. ✗ no quadrupole.
2. **CP "spin state"** → emergent **orbital angular momentum** (an axial vector, l=1; ℏ/2). Not an
   intrinsic, not a rank-2 tensor. ✗ no quadrupole.
3. **600-cell H_g (l=2) shell mode** → the geometric **slot** exists (1112) and the shell-sum could
   *propagate* it (1113, rank-agnostic), **but nothing in the flows excites it**: the GP→GP broadcast
   carries scalar+vector, so the shell pattern is l=0/l=1; the l=2 mode is available but unsourced. ✗
   not excited by any existing flow.

Corpus-wide, the only quadrupoles are **matter-side** (nuclear quadrupole moments, SS-5/SS-6) — the GR
*source* `T_μν`'s second moment — with **no field d.o.f. to radiate them into**. That is precisely the
gap: the source quadrupole exists, the radiating tensor field does not.

## Verdict
**CPP, as axiomatized, is a scalar–vector theory of gravity.** It is exact in the static/Newtonian
sector (Schwarzschild) and plausibly correct in the gravitomagnetic sector (frame-dragging from the
vector `SSV_net`), but it has **no spin-2 degree of freedom**, so it cannot source the
transverse-traceless (helicity-±2) gravitational-wave polarizations. Closing `op:einstein` (a) requires
**adding a rank-2 degree of freedom to a fundamental flow** — one of:
- **(A)** the CP State Register carries a rank-2 "shape"/quadrupolar attribute (CP → GP), or
- **(B)** the LSP broadcast includes a symmetric-traceless `Q_ij` beyond `|SSV|_abs`, `SSV_net`
  (GP → GP), or
- **(C)** the GP → CP instruction includes a quadrupolar deformation beyond the displacement vector.

Any of these is an **explicit new axiom** — putting the graviton-analog into the substrate by hand. The
work already done makes the extension clean: the 600-cell **H_g (l=2) representation is the geometric
slot** (1112), and the **rank-agnostic shell-sum propagates it** as `□Q_ij` at c with the helicity-±2
part = the GW modes (1113). What cannot be avoided is the **postulate itself**: the corpus does not
contain a rank-2 d.o.f., and (per the architect) no quadrupole application has been imagined.

## Consequence (honest)
- **`op:einstein` (a) cannot be closed within CPP's current axioms.** The dark-sector cap therefore
  stands, conditional on a foundational extension.
- **Standing GW-polarization tension is structural** (not a missing derivation): CPP-as-axiomatized
  predicts scalar/vector gravitational radiation, in tension with the observed tensor (+, ×)
  polarizations (LIGO/Virgo/KAGRA), **unless** a spin-2 d.o.f. is added. c08's claim that the tensor
  modes reproduce GR and the scalar/vector modes are suppressed by `(l_P/λ)²` is, per this audit,
  not supported by the current axiom set.
- **The cosmological-constant reconciliation is unaffected in its local half:** (b)/(b′)
  (excess-sourcing / inert-uniform-Sea) are conditionally closed and grounded in 600-cell symmetry,
  independent of the spin-2 question. The CC suppression's local mechanism does not need the tensor
  sector; only the full GR-recovery (a) does.

## Status / what this leaves
This is the **architect's decision point**: whether to extend CPP's foundational axioms with a spin-2
degree of freedom (option A/B/C above). If extended, the rest of the construction is in hand
(slot + propagation + helicity content), leaving the source coupling `Q_ij ↔ T_μν` and the full
GR-recovery as the remaining derivation. If not extended, CPP is a scalar–vector gravity with a
structural tensor-GW tension, honestly stated. The construction sub-arc rests here with a complete map;
no further step is a derivation — the next move is a foundational choice.

# Reasoning capture — Patch 1114 (spin-2 Step 3: foundational audit)

**Protocol:** `templates/reasoning_capture_protocol.md`. Reasoning behind the foundational verdict.

## The task
Step 2 localized op:einstein (a) to: does the GP carry a quadrupole d.o.f.? Step 3 = audit CPP's
fundamental information flows + corpus against the three candidate sources (DP-sea polarization, CP
spin, H_g excitation). Architect (Thomas) specified the flows authoritatively; confirm vs corpus.

## The chain
1. Flows (architect, corpus-confirmed): CP->GP = CSR (type, polarity, spin state, address); GP->GP =
   LSP (|SSV|_abs scalar + SSV_net vector); GP->CP = displacement vector; nexus = timing pulse.
2. Corpus (c07): 'Spin is not an intrinsic CP property' -- emergent orbital angular momentum (vector,
   hbar/2). So even spin is l=1, not rank-2.
3. Candidate audit: DP-sea polarization = SSV_net (vector, already counted); CP spin = emergent vector;
   H_g (l=2) slot exists + shell-sum could propagate it (1113) but NOTHING excites it (broadcast is
   scalar+vector). All three fail to supply a rank-2 d.o.f.
4. Corpus-wide grep: only quadrupoles are matter-side nuclear (SS-5/SS-6) = the GR SOURCE, no field
   d.o.f. Corpus mute on a fundamental rank-2 d.o.f.

## Verdict
CPP as axiomatized = scalar-vector gravity. No spin-2 d.o.f. anywhere. Closing op:einstein (a) requires
a NEW AXIOM: a rank-2 element in one of the 3 flows (A: CSR; B: LSP; C: GP->CP). Geometrically slotted
(H_g, 1112) + propagation-ready (rank-agnostic shell-sum, 1113), but the d.o.f. is a postulate the
corpus does not make. => (a) cannot close within current axioms; GW-polarization tension is STRUCTURAL.

## What I did NOT claim
- Did NOT invent/adopt the axiom: that's the architect's decision. I specified the options + costs.
- Did NOT move any verdict; (b)/(b') CC local half unaffected.
- Honest about the negative result: CPP currently lacks gravity's tensor sector. Stated plainly, not
  softened; also constructive (the fix is fully scoped bar the postulate).

## Confidence
- Solid: the flows carry only l=0/l=1 (architect + corpus); no rank-2 anywhere; candidates fail.
- The decision (extend the axioms or accept scalar-vector gravity) is foundational, not technical.

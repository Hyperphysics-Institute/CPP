# Reasoning capture — Patch 1129 (registration)

Sign-off + lease clearance received in-session. Scope held strictly to the warned manifest
(axiom-registry.md, master_glossary.md) + private lane; frontier_sectors/SR.md, theorem-registry.md,
predictions.md deliberately EXCLUDED (each contested, each unwarned) and queued as follow-ups with
their own warns — four-guard scope discipline over convenience. Registry entry preserves the
QM-era A3 content (ψ = √ρ·e^{iφ}) inside the consolidation statement rather than deleting history,
mirrors the panel-settled T5 accounting (9 + audit note), and points to the v0.4 candidate file as
the authoritative full text. Trajectory row records the programme-defining datum: +1 degree of
freedom, 0 parameters, zero-parameter GW-sector yield. CONV-002 re-fetch executed immediately
before commit; no drift.

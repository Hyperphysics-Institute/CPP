# Velocity-Induced SSV & Time Dilation — Founder's Mechanism + Evaluation (DISCUSSION)

**Patch:** 2023 (22 June 2026) · **Window:** 2000-band · **Status: DISCUSSION / sketch — recorded, NOT a
derivation; two named gates open.** Captures the founder's velocity-SSV time-dilation picture, its evaluation
against the physics literature, and its grounding against SR-1. NO THEO; nothing counted.

---

## 1. The founder's picture (his voice, paraphrased)

The B-field mechanism (a moving charge curling the surrounding DPs about their centres, lateral to its path)
together with the increased radial DP polarization at each velocity increment toward l_P_abs/Moment, changes
the local μ,ε and hence the local experience of time. Two superposed Lorentzian systems:
- **Frame 1 — environmental (gravitational):** the GR-type SSV bias of the entire volume the mass moves
  within. Sets c_environment via the bulk SSV_abs. Isotropic; unaffected by the mass's own velocity.
- **Frame 2 — internal (kinematic):** the space within/around the neutral mass, whose μ,ε is modified by the
  KE_abs of its motion. A *neutral* mass has net E = net B = 0 (the absolute motion's induced fields cancel
  macroscopically), but the motion still raises the *local* SSV_abs (energy banked as E/B polarization), which
  is direction-dependent (along v). As v → l_P_abs/Moment the stored polarization energy → ∞ in Lorentzian
  proportion ⇒ time dilation. The bulk environment's μ,ε (outside the KE-affected region) does not change with v.
The factors "conspire" to remain invisible, reproducing the relativity principle.

## 2. Evaluation — lineage and the hard obligations

**Lineage (respectable).** This is the **Lorentzian / ether interpretation** of SR (rods really contract,
clocks really slow from motion through the substrate; SR kinematics is the observable shadow — cf. Bell, "How
to teach special relativity") fused with the **electromagnetic-mass program** (Abraham–Lorentz–Poincaré:
source the relativistic energy from stored field energy). The energy→∞ as v→c_abs IS the relativistic KE
(γ−1)mc² sourced from field/polarization energy. Legitimate, with serious pedigree.

**Obligation A — exact γ + undetectability ("the conspiracy").** Absolute uniform velocity must be locally
*undetectable* (Michelson–Morley). The Lorentzian escape requires the substrate dynamics to be **exactly**
Lorentz-invariant: contraction + dilation + clock-resync must conspire so the absolute frame is exactly
hidden. Equivalently, ΔSSV(v) must take **exactly** the (γ−1) form, not merely grow with v. Exact Lorentz
invariance or a detectable ether — no in-between. The EM-mass program got γ-like factors but tripped on the
4/3 problem, needing non-EM (Poincaré) stresses; the DP-Sea has more structure and *might* supply them, but
that must be shown.

**Obligation B — preserve Z₀ (this IS R2).** Time dilation lives in the **product** μ₀ε₀ (c = 1/√(μ₀ε₀)).
For the velocity effect to be *pure* time dilation that leaves a moving atom's α unchanged, the velocity-
induced ΔSSV must hold the **ratio** Z₀ = √(μ₀/ε₀) fixed. That is exactly R2 — now for the velocity frame.
Current status (Patches 2021/2022): Z₀ ∝ √C, **leaning FAIL**. So frame 2 inherits R2; a moving atom's α is
tested ferociously by Ives–Stilwell and accelerated optical clocks. **R2 is the shared precondition for both
frames to preserve α — same knot, both frames.**

## 3. Grounding against SR-1 (what already exists vs what is new)

Reading `series_relativity/papers/mechanism-SR-1.md`:
- **l_P_absolute IS already defined.** l_P := Voronoi insphere radius r_in = 1/(φ√2) ≈ 0.437, "the maximum
  spatial displacement a CP can execute per Absolute Moment"; SSV = 0 (undistorted lattice) is the state of
  *maximum* displacement. So **c_abs = l_P/t_P at SSV = 0 exists and is geometric** — the reference the
  founder thought might be undefined is already grounded in the 600-cell.
- **Frame 2 is already in SR-1 as PSR.** KE → ΔSSV stored as "increased dipole separation along the direction
  of motion" (explicitly anisotropic/longitudinal ⇒ length contraction L'=L₀/γ); PSR_eff = l_P/(1+k·ΔSSV);
  γ_CPP = 1 + k·ΔSSV; universal Absolute-Moment rate, only per-Moment displacement varies.
- **Frame 1** = the same ΔSSV sourced gravitationally; both frames superpose in one ΔSSV_total feeding one
  PSR_eff. The founder's two-frame split is consistent with SR-1, not a new postulate.
- **What is NEW (the founder's actual contribution).** SR-1 *asserts* "KE is stored as ΔSSV" but never
  derives the **mechanism** — it refers to it only generally as an inductive effect. The E/B-production
  mechanism under examination (moving charge → curl of DPs = B + radial polarization = E, energy banked in
  the configuration) is the **microphysics underneath SR-1's KE→ΔSSV black box**. This fills SR-1's gap.

## 4. Open gates (registered, not closed)
- **GATE-VTD-1 (exact γ):** does the induction mechanism produce ΔSSV(v) of *exactly* the (γ−1) /
  Pythagorean-budget form (internal budget = √(l_P² − (v t_P)²) ⇒ rate ∝ 1/γ exactly)? SR-1 carries both a
  linear γ_CPP=1+kΔSSV and the budget argument; exactness needs the ΔSSV(v) form pinned. This = the
  undetectability conspiracy in microphysical form.
- **GATE-VTD-2 (≡ R2):** does the velocity-induced ΔSSV preserve Z₀ (α-constant for a moving atom)? Tied to
  the R2 result (currently Z₀∝√C, leaning FAIL). Frame 2 cannot preserve α unless R2 resolves to geometric Z₀.

## 5. Proposed (integrator) — NOT edited here
SR-1 cross-ref: register that the KE→ΔSSV storage step has a candidate microphysical mechanism (the E/B
induction) and two open gates (VTD-1 exact-γ, VTD-2 ≡ R2). Defer to integrator (SR-1 is a shipped flagship).

This is discussion captured for the record. The mechanism is coherent and well-pedigreed; it inherits the
exact-γ and Z₀-preservation obligations rather than escaping them, and Z₀-preservation (R2) is currently
leaning FAIL. Nothing here is overclaimed as resolved.

---
## Update — Patch 2024: VTD-1 conditional-PASS, VTD-2 resolved, R2 reopened
VTD-1 (exact-γ): the quadrature budget gives γ exactly — PASS conditional on bulk⊥internal orthogonality.
VTD-2 (velocity α): RESOLVED — α is a Lorentz scalar, preserved given VTD-1; not an independent gate (my
"VTD-2 ≡ R2" was too strong — gravity lacks the scalar protection). R2 (gravity): the 2021 FAIL used the
phonon speed for the photon; REOPENED to OPEN. See `MISSING-MACHINERY-FOUND.md`.

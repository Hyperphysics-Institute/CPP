# Reasoning capture — Patch 2032: VTD-1 campaign handover

**STATUS: verbatim (captured at-patch).** Window: 2000-band. Opus worker; integrator = Thomas.

Thomas asked whether VTD-1 finishes in this window or needs a fresh one. Honest assessment: VTD-1 is
foundational (SR-1 budget mechanism / PCD cycle / Absolute-Moment partition), needs the founder's view (the
bulk-perp-internal orthogonality is a mechanism claim, not a solo derivation), and is likely a multi-patch arc
with its own review rounds -- comparable to the 2024-2031 universality arc. This window is 8 patches deep
post-compaction. So: hand over at the clean boundary (R2 just reached a stable conditional-PASS end-state)
rather than start VTD-1 and stall mid-derivation.

Wrote a self-contained handover (VTD-1_handover.md) while the R2 arc is fresh: the precise VTD-1 question
(is the quadrature forced [A] or assumed [B]?), why it needs TLA, a starting plan (SR-1 + PCD reads, candidate
decompositions, founder consult, CONV-001), the honest discipline that worked on R2 (follow physics not
verdict; scrutinise convenient results harder; never collapse 'narrowed' into 'removed'; NO THEO), the
environment/protocols, key files, and a definition-of-done that explicitly permits a FAIL outcome (B) so the
new window isn't primed to force PASS.

Committed the handover to the repo so the trail is preserved and the new window can read it from main. Patch
number 2032 (next sequential in the 2000-band); flagged in the handover that the new window should confirm its
band with TLA or take a fresh one if parallel.

NO THEO. Owned path.

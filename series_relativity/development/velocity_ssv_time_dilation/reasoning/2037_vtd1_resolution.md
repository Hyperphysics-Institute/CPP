# Reasoning capture — Patch 2037: VTD-1 resolved (quadrature ≡ f_eff ≡ energy-bridge)

**STATUS: verbatim (captured at-patch).** Window: 2000-band. Opus worker; integrator = Thomas.

Thomas said: continue the 2000-band at 2037, do VTD-1 now, founder's voice can catch up later. I booted on
the lightweight protocol, took the 2000-band (next free 2037 after 2034 HEAD), read the VTD-1 handover, the
MISSING-MACHINERY finding (2024), R2-STATUS, and SR-1's mechanism.

The handover framed VTD-1 as "is the quadrature forced (A) or assumed (B)?" and warned that a general
internal displacement need not be ⊥ v. I went in expecting to have to either ground the orthogonality in the
PCD cycle or report (B).

The turn came from reading the ONE appendix that looked like it would block a geometric quadrature: SR-1's
Appendix H (Geometric Insufficiency Theorem). It proves geometry alone can't give exact γ — and in the same
breath it *characterises the unique consumed fraction* f_eff = 1 − 1/γ (confirmed in reviews-SR-1.md v15
resolution). That is the whole answer. The quadrature remainder √(1−v²/c²) equals 1 − f_eff equals the
energy-bridge rate 1/(1+kΔSSV) — one object, verified to 1e-12. So the orthogonality is neither naked
geometry (Appendix H respected — we do NOT claim geometry forces it) nor a new assumption (it's the geometric
face of f_eff, an input SR-1 already owns and the A− review already accepted). VTD-1 is (A) forced **by
reduction** to SR-1's existing physical input, adding no new gap.

Discipline I held: I did NOT claim "geometry proves orthogonality" — that's the dishonest force, and it would
contradict Appendix H. I kept the FAIL branch genuinely open (the linear split IS falsified; the orthogonal
one is *licensed*, not decreed) and named the residual honestly: VTD-1 inherits the status of SR-1's
energy-momentum bridge, which is a physical identification, not a from-geometry theorem — a known SR-1
feature, not a fresh VTD-1 weakness. I did not collapse "reduced to an existing input" into "derived from
geometry." The handover's 2028→2029 lesson (don't over-claim "VTD-1 alone") is why §4 is explicit that this
does NOT upgrade the bridge and does NOT reopen rest-frame SR-1.

The orientation worry dissolves: you don't need every internal displacement ⊥ v, only the consumed fraction
= f_eff; the orthogonal-complement *magnitude* on a fixed-magnitude 4-displacement is l_P/γ regardless of
where in the complement the internal process points (the invariant-4-velocity reading, U·U = c²).

Left for TLA: a mechanism-level confirm that the budget cost is the quadrature fraction f_eff (velocity
rotates a fixed-magnitude 4-displacement timelike→spatial), not a separate linear consumption. SR-1 App. H
already commits to f_eff, so I expect a confirm — but it's a substrate statement and TLA owns it. Founder's
voice (the 2033-style capture) deferred per Thomas; this is the worker finding.

Owned greenfield path (velocity_ssv_time_dilation/). Registry/R2-STATUS/SR.md/CONJ.md edits NOT made —
deferred to integrator as the §"Proposed for integrator" block. NO THEO.

# Reasoning fragment — Patch 2063 (Round 3: broadcast-kernel boost probe)

STATUS: verbatim (captured at-patch).
Companion to `lorentz_root/2063_round3_broadcast_kernel_boost_probe.md` and
`lorentz_root/verify/2063_broadcast_cone_dispersion.py`.

## The chain of reasoning, as it ran

1. **Pull the actual broadcast kernel, not a reconstruction.** Read the A3′ axiom text (1123). Three
   clauses are load-bearing: A3 says the broadcast propagates at fixed c = l_P/t_P; C3 says the icosahedral
   PSR shell-sum's *continuum limit* is □Q = S (wave at exactly c); C4 gives the *retarded* far field
   h^TT(t_ret). So the broadcast is a genuine finite-speed retarded propagator whose continuum limit is the
   d'Alembertian. That already carries the Minkowski signature — the opposite of the Euclidean budget.

2. **The key conceptual move: the broadcast's invariant is a SPEED, not a LENGTH.** Round 2's obstruction
   was that the budget partition fixed τ = l_P as a +-quadrature *length* → compact. The broadcast fixes
   c = l_P/t_P as a *speed* (the cone slope). The same Absolute Moment supplies both, but a boost preserves
   the cone slope (speed c), not a timelike length. So the boost-relevant reading is the causal one, and it
   is Minkowski. This is the whole resolution — and it's why Round 2 and Round 3 are clean inverses.

3. **Answer the Round-2 panel's T3 caveat head-on.** The panel said ds² = (c·t_P)² − |d_spatial|² is
   bookkeeping until the broadcast *dynamically enforces* the cone. It does: the broadcast is a retarded,
   speed-c propagator by axiom (A3) and by C3/C4. So the − sign is dynamical, not algebraic. I made sure to
   state the non-circularity explicitly (input = corpus fixed-speed fact; output = Einstein's
   constant-c-preserving transforms are boosts), because T4 will probe exactly that.

4. **Verify Part A.** Cone-preserving maps fix the null vectors (1,±1), compose by rapidity addition
   (tanh, β₃ < 1 always, monotone), generator N² = +I. Exact inverse of Round 2's circle-preserving,
   sin-addition, M² = −I. Clean.

5. **Don't overclaim — find the real remaining gap.** C3 says □ holds *in the continuum limit*. So the
   broadcast carries the boost in the limit (W2), but exact-discrete (W1) is a separate question: is the
   discrete light-cone exact at finite a? That is exactly: is the discrete dispersion ω(k) linear and
   isotropic at finite a?

6. **Verify Part B — the fork.** (a) 1D leapfrog at Courant 1: ω = ck exactly (machine precision) — so a
   single-axis boost can be exact; the obstruction is isotropy across all n̂, which the target T1 demands.
   (b) cubic lattice: anisotropy ∝ q², no Courant fix → generic W2. (c) icosahedral z = 12 (the 600-cell
   coordination): anisotropy ∝ q⁴, ~10²–10³× smaller — because icosahedral symmetry has no degree-4
   anisotropic invariant (lowest harmonic l = 6) vs cubic's l = 4. The 600-cell is uniquely favourable.

7. **Stay honest about Part B.** Even icosahedral nearest-neighbour is q⁴-anisotropic, NOT zero. So the toy
   realizes W2 with a tiny floor, not W1. W1 needs the FULL nested PSR shell-sum to cancel the residual
   exactly — unproven, Round-4 target. I resisted writing "the 600-cell gives exact Lorentz"; it gives the
   strongest possible *approach* to it, with the exact-cancellation question open.

8. **World-call effect, scoped.** W3-as-real-preferred-frame ↓↓ (continuum limit IS Lorentz-invariant; the
   substrate has a genuine cone). W2 ↑ as the secured floor (modulo OPEN-SR-9 for the discrete→continuum
   step's full rigor). W1 open upside with a concrete favourable mechanism. Big narrowing; no committed
   call (Round 15).

9. **Ground it forward.** The dispersion-isotropy question IS R2 premise (i)/(ii) (c_photon scalar, not
   anisotropic f(C,Σ)) and OPEN-SR-9 (from-substrate c_photon). So Round 4 grounds existing corpus open
   items — the leverage the handover promised.

## What I deliberately did NOT do
- No claim of exact Lorentz (W1). The broadcast carries the boost in the *continuum limit*; exact-discrete
  is open.
- No THEO, no status move, no world-call. Numerics consistency-evidence only.
- Did not compute the full nested-shell 600-cell dispersion (Round 4) — only the z = 12 nearest-neighbour
  toy, and labelled it as such.

## Round-4 target (named)
Full 600-cell PSR multi-shell dispersion: does the residual q⁴ anisotropy cancel exactly at finite a (W1)
or only suppress (W2)? = R2 premise (i)/(ii) + OPEN-SR-9.

# Reasoning fragment — Patch 2062 (cycle-close, Round-2 probe review)

STATUS: verbatim (captured at-patch).
Companion to `review/reviews-2060.md` and the v1.1 calibration of
`lorentz_root/2060_round2_quaternion_boost_commutator_probe.md`.

## How I closed the cycle
1. Read all four responses (ChatGPT ×2, Grok, Copilot). Checked for any verdict-flip on a top-triage
   question — the only thing that would force a restate rather than a calibration. None. All four are
   SOUND or SOUND-WITH-CALIBRATION, and the disagreement is entirely about *one* point (T3), not about
   the demonstrated claim.

2. Separated demonstrated from hypothesized, because that is exactly what the panel pressed. Demonstrated
   (unanimous, two by execution): the static quaternion bridge is compact and cannot carry a Lorentz
   boost (T1/S2 airtight; T4 composition correct; T5 corroboration correctly read). Hypothesized (the T3
   calibration): the − sign lives in the causal A3′ broadcast — plausible, not shown. ChatGPT (both runs)
   and Copilot independently flagged that the finding's prose let ds²=(c·t_P)²−|d_spatial|² read like a
   derivation when it is an algebraic rearrangement that only *locates* the candidate.

3. Applied the calibration honestly rather than defensively: softened "the − sign lives in the broadcast"
   → "the broadcast is the *candidate*"; added the explicit bookkeeping caveat; added the calibrated
   probability statement (the key correction — Round 2 moves mass *off* the static bridge but does NOT
   raise the causal route's success probability; mass goes to the open causal route AND to W3). Added the
   Grok/Copilot precision that the circular composition assumes independent successive splits with no
   intervening broadcast. Softened "independently corroborated" → "convergent consistency signal" per
   ChatGPT T5.

4. Held the line on discipline: this is a panel verdict on a FINDING, not a THEO, and I made NO status or
   registry move (R2-STATUS, SR.md, CONJ.md, world-probability ledger all untouched). The handover keeps
   those for TLA. The panel endorses the finding and its scope; it does not authorize a status change.
   I resisted the temptation to "bank" a world-probability shift just because the panel agreed — the
   panel's own T3 note is precisely that the shift is an *elimination*, not a positive advance.

5. Left the review package untouched (immutable request record); the responses live in reviews-2060.md.

## What did NOT change
- S1–S4 substance (panel found them sound).
- The verify script and its numbers (Grok + Copilot reproduced them).
- The Round-3 target (build the boost from the A3′ retarded broadcast kernel; same three diagnostics).
- The Round-15 committed world-call (unchanged).

## Forward
Round 3 is the decisive test the calibration sharpened: does the retarded broadcast kernel carry N²=+I
(causal route live) or +-quadrature/compact (W3 obstruction evidence)?

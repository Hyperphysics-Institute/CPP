# Reasoning fragment — Patch 2482 — OPEN-SR-H1-CLASS opened: the codim-2 identity

**Session:** AUDIT-WARM-2476 continuation, 15 July 2026.
**Scope:** handover queue item 4. Campaign file:
`series_relativity/development/OPEN-SR-H1-CLASS_campaign.md`. Verify script:
`code/2482_exclusion_family_exponents.py` (stdlib, all checks pass).

## The order in which things actually happened (this is the G7 record)

1. Read App H.1's corrected setup: 4-ball, directed displacement d = f·l_P, three models,
   exponents {1, 1, 5/2} bracketing the required n = 2.
2. Designed the enumeration frame BEFORE computing anything: the systematic family is
   "f-neighborhoods of central loci," parameterized by codimension. This choice was
   motivated by completeness (it exhausts central-subspace neighborhoods and contains
   Models 1/2 as k = 1 structures), not by any exponent.
3. While writing the frame down, the exponent law n = codim became obvious by inspection
   — and with it, that codim-2 hits n = 2. From that moment the enumerator knew the
   target-hitting member before verifying anything. This is disclosed in the campaign
   file §1 rather than laundered into a "we then discovered" narrative.
4. Verified mechanically (single script, stdlib): the exponent law (C1), and then the
   thing that was NOT foreseen at step 3 — the codim-2 tube at radius exactly d gives
   V_free/V₀ = (1−f²)² exactly, hence ε = γ_SR − 1 **to all orders**, machine precision
   across f = 0.01–0.99. The −f⁴ term the fourth-root strain rule needs is exactly the
   term the tube's geometry supplies. No tuning anywhere.
5. Wrote the pre-registration (kill conditions K1–K3, mechanism burden M1–M3) BEFORE
   attempting any mechanism work, and stopped there. No mechanism candidate has been
   evaluated. The candidate list in the campaign file is deliberately unranked.

## Why the result is billed as small even though it looks enormous

Given the strain rule ε = (V_free/V₀)^(−1/4) − 1, the profile V_free/V₀ = (1−f²)² that
yields γ exists by inverse construction — anyone could write it down. Two things are
genuinely new: that this profile is realized by a natural geometric object (the d-radius
neighborhood of a central 2-plane — not an arbitrary function), and that within the
systematic family that object is the unique n = 2 member. What remains completely open is
selection: CPP must derive the distinguished 2-plane, the full-neighborhood exclusion
rule, and the radius-equals-d coefficient, forward from postulates, without referencing
the target (K3). Until then the honest sentence is: "the geometry admits γ exactly; whether
the dynamics selects it is undetermined."

The failure asymmetry is stated in the campaign file on purpose: the success branch is the
programme's biggest single result, the kill branch is a clean four-model negative, and both
are publishable — so there is no branch on which fudging helps. That sentence exists
because the k defect was produced by exactly the pressure this item now sits under.

## Contact points logged for the mechanism phase (not evaluated)

The (ê_motion, τ̂) plane is postulate-distinguished for every moving aggregate; the ZBW
rotation plane is a second natural candidate; the A3′/C0 source-clause question (parked,
per the handover) may bear on candidate (d). Reading R1 — "the obligatory drift pre-empts
a floor of the in-plane budget each Moment" — is logged as a reading, not a derivation.
The 4D→3D projection consistency requirement (M3, second half) exists because the
corrected SR-1 carries a specific τ-invariance decomposition and the identity must survive
that same treatment; a bespoke projection invented for this candidate would be a K3 flag.

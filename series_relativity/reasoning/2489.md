# Reasoning fragment — Patch 2489 (M9 stress-scaling)

*Verbatim reasoning capture. The chain as conducted, including the point where the
kill looked live and the discipline decision about who adjudicates it.*

## Reads, in order

1. `code/2486_stressed_arena_fork.py` — the pinned fork, read first so the
   derivation answers its exact definitions. Noted immediately that the script
   treats v/c as a single given number in both branch formulas. Flagged that as
   the joint to examine: the branches can only be exclusive if "v" denotes the
   same variable in both.
2. `SR_companion_papers/c07_weak_field_GR/c07_weak_field_GR.tex` — read BEFORE
   deriving, specifically to answer "what does uniform background stress do to
   rods?" from pre-target material rather than from my own construction. Found
   the channel split: g_tt ← Δ|SSV|_abs (magnitude); g_ij ← |∇SSV_net|_ij
   (gradient tensor). Uniform background ⇒ ∇ = 0 ⇒ g_ij = δ_ij ⇒ rods untouched,
   clocks slow. Also noted this split carries the factor-of-2 lensing result —
   it is anchored, pre-target, and load-bearing elsewhere.

## The chain, with the moment it could have gone wrong

Started from kinematics: the exclusion radius is the per-Moment drift,
d = v_abs·t_P (M3's registered definition). That is Branch L's formula. First
honest reaction, recorded as felt: *this looks like the kill* — Branch L is the
branch 2486 says breaks γ. The pressure at that moment was to find a reason d
should scale with PSR_eff instead. I checked whether any postulate rescales a
coordinate drift distance: none does; the aggregate is where it is in the Nexus
frame. So Branch L's formula stands, and if the kill followed, the kill would be
taken (the campaign's failure-asymmetry note exists for exactly this moment).

Before firing K1, the fork's premise had to be checked: does "γ(v)" in the kill
inference use the same v as "d = (v/c)·l_P"? The observable γ is a function of
whatever velocity local apparatus measures. That turned the question into a
corpus question — the rod/clock behavior under uniform stress — answered by
c07's channel split (read above, not constructed). With rods untouched and
clocks slowed by g_bg: v_loc = v_abs·g_bg, so f = (v_abs/c)·g_bg = v_loc/c, and
d = (v_abs/c)·l_P ≡ (v_loc/c)·PSR_eff identically. The fork dissolves: one rule,
two variables. ε = γ(v_loc) − 1 at every stress; composition multiplicative;
the c/g_bg ceiling is the coordinate ceiling at which v_loc = c — Shapiro
structure, not pathology.

Dead end / near-miss worth recording: I briefly computed the case where rods
also contract (both metric factors sourced by magnitude), got
v_loc = v_abs·g_bg², f = (v_loc/c)/g_bg, identity broken — and noticed the
temptation to dismiss that case *because* it breaks the identity. K3′ says the
dismissal must come from the corpus, not the consequence. It does: c07's g_ij is
gradient-sourced, explicitly, in a Definition that predates the campaign and
whose alternative (magnitude-sourced) would have wrecked the lensing factor of
2. The broken case is retained in the verify battery as V5 — the kill
counterfactual — precisely so the record shows the outcome was decided by
pre-target structure and the kill path was live.

## The adjudication discipline decision

The derivation defuses a registered kill trigger ("lands absolute ⇒ K1 fires").
A session that lawyers its own kill conditions is exactly the failure mode the
2471–2475 triage taught. Decision: M9 is recorded as RESOLVED-BY-DISSOLUTION,
**PENDING K2 PANEL CONFIRMATION** — not self-certified. If the panel holds the
trigger to its letter, K1 fires and the negative is taken. The three
against-interest caveats (trigger semantics, uniform-background scope, v_loc
operationalisation) are in the session file.

## Verify

`code/2489_m9_stress_scaling.py` — V1 branch-formula identity; V2 identity
survives at every stress; V3 multiplicative composition; V4 ceiling
consistency; V5 kill counterfactual. ALL PASS, stdlib.

## What this patch does NOT claim

No γ derivation. No candidate-plane work (K4 untouched — nothing here concerns
the (ê_motion, τ̂) plane). No promotion, no billing (K2). M9's dissolution is a
panel-pending finding; M1–M3 and M5–M8 remain fully open.

# Integrity audit — mechanical pass adjudication memo

**Date:** 15 July 2026 · **Session:** AUDIT-WARM-2476 continuation · **Patches:** 2477 (gate) / 2478 (this pass)
**Open item:** `OPEN-WORKFLOW-PREDICTION-AUDIT` (frontier_sectors/WORKFLOW.md) — queue items 1–2 of the 2026-07-15 handover.
**Raw output:** `2026-07-15_mechanical_pass_raw.md` (same folder; regenerate with
`python3 scripts/integrity_audit.py --all --report <path>`).

## What was run

The Patch-2477 integrity gate, programme-wide: 56 papers, every cited `.py`/`.ipynb`/`.sh`
resolved and (for `.py`) AST-checked; every paper's prose checked for live
dimensional-necessity billing, withdrawn-claim contradiction, and the WARN-band
patterns. Static pass only — cited scripts were parsed and stub-checked, not executed
(`--run` exists; execution is the per-paper adjudication step, not the sweep step).

## Headline

**19 unique integrity-class findings (F1/F4/F5/F6/F7) across 7 paper clusters.
116 reproducibility-class findings (F3, undeclared numpy/scipy/matplotlib — endemic
in pre-2471 scripts). 72 WARN-band items requiring human adjudication.**

The integrity-class concentration matches the SR-1 hypothesis: the epicenter is
SR-1/SR-2, with a genuine but smaller tail elsewhere. **No SM-series, QM-series,
EU-1, SF-4, SF-6, SF-7, or SS-5/6 integrity-class findings** — the mass-ladder
and neutrino flagships came through the mechanical pass clean at this level.

## The integrity-class queue (adjudicated where possible this session)

> **Terminology correction (Patch 2481, founder statement of 15 July — see
> `founders_voice/2026-07-15_on_unrecorded_verification_and_programme_history.md`):**
> the pre-protocol (Sonnet-era) Monte-Carlo runs OCCURRED; what failed was the
> recording — the capture protocols did not yet exist and committed placeholders
> replaced the real in-session scripts. "Fabricated" is withdrawn as the programme's
> characterization; the correct term is **unrecorded verification**, and each such
> item is a RECONSTRUCTION CANDIDATE (see the Reconstruction Track below), not a
> terminal defect. The artifact-level findings themselves are unchanged: a claim
> resting on an unrecorded run remains unverifiable until reconstructed.

### Cluster 1 — SR-1/SR-2 (8 findings) — KNOWN CASUALTY, PARTIALLY FIXED AT 2479
- `notebooks/600-cell-monte-carlo-k-fit.py` — F4 stub + F5 elision ("For brevity"). One of the four unrecorded-verification citations withdrawn at 2471. **Disposition: quarantine or delete; founder call** (names embed the retracted claim, cf. handover STILL-UNAUDITED list).
- `notebooks/600cell_monte_carlo_voronoi_k_fit.py` — F4 stub (`vertices = []`, used, never filled). Same family. Same disposition.
- `notebooks/SR-2_figures.py` — F4 stub (`raw = []`, used, never filled). **NEW: SR-2, not SR-1** — the figure-generation script for a shipped paper is a stub. Adjudicate: were SR-2's figures generated by this script as billed?
- `papers/mechanism-SR-1.md`, `papers/development-SR-1.md`, `papers/glossary-SR-1.md` — F6: the 2471-withdrawn "dimensional analysis forces the prefactor" claim LIVE, including citation of the unrecorded MC ("machine precision... 500 trials"). **FIXED at Patch 2479.** This is the ninth instance of the eight-passes lesson.
- `papers/SR-1_special_relativity_emergence.tex` — F7: three "parameter-free" billings of the γ equivalence (lines ~2185/2390/2491) survived the 2474 triage. An identity is trivially parameter-free; the phrasing bills it as a result. **FIXED at Patch 2479.**

### Cluster 2 — SF-2 (5 findings) — SERIOUS, UNRESOLVED
- `review/sf2_deltacp_scoping_review_package_v1.0.md` cites `verify_sf2_deltacp_scoping_context.py`; `review/sf2_portfolio_scoping_review_package_v1.0.md` cites `verify_sf2_portfolio_scoping_inventory.py`. **Neither script exists anywhere in the repo. These are PANEL REVIEW PACKAGES citing verification that was never committed** — the exact "a verification is cited that was never run" pattern, in documents that went to the swarm. Adjudicate: do the packages' quantitative claims rest on those scripts, and can the scripts be recreated and re-run?
- `sketches/SF-2_W0_derivation.md` cites `compute_shells.py`, `find_substructures.py`, and `/home/claude/compute_600cell_spectrum.py` — the last is a **session-container path**: in-session output cited and never committed (the SR-1 unrecorded-verification mechanism, caught in a sketch). Sketch-tier, so lower severity, but the W0 derivation's provenance is unverifiable as cited.

### Cluster 3 — SF-1 (2 findings) — PROTOCOL VIOLATION
- `reasoning/1402.md` and `reasoning/1403.md` cite `scripts/1402.py` / `scripts/1403.py`; neither exists. The reasoning-capture protocol requires .tex + fragment + verify script in ONE `git am`; the script leg of patches 1402/1403 never landed. Adjudicate: recover from the session transcript or re-derive.

### Cluster 4 — SS-9 (3 findings) — MIXED
- `documentation_suite/reasoning-SS-9.md` and `letters/SS-9_post_ship_OSF_arXiv_submission_guide.md` cite `SS-9_OPEN-SS-32_Ushape_anharmonic_phase4.py` — **wrong-name citation**: the artifact exists as `scripts/SS-9_OPEN-SS-32_Ushape_phase4_anharmonic_K3_xi4.py` (word order). Mild; fix the citations. Note one wrong-name cite is in the **OSF/arXiv submission guide**.
- `sketches/SS-9_table1_residual_fingerprint.md` cites `scripts/SS-9_table1_residual_decomposition.py` — genuinely absent. Adjudicate.

### Cluster 5 — SD-2 (1 finding)
- `SD-2_h4_angular_structure.tex` §Supplementary: "All angular calculations at `CPP/series_foundations/compute_h4_angles.py`" — file absent repo-wide. A shipped paper directing readers to supplementary code that does not exist. Recreate or withdraw the pointer.

### Downgraded during calibration (recorded so the next instance doesn't re-litigate)
- SS-1a's `mc_hadron_mass.py` — explicitly marked "(planned)" in the .tex; forward reference, not billing. W-PLANNED-SCRIPT.
- SS-9's `irreps_dim = {}` — correct branch code (rebound in the else-branch); detector fixed.
- DM-1/DM-2/DM-3 F7 hits — withdrawal tokens on 6,000-char campaign mega-lines referring to specific sub-claims already discharged (e.g., the 1870 ×3.5-margin retraction, integrated at v1.2); proximity window added.
- `parameters_600cell.py` — exists (twice); ambiguity resolved by path proximity.

## Reconstruction Track (opened Patch 2481, per the founder statement of 15 July)

The pre-protocol-era unrecorded verifications are reconstruction candidates, not
terminal defects. Reconstruction = write the script per the 2471 stdlib standard, run
it, commit it in a per-patch bundle, and update the citing artifact from
"unrecorded" to the committed path. Candidates, with targets:

| Item | Reconstruction target | Note |
|---|---|---|
| SR-1 MC trio (`600-cell-monte-carlo-k-fit.py`, `600cell_monte_carlo_voronoi_k_fit.py` + the E.2 run) | the Voronoi second-moment / stiffness integral over the 120 cells | the legitimate computation. The k-CONFIRMATION framing is NOT reconstructible — no run can confirm a convention (α-cancellation, 2471). PHEN-SR1-V2's STRUCK status therefore survives reconstruction; what reconstruction restores is the stiffness-integral datum, re-billable under OPEN-SR-C02-NORM / OPEN-SR-EPSILON. |
| SF-2 review-package verify scripts (`verify_sf2_deltacp_scoping_context.py`, `verify_sf2_portfolio_scoping_inventory.py`) | recreate from the packages' own quantitative claims; re-run; re-cite | highest priority — panel-facing documents |
| SF-2 W0 sketch scripts (`compute_shells.py`, `find_substructures.py`, `/home/claude/compute_600cell_spectrum.py`) | 600-cell shell/spectrum computations | sketch-tier; reconstruct if W0 promotes |
| SF-1 reasoning 1402/1403 script legs | recover from session transcripts if extant, else re-derive from the fragments' stated results | protocol-era items — transcripts may exist |
| SS-9 `SS-9_table1_residual_decomposition.py` | Table-1 residual decomposition | sketch-tier |
| SD-2 `compute_h4_angles.py` | H4 special-angle table backing §Experimental programme | shipped-paper supplementary pointer — reconstruct or remove the pointer |
| SS-9 wrong-name citations (×2, incl. the OSF submission guide) | not a reconstruction — a citation fix to the existing `..._phase4_anharmonic_K3_xi4.py` | trivial; queue with next SS-9 patch |

## Reproducibility class (F3 — 116 findings)

Undeclared numpy/scipy/matplotlib across essentially all pre-2471 verify scripts.
**Recommendation: do NOT mass-rewrite.** One convention patch: a repo-level
declaration (e.g., `code/README.md`: "scripts under code/ and notebooks/ may assume
numpy/scipy/matplotlib unless marked stdlib-only") plus the 2471 stdlib-only standard
for NEW scripts, then teach the gate to honor the repo-level declaration. Founder call.

## WARN band (72 items — the human queue, priority order)

1. **W5-IDENTITY-BILLING (14)** — the γ-bridge pattern. Highest value: this is the
   defect class that voided SR-1's predictions. SR-2's hit is first (it also has the
   F4 figure-stub). Then the SS-7 family.
2. **W4-CIRCULARITY? (19)** — target/expected variables assigned literals. Each needs
   a 5-minute read: is input data generated from the fitted quantity?
3. **W1-HARDCODED-PRINT (19)** — high-precision literals inside print strings. Most
   will be honest reference-value prints; each needs the same 5-minute read.
4. W2/W-NONSTDLIB-DECLARED/W-PLANNED — low priority.

## The theorem track (NOT covered by this pass — explicitly still open)

H.1 was a THEOREM, not a prediction, and it was false. The gate catches theorems
only where they cite scripts. Re-verifying proofs in `theorem-registry.md` is not
mechanizable by this tool; it requires the SR-1-style adversarial re-derivation,
theorem by theorem, highest-load-bearing first. That track remains the open half of
`OPEN-WORKFLOW-PREDICTION-AUDIT`. Candidate ordering: theorems that (i) feed the
most predictions, (ii) claim class-coverage ("for all models/geometries" — H.1's
failure mode), (iii) have no verify script.

## What this pass does and does not establish

It establishes that the corpus contains no FURTHER instance of the full five-step
k-pattern at the mechanical level, and it localizes the artifact-level debt to the
19 items above. It does NOT establish that the ~108 correspondences are genuinely
independent or genuinely zero-parameter — that is adjudication ((c)–(e) of the
WORKFLOW spec) and the theorem track, which no static tool can close.

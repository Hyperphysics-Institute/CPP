# Reasoning fragment — Patch 3307 (20 Aug 2026)

**Scope:** GR-1g V1 → V1.1 — W-D row 4 merged with the C*/F-R1
dependency audit. Same design as 3306; the audit ran first and its
findings are the notes.

## Findings specific to this paper

The KN bound and the extremality remark inherit GR-1f's two exposures
verbatim (same proof shape, evaluated at r_+, ceiling c), so the audit
note lives at the extremality remark — the one place the paper states
the saturation physics in prose — rather than duplicating GR-1f's
theorem-level note. One statement in the RN section ("charge completely
dominates and no horizon forms") is ironically ALREADY horizonless and
needed nothing.

OP-2's note introduces a phrase worth keeping in the toolbox: "still
open, now two layers deep" — the charged-evaporation problem cannot
even be posed until the uncharged surface-emission problem (GR-1e V1.1
re-opening) is solved. Rule 2 in its strongest form: not just no
upgrade, but an honest DEEPENING of the open status where the arc's
progress moved the problem's foundations.

Both correction-order estimates (r_Q²/r_S² here, (a/r_S)² in GR-1f)
were computed against the superseded near-horizon geometry; both notes
declare them unverified rather than silently keeping the numbers.

Five anchors, all assert-unique. Compile ×2: 0 errors, 9pp.

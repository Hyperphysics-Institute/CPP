# Reasoning — 3654

The dynamical member of H-WALL-LOCK-C5 was cheaper than I expected because the closure's wall ratio is
local: no interior dynamics enters the induced-metric junction, only the reconstruction. I checked the
ω → 0 limit against 3650 to four figures before trusting it — the first version of my static-limit code
dropped a term and disagreed by 30%, which the exact symbolic route exposed. Then the spectrum: no pole
where the ringdown is, and a nearly undamped mode at a third of the frequency. That is a lossless wall
doing what lossless walls do. The hypothesis fails its second member, and I have said so.

The more useful half is the impedance hypothesis's static face, which I had left as "owed" since 3647.
It costs one line (β → 0) and lands at the one 3633 reading that used the lapse as the register — inside
GW250114 by a factor of ten. Four members, one number, the static one free. Rule 6's threshold for an
axiom-reconnection attempt is met, and the static face is where to start: it asks for a count→metric map
with K/H = 1.46 at the level set, which no fixed dictionary the corpus has gives.

The thing I most want on record is §4: the closure's ratio and the data's ratio differ by a factor
between 1.5 and 2.2 at the same point. The surface is an impedance, not a dictionary.

# Patch 3382 — reasoning fragment (verbatim, Tier 4)

The founder answered both questions with pictures, and both pictures
did work.

F-1: "the same Moment is happening at the surface as at a distant
observer; the communication is separated by Moments." Simultaneity is
substrate-set; a wave delays the news, it does not tilt the now. So the
slice on which the register pins the conformal factor is the
unperturbed absolute-time slice, which for the static exterior is the
t = const slice, and any candidate slice within the G = h1 = 0 family
differs from it by the time-gauge xi_t — which 3378's L3 already showed
leaves H2 and K alone. The panel's universal caveat is discharged by a
theorem I had already proved for a different reason. That is the kind
of thing that makes me think the derivation is on rails.

F-2: "if a register doesn't see a wave, how could it interact?" I had
been treating the odd sector as governed by nothing. He turned it
around: to interact it must be registered; if it arrives by DI-bits it
IS registered — in SSV_net, the vector register. And SSV_net has no
cap. So the surface has no refusal mechanism for a shear. The axial
wave enters the core and comes back from the centre. That is 3374's
law (A) — the one I had retired for the even sector because the scalar
register is over-demanded — alive again for the odd sector, where the
relevant register is not over-demanded at all. The two sectors have
different walls because they live in different registers. I have
written the rule and checked it against the panel's eight constraints;
the computation is blocked at the interface by the c_* map, which now
blocks two things and should be minted.

Enactment: V1.8 with the folded conditions and the sentence four seats
wrote versions of. The clause "conditional on the slicing" became a
statement with an argument. The paper now says no Kerr echo frequency
is yet derived. Twelve pages; compiles clean; the ledger row is
updated; the queue moved one row.

Next, and mine: the complex poles, which decide whether the -13.4%
survives. Then the odd sector under both c_* brackets, so the map's
cost is visible in a number.

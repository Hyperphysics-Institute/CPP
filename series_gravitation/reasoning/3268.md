# Reasoning fragment — Patch 3268 (Session 150): CONV-029 dispatch (GR-1i)

**Deliverables:** the CONV-029 single-block package + receiver +
registries. Pure dispatch; the inlined script is the paper's own 3228,
unmodified (8/8 standing).

## Design decisions

1. **Single-package round** — GR-1i is the last unreviewed GR paper;
   no bundling partner exists. §4 explicitly reserved-empty rather than
   silently renumbered, to keep the section map identical across CONV
   rounds for reviewer familiarity.
2. **Scope fence:** everything CONV-027/028 settled is out of scope;
   triage 5 deliberately ASKS the panel to check GR-1i's mechanism
   language against the newly ratified FE-1 picture — a
   language-harmony check, flagged as such (the values are untouched).
3. **Triage 4 surfaces the constants-provenance call** (script GM vs
   IAU GM) as a genuine judgment question rather than hiding the
   deliberate choice made at drafting (reasoning/3252 §3).
4. **Binding rules:** flipper on Q1 blocks BOTH status moves (the
   discharge and the ship-path ride on the same derivations); Q6b
   additionally gated on Q2 discipline, per the CONV-028 pattern.
5. **Steers:** ChatGPT the perturbation bookkeeping + script run; Grok
   the deflection recompute; Gemini the Shapiro/γ honesty + constants
   call; Copilot the line-level audit vs Table 1; DeepSeek the GPS
   recompute + the FE-1 harmony check.

## Founder mechanical actions

Apply/push 3268; one identical paste per seat; return five responses
verbatim.

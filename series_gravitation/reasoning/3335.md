# Patch 3335 — reasoning fragment (verbatim, Tier 4)

CONV-034 dispatch. Design notes:

The round's real question is Q5 — the flagship prediction's
phenomenology is what changes on ratification — so the binding rules
put the strictest gate exactly there: a majority OVERCLAIMS on Q5
blocks the predictions.md execution even if Q7b clears. That is the
CONV-033 Q4 rule transplanted to where this round's weight sits.

The triage hands the panel the two things the worker would attack if
seated: the +17% single-point calibration transported across five
Kerr modes (T-1 — if any item gets softened this round, it's the
"~247–344 Hz" band), and the ℓ=2 correspondence error under the thin
burial margin (T-2 — the 0.026 margin could be INSIDE the O(1/ℓ)
mapping error, and the record says so, but the panel should rule
whether "geodesic grade" carries that honestly). T-3 exists because
the census verdict is an integer with factor-~2 margin; integers
survive quibbles, but the ¾ convention deserves one hostile look.

Instruments get separate questions (Q1/Q2) with separate revert
consequences because they are separable failures: Leg A could be
sound while the WKB census is not, and vice versa. The a→0 anchor is
in evidence for both directions — it validates Leg B against Leg A,
which means an attack that breaks Leg A also un-anchors Leg B; the
panel should see that coupling and the receiver notes it here so the
adjudication remembers to check whether any Q1 defect propagates.

Retraction audit (Q4) is a question in its own right, not buried in
Q1, because the near-published "+1%" is the session's most valuable
process artifact: a converged, parity-consistent, WRONG number. If
the panel finds the trail anything less than fully honest, that
matters more than any single physics verdict this round.

Seat economics: both scripts own-run in ~a minute total (3334
all-FAST; 3333 --fast). No excuse surface left for reference-quoting.

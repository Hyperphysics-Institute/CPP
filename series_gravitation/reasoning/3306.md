# Reasoning fragment — Patch 3306 (20 Aug 2026)

**Scope:** GR-1f V1 → V1.1 — W-D row 3 merged with the C*/F-R1
dependency audit (merge authorized in the Session-154 handover; both
passes target this paper, and the audit findings ARE the notes'
content, so separating them would have written every note twice).

## The audit, run before writing (adoption 4)

Grepped every horizon/velocity/c-consuming statement. Three hidden
consumers found beyond the c_* normalization the old note flagged:

1. **thm:kerr_bound** is the sharpest: its proof evaluates
   subluminality AT r_+. Two independent exposures: the surface (r_+
   never forms; the Kerr exclusion surface is underived) and the
   ceiling (c vs local c_* — the ratified map halves the ceiling at
   the Schwarzschild surface). The note flags both WITHOUT resolving
   either — resolving the bound for the horizonless body is real
   physics, not a status note. GP-B/LT explicitly fenced as
   unaffected so the note cannot be read as impugning the paper's
   verified results.
2. **sec:horizons** tabulates r_± as CPP structure. The ergosphere row
   genuinely survives (exterior); the Cauchy-horizon row does not.
3. **Penrose** "falling into the horizon" — re-read as surface
   absorption/reflection, which with |R| = 1 makes the ergoregion
   instability live rather than hypothetical.

## Rule discipline

Rule 1 (deliverer + limits): OP-2's note names GR-1g AND defers its
caveats to GR-1g's own W-D notes rather than duplicating; OP-4 names
GR-1h and carries the horizon-threshold caveat. Rule 2 (no upgrades):
OP-3 stays open and is actively DOWNGRADED — its (a/r_S)² estimate is
declared unverified against the amended baseline, since it was
computed on the superseded geometry. op:allorders untouched (Rule 3).

Six anchors, all assert-unique. Compile ×2: 0 errors, 10pp.

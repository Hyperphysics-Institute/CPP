# Reasoning fragment — Patch 3258 (Session 150): W-2 T-1 derivation + HALT finding

**Deliverables:** `series_gravitation/fe1_derivation/T1_derivation.md`
+ `series_gravitation/code/3258_t1_relay_verify.py` (10/10 PASS) +
registry updates. Order of operations: verify script written and run
FIRST; the derivation document transcribes machine results (no claim
precedes its computation).

## Route

1. **Frame choice — absolute lattice, not measured metric.** Forced by
   the founder's Q5 (lattice fixed/absolute, zero freedom): the DI-bit
   kernel acts on rigid GPs with variable REACH (PSR_eff), so the
   natural operator is flat-lattice with variable shell radius. This
   immediately explained a corpus fact that was previously a
   coincidence: the exact solution is conformally flat in space
   (isotropic coordinates) because the lattice IS flat — absolute
   coordinates ≡ isotropic coordinates. Coherence win recorded in §3.

2. **Statics before dynamics.** The mean-value property of harmonic
   functions holds for EVERY shell radius, so variable R(x) cannot
   deform vacuum statics: Laplace exact, solution = GR-1a profile
   exact. Checked numerically at variable R (Check 1) and symbolically
   (Check 5 tail). The Voronoi/PSR ratio cancels from every mean —
   the W-1 open-input constraint is satisfied by structure, not by
   care.

3. **Dynamics — the closure hunt.** One-level relay u⁺ = M_R u:
   eigenvalue sinc(kR) ∈ (−1,1) ⇒ all modes damp ⇒ DEAD END
   (documented; Check 3). It also contradicts messenger conservation.
   The conserved, deterministic, full-Moment-symmetric automaton
   forces the time-symmetric two-level closure
   u(t+τ)+u(t−τ) = 2M_R u(t): dispersion cos(ωτ) = sinc(kR), real ω
   for all k (unitary), long-wave speed R/(√3τ) phase AND group
   (Checks 2, 4a–c).

4. **The √3 (Finding F-1).** First-run surprise: the emergent speed is
   PSR_eff/(√3 t_P), not PSR_eff/t_P. Options considered: (a) treat as
   refutation — rejected: the kernel forces it, nothing was tunable;
   (b) absorb into the c-identification as a kinematic normalisation —
   adopted, explicitly at the same epistemic standing as k (GR-1 §7),
   with the dispersion FORM flagged as future falsifier material, not
   minted. A first-draft numeric cross-check of the speed used a
   two-point 1D kernel — WRONG reduction (1D kernel disperses as
   cos(kR), no √3) and an undersized domain; replaced with
   group-velocity extraction from the exact dispersion relation. The
   defective check and its diagnosis are preserved here per the
   dead-end discipline.

5. **The HALT finding (Check 5).** Target test per charter: static
   reduction vs GR-1c Prop field_eq. Symbolic facts (sympy, exact
   series):
   - □_g(a/kr) on the isotropic metric = −a³/(2kr⁵) + O(a⁴); the
     O(a²) coefficient cancels identically (non-obvious; verified).
   - Required compensator F* = +a³/(2kr⁵)+…
   - GR-1c's 𝓕 on the same profile: O(a⁴), coefficient class −2/r⁶,
     under flat-□, curved-□, and literal-k readings alike.
   ⇒ No reading rescues the stated formula; and the failure is against
   GR-1c's OWN exact solution, so it is not an artifact of this
   derivation's frame choice. Charter branch executed: HALT, register
   (OPEN-GR-FE1-FTERM), GR-1c untouched, adjudication to
   founder/panel. Tempting shortcut REJECTED: writing the corrigendum
   myself — expressly barred ("do not adjust GR-1c").

6. **What was NOT used:** Einstein equations, variational principles,
   metric ansätze, tuned constants, the fixed Voronoi/PSR ratio, and
   the founder's full-duplex Nexus speculation (3257 addendum) —
   unused as ruled.

## Founder-contribution block (RCP §10)

The physical content is the founder's registered picture end to end:
the GP-executor census, DI-bit conservation + equal redistribution
(→ C-i/C-ii), the origin→PSR one-hop transport (→ C-iii), full-Moment
symmetry (→ the two-level closure), compressed-DP SSV_abs as source
(→ §3.3), and the rigid absolute lattice (→ the frame choice that
made the statics exact). The worker's contribution is the mathematical
transcription and the machine checks.

## Bundle

One git am: T1_derivation.md + code/3258_t1_relay_verify.py + this
fragment + GR.md (OPEN-GR-FE-1 status → W-2 EXECUTED-WITH-HALT-FINDING;
OPEN-GR-FE1-FTERM minted) + frontier dashboard. CLONE-FIRST honored:
registry grepped for GR-1j/fe1/T1/FTERM IDs before minting (none
existed).

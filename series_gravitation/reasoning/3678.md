# Patch 3678 — reasoning fragment (verbatim, Tier 4)

I tried to pull the data and the container said no: gwosc and zenodo
are not on its list. That is a network permission, not a physics
obstacle, so I built the whole test anyway and validated it on
synthetic noise, which I can make here. The first two runs were
wrong — an SNR of eighteen thousand on pure noise, then twelve — and
both errors were mine: a factor of two in how the inverse transform
folds negative frequencies, and a pair of "quadratures" that were not
quadratures at all because I had rotated the wrong phase. The noise
told me both times. After the fixes the null peaks at five and the
injected signal at nine, four sigma apart, which is what the test
needs.

I froze the verdict before anyone sees the data, including the case
where the real noise is too ugly for the instrument to decide. Then I
wrote the two lines Thomas or Isak needs to run it on a machine that
can reach the archive. The physics of the next hour is theirs to
press; mine is the register sector.

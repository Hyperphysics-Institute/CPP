# Patch 3624 — reasoning fragment (verbatim, Tier 4)

The first static computation, and the first number that a detector
could read as the difference between the R-core and a black hole.

I derived the static equations from the linearized Ricci tensor rather
than recalling them, and they reproduced Hinderer's vacuum equation
with the known growing solution — the check that keeps me honest
since 3398. The surface condition came from the corpus's own static
picture: the interior at cap is rigid, the surface is a level set that
moves, and matching a rigid interior across a moving surface gives one
condition, the angular metric perturbation vanishing at the surface.
One condition for a second-order problem: well posed.

The number is minus 0.08, a tidal deformability of about minus seven.
A black hole has zero. A neutron star has hundreds to thousands.
Present LIGO bounds on black-hole binaries are in the hundreds; the
next generation reaches single digits. So the R-core's static
signature sits just past today's instruments and inside tomorrow's,
with no adjustable number in it.

Two things I have not claimed. The sign: it is negative in this model,
which happens for thin-shell objects in the literature, but the model
is the register-only surface, and under the June axiom the interior's
shape register can deform under a tide — the founder's own picture of
what a tide does to the Sea. That interior response enters the
matching as a component the Regge-Wheeler gauge sets to zero, so the
exterior must be transformed to meet it: one more computation, owed
before the sign is the theory's. And the magnetic Love number, which
the odd sector's transmit picture would make different again.

What I have claimed is the magnitude, which is robust across the
conditions I tried, and the fact: a surface at 1.33 Schwarzschild
radii responds to a tide, and a horizon does not.

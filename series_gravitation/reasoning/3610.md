# Patch 3610 — reasoning fragment (verbatim, Tier 4)

Two things owed, one done and one set up.

The governance note is done: AP-4's payload and A3-prime's packet are
one statement — the DP-level vectors E and S are what the vector
channel is made of, and the moments a grid point holds and broadcasts
are the count, the vector sums and the alignment tensor, rank zero,
one and two, with the receiver computing all three. A reader of AP-4
alone would have inferred a packet of rank at most one; the note
removes that inference without dropping a word of either text.

The physics is set up and not computed, on purpose. Under A3-prime the
surface has two channels for the even wave: the trace, pinned on the
level set because the count is capped, and the traceless part, which
is Q_ij content, uncapped, relaying into the core the way the odd
sector's vector content does. The coupling is fixed by clause C5. What
is not yet fixed is the answer to a question I asked at 3396 and got
wrong twice since: if the level set alone pins the exterior mode with
unit reflection, where does the energy in the transmitted traceless
wave come from? I have written the two candidate answers and refused
to choose between them by argument, because both of my previous
arguments were wrong. The tool that decides it is A3-prime's own
energy lemma — absorption is TT-only, there is no independent channel
energy — and the junction's ledger must close under it. Whichever of
"unit reflection with a decoupled interior" or "partial reflection
with a coupled interior" closes the ledger is the answer.

The gauge plan is written because that is where I would fail: the
exterior is in Regge-Wheeler gauge and the register reads harmonic-
pattern components. The map between them will be derived from the
equations, as the shift was, and checked two ways. The founder said
this is really nice and he is right; the way to keep it nice is to
compute the next step instead of asserting it.

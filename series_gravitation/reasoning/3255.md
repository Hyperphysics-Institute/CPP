# Reasoning fragment — Patch 3255 (Session 150): OPEN-GR-FE-1 W-1 — founder picture registered

**Deliverable:** `founders_voice/founder_ruling_GR-FE-1_physical_picture_2026-08-19.md`
(verbatim registration) + charter §4 W-1 execution note + GR.md status +
frontier dashboard. Pure-registration patch: no computation performed, no
verify script (RCP §1.3). Founder-contribution block: the ENTIRE patch is
the founder's contribution — the five verbatim answers ARE the content;
see the registration file (RCP §10 satisfied there, not duplicated here).

## What the answers fixed (worker reading, mirrors the labelled commentary)

1. **E-1 redirected.** Expected: per-Moment LSP-flux conservation on cell
   boundaries. Ruled: the conserved quantities are the conscious points
   themselves (GPs, CPs, DI-bits) plus the per-Moment invariant that
   every GP starts each Displace cycle with equal DI-bit count and equal
   total DI-bit influence on its PSR. W-2 therefore builds T-1 as the
   continuum statement of MESSENGER bookkeeping under the
   equal-redistribution invariant, with the PSR response as closure.

2. **E-2 narrowed.** Source = compressed-DP SSV_abs content; no
   independent kinetic-gravitation source term posited — anything
   kinetic must EMERGE from the primitives. SSV_net remains broadcast
   structure (GR-1b), sourcing g_ij as transmission, not as source.

3. **A3′ confirmed load-bearing** for the symmetry statements: "no
   symmetry in the pieces of a Moment" — invariances formulated at
   full-Moment granularity only.

4. **T-2 skeleton fixed:** rigid lattice (zero freedom), CP
   configuration the only freedom, PSR response pins the static profile;
   NEW mechanism content: multi-CP occupancy extinguishes the local
   gradient → migration toward full GP occupancy in black-hole
   configurations. Flagged for cross-check against GR-1c (Planck core)
   and GR-1e (remnant) language before any T-2 text quotes it.

5. **Open input registered:** Voronoi-cell-to-PSR scale ratio not fixed
   (founder: "I'm not sure how big a Voronoi cell is in relationship to
   the PSR"; anchor ~10^30 GPs per PSR at STP at absolute rest). W-2
   design constraint: no result may depend on the unfixed ratio, or the
   dependence is carried symbolically.

6. **Scope integrity held:** Q4's galactic-scale DM attractor content is
   DM-lane property; nothing imported into the LOCAL sector. The
   founder's "net zero locally" is registered as expectation, not
   theorem; DM-lane confirmation owed.

## Flags raised (not actioned)

- **PCD-executor wording:** the ruling refines the executor to the GP
  (CP displaces per GP-computed SSV_net; DI-bits a third conserved CP
  type). Older corpus shorthand says "CPs execute PCD." Registered as a
  glossary/axiom-impact hygiene item for the next registry pass — the
  FE-1 paper must use the ruled wording; no retroactive corpus edit at
  this patch. (Same class as the Session-146 PCD-expansion cleanup:
  terminology drift caught at registration time, fixed at hygiene time.)
- Verbatim preservation notes: the ruling text contains "PCP" (Q1,
  apparent typo for PCD) and "mettric"/"due ot" (Q5/Q4) — preserved
  verbatim per registration discipline; commentary marks [sic] only
  where ambiguity could arise.

## Gate state

W-0 (3254) → W-1 (this patch) COMPLETE. W-2 (T-1 derivation attempt)
UNBLOCKED, own session, with the HALT rule live: a T-1 whose static
reduction is not the GR-1c wave equation halts and registers — GR-1c is
not adjusted.

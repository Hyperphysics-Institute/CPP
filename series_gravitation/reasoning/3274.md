# Reasoning fragment — Patch 3274 (Session 150): W-A2 executed — legacy repairs GR-1b/1e/1h, three new figures created

Founder instruction: repair files/figures/names; create new pictures
for the missing ones. Executed:

- **GR-1b (3 defects → 0 errors):** the three SVG figure assets were
  absent from the repo entirely (never committed). THREE NEW FIGURES
  CREATED (matplotlib → PDF, committed alongside the source): fig1 the
  four-component LSP with its channel map + the equal weak-field
  potentials (the factor-of-two visually); fig2 the lensing geometry
  with α = 4GM/c²b at the limb and the half/half channel attribution
  in the title; fig3 the PSR-contraction curve with the exclusion
  radius marked ("rulers shrink; the lattice does not bend"). The .tex
  switched \includesvg → \includegraphics and \usepackage{svg} →
  graphicx (removes the inkscape build dependency); a stray \svgsetup
  removed. Captions untouched — the figures were drawn TO the captions.
- **GR-1e (\TH clash → 0 errors):** \TH is a LaTeX built-in (thorn);
  renamed \TH → \THaw at definition and all six uses.
- **GR-1h (\EP undefined → 0 errors):** the Planck-energy macro was
  used but never defined (same legacy-defect class); \EP defined
  beside \mP.

All three compile gates clean ×2. W-A2 CLOSED. PPP-1 queue now: W-B
suites ×11 → W-C chapters 1–4 ("The One Formula" opens the next book
session). Figure-creation note: the new figures are worker-drawn
schematics matched to the shipped captions; if the founder prefers a
different visual style for TATWD/CHS reuse, they regenerate from the
committed matplotlib source in reasoning — the code is the asset.

# Reasoning fragment — Patch 3297 (Session 154): OPEN-GR-RCORE-1, the reflectivity calculation and what it forced

**Deliverables:** `rcore_derivation/RCORE_derivation.md` + verify
`code/3297_rcore_verify.py` (9/9) + registry updates. Executed on the
founder's opening instruction to run the priority calculation, superseding
the queued W-D rows (which move to the next session unchanged).

## How the two open threads collapsed into one problem

The handover carried two unacted threads: the reflectivity bottleneck and
NOTE-GR-CSTAR-STRONGFIELD's five-paper exposure. Reading the CONV-027 record
showed they are the same object: the note's c_*(x) profile is the T-1 wave
speed in exactly the region where the reflectivity was uncomputed, and T-1
is precisely "the full CPP field equation evaluated in the strong-field
interior" that GR-1d Open Problem 1 named as its own missing prerequisite.
The prerequisite was discharged at Patch 3262; the calculation was simply
due.

## The reflectivity argument (why |R| = 1 is conservation, not modeling)

First instinct was to model the surface as an impedance mismatch and compute
a frequency-dependent |R(ω)| < 1. That dissolved on contact with the axiom
layer: absorption requires either storage (forbidden — the register at the
floor has no compression headroom) or a sink (forbidden — AP-4 conserves
DI-bits) or secular transfer to the core state (forbidden — the GR-1e fixed
point is stable). With all three channels closed, |R| = 1 is not an
assumption to justify but the only outcome the axioms permit. The frequency
dependence the phenomenology needs lives in the cavity and barrier, not the
wall. The phase followed from asking what the clamped register does to a
linearized perturbation: a pinned value is a fixed end — Dirichlet, phase π,
which is what GR-1d had assumed on acoustic analogy. The one-sided character
of the constraint (only compression is forbidden) is real and is registered
as a refinement rather than hidden.

## The relocation finding — found while setting up, not while concluding

Placing the wall for the scattering solve required a coordinate decision:
r_core = GM/c² in WHICH r? GR-1d treats it as areal ("inside the horizon").
But GR-1c — the paper that derives the formula — declares its coordinates
isotropic throughout, and T-1's ratified derivation states outright that
lattice coordinates ARE the isotropic coordinates. Applying the exact map:
r̄ = μ lands at areal 9μ/4, OUTSIDE the horizon. I checked this three ways
before believing it (direct map; the artanh dictionary's censored domain
k·Δ|SSV| ∈ [0,1] with the horizon at the unreachable value 2; the lapse
computed by two independent routes agreeing at exactly 1/3).

Then the recognition: 9GM/4c² is the Buchdahl radius, and lapse 1/3 is the
Buchdahl-critical surface lapse. The CPP core is incompressible by
construction (the Exclusion floor IS incompressibility), and GR's own
theorem says incompressible matter at critical pressure sits exactly at
9GM/4c². Neither result knew the other. Nothing was tunable. I am recording
this as consilience rather than prediction because it is a structural
coincidence-check, not a new measured number — but it is the strongest
single argument that the isotropic reading is physical.

## Why this fires a HALT instead of a correction

The relocation kills the ln(r_S/l_P) enhancement in GR-1d's shipped Theorem:
the delay drops from 112 ms to ~2 ms for GW150914. GR-1d's number was
consistent with its own premises (areal reading + quantum-displaced surface);
it is the premises that the later, ratified work supersedes. That is exactly
the FTERM shape: a shipped paper contradicted not by itself but by the
chartered framework, resolved by HALT + registration + adjudication, never by
silent editing. The founder should also see the exposure honestly: the
revised prediction is much easier to falsify with existing data. If LIGO
ringdown analyses already exclude ms-scale 5% echoes, the arc learns
something real — that is what the falsifier machinery is for.

## Dead ends / near-misses

- Wigner-delay resonance-spacing check: designed it, then realized the
  Buchdahl cavity is too short to trap multiple sub-barrier modes — the
  check was unphysical for this geometry. Replaced with the direct
  time-domain echo-gap measurement, which is also the actual observable.
- The measured gap (8.20 GM/c³) initially "failed" against the eikonal
  closed form (7.05) — the discrepancy was the finite-ℓ barrier-peak
  location (r ≈ 3.28μ, round trip 8.60), not an error. Assertion re-pointed
  to the true peak; the closed form retained as the quoted eikonal number.
  Lesson consistent with the standing caution: verify the OBSERVABLE.
- Considered folding the C* five-paper pass into this patch as dated notes;
  rejected — the notes' correct content depends on the §5 adjudication, and
  writing them first would bake in an unratified premise.

## Founder-contribution block (RCP §10)

The instruction to prioritize this calculation is the founder's (Session-154
opening, delegating method under PD-006). All physical inputs are previously
ratified founder-picture content (PSR formula, Exclusion floor, AP-4,
R-CSTAR-MAP); the coordinate identification is GR-1c's own and T-1's. The
derivation chain, the Buchdahl recognition, and the HALT framing are the
worker's.

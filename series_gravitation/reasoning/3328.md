# Patch 3328 — reasoning fragment (verbatim, Tier 4)

GR-1h settled notes, ratification item 3 (second paper). GR-1h is
where the 3308 pass deliberately placed the falsifier-shaped
statement "where a hostile reader would look" — the ergoregion
instability as load-bearing for rapidly spinning CPP objects. The
settled note answers it in the same spot, and does two things a
weaker note would skip: (i) it names WHERE the load moved — not
"resolved, done" but "resolved conditional on A1–A3, and here is the
panel's own counterexample (γ-weighted norm) showing the
conditionality is real, with OPEN-GR-RCORE-4 as the discharge path."
The hostile reader who found the honest weak point at V1.1 now finds
the honest residual at V1.2. (ii) It keeps the two OTHER open limbs
of the original item visibly open (scalar/vector wall,
RCORE-2(iii)) so the resolution can't be over-read.

The boson-cloud note delivers geometry only — the saturation problem
stays "unposed," matching the delivered-input-NOT-closed discipline
from the 3308 pass. The superradiant condition re-anchored to the
surface's angular velocity (not a horizon's) is the one substantive
forward pointer, tied to the co-rotation commitment in amended
RCORE-3.

Baseline compile pre-edit 0 errors 10 pp; post-edit gate 0 errors,
0 undefined, 11 pp.

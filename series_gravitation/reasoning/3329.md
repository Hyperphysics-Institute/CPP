# Patch 3329 — reasoning fragment (verbatim, Tier 4)

GR-2 V0, ratification item 6 — the founder's explicit "Please proceed
with GR-2." Design decisions:

1. HOME: series_gravitation/papers/, peer of GR-1 — GR-2 is the
   series' second paper, not a companion; ID grepped free before
   placement (CLONE-FIRST).
2. GENRE: falsifier's handbook, deliberately derivation-free. Every
   input is inherited at its ratified strength with the source table
   up front (§2), because the paper's credibility IS its provenance:
   an experimentalist should be able to audit the chain without
   reading eleven papers. The A1–A3 conditionality is stated in the
   abstract's own words per the Q4 prose law, with the panel's
   γ-weighted counterexample quoted in §2 — the paper carries its
   own strongest objection.
3. VERIFY SCRIPT with the FAST-mode subset — first enactment of the
   CONV-032 adoption, in the very paper the round unlocked. Four
   seconds-scale checks with their own count line (4/4), so the
   eventual review round's time-boxed seats can own-run the core.
4. THE FINDING: quantifying the mandated ±0.1 spin bar produced the
   session's one genuinely new number — the template saturates above
   burial onset (dΔt/dχ = 0.299 GM/c³ ⇒ ±0.35%), making the remnant-
   population template effectively mass-only. My check-6 prior
   encoded a 3–10% guess and FAILED on first run; the failure was
   the physics correcting the claim-writer. Near-miss owned in the
   script header and here: the check was re-pointed to the computed
   behavior, the original expectation preserved in the comment.
   Sharpens PRED-O-39's error budget; NOT counted as a new
   prediction (it is a property of the registered one).
5. CLAIM DISCIPLINE: "Archived data can convict this theory" closes
   the conclusion — the arc's own sentence, now with the band and
   template to make it actionable. §6 carries the search-systematics
   caveat (RCORE-2(viii)) so a null result is read against the
   selection function, not oversold either way.

§15 Step E: .tex changed this session (GR-1f, GR-1h, GR-2 new) ⇒
deposit queue re-run in this closing patch; orphan verification per
the 3319 rule.

# Patch 3325 — reasoning fragment (verbatim, Tier 4)

The founder resolved both 3324 anomalies in one message: "DeepSeek
mislabeled itself as GPT" (Seat 5 authorship confirmed — identity-echo
#1, and the receiver flag notes the aggravating detail that the echo
occurred while the seat was quoting the IDENTITY mandate verbatim in
its own reasoning), and the true GPT-5.6 Sol CONV-032 return delivered
inline. Round complete at 5/5.

The GPT return changed the adjudication materially — it is the round's
only dissent and the best kind: no counter-verdict on the theorem,
three independent recomputations, one genuine counterexample, one new
OPEN. How each dissent was adjudicated, and why:

1. Q1 SOUND-WITH-GAPS. The gap claim is simply correct: agreement on
   the a = 0 submanifold constrains nothing that vanishes as a → 0.
   No majority seat addressed this (they verified check 0 and
   stopped). Adjudicated by ADOPTING the withdrawal of "unique" —
   the construction survives as minimal; the overclaim does not. Cost
   to the result: zero. Cost of ignoring it: a hostile reader finds
   it in one line.

2. Q3 SUSPECT — the round's most interesting object. The frozen
   SURVIVES/SUSPECT vocabulary turned out to conflate a historical
   question (was the identity reverse-engineered? — the paper trail
   answers no, and GPT does not dispute it) with a structural one (is
   the specific norm load-bearing? — yes). The γ-weighted family
   F = s² + γv², γ < 1 is the counterexample the four other seats
   missed because they searched the UNWEIGHTED norm space (L¹, L∞,
   max — all of which still censor, or in the L∞ case censor
   marginally). One free weight parameter, invisible to every static
   check, controls whether the ergosphere is censored. That is the
   definition of load-bearing. Adjudication SPLIT the question:
   paper-trail SURVIVES 4–1 sustained; load-bearing-ness sustained as
   a derivation debt. Lesson for future dispatch design: when a
   frozen verdict word can carry two questions, freeze two words.

3. Q4 retirement NOT WARRANTED. Adjudicated to GPT's own proposed
   label — SUPERSEDED-CONDITIONAL-ON-A1–A3 — because it is not
   actually a compromise: the theorem's registered grade IS
   derivation-conditional-on-A1–A3, so the majority's WARRANTED and
   the minority's label describe the same state of affairs at
   different explicitness. The record takes the explicit one.

4. Q7 → OPEN-GR-RCORE-4 minted (registry grepped: RCORE-1/2/3 exist,
   4 free). The mint deliberately bundles A2's normalization and
   A3's norm/weighting as two conjuncts of ONE problem — GPT's
   framing — because they are the same question ("what functional
   does the register actually compute?") asked of two components.
   Discharging it is what upgrades model-theorem to physical theorem;
   until then the Q4-adopted prose law keeps every censorship
   sentence conditional.

Ledger items closed: Grok INDEPENDENT-HARNESS classification enacted
as adjudicated at 3324; Gemini cure confirmed effective; DeepSeek
identity-echo #1 with the mirrored per-seat cure; and a new
dispatch-design adoption from GPT's honest own-run timeout (second
consecutive round of GPT own-run friction, each time a different
failure mode: attachment format at CONV-031, runtime at CONV-032) —
verify scripts henceforth ship a FAST-mode subset with its own count
line, so a time-boxed seat can own-run at least the core identities.
The mandate's purpose is independent verification, not endurance.

What still did NOT happen: no paper edits, no predictions.md entry,
no χ_crit relabel in the 3318 file, no GR-2 opening. All seven items
sit in the ratification bundle (§3 of the adjudication) awaiting the
founder's word — the standing "no paper edits from 3320 until
CONV-032 + ratification" caution holds until that word lands. This
patch is registration + adjudication + registry only.

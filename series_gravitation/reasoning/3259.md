# Reasoning fragment — Patch 3259 (Session 150): FTERM reconciliation

**Trigger:** founder instruction post-3258 ("I don't know how to
reconcile this. Please examine and see if you can figure out what the
problem is."). **Deliverables:** fe1_derivation/FTERM_reconciliation.md
+ code/3259_fterm_reconciliation_verify.py (8/8) + registry updates.
Computation-before-claims throughout.

## Route

1. **Hypothesis selection.** Three candidate diagnoses were considered:
   (a) the 3258 frame choice was wrong (rejected up front: the failure
   was against GR-1c's OWN solution, frame-independent); (b) the exact
   profile was mis-registered in the corpus (checked and rejected:
   GR-1a/GR-1c both pin k·Δ|SSV| = GM/rc² in isotropic radius, and the
   metric construction depends on it); (c) the Proposition's equation
   was written for the wrong POTENTIAL — the compensator formula being
   a mis-transcription of the chain-rule term that appears when the
   true potential's equation is rewritten in Δ|SSV|. (c) was testable
   in one sympy line per candidate potential.

2. **Candidate potential.** The static-vacuum harmonic function of the
   Schwarzschild background is classical: integrating (√A B^{1/2} r² u')
   = const gives u ∝ ln((1−ϱ)/(1+ϱ)) = ln√(−g_tt) — the log-lapse.
   Tested: □_g N = 0 EXACT (C1). Chain rule then yields the exact
   compensator F_true = (k²u/2)/(1−(ku/2)²)·|∇u|²_g (C2 exact).

3. **The unexpected bonus — the equivalence theorem.** Probing the
   structure with a GENERIC v(r) (not assumed harmonic) returned a
   pure algebraic ratio: □_g atanh(kv/2) = [32k/((2−kv)(2+kv)⁵)]·∇²_flat v
   — no derivative terms (C3; denominator factorisation verified).
   The 3D generalisation reduces to the coefficient identity
   f″/f′ + d/du ln(√A B^{1/2}) = 0, which holds identically (C4):
   every |∇u|² term cancels, so □_g f(u) = [f′/B]·∇²_flat u in full
   3D. Consequence: the corrected measured-frame equation and the
   3258 lattice-frame T-1 statics are the SAME equation. The HALT's
   substantive content is discharged, not merely explained.

4. **Slip localisation (C6).** □ln(1+ku) does contain (∇u)² (flat
   radial identity verified), so the sketch grabbed the right building
   block; the prefactor carries one extra power of u (a² vs a¹
   leading), and the resummation is (1+ku)-shaped where the true
   potential is artanh/(1−(ku/2)²)-shaped. Honest classification:
   transcription defect in a correspondence-level Proposition.

5. **Discipline held:** GR-1c not edited (corrigendum text PROPOSED in
   §6 of the reconciliation doc; panel dispatch + founder ratification
   owed). F-3 (static superposition-in-u as a distinctive CPP feature
   vs GR nonlinear constraints) kept FLAGGED, unminted. Piecewise
   a = 2r branches identified as the horizon-coordinate surface and
   excluded as measure-zero, stated openly.

## Dead ends

- Candidate potential ln(1+ku) (the sketch's own object): □_g ln(1+ku)
  ≠ 0 on the exact profile (nonzero at O(a³)) — rejected before the
  artanh candidate; not written into the doc body to keep §2 clean,
  recorded here.
- A brief attempt to repair the stated 𝓕 by constant rescaling: ruled
  out structurally (leading orders differ by one power of u; C6).

## Founder-contribution block (RCP §10)

The reconciliation instruction and the underlying physical picture are
the founder's; the log-lapse identification, the equivalence theorem,
and the machine verification are the worker's mathematical
transcription on that picture. The corrigendum decision remains the
founder's/panel's.

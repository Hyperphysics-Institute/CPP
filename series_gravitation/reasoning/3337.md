# Patch 3337 — reasoning fragment (verbatim, Tier 4)

Founder ratified ("Ratify the bundle"). This patch enacts the two
non-.tex items; GR-2 V1.1 gets its own patch (3338) with its own
compile gate, per the one-paper-per-patch rule and the CONV-032
enactment-wave precedent (3326 non-.tex, then 3327/3328 papers).

The PRED-O-39 amendment is the most consequential registry edit of
the session, so the discipline is spelled out in the file itself:

- ANTI-ERASURE by quotation, not by pointer. The superseded Patch-3326
  language ("echo comb ... f_echo ~ 380 Hz ... parameter-free") is
  quoted verbatim inside the amendment note, so a reader who arrives
  later can see exactly what the corpus used to claim and what
  replaced it. A prediction that quietly changes shape is worse than
  one that was wrong.
- The 2.62 ms is NOT withdrawn. It would have been easy to let the
  comb's retirement drag the delay with it; it shouldn't, because the
  delay is the equatorial eikonal light-travel time and Leg A found
  the early transients still arrive on it. The amended row separates
  (a) transients at 2.62 ms from (b) the no-comb integer from (c) the
  lines near the tops — three statements at three grades, each
  labeled.
- What the round withdrew is stated as withdrawn, with the reason in
  one clause ("a single chi = 0 calibration transported across five
  modes is not an error model"). Future readers should be able to
  see the argument, not just the outcome.
- Swarm count untouched at 108: this is a refinement of an open
  prediction. Refinements don't increment; resolutions do. Checked
  the headline and tier tables — no edit owed.

The RCORE-3 registry entry now carries Leg B's adopted grade label
verbatim ("the integer census at eikonal-WKB grade") rather than a
paraphrase, because the label IS the dissent seat's contribution and
paraphrasing would soften exactly what it sharpened.

Remaining RCORE-3 is now stated as a ranked list with item (1) named
for what it would buy: full Teukolsky is the computation that would
convert the orientation-scale tops into a registered band and give
Kerr mode-dependent Q. That is the honest description of the gap the
panel found, and it is the natural next physics charter.

Founder's operational note (Fable at 75%, reset 24 Aug) registered
in the ruling file with the worker's assessment: enactment is
mechanical, Opus 5 is the right tier for it; the Teukolsky charter is
where the higher tier earns its keep.

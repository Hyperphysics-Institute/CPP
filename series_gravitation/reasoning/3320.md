# Reasoning fragment — Patch 3320 (21 Aug 2026)

**Scope:** RCORE-2(iv) proper — the Kerr exclusion surface derived
from the two-component census; the ergoregion-censorship theorem;
the prograde-ring burial finding; GR-2 template inputs.

## The construction's logic

The recon's weakness was known and stated: scalar-only. The missing
piece was the rotational census's contribution to the register
MAGNITUDE. Three assumptions carry the construction and are stated
as the panel's attack surface:

- A1 is forced: inverting the ratified dictionary gives
  s = 2(1−α)/(1+α), and the script proves this IS μ/r̄ on
  Schwarzschild — not an analogy, an identity. Any other scalar
  extension would break the ratified static limit.
- A2 required a choice among gravitomagnetic scalars. The dragging
  speed v = ωϖ/α won on three grounds: it is the azimuthal
  displacement demanded per Moment IN LOCAL REACH UNITS (the same
  units as the scalar demand — resolving the C*-vs-c ceiling
  question by construction); it is frame-invariantly defined (ZAMO
  vs static); and it turns out to be the quantity GR itself
  distinguishes: g_tt = −α²(1 − v²).
- A3 (quadrature) is the geometric statement that compression and
  circulation are orthogonal demands on the register. The panel
  should attack whether the register's capacity constraint is
  Euclidean in these components; I could not construct a principled
  alternative that preserves the a = 0 limit and isotropy, but that
  is an argument from failure of imagination, and the adjudication
  should say so.

## Why the theorem is one line

Once A1–A3 are granted, the identity g_tt = −α²(1 − v²) does
everything: the ergosphere is DEFINED by v = 1, so F ≥ 1 there with
equality impossible. I did not know this identity would make the
ergosphere exactly the circulation-saturation surface when I chose
A2 — the choice was made on the units argument, and the identity was
found while building check 2. That the ratified dictionary's natural
vector partner turns the ergosphere into a register statement is
either a deep consilience or a sign A2 was secretly reverse-
engineered; the honest defense is the paper trail: the units
argument stands on its own, and the panel can judge.

## The self-surfaced finding

Check 7 was designed to produce a two-ring template; its first run
crashed the plan: the prograde ring at χ = 0.68 is INSIDE the
derived surface. The check was redesigned around the finding rather
than the finding suppressed to save the check — prograde-ring
burial from χ ≈ 0.55 is now a registered result with its own open
problem (RCORE-3: what happens to m > 0 barrier modes when the
eikonal ring is buried — the finite-ℓ barrier is NOT the ring, so
the modes do not simply vanish; the time-domain solve decides).
Also np.trapezoid migration (numpy 2.x) recorded.

## Grade discipline

Everything is derivation-CONDITIONAL: no paper edits, no χ_crit
claims in any .tex, CONV-032 owed with A2/A3 as the named attack
surface. The 3318 number is retired in the frontier but the recon
file stands untouched (anti-erasure; it was labeled recon-grade and
behaved exactly as a recon should — conservative).

## Files

- series_gravitation/code/3320_kerr_surface_derivation_verify.py (8/8)
- series_gravitation/rcore_derivation/3320_kerr_surface_derivation.md
- frontier_sectors/GR.md, research_frontier.md
- this fragment

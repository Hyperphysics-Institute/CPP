# Reasoning — 3647 (row 6, Love number under budget interior)

The computation rests on one identity that holds exactly:

    chi_eff(rbar) = cap / v_census(rbar)

for the budget law v_eff = 2·cap − cap²/v. At the level-set surface (v = cap):
chi_eff = 1. This is not approximate; it follows directly from the algebra.

Once chi_eff|_surface = 1, the result is immediate: the level-set displacement
under a tidal perturbation is xi = −delta_v / (dv_eff/drbar)|_surface, and since
dv_eff/drbar = chi_eff × dv/drbar = 1 × dv/drbar, the displacement is identical
to the census (free, chi = 1) case computed in 3633. So k₂ is the census value.

The Hinderer landscape confirms this: the denominator zero at y* = −6.81 divides
the spring family into a RIGID branch (k₂ < 0, y < y*) and a FREE branch (k₂ > 0,
y > y*). The census y_R = −3.22 and the budget y_R = −3.22 are the same point,
on the FREE side of the pole.

The spring family gives chi* ≈ 0.50: below this, k₂ is negative; above it, positive.
The H-SURFACE-IMPEDANCE hypothesis (s = 3.22) does not specify chi_s directly; the
junction derivation is the path. The chi* crossover is the observable: it tells
which branch the hypothesis occupies once chi_s is derived.

JUNCTION-1's two-channel problem at ω = 0 (the static sector) is not computed here.
It could in principle give chi_s from the A3′ count-tensor coupling, which would
pin the hypothesis' fifth group member. That is the natural next step after attempt 2b.

No new parameters introduced. The budget law's chi_eff = 1 at the surface is a
consequence of the law's structure, not a tuning.

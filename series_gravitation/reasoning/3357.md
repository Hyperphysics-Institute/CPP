# Patch 3357 — reasoning fragment (verbatim, Tier 4)

Rung 3a. Three checks failed on the first run and each was worth
more than a pass.

K1/K3: ell = 3 converges at first order because one pole exponent is
exactly 1/2 — an infinite derivative a uniform grid resolves badly. I
applied the endpoint rule BEFORE running this time (learned at 3353),
which is why ell = 2 was already fenced; what I did not anticipate is
that exponent 1/2 is marginal in a different way from exponent 0. So
ell = 3 is reported with a Richardson extrapolation and a stated
uncertainty rather than dropped or passed off as exact. Observed
order 0.99 — it really is first order, and the record says so.

K4 is the one that corrected me. I wrote the check expecting the
s = -2 residual (beyond the trivial -s(s+1) = -2 offset) to SHRINK with
ell. It sits at +0.7 and grows. I went to find the bug and found the
physics instead: the residual is the first-order spin-weight term,
+8c/(l+1) for m = -l, and c = a*omega_top grows with ell while the
coefficient falls as 1/ell, so the product is a constant. The
measured values track that analytic prediction at every ell. The
number was right; my expectation was wrong; the check is now written
against the analytic prediction instead of against me.

Relative to A ~ ell^2 the correction does shrink — 1.3% at ell_crit —
which is the same order as the eikonal error 3354 showed cancels in
Phi. I am NOT asserting the same cancellation here, because the s = -2
census is not a drop-in Q swap: the radial operator itself changes
for s != 0. That is rung 3b's problem and I have said so in the check
rather than letting "the correction is small" slide into "the census
is unaffected."

Net: 3353's fence now has a price on it (~1.3% at the ladder), the
gravitational angular sector is validated two independent ways for
ell >= 3, and the two things still open — the radial Kerr build and
the ell = 2 endpoint regularisation — are both method work for the
higher tier, starting from instruments that have each passed a known
case. The lane has no cheap items left. That is the correct state to
hand over in.

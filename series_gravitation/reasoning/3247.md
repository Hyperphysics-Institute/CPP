# Reasoning fragment — Patch 3247 (CONV-026 cycle close)

**STATUS: verbatim (at-patch capture).** Session 149, 19 Aug 2026.

The confirmation pass came back 3/3 CONFIRMED-DISCHARGED, and the cycle
closes on the panel's answer, mechanically: the 2-of-3 rule was frozen in
the dispatch before any seat saw the edits, so the conversion of Q4 to
SHIP-PATH-CLEAR is the rule executing, not a judgment call made now.

Three things worth preserving:

1. **The cross-label recurred exactly as §4.1 predicts.** The Gemini
   window's return signed itself "ChatGPT." The standing remedy held:
   attribute by receipt, never double-count, record the mismatch. The
   verdict was unanimous, so no outcome turned on attribution — but it is
   a second data point that the in-document IDENTITY instruction reduces
   without eliminating the failure mode.

2. **A worker sequencing near-miss, owned.** The first build of this
   close was constructed against origin/main at Patch 3245 — the founder
   had pasted the confirmation block to the reviewers from the outputs
   file WITHOUT yet applying/pushing Patch 3246, so the container's
   reset-to-origin silently dropped the dispatch commit from the base.
   The registry edit's own guard assertion (`count(old) == 1` against the
   3246 frontier entry) failed and exposed the fault before anything
   shipped; the stray commit was reset away, 3246 re-applied from the
   staged patch, and the close rebuilt on the correct base. Two lessons:
   (a) "origin is the founder's state" holds only for what he has PUSHED
   — outputs-file delivery and patch application are separable acts, and
   the sync step must check for exactly this divergence; (b) the
   guard-assertion habit on every registry edit is not paranoia, it is
   the tripwire that caught this.

3. **"Ship-path clear" was scoped deliberately narrowly** everywhere this
   patch writes it: it unblocks V1.0 preparation and nothing else. The
   deposit gates are the founder's own; the lane's substantive debts are
   unchanged — the tests companion is the bounded next paper (targets
   frozen in GR-1 Table 1 and the 3228 verify precisely so it can be
   written against fixed numbers), and OPEN-GR-FE-1 remains the deep
   item. A cycle-close that quietly widened "clear" into "done" would be
   the same inflation the round just spent itself correcting.

Cycle ledger: one dispatch (3240), five independent returns, one
adjudication (3242), one restate (3243), one calibration patch (3244),
one three-seat confirm (3246), one close (this). Founder paste-labor:
eight pastes. Both packages moved under rules frozen before any seat saw
a word; worker errors sustained by the panel: none.

# Reasoning — 3670

The queue said attempt 4 on OPEN-GR-SURFACE-IMPEDANCE-1 with a two-datum target. Five surface
derivations had already failed on one datum, and 3655's candidate was a constant with a mechanism
attached after the fact. Before spending a sixth derivation I wanted to know what the target was as a
function, because the CD instrument from 3668 makes that cheap: r_w(χ) from the F_n = 4/9 rule,
Leaver's pole at each spin, one real-ω integration each. I also wanted to test the parameter-free
surface mechanisms the corpus already names — a real step, the one-Moment-delay compliance of
3375/3376, the co-rotating step — against both data at once, since a mechanism that meets one datum
is what every previous attempt had.

The first run failed my expectation at χ = 0. I had written "0.53 at −3°" as the a = 0 datum and
the machine gave 0.39 at −43°. Rule 4: locate the failure before touching the criterion. The
potential was the same code that passed T1–T4 at 3668, so the discrepancy had to be in the
reference. It was 3657 §4's row: `refl(bhZ0, wGR.real)` with bhZ0 the complex-pole value of β.
That is the same mistake 3668 §2 had found one level up — 3644's Zerilli point is a complex-ω
number — now propagated into a reflection coefficient with a real k. I had carried it into 3668's
own §3 and §5 and into the V2.5 paragraph I wrote this morning, comparing Kerr real-ω readings
against an a = 0 mixed number. So the "two data" were never the same object. The honest thing was
to state the target where the pole is actually decided, at the complex frequency, and to keep the
real-ω readings as readings.

Stated at the poles, the picture is simple and worse for any surface law than I had thought. At a
= 0 the hypothesis's law sits 0.036, 0.043, 0.058 from the horizon's admittance at ℓ = 2, 3, 4;
that is the whole reason the group passed — the box tolerates about 0.05 in β. The ratio ω/|Im β|
is not ℓ-flat (3.24 → 3.90); I had expected it to be, and the failed (B1) check told me the
descriptiveness is a small absolute mismatch, not a constant. At Kerr, from χ ≈ 0.4, Im β_hor at
the pole is positive in the even variable. No absorbing law −ik/s, no delay, no frame can change
the sign of an imaginary part. That is where 3657 and 3668 failed, and it says the failure is not
about the number or the phase but about the kind of object: at the pole the excised region is not
an absorber at all in Z⁺.

I checked the one-Moment delay numerically because it is the one surface degree of freedom the
corpus actually derived (3375/3376): at 62 M_⊙ one Planck time is 10⁻⁴⁰ M and the phase it produces
at ringdown frequencies is 10⁻³⁹ degrees. It is a phase-zero surface. That closes a door the lane
had left half-open in prose.

The conclusion I have drawn is a re-cut, not a derivation: the problem "derive s from the cycle" has
no surface solution because s is not a surface property — it is the ℓ = 2, a = 0 pole value of a GR
function, the admittance of the region the R-core replaces. A saturated register meets the target
only if it propagates the exterior's wave equation below the cap, which is the black-hole end of
V2.0's map and the same question as CORE-DISSIPATION-1. I have said this plainly rather than
numbering an attempt 5 on a surface, because another surface attempt would be another failure by
construction. CANDIDATE-S-AREA loses its object and is withdrawn; the hypothesis keeps its count and
gains an identification. I did not dispatch a panel: a re-cut of an open problem plus a record
correction is neither a win nor a stuck point under the review-economy triggers, and the founder
has had to push back on exactly that before.

What I owe from this: the V2.5 paragraph I wrote today says "a = 0 real step 0.53 at −3°" and calls
the real-ω readings "the requirement". It is restated at 3671 with the pole statement, and the
"= the horizon's own from that radius" clause that has stood since V2.3 is corrected there too.

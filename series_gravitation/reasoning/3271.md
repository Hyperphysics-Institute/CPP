# Reasoning fragment — Patch 3271 (Session 150): honest PPP audit + GR-1 formatting completion + OPEN-GR-PPP-1

**Trigger:** the founder's audit question. Honest answer: NO — the
Paper Production Protocol is not complete across the arc; the gap was
quantified by grep audit before any claim.

## Audit findings

- **.tex PD-001 sections:** GR-1 missing Keywords + PLS + CP/GP
  Signature at V1.0 (ship defect — FIXED this patch, V1.0.1); GR-1a
  missing PLS + Signature; GR-1b–h missing Signature (predate PD-001);
  GR-1i/1j complete.
- **Documentation suites:** changelog-only across the arc vs the
  ~10-file SPIN-3 standard (FAQ, development, glossary, keywords,
  mechanism, phenomena, philosophy, reasoning, reviews, transcript).
- **Anthology/TATWD:** 17 chapters exist in book_project/chapters/;
  ZERO cover the GR arc.

## Executed (bounded)

GR-1 formatting completion on the CORRECT base (see incident below):
Keywords, PLS, CP/GP Signature inserted after the abstract; V1.0 →
V1.0.1; compile clean; changelog names the trigger honestly.

## OPEN-GR-PPP-1 (registered program)

W-A: formatting pass GR-1a–h (PLS for 1a; Signature ×8) — bounded,
next warm-up. W-B: documentation suites ×11 — multi-session; source
material in-repo (CONV-026–029 records; reasoning fragments). W-C:
anthology chapters — ≈1 session each per the template; FOUNDER
DECISION FLAGGED (creative scope): chapter-per-paper (≤11) vs a
smaller arc-spine set; the FE-1 story (HALT → corrigendum → two panel
rounds → Birkhoff-in-the-census) is prime chapter material. None of
this gates the Zenodo wave mechanically; a suites-before-deposit
preference on flagship rows is flagged for the founder, not decided.

## Tool-chain incident owned

This turn's first sync (`reset --hard origin/main`) discarded the
LOCAL 3270 commit — origin was still at 3269 because the founder had
not yet applied 3270 (he asked the audit question instead). The first
edit pass therefore landed on the wrong base: the PD-001 insertion
succeeded against V0.2-state text, the V1.0.1 date replace silently
no-opped (unconditioned replace — a second defect), the changelog
append failed on the nonexistent directory, and the frontier guard
correctly refused. Recovery: 3270 restored from reflog (85a4a397),
all edits re-applied on the correct base with asserts, compile re-run.
LESSONS RECORDED: (1) never hard-sync to origin without first checking
whether the founder has applied the last presented patch — the
handover's apply-state is part of the base; (2) version-line replaces
must be asserted like every other anchor.

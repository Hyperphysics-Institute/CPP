# Reasoning fragment — Patch 3265 (Session 150): CONV-028 dispatch

**Deliverables:** the CONV-028 single-block package + receiver + registry
updates. Pure dispatch; the inlined script is 3263 unmodified.

## Design decisions

1. **Scope fence up front:** T-1, the corrigendum, R-CSTAR-MAP, and the
   annex are declared SETTLED and out of scope — prevents relitigating
   founder-ratified outcomes and keeps the round bounded.
2. **The triage hands the panel the real weaknesses:** the potential
   circularity of conditional-Birkhoff (A-3 uses T-3; both come from the
   same picture — the package asks whether that is a circle or a
   legitimate conditional), what "no-incoming" buys relative to GR's
   unconditional theorem, the decay class in A-1, the scalar-vs-rank-2
   charter-language question on T-3, and the fairness of the paper's
   CONV-027 representation.
3. **Seat steers:** ChatGPT gets the one genuinely delicate proof step
   (the independence argument's smoothness class); Grok gets the framing
   attack + a counterexample hunt; Gemini gets an explicit identity
   reminder (last round's defect) + the charter-language audit; Copilot
   repeats its strong line-level discipline audit; DeepSeek recomputes
   the two central chains and audits the handling of its own prior
   contribution.
4. **Only script 3263 inlined:** the CONV-027 scripts are settled record
   with registered panel digits; re-inlining them would invite
   re-adjudication and bloat the paste.
5. **Q6(c) SHIP-PATH-CLEAR** is deliberately "V1.0 prep may begin," not
   "deposit" — deposit remains governed by the Patch-3231 gate + the
   founder's queue approvals; the package does not leak deposit
   authority to the panel.

## Founder mechanical actions

Apply/push 3265; paste the package file's entire contents once per seat;
return the five responses verbatim.

# Patch 3326 — reasoning fragment (verbatim, Tier 4)

Founder ratified the CONV-032 bundle verbatim ("Ratify the bundle.
Please proceed with GR-2") — registered in founders_voice/ with the
enactment map. This patch enacts every item that touches no .tex:

- Item 2: the 3318 recon gets a status banner, not an edit — the
  criterion file remains the record of what the scalar sector alone
  guarantees, which is exactly why the conditional-supersession label
  (GPT's, adopted) is the honest one. If RCORE-4 someday finds A2/A3
  wrong, the scalar-only χ_crit is the fallback floor; erasing it
  would delete the theory's own safety margin.
- Item 4: PRED-O-39. Decision: register the SPIN-CORRECTED
  retrograde-keyed 2.62 ms as the prediction (with the 2.15 ms
  Schwarzschild limit inside the row), NOT two rows — the physical
  claim is one comb keyed to one ring, and the Schwarzschild number
  is its χ → 0 limit, not an independent prediction. Swarm count
  untouched: Section 2 (open), counts on search resolution. The row
  carries all three CONV-032 falsifier prongs so an experimentalist
  reading only predictions.md sees the kill conditions.
- Item 5: RCORE-3 amended in the sector file with all four Q7
  adoptions, including GPT's closing caution verbatim.

Items 3 (GR-1f/1h notes) and 6 (GR-2) are .tex work: one paper per
patch, own compile gates — 3327, 3328, 3329. Item 7 rides the next
CONV dispatch.

# Tier-4 reasoning — GR-1e (canonical pointer map)

**STATUS: reconstructed.** GR-1e predates the reasoning-capture protocol
(Session 146, Patch 0610). No verbatim at-patch fragments exist for its
authoring.

**Contemporaneous material (Tier-3, in-repo):**
- `../development/` — the paper's development folder.

**Later fragments bearing on this paper** (repo-relative to
`series_gravitation/`):
- `reasoning/3232.md` — re-identification (c10 → GR-1e).
- `reasoning/3273.md` — W-A formatting pass.
- `reasoning/3274.md` — W-A2: the `\TH` / thorn macro collision repaired
  (`\THaw`, definition plus six uses).
- `reasoning/3261.md` — CONV-027, which minted
  **NOTE-GR-CSTAR-STRONGFIELD** on the DeepSeek seat's contribution
  (census speed c_* → ~0.29c near the exclusion radius) and flagged it
  explicitly for the **GR-1d/GR-1e lane**. Never folded into this paper.
- `reasoning/3266.md` — CONV-028's extension of that flag to
  frequency-dependent dispersion and birefringence in strong fields
  (unminted).

**Forward pointer for whoever next works this paper.** The c_*
suppression lives in the same near-exclusion-radius region as the
termination argument and the stability fixed point. Whether a reduced
census speed bears on either — the geometric-breakdown criterion is
about *counting cells* and may well be untouched, while the
force-balance argument involves propagation and may not be — is an open,
bounded question nobody has asked. Recorded rather than guessed at.

**What is NOT recoverable.** The reasoning behind the geometric
termination proposition, the T_P/8π comparison, the stability fixed
point, and the information-capacity estimate was never captured. These
are also precisely the parts no external round has examined.

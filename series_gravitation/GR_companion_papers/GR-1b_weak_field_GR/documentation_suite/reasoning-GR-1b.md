# Tier-4 reasoning — GR-1b (canonical pointer map)

**STATUS: reconstructed.** GR-1b predates the reasoning-capture protocol
(registered Session 146, Patch 0610); no verbatim at-patch fragments
exist for its authoring or for the V3.2 revision. The gap is recorded
rather than filled.

**Contemporaneous material (Tier-3, in-repo):**
- `../development/` — the paper's own development folder.
- `../figures/` — the figure assets, **created in August 2026, not at
  authoring time** (see below).
- `../duplicates/` — legacy duplicate files retained from the pre-move
  filing.
- `../gr_companion.bib` — the companion bibliography.

**Later fragments bearing on this paper** (repo-relative to
`series_gravitation/`):
- `reasoning/3232.md` — re-identification (c07 → GR-1b).
- `reasoning/3273.md` — W-A: CP/GP Signature inserted.
- `reasoning/3274.md` — **W-A2, and the finding worth reading**: the
  three SVG figure assets this paper cites had never existed in the
  repository. The captions had shipped without images. Three figures
  were drawn to the shipped captions and committed; the .tex switched
  from `svg` to `graphicx`.
- `reasoning/3258.md`, `reasoning/3259.md`, `reasoning/3263.md` — the
  field-equation programme, which takes up the strong conjecture this
  paper's §6 named and deferred. Read for what became of it: T-1 derived
  from the census, a *conditional* Birkhoff theorem, a conserved current
  rather than a rank-2 tensor, and `op:einstein` still open.

**Registry material:**
- The V3.3 calibration label traces to R-CC-CALIBRATE (Patch 3125,
  CONV-020-endorsed) and was applied at Patch 3204 in the F-SW-10 R-1
  fix pass.

**What is NOT recoverable.** The reasoning behind the LSP construction,
the holographic summation, and the equal-shares result — the paper's
central quantitative claim — was never captured. A future review round
would be reconstructing the argument from the paper alone.

# Mechanism — GR-1j

The derivation's engine, in causal order. Nothing is posited at the
level of the field equation; everything descends from the census.

1. **Three registered facts about the automaton.** Grid Points compute;
   charge-bearing CPs displace once per Moment; DI-bit messengers are
   conserved, emitted at fixed per-GP count, and delivered at the PSR
   shell in one Moment (A1′, AP-3, AP-4).
2. **Three consequences, immediately.** Census *linearity* (fixed
   emission + additive tally). *Homogeneous-Sea cancellation* (a uniform
   Sea contributes nothing, so the dynamics closes on the departure
   field). The *one-hop shell kernel* on the rigid absolute lattice
   (founder ruling Q5).
3. **Statics is exact, and it is Laplace.** A shell-averaged tally in
   equilibrium satisfies the mean-value property at every radius — which
   *is* Laplace's equation, for ANY PSR profile, machine-checked
   including variable R(x). Vacuum's unique decaying spherical solution
   is the shipped relation k·Δ|SSV| = GM/rc². And the absolute lattice
   coordinates are identified with isotropic coordinates, which derives
   what had only been observed: why the corpus solution is conformally
   flat. The lattice IS flat; only rulers and clocks shrink.
4. **Dynamics: one level dead-ends, two levels are forced.** An
   irreversible one-level relay damps every mode (kernel eigenvalue
   sinc(kR) ∈ (−1,1)) and violates messenger conservation. Conservation
   plus determinism plus A3′ full-Moment symmetry force the two-level
   relay u(t+τ) + u(t−τ) = 2M_R u(t) — unitary, with dispersion
   cos(ωτ) = sinc(kR) and an emergent long-wave speed c_*, phase and
   group alike.
5. **The closure doesn't matter — and that's the result.** The panel's
   shared objection to a "unique closure" claim was conceded and
   replaced by something stronger: the whole admissible one-hop-local
   class yields the SAME continuum operator. Only the coefficient moves,
   and it is degenerate with the c-identification (R-CSTAR-MAP).
6. **Two bookkeepings, one law.** On the lattice, messenger counts ADD,
   so the potential is Δ|SSV| and statics superpose linearly. For
   observers, clock rates MULTIPLY along chains of frames, so the
   natural potential is the clock-rate LOG. □_g N = 0 exactly, and the
   generic-v identity shows the two equations are one equation in two
   variables. The measured-frame nonlinearity of static gravity is
   *dictionary*, not substrate dynamics.
7. **Uniqueness, and where Birkhoff actually lives (T-2).** The general
   static spherical vacuum solution is C₁ + C₂/r; decay kills C₁, Gauss
   matching pins C₂. But the bare equation admits monopole radiation —
   so the Birkhoff-type theorem is proved *conditionally*, on census
   conservation and no-incoming: outgoing flux must equal the census
   constant at every radius and time, forcing f′ = 0. GR stores
   no-monopole in its field equations; CPP stores it in the ledger.
   Identical physics for census-conserving sources.
8. **The source (T-3).** ρ = the compressed-DP SSV_abs excess density;
   **J** = its CP-displacement flux; continuity exact from CP
   conservation and once-per-Moment displacement, with exact-integer
   discrete checks. Weak field: ρ ↔ mass density via the Poisson
   normalization and the k + G Gauss anchor.

**The load-bearing novelty is step 7.** The theorem is not weakened by
being conditional; it is *relocated*. Knowing which structure carries a
law is worth more than asserting the law unconditionally in the wrong
place.

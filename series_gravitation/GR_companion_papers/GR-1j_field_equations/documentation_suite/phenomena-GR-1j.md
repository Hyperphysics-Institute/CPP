# Phenomena — GR-1j

**Explained (within the CPP ontology, conditional on the PSR form):**
- Why gravitational statics obeys Laplace/Poisson exactly: a conserved
  shell-averaged census in equilibrium has no choice but to be harmonic
  in vacuum. Not an approximation, not a weak-field limit — exact, for
  any PSR profile.
- Why the arc's exact solution is naturally conformally flat: the
  absolute lattice is flat and rigid, so lattice coordinates ARE
  isotropic coordinates. This converts a long-standing curiosity of the
  corpus into a derived fact.
- Why gravity propagates as a wave rather than damping: conservation
  plus time-reversal symmetry forbid the irreversible one-level relay
  and force a unitary two-level closure.
- Why static gravity looks nonlinear to an observer but linear on the
  lattice: clock rates multiply, messenger counts add. The nonlinearity
  is the dictionary between the two potentials.
- Why an isolated spherical star's gravity cannot ripple — Birkhoff's
  content, relocated: not because the equation forbids it (it doesn't;
  the bare equation admits monopole radiation) but because the ledger
  does. An isolated source's census cannot change.

**Predicted (registered but deliberately UNMINTED — no new
zero-parameter predictions are claimed):**
- The dispersion FAMILY cos(ωτ) = α·sinc(kR) + (1−α): Planck-scale,
  non-vacuous (0.82 rad separation at kR ~ 1), falsifier-shaped.
- Strong-field census-speed suppression: c_* → ~0.29c near the
  exclusion radius (NOTE-GR-CSTAR-STRONGFIELD, flagged for GR-1d/GR-1e),
  with the CONV-028 extension that a stress-dependent wave speed implies
  frequency-dependent dispersion and birefringence of gravitational
  waves in strong fields, in the eventual dynamic sector.
- Static superposition-in-Δ|SSV| (finding F-3), flagged.

**Explicitly NOT claimed (the honest boundary, stated in the paper's own
Swarm-Validation section):**
- Zero new predictions are minted. T-1's static sector reproduces
  already-counted correspondences (GR-1a's relation, Newton, linearised
  Einstein, the GR-1c metric); counting them again would double-count.
  The contribution is structural — the arc moves from "solutions with a
  correspondence claim" to "equations with solutions."
- No equivalence to the full rank-2 Einstein equations. CPP has the
  derived trace-sector current, not the full T_μν. Bounded at
  op:einstein.
- Unconditional Birkhoff. GR's version is unconditional; this one fails
  under residual incoming radiation. Vaidya-type accretion is non-static
  in both theories.
- Cosmology: excluded by scope; C₁ ≠ 0 is a cosmological embedding, and
  the uniqueness claim is explicitly "not a universal vacuum statement."

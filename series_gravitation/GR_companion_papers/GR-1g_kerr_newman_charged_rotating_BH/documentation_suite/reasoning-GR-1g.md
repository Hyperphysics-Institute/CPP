# Tier-4 reasoning — GR-1g (canonical pointer map)

**STATUS: reconstructed.** GR-1g predates the reasoning-capture protocol
(Session 146, Patch 0610). No verbatim at-patch fragments exist.

**Contemporaneous material (Tier-3, in-repo):**
- `../development/` — the paper's development folder.

**Later fragments bearing on this paper** (repo-relative to
`series_gravitation/`):
- `reasoning/3232.md` — re-identification (c12 → GR-1g).
- `reasoning/3273.md` — W-A formatting pass.
- `reasoning/3261.md` — CONV-027 and NOTE-GR-CSTAR-STRONGFIELD; as for
  GR-1f, a near-horizon census-speed reduction bears on a bound derived
  from a near-horizon velocity reaching c. Unworked.
- `reasoning/3263.md` — T-2, whose uniqueness result covers the *static
  spherically symmetric* case only; the rotating-charged sector is
  untouched by it.

**Sibling dependencies.** This paper composes results from GR-1c
(scalar), GR-1f (azimuthal, and the subluminality argument it
generalizes), and the Stiffness C companion (radial charge SSV). Its
standing is bounded by theirs; see `reviews-GR-1g.md`.

**What is NOT recoverable.** The reasoning behind the additivity claim —
that three broadcast components superpose without interference terms —
was never captured. That is also the step a future round should attack
first, so the uncaptured part and the exposed part coincide, as they do
in GR-1c, GR-1e, and GR-1f.

# CONV-041 adjudicated (5/5): the static picture survives with one open datum named — the shell's second junction condition — and three corrections made by computation: the interior argument restated (vacuum tide = lapse + conformal, both pinned; the traceless tensor needs stress), the magnetic Love number re-extracted cleanly (+0.028; the earlier route was ill-conditioned, as GPT said), and the compactness claim's standing decided (neutron stars do not saturate — the threshold is the Buchdahl bound). GR-2 V2.2 enacted

**Patch 3629, Session 161, 4 Sep 2026.** Receiver `review/reviews-CONV-041.md` (table). Verify `code/3629_conv041_adjudication_verify.py` (9/9). Reasoning `reasoning/3629.md`.

## §1 The tallies and what binds
- **Q1 SOUND 3–2** — the static equations; caveat adopted (the vacuous "or True" check in 3624 replaced by a real black-hole-limit check: Hinderer's k₂ for the horizon-regular solution vanishes to 10⁻¹⁰). Grok's D1 confirmed: **`y = −31/3` exactly** — the `K(R) = 0` log-derivative has the closed form `y(R) = −(2R² − 8MR + 4M²)/(R(R−2M))`-type rational in R; the Love number is analytic in the surface radius.
- **Q2 SWC 4–1; hidden parameter YES 4–1 → BINDING.** The second Israel junction condition — the shell's stress response — is not derived from the register; "adjusts to maintain the cap" is an unspecified response. **The Love numbers are the `K(R) = 0` member of a family over that datum.** OPEN-GR-SHELL-DATUM-1. (Gemini's NO — that the second condition merely *dictates* the induced shell stress without constraining the exterior — is the worker's original position and is the minority; the majority is right that it is an assertion until the register supplies S_ab.)
- **Q3 SWC 4–1** (SOUND folds). GPT's UNSOUND rested on "vacuum tidal curvature needs no matter" — answered by computation: the static *vacuum* tide inside a flat region is a lapse-plus-pure-trace perturbation, both register-pinned at cap; the traceless harmonic tensor has **nonzero linearized Ricci** (`(10/3)Bδ_ij`) and is therefore not a vacuum field — it needs stress. The rigid-interior conclusion stands in that form; the boundary-relative constant strain is the moving level set.
- **Q4 SWC 4–1.** GPT's numerical objection **upheld**: the 3625 extraction subtracted ~10⁹ quantities and did not converge in the extraction radius (7.7 → 7.6 → 75.6). Re-done: the static axial equation has the **exact** horizon-regular solution `h₀ = r²(r − 2M)` (no r⁻² tail — the black hole's magnetic Love number is exactly 0), the decaying solution by reduction of order (normalised to → r⁻² to 0.3%), no cancellation: **`k₂^B = +0.0277`**. Caveats carried: the shell's possible axial current (T-4); a named convention (Grok: BP vs Damour–Nagar/Landry–Poisson differ, even in sign between static and irrotational fluids — the corpus's "structural" convention is stated as its own).
- **Q5 CONSISTENT 3–2**, bridge sentence recorded: the ADM `M` is the exterior parameter = GR-1c's `M` = the count at cap by GR-1c's dictionary; the shell "rest mass" `4M/3` is GR's bookkeeping of that same `M`, not a CPP quantity.
- **Q6 SWC 4–1; standing UNDETERMINED 4–1 → decided by the computation every seat asked for.** A uniform-density body's register reaches the cap at the centre only for **`M/R ≳ 0.44` — the Buchdahl bound** — far above any neutron star (0.15–0.30). **Neutron stars do not saturate.** `C = 0.375` is a statement about collapsed (post-Buchdahl) objects; the NS mass–radius data do not test it. Copilot's EXCLUDED does not carry. OPEN-GR-SATURATION-THRESHOLD-1 for the relativistic version.
- **Q7 FAIR 3–2**, wording adopted: "a target for ET/CE pending a full-waveform PE study."
- **Q8** items adopted (listed in the receiver). **Q9a PWR 3–2. Q9b RESTATE-REQUIRED 3–2 → V2.2.**
- **EK-1:** four matches; DeepSeek's key read from a 2-decimal print — the second seat the key has caught.

## §2 What V2.2 says (enacted; 14 pp, compiles clean)
Four qualifications after the V2.1 block: the shell datum open (Love numbers = the `K(R) = 0` member of a family; magnitude robust); the rigid-interior argument in its corrected form; `k₂^B = +0.028` re-extracted; detectability as a target. And the compactness statement with its threshold: saturated objects only; neutron stars do not saturate. Ledger A1 → 2.2; PRED-O-40 annotated; changelog.

## §3 What the round bought
The picture the next derivations rest on is now honest to one named datum (the shell's stress response) instead of a hidden one, and the compactness exposure is closed at the level that matters: the theory does not claim neutron stars are R-cores, and the reason is a threshold at the Buchdahl bound — which is also a new statement: **in CPP, collapse past Buchdahl is register saturation.**

## §4 Open, ranked
OPEN-GR-SHELL-DATUM-1 (derive `S_ab` from the register cap — the item that would make the Love numbers the theory's); OPEN-GR-SATURATION-THRESHOLD-1 (relativistic; a Buchdahl-type theorem in CPP); the axial convention and shell current; CORE-CONVERSION-1; LOVE-SPIN-1; EXCITATION-1; LATTICE-FRAME-1; KERRWALL-1.

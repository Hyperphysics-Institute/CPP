# AP-5 owed items 1 and 2 done: depth arithmetic (with a conservation theorem — layer capacities cannot shrink geometrically, or a black hole's overflow could not be held) and the linear stiffness of a saturated medium, DERIVED: zero shear modulus from the 1/r² count law (icosahedral Hessian sum vanishes) plus rigid compression from the floor = the linear response of an incompressible fluid, with equality. 3685's assumed class becomes AP-5's result; 3637's flat-core stability is discharged; PRED-O-40 sharpens to Λ̃ ≈ 1.7 (full-body limit; the visible shell alone gives less) — non-zero where GR gives 0: the amendment's own prediction

**Patch 3693, Session 166, 8 Sep 2026.** Verify `code/3693_ap5_depth_and_linear_stiffness_verify.py` (5/5). Reasoning `reasoning/3693.md`. AP-5 v0.1 → v0.2 (§4 below). No paper touched; PRED-O-40's sharpening goes into `predictions.md` with GR-2 V2.8.

## §1 Depth (D1) — T1
Layer n receives layer n − 1's overflow; layer 1's demand is D = (v/cap) K, cap = ⅔. With equal capacity per layer, depth = ⌈1.5 v⌉: an ordinary 1.4 M☉ star (v ≈ 0.5) has one layer; the threshold star sits exactly at capacity; a heavy pulsar's core (central lapse ~0.4, v ≈ 0.86) has **two** layers, the second at ~30% load; the R-core surface is at capacity; the wave horizon (v = 2) has **exactly three**, the third at full load; a Planck-density region of size R has v ~ (R/l_P)² and depth ~1.5 v — very deep (founder 3691), and inside its own sea-metric wave horizon (v ≫ 2), so nothing escapes the pre-inflation state.

## §2 Conservation theorem (D4) — T2
If capacities shrank with the layer's PSR area, K_n = K/4ⁿ⁻¹, the total capacity of an infinite hierarchy is Σ K_n = (4/3) K — enough to hold demand only up to v = 8/9, below the wave horizon's v = 2. Such a hierarchy would have to *discard* overflow at every black hole. D4 forbids it. **Layer capacities must not decrease geometrically** (equal K per layer, or growing). Observation for the record, not a claim: (4/3)K is numerically the asymptote 2·cap of the budget register law v_eff = 2cap − cap²/v (3640); whether the register law is the count of some capacity-scaling hierarchy is a question for the layer-1/layer-2 re-derivation AP-5 §5 already owes.

## §3 Linear stiffness (D1 + D2 + the count law) — T3, T4, T5
- **Shear:** the Hessian of the icosahedral shell-sum Σ 1/|r_i − x| at the centre is Σ (3 r̂ r̂ᵀ − I)/a³ = 0 exactly (Σ r̂ r̂ᵀ = 4I for the 12 vertices). A lattice interacting by the 1/r² count law has no linear restoring response to a strain — the strain version of 3692's single-CP result, and the Earnshaw-type property of any harmonic interaction. **No shear modulus at linear order.**
- **Compression:** R-FLOOR-REGISTER pins CPs at the PSR floor l_P/2 ("packed at the PSR floor", 3374/3621); layer 1 cannot move a CP below it, and layer 2's cell motion is quintic (3692) — negligible compliance at small amplitude. **Rigid under compression.**
- Free under shear and rigid under compression is, exactly, the linear response of an **incompressible fluid**. So the class 3685 assumed ("at least as stiff as a fluid") is not an assumption under AP-5 but the medium's derived response — with equality: **k₂(shell) = k₂_incomp(C) at leading order = 0.0188 at C = 3/8, Λ = 1.69.** CONV-043 Q3's "class too narrow" (3–2) is answered by derivation, not by widening the class.
- **NS.2 discharged.** 3637: "stability is exactly the rigidity of the cap" — T4 supplies the rigidity, so the flat-core branch inherits the TOV branch's stability up to ~2.9 M☉, CPP passes J0740 with a flat-core star, and the signature stands: **radii rise with mass above the 1.78 M☉ knee.**
- **PRED-O-40 sharpened:** from "0 < Λ̃ ≤ 1.7" to **Λ̃ ≈ 1.7 in the full-body limit**, the visible shell 2M → 8M/3 alone giving less (its exact value with the inner boundary at v = 2 is the remaining computation). Non-zero where a GR black hole gives exactly 0: this is the amendment's own signature, the one the CONV-043 hostile pass asked for.
- **Refinement owed:** the census speed c/2 bounds signal propagation, so the medium is a causal-limit fluid with c_s ≤ c/2 rather than strictly incompressible; k₂ for a c_s = c/2 shell lies *below* the incompressible value. The bound holds; the exact Λ̃ lies between. (3649's "the budget medium is not a fluid" was a statement about the excluded budget *register* law's anisotropic stress; AP-5's medium is fluid-like at linear order by T3/T4, and its tangential stress is what the floor supplies.)

## §4 AP-5 v0.2 — status of the owed list
1. Depth arithmetic — **done** (§1), with the conservation theorem (§2) added to the clause as a constraint on D1.
2. Linear stiffness — **done at leading order** (§3); the c_s = c/2 refinement and the shell-with-inner-boundary k₂ are the remaining numbers, both bounded above by 0.0188.
3. EM coupling of cell motion (BH.6 / OPEN-GR-SEA-SHELL-EMISSION-1) — owed.
4. BB.1 check under deep nesting — owed.
With 1 and 2 in, the amendment discharges eighteen sheet lines by definition or derivation and carries one new prediction (Λ̃ ≈ 1.7) and one structural theorem (no shrinking capacities). Items 3 and 4 remain before a panel.

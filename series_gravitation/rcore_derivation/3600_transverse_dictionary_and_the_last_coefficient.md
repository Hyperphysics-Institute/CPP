# The founder's transverse dictionary: E (separation) ↔ the even vector harmonic (the shift), B (arc) ↔ the odd sector — both in SSV_net. The junction is now short exactly one coefficient, and the corpus cannot supply it: this is the rank-2 dictionary, named

**Patch 3600, Session 161, 3 Sep 2026 — first patch of the GR continuation block 3600–3699 (G-GR-BLOCK-3600).** Founder ruling R-TRANSVERSE-IS-DP-POLARIZATION. Reasoning `reasoning/3600.md`. No new computation; the record's shape changes.

## §1 What the ruling gives

The founder's picture is a dictionary at the level of *which register carries what*:

| wave content | GR name (Schwarzschild perturbation theory) | CPP register (founder) |
|---|---|---|
| scalar: trace + lapse | `K`, `H₂` (= `H₀`) — the register's two dictionaries `ψ(v)`, `N(v)` | **SSV_abs** (the count) |
| even-parity vector: gradient-type, `∇Y` | `H₁` — the shift `h_tr` | **SSV_net, E-type (DP separation)** |
| odd-parity vector: curl-type, `ε∇Y` | `h₀, h₁` — the axial sector | **SSV_net, B-type (DP arc)** |

This is exactly the E/B (electric/magnetic parity) decomposition of vector harmonics, arrived at from the substrate side. It confirms 3382's "the shear is registered in SSV_net" and refines it: the even mode's shift is `SSV_net` too, but the *gradient* part. The founder's answer names EM waves; his opening clause — "as all waves are, at the most fundamental level, summations of the underlying DI-bits" — is what makes it apply to the census wave as well.

## §2 What the junction now needs, exactly

From 3396–3398: exterior `Z⁺` → `(K, H₂, H₁)` at the wall, all three derived; interior vector wave (3384 machinery, turn-around at 0.98 M); the register counts both sides (3397). Conditions:
1. **Register level set** — `δv_total + ξv′ = 0` in both scalar dictionaries, where `δv_total` includes the returning interior wave's load on the count.
2. **E-polarization continuity** — exterior shift `H₁` ↔ interior E-type `SSV_net` wave at the surface.

Two unknowns (R, T), two conditions — well-posed — **and one coefficient**: `κ`, how a unit of E-type `SSV_net` polarization reads in the metric's `h_tr` (equivalently, in register units, how much *count* a returning E-wave adds at the surface). The corpus has `ψ(v)` and `N(v)` for the scalar register; it has **no metric dictionary for a vector-register configuration**. CONV-039 (GPT, Grok) named this as "CPP has no rank-2 dictionary"; it is now narrowed to a single number, and it is still absent.

## §3 Why the worker will not set it

- **Not by choice:** a coupling chosen to make the lines come out is the special rule C-NO-SPECIAL-RULE forbids.
- **Not by energy conservation alone:** requiring the junction to be lossless fixes `κ` only jointly with the interior flux normalisation (a second unknown of the same kind). Underdetermined.
- **Not by the exterior:** in the exterior CPP reproduces GR through the *scalar* dictionaries; the shift has no CPP exterior counterpart to match against. That is the theorem of 3378 seen from the other side — the register is not the even-parity wave, so the exterior does not fix how the wave's vector part reads in the substrate.

So `κ` is a genuine blank of the same kind as `β_SR1` was (3389): a place where the theory as written has not spoken. `β_SR1` was fixed by an observation (Mercury). `κ` would be fixed by a derivation of the DP Sea's E-polarization dynamics — the transverse (rank-2) sector the corpus has deferred since CONV-028 — or by an observation sensitive to the even sector's transmission (the returning-wave delay).

## §4 What this does to the line set (no numbers claimed)

- `κ = 0` is the closed-channel model: the V1.9 lines (195 / 292 / 208; 193 / 291 indicative) — **unchanged, correctly labelled.**
- `κ ≠ 0`: |R| < 1 on the even sector; a delayed return (single turn-around at this frequency, 3397); the sharp model poles broaden. The magnitude is `κ`-dependent and *not* computed.
- The 34% vector fraction (3398) says the effect is not perturbatively small unless `κ` is.

## §5 Standing and next
- **OPEN-GR-SHIFT-DICT-1** re-scoped: *the E-polarization ↔ metric-shift coefficient κ; the rank-2 dictionary; absent from the corpus.* It sits beside OPEN-GR-KERRWALL-1 as the second thing the flagship's next version depends on.
- Under the economy protocol this is a **trigger 2a candidate** (the worker knows exactly what is missing and that further unilateral work would be guessing), but it is not yet a *question for a panel* — it is a **question for the theory**: the DP Sea's transverse polarization dynamics as a derivation. That is the founder's domain first.
- **F-7 (physical picture):** in the substrate, when the DP Sea carries an E-type (separation) polarization wave, **does the local PSR / register respond to it at all** — i.e. does a transverse separation of DPs change what a GP counts or how far it perceives? If the register is *blind* to a pure E-polarization (only the count matters, and a tilt is not a count), then `κ = 0` at the surface, the vector channel and the register channel decouple, the even wave's shift part passes into the core without loading the level set — and the only remaining question is where its energy goes. If the register *does* respond to E-polarization, that response *is* `κ`.

Nothing enacted. GR block 3600–3699 open; 3399 left unused as the old block's terminal.

# The GW250114 survival requirement in the corpus's own dictionary: the closure's wall ratio is K/H₀ = 1 − v/2 exactly under c07 — the c07 trace lock −3(1 − v/2) = −2 at the wall. Survival (q ≥ 0.762) ⟺ **trace lock ≤ −2.29 at the wall**. c07's nonlinear lock (−2) fails; C5's linear lock (−3) passes with Λ = +9.2, k₂ = +0.102. Read as two channels: the lattice-spacing (conformal-factor) channel must respond ≥ 14% more than the clock-rate channel to the same over-supplied count — 3640 §4's question, now with a number and a direction. Physics question for the founder

**Patch 3652, Session 163, 6 Sep 2026.** Verify `code/3652_survival_condition_trace_lock_verify.py` (7/7). Reasoning `reasoning/3652.md`. No paper touched.

## §1 Closed form
The register closure (3650) gives the wall ratio `q = K/H₀ = −N/(ψ N_v)`. With c07's `N = (1 − v/2)/(1 + v/2)`, `ψ = 1 + v/2`: **`q = 1 − v/2`**, exactly; 2/3 at the cap. In 3633 §2's language the closure's static trace lock is `g^{ij}h_ij / (g^{tt}h_tt) = −3K/H₀ = −3(1 − v/2)` — the linearised-c07 lock 3633 derived — equal to −2 at the wall, where C5's linear dictionary says −3.

## §2 The two dictionaries scored
| dictionary at the wall | lock | q | k₂ | Λ | GW250114 (Λ̃ < 34.8) |
|---|---|---|---|---|---|
| c07 nonlinear (the corpus's static one; 3650 as computed) | −2 | 2/3 | +7.9 | +714 | excluded |
| C5 linear (c07 at v = 0) | −3 | 1 | +0.102 | **+9.2** | inside, ×4 to spare |

**Survival ⟺ lock ≤ −2.29 at the wall.** c07's lock reaches −2.29 only at v = 0.48, not at the cap.

## §3 The two-channel reading — 3640 §4 with a number
If the conformal factor (lattice spacing) and the lapse (clock rate) follow the same count with compliances χ_ψ and χ_N, then `q = (2/3)·χ_ψ/χ_N`. The budget law gives `χ_N = cap/v = 1` at the surface (the clock rate's own truncation). Survival needs **`χ_ψ/χ_N ≥ 1.14`**: the spatial-metric channel must respond at least 14% *more* than the clock-rate channel to the same over-supplied demand. The budget law as written (K/D truncation applied identically to both) gives exactly 1 — and fails. 3640 §4 asked whether the scalar cap and the vector scaling act on the same quantity; GW250114 now says: if they do, identically, the extension is excluded; if the lattice spacing saturates *less* than the clock (by ≥ 14%), it survives with Λ ≈ 10–35.

**Physics question for the founder (in the picture):** when a CP is over-supplied with DI-bits, does the *spacing* of the lattice around it (how far its neighbours' PSRs are displaced — the conformal factor) saturate by the same K/D fraction as its *clock* (the absolute count it can record), or does the displacement keep responding to the true demand while the count is capped? The second reading is the one that survives; it is also the one in which the tensor/vector content is "not capped" (A3′), i.e. the corpus's existing statement that the traceless `Q_ij` register is uncapped extended to the isotropic displacement.

## §4 Standing
- Row 6 survival window restated in dictionary terms: lock ≤ −2.29 (equivalently χ_ψ/χ_N ≥ 1.14). C5's linear lock satisfies it; c07's nonlinear lock does not.
- OPEN-GR-LATTICE-FRAME-1 (static face): the decision between c07 and C5 at the wall — or the derivation of χ_ψ from the PCD cycle — is now the one item that decides whether [PCD-EXT] survives GW250114. Under PD-006 this is a physics question in the founder's picture: put to him with 3652.
- If χ_ψ ≠ χ_N, the budget interior itself changes (ψ profile ≠ N profile's χ): the C¹ join, the effective stress-energy (3649), the echo cavity (3640) and the wall laws (3643–3646) all inherit it. One rerun, after the founder's answer.

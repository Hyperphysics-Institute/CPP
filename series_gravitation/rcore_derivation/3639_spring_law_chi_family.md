# R-CAP-SPRING enacted (founder, 5 Sep): the loaded register settles. The arc is static again — and it has acquired ONE number, the stiffness, written as the minimal law register = cap + χ(demand − cap). Rung 1 of the χ-family: the R-core surface stays at 8M/3 for every χ (the exterior demand is unchanged); the interior lapse runs from ½ (χ = 0, flat) to ⅓ at the centre (χ = 1, the unclamped PSR law = isotropic Schwarzschild's, no horizon); the echo cavity lengthens by T(χ)/T(0) = 1.04 / 1.11 / 1.24 / 1.56 at χ = 0.1 / 0.25 / 0.5 / 1 — PRED-O-39's 0.70 ms becomes 0.70 ms × T(χ)/T(0), so an echo delay MEASURES χ; and the surface shell's share of M falls from all of it (χ = 0) to none (χ = 1) — the spring takes up the load 3638 exposed

**Patch 3639, Session 162, 5 Sep 2026.** Verify `code/3639_spring_law_chi_family_verify.py` (8/8). Reasoning `reasoning/3639.md`. Picture `founders_voice/founder_picture_cap_is_a_spring_2026-09-05.md`. No paper touched. CONV-042 held.

## §1 What the picture settles
Elastic. A static load displaces the register and it settles; the R-core can be static under its surface stress (3638 §2); the Love numbers of 3631–3633 (a settled, displaced level set) are the right kind of computation; 3637's constrained flat-core equilibria become *spring-held* equilibria — static after all, but displaced by load/stiffness, i.e. between the flat-core star (infinite stiffness) and GR's star (zero stiffness). Every "either/or" of 3636–3638 becomes a one-parameter interpolation. The one number is the stiffness.

## §2 The minimal law, and where it sits in the corpus
`register = demand` below the cap; `cap + χ(demand − cap)` above it, `0 ≤ χ ≤ 1`. Two facts make this less arbitrary than it looks:
- The corpus's PSR law `N = (1 − v/2)/(1 + v/2)` **is** isotropic Schwarzschild's lapse and continues smoothly through v = 2/3 to N = 0 at v = 2. The floor at v = 2/3 was an added clamp (3389/3390: "second order fixed; third order assumed"). **χ = 1 is the unclamped law; χ = 0 is the clamp; the founder's spring is between.** The stiffness is the third-order question the corpus already had open.
- In the weak field the χ-law is a softening of gravity inside over-demanded regions only; everything below the cap is untouched, so no solar-system or binary-pulsar test moves.

## §3 Rung 1: the R-core under the χ-law (uniform count, census in lattice coordinates)
`v(r̄) = (M/2R̄)(3 − r̄²/R̄²)`, `v_eff = 2/3 + χ(v − 2/3)`, lapse `N(v_eff)`, conformal `ψ(v_eff)`.
- **The surface does not move:** v = 2/3 at R̄ = 3M/2 for every χ — areal 8M/3, lapse ½, the exterior Schwarzschild. GR-2's surface, cavity geometry and ringdown boundary are χ-independent in position.
- **The interior does:** central lapse ½ → ⅓ as χ: 0 → 1. No horizon for any χ ≤ 1 (v_eff ≤ 1 < 2).
- **The echo cavity lengthens:** coordinate light-crossing of the diameter, `T(χ)/T(0)` = 1 / 1.043 / 1.111 / 1.239 / 1.386 / 1.556 at χ = 0 / 0.1 / 0.25 / 0.5 / 0.75 / 1 (T(0) = 32M/3, the flat-at-½ value). **PRED-O-39 becomes a one-parameter prediction: echo delay = 0.70 ms × T(χ)/T(0) at 62 M☉ — an observed delay measures χ.** (Reflectivity and the wall's impedance also carry χ-dependence: owed.)
- **The load moves off the shell:** the interior mass function at the surface, m(R⁻)/M = 0 / 0.31 / 0.58 / 1.00 at χ = 0 / 0.25 / 0.5 / 1. At χ = 1 the surface carries no shell at all — the object is a smooth ultra-compact census ball — and the P = σ/4 load of 3638 is gone. The spring takes up exactly the load a rigid cap had to hold.

## §4 What the founder's words add beyond the law
"Collision ringing transmits internally" — the loaded interior is not opaque to waves: the R-core's interior *does* carry the ringdown inward, with a wave speed set by N/ψ² (3389's lattice hop per Moment), which the χ-law now gives as a profile. "A stiffness that prevents full PSR displacement" — χ < 1 strictly: the object is not the unclamped census ball; there is a real floor, softened. So the corpus's prediction is a *window* in χ, and the observables (§3) turn it into a number.

## §5 Standing and next
- OPEN-GR-SATURATED-CORE-1: heavy neutron stars are spring-held between flat-core (χ = 0: 11.2 km at 2.08 M☉, SLy) and GR (χ = 1: 9.9 km); NICER's 12.4 ± 1 km mildly favours the stiff end. Owed: R(2.08 M☉; χ) and the maximum mass M_max(χ) for SLy/APR4 — one scan.
- GR-2: PRED-O-39's delay is now 0.70 ms × T(χ)/T(0); the reflectivity/impedance χ-dependence owed; V2.3 must carry the χ-window. The 3633 Love-number frame question inherits χ (the level-set displacement under a tide is a spring deflection).
- OPEN-GR-SURFACE-STABILITY-1: branch (a) with a named regulator; 3390's b₂ < 0 to be re-run with the χ-interior.
- **Derivation target:** χ from the PCD cycle — the anchoring of a CP at its PSR is a statement about the register's response to excess demand, i.e. the third-order coefficient of the PSR law; if the corpus can derive it, χ is not a datum.

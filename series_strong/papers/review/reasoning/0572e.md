# Reasoning — 0572e (THEO-SPIN-1 review cycle opened)

**Session 149. Workflow-only patch:** no physics artifact changed, no theorem registration changed, no new axiom. THEO-SPIN-1 stays a candidate (Patch 0572d). This patch creates the self-contained multi-AI review package per `templates/review_dispatch_protocol.md` and opens the cycle.

**Why now:** THEO-SPIN-1 (candidate) corrects a load-bearing claim ("2:1 frequency" → radius 2 / frequency 2√2) that propagated into SS-1 and SF-4. Per the THEO-DSL-N candidate→confirmed precedent and Thomas's reviewer-sign-off criterion (canonical = correct AND reviewers sign off), the candidate goes to the panel (ChatGPT + Grok + Copilot) before the qualifier is removed.

**Package design choices:**
- IS/IS-NOT (§0) foregrounds that the *substrate model* (two DP poles at different ω held from winding by the radial-ZBW phase-lock) is a foundational input, not an A1–A11 derivation — this is the existential check, placed top of triage (§5 Q2).
- The arithmetic (2√2 from ω ∝ r^(−3/2) at radius ratio 2) is cheap and decisive; the §7 script settles it (author ran it: ALL CHECKS PASS, exact + numeric). Embedded in full per the Patch-0656 process-learning so reviewers reach SCRIPT-EXECUTED.
- Q4 explicitly asks reviewers to confirm the correction does NOT disturb SF-4's d_eff=5 closure (lock, not value) — guards against a reviewer assuming the neutrino numbers moved.

**Anti-priorities:** the package is the immutable v1.0 request record; reviewer responses aggregate later in `reviews-THEO-SPIN-1.md`. If Q2 flips (model incoherent / equal speeds forced) or Q1 flips (arithmetic), THEO-SPIN-1 → v1.1 restate; otherwise calibration + close (target 3/3), then candidate→confirmed and the count/header updates deferred at 0572d get reconciled.

**Next:** emit the Dispatch Output (done in-session); integrate responses when they return.

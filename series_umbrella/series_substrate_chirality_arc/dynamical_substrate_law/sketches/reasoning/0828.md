# Reasoning — Patch 0828 (Path A repair: pointwise refined-chord bound)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Infrastructure (corrects 0827); verdict NOT moved.

The chirality lane found a real hole in 0827 and I have to own it cleanly. 0827's average-row-sum bound (Sum_v RowSum_v <= N => avg <= 1) is rigorous and stands. But the step that actually carried the closure — "homogeneity => all row sums equal => max = avg" — is false for the physical eta. An n-hat-dependent rule is the same functional form at every vertex but is NOT H4-invariant, so the 600-cell's vertex-transitivity does not equalize the row sums; the n-hat-extremal vertices concentrate to c_max~1 and saturate a link to arc(1)=1, so max/avg ~ 1.7 and rho -> 1 at MEAN participation ~2. I reproduced it directly. What stings is that I had flagged exactly this in my own 0827 notes ("the n-hat-rule isn't perfectly vertex-transitive") and then used a transitive matching rule for the numerics and asserted max=avg for the physical eta anyway. The chirality lane was right to read the artifact and stress the one step that could fail, given the 0826 slip. I should have stress-tested my own load-bearing step instead of the convenient one.

The repair is cleaner than the thing it replaces, which is some consolation but not an excuse. Drop max=avg entirely. Bound rho through the quadratic form with a refined chord: (2/pi)arcsin(z)/z is increasing, so for z<=z*, (2/pi)arcsin(z) <= kappa z with kappa=(2/pi)arcsin(z*)/z* < 1. Then |xMx| <= 2 sum_edges kappa c^v c^w |x_v||x_w| <= kappa sum_edges[(c^v)^2 x_v^2+(c^w)^2 x_w^2] = kappa sum_v x_v^2 = kappa||x||^2, so rho(M) <= kappa(z*), where z* = (max single-edge weight, over all vertices)^2. No homogeneity, no vertex-transitivity — it holds for the very n-hat-dependent rules that broke 0827, because the only input is a POINTWISE cap on the max weight. Pointwise p>=4 => c_max<=(1/4)^.25=0.707 => z*=0.5 => kappa=2/3, margin 33%. Verified: 400 adversarial n-hat-dependent rules capped pointwise at 0.707 give worst rho=0.634 <= 2/3, matching the chirality lane's 0.642.

This answers 0922's two asks: the floor restated as POINTWISE (which is what "a local handedness at every vertex" actually means — the orientation requirement is inherently per-vertex, and the counterexample is precisely a field that has no handedness defined at the n-hat-extremal vertices), and a CLEAN closed-form rho<1 bound (the refined-chord kappa, not the too-loose max-row-sum / Cauchy-Schwarz).

I want to be honest about what is and isn't now established, because the temptation is to call it fully closed. The BOUND (piece 2 in the chirality lane's framing — "the pointwise rho<1 bound closes cleanly") is now PROVEN, not merely empirical. What remains is piece 1: that the orientation requirement IMPLIES pointwise non-degeneracy of the DYNAMICAL eta. That a genuine local handedness is non-degenerate at every vertex is definitional; whether the substrate's dynamics guarantees it (rather than letting eta degenerate at special n-hat-extremal vertices) is the eta-identity in its final, narrowest form, and if it must be derived rather than taken as definitional, that is the narrow door where the PCD layer re-enters. So the chirality lane's read is exactly right: the door to outcome 2 is narrower than before — the math is shut — but not closed; it now hinges on one physical statement.

Anti-priorities held: owned the max=avg error without hedging; did not defend 0827; the new bound is a proof (verified), not an assertion or a bigger sample; located the residual precisely (pointwise non-degeneracy of the dynamical eta) and did not over-declare outcome 1; no verdict moved; no THEO/ID/CHIR.md/package; PCD insight still held; conditional on Mechanism A. Verify 0828.

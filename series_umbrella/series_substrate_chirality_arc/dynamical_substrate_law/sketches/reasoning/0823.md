# Reasoning — Patch 0823 (residual 3: true K_c, exact margin)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Infrastructure; verdict NOT moved.

Residual 3 was meant to replace the mean-field K_c=1/12 lower bound with the true value, making the margin exact. I computed it three ways -- mean-field (0.083), Bethe-Peierls (0.091), and a direct finite-N Monte Carlo on the actual 600-cell graph (susceptibility peak ~0.100). All exceed mean-field, as fluctuations require, so the margin only widens: K_lift/K_c ~ 0.53-0.58, margin ~42-47% vs the 36% lower bound. And at K_lift the MC shows the model plainly disordered (small <|m|>, finite chi well below the peak). That is the deliverable.

The clarification I had to get right, and which I think strengthens the result: the order parameter that corresponds to EMERGENT chirality is the UNIFORM <eta> -- a net global handedness, the sign(n-hat) / det-coset condensation -- not just any eta ordering. That is the ferromagnetic threshold, which is what I computed. The measured coupling is antiferromagnetic, which suppresses exactly that uniform mode (chi_uniform = 1+12*C_nn ~ 0.36 < 1), and a staggered AFM mode is not a net handedness and is frustrated anyway. So the AFM structure is intrinsically primitive-favoring. I deliberately marked this as reinforcing, not load-bearing, because 0820 flagged the sign as somewhat convention-dependent; the load-bearing fact remains |K_lift| < K_c regardless of sign.

Two honesty notes I made sure to include. The finite-N MC K_c (0.100) is a finite-size proxy, not a sharp thermodynamic value -- the substrate is really the extended tiling, and on a single finite 600-cell there is strictly no spontaneous breaking at all. So I framed the true K_c as estimated (Bethe + finite-N proxy), bounded below by mean-field, with the exact extended-lattice value a refinement that can only widen the margin. I did not claim a single exact number; I claimed a pinned range (0.09-0.10) and a margin (42-47%) that is robustly wider than the lower bound.

The discipline held throughout: this completes the DG-3 input PACKAGE, it is not the verdict. All three residuals are now closed and none needed the PCD layer -- which is the answer to Thomas's running question -- but CAPACITY-1 and the swarm review belong to the chirality lane. I said the package is complete "for" the review, not that the theorem is proven.

Anti-priorities held: no verdict moved; no THEO/ID; no CHIR.md/registry edits; conditional on Mechanism A; finite-N and AFM-convention caveats stated. Verify 0823.

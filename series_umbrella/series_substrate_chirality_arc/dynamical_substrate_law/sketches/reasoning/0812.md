# Reasoning — Patch 0812 (§14.17 / H-NESS lift scoping, go/no-go)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Scoping pass, analogue of DM-2 Step 0; no verdict.

The first thing the read clarified is that "§14.17" is not a section to build inside the F.1 paper — it is the programme-level effective-action target (Patch 0528 §14.17). The concrete, already-reduced gate is the H-NESS lift, and 0694 had already done the expensive half: the single-walker NESS pi is constructed and characterized. So I scoped the residual (the lift), not the full effective action, which is the honest framing and keeps this from looking like a wall it no longer is.

The substantive scoping finding is that the two named lift routes converge, and the convergence is what makes it tractable. A non-interacting occupation lift gives a product measure; a product base makes the single-site/mean-field reduction exact for the symmetric part; so routes (i) and (ii) are the same object for the part that sets the sign, and only the O(delta^3) current is genuinely non-product. That collapses "which route" into "is the symmetric base a product (off-critical) measure," which is a decidable question.

Then the cross-arc connection, which I was careful to grade as an observation not an identity: the n_s/EU-1 arc already DERIVED the substrate occupation dynamics as a symmetric constant-rate ZRP with a product-Poisson stationary measure (0772-0775). That is a derived template for the symmetric base of the lift — and in a product base the connected susceptibility collapses to the on-site variance, finite and positive. If the eta-correlator inherits that structure, chi is finite-positive, which is mu^2>0, which is chirality V3 at the engine level AND (via the DM-2 0810 current-vs-skew result) clean horizon-only Lambda. Both sectors favorable from one computation. The hedge I kept front and centre: eta is the enantiomorph label, not the occupation number, so the product inheritance is exactly what the build must verify, not something I get to assume. Three arcs meeting at one substrate measure is the kind of tidy unification the handover warns about, so I stated it as "template/candidate," not "the measure."

The feasibility script does two honest things and no more: shows the NESS lift base is constructible (and correctly distinguishes a conservative tilt, J~0 detailed balance, from a non-conservative bias, J!=0 genuine NESS — I had to fix a first version where a smooth cos-tilt was secretly a gradient and gave zero current), and shows the go/no-go quantity (finite vs divergent chi) is well-posed with the product base landing in the finite-positive branch. It does NOT compute the real 600-cell chi or extract a sign — that is stage 2-3 and the sign is review-gated.

Lane discipline mattered here because I am working an object the chirality lane consumes. I drew the line at: the lift and its scoping are shared F.1 infrastructure (this window), but the sign(mu^2) -> V3->V1 extraction is verdict-moving and stays in the chirality review process (DG-3). I build/scope the lift and read the DM third moment; I do not move the chirality verdict from this window. The n_s/ZRP material is read-only reference.

Verdict: GO, tractable, not a wall — with the build sequence named (define eta on the 600-cell; compute symmetric chi finite-vs-critical in the product base; then the O(delta^3) current correction). Anti-priorities held: no verdict moved, no THEO/ID, no CHIR.md/registry/predictions edits, conditional on Mechanism A throughout. Verify 0812 as described.

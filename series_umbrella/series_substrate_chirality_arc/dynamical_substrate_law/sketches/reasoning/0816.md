# Reasoning — Patch 0816 (geometric description of sign(n̂) / FI-C-9)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Descriptive consolidation; no verdict, no new claim.

The review (0904) named the in-bounds, productive task — describe the existing primitive cleanly rather than reach for a new axiom-level claim — and Thomas asked me to take the geometry-side first pass. The discipline I held: this DESCRIBES existing objects, it does not redefine FI-C-9 or move any verdict. Everything is cited to its source (STATUS-2, MERGE-2, CHI-1, CAP-1, DSL-3, MERGE-β); I added clarity and consolidation, not content.

The single clarifying move that I think earns its place: separating n̂ the DIRECTION (P-even, T-even) from sign(n̂) the ORIENTATION BIT (P-odd pseudoscalar, T-even). That distinction is implicit across the corpus but stating it sharply is what makes "what sign(n̂) is" answerable in one line — it is the H4/H4+ coset label, a det-sign, and FI-C-9 is exactly that bit. I grounded it concretely rather than asserting it: the verify script shows the local-frame det is +1 and flips to -1 under a global reflection, with no time dependence — P-odd, T-even, as claimed. And the shell demo reproduces the 0638 spectrum and the two-nearest-shell (icosa+dodeca) breaking neighbourhood where CHI-1's phi^-3 lives, so "where on the 600-cell" is concrete too.

I was careful about one honest subtlety rather than smoothing it: the PHYSICAL actualization of the sign is not free of conditions. DSL-3 itself says σ in ω_PCD = σ·n̂ is a Layer-3 convention, physical only via the F.2 Wigner-Eckart coupling, and MERGE-β found the bridge σ_cycle = sign(n̂) is M3-undetermined. So I described sign(n̂) as the abstract primitive AND flagged that its appearance as a physical cycle-sign is gated (F.2 coupling + Layer-4 Mechanism A). I made sure to mark this as distinct from the §14.17 V3->V1 derivation question — it is about pinning the physical sign, not about deriving chirality — so the description does not accidentally reopen or conflate the parked gate.

On the "where is it actualized" question that was really driving Thomas: the geometry says it is a cosmological boundary condition (the primitive direction and its orientation selected from the degenerate manifold at some epoch), not a dynamical output — and the recent mu^2>0 result is consistent with that (engine neutral, so the handedness lives in the primitive). I tied those together because it answers the felt question ("where does the handedness live?") with the framework's own answer (FI-C-9, fixed as a boundary condition) rather than a new metaphysical claim — which is the calibrated channel the review pointed to.

I kept this in the F.1/DSL lane (geometry + DSL-3 home) and marked it a hand-off to the chirality lane, since Thomas is passing it over and it is near their machinery; I did not touch CHIR.md or any verdict registry.

Anti-priorities held: no verdict moved; no THEO/ID; no redefinition; descriptive grade stated; sources cited; M3-bridge and §14.17 left open as-is. Verify 0816 as described.

# Reasoning — Patch 0824 (residual 3 corrected: AFM threshold)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Infrastructure correction; verdict NOT moved.

The chirality lane (0911) held DG-3 on one point and they were right: I had compared K_lift to the ferromagnetic threshold 1/lambda_max=1/12 throughout 0821-0823, but the measured coupling is antiferromagnetic, whose instability is the staggered mode (lambda_min), not the uniform one. Worse, in 0823 I hand-waved this away by saying "staggered isn't net handedness" -- but a staggered state breaks the eta->-eta symmetry (the two staggered states map to each other under the flip), so it is a genuine spontaneous breaking and is verdict-relevant. Dismissing it was the error; computing it is the fix. I owned that plainly in the doc rather than reframing it as a refinement.

The fix is a one-pass computation and it comes out strongly favorable, which is also the honest reason the lane could call it "almost certainly" conservative: lambda_min = -3.71, so |lambda_min| ~ 3.71 << 12 (the 600-cell is dense in triangles, very non-bipartite). The AFM threshold is 1/|lambda_min| = 0.27, and |K_lift|*|lambda_min| = 0.197 << 1, so the margin against the CORRECT threshold is ~80%, more than double the 36% the ferromagnetic proxy showed. The frustration that made the proxy wrong is exactly what widens the true margin. The AFM MC confirms it: staggered susceptibility stays flat and tiny up to |K|=0.20 (~4x K_lift), no ordering signal. So both channels are clean -- uniform/FM is suppressed for AFM coupling, staggered/AFM is sub-critical by ~80%.

I made sure to propagate the correction to residual 1: 0821's mode-scan margins were also against the FM threshold; recomputed against the AFM one they are all ~0.18-0.20 of threshold, so "no candidate mode orders" is reinforced, not weakened. And I recorded the Q1 reconciliation (the 1.95 -> ~0 -> 0.50 history) as standing, with the full-correlator 0.50 the right value, flagged for the review to confirm rather than dismiss.

What I did NOT do is let the better number become a verdict. The margin is now ~80% against the right threshold, the trajectory is a clean primitive, but C1/C2/C3 being discharged assembles the DG-3 package -- it is not THEO-CHIR-CAPACITY-1. That verdict and the swarm review are the chirality lane's. I said the package is complete "against the correct thresholds," not that the theorem is proven.

This is the cross-lane review doing exactly its job: a long season converging on the hoped-for answer is precisely when a ferromagnetic-stand-in-for-antiferromagnetic slip could have gone to the swarm unnoticed, and the verdict-owner caught it. The right response was to take the computation, not defend the proxy.

Anti-priorities held: no verdict moved; no THEO/ID; no CHIR.md/registry edits; conditional on Mechanism A; the 0821/0823 threshold imprecision owned and corrected; residual-1 margins propagated; Q1 reconciliation recorded. Verify 0824.

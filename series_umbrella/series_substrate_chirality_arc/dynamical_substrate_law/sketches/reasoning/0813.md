# Reasoning — Patch 0813 (H-NESS lift steps 1–2: η + symmetric χ)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Infrastructure; chirality verdict NOT moved.

Step 1 was the bounded geometric task: build the 600-cell honestly (120 unit quaternions, degree-12 adjacency at edge 1/φ — verified, not assumed) and define a local enantiomorph indicator. I used a canonical-frame signed determinant: pick the 4 highest-n̂-projection neighbours, take sign det of their direction vectors. It is local (depends on N[v]), ℤ₂-valued, and flips under global reflection — a genuine pseudoscalar. One honest feature I did not hide: the homogeneous ⟨η⟩ is ≈0, not ±1. That is correct and meaningful — the bare 600-cell is achiral (H₄ has reflections), so there is no net handedness to find in the unperturbed polytope; η fluctuates about ~0 and the question is the susceptibility of those fluctuations, not a pre-existing polarization. I flagged η as a proxy for the real H₄/H₄⁺ object, because the precise field is the chirality lane's representation-theoretic call. The reason the proxy is good enough: the step-2 result depends on η being LOCAL, not on its exact form.

Step 2 is the decisive sub-result. In the product (ZRP-template) base the connected correlator is ≈1 on-site and at noise level for every d≥1 — no inter-site correlation — so χ_η = 1.0, finite, dominated by the on-site variance, with finite support (d≤2). Robust across σ (0.87–1.01). The structural statement behind the number: a local observable convolved with a zero-correlation-length measure has finite-support connected correlations, hence finite χ; divergence needs a correlated/critical base, and the n_s ZRP base is product. So finite is structurally expected, and confirmed.

The part I was most careful about is the implication, because this is verdict-adjacent and it is the favorable direction, which is exactly when discipline matters most. Finite-positive symmetric χ means χ^-1=2μ^2>0, i.e. μ^2>0, i.e. V3 stands by principle — chirality emergent at the engine level, which would close the deepest chirality question. And because the symmetric part is non-marginal, the O(δ^3) current can only perturb μ^2, not flip it, so step 3 is no longer a gate. That is a big statement, so I did three things: kept it as an IMPLICATION handed to the chirality review under DG-3 rather than a verdict I assert; stated the three real conditions plainly (η is a proxy that must be confirmed local; the η-correlator's product inheritance must be confirmed for the field not just the occupation; Mechanism A is still an axiom); and did not touch CHIR.md or any verdict registry. The infrastructure fact (finite χ) is solid and mine to compute; the verdict move (V3 stands) is theirs to make.

For DM-2 (mine to read): no criticality in the symmetric base + symmetric measure carrying no skew (0810) confirms clean horizon-only Λ on the favorable branch, and the O(δ^3) current's skew is the separate divergence-free residual 0810 already isolated. So both sectors land favorably from one computation, which is what the scoping pass said they would if χ came out finite.

I resisted the pull to declare "chirality is emergent, DM-2 Λ is clean, done." It is the favorable branch and a strong result, but it rides on the three conditions and on a review I do not own. The honest status is: the cheapest decisive sub-result came out favorable, conditionally, with the verdict handed to the right lane.

Anti-priorities held: no chirality verdict moved; no THEO/ID; no CHIR.md/registry/predictions edits; conditional on Mechanism A. Verify 0813 as described.

# Reasoning — Patch 0827 (Path A: weight-concentration via vertex-transitivity)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Infrastructure (Path-A two-part computation); verdict NOT moved; DG-3/CAPACITY-1 stay chirality-lane.

The re-fire (0920, built on my 0826) came back 2 CONFIRM / 1 RESTATE, and ChatGPT's lone RESTATE is a real falsifier: my R(m) assumed equal-weight reads, and a weighted observable can concentrate on the shared edge, sliding toward the critical single-edge case while nominally keeping m>=4. The chirality lane verified it and was sharper than ChatGPT — the loose worst case stays ~1.7 even at participation 4-5, so a bare m_eff>=4 floor does not on its own restore rho<1. Granted, all of it. The equal-weight assumption in 0826 was doing silent load-bearing work; this is the η-identity in its sharpest form (which WEIGHTING is dynamical), the fourth surfacing.

The chirality lane's own diagnosis pointed at the resolution: the loose adversary is unphysical because it needs every neighbour of v to concentrate onto v, i.e. in-degree 12 at one vertex, which a single homogeneous rule cannot deliver. I made that precise. Two facts. (1) arcsin is convex on [0,1] so (2/π)arcsin(z) <= z (chord bound); summing the row sums and pairing each edge from both ends, sum_v RowSum_v <= sum_edges(cv^2+cw^2) = N, so the AVERAGE row sum <= 1 for ANY normalized weighting, with equality only at full single-edge concentration. (2) a single translation-invariant rule on the edge-transitive 600-cell gives every vertex the same row sum, so average = max = R* <= 1, and rho(M) <= rho(|M|) = R* (Perron, vertex-transitive). So rho <= R* <= 1, strict for any participation p>1. The loose adversary's 1.7 requires a high-in-degree special vertex, which the average bound forbids at every vertex — it is genuinely non-homogeneous, hence not an admissible order parameter. Verified numerically with the worst-case homogeneous rule (reciprocal matching concentration): rho rises from 0.637 (equal weight) toward 1 ONLY as p->1, e.g. 0.652 at p=3.7, 0.685 at p=2.

That is Part (b), and it essentially does the whole job: the verdict holds for every homogeneous weighting except full single-edge concentration. Part (a) is then almost trivial — p=1 is the sign of one edge variable, which is not a handedness (the orientation is the fixed weight, not the configuration), and a 4-D enantiomorph is the sign of a 4-direction determinant so its floor is p>=4, giving margin ~35%. So orientation => p>1 => rho<1, with a definite margin at the 4-D floor.

I want to be careful about what this does and does not establish, because the temptation here is to over-declare. It does NOT require pinning the exact η-weighting: the verdict is robust over the whole homogeneous non-degenerate class, so the η-identity is dissolved for the verdict and the PCD layer is not called — this is outcome 1 of the gate, not outcome 2. What it RESTS on is homogeneity (vertex-transitivity) of the dynamical η. That is the load-bearing input. It is well-grounded — the order parameter on a homogeneous substrate has a translation-invariant coupling, and a non-homogeneous η would be spontaneous translation-symmetry breaking, a different instability — but it is an input, a property of the Mechanism-A measure, and I stated it as such rather than burying it. If someone insisted the homogeneity itself be derived from the PCD dynamics, that would be the residual; but it is a homogeneity statement about the measure, not the open-ended weighting question, so I do not think it bottoms out at the PCD layer.

This addresses exactly the "two parts, a real computation not yet done" that 0921 named: (a) the orientation floor and (b) the homogeneous worst-case row-sum bound — the latter being the piece the two CONFIRMs asserted but did not test. The honest status is that ChatGPT's falsifier was right, the two CONFIRMs were premature, and the conclusion survives via the vertex-transitivity bound the dissent's worst case did not account for.

Anti-priorities held: granted the falsifier fully rather than defending 0826's equal-weight bound; did not substitute assertion for the worst-case computation (the AM-GM bound is a proof, numerics verify); located the load-bearing input (homogeneity) and stated it; did not declare CAPACITY-1 or move the verdict; no THEO/ID/CHIR.md/package edits; PCD insight held, not spent; conditional on Mechanism A. Verify 0827.

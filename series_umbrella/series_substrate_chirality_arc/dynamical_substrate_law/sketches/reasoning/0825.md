# Reasoning — Patch 0825 (threshold reconciliation)

STATUS: verbatim, at-patch (Opus, Session 156, 8 June 2026). Reconciliation; verdict NOT moved.

The chirality lane fired the DG-3 package (0912) on the 0823 framing, before seeing my 0824. So there are now two inconsistent stories on origin -- 0912 says staggered is orthogonal and quotes 42-47% against the uniform K_c; my 0824 says staggered is the relevant channel and quotes 80% against the AFM K_c. Two different margins and contradictory framings cannot go to the swarm. This patch settles it, and settling it honestly means correcting BOTH of us, including walking back how I framed my own 0824.

The physics, stated cleanly: |K_lift|~0.053 is a robust magnitude; the sign is convention-dependent (0820). eta=0 is stable iff |K_lift| is below the binding threshold, and which mode binds depends on the sign -- FM binds the uniform mode (K_c=1/lambda_max~0.083-0.095), AFM binds the staggered mode (K_c=1/|lambda_min|~0.27). |K_lift| is below BOTH, so eta is disordered regardless of sign -> primitive, robustly. The CONSERVATIVE margin is the uniform one (~44%), because the uniform threshold is the smallest (lambda_max is the largest eigenvalue) and therefore binds for the worst-case sign. The AFM 80% is the favorable case.

So the chirality lane's number (42-47%) is actually right -- it is the conservative margin -- but their justification ("staggered orthogonal") is not, because a staggered order does break the eta->-eta symmetry (the two domains swap under the global flip). And my 0824's 80% is right as a number but I mislabeled it: it is the favorable, sign-dependent case, not THE correct threshold, and I over-framed it as a correction of 0823. The honest synthesis is "both channels cleared": |K_lift| below both thresholds, conservative 44% headline, staggered also cleared at 80%.

I want to be plain that this includes walking back my own prior patch. In 0824 I wrote that I was "correcting" 0823 and that the AFM threshold was "the correct" one. That was an over-claim -- I traded one partial view for another. The complete view needs both channels, and the conservative margin is the uniform one. Better to say that now, before the swarm, than to let two inconsistent margins go out under the programme's name.

The reason this matters operationally: the package invites reviewers to break it (Q3 is exactly the AFM/threshold question). "Staggered orthogonal" is a debatable claim a sharp reviewer would push on; "both channels cleared, |K_lift| below both thresholds" is stronger and not debatable. So reconciling first protects the review.

The verdict is unchanged throughout -- primitive, robustly, eta below all thresholds. Only the framing of the margin moved. I recommend the chirality lane fold this into 0912 before firing; I did not touch their package or the verdict registry.

Anti-priorities held: no verdict moved; no THEO/ID; no CHIR.md/package edits (flagged for the chirality lane); my own 0824 over-framing owned and corrected; conditional on Mechanism A. Verify 0825.

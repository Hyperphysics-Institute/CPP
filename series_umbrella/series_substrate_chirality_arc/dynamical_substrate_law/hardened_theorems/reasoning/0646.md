# Reasoning trail — Patch 0646 (OPEN-FP-F1-2 scope sketch; the Layer-4 Mechanism-A derivation)

**Patch:** 0646 (Session 149, 29 May 2026)
**Artifact:** scope sketch — `sketches/F1_open_fp_f1_2_mechanism_a_layer4_scoping.md` (no theorem, no .tex, no computation)
**Sub-question:** OPEN-FP-F1-2 (Layer-4 axiomatic derivation of Mechanism A from CPP primitives A1–A11)
**Layer:** scoping only (no Layer assignment; opens a multi-session trajectory)
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this Patch, why now

Session 148 closed the CHIR audit-downstream arc (Patches 0632–0644). THEO-CHIR-MERGE-1 (0644)
established that the chirality programme carries **at most two sign primitives** — `sign(n̂)` = FI-C-9
(spatial) and `σ_cycle` (temporal) — and that the question "one sign or two" (MERGE-β) is the **same**
question as the PCD time-reversal asymmetry (OPEN-CHIR-2a), with both gated on **OPEN-FP-F1-2**: does
`sign(δ)` (the Mechanism-A asymmetry parameter) tie to the enantiomorph `sign(n̂)`? The Session-148
handover recommended opening OPEN-FP-F1-2 with a scope decision, preceded by a read of the THEO-DSL-3
arrow construction + the Mechanism-A `sign(δ)` status in `dynamical_substrate_law.tex`. Thomas confirmed
recommendation (1)+(2): scope-sketch the whole thing, carve the `sign(δ)` gate as the explicit first
substantive sub-target, defer the F.2-paper decision.

## §2 The reads (what I read before scoping)

- F.1 §4 (Mechanism A as framework axiom): MA.1 `r(ê;v) = r₀(1 + δ ê·n̂)` + vertex-uniformity;
  MA.2 antisymmetric first-shell current `j_net(v) = Σ[r(ê)−r(−ê)]ê`; §4.3 the three-Layer status
  (Layer-3 framework input / publication-grade Layer-3 hypothesis / Layer-4 deferral = OPEN-FP-F1-2).
- F.1 §2.3 (Scenario-A motivation): the PCD-coupling narrative (`+n̂`-side DI-bits arrive faster →
  energy-minimization picks the forward cycle direction → `ω_PCD` aligns with `n̂`); the sign-convention
  paragraph (σ a Layer-3 convention, physical only via F.2 Capotauro v2.0 §20); the δ–ε open-question
  paragraph.
- F.1 §3.1 (A1–A11 recap): the load-bearing primitives — A1 (CP + DI-bit carrying ê + r(ê)),
  A6′ (PCD cycle / ω_PCD pseudovector), A11 (600-cell). δ–ε flagged independent at Layer 3.
- F.1 §7.2 (Theorem 7.1 proof): the assembly `j_net(v_host) = (6δ/φ²)n̂` — MA.2 enters at Step 2; arrow
  direction = `sign(δ)·n̂`. This is the exact object whose sign L4-D interrogates.
- F.1 §8 (op:layer4-mechanism-a): the in-body OPEN-FP-F1-2 statement — derive the rate asymmetry from
  A1+A6′, derive vertex-uniformity from A11; long-term, multi-Patch, substantial.
- FP.md OPEN-FP-F1-2 entry (HIGH priority; 10–20 sessions for full Layer-4 per item (C)); CHIR.md
  OPEN-CHIR-MERGE / 2a / 4; theorem-registry 0644 changelog; future_projects §300 (F.2 Route-β reserve).

## §3 The decomposition logic

OPEN-FP-F1-2 = discharge MA.1 + MA.2 to A1–A11. I split it along the **four faith-points** of F.1 §4.3:
(faith-1) the rate-law FORM, (faith-2) vertex-uniformity, (faith-3) the current construction, (faith-4)
the sign + magnitude of δ. faith-1→L4-A, faith-2→L4-B, faith-3→L4-C. faith-4 splits cleanly into a
**sign** half (the enantiomorph tie — the MERGE-β / 2a gate) and a **magnitude** half (the δ–ε relation):
L4-D and L4-E respectively. The split is load-bearing: `sign(δ)` and `|δ|` are separable, and only the
sign half gates the two upstream chirality arcs, so L4-D is the carve-out, L4-E a low-priority refinement.

## §4 Why L4-D is the gate, and the honest cap on it

The arrow direction is `sign(δ)·sign(n̂)`. MERGE-β was M3 for two reasons (THEO-CHIR-MERGE-1):
(i) σ is a Layer-3 convention until the F.2 coupling; (ii) δ's tie to the enantiomorph is unpinned.
L4-D attacks (ii). The §2.3 PCD-coupling narrative gives the structural seed: the energy-minimization
criterion that picks the forward cycle direction is what fixes `sign(δ)` — the question is whether that
criterion is tied to the FI-C-9 enantiomorph (→ M1) or satisfiable for either sign independently (→ M2).

The critical honesty point I built into the sketch (§3.4): **a clean structural tie does NOT by itself
give M1.** Reason (i) still stands — σ physicalizes only via the F.2 Wigner-Eckart coupling, which is
unbuilt. So the realistic near-term L4-D output is:
- M2 is decidable now (independence breaks the tie regardless of F.2);
- M1 is NOT decidable now (needs F.2 to physicalize σ); a structural tie gives "M3-structural-resolved:
  one sign IF σ physicalizes as the tie predicts."
This is the same staged-closure honesty as E19/E21/MERGE: report the reachable half, gate the rest, no
merge-for-elegance. And FI-C-9 is not eliminated by any L4-D outcome (M1 consumes it, M2 adds a second
consumed sign; only 1d-β touches FI-C-9's input status).

## §5 Routes and the recommendation

Three routes: α (standalone FP multi-Patch, mirrors the OPEN-FP-F1-1/1-5 13-artifact sequence),
β (dedicated F.2 paper, future_projects §300, 5–15 sessions, heaviest), γ (narrow-first on L4-D alone).
Recommendation per the instruction: **γ-leaning-α with β deferred** — carve L4-D as first substantive
sub-target, open it next, drop in a thin L4-A/L4-C scaffold only if the energy-minimization argument
demands it, keep L4-B/L4-E as later refinements, and do NOT commit the F.2 paper (DG-2 deferred). This
front-loads the one sub-target two upstream arcs wait on while staying reversible about the 10–20-session
full-closure reality.

## §6 Registry actions taken

- FP.md OPEN-FP-F1-2 entry: enriched with the L4-A..E decomposition + the L4-D carve-out + the
  Route α/β/γ note + the F.2 / δ–ε dependencies + a pointer to the scope sketch. Status stays OPEN
  (scope only). **No header count change** (no new programme-level Open Problem; sub-targets are
  scoping-internal per the anti-priority discipline).
- theorem-registry.md: Patch 0646 changelog line (scope only, NO proved-row); 0644 demoted to Earlier.
- No theorem registered, no .tex, no computation → no verify script (scope sketch, governance-adjacent).

## §7 Honest caps carried forward (inherited from THEO-CHIR-MERGE-1)

1. No derivation performed — this opens a trajectory, closes nothing.
2. No merge-for-elegance — L4-D will report M2/M3-structural honestly; M1 only with F.2.
3. FI-C-9 not eliminated by any outcome.
4. The F.2 substrate-Wigner-Eckart coupling is unbuilt and gates the physicalization half (DG-1).
5. Route β (F.2 paper) is DEFERRED, not taken (DG-2).

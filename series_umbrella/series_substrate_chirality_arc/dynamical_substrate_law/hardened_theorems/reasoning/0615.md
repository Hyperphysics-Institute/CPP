# Reasoning fragment — Patch 0615 (Session 147)
# Face-aligned O(delta^2) coefficient closure (THEO-DSL-9 candidate) + V_4 correction to THEO-DSL-8
# STATUS: verbatim (captured at-patch)

## Goal
Close OPEN-FP-F1-5 Sequence-2B: compute the O(delta^2) face-aligned substrate-current
coefficients, the third Reading-C variant after vertex (THEO-DSL-5) and edge (THEO-DSL-7).
Reserved candidate ID: THEO-DSL-9.

## Assembly (identical to vertex/edge, only n changes)
j_2(v_host) = sum over the 144 directed 2-edge paths v_host -> u -> v' of
              (e1 . n)(e2 . n) * e1,   e1 = unit(u - v_host), e2 = unit(v' - u),
with path-class weight W(P)=1 (sketch Layer 3, same ansatz as THEO-DSL-5/-7).
For face-aligned, n = n_Fperp = unit(v_host + u_i + u_j) (centroid direction of a
triangular face F = {v_host, u_i, u_j}; 30 such faces per vertex).

## The discovery (how the V_4 correction surfaced)
1. Grounded in THEO-DSL-8 (Patch 0597), which claimed: residual symmetry C_s order 2,
   invariant subspace 3D (u_i-u_j)^perp = span{n_rho, n_ax, n_Fperp}, growth 1->2->3.
2. First attempt: decompose j_2^face in the THEO-DSL-8 basis {n_rho, n_ax, n_Fperp}
   via a 3x3 Gram solve. The Gram matrix came out SINGULAR.
3. Reason: n_Fperp propto v_host + u_i + u_j. Decompose u_i+u_j = phi*v_host +
   P_perp_vh(u_i+u_j) (since (u_i+u_j).v_host = phi). So
   n_Fperp propto (1+phi) n_rho + |P| n_ax  IN span{n_rho, n_ax}.
   => {n_rho, n_ax, n_Fperp} are linearly DEPENDENT; they span only 2D. (Error #2 in
   THEO-DSL-8: its "three mutually orthogonal directions" claim, line 154, is false.)
4. The genuine 3D (u_i-u_j)^perp needs a third independent direction: n_perp3 = the
   unit normal to the 3-flat span{v_host, u_i, u_j} (computed via SVD null vector).
   Frame {n_rho, n_ax, n_perp3} is genuinely orthonormal, all perp to n_diff=(u_i-u_j).
5. Decomposed j_2^face in {n_rho, n_ax, n_perp3}:
     alpha_rho   = -14 + 7 phi = -7/phi^2 ~ -2.674
     alpha_ax    =  -6 + 2 phi           ~ -2.764
     alpha_perp3 =  0   EXACTLY  (robust 1e-16 over all 30 faces)
   So the current lies in 2D span{n_rho, n_ax}, NOT the full 3D (u_i-u_j)^perp.
6. Why does alpha_perp3 vanish? C_s={e,sigma_E} does NOT force it (sigma_E fixes
   n_perp3 since n_perp3 in (u_i-u_j)^perp). So there must be a LARGER symmetry.
   The vanishing direction n_perp3 is flipped by sigma_F = reflection through the
   3-flat span{v_host,u_i,u_j}. Checked: sigma_F = I - 2 n_perp3 n_perp3^T is a
   genuine 600-cell automorphism (maps the 120-vertex set to itself), fixes
   v_host,u_i,u_j individually, and FIXES n_Fperp (centroid in the 3-flat).
   => sigma_F preserves the rate function => j_k is sigma_F-invariant =>
   j_k . n_perp3 = -(j_k . n_perp3) = 0 at ALL orders.
7. Therefore the true stabiliser of (v_host, n_Fperp) is V_4 = {e, sigma_E, sigma_F,
   C_2=sigma_E sigma_F}, order 4 (NOT C_s order 2). Verified directly: all three
   non-trivial elements are 600-cell automorphisms fixing v_host AND n_Fperp; the
   group <sigma_E,sigma_F> has order 4 and its R^4 invariant subspace has dim 2.
   (Error #1 in THEO-DSL-8: line 132 claims two V_4 elements flip n_Fperp; in fact
   all four fix it because n_Fperp is the centroid, symmetric under the whole face
   stabiliser.)

## Why THEO-DSL-8 went wrong (root cause)
n_Fperp was defined as the centroid direction (v_host+u_i+u_j)/|.|. The centroid is
fixed by EVERY automorphism that fixes v_host and permutes {u_i,u_j} -- i.e. by the
entire face stabiliser V_4, not just the edge-swap sigma_E. THEO-DSL-8 implicitly
assumed the face-plane reflection flips n_Fperp; geometrically that I_h element fixes
the whole 3-flat span{v_host,u_i,u_j} (which contains n_Fperp) and flips only the
out-of-3-flat normal n_perp3. So it fixes n_Fperp. Hence V_4, hence 2D, hence 1->2->2.

## Closed forms (exact in Q[phi]) and per-class
alpha_2^(rho) = -14+7phi = -7/phi^2 ;  alpha_2^(ax) = -6+2phi = 2phi-6.
alpha_2^(perp3)=0 (sigma_F) ; alpha_2^(diff)=0 (sigma_E; the surviving true part of
the THEO-DSL-8 conclusion).
Per shell-class (basis {n_rho,n_ax}):
  B1(12): (-7/6+phi, -1/3+2phi/3)
  B2(60): (-5+10phi/3, -5/3+5phi/3)
  B3(60): (-20/3+5phi/2, -10/3)
  B4(12): (-7/6+phi/6, -2/3-phi/3)
  TOTAL : (-14+7phi, -6+2phi).  All B-classes have zero n_perp3 AND zero n_diff.
Vertex cross-check (n=v_host, same code path): -9/phi^2. PASS.

## Cross-variant picture (corrected)
vertex: -9/phi^2 (1D, I_h)
edge  : (9/(2phi), 9phi-12) ~ (+2.781,+2.562) (2D, D_5)
face  : (-7/phi^2, 2phi-6) ~ (-2.674,-2.764)  (2D, V_4)
Dimensional growth: 1 -> 2 -> 2 (NOT 1 -> 2 -> 3). Radial face coeff -7/phi^2 is
sign/scale-cousin of vertex -9/phi^2; edge is the sign-outlier (its non-vanishing
B.2 first-shell-transverse class).

## Dead-ends / discarded
- The 3x3 Gram solve in {n_rho,n_ax,n_Fperp}: discarded (singular; basis degenerate).
- An alternative delta-parity argument for alpha_perp3=0 (sigma_F equiv delta->-delta):
  initially mis-stated because I conflated "perp to face plane" (2D) with "flips both
  n_Fperp and n_perp3". The correct statement: sigma_F (the I_h element) fixes the
  whole 3-flat including n_Fperp and flips ONLY n_perp3. The clean all-orders argument
  is direct V_4-equivariance (Lemma 3), no delta-parity needed.

## Anti-erasure decision
Register THEO-DSL-9 with the correct 2D result + a prominent anti-erasure remark
documenting BOTH THEO-DSL-8 errors (C_s->V_4; degenerate 3-vector basis). Do NOT
silently rewrite the THEO-DSL-8 entry; flag the discrepancy in registry + FP.md and
recommend a dedicated THEO-DSL-8 structural-revision Patch under multi-AI review,
plus propagation of 1->2->2 to all records currently carrying 1->2->3.

## Verification
code/verify_face_alpha2_closure.py: build 600-cell; vertex cross-check; V_4 auto +
2D invariant-dim; THEO-DSL-8 basis-degeneracy; face closure; per-class; 30-face
robustness. RESULT: ALL CHECKS PASS.

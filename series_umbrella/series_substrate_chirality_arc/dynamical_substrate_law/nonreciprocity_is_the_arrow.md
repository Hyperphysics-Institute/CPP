# 0973 — The non-reciprocity of 0972 is not a new field. It is the T-arrow.

**Patch:** 0973, 13 Sep 2026. **Verify:** `code/0973_nonreciprocity_is_the_arrow.py` (6/6). **No verdict moved, no claim registered.** Answers the founder's question of 13 Sep on the 0972 escalation.

**The question.** *"It sounds like a field is present between the two, costing more to go one direction than the other, but I don't know what that field would be. We are just looking at a lattice… I think we are just talking about the lattice's geometry. Am I correct? If it is just geometry, then I don't know what could be causing the non-transitive behavior."*

**He is correct, and that is the key to the resolution.**

---

## 1. Pure geometry cannot do it — which is the point

A length comes from a metric, and a metric is a **symmetric** bilinear form: `g(−v, −v) = g(v, v)` identically. So **every length built from an inner product is reversal-even, on any lattice whatever.** Checked over 200 arbitrary positive-definite metrics on the 600-cell: forward and backward edge lengths differ by exactly 0.0 (T1).

**So if Reading C's `ℓ` were geometric, it would be reversal-even — and `δ` would vanish** (0972 T4). The founder's instinct that "it is just geometry" is right, and it is precisely why the asymmetry cannot *be* geometric.

## 2. What the "field" is, mathematically

The only way to get a direction-dependent length is a term **linear** in the direction — a 1-form. That is a **Randers/Finsler structure**:

`F(v) = √(g(v,v)) + β(v)`,  with here  `β = ε n̂`  (T2).

**But it is not a new field.** `β` is built from `n̂`, already a substrate primitive (FI-C-RC-1). Nothing is added to the lattice. What changes is whether `n̂` enters as a **direction** — giving even structure only — or as a **1-form**, which makes odd structure available (T3).

## 3. The physical identification

Where does the time-asymmetry actually come from? Tested on all 1200 triangular faces:

| rate-law part | max cycle affinity | violating faces |
|---|---|---|
| reversal-**even** `A(m̂·n̂)`, at A = 0.3, 0.5, **1.0** | **0.0 exactly** | **0 / 1200** |
| reversal-**odd** `B(ê·n̂)` | 5.0×10⁻⁷ | **420 / 1200** |

**Detailed-balance violation comes entirely from the reversal-odd part.** The even term produces none, at any magnitude (T4).

So the chain closes:

> *"costs more one way than the other"* ⟺ *"detailed balance is violated"* ⟺ *"the process is not time-reversible."*

And **THEO-CHIR-MERGE-2** already established that `sign(δ)` is **P-even and T-odd — an arrow, not a chirality.**

**Therefore the non-reciprocity of 0972 IS the T-arrow, which the arc already carries as the named primitive W3** (T5). It is not a mysterious extra field; it is the thing the temporal half of this arc has been about all along.

## 4. The answer to the founder's question

**The substrate is not non-reciprocal in space. Its geometry is perfectly reciprocal.** What is non-reciprocal is its **dynamics, in time** — which is what an arrow of time *means* (T6).

He was looking for a spatial cause because the formula is written as a spatial quantity. There is no spatial cause, because the asymmetry was never spatial.

**Closest physical analogy:** light in a flowing medium (Fizeau). The *distance* upstream and downstream is identical; the *traversal time* is not, because the medium moves. The substrate's "flow" is its advance through Absolute Moments — and that advance is exactly what the T-arrow names.

## 5. Recommendation — the prose changes, and the ruling is the founder's

- **Reading C's `ℓ` should be relabelled** from "effective length" to **directed traversal cost** (or *hop time*), with a note that its reversal-odd part is the T-arrow.
- **The equation stands.** It has been right since it was written; only its description was wrong.
- **A gain, not a loss:** this removes an apparent commitment to exotic spatial geometry and replaces it with a commitment the programme already makes and has already registered. `MA.1`'s form requiring non-reciprocity (0972) becomes `MA.1`'s form requiring a T-arrow — which is not an extra assumption at all.

**PD-006(a) ruling owed:** accept the relabelling, or defend `ℓ` as a genuine length and accept that `δ = 0` follows. **These are the only two options** — 0972 T4 showed a reversal-even `ℓ` generates the A-term and no `δ`.

## 6. Disposition

- **No verdict moved, no claim registered, no count change.**
- **Owed:** the relabelling in Reading C / Capotauro §2.3 and the F.1 §"Structural connection" paragraph, bundled with the corrigenda queue — **contingent on the founder's ruling.**
- **Recorded:** the identification `reversal-odd ⟺ T-odd ⟺ detailed-balance violation` is verified here and was not previously stated in one place, though MERGE-2 and TARROW-2 each hold half of it.

# Reasoning fragment — Patch 0632 — THEO-CHIR-AUDIT-1 v1.0

**Bound to:** `chirality_audit/theo_chir_audit_1.tex` (v1.0)
**Session:** 148
**Protocol:** `templates/reasoning_capture_protocol.md` — verbatim reasoning fragment bundled in the same `git am` as the artifact it explains.

---

## Context: this is a rebuild, not a fresh start

The Session-147/148 working window overflowed mid-construction of this artifact, with no handover document written. The last successfully pushed commit was `15d392e` (Patch 0631, PCD acronym terminology correction). The audit artifact, its reasoning fragment, the `frontier_sectors/CHIR.md` file, and the registry updates — all the in-flight Patch 0632 work — lived only in the dead session's ephemeral container and were never committed. **There was nothing recoverable on disk.**

What survived, and what this rebuild is built from, is the durable input: the **Patch 0630 scope-and-precondition sketch** at `chirality_audit/sketches/theo_chir_audit_1_scope.md`. That sketch is a complete section-by-section blueprint (its §3 specifies the eight-section structure; its §2 the three operational senses; its §4 the registry outputs; its §5 the precondition gaps; its §6 the patch sequence). The rebuild follows it faithfully.

**Patch-number note.** The scope sketch (§6) provisionally slotted the audit artifact at "Patch 0631." In execution, the PCD-terminology cleanup (scope-sketch §5.2) was landed as Patch 0631 (commit `15d392e`, confirmed by `git show`), so the audit artifact is **Patch 0632**. The previous (overflowed) session's transcript confirms it was building "Patch 0632."

## Preconditions cleared before building (clone-first gate)

Per the CLONE-FIRST GATE, I cloned the repo and grepped the registry before placing any file or registering any ID:

1. **PCD terminology (scope-sketch §5.2).** `git show 15d392e` confirms Patch 0631 restored "Perceive-Compute-Displace" across 15 active files; `master_glossary.md` now reads the canonical agentic expansion. The audit's §4 (dynamics pass) therefore uses canonical PCD terminology, with an anti-erasure footnote on the Session-146 drift (`311bc1e`).

2. **Axiom count (scope-sketch §5.1).** `axiom-registry.md` confirms **9 axioms** post-consolidation: A1, A2, A3, A4 (Tier 1 ontological); A5 (Tier 2 metric); A6′ (Tier 3, consolidating former A6+A7+A8+A9); A10 (Tier 4 sign); A8′ (Tier 5 cage scaling); A11 (Tier 6 lattice grounding). The non-consecutive numbering (A7/A9 retired, A8′/A10/A11 later additions) is the canonical "A1–A11" colloquial set reduced to nine. The audit's §2 axiom-level pass walks these nine.

3. **ID collision grep.** `grep -rn "CHIR-AUDIT|OPEN-CHIR-1..4|OPEN-CHIR-F1"` over `theorem-registry.md research_frontier.md frontier_sectors/` returned NONE. Clean to register THEO-CHIR-AUDIT-1 and the OPEN-CHIR-* programme.

## Key classification reasoning

The audit's job (per scope-sketch §1) is a classified inventory, not a derivation. The three-category scheme (primitive / emergent / unregistered) crossed with three operational senses (spatial / temporal / CP-asymmetric) produces a 27-entry table (§6 of the artifact).

The load-bearing structural finding — which the systematic walkthrough produces rather than assumes:

- **Spatial chirality reduces to a single primitive: `n̂` (FI-C-RC-1, entry E16).** The static 600-cell is achiral (H₄ contains reflections). Every other geometric handedness is either gauge-removable (Wythoff orientation E14, φ-coordinate signs E15) or emergent from `n̂` + the polytope (the 2I spinor reps E12, the icosahedral rotation sense A₅ E13). `n̂`'s mere presence breaks H₄ → H₃ = I_h at the host vertex; it is the irreducible spatial source.

- **Temporal chirality reduces to the PCD sequence carried by A1 + A4 (E2/E5/E17).** Perceive → Compute → Displace is not manifestly t → −t symmetric (reversed it would be Displace → Compute → Perceive, different dynamics). This is a primitive — but its time-reversal asymmetry is *presupposed* rather than explicitly asserted in the canonical specification. That gap is registered (the E2-asymmetry unregistered entry; OPEN-CHIR-2a).

- **CP-asymmetric chirality is uniformly emergent or unregistered — never primitive** (E4 φ-winding, E19/E23 capture handedness CP-variant, E26 SM parity link). This is consistent with the programme presumption that observed CP violation should be *derived*, not axiomatized.

- **The deepest unregistered entry is the capture handedness D3 (E19).** Every SD-CHIR closure (SD-CHIR-1's ζ^W, SD-CHIR-2's combined-CP ζ^qDP) *consumes* a definite partnering handedness, but none derives it from a registered axiom. This is the single most load-bearing unregistered assumption surfaced by the audit.

The existing-derivation pass (artifact §5) confirms the convergence: every registered CHIR theorem (CONT arc via the χ handle; SD-CHIR arc directly; DSL arc) consumes `n̂` as its chirality input. χ = φ⁻³ and the cross-sector handle χ/6 ≈ 0.0394 trace to `n̂` + 600-cell ratios (topological, per CONT-1.3).

## n̂ ↦ ω_PCD (D4, E20) left conditional on purpose

The F.1 sub-question (`F1_subquestion_pcd_orientation_link.md`) asks whether ω_PCD(v_host) = σ·n̂ via a substrate mechanism (Scenario A → ω_PCD emergent from n̂) or whether ω_PCD is an independent primitive (Scenario B → both primitive). The audit does NOT pre-commit (scope-sketch §5.5). E20 is classified emergent under the working presumption (Scenario A) with an explicit conditional flag, and the resolution is registered as OPEN-CHIR-F1-LINK. The likely first downstream theorem is THEO-CHIR-PCD-ORIENTATION-1 (Scenario-A derivation of ω_PCD = σn̂).

## Layer / scope

Publication-grade Layer 3 unconditional structural-cataloging theorem (pending multi-AI review per scope-sketch §6 Patches 0633+). It consumes the current axiom set + geometric primitives as given; introduces NO new framework axiom (X3). Falsifiers are explicit (artifact §7): exhibit a chirality-touching element absent from the 27 entries, show an entry misclassified, or exhibit a fourth operational sense.

## Verification

`pdflatex` three-pass clean compile: 12-page PDF, zero errors, zero undefined references/citations. No new framework axiom; no modification of existing THEO entries (anti-priorities sustained). Registry/dashboard/FP propagation bundled in this same patch per the patch-delivery contract.

# Reasoning trail — Patch 0663 (THEO-CHIR-BRIDGE-1; B-i of the CHIR↔EW bridge)

**Patch:** 0663 (Session 150, 30 May 2026)
**Theorem:** THEO-CHIR-BRIDGE-1 — the substrate↔electroweak chirality correspondence (the ℤ₂-match + the P/T-face dictionary)
**Layer:** 2.5 (provisional) — a structural CORRESPONDENCE theorem, NOT a derivation
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 What this is

Crystallizes B-i of the bridge scope sketch (0662): the first reachable step toward deciding if
chirality is emergent or primitive, now that everything inside CHIR is done. Built entirely on
review-hardened inputs F1–F5 (MERGE-2, STATUS-2, TARROW-1, CHI-1, CAP-1) — no new dynamics.

## §2 The three results

- **Thm 1 — the ℤ₂-match (centerpiece).** The OPEN-SM-4 activation ℤ₂ (in [600-cell]×ℤ₂→[600-cell],
  breaking establishes sign(χ)) and the STATUS-2 quotient ℤ₂ = H₄/H₄⁺ are the SAME ℤ₂ object — both
  the 600-cell det-coset (orientation/enantiomorph) ℤ₂, generator an orientation-reversing reflection
  (det=−1), order parameter sign(n̂)=sign(χ)=FI-C-9, action = enantiomorph exchange. They coincide as
  the tuple (group, generator-class, order-parameter, action).
- **Thm 2 — the P/T-face dictionary.** sign(n̂)=FI-C-9 (P-odd) ↔ EW parity violation (V−A; E26);
  sign(δ) (T-odd) ↔ SM CP-violation (δ_CP); CPT-unified (F3) into one EW chiral structure. Magnitude
  thread (Prop): |FI-C-9|=φ⁻³ → Δp_LR=φ⁻³/6≈0.0394 (CAP-1, shipped, the one load-bearing rung).
- **Cor — effect on CONJ-CHIR-1.** Discharges its kinematic half (one ℤ₂ object + CPT-consistent
  dictionary), isolates its dynamical half (does the ℤ₂ break? is the break EWSB? = B-iii/1d-β-ii).

## §3 The honest line I held (why this is real but doesn't overclaim)

The temptation here is to declare the bridge built. I did not. The load-bearing distinction:
- **Kinematic (what BRIDGE-1 establishes):** the ℤ₂ is the *same group object* with the *same order
  parameter*; the dictionary is a correspondence of *character*, CPT-unified. This is genuinely
  provable group theory + the registered identifications.
- **Dynamical (what BRIDGE-1 does NOT establish):** that the ℤ₂ *breaks* (capacity), that the break
  *is* EWSB (CONJ-CHIR-1), that FI-C-9 *produces* (vs corresponds to) EW parity violation. Untouched.
So **no verdict move** — chirality stays V3/W3. BRIDGE-1's value is exactly this: it makes the bridge's
skeleton rigorous and isolates the one deep question (B-iii) whose resolution *would* move the verdict.

Two premises carried honestly:
- **P1** STATUS-2's det-coset ℤ₂ — rigorous.
- **P2** reading OPEN-SM-4's ℤ₂ as the enantiomorph ℤ₂ — a well-supported *interpretation* of the
  OPEN-SM-4 registration ("[600-cell]×ℤ₂→[600-cell]" + "establishes χ"), NOT a re-derivation of
  Capotauro. Registered as falsifier B1 (= CONJ-CHIR-1 falsifier (a)). STATUS-2 itself flagged even
  "ℤ₂ = FI-C-9" as a framework step, so this honesty has precedent.

And a flagged loose end I did not paper over: Capotauro's χ≈φ⁻¹ vs CHI-1's |χ|=φ⁻³ normalization needs
reconciling — that's a B-ii task, falsifier B4. I did not silently pick one.

## §4 Anti-priorities held

No new dynamics; no verdict change; no modification of F1–F5 (consumed, not edited). The ℤ₂-match is a
kinematic identification, not a proof that the bridge dynamically holds — no merge-for-elegance. B6 is
an explicit anti-overclaim falsifier (guards against anyone reading a verdict move into BRIDGE-1).

## §5 Verification

`code/verify_bridge_1_z2_match.py` — CHECK 1 (det-coset ℤ₂: 14400→7200, index 2, quotient ℤ₂,
generator det=−1), CHECK 2 (the ℤ₂-match as tuple equality + the kinematic-only flags), CHECK 3
(dictionary + CPT consistency with TARROW-1 + magnitude numerics φ⁻³, φ⁻³/6 + honest caps: V3/W3
preserved, CONJ-CHIR-1 kinematic half discharged / dynamical half isolated). All pass. Theorem compiles
clean (three-pass pdflatex, 5 pp, zero undefined refs).

## §6 Status / next

Single-pass; multi-AI review is the natural next step (BRIDGE-1 advances a registered bridge and is the
load-bearing structural step of B-i). Triage for that cycle should foreground: the kinematic/dynamical
boundary (is the honest cap airtight, or does any step smuggle a dynamical claim?), premise P2 (is the
OPEN-SM-4 ℤ₂-reading sound?), and whether the dictionary's "correspondence not causation" framing
holds. After review-close: B-ii (the magnitude anchors, incl. the χ-normalization reconciliation), then
the deep B-iii (1d-β-ii / OPEN-SM-4 (a)/(b)) — the sole verdict-moving engine — per Thomas's choice.

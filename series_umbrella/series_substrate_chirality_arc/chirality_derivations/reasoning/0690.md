# Reasoning — Patch 0690 (Session 152)

**Artifact:** NEW `theo_chir_tarrow_2.tex` (v1.0, 4-page PDF, clean 3-pass, zero undefined refs) — registers **THEO-CHIR-TARROW-2**, the derived-mechanism theorem for the substrate time-asymmetry. Reuses the existing verify script `code/verify_odelta3_kolmogorov_curl.py` (from 0689; CHECK 1/2 PASS, CHECK 3 reports curl content). NO new verify script. THEO-CHIR-CAPACITY-1 stays reserved (this is the T-side, not the P-side/μ²). **NO closed verdict move — V3 (spatial) untouched; W3 STANDS until review (DG-3).**

## Decision (the fork from 0689)
Thomas's choice between (a) the μ²-sign computation and (b) banking the arrow theorem. Recommended + took (b): the two are NOT independent — the μ² computation builds on the non-equilibrium stationary measure, whose existence/character IS the O(δ³) non-reversibility result. Reviewing that foundation first de-risks the harder (a). And (b) is itself potentially the first verdict move in the arc (W3→W1 on the T-axis), which needs review anyway (DG-3). So harden the foundation, then build the verdict-mover on reviewed ground.

## What the theorem claims (THEO-CHIR-TARROW-2, Layer 2.5, provisional)
Supplies exactly what TARROW-1 said it did NOT (𝒞_T=No, "F.2 physicalization unbuilt"): a derived substrate T-asymmetry mechanism. Content:
- **Order-counting (Lemma, corrects 0688):** the Kolmogorov log-ratio L=log[(1+δc)/(1−δc)] is ODD in δ ⇒ no O(δ²); first violation O(δ³).
- **Per-face formula (Lemma):** a+b+c=0 (closed loop + uniform edge length) ⇒ O(δ¹) vanishes for every face; O(δ³) = 2δ³abc (via a³+b³+c³=3abc).
- **Computation (Theorem core):** 420/1200 faces have abc≠0 (values 1/8,1/4; all touch 2nd shell) ⇒ NON-reversible at O(δ³) ⇒ steady-state probability current. Verify script (machine precision).
- **Arrow + upgrade (Prop):** a non-reversible process carries a current = a time-asymmetry ⇒ 𝒞_T=Yes at the Mechanism-A level ⇒ CANDIDATE W3→W1 (mechanism derivable; direction free, per TARROW-1's W2-exclusion). NOT W2.
- **Non-conflict with TARROW-1:** TARROW-1's "no T-odd object" was about static GEOMETRIC invariants; the current is a DYNAMICAL object (from the rates) = the F.2 physicalization TARROW-1 flagged unbuilt. Builds it; no contradiction.

## What it does NOT claim
- NOT a spatial V-move (FI-C-9 stays V3; this is the T-axis).
- NOT the arrow direction (W1, not W0).
- NOT μ²<0 / RP-failure (P-side capacity is separate; DB sufficient-not-necessary for RP; RP=OS reflection≠T-symmetry).
- NOT a closed W-move: v1.0 is PROVISIONAL; the W3→W1 claim is made only after multi-AI review (DG-3). Registration ≠ verdict move.

## Conditionality + falsifiers
Conditional on Mechanism A (axiom, OPEN-FP-F1-2); computed at O(δ³) (one nonzero face breaks reversibility ⇒ order-stable). Falsifiers T2-1 (faces generate cycle space — they do, simply-connected 3-sphere), T2-2 (per-cycle, no global cancellation — 420 independent violations), T2-3 (current⇒asymmetry is the defining property), T2-4 (Layer-4 Mechanism-A derivation could alter the rate-law form).

## Why TARROW-2 (naming)
TARROW-1 = the STATUS theorem (W3 placement, explicitly NOT deriving the mechanism). TARROW-2 = the mechanism it lacked. Natural sequel; TARROW-2 free in the registry. THEO-CHIR-CAPACITY-1 (P-side/μ²) untouched.

## Next
Multi-AI review cycle for TARROW-2 (the W3→W1 move is claimed at review-close, like VW-1/VW-2/TARROW-1). Then the μ²-sign computation on the reviewed foundation. Per Thomas. Within 0688–0699 band, below DM 0700 reserve.

## Anti-priorities held
No closed verdict move (W3 stands, V3 untouched); candidate upgrade is provisional/conditional; no μ²/RP overclaim; THEO-CHIR-CAPACITY-1 reserved; closed theorems (VW-1/2, STATUS, TARROW-1, BRIDGE, CAP) + F.1/DSL/OPEN-SM-4 consumed not edited; reuses 0689 verify script (no duplicate). Changelog-style CHIR.md; NO theorem-registry body-table row (chirality sub-corpus precedent).

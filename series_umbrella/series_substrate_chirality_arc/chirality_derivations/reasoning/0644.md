# Reasoning fragment — Patch 0644 (THEO-CHIR-MERGE-1; OPEN-CHIR-MERGE partially resolved)

**Session 148, 29 May 2026. Per `templates/reasoning_capture_protocol.md`.**

## What the DSL reads settled (and what I expected vs found)

Going in (from the 0643 scope), I expected MERGE-α (cycle = arrow) to be reachable and MERGE-β
(the sign) to be M3-gated. The DSL paper confirmed both — and, usefully, it is unusually explicit
about exactly the two sign questions, so the verdict rests on the DSL paper's own statements, not
on my interpolation:

1. **MERGE-α — the mechanism narrative IS an identification.** DSL-3's narrative is verbatim:
   "asymmetric DI-bit arrival → capture-phase bias in the PCD cycle → preferred PCD orientation →
   macroscopic arrow asymmetry." Read as a claim about identity (not just correlation): the PCD
   cycle orientation ω_PCD and the thermodynamic arrow are the SAME temporal orientation — the
   arrow is the coarse-grained face of the cycle bias. So the temporal chirality is ONE
   orientation, not two (a microscopic cycle sense + a separate macroscopic arrow). I capped this
   at Layer 2.5 / the DSL viability ceiling, because DSL-3 itself only SUPPORTS (does not derive)
   the full arrow emergence.

2. **MERGE-β reason (i) — the sign is a Layer-3 convention.** DSL-3 says σ in ω_PCD = σ·n̂ is "a
   convention fixed at the framework level," the two choices "time-reversal-symmetric framings of
   the same physics … not distinguishable at Layer 3 rigor," physical only when coupled to the
   F.2 Wigner-Eckart datum. So at this level σ_cycle is not even a physical sign that COULD equal
   sign(n̂) — it's a convention. That alone blocks asserting M1.

3. **MERGE-β reason (ii) — the arrow sign carries sign(δ).** The current is j = (6δ/φ²)n̂
   (VERIFIED: uniform û·n̂ = −1/(2φ), j direction = sign(δ)·n̂ at δ=±1, ±0.37). So the arrow sign
   is sign(δ)·sign(n̂). And DSL-3 says δ and ε (the spatial chirality parameter) are "independent
   framework inputs," their relation "not pinned" — that's the Layer-4 Mechanism-A derivation
   (OPEN-FP-F1-2). So sign(δ) is free at current rigor; the arrow sign does not reduce to
   sign(n̂). Second block on M1.

Neither reason ESTABLISHES independence (M2) either — the Layer-4 derivation could still tie
sign(δ) to sign(n̂). So the honest verdict is **M3 (undetermined)**, with two named gates: the
F.2 coupling (makes σ physical) + the Layer-4 Mechanism-A derivation (ties sign(δ)). I refused to
round M3 up to M1 for elegance — one sign is prettier, but the DSL paper explicitly says the sign
is a convention at this level, so claiming the merge would contradict the very source that
supplies the route.

## The OPEN-CHIR-2a insight (the one genuinely new structural result)

The nicest thing to fall out: OPEN-CHIR-2a (the PCD T-asymmetry) is NOT an independent owed item.
DSL-3 says the two σ-choices are "time-reversal-symmetric framings" at Layer 3 — i.e. the cycle
has NO T-asymmetry at Layer 3; reversing σ reverses (conventional) time with no physical
consequence. The T-asymmetry appears EXACTLY when σ becomes physical (the F.2 coupling), which is
EXACTLY the condition for MERGE-β's sign to be physical. So **2a ⟺ MERGE-β-physical** — the
T-asymmetry is the physicalization of the merge sign, not a separate question. This subsumes the
audit's "ordered (primitive) vs T-asymmetry (owed)" distinction: the ordering is the
convention-level sense; the T-asymmetry is its physicalization. I registered this as a proposition
(structural advance), explicitly NOT a closure of 2a — both 2a and the merge sign remain gated on
the same F.2 + Layer-4 work.

## Primitive-count bookkeeping (the headline)

MERGE-α does real count work: it collapses the temporal chirality from a potential two data to ONE
orientation. So the programme's chirality sign ambiguity is now exactly two-way — sign(n̂)
(spatial, = FI-C-9, pinned by CAP-1) and σ_cycle (temporal) — with merge-to-one (M1) gated on
Layer-4 Mechanism A, and M2 (two signs) not excluded. I stated "at most two chirality sign
primitives" as the firm result, and the merge-to-one as gated, not done. This is the honest
capstone: the determination of "emergent or primitive" is now as far as it goes WITHOUT the
Layer-4 Mechanism-A derivation and the deeper 1d-β dynamics.

## FI-C-9 (kept straight)

Either merge outcome consumes FI-C-9, neither derives it. M1 routes σ_cycle TO sign(n̂)=FI-C-9;
M2 adds a second consumed sign. The merge changes the COUNT, not FI-C-9's input status — only
1d-β touches that. Said so explicitly so the merge isn't mistaken for a deeper reduction.

## Honesty caps held

- Layer 2/2.5 partial-closure; MERGE-α provisional (DSL ceiling); MERGE-β reported (M3) not closed.
- No merge-for-elegance: M3 stated plainly despite M1 being the prettier answer.
- 2a clarified (⟺ MERGE-β-physical), explicitly not closed.
- FI-C-9 not eliminated either way.
- OPEN-CHIR-MERGE → PARTIALLY RESOLVED (not RESOLVED); the cross-links (PCD-ORIENTATION §5.3,
  CAP-1 §5) move from "hypothesis" to "half-resolved: one orientation, sign gated," not to merged.

## Next

The merge sign (M1 vs M2) and OPEN-CHIR-2a both now route through the SAME two gates: the F.2
substrate-Wigner-Eckart coupling and the Layer-4 Mechanism-A derivation (OPEN-FP-F1-2). So the
natural next chirality-primitive-count step is OPEN-FP-F1-2 (does sign(δ) tie to the enantiomorph?)
— which is an F.1/FP-sector target, deeper than the CHIR-derivations sub-corpus. The CHIR
audit-downstream arc is now at its honest floor: everything tractable from {n̂, FI-C-9, σ_cycle}
+ the DSL route has been extracted; the remaining reductions (the merge sign, 2a, 1d-β) are all
Layer-4/dynamics work in the FP/SM sectors.

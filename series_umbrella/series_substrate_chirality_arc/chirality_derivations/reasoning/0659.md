# Reasoning trail — Patch 0659 (THEO-CHIR-TARROW-1 multi-AI review cycle OPENED)

**Patch:** 0659 (Session 150, 30 May 2026)
**Scope:** workflow-only — review package issued for THEO-CHIR-TARROW-1 (Patch 0658); NO theorem change, NO new axiom
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this Patch

TARROW-1 (Patch 0658) answers a registered open problem (OPEN-CHIR-2a) and completes the
chirality-status capstone on the temporal side; it was registered single-pass. Per
`operating_system.md` §17 + the AUDIT-1 / MERGE-2 / STATUS precedents, an artifact that answers a
registered problem (or advances a registered verdict) goes to the multi-AI panel before its
"single-pass" qualifier is removed. This issues the review package; the "single-pass" qualifier is
removed only on cycle completion.

## §2 What the package does

NEW `review/theo_chir_tarrow_1_review_package_v1.0.md` — self-contained, all content inline:
- §0 IS/IS-NOT; §1 the MERGE-2 context (the (P,T) table, the STATUS spatial result); §2 the four
  TARROW-1 claims inline (A verdict instantiation; B the T-even-geometry lemma + no-chain; C the W3
  placement; D the W2-exclusion/W1-pin; E the CPT unification); §3 the registered goal-answer (both
  halves); §4 seven scrutiny questions; §5 triage; §6 reviewer-specific framing; §7 the verify code
  EMBEDDED IN FULL; §8 response format.
- **Triage foregrounds Q4 (the CPT unification — the novel, most-flippable claim) and Q1 (is it
  informative AND independent of STATUS-1/2, or a P→T transliteration?).** Q1's second deflation route
  (transliteration) is specific to this theorem: I explicitly name the two pieces of independent
  content the reviewers should test — the T-even-geometry lemma (a structural *absence* with no
  spatial analog) and the CPT unification — and invite the panel to find them empty if they are.
- **Process-learning fix applied (Patch 0656 lesson):** the verify code is **embedded in full** in
  §7 (not referenced by filename), so Grok can reach SCRIPT-EXECUTED without external files. I
  extracted and ran the embedded copy standalone — CHECK 1/2/3 all pass.

## §3 Why these questions

The load-bearing, non-obvious claims of TARROW-1 (the places a careful adversary could break it):
1. **CPT unification (Q4)** — the genuinely new result, and it rests on an *assumption*
   (CPT-invariance of the CPP substrate). I split it into 4a (is the assumption honestly scoped or
   load-bearing-in-disguise?), 4b (is T-odd↔P-odd via CPT applied correctly?), 4c (is it really one SM
   object, or could CPP admit independent cross-sector T-odd and P-odd quantities?). This is the
   highest-value verdict-flip target.
2. **Transliteration deflation (Q1)** — the honest risk that "P→T, V→W" adds nothing. The defense is
   B + E; the package puts them on trial rather than asserting them.
3. **T-even-geometry / no-chain (Q2)** — a claim of *absence* (no T-odd geometric quantity, no
   finite-group breaking chain). Absence-claims are the hardest to certify; Grok is asked to recompute
   the (P,T) table from first principles and hunt for a missed T-odd static object.
4. W2-exclusion (Q3), partition instantiation (Q5), one-theorem justification (Q6), overclaim sweep
   (Q7) round out the surface.

## §4 Anti-priorities

TARROW-1 body unmodified (stays v1.0); no theorem entry changed; no new axiom; the package v1.0 is
the immutable request record (reviewer responses + synthesis will go in the separate
`reviews-CHIR-TARROW.md`). Opening a cycle is not closing it: if Q4 flips (the CPT identification is
unsound, or CPT-invariance is shown to be doing hidden work), TARROW-1 → v1.1 with the CPT-unification
claim demoted to a conditional; that is an outcome the package explicitly invites, not a failure to
avoid.

## §5 Status / next

Integrate reviewer responses (the MERGE-2 / STATUS pattern): if a verdict-flipping objection on Q4/Q1
lands, restate (→ v1.1); else apply calibration and close 3/3, removing the "single-pass" qualifier
and review-hardening the full chirality-status capstone (spatial V3→V1 + temporal W3→W1, unified by
the CPT reopener) end to end. After that, the only remaining engine for *actual* emergence on either
side is the deep cross-sector arc — 1d-β-ii (the chiral-vacuum mechanism) and/or 1d-β-v / OPEN-SM-4
(the SM CP/T phase that, via CPT, fixes BOTH signs at once).

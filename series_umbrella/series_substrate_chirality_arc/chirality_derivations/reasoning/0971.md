# reasoning-0971 (verbatim, captured late — see 0942 header note; same provenance)

The TARROW-2 re-read. Two pre-commitments were on the record — the PH's "would unconditionalize BOTH" and TARROW-2's own header — and I had already predicted at 0949 that TARROW-2's cross-check *consumes* the residual. So I expected the prediction and the pre-commitment to collide, and they did.

Reproduced the argument at A = 0 first, before touching the residual: 1200 faces, `a+b+c = 0` to 2×10⁻¹⁶, exactly 420 faces with nonzero abc at |abc| ∈ {1/8, 1/4}, slope 3.00. Those are TARROW-2's headline numbers obtained independently, which is what makes the rest of the patch trustworthy.

**The residual breaks the order-counting.** With constant A the per-face O(δ¹) cancellation fails, because the cycle log-ratio per edge becomes ≈ 2Bc_e/(1 + A m_e) — the effective tilt is position-dependent, so the face sum is no longer proportional to `a+b+c = 0`. Slope 3.00 → 0.99. My 0949 prediction was right.

**But the conclusion survives and is strengthened, which I did not expect.** The *same 420 faces* violate at every A tested. The residual changes the **order** at which detailed balance fails, not **whether** — and it fails *earlier*. So `C_T = Yes` and the W3 → W1 candidate stand, and the residual can only help them.

So the PH's prediction is half right, and the half that matters is right. Wrote it that way rather than as a clean yes or no.

**Structurally identical to CONV-048's C2 finding** — conclusion survives at the physical bias, asymptotic characterisation does not — and the same repair applies. Noticing the parallel is what told me this needs a scope clause rather than a re-derivation, and therefore no panel.

**Explicitly did not claim the W3 → W1 upgrade.** Removing a conditionality from a conclusion is not performing the upgrade, and the two are easy to blur in a summary.

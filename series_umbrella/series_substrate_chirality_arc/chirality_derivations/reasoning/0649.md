# Reasoning trail — Patch 0649 (THEO-CHIR-MERGE-2 multi-AI review integrated; v1.1 calibration)

**Patch:** 0649 (Session 149, 29 May 2026)
**Type:** review integration — THEO-CHIR-MERGE-2 v1.0 → v1.1 (calibration; no verdict change)
**Reviewers:** ChatGPT, Grok, Copilot (responses in `review/reviews-CHIR-MERGE-2.md`)
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Outcome in one line

No falsifier. Verdict **M1-χ stands**, now stated **conditional on MERGE-α** (the registered
identification of `j_net` with the T-odd thermodynamic arrow). v1.1 is a calibration that makes two
things explicit, both surfaced by ChatGPT and corroborated by Grok/Copilot: (1) what secures `δ`
T-odd, and (2) the precise scope of the category-mismatch corollary.

## §2 The three reviews

- **Grok** — Tier-3 ran CHECK 1 (600-cell achirality confirmed); *derived* `δ` T-odd; explicitly
  attempted and rejected the static-anisotropy counter-argument ("the asymmetry is tied to the cycle
  direction, which T reverses"); verdict defect-free, M3→M1-χ stands, no changes required.
- **Copilot** — per-row PASS; decomposition strictly forced; category-mismatch airtight (independently
  dismissed the third-object route — no (P-odd,T-odd) primitive is allowed in an achiral substrate);
  flagged `δ` T-odd as the *thinnest* justification and recommended stating the T-action on `ê`
  explicitly. Verdict: logically sound, M3→M1-χ justified.
- **ChatGPT** — the substantive dissent. (a) MA.1 *alone* does not force `δ` T-odd: Interpretation B
  (δ a static T-even anisotropy) is available unless `j_net` is independently established T-odd, and
  "all currents are T-odd" was not a listed premise. (b) The category-mismatch overstates: direct
  *identification* is forbidden (airtight), but a covariant *tie* via a third (P,T)=(−,−) pseudoscalar
  is not — "category mismatch forbids identification, not necessarily dependence"; no such X currently
  exists, so the wording should be "no known covariant relation presently exists," not "can exist."

## §3 Why this is calibration, not a falsifier (the judgment)

ChatGPT is correct that the rate law alone underdetermines `δ`'s T-character. The resolution:
- **δ couples ODDLY to ê** (`r(ê) ≠ r(−ê)`), so it is a polar drift-/flux-like bias, NOT an
  even-in-direction static anisotropy (conductivity, crystal-axis, nematic — all even, `r(ê)=r(−ê)`).
  This excludes ChatGPT's *even*-anisotropy analogy class outright (new CHECK 3 demonstrates it).
- The remaining T-even reading — `j_net` a static *polar* order parameter — is excluded not by the
  rate law but by the **registered MERGE-α / THEO-DSL-3 identification of `j_net` with the T-odd
  thermodynamic arrow**. ChatGPT's Interpretation B is therefore not a neutral alternative; it is a
  *rejection of MERGE-α*. So `δ` T-odd is forced *conditional on MERGE-α*, inheriting its Layer-2.5
  viability ceiling. If MERGE-α's arrow-identification fails, `δ` is T-even and the verdict reverts to
  M3 — which is exactly falsifier G2, now located precisely at the MERGE-α ceiling rather than as a
  free interpretive choice.

The verdict M1-χ holds because the framework *commits* to MERGE-α; v1.0's error was asserting `δ`
T-odd as if forced by MA.1 alone. v1.1 fixes the grounding and states the conditional. This is the
honest improvement — ChatGPT's own stated condition ("the decomposition is valid IF δ is established
T-odd") is now met by the explicit MERGE-α grounding.

On the category-mismatch: ChatGPT + Copilot converge. v1.1 splits the corollary into (a) no direct
identification [airtight, different P,T characters] and (b) no *presently-available* covariant tie
[would need an independent (P-odd,T-odd) mediator; none exists — achiral 600-cell, and σ_cycle/ω_PCD
are not independent]. The universal "can never exist" is removed; the residual route is registered as
new falsifier G6.

## §4 Edits applied (v1.1)

- `theo_chir_merge_2.tex`: Lemma (δ T-odd) strengthened (oddness-in-ê + MERGE-α grounding + the
  conditional); category-mismatch corollary split (a)/(b); §caps + abstract note the conditional;
  falsifier G2 refined (MERGE-α ceiling), new G6 added; CHANGELOG v1.0→v1.1; date/version bumped.
  Clean 3-pass pdflatex, 6-page PDF, zero undefined refs (PDF not tracked). Canonical filename
  unchanged (version history in the CHANGELOG header, per convention).
- `code/verify_merge_2_parity_decomposition.py`: NEW CHECK 3 (δ odd-in-ê: `r(ê)≠r(−ê)` linear in δ;
  contrasted with an even static anisotropy `(ê·n̂)²` giving `r(ê)=r(−ê)`). All 3 checks pass.
- NEW `review/reviews-CHIR-MERGE-2.md` (verbatim verdict lines + synthesis + triage).
- `frontier_sectors/CHIR.md`: OPEN-CHIR-MERGE — review integrated, v1.1, M1-χ conditional on MERGE-α.
- `frontier_sectors/FP.md`: OPEN-FP-F1-2 L4-D — review-integrated, conditional note.
- `theorem-registry.md`: Patch 0649 changelog; 0648 demoted.

## §5 Honest status

Multi-AI review integrated (3 reviewers, no falsifier). M1-χ stands, conditional on MERGE-α. The
"single-pass" qualifier is removed. An optional ChatGPT v1.1 re-review (confirming the conditional
framing meets its stated condition) would formally close the cycle, mirroring the AUDIT-1 v1.1
re-review close — but is not required for the verdict to stand at its honest Layer-2.5 scope.

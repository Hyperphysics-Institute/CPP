# Reasoning trail — Patch 0664 (THEO-CHIR-BRIDGE-1 multi-AI review cycle OPENED)

**Patch:** 0664 (Session 150, 30 May 2026)
**Scope:** workflow-only — review package issued for THEO-CHIR-BRIDGE-1 (Patch 0663); NO theorem change, NO new axiom
**Capture mode:** verbatim at-patch (`STATUS:verbatim`); produced by the "initiate review protocol" command (OS §1 / `review_dispatch_protocol.md`)

---

## §1 Why this Patch

Thomas invoked "initiate review protocol" on BRIDGE-1. Per `review_dispatch_protocol.md` §2, the
command presupposes a self-contained review package; none existed for BRIDGE-1, so the protocol has
the assistant create it first (the cycle-opening patch) and then emit the Dispatch Output. BRIDGE-1
(0663) advances a registered bridge (B-i of OPEN-CHIR-3 ∪ 1d-β-v), so it goes to the panel before its
"single-pass" qualifier is removed.

## §2 What the package does

NEW `review/theo_chir_bridge_1_review_package_v1.0.md` — self-contained, all content inline:
- §0 IS/IS-NOT; §1 the F1–F5 + OPEN-SM-4 context; §2 the BRIDGE-1 claims inline (Thm 1 the ℤ₂-match,
  Thm 2 the P/T-face dictionary, Prop the magnitude thread, Cor the CONJ-CHIR-1 effect); §3 the
  registered position; §4 seven scrutiny questions; §5 triage; §6 reviewer-specific framing; §7 the
  verify code EMBEDDED IN FULL; §8 response format.
- **Triage foregrounds Q1 (the kinematic/dynamical boundary — the existential honesty check for THIS
  theorem) and Q2 (premise P2, the OPEN-SM-4 ℤ₂-reading).** Q1 is the highest-value target precisely
  because BRIDGE-1's whole value is being an *honest correspondence that does not move the verdict* —
  if any step smuggles a dynamical claim (the ℤ₂ breaks / the break is EWSB / FI-C-9 produces parity
  violation), the theorem flips from honest-correspondence to overclaim. The package invites the panel
  to find exactly that.
- **Process-learning fix sustained (Patch 0656):** the verify code is embedded in full (§7); extracted
  and run standalone — CHECK 1/2/3 pass.

## §3 Why these questions

The places BRIDGE-1 could be wrong or overclaimed:
1. **Q1 (boundary):** the load-bearing honest cap. Every step must be kinematic; none dynamical.
2. **Q2 (P2):** the ℤ₂-reading. If OPEN-SM-4's ℤ₂ is a different ℤ₂ (CP, or non-orientation), the
   match fails (B1). Grok asked to press this + recompute the det-coset group theory.
3. **Q3 (group theory), Q4 (correspondence-not-causation), Q5 (magnitude + the χ φ⁻¹/φ⁻³ flag),
   Q6 (the corollary's fairness), Q7 (overclaim sweep)** round out the surface.

## §4 Anti-priorities

BRIDGE-1 body unmodified (stays v1.0); no theorem entry changed; no new axiom; the package v1.0 is the
immutable request record; reviewer responses → `reviews-CHIR-BRIDGE.md`. Opening a cycle is not
closing it: if Q1 flips (a smuggled dynamical claim) or Q2 flips (P2 unsound), BRIDGE-1 → v1.1 with the
offending step demoted; those outcomes the package explicitly invites.

## §5 Status / next

Integrate reviewer responses (the MERGE-2/STATUS/TARROW pattern): verdict-flip on Q1/Q2 → v1.1 restate;
else calibration + close 3/3, removing the single-pass qualifier. After close: B-ii (the magnitude
anchors + the χ-normalization reconciliation), then the deep B-iii (1d-β-ii / OPEN-SM-4 (a)/(b)) — the
sole verdict-moving engine — per Thomas's choice.

# Reasoning — Patch 0906 (descriptive: what sign(n̂) is and where chirality enters the axioms)

**What this is.** A grounded, strictly descriptive answer to the post-0903 question "if chirality is primitive, what carries it and where does it enter?" — written from the registry, asserting nothing new.

**Key grounding finding (and the correction it makes).** The 9 axioms A1–A11 contain no handedness: A1 = CP polarity, A2 = the 600-cell whose group H₄ (order 14400) *contains reflections* = achiral. So "chirality is an axiom / the world just is this way" is **not** the framework's actual status. Chirality enters as Foundational Inputs one layer above the axioms: `n̂` (the primitive 4D direction) = FI-C-RC-1; the enantiomorph sign `sign(n̂)` = the det-pseudoscalar order parameter of the H₄→H₄⁺ (index-2, ℤ₂) breaking; the magnitude χ = φ⁻³ = FI-C-9. All chirality reduces to this one primitive (MERGE-2) + the T-arrow sign(δ).

**The "hinge" (what Thomas asked for, "where is it actualized").** `n̂` imprints a direction-correlated edge perturbation `ε(ê·n̂)` on the 600-cell's 720 edges — odd under n̂→−n̂, max|δ| = φ⁻³. Its sign = which enantiomorph; its magnitude = φ⁻³ (selected by THEO-CHIR-CHI-1 locality). That imprint *is* the handedness.

**Lane discipline.** Cited the DM lane's 0813 local η-construction descriptively as a concrete local realization (its ≈0 mean = the bare achirality), without importing any verdict. No CHIR.md edit (kept to arc-local files + INDEX to minimize shared-file touches while the DM window is active); disjoint from DM's SR.md/F.1 work.

**Scope.** Descriptive only. 0903 V3/W3 stands; FI-C-9's status as Foundational-Input-under-derivation (OPEN-FI-C-9-FP-MECHANISM, 1d-β-ii, §14.17) is unchanged. No verdict, no THEO, no ID, no count change. The corrective value: replaces "settled axiom" with the accurate "Foundational Input, precisely located, derivation open."

# 0979 — reasoning fragment (verbatim, at-window; captured at 0980)

**Lane:** chirality (09xx). **Patch:** 0979, 14 Sep 2026.
**Scope test:** records two arithmetic corrections and overturns an exemption ⇒ **not exempt.**

---

The founder said the reasoning had been recovered "in the 0974b patch." There is no 0974b. Origin had
**0975b**. That is trap #2 of the Session 228 handover — *"the founder's ledger drifts by one"* — firing on
the handover's own author, and the handover's own instruction is to verify against origin rather than take
the number on trust. Did that before building.

**0975b is right and I am not second-guessing it.** The originating window was still live, so what it wrote
is primary source rather than reconstruction — which is precisely the distinction 0977 refused to blur, and
0977's error was in not asking whether that window was reachable, not in refusing to fabricate. 0975b also
enacted the governance fix I had declined to make: the content-not-wrapper scope test in bootup §3 and the
capture protocol, plus a new §15 Step E′ placed **before** the handover, because only the originating window
can capture and an audit that fires after the handover fires after the ability to act.

**Two counts in its prose do not match the tree it shipped.** It says 21 fragments; its own commit file list
contains 20. It says "thirteen patches are genuinely exempt" and then enumerates fifteen. I recounted
against the working tree independently rather than trusting either number: 25 of 39 in the 0936–0974 range
now have fragments, 14 do not. The reconciliation is 20 captured + 14 in-range exempt = 34, with `0974a` a
fifteenth entry lying **outside** the range over which the 34 was counted — which is probably how the two
errors arose and why they nearly cancel. No work is affected. I corrected the ledger rather than the work,
and said which was which, because a reader who finds the discrepancy later should not have to wonder whether
fragments are missing.

**0974a fails 0975b's own test.** The test says a patch is bookkeeping only with no derivation, no
computation and **no finding**, and that a shipped code change disqualifies the exemption. 0974a found — by
testing, not by reading — that `next_id.py` matched `^(0\d{3})\s+`, so a suffixed ID put a letter where
whitespace was expected and **the first suffixed patch in the block would have been invisible to the very
collision gate the renumber existed to satisfy.** Then it shipped the parser fix in the same patch.

I considered leaving this alone as pedantry against a patch that had just done the lane a large favour, and
rejected that: the whole content of 0975b's fix is that exemptions must be decided by content rather than by
wrapper, and 0974a arrived wrapped as a renumber. **It is an instance of the failure mode the test was
written against, committed one patch after the test was written** — which makes it evidence that the test is
right and that applying it is the hard part, not evidence against it. Said so in those terms rather than as
a correction.

**0936 and 0937 not re-filed from the founder's window scrape, though it covers them.** The scrape's head is
the assistant's user-facing response text; the at-patch fragments on file are the internal reasoning and are
markedly richer. `reasoning/0936.md` records *"Before running it I expected (c) to give a nonzero doublet
under both readings. The script says otherwise"* — an expectation overturned by its own script, which
appears nowhere in the response text. **Overwriting Tier 4 with the thinner object would be a loss disguised
as a capture**, and the filename on the upload would have made it look like an upgrade.

Measured the scrape's coverage rather than trusting its filename (`…_0936-0974a`): bootup, 0936, 0937, then
a jump straight to the 0974a material; 0939–0973 absent. Both supplied scrapes overstate their range in the
filename, so the measured coverage went into the provenance file's header.

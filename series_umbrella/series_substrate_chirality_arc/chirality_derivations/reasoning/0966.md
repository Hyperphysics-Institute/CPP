# reasoning-0966 (verbatim, captured late — see 0942 header note; same provenance)

Five pre-dispatch returns, unanimous HOLD, and all four of their corrections were right.

**Grok's objection first, because if it stood the whole evidence base was worthless:** "C_nn is a proxy for participation. If C_nn is insensitive to a drop from p=12 toward p=4, a 3×10⁻⁴ shift is not evidence." Tested that before anything else rather than defending the measurement. C_nn(p=12) = 0.0531 against C_nn(p=4) = 0.1609 — a factor of 3.03 across exactly the disputed range.

**And that converts the null into a bound**, which is the right answer rather than a defence of the proxy: |ΔC_nn| ≤ 3×10⁻⁴ excludes any participation below p ≈ 11.93. The measurement does not merely fail to detect concentration; it bounds it.

The owed test done properly: six δ values through φ⁻³, six replicates each, error bars, and p_eff reported directly as two seats required. p_eff = 12.24 at the physical bias, and the trend runs *upward* — the bias slightly de-concentrates.

**Link (c) withdrawn.** GPT: "four vectors required to define chirality ⇏ four nonzero coefficients required to read an already-defined chirality variable." Correct, and it defeats (c) as I wrote it — only **one** of {d̂, n̂, r̂₁, r̂₂} is an edge direction; the rest are the fixed frame. My T2 never carried the prose it was attached to. What replaces it is **scope**: a single-edge determinant is frame-dependent, its sign flipping under 98 of 200 admissible frames, so it is not an invariant handedness reading at all.

**Headline withdrawn** on two independent rulings. GPT's reason is the one worth keeping: it prevents the last assumption disappearing by *renaming rather than deriving* it.

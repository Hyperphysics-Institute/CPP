# Reasoning trail — Patch 0654 (THEO-CHIR-STATUS-2; 1d-β-i + partial 1d-β-iii)

**Patch:** 0654 (Session 149, 29 May 2026)
**Theorem:** THEO-CHIR-STATUS-2 — the chiral-vacuum breaking chain H₄→H₄⁺ + the axiom-level V2-exclusion (OPEN-CHIR-1d-β sub-targets 1d-β-i + partial 1d-β-iii)
**Layer:** 2.5 (provisional) — structural; extends STATUS-1
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this Patch

Per the 0653 recommendation: deliver the two Layer-2.5-reachable structural sub-targets of 1d-β —
the breaking-chain group theory (1d-β-i) and the partial sign-non-fixing result (partial 1d-β-iii) —
to sharpen STATUS-1's "V3, upgradeable to V1" into "V3, and the upgrade is *exactly* V1 (not V2)."
This does NOT attempt the deep mechanism (1d-β-ii); the verdict stays V3.

## §2 The result

- **1d-β-i (breaking chain).** The 600-cell isometry group is the Coxeter group H₄ (order 14400,
  reflection-generated, achiral — MERGE-2). The det map H₄→{±1} is a homomorphism with kernel the
  rotation subgroup H₄⁺ (order 7200, index 2); quotient ℤ₂. A chiral vacuum is the SSB H₄→H₄⁺; the
  order parameter is the det-pseudoscalar sign(n̂) = FI-C-9; two degenerate vacua, related by a
  reflection coset. (CHECK 1: closure under R=diag(−1,1,1,1) + the det grading on {I,R,R²,rot}.)
- **partial 1d-β-iii (sign non-fixing).** Distinguishing the two enantiomorphs requires a P-odd
  pseudoscalar (every P-even invariant — Gram, |signed volume|, distance spectrum — is invariant
  under the relating reflection; the signed 4-volume is P-odd and flips). By MERGE-2 the unique
  primitive pseudoscalar is FI-C-9 itself → the only sign-fixer is circular → no axiom-level
  pseudoscalar fixes the sign → value-axis ≠ derived at axiom level → **V2 excluded at axiom level**.
  (CHECK 2: signed volume flips under R; Gram/|vol|/distance multiset invariant.)
- **Corollary (V1-pinning).** With STATUS-1: the capacity=Yes outcome is V1 (mechanism derived, sign
  spontaneous/free), not V2 — at axiom level. The FI-C-9 emergence target is exactly standard SSB.

## §3 Honesty discipline

- **Placement unchanged — still V3.** No chiral-vacuum mechanism is derived (1d-β-ii open); capacity
  axis stays No. This sharpens the UPGRADE target, not the current status. Stated explicitly (§setup,
  Rem "this is the structure, not the dynamics," caps).
- **V2-exclusion is axiom-level, not absolute.** A cross-sector pseudoscalar (1d-β-v, e.g. an SM
  CP-violating phase) would reopen V2 — registered as falsifier H3′ + Remark "the reopening." No
  overclaim that V2 is impossible.
- **Inherits MERGE-2 conditionality** (FI-C-9 uniqueness conditional on MERGE-α; "currently-identified").
- **Spatial chirality only** (T-arrow status = parallel 2a).
- The group orders (14400/7200) are cited standard facts; the structural claims (closure, det grading,
  P-odd vs P-even) are machine-checked.

## §4 DG actions

- DG-4 EXECUTED: OPEN-CHIR-1b (the chiral subgroup / icosahedral rotation sense) consolidated into
  1d-β-i's breaking chain (the rotation subgroup H₄⁺ is the chiral subgroup; no duplication).
- DG-1 (already executed 0653): OPEN-CHIR-1d-β ID reserved; this Patch fills two of its sub-targets.

## §5 Registry actions

- NEW `theo_chir_status_2.tex` (v1.0; clean 3-pass pdflatex, 5-page PDF, zero undefined refs — PDF
  not tracked; \jnet macro defined in-preamble to avoid the 0653 compile bug) + this reasoning
  fragment + verify `code/verify_status_2_breaking_chain.py` (CHECK 1 + CHECK 2; both pass).
- Changelog-style registration (CHIR precedent); CHIR.md sector record (1d-β note + sub-corpus note);
  NO theorem-registry body-table proved-row.
- `theorem-registry.md`: Patch 0654 changelog; 0653 demoted.

## §6 Honest caps

1. Structural result; no mechanism derived; verdict stays V3.
2. V2-exclusion axiom-level only (cross-sector pseudoscalar reopens it).
3. Conditional on MERGE-α; "currently-identified" (G6).
4. Spatial chirality only; T-arrow = 2a.
5. Single-pass; review optional (conservative structural result; the group theory is standard and
   machine-checked).
6. Remaining 1d-β sub-targets: ii (the deep mechanism — the sole V3→V1 engine) and v (the SM
   cross-link — the sole V2-reopener), both cross-sector behind §14.17.

# reasoning-0957 (verbatim, captured late — see 0942 header note; same provenance)

Five addendum returns; GPT withdrew its falsifier. The temptation was to adjudicate.

**Two seats refused the accumulation argument and demanded the same test.** Grok: "What concerns me is not the separate clearances. It is stacking them… Multiplicative-on-the-slow-edges they are unshown." Copilot required an explicit combined bound. I had run negative A and the odd quadratic as **two separate experiments** and never together. That was a real gap and I had not seen it.

Ran the corner. Two results, and both went against my expectation:

**The admissible region is not a rectangle.** I had published |A| ≤ 1.025 — computed at C = 0. With the odd quadratic on, positivity fails earlier: at A = −1.00, C = +φ⁻³ the minimum rate is **−0.0434**, negative. So the A-bound in the package was *false as written*. Second time in three patches a published scope claim was narrower in reality than in text.

**Stacking is superadditive.** Joint J² is 4.0× the *sum* of the separate J² values — amplitudes roughly double rather than adding in quadrature. That refutes Grok's "additive-in-amplitude" expectation **and my own first assertion**, which I had written as "additive, not multiplicative" and which the test refused. Same pattern as the negative-A prediction at 0955: the reviewer was right that the test was needed and wrong about what it would show. Twice now — which is the argument for running rather than reasoning.

**And a margin claim I had been overstating.** "Five or more orders below" holds at framework scale and *not* over the extended scan, where it is about two. I had used that phrase, and several returns had repeated it back. Split it by scale rather than keeping the flattering number.

Did not adjudicate: adjudicating a document with two known-wrong numbers is adjudicating the wrong thing.

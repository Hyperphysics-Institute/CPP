# Reasoning — Patch 1100 (Phase-0 parallel round, worker window W1)

**Artifact:** NEW `sketches/chir_biii_eta_susceptibility_symmetric_form_hness_stop.md` + NEW verify `code/verify_eta_susceptibility_symmetric_form.py` (CHECK 1/2/3/4 PASS) + this reasoning fragment. Stage-2 structural pass on the sign(μ²) verdict-mover. NO new theorem ID, NO reserved-ID consumed (THEO-CHIR-CAPACITY-1 stays reserved). NO verdict move (V3/W3 stand).

## What I set out to do
Advance the μ²-sign computation toward THEO-CHIR-CAPACITY-1 by building the local η-field connected susceptibility χ_η in the Patch-0694 NESS π and attempting sign(μ²) = sign(χ_η⁻¹).

## What is reachable, and the reduction I obtained
Stage 2 of the 0694 pipeline (the symmetric-part *form*) is reachable. In the equilibrium-like Gaussian approximation the symmetric η-field action is ½ ηᵀ(L + m²I)η with L the 600-cell graph Laplacian (the 0684 kinetic channel). The integrated susceptibility is 1ᵀ(L+m²I)⁻¹1, and because L·1 = 0 (constant zero mode, verified ‖L·1‖ = 0, λ₀ = 0, gap λ₁ = 2.2918, λ_max = 15.708), this is **zero-mode dominated: χ_sym = N/m² exactly** (verified to machine precision, χ·m²/N = 1.00000000 across m² ∈ {0.05…2}). Therefore χ_η⁻¹ ∝ m² and **sign(μ²) = sign(m²)** — the entire sign bit collapses to the sign of the field mass. The O(δ³) current part (0689/0694) is subdominant by scaling, so it perturbs rather than sets the sign wherever m² ≠ 0 (and χ_sym = N/m² is finite, not marginal, for m² > 0).

## Where it bottomed out, and why I stopped instead of pushing through
The reduction sharpens the residual to sign(m²), the η-FIELD curvature. The constructed π is the **single-walker** stationary measure — a 1-point marginal on 120 vertex states. It does not contain the η-field two-point function and does not supply m². I demonstrated this concretely (CHECK 4): the only off-diagonal connected correlator a single walker affords is the single-occupancy artifact ⟨n_v n_w⟩_c = −π_vπ_w (v≠w) — sign-definite negative by the one-walker constraint, carrying zero m² information. A χ assembled from π alone measures occupancy, not chiral curvature.

Bridging π → m² needs either (i) an occupation/many-walker generator (a NEW mechanism — constructing the many-body process is itself new machinery, and a non-interacting product assumption would be an unjustified closure) or (ii) a justified single-site reduction (which is the (H-NESS) hypothesis itself, not a derivation). Per the round's escalation rule — and consistent with the 0684 §5.2 anti-erasure ruling that I will not present an (H-…)-contingent proxy as sign(μ²) — I did not invent either. I stopped at (H-NESS) and registered exactly where and why.

## Discipline check
No η-field fluctuation measure built; no susceptibility value; no sign(μ²); no theorem ID; THEO-CHIR-CAPACITY-1 reserved/untouched (still below the "DSL constrains sign(μ²)" crystallization bar — the reduction to sign(m²) sharpens but does not meet it); no header count change; no new programme-level Open-Problem (stage-2 of B-iii-(i)/0668); closed theorems + F.1/DSL/OPEN-SM-4/Capotauro consumed not edited; BRIDGE-1 P2/kinematic cap rides cross-sector statements; no verdict move (V3/W3 stand); conditional on Mechanism A (OPEN-FP-F1-2). Owned-path-only (NEW files under `chirality_derivations/`); NO shared registry / `frontier_sectors/` file touched — implied updates are in the sketch §7 REGISTRY HANDOFF NOTE for the integrator to batch. Not pushed (W1 hands off the patch only).

# Reasoning — Patch 0680 (Session 152)

**Artifact:** `chirality_derivations/theo_chir_vw_1.tex` (THEO-CHIR-VW-1, v1.0; clean three-pass pdflatex, 7-page PDF, zero undefined refs) + verify `code/verify_vw_1_reflection_positivity.py` (CHECK 1/2/3 all pass).
**Type:** Layer-2.5 provisional structural / conditional theorem. NOT a derivation, NOT a positivity proof, NOT a verdict move. Crystallizes THEO-CHIR-VW-1 (reserved at the 0679 scope sketch) at the reachable level.

## The load-bearing idea

B-iii (0668) reduced capacity to one bit, `sign(μ²)`, and pinned it behind §14.17. VW-1 attacks the bit two ways at once, both without the full action:

- A symmetry breaks only **explicitly** (an odd term in the action) or **spontaneously** (a vacuum instability). 
- STATUS-2's axiom-level **V2-exclusion** already closes the *explicit* route: no axiom-level P-odd pseudoscalar exists, so the action is parity-even (no η-linear term). 
- **Vafa–Witten** closes the *spontaneous* route — *if* its three hypotheses hold (H1 reflection positivity, H2 vectorial-not-axial, H3 no θ-term), then `μ²>0` is forced. 
- So the two no-gos are **two faces of one property**: the substrate measure is parity-even (→ no explicit breaking) and reflection-positive (→ no spontaneous breaking). Together they say the substrate parity ℤ₂ is unbroken within the substrate axioms — conditional on H1 — and any handle to break it must be **external** (the SM bridge).

That unification is the centerpiece (Thm 5.1). It is genuinely non-trivial: it shows the programme's separately-established value-side and capacity-side no-gos share a single root cause, while keeping the two bits distinct (DG-4).

## Established vs conditional vs open (the honest split)

- **Established (reachable now):** VW-b — the det-coset ℤ₂ is a vectorial reflection (det=−1, from STATUS-2's H₄→H₄⁺), in VW's protected class (Lemma 3.1). VW-c — the bare substrate carries no VW-evasion route {chiral content, θ-term, complex measure} at the level hardened (achiral; sign(δ)=W3 not an action term; real ℚ[φ] coefficients), and each route enters via the SM bridge (Lemma 4.1). The unification (Thm 5.1). The reduction: the verdict-moving capacity question = "is the DSL measure reflection-positive?"
- **Conditional (on H1):** the verdict map (Thm 6.1) — H1 ⇒ μ²>0 ⇒ η=0 within the substrate axioms ⇒ observed FI-C-9≠0 is either V3-by-principle (spontaneous branch closed, strictly stronger than "not yet derived") OR bridge-sourced (explicit, external = B-iii-(ii)/CONJ-CHIR-1). Stated conditionally; no branch decided.
- **Open (the residual):** H1 (reflection positivity) itself — only criteria + a first-pass real-measure assessment (Prop 4.2); a full Osterwalder–Schrader proof may need the explicit DSL action (§14.17). VW-a is "no manifest obstruction," NOT proved.

## Why it does not move the verdict

H1 is open, so the verdict map is conditional; V3/W3 stand. The payoff is *relocation + sharpening*, not closure: the entire verdict-moving capacity question is now one positivity question instead of "compute the whole effective action," and it *may* be forced outright if the first-pass H1 survives scrutiny. Per DG-3 (the maximal-honesty gate — this is the first thing that could move V3 off "not yet derived"), any verdict-bearing outcome requires multi-AI review before verdict language changes. This artifact registers single-pass; review is the natural next step.

## Load-bearing distinctions kept explicit

- **Capacity vs value** (DG-4): VW addresses capacity (sign(μ²)); V2-exclusion addresses value (sign-selection). The unification shares their root, does not conflate them.
- **Spontaneous vs explicit:** VW forbids only *spontaneous* breaking; STATUS-2 closes *explicit* at the axiom level; the only escape is an explicit *external* (bridge) source — which is why branch (B) inherits BRIDGE-1's kinematic/P2 cap.
- **Continuum limit (VW-e):** strict SSB needs the thermodynamic/continuum limit; the analysis is sharpest on the Φ-continuum EFT (CONT-1); finite-600-cell facts are the lattice precursors. (Rmk 6.3.)

## Caps

H1 not proved; no sign(μ²) computed; no positivity proved; NO verdict move (V3/W3). All bridge-side statements inherit BRIDGE-1's kinematic/P2 cap. Vafa–Witten is consumed as a cited standard result, not re-derived. Falsifiers VW-F1..F5.

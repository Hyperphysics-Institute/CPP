# Reasoning trail — Patch 0658 (THEO-CHIR-TARROW-1; OPEN-CHIR-2a, the T-arrow status)

**Patch:** 0658 (Session 149/150 boundary, 30 May 2026)
**Theorem:** THEO-CHIR-TARROW-1 — the primitive/emergent status of the time-reversal arrow sign(δ) (OPEN-CHIR-2a); the temporal counterpart of STATUS-1 + STATUS-2
**Layer:** 2.5 (provisional) — classification/status result, not a derivation
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this Patch

The Session 149 handover left the chirality primitive/emergent-status programme at a review-closed
resting point with three forward options (the deep 1d-β-ii engine; OPEN-CHIR-2a; OPEN-FP-F1-2).
Thomas chose OPEN-CHIR-2a — the T-arrow's status, the other half of the full chirality picture, at
comparable rigor to the just-finished STATUS pair. This is the natural complement: it completes the
status picture on the temporal side without committing to the deep cross-sector dynamics.

MERGE-2 (closed 3/3) already isolated the object: the (P,T) decomposition
σ_cycle = sign(δ)·sign(n̂) put the *entire* P-odd (chirality) content in sign(n̂) = FI-C-9 (the
subject of STATUS-1/2) and the *entire* P-even/T-odd (arrow) content in sign(δ). OPEN-CHIR-2a is the
status of that residual arrow.

## §2 The result

THEO-CHIR-TARROW-1 places the T-arrow sign(δ) at **W3** at current rigor, pins its upgrade to **W1**,
and unifies its sign-reopener with the spatial one via CPT:

- **Verdict structure (instantiated, not re-proved):** STATUS-1's partition is generic to any
  (capacity, value) emergence question. Instantiate on the time-reversal axis — capacity 𝒞_T (is a
  substrate T-asymmetry mechanism derivable?), value 𝒱_T (is the arrow *direction* fixed?) — giving
  W3 / W1 / W2 by the same map. (CHECK 1.)
- **The disanalogy with parity (the genuinely new content):** STATUS-2 had a rich H₄→H₄⁺ breaking
  chain because the achiral 600-cell still hosts a P-odd order parameter (sign(n̂)). For time
  reversal there is **no such chain**: T acts on the *dynamics*, not the static lattice, and every
  static-geometric invariant (coordinates, Gram, distances, signed/unsigned volume, the orientation
  pseudoscalar sign(n̂) itself) is **T-even**. So the geometry supplies **no T-odd quantity at all**.
  (Lemma "T-even geometry"; CHECK 2a.) This is the row of the MERGE-2 table that makes it sharp:
  sign(n̂) is P-odd but T-*even*.
- **Current-rigor placement W3:** no registered mechanism *derives* a substrate T-asymmetry. DSL-3
  *narrates* the arrow (current ∥ n̂ → capture-phase bias → ω_PCD → macroscopic arrow) but inherits
  the DSL viability ceiling (entropy/irreversibility not derived; F.1 §14.17), and the F.2
  physicalization of σ is unbuilt. So 𝒞_T = No → W3. (Thm "placement"; CHECK 2c.)
- **Axiom-level W2-exclusion (the parallel of STATUS-2's V2-exclusion):** fixing the arrow direction
  needs a T-odd quantity (only T-odd quantities distinguish the two arrows — the T-analog of
  STATUS-2's pseudoscalar lemma). The geometry supplies none, and by MERGE-2 the **only** T-odd
  object in the framework is sign(δ) itself (ω_PCD isn't independent: ω_PCD = σ_cycle·n̂, and the
  T-odd part of σ_cycle *is* sign(δ)). So the only available sign-fixer is sign(δ) fixing itself —
  circular. ⇒ 𝒱_T ≠ derived at axiom level ⇒ W2 excluded at axiom level ⇒ upgrade pinned to W1.
  (Thm "W2-exclusion"; CHECK 2b.) The natural 𝒱_T = free subtype is **cosmological inheritance** —
  the arrow direction set by a low-entropy boundary state — exactly the standard thermodynamic-arrow
  picture, and exactly the subtype STATUS-1 named at Patch 0656.
- **The CPT unification (the payoff that makes this informative, not a relabeling):** STATUS-2's only
  V2-reopener is an axiom-external P-odd pseudoscalar, the candidate being the SM CP-phase
  (1d-β-v, OPEN-SM-4). TARROW-1's only W2-reopener is an axiom-external T-odd quantity. By the CPT
  theorem (T-violation ⟺ CP-violation), these are the **same** object. So "fully deriving chirality"
  — fixing both the P-sign FI-C-9 *and* the T-arrow sign(δ) — is a **single** cross-sector target,
  not two. (Thm "CPT unification"; CHECK 3.)

## §3 The assembled full chirality status

- Spatial pseudoscalar sign(n̂) = FI-C-9 (P-odd/T-even): **V3**, upgrade pinned to **V1**; V2 reopens
  only via the SM CP-phase (1d-β-v).
- Temporal arrow sign(δ) (P-even/T-odd): **W3**, upgrade pinned to **W1** (direction free =
  cosmological inheritance); W2 reopens only via SM T-violation = (CPT) the same SM CP-phase.
- A single CPT-linked cross-sector object (OPEN-SM-4) is the sole route to fully deriving *either*
  sign. The two halves of the picture close on one target.

## §4 Why one theorem (not a STATUS-1/STATUS-2 pair)

The spatial half needed two artifacts because the verdict partition (STATUS-1) and the group-theory
breaking chain (STATUS-2) are independent pieces. For the temporal half the partition is *inherited*
(STATUS-1 is generic; I instantiate it) and the breaking chain *has no nontrivial finite-group analog*
(that absence IS Lemma "T-even geometry"). So the temporal structural content is lighter and is
consolidated into one theorem, with a Remark stating exactly why. This is "comparable rigor"
(Layer 2.5, verify-script discipline, falsifier set, honest caps) appropriately scoped — not a
short-cut.

## §5 Honest caps (no merge-for-elegance)

- Classification/status result, **not a derivation**: does not derive the T-asymmetry mechanism, does
  not fix the arrow direction. sign(δ) is not derived.
- W3 = "not yet derived," NOT "underivable" — current-rigor floor, explicitly upgradeable.
- **Conditional on MERGE-α** (δ is T-odd only granting the j_net-arrow identification; MERGE-2 G2/G6).
  If δ is shown T-even (T1), the whole framing changes.
- **CPT-invariance is an assumption** (Thm "CPT unification"): if the substrate is CPT-violating (T5),
  the two reopeners need not coincide. Flagged as a falsifier, not hidden.
- The capacity engine (F.2 physicalization + DSL arrow-emergence) is the deep, gated counterpart of
  the spatial 1d-β-ii; not attempted here.

## §6 Verification

`code/verify_tarrow_1_arrow_status.py` — CHECK 1 (temporal verdict partition exhaustive/exclusive),
CHECK 2a/b/c (T-even geometry; the unique-T-odd-object → W2-exclusion; the W3 placement + W1 pin),
CHECK 3 (the CPT linkage table: both sign-reopeners are the same OPEN-SM-4 object). All pass.
Theorem compiles clean (three-pass pdflatex, 7-page PDF, zero undefined refs).

## §7 Status / next

Single-pass; **multi-AI review is the natural next step** (TARROW-1 answers a registered open problem,
OPEN-CHIR-2a, and completes the chirality-status capstone — same posture as STATUS-1/2 at v1.0). On
review-close the full chirality-status capstone (spatial V3→V1 + temporal W3→W1, unified by the CPT
reopener) is review-hardened end to end. After that: the deep cross-sector arc (1d-β-ii and/or
1d-β-v / OPEN-SM-4) is the sole remaining engine for *actual* emergence on either side.

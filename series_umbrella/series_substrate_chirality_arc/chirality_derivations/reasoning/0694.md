# Reasoning — Patch 0694 (Session 153)

**Artifacts:** NEW `sketches/chir_biii_signmu2_ness_construction_scoping.md` (construction + setup sketch) + NEW `code/setup_ness_stationary_measure.py` (CHECK 1/2/5 PASS; CHECK 3/4 confirm δ-scaling). The first step of Priority 1 (the μ²-sign verdict-mover) cleared by TARROW-2: **construct the Mechanism-A NESS before reaching for the η²-curvature.** NO η-field, NO susceptibility, NO sign(μ²), NO theorem, NO verdict move (V3/W3 stand). Conditional on Mechanism A (axiom, OPEN-FP-F1-2).

## Why / what
Thomas chose Priority 1 and asked to start with a scoping pass on constructing the NESS stationary distribution before the η²-curvature. Read the exact foundation first (the 0689 O(δ³) curl script + finding; the 0684 SUSC sketch with its "missing measure" crux; the 0688 engine decomposition; STATUS-2's η = det-coset label; the TARROW-2 close in CHIR.md). The right first move is to build the measure 0684 found missing and verify it against the reviewed TARROW-2 result, then map the pipeline — not to lunge at the sign.

## The pivot (load-bearing)
0684 (SUSC) stalled because a *connected* correlator needs a measure, and a fixed 600-cell configuration has no fluctuations. TARROW-2's reframe is exactly the fix: Mechanism A is a rate law ⇒ the rates of a Markov process on the 600-cell ⇒ it has a stationary measure π ⇒ **π IS the missing measure.** So Priority 1 is no longer blocked on "find/derive the §14.17 action"; it is reduced to "build π (done), lift it to the η-field measure / justify a single-site reduction, then take one correlator's sign."

## What I built
Generator Q on the 120 vertices, Q_vw = r₀(1+δ ê_vw·n̂) on edges, Q_vv = −Σ; π = left null vector (πQ=0), normalized; steady current J_vw = π_v r(v→w) − π_w r(w→v). Reused the validated 0689 600-cell build.

## The verification (the satisfying part)
- δ=0: π uniform to 3e−17, J_max 3e−17 ⇒ reversible equilibrium (VW-2's δ=0 RP anchor, stationary side). ✓
- δ≠0: π_v>0, Σπ=1 ⇒ valid NESS. ✓
- **tilt scaling slope = 1.016 ≈ 1** ⇒ π tilts from uniform at O(δ¹) (the conservative gradient part; detailed balance holds through O(δ²), so π has a leading equilibrium-like tilt).
- **current scaling slope = 3.000 ≈ 3** ⇒ the steady current ONSETS at O(δ³), **reproducing the 0689 detailed-balance violation from the stationary-measure side.** The O(δ³) Kolmogorov per-face content and the O(δ³) stationary-current onset are the same non-reversibility seen from the cycle side and the steady-state side — an independent cross-check of TARROW-2.

## The pipeline + where the sign lives (mapped, not executed)
Stage 1 construct π (DONE). Stage 2 define the local η-field (ℤ₂-even det/orientation precursor of FI-C-9; reachable structural). Stage 3 the connected susceptibility χ_η **in π** — THE SIGN — with the generalized-FDT (Agarwal/BMW) decomposition into a **symmetric part** (equilibrium-like, ~O(δ⁰–δ²), tends μ²>0 where DB survives) + a **current/frenetic part** (genuinely non-equilibrium, ~O(δ³), the only sign-flip source). Sharp sub-question: does the symmetric part *set* the sign (finite-positive ⇒ μ²>0 ⇒ V3-by-principle) or is it marginal/divergent so the O(δ³) current piece becomes sign-determining (⇒ V3→V1)? NOT decided. Stage 4 sign(μ²)=sign(χ_η⁻¹), review-gated (DG-3).

## The one new gap, named (H-NESS)
π is a measure over a *single walker's vertex position*; χ_η is a fluctuation property of an *η-field configuration measure*. The bridge — (H-NESS): the single-walker π's η-susceptibility tracks the η-field potential curvature sign — is the load-bearing, currently-unjustified link (the NESS analog of 0684's H-SUSC and the THEO-DSL H5 path-weight hypotheses). Honest framing: this is a genuine REDUCTION of the §14.17 gate (from "derive the whole action" to "lift π to the field measure / justify the single-site reduction, then one correlator"), NOT a discharge. Anti-erasure: refines 0684's "§14.17-gated, no shortcut" — the measure now exists; the residual is sharper and more local.

## Why setup, not a result
DG-3: no verdict language without review; and the discipline of the arc (scoping/setup before the number). A geometric proxy from π is producible but (H-NESS)-contingent — computing it and calling it sign(μ²) would overclaim (0684 §5.2). So: build + validate + map, stop before the sign.

## Anti-priorities held
No η-field; no susceptibility; no sign; no theorem ID; THEO-CHIR-CAPACITY-1 reserved/untouched; no header count change; no new programme-level Open-Problem (setup component of B-iii-(i)/0668, route re-cleared by TARROW-2); conditional on Mechanism A; the symmetric-vs-current split is the NESS susceptibility STRUCTURE, not a value; closed theorems (TARROW-1/2, VW-1/2, STATUS, BRIDGE, CAP, MERGE, THEO-DSL, CONT) + F.1/DSL/OPEN-SM-4/Capotauro consumed not edited; BRIDGE-1 P2 cap rides cross-sector statements; no verdict move (V3/W3). Changelog-style CHIR.md Last-updated append; no theorem-registry body-table row. Within the 0688–0699 chirality band, below the DM 0700 reserve.

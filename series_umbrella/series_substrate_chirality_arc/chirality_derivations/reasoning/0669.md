# Reasoning trail — Patch 0669 (B-ii magnitude anchors SCOPED; χ φ⁻¹-vs-φ⁻³ reconciliation RESOLVED)

**Patch:** 0669 (Session 151, 30 May 2026)
**Scope:** scope sketch + an embedded documentation/structural resolution; **NO new physics, NO theorem, NO verdict change**
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this Patch

With B-i closed (BRIDGE-1) and B-iii scoped (Patch 0668), B-ii was the remaining named bridge sub-target
and the handover's tractable alternative. The handover flagged it as "partly load-bearing already
(Δp_LR), but does not move the verdict." I took the reachable core: (a) decompose B-ii's load-bearing
status precisely, and (b) resolve the χ "φ⁻¹-vs-φ⁻³ normalization reconciliation" that BRIDGE-1 carried
as an honest flag + falsifier B4. I went looking for the actual tension and found it is not one.

## §2 The decomposition (P-face load-bearing, T-face deep)

B-ii has two anchors and they are at very different depths:
- **B-ii-P (Δp_LR = χ/6 = φ⁻³/6 ≈ 0.0394):** *derived from χ* (CAP-1, sub-claim (c) shipped), within ~2%
  of the ~0.04 anchor. This is the one already-load-bearing rung — BRIDGE-1's Prop:mag says exactly this.
  Essentially complete; only the W-bracelet Layer-4 EFT projection remains (SF-2 v2.0+), not a magnitude
  question.
- **B-ii-T (δ_CP ≈ 193–195°):** signpost only — "supports φ⁻³ but not used in the proof" (CHI-1).
  Deriving it from χ IS OPEN-SM-4 (a)/(b) = the deep engine = B-iii, behind §14.17. So the T-face
  magnitude is as deep as the capacity engine; B-ii cannot make it load-bearing without B-iii.
So B-ii is genuinely "partial now," exactly as the handover said.

## §3 The χ reconciliation — the substantive find

The flag reads "Capotauro's χ ≈ φ⁻¹ vs CHI-1's |χ| = φ⁻³." I checked every live source. χ = φ⁻³ is
unambiguous everywhere: FI-C-9, CHI-1 (rigorous, excludes alternatives), Capotauro v1.0 AND v2.0
(φ⁻³ throughout, feeding |M| = χ/6). The φ⁻¹ value is NOT a competing normalization — it is two things:
1. **A registered DEAD END** (Session 86, Findings C-1/C-2/C-3, in the Capotauro development record):
   C-1 geometric inconsistency (φ⁻¹ = the edge-length scale → would distort H₄; capotauro.tex line 529
   explicitly rejects |χ|=φ⁻¹ on this ground); C-2 empirical mismatch with the Δp_LR anchor; C-3 an
   arithmetic correction (a lost factor 1/φ took φ⁻²→φ⁻³). The φ⁻³ value, not φ⁻¹ or φ⁻², became FI-C-9.
2. **A DISTANCE scale.** φ⁻¹ is the 600-cell first-shell vertex distance. CHI-1 *uses it as input* and
   *builds* χ = φ⁻³ as the symmetric bias of shells (φ⁻¹, 1): (1−φ⁻¹)/(1+φ⁻¹) = φ⁻²/φ = φ⁻³. The dead
   conjecture's whole error was conflating the input distance with the output bias.
So φ⁻¹ → φ⁻³ is a derivation step, not a tension. **No live tension exists.** I machine-checked the
arithmetic (verify_bii_chi_normalization.py): φ⁻¹≠φ⁻³; (1−φ⁻¹)/(1+φ⁻¹)=φ⁻³ exactly; Δp_LR=φ⁻³/6≈0.0394
within 2%; and the dead φ⁻¹ value gives χ/6≈0.103 (158% off) — re-confirming C-1/C-2.

## §4 BRIDGE-1 falsifier B4 reclassified

B4 ("χ normalization irreconcilable, φ⁻¹ vs φ⁻³") is not triggered and cannot be triggered by the
*historical* φ⁻¹ (it is settled as superseded/distance). It survives only as a *forward hook on
sub-claim (b)*: if a future first-principles derivation of |χ| returned φ⁻¹/φ⁻² as the MAGNITUDE,
capotauro.tex itself says the perturbativity constraint would need revisiting. So I reclassified B4
in the sketch (resolved-as-documentation; retained as a sub-claim-(b) hook) but did NOT edit BRIDGE-1.tex
— it is review-closed and a non-substantive annotation does not justify touching it (DG-3, deferred,
candidate todolist item).

## §5 Root cause + the isolated cross-sector fix

The "Capotauro χ≈φ⁻¹" string traces NOT to the paper (φ⁻³) but to the stale OPEN-SM-4 one-line in
SM.md (line 28 "establishes χ ≈ φ⁻¹"; line 29 "derive χ = φ⁻¹") — March-2026 placeholders contradicted
by the SAME entry's "Current best lead" (φ⁻³ throughout). That is the documentation root cause. I routed
its correction to a SEPARATE companion patch (0670) rather than bundling a cross-sector registry edit
into this CHIR-arc patch: 0669 stands alone (my arc), 0670 is the labeled hygiene fix Thomas can apply
or hold. Anti-priority #6.

## §6 Anti-priorities held

NO new physics (χ=φ⁻³ consumed, not re-derived); NO δ_CP derivation (that's B-iii); NO verdict change
(B-ii is supporting evidence, not status-determining — FI-C-9 stays V3, sign(δ) stays W3); NO new
problem/conjecture/header change; NO edit to any review-closed theorem (BRIDGE-1 untouched); the one
cross-sector source edit isolated to companion patch 0670. BRIDGE-1's "kinematic only" + P2 tags kept
prominent.

## §7 Next

DG-2: companion Patch 0670 corrects the stale SM.md one-line. DG-3 (deferred): annotate BRIDGE-1 B4 at
its next maintenance bump (don't touch the review-closed theorem now). Remaining live B-ii depth (the
T-face δ_CP) is the deep engine B-iii, behind §14.17. With B-i closed, B-ii scoped (P-face load-bearing,
reconciliation resolved), and B-iii scoped (capacity ⟺ sign(μ²)), the bridge's three reachable faces are
mapped; the only verdict-moving work left is the §14.17-gated DSL dynamics.

# reasoning-0963 (verbatim, captured late — see 0942 header note; same provenance)

Route A's first step: does the corpus already pin the dynamical η's weights? Searched rather than derived — that was the whole point of the bounded session.

Every η construction in the sub-corpus — 0819, 0820, 0821 — builds the per-edge weight as `sign det[d̂, n̂, r̂₁, r̂₂]`. Unit magnitude. And the det-coset ℤ₂ *is* a sign structure, so that is the construction rather than a modelling convenience repeated in three scripts.

That matters because for unit weights **participation = support** exactly. And the floor argument already existed, in `lcapa_axis2_signcorr_closure.md` §5: a 4-D orientation is the sign of a 4×4 determinant, needing ≥ 4 independent directions. That is the source of CHIR.md's "per-vertex 4-D orientation floor" — the 4 is dimensional, not chosen.

**The gap I had to keep visible.** The dimensional argument gives a SUPPORT floor, not a PARTICIPATION floor, and they coincide only for unit weights. Full 12-edge support with 70% concentration gives p = 3.79 and fails. So everything turns on the weights genuinely being unit magnitude — a claim about the substrate, not a definition, since CAPACITY-1's class admits general weights. Wrote it up as three statements with that one flagged, rather than presenting a clean chain.

**Correction to my own 0961.** I had written that the weights "must be derived from the dynamics." They already were; they had simply never been stated in one place as a claim about the dynamical η. Recorded the correction rather than quietly re-aiming.

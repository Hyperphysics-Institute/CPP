# Reasoning fragment — Patch 0640 (THEO-CHIR-CAP-1; resolves audit E19 emergent via R1)

**Session 148, 29 May 2026. Per `templates/reasoning_capture_protocol.md`.**

## The 1c-β decisive test, run

The scope sketch (0639) decomposed E19 as ζ (registered involution) × σ_capture (sign), with
three live outcomes for the sign: R1 (= FI-C-9 enantiomorph → emergent), R2 (= σ_cycle → merge
E20), R3 (independent → register as primitive). The artifact required running the decisive test:
is the SD-CHIR chirality matrix-element SIGN fixed by n̂ alone, or does it need an extra input?

Read the SD-CHIR proof chain's sign bookkeeping (Capotauro_chiral_mechanism_candidate.md). Three
links settle it:
1. The matrix-element sign is carried by the edge-perturbation pattern (C_χ ∈ B₂ picks up the
   B₂ projection of the edge perturbation; the E-irrep content is the bias carrier, lines 706-712).
2. The edge-perturbation sign is the n̂ sign: δ(e) = ε(ê·n̂) is odd under n̂→−n̂. VERIFIED
   numerically (verify_capture_involution.py): over all 720 edges the field reverses exactly,
   max|δ| = φ⁻³ ≈ 0.236. (The 30 first-shell↔first-shell edges are tangent, ê·n̂=0 — the
   local-I_h-preservation theorem — so the bias lives on first→second-shell edges; sign still
   tracks n̂.)
3. The n̂ sign IS the FI-C-9 enantiomorph: "the sign of n̂ determines which enantiomorph is the
   actual substrate" (§2.1); "Operation B implies Operation A: any primitive direction picks one
   enantiomorph" (§Q2 closure).

So σ_capture = sign(n̂) = FI-C-9. There is NO separate sign degree of freedom. **Verdict R1.**

## What I concluded and what I refused to conclude

- **R1 confirmed → E19 emergent.** Both factors registered: ζ (n̂ + A2; + A1 charge-conjugation
  for ζ^qDP) and σ_capture (FI-C-9). No independent third primitive. This is a real reduction of
  the audit's deepest unregistered entry, and it retroactively grounds the ζ generators of three
  shipped theorems.
- **R3 refuted** — there is no independent sign DOF (the matrix-element sign tracks n̂).
- **R2 LEFT OPEN, deliberately.** R1 identifies the SPATIAL capture handedness with the FI-C-9
  enantiomorph. E20's σ_cycle is the TEMPORAL cycle sign (A1+A4). Whether σ_cycle ALSO traces to
  sign(n̂)/FI-C-9 — the merge — is a SEPARATE question I did NOT prove. I wrote it as a plausible-
  but-unproven unifying hypothesis (one substrate enantiomorph fixing spatial capture + temporal
  cycle + n̂-orientation), with the PCD-ORIENTATION §5.3 cross-link flag standing. Resisting the
  urge to declare the merge is the discipline — R1 is about the spatial sign; the temporal sign's
  identity is not established by this work.

## Honesty caps held

- Layer 2/2.5 (structural reduction over registered objects), not a dynamical derivation.
- E19 → emergent (PROVISIONAL), not established: the reduction is Layer 2/2.5 AND it CONSUMES the
  FI-C-9 sign rather than deriving it.
- **FI-C-9 NOT eliminated.** The theorem uses the enantiomorph sign as input; deriving it is the
  symmetry-breaking dynamics 1d-β (deep deferred). Said so explicitly. E19's resolution is about
  "is the capture handedness a further primitive beyond n̂+FI-C-9" (no), not "derive FI-C-9".
- No-false-reduction discipline honored: every step terminates in a registered object (A1, A2,
  FI-C-9, FI-C-RC, registered theorem outputs). The one ingredient beyond pure geometry (qCP-sign
  flip for ζ^qDP) is A1 charge-conjugation — registered — and I checked it adds no 4th input.

## The local-I_h surprise (and why it's not a problem)

My first verify-script version checked the perturbation field on first-shell↔first-shell edges
and got identically zero. That's not a bug — it's the local-I_h-preservation theorem (Finding
C-W39): those edges are tangent to n̂. Fixed the script to check the full edge graph (the bias
lives on first→second-shell edges); oddness-under-n̂→−n̂ holds there and is non-trivial. The
tangency is now a separate confirmed check (C3b). Worth recording: the bias carrier is NOT the
nearest-shell edges (those are protected) but the first→second-shell edges.

## Programme state

This resolves the third and last of the three audit-downstream derivation targets:
E20 (PCD-ORIENTATION, emergent P), E21 (CHI-1, 1d-α closed), E19 (CAP-1, emergent P). With E19
emergent, the CHIR audit's three "deepest" open entries all have downstream resolutions at
Layer 2/2.5, each honestly capped (provisional; FI-C-9 + 1d-β + the E19/E20 merge remain).
Natural next: the CHIR-derivations documentation-suite consolidation (per the agreed sequencing).

## Registration

THEO-CHIR-CAP-1 registered via the changelog precedent; CHIR.md holds the sector record (E19 →
emergent, 1c/2d collapsed onto 1c). Audit .tex stays v1.1-frozen. Verify script all checks pass;
artifact compiles clean (3-pass, 6 pages, zero undefined refs).

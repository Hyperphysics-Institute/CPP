# Reasoning — Patch 0689 (Session 152)

**Artifacts:** NEW `sketches/chir_odelta3_curl_finding.md` (computation + finding) + NEW `code/verify_odelta3_kolmogorov_curl.py` (CHECK 1/2 PASS; CHECK 3 reports curl content). The first verdict-relevant computation of the deep engine (1d-β-ii / §14.17). NO theorem registered, NO verdict move (V3/W3 stand). Conditional on Mechanism A (axiom).

## What I did
Executed the first reachable gate from 0688 — the detailed-balance / curl content of the Mechanism-A rate field. Read the EXACT machinery first: the full Patch-0535 first-order curl script (the per-vertex current j(v)=Σ r(ê)ê is exactly O(δ¹), so O(δ²) cannot live there) and DSL §6 (the path-amplitude expansion A(p)=r₀ⁿ∏(1+δ ê·n̂), Theorem 6.1: O(δⁿ) current = closed paths with n perturbed steps, confined to the n-ball). Then computed the Kolmogorov cycle condition directly (the right object, D1 done properly).

## The order-counting correction (honest amendment to 0688)
0688 said the gate is O(δ²). It is NOT. The Kolmogorov log-ratio L(a→b)=log[(1+δc)/(1−δc)]=2(δc+δ³c³/3+…) is ODD in δ (forward/backward antisymmetry) ⇒ NO O(δ²) term; the first possible violation is O(δ³). Corrected in the finding (§1) and dictionary D4.

## The per-face formula (derived)
DB ⟺ Σ L vanishes around every face (faces generate the cycle space; 600-cell boundary = simply-connected 3-sphere). Per triangular face, oriented edge-projections satisfy a+b+c=0 (closed loop + uniform edge length 1/φ) ⇒ O(δ¹) vanishes for EVERY face; O(δ³) = (2δ³/3)(a³+b³+c³) = 2δ³·abc (via a+b+c=0 ⟹ a³+b³+c³=3abc). So per-face O(δ³) Kolmogorov content = the triple product of edge-projections.

## The computation (verify script, machine precision)
Built 600-cell (120 V, 720 E, 1200 triangular faces, degree 12 — all correct). n̂ vertex-aligned = (1,0,0,0). CHECK 2: max|a+b+c| = 2.2e-16 ≈ 0 (DB at O(δ¹)); identity a³+b³+c³=3abc verified 5.6e-16. **CHECK 3: max|abc|=0.25; 420 of 1200 faces have abc≠0 (values {1/8,1/4}); ALL 420 nonzero faces touch the 2nd shell** (consistent with first-shell cancellation: icosahedral faces all-⊥, host-side faces cancel by orientation+cubing). **DETAILED BALANCE VIOLATED at O(δ³).**

## Physical picture
The bias δ ê·n̂ is a single-step gradient (conservative ⇒ O(δ¹) cycle-vanishes), but the Kolmogorov condition lives in log r, NONLINEAR in ê·n̂; the δ³ term ∝(ê·n̂)³ is non-conservative ⇒ nonzero circulation. The substrate is reversible to 2nd order and develops a probability current (arrow) at 3rd order — the arrow is a nonlinear effect of the multiplicative rate bias, appearing only at the 2nd shell.

## Honest implications (framed, NO verdict move — DG-3)
- **T-side (clean positive):** a non-reversible process has a steady-state probability current = a time-asymmetry ⇒ the O(δ³) curl is a concrete derived candidate mechanism for the TARROW-1 arrow (W3 → derived-mechanism candidate). The arrow now has a computed source.
- **P-side (landscape-clarifying):** DB fails ⇒ the VW-2 detailed-balance route to RP does NOT extend to δ≠0 ⇒ μ²>0 no longer forced by this route ⇒ the V3→V1 route stays OPEN (the curl-free "V3-by-principle" branch is RULED OUT).
- **CAUTION (kept prominent):** does NOT prove RP fails or μ²<0. RP=OS reflection ≠ T-symmetry (VW-2 Θ_OS/P_det); DB is sufficient not necessary for RP; μ²<0 is now possible, not established (η-curvature sign = a further computation). **So V3/W3 STAND** — only the landscape changed (curl-free closure eliminated; live target = μ² sign).

## Why a finding, not a theorem
DG-3: no verdict language without review. This is a computed finding that rules out one branch and surfaces an arrow candidate; it does not itself move the verdict. No ID registered. The clean T-side arrow result is theorem-assembly-ready (review next); the P-side makes the μ²-sign computation the live verdict-mover.

## Anti-priorities held
No theorem ID; no verdict move (V3/W3); conditional on Mechanism A; D2/D3 forward-implication failure does NOT invert to ¬RP/μ²<0 (the §5 caution); computed at O(δ³) (single nonzero face already breaks reversibility ⇒ qualitatively order-stable); closed theorems (VW-1/2, STATUS, TARROW, BRIDGE, CAP) + F.1/DSL/OPEN-SM-4 consumed not edited. Changelog-style CHIR.md; no theorem-registry body-table row. Within 0688–0699 band, below DM 0700 reserve.

# Reasoning fragment — 0936 (chirality lane), 13 Sep 2026

STATUS: at-patch, verbatim (housekeeping and tool narration removed; no content added). Artifact: `review/0936_e1_cross_lane_response_c_w46_four_state.md`. Script: `code/0936_first_shell_antipode_check.py`.

---

The DM window hands over forward-queue item 1: "does a DP in the n̂-asymmetric substrate (F.1 Mechanism A, r = r₀(1 + δ ê·n̂)) have an energy depending on which way its dipole points along n̂ (case c) or only on where it sits (case b)? Extend C-W46 to the full 4-state representation; note that C-W46's |+, v⟩ is not a first-shell configuration (Theorem 5.1)."

D-7 first: resolve every symbol against the lane that wrote it. Mechanism A in F.1 is r(ê) = r₀(1 + δ ê·n̂), a DI-bit propagation-rate asymmetry. THEO-CHIR-MERGE-2 classified δ as P-even, T-odd — the arrow. An energy is T-even, so a first-order-in-δ energy term is forbidden; δ enters currents. Meanwhile 3528's ΔF_dress = −(12/φ)κ used κ = χ·E_DP — χ is Reading C's ε ≡ χ = φ⁻³, the spatial, P-odd, T-even perturbation. The DM lane's question names the wrong primitive: the operative object is Reading C / THEO-SD-CHIR-2, not Mechanism A. That also means this patch cannot touch Mechanism A's conditionality — it is a different sector.

Second: C-W46's state space is |±, ±v⟩ — qCP sign at host × Linear-ZBW extra along ±v, v a first-shell vertex direction. Is −v the icosahedral antipode or the host-centred inversion? The first-shell icosahedron around v_host lies in the hyperplane x·v_host = φ/2, centred at c = (φ/2) n̂; its antipodal pairs are (v, φn̂ − v) — the ζ^W form of §17.3. Both members are first-shell vertices, both at û·n̂ = −1/(2φ). The host-centred inversion 2v_host − v has norm² 5 − 2φ ≠ 1: not a vertex. §20.2 says "host-CP-centered spatial inversion (v → −v)" (R2), but §19.4/§20.4 build on the antipodal-pair D_{5d} stabilizer, which only exists for the icosahedral antipode (R1). The wording and the machinery disagree.

So 3533 §2 ("|+, v⟩ is not first-shell") is right under R2 and wrong under R1; and the DM lane's "inward vs outward" is not a distinction between two first-shell placements at all — every first-shell placement is inward at −1/(2φ).

Third: the dipole of a bound extra is slaved to the host sign: opposite end nearer the host, so d̂ = s û. Then case (c) energy M s (û·n̂) and case (b) energy M (û·n̂) are the C-odd and C-even first-order forms. Build the four-state table under R1 and R2 and compute three splits: C-W46's doublet E(+,v) − E(−,v′), the complementary pair E(+,v′) − E(−,v), and the physical pair E(+,v) − E(−,v).

Before running it I expected (c) to give a nonzero doublet under both readings. The script says otherwise: under R1, (b) vanishes in every pair (all four entries equal — the I_h-preservation of C-W39 in action) and (c) gives −1/φ in every pair; under R2, (b) gives −1/φ in the doublet and +1/φ in the complementary pair and zero in the physical pair, while (c) gives zero in the doublet. So C-W46's nonzero doublet element — its one datum — selects (c) under R1 and (b) under R2. 3533's "the doublet cannot decide" is true only while the reading is open; each reading decides. That is the whole result.

If C-W46's element is a diagonal split in the configuration basis (both lanes have assumed this; C-W46 never states Ĉ's configuration-basis form — flag it), then under R1: case (c), + host lowered in every pair, |+, v′⟩ lowered by the same amount |−, v′⟩ is raised, S = +qCP; the absolute sign is FI-C-9's convention, fixed only by the corrected §10 phenomenology, as 3528 said.

Consistency worry against C-W46 §20.5: it assigns Ĉ^qDP ∈ A_{2u}(D_{5d}), odd under the C₂ dihedrals. Under R1 the C₂ is a proper rotation of the preserved local I_h fixing n̂ and s, mapping |s,v⟩ → |s,v′⟩; a diagonal first-order energy is then C₂-even, and a pseudoscalar branches A_u(I_h) → A_{1u}(D_{5d}). A_{2u} is how the coordinate along the pair axis transforms — a polar component. Either the irrep or the doublet labels need one pass. D-1: this is not a declaration that THEO-SD-CHIR-2 fails — χ/6 is reproduced independently in the K3 and W sectors — it is an owed consistency pass, registered as a flag, not a verdict.

Governance: the 09xx lane is not in id_block_registry.md and next_id.py's regex only sees "Patch NNNN"; the lane's commits are bare-numbered ("0935 EVAL …"). Add the block, add a leading-zero bare-number pattern (years never start with 0), next free 0937. D-8: no session close this turn.

# Reasoning fragment — Patch 0638 (THEO-CHIR-CHI-1; closes OPEN-CHIR-1d sub-gap 1d-α)

**Session 148, 29 May 2026. Per `templates/reasoning_capture_protocol.md`.**

## The exploration succeeded — and the result is clean

The 1d-α task was: enumerate the 600-cell distance-pair symmetric biases and test whether the
edge-to-first-non-edge pair (giving φ⁻³) is uniquely selected, OR honestly show it isn't. The
exploration (`verify_chi_phi3_ratio.py`) found a clean selection criterion, so per the
pre-authorized success branch, THEO-CHIR-CHI-1 ships.

**Distance spectrum (R=1):** 8 distinct shells — φ⁻¹ (12, icosahedron / vertex figure), 1 (20,
dodecahedron), √(2−φ⁻¹) (12), √2 (30), φ (12), √3 (20), √(2+φ) (12), 2 (1, antipode).

**Key finding:** among all 28 distance-pair biases, the two-nearest pair (φ⁻¹, 1) is:
- the UNIQUE adjacent pair giving φ⁻³ (the seven adjacent biases are 0.236, 0.081, 0.092,
  0.067, 0.034, 0.047, 0.025 — only the first is φ⁻³);
- the MAXIMAL adjacent bias (0.236 vs next 0.092);
- and the two-shortest-distance pair.
The alternatives are non-local: 1/√5 = (edge, φ-shell) skips three shells; 5−2√5 = (edge,
antipode) is the global extreme. So a LOCALITY criterion (symmetric bias of the two nearest
shells = the icosahedron+dodecahedron vertex-figure neighborhood where n̂ breaks H₄→I_h)
uniquely selects φ⁻³ and excludes the alternatives. That answers "why −3" by answering "why
the two-nearest pair."

## The honest layer and the two residual freedoms

I shipped this at Layer 2/2.5 with two explicitly-stated residual freedoms, because the result
is real but bounded:

1. **The symmetric-bias FORM is assumed, not derived** (§4 of the artifact). 1d-α selects the
   ratio WITHIN the symmetric-bias family; whether the chirality magnitude must take the form
   (d_j−d_i)/(d_j+d_i) of a distance pair — vs some other functional of the spectrum — is a
   structural assumption, recorded as falsifier F2, not closed. I resisted the temptation to
   present the locality argument as a full derivation of φ⁻³; it is a ratio-SELECTION within a
   stated form.
2. **1d-β (dynamics / FI-C-9) is untouched.** The value remains a foundational input until the
   symmetry-breaking dynamics are derived. The theorem strengthens E21 from "value computed,
   ratio unjustified" to "value selected by a locality criterion"; it does NOT eliminate FI-C-9.

So E21 stays emergent (provisional): 1d-α closed at Layer 2/2.5, 1d-β open. This is the honest
analog of the E20 economy — there, firm count / provisional magnitude; here, ratio-selected /
form + dynamics owed.

## Why I'm confident the locality criterion is well-motivated (not ad hoc)

The two nearest shells are not arbitrary: shell 1 (12 vertices) is the icosahedral vertex
figure of the 600-cell, shell 2 (20 vertices) the dual dodecahedron, and together they ARE the
local neighborhood at the host vertex where vertex-aligned n̂ breaks H₄→H₃=I_h (Capotauro
Reading C). A chirality magnitude is a local property at that symmetry-breaking locus, so the
bias of the two nearest shells is the natural local imbalance. The alternatives pair the edge
with distant shells (φ-shell, antipode) — global, not local. The geometric content is solid;
I stated it as the criterion's motivation, not hand-waved it.

## Signposts kept as signposts

δ_CP ≈ 193.3° (vs NuFIT 195°±40°) and Δp_LR = φ⁻³/6 ≈ 0.0394 (vs ~0.04) support φ⁻³ but are
NOT used in the theorem's proof (Finding C-5 discipline + AI_team_expectations §1.3). The proof
rests only on the enumeration.

## Verification

`verify_chi_phi3_ratio.py` asserts: 120 unit vertices; 8 distinct distances; two nearest =
φ⁻¹×12 + 1×20; bias = φ⁻³ at machine precision; (φ⁻¹,1) the unique adjacent φ⁻³ pair and the
max adjacent bias; 1/√5 and 5−2√5 produced only by non-adjacent edge-pairings. ALL CHECKS PASS.
Artifact compiles clean (3-pass, 5 pages, zero undefined refs).

## Registration

THEO-CHIR-CHI-1 registered via the changelog precedent (audit/PCD-ORIENTATION pattern); CHIR.md
holds the sector record (1d-α RESOLVED at Layer 2.5; 1d-β open). Audit .tex stays v1.1-frozen;
the CONT-1.3→Finding-C-3 correction stays in the scope sketch + CHIR.md + this artifact.

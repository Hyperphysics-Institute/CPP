# Reasoning — Patch 0688 (Session 152)

**Artifact:** NEW `sketches/chir_open_1d_beta_ii_deep_engine_scoping.md` — decomposes the deep capacity engine (1d-β-ii / F.1 §14.17 / OPEN-SM-4 (a)/(b)) one level deeper and locates its first reachable gate. NO verify script (pure scoping; the structural facts cited — Patch 0535's curl result, VW-1/2, the F.1 rate law, OPEN-SM-4 — are consumed from existing work, not newly computed; 0679/0683 precedent). NO new theorem ID; THEO-CHIR-CAPACITY-1 untouched. NO verdict move (V3/W3 stand).

## Why / what
Thomas's strategic call: commit to the deep §14.17/F.2 engine (the sole V3→V1 lever, confirmed by both VW and SUSC terminating there). Grounded the sketch by reconnoitring the F.1 DSL paper + OPEN-SM-4/Capotauro state first. The 0652 decomposition stopped at "1d-β-ii = deep engine, deferred" and never opened it; this opens it.

## The two finds (load-bearing)
1. **The measure is not free — it's the Mechanism-A stationary measure.** F.1 Mechanism A is a *rate law* r(ê)=r₀(1+δ ê·n̂) — the transition rates of a Markov process on the 600-cell. SUSC's "missing measure" e^{−S} is that process's stationary distribution. So §14.17 is the stationary measure of a *specified* process, not an arbitrary action; "does the substrate break to a chiral phase (sign μ²)" = "does π's η-curvature go negative."
2. **The capacity bit reduces to the curl (Kolmogorov-cycle) content of the rate field, and that curl is ALREADY computed at first order — and vanishes.** `dynamical_substrate_law/code/verify_b1q2_curl_content.py` (Patch 0535) established the discrete curl of jₙₑₜ vanishes identically at O(δ¹) (zero circulation on all 30 first-shell side faces, I_h-spanning to the full 4D curl 2-form). Curl-free ⇒ conservative rate field ⇒ detailed balance at δ≠0 to first order ⇒ (VW-2 Thm A extends) RP ⇒ (VW-b vectorial + VW-c) μ²>0 at leading order. **This is the leading-order case of VW-a-4's reachable sufficient condition — already computed, never connected to the verdict.** The capacity (P-side) and the arrow (T-side, TARROW-1) are the *same* object: the curl of the Mechanism-A current — the CPT unification made computational.

## The reachable first gate
sign(μ²) is the η²-curvature; near the symmetric point η~δ, so the instability is an O(δ²) property. The O(δ¹) curl-free result is leading-order only; the verdict-determining order is O(δ²) — exactly the deferred OPEN-FP-F1-1 / op:delta-squared (the second-shell, 20-vertex dodecahedral extension; F.1 calls it "substantive but methodologically analogous"). So the **first reachable gate = the O(δ²) curl computation**, a bounded second-shell 600-cell calculation, the next-order replay of Patch 0535. Outcome map: curl=0 ⇒ μ²>0 ⇒ V3-by-principle (capacity settled, spontaneous branch closed); curl≠0 ⇒ probability current ⇒ substrate arrow + μ²<0 possible ⇒ V3→V1 opens. Either branch is verdict-relevant.

## Dictionary to nail (D1–D4, stated not assumed)
D1 (curl ⟺ Kolmogorov-cycle ⟺ detailed balance; tie the current-curl to the rate-field cycle products); D2 (detailed balance ⟹ RP at δ≠0, VW-2 Thm A extension, carrying Θ_OS/P_det); D3 (RP ⟹ μ²>0, VW-1 no-go, VW-b vectorial + VW-c); D4 (order-counting: μ² ↔ O(δ²)). These are the links the computation patch must establish.

## Cross-sector half (OPEN-SM-4) — distinct from the capacity front
(b) magnitude = Reading-C edge law ℓ=ℓ₀(1+ε ê·n̂), the P-face twin of Mechanism A's temporal rate law (candidate registered); (a) sign-selection nucleation = cosmological (the V→fixed / W1 slot); (c) |M|=χ/6 SHIPPED (THEO-CAP-1). The EWSB identification (CONJ-CHIR-1) is the V1→SM layer, behind BRIDGE-1's P2 cap, not on the capacity critical path.

## Verdict
The deep engine is MORE reachable than "behind §14.17" implied: not a free construction, but the curl content of a specified measure. Ordered path: (1 DONE, Patch 0535) O(δ¹) curl-free; (2 FIRST GATE, reachable) O(δ²) curl (= op:delta-squared, second-shell); (3) settles V3-by-principle or opens V3→V1 + the arrow. Prerequisite: the second-shell inner-product/edge-projection identities (geometric, bounded). **DG → Patch 0689: open the O(δ²) curl computation** (the first verdict-moving computation in the arc; carries a verify script). Per Thomas; within the 0688–0699 band, below the DM 0700 reserve.

## Anti-priorities held
No measure built; no μ² computed; no O(δ²) curl computed (that's 0689); conditional on Mechanism A (axiom) + the D1–D4 dictionary; RP=unitarity≠T-symmetry (curl≠0 lifts the no-go, does not prove μ²<0); no verdict move (V3/W3); no new ID / no header count change; closed theorems + F.1/OPEN-SM-4/Capotauro consumed not edited; BRIDGE-1 P2 cap rides cross-sector statements. Changelog-style CHIR.md; no theorem-registry body-table row.

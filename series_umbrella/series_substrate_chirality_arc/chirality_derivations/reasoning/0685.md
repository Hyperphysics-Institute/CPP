# Reasoning — Patch 0685 (Session 152)  [re-cut of the former 0684; ceded 0684 to SUSC]

**Artifacts:** NEW `theo_chir_vw_2.tex` (v1.0, clean three-pass, 4pp, zero undefined refs) + NEW verify `code/verify_vw_2_delta0_rp_reduction.py` (CHECK 1/2/3 PASS) + reasoning `reasoning/0685.md` + CHIR.md registration. Registers **THEO-CHIR-VW-2** (THEO-CHIR-CAPACITY-1 stays reserved). Layer-2.5 structural theorem. NO verdict move (V3/W3 stand).

## Renumber note (multi-window coordination)
Originally cut as 0684; ceded 0684 to a parallel window's **SUSC** patch (the chiral-susceptibility computation backstop) at Thomas's instruction, and re-cut on top of it as 0685 (review package follows as 0686). The tetra-gravity 0672a/0672b commits that landed on main meanwhile left all chirality files untouched (verified), and SUSC-0684 applies cleanly under them; this re-cut chains on (0672b + SUSC-0684). History is linear and intact throughout — no force-push, no divergence.

## The two results
- **Thm A (UNCONDITIONAL given THEO-DSL-3):** at δ=0 the substrate is H₄-symmetric with outgoing–incoming detailed balance (THEO-DSL-3, vanishing net current); a reversible (detailed-balance) measure is reflection-positive (self-adjoint on L²(π) ⇒ positive transfer operator T=e^{−τH} ⇒ ⟨θ(Ā)A⟩=⟨A,TA⟩≥0). So **H1 holds at δ=0** — the unconditional nugget; H1 for the full measure becomes a persistence-under-δ-perturbation question.
- **Thm B (Layer 2.5):** OS split S = S₊ + θS₊ + S_I, RP ⟺ e^{−S_I} positive. Given VW-a-1 (θ a lattice symmetry — VW-b), VW-a-2 (measure real — first pass), VW-a-3 (action θ-symmetric on its I_h-equivariant geometric part — THEO-DSL-4; dynamical part assumed/§14.17-gated), all reflection-asymmetry is confined to S_I ⇒ **H1 ⟺ VW-a-4**, δ=0 base case by Thm A; VW-a-5 (RP under the Φ block-spin flow) the continuum lemma.

## The coupling (honest)
VW-a-4's reachable candidate sufficient condition: δ≠0 detailed-balance persistence ⇒ positive transfer operator ⇒ RP ⇒ μ²>0 ⇒ V3 by principle. Couples VW-a-4 to TARROW-1 (reversible→RP free; irreversible→guarantee removed). **Load-bearing caution:** RP encodes *unitarity*, not T-symmetry — failure of detailed balance does NOT establish RP-violation, only removes the cheap route. The δ≠0 reversibility/arrow status is the unbuilt object (§14.17/F.2; W3) — the same gate as the arrow, P-side and T-side (VW-1/TARROW-1 CPT unification).

## Convergence with SUSC (Patch 0684, parallel window)
SUSC-0684 independently set up the χ_η susceptibility route and found sign(μ²)=sign(χ_η⁻¹) lives in the §14.17-gated mass/η² channel (the THEO-DSL pipeline reaches the vector-current and kinetic channels but not the even-potential curvature; a connected correlator needs the measure). Its convergence finding — **VW-a-4 (principle) and SUSC-sign (computation) are the SAME §14.17 object reached by two independent routes** — is exactly the structural (not VW-framing-artifact) reading VW-2 supports: the capacity gate is real. VW-2 (the registered theorem marking the VW branch's reachable boundary) + SUSC-0684 (the route map declaring reachable capacity work complete) together close out the reachable sign(μ²) work.

## Verify script
CHECK 1 (numerical): a reversible weighted-graph random walk (P=W/deg, π=deg/Z) has exact detailed balance (~1e-17), self-adjoint symmetrized operator, real spectrum, PSD generator, positive transfer operator — the Thm A engine; a non-reversible contrast breaks self-adjointness (= what VW-a-4 risks). [Kernel-construction bug caught + fixed at build: a Metropolis build + row-renormalization broke detailed balance; replaced with the weighted-graph walk.] CHECK 2 (structural): the OS ingredient partition + H1⟺VW-a-4 reduction + boundary marking. CHECK 3: no verdict move.

## Anti-priorities held
No H1 proof (VW-a-4 OPEN); no sign(μ²); no claim the δ≠0 dynamics are reversible/irreversible; RP≠T-symmetry explicit; no verdict move (V3/W3); THEO-CHIR-CAPACITY-1 untouched; closed theorems + DSL/F.1/OPEN-SM-4 consumed not edited; BRIDGE-1 kinematic/P2 cap rides downstream. Changelog-style CHIR.md; NO theorem-registry body-table row.

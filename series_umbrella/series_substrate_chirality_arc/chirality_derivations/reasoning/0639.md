# Reasoning fragment — Patch 0639 (OPEN-CHIR-1c/2d / E19 capture handedness scope sketch)

**Session 148, 29 May 2026. Per `templates/reasoning_capture_protocol.md`.**

## Why this one needed the no-false-reduction discipline up front

E19 is the deepest unregistered entry — "the load-bearing dynamical chirality source,"
consumed by every SD-CHIR closure. That makes it the entry where an eager reduction does the
most damage: a spurious derivation would silently relabel a load-bearing input (the ζ
generators of three shipped theorems) as a result. So I set the firewall (§0) to require every
reduction step to terminate in a registered object, and to keep BOTH terminal outcomes live —
"emergent" and "genuine primitive, register it." The sketch's job is an honest map, not a
closure.

## The structural insight that organizes everything: involution × sign

Reading the audit's existing-derivation pass, the decisive datum is that the capture handedness
AS CONSUMED is the SD-CHIR ζ generator: ζ^W = p ↦ φn̂ − p (icosahedral-center inversion),
ζ^qDP = host-CP inversion + n̂-flip + qCP-sign flip. These are built from registered objects
(n̂, A2, charge-conjugation). But ζ is an INVOLUTION (ζ²=1) — it relates a config to its
n̂-flipped partner without preferring one. A handedness is a SIGNED datum; an involution is not.
So:

  capture handedness (E19) = ζ (registered-geometric involution) × σ_capture (the sign).

This is EXACTLY parallel to E20's ω_PCD = σ_cycle·n̂ (registered axis × sign). That parallel is
the cross-link I flagged in THEO-CHIR-PCD-ORIENTATION-1, now made structural. It splits E19
into 1c-α (the involution — registered, reachable) and 1c-β (the sign — the crux).

## 1c-α answers ChatGPT's objection; 1c-β is where the real question lives

ChatGPT calibrated Grok's E19 seed (sgn(n̂·(disp×pol))) down because disp/pol/cross-product/
capture-rule were "not shown to be in the framework." The registered fix: replace disp×pol with
v₁×v₂ for two specific first-shell 600-cell vertices (registered via A2), making the pseudoscalar
a registered construction, and identify it with the ζ-parity bookkeeping SD-CHIR already uses.
That's 1c-α — reachable, Layer 2/2.5, reserve THEO-CHIR-CAP-1.

1c-β (the sign) has three candidate resolutions I deliberately kept distinct rather than picking:
- **R1 (unifying):** σ_capture = the FI-C-9 frozen enantiomorph. ζ^W flips n̂; which of {n̂,−n̂}
  is physical IS the FI-C-9 substrate-vacuum chirality sign (Capotauro's "frozen boundary
  condition coeval with CPs"). Under R1, E19 is emergent from {ζ registered, FI-C-9 registered},
  and ONE substrate sign (FI-C-9) sources E19, E20/σ_cycle, and the n̂-orientation alike. Elegant.
- **R2 (merge):** σ_capture = σ_cycle (capture happens during the PCD cycle). Merges E19+E20.
  Compatible with R1 if σ_cycle itself traces to FI-C-9.
- **R3 (honest negative):** σ_capture is independent → E19 is a genuine primitive, register it.

The decisive test (answerable from the existing SD-CHIR proof chain): is the chirality-operator
matrix-element sign fixed by the n̂-orientation/FI-C-9 alone, or does it need an extra input? I
did NOT run that test this turn (it needs a read of the SD-CHIR ζ-parity sign bookkeeping); it's
the substance of the 0640+ artifact. Scoping it honestly without pre-judging the verdict is the
point.

## Why I'm confident R1 is the likely (but unproven) resolution

The FI-C-9 note explicitly says the chirality sign is "more primitive than any specific
dynamical event… a property of the substrate vacuum state itself." That is precisely an argument
that there is ONE chirality sign, not a per-process sign. ζ^W's linear part −I flips n̂, tying
its handedness to the n̂-vs-−n̂ (= FI-C-9) choice. So R1 is well-motivated. But "well-motivated"
is not "proven" — the matrix-element sign test must confirm it, and I flagged it as hypothesis
(R1), not result, throughout. Resisting the urge to write R1 as the conclusion is the discipline.

## What this does NOT do

- Does NOT close E19 (both 1c-α and 1c-β must resolve; the sign test is unrun).
- Does NOT eliminate FI-C-9 (R1 CONSUMES the FI-C-9 sign; deriving it is 1d-β, deep deferred).
- Does NOT assert the E19+E20 merge (kept as hypothesis R2; the PCD-ORIENTATION cross-link
  stays flagged-not-resolved).
- Does NOT manufacture a reduction: R3 (register E19 as a primitive) is kept as a real outcome.

## Registration

Scope sketch only; no theorem-registry proved-row. THEO-CHIR-CAP-1 reserved for 1c-α (+ the
1c-β verdict). CHIR.md OPEN-CHIR-1c/2d updated with the decomposition and the R1/R2/R3 outcomes.
Audit .tex stays v1.1-frozen.

# Reasoning trail — Patch 0648 (THEO-CHIR-MERGE-2 multi-AI review cycle opened)

**Patch:** 0648 (Session 149, 29 May 2026)
**Type:** workflow-only (review-cycle opening) — NO physics artifact, NO theorem registration, NO new axiom
**Target under review:** THEO-CHIR-MERGE-2 (Patch 0647)
**Capture mode:** verbatim at-patch (`STATUS:verbatim`)

---

## §1 Why this patch

THEO-CHIR-MERGE-2 (Patch 0647) advances a *registered verdict* — it moves THEO-CHIR-MERGE-1's
MERGE-β from M3 to M1-χ. Per the recommendation at 0647 close and the programme discipline
(`operating_system.md` §17 review protocol; the THEO-CHIR-AUDIT-1 precedent of opening a review
cycle for a result that changes the registered picture), a single-pass Layer-2.5 theorem that
advances a prior theorem's verdict should go to the multi-AI team before the qualifier "single-pass /
review pending" is removed. This patch issues the self-contained review-request package.

## §2 The load-bearing thing to test: G2 (δ T-odd)

The entire decomposition hinges on two (P,T) assignments, of which **δ T-odd** is the crux. The
argument: under T, "DI-bit propagates along ê" ↦ "along −ê," so the T-image rate function is r(−ê),
giving δ ↦ −δ. If a reviewer can mount a defensible P,T-covariant argument that δ is T-EVEN (e.g.
the rate asymmetry is a static substrate property, not an arrow-like flux), then sign(δ) becomes
P-even/T-even — a true scalar — and the clean "arrow vs chirality" split collapses, reverting
MERGE-β toward M3. So the package marks G2 as the falsifier to press hardest, assigns it to ChatGPT
(detail catches) and Grok (independent first-principles parity assignment) and the optional Sonnet
hostile pass, and makes it triage-priority 1. Secondary: G1 (ω_PCD axial/T-odd), G5 (the polar→axial
conversion factor in relation ★), G3 (a second primitive pseudoscalar beyond FI-C-9).

## §3 Structure (modeled on the AUDIT-1 package, Patch 0633)

- §0 IS/IS-NOT (Layer-2.5 provisional decomposition; NOT full M1, NOT a FI-C-9 derivation, NOT the
  F.2 physicalization, NOT a Mechanism-A derivation, NOT a flagship pause).
- §1 premises consumed (MERGE-1 relation ★; ω_PCD axial; CAP-1 ζ×FI-C-9; FI-C-9 identification; MA.1).
- §2 the argument inline (the (P,T) table + decomposition + three consequences + honest caps).
- §3 the verification inline (CHECK 1 achirality, CHECK 2 parity table).
- §4 PD-002 tier labeling.
- §5 explicit asks mapped to G1–G5 (G2 press-hardest) + verdict-honesty (M1-χ over/under-claim;
  the category-mismatch correction).
- §6 reviewer-specific framing (Copilot structural; Grok recompute + first-principles parities;
  ChatGPT G2 + category-mismatch + verdict; optional Sonnet hostile). Includes the ChatGPT
  disambiguation (this is MERGE-2 chirality parity, NOT a nuclear OPEN-SS audit, NOT AUDIT-1, NOT a
  prior DSL review) + the inline-content rule (engage inline, don't reconstruct from memory).
- §7 submission/triage (responses → reviews-CHIR-MERGE-2.md; triage G2 > G1/G5 > G3 > verdict-cal >
  confirmations; AI_team_expectations §1.3 plausibility-sketch-is-not-derivation).
- §8 reference documents.

## §4 Registry actions

- NEW `chirality_derivations/review/theo_chir_merge_2_review_package_v1.0.md` (new `review/` subfolder
  under chirality_derivations, paralleling chirality_audit/review/).
- MOD `frontier_sectors/CHIR.md`: OPEN-CHIR-MERGE — review cycle OPENED (Patch 0648); the
  "single-pass / review pending" qualifier on MERGE-2 is removed only on cycle completion.
- MOD `theorem-registry.md`: Patch 0648 changelog (workflow-only); 0647 demoted.
- Anti-priorities: MERGE-2 artifact body UNMODIFIED; no THEO entry changed; no new axiom; the package
  is the immutable v1.0 request record (reviewer responses + synthesis go in the separate reviews file).

## §5 Honest note

Opening a review cycle is not closing it. MERGE-2 stays Layer-2.5 provisional with its qualifier
intact until reviewers converge. If G2 flips (δ T-even), MERGE-2 goes to v1.1 reporting back to
M3-structural — that is the honest outcome the package explicitly invites, not a failure mode to avoid.

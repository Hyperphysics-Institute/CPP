# Reasoning — Patch 0691 (Session 152)

**Artifact:** NEW `review/theo_chir_tarrow_2_review_package_v1.0.md` — opens the multi-AI review cycle for THEO-CHIR-TARROW-2 (Patch 0690). Self-contained (theorem claims inline + verify script embedded in full + triage battery). NO theorem edit, NO verdict move (V3/W3 stand; the W3→W1 move is claimed only at review-close, DG-3).

## What / why
Per the VW-1/VW-2/TARROW-1 pattern: deliver theorem → open review package → 3 reviews → synthesize + close. This opens the TARROW-2 review. Verified self-containment by extracting the embedded python block and running it (420/1200, all checks pass, no stderr).

## Triage design (Q1 foregrounded — the existential risk)
- **Q1 (DEEPEST):** the capacity-bar inference. TARROW-1's 𝒞_T=No was "no DERIVED mechanism." TARROW-2 derives one CONDITIONAL ON MECHANISM A (itself an axiom, OPEN-FP-F1-2). Does "a mechanism conditional on an undischarged axiom" meet 𝒞_T=Yes (⇒ W1), or merely RELOCATE the No to Mechanism-A-underived (⇒ W1-conditional, or W3-pending-MA)? Apply the STATUS-1 falsifiable-constraint calibration. Is "candidate W3→W1, conditional on Mechanism A" the right registration language? — most likely restatement driver (the Proposition).
- **Q2:** current ⟹ time-asymmetry (real arrow? direction tied to sign(δ) ⇒ W1 not W0?).
- **Q3:** Lemma B rigor — do the 1200 faces generate the 601-dim cycle space (E−V+1=720−120+1)? rank-check (Grok).
- **Q4:** order-stability (one nonzero face ⇒ non-reversible; higher odd orders can't rescue).
- **Q5:** TARROW-1 non-conflict — likely calibration: the current is better framed as the DYNAMICAL REALIZATION of the unique T-odd object sign(δ), not "a new dynamical T-odd object" (strengthens the non-conflict).
- **Q6:** run the embedded script; recompute abc per face.
- **Q7:** caps / no overclaim / no closed W-move.

Reviewer-specific: ChatGPT → Q1 + overclaim sweep + adjudicate registration language; Grok → SCRIPT-EXECUTED + Q3 rank-check + identities; Copilot → Q5 + Lemma-B chain structural consistency.

## Honest posture
Foregrounded Q1 (the derivation-vs-axiom capacity bar) as the existential risk and Q5 (the TARROW-1 framing) as a likely calibration, rather than presenting the theorem as airtight — same integrity stance as the VW-2 package (which foregrounded its Q1 and indeed drew a restatement). The §8 CONFIRM/RESTATEMENT/FALSIFIER criteria make the decision space explicit. Adjudication at close will be on merits, not headcount (the VW-2 precedent).

## Anti-priorities held
No theorem edit; no verdict move (V3/W3 stand; W3→W1 claimed only at close); self-contained package (verify embedded + run-checked); THEO-CHIR-CAPACITY-1 reserved/untouched; closed theorems consumed not edited. Changelog-style CHIR.md. Within 0688–0699 band, below DM 0700 reserve.

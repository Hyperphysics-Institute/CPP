# Reasoning — Patch 0681 (Session 152)

**Artifact:** `chirality_derivations/review/theo_chir_vw_1_review_package_v1.0.md` (multi-AI review-request package for THEO-CHIR-VW-1, Patch 0680). Produced via the canonical "initiate review protocol" command (templates/review_dispatch_protocol.md, codified Patch 0660).
**Type:** workflow-only. NO physics artifact, NO theorem registration, NO new axiom; the THEO-CHIR-VW-1 body (theo_chir_vw_1.tex v1.0) is UNMODIFIED. The package is the immutable v1.0 request record; reviewer responses will aggregate in `review/reviews-CHIR-VW.md` at close.

## Why a cycle now

Per DG-3, VW-1 is the first artifact in the programme that could eventually move V3 off "not yet derived" (via branch (A) V3-by-principle, conditional on H1), so the honesty bar is maximal: it registered single-pass and goes to the panel before the "single-pass" qualifier is removed (the AUDIT-1 / MERGE-2 / STATUS / TARROW / BRIDGE precedent).

## Triage design (the load-bearing choices)

- **Q1 (H1 honesty / no-verdict-move)** foregrounded: the existential check — does any step smuggle a claim that reflection positivity H1 holds (a false positivity proof / illegitimate verdict move)? Is VW-d genuinely conditional? This is the BRIDGE-1-Q1 analog.
- **Q2 (Vafa–Witten transposition legitimacy)** foregrounded as the deepest risk: VW is a vector-like *gauge*-theory theorem; is its transposition to the CPP substrate/DSL valid, or are there hypotheses (gauge structure, fermion-bilinear order parameter, the specific positivity) that don't transpose? If invalid, the whole route is void. ChatGPT pressed hardest here.
- **Q3 (unification vs conflation)**: does Thm 5.1 genuinely share a root between V2-exclusion (value) and the VW no-go (capacity) without conflating the two distinct bits? (DG-4.)
- Then Q4 (group theory — Grok recompute), Q5 (evasion audit), Q6 (spontaneous/explicit), Q7 (reduction fairness), Q8 (overclaim).

## Process discipline carried

Verify code EMBEDDED in full in §7 (the Patch-0656 process-learning: reviewers reach SCRIPT-EXECUTED only with inline code). Self-contained (all context + claims + questions + code inline). Disambiguation + inline-content rule appended for ChatGPT. Default panel ChatGPT/Grok/Copilot + optional Sonnet hostile pass. NO verdict move (V3/W3 stand) — the cycle decides whether the conditional position holds, not whether the verdict changes.

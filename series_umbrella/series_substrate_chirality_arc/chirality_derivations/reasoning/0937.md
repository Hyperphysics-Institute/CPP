# 0937 — reasoning fragment (verbatim, at-patch)

**Lane:** chirality (09xx). **Patch:** 0937, 13 Sep 2026. **Artifact:** `review/0937_c_w46_flags_f1_f3_closure.md`. **Verify:** `code/0937_d5d_extended_group_bookkeeping.py` (14/14).

Booted into the lane with 0936 unpushed (D-5 caught it; founder applied and pushed before this patch; clone reset to the pushed 0936 as base). Read 0936 in full and C-W46 at source (§17.4, §20.1–§20.7).

The 0936 flags were three questions about one theorem. Rather than argue readings I built the objects: the 120 vertices of the unit 600-cell, the host at n̂, the 12-vertex first shell, then the local point group as the set of orthogonal maps of the ⟂ 3-space that permute the shell (120 — I_h), its pair-stabiliser (20, class sizes of D₅d), and the four 1-D characters constructed from the elements themselves (g/u by det, 1/2 by the sign on the five perpendicular C₂). Pseudoscalar restriction: det → A₁u, not A₂u. C-W46's printed character is A₂u (−1 on proper C₂) — that is the character of a polar coordinate on the pair axis; the slip is "sign-flipping on the C₂ dihedrals (pseudoscalar)".

Then the extended group D₅d × Z₂^C on the four states and the projectors: A₁g⁺ ⊕ A₁g⁻ ⊕ A₂u⁺ ⊕ A₂u⁻ each once; Ψ⁽¹⁾ = (A₁g⁺ + A₂u⁻)/√2, Ψ⁽²⁾ = (A₁g⁻ + A₂u⁺)/√2. The decisive step was the operator projectors: the 16-dim operator space has zero A₁u and zero A₂g content. So no pseudoscalar of the geometric group has any matrix element on the pair at all — the D₅d refinement of §20.1 does not by itself do what it was introduced to do. The doublet element survives only for A₁g⁻ (the charge sign ŝ) and A₂u⁺ (v-vs-v′). C-W46's literal "A₂u, ζ-odd" = A₂u⁻ gives 0 — the ζ Z₂ check passes it, the full group kills it.

Which of A₁g⁻ / A₂u⁺ is physical at O(ε): the preserved local I_h (C-W39) makes v and v′ equivalent, so nothing supplies A₂u⁺; ŝ is available and its coefficient is the c-number χ. Ĉ^qDP = χ·(1/6)·ŝ. That is case (c), and it is what §20.2 says in words ("qCP-sign × n̂ coupling") — D-1 satisfied: this confirms the theorem's content and corrects three labels; the χ/6 magnitude and the cage-shell factor are untouched.

F1 fell out of the same construction: the inversion of the local group fixes n̂ and is p ↦ φn̂ − p on the shell. The "n̂ ↦ −n̂" of Def. 20.2 is not a group element; it is the substrate mirror — the sign of χ.

Deliberately not done: pinning M to E_DP (that is the DM lane's E2 with E_coc; 3528's 1/φ per direction vs C-W46's χ/3 per DP differ by a normalisation that is not on file — flagged, not fixed). Not edited: `capotauro.tex` (shipped, 3/3-reviewed; corrigendum text is paste-ready in the review; recompile is the founder's action). Not touched: DMDE.md, the DM charter's pointers.

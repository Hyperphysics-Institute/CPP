# Reasoning fragment — Patch 0636 (THEO-CHIR-PCD-ORIENTATION-1 artifact)

**Session 148, 29 May 2026. Per `templates/reasoning_capture_protocol.md`.**

## What the theorem is, and the one decision that shaped it

THEO-CHIR-PCD-ORIENTATION-1 is the first downstream derivation theorem of the
audit-spawned OPEN-CHIR-* programme. It resolves audit E20 (the n̂↦ω_PCD link), left
unregistered(conditional) by THEO-CHIR-AUDIT-1 v1.1.

The central decision (carried from the Patch 0635 scope sketch): answer the
**primitive-count** question, not the magnitude question. The F.1 sub-question sketch
already derives, at viability level, ω_PCD = σ_cycle·ĵ^net = σ_cycle·n̂ (F.1 §11 Phase 1
net current ∥ n̂; §12 Phase 2 coupling rule). The temptation to "re-prove this at Layer 3"
is wrong on two counts: it duplicates F.1 Phase-2 foundations work (a separate trajectory
with three open commitments) and it overclaims (the F.1 result is Layer 2.5, not 3). The
narrow primitive-count question — "does ω_PCD introduce an independent primitive?" — is
answerable robustly: the decomposition shows ω_PCD is a product of two ALREADY-REGISTERED
primitives (n̂ = E16 spatial; σ_cycle = E2/E5/E17 temporal), so no third primitive,
Scenario B refuted, E20 emergent.

## Why emergent (provisional), not established — and the robustness argument

The theorem caps E20 at emergent(P), inheriting the F.1 viability ceiling. This is the
v1.1 grading instrument used correctly: E20 leaves "unregistered" (there is now a
registered reduction with no second primitive apparent) but does not reach "established"
(the reduction rests on a Layer-2.5 result). The load-bearing move is §6's robustness
table: the three F.1 open commitments (local algebraic representation ansatz; σ_cycle
algebraization; Case A.1 δ=χ) affect representation/explicitation/magnitude — NONE
reintroduces an independent direction or handedness. So the primitive count is firm even
where the magnitude is provisional. That asymmetry (firm count, provisional magnitude) is
the entire reason E20 was the right first theorem to attempt: it yields a real,
defensible classification result without needing the hard magnitude closure.

## Precondition cleared: the A5→A1+A4 reconciliation

The F.1 sketch §12.3 attributes σ_cycle to "axiom A5." Verified against axiom-registry.md:
A4 = the Nexus (Absolute-Moment cadence/consistency); A5 = the metric (η=1/φ, possibly
derivable from A2). So A5 carries no cycle-direction content. The cycle ordering is A1
(perceive/respond, defines the steps) + A4 (Nexus, sequences them) — the audit's temporal
primitive. The artifact §3 attributes σ_cycle to A1+A4 and records the F.1 "A5" as a
pre-canonical-numbering drift (anti-erasure: preserved for the record, not edited inline
in the F.1 sketch, which is a prior immutable workstream). This was the gate item that made
the artifact a 0636 deliverable rather than part of 0635.

## E19 cross-link kept distinct (not resolved)

σ_cycle (temporal cycle ordering) is kept distinct from the capture handedness E19
(spatial/CP partnering). §5.3/§F3: if a future result identifies them, that MERGES E20
into E19 (two items → one, programme-positive) rather than refuting the theorem. Flagged
as falsifier-F3-in-the-merge-direction and an open cross-link, not asserted.

## Registration choice

Followed the audit (Patch 0632) changelog-registration precedent rather than adding a
heavy theorem-registry body-table row: the CHIR-sector theorem records live in CHIR.md
(where the AUDIT-1 RESOLVED record already sits). theorem-registry.md gets the Patch 0636
changelog entry; CHIR.md gets the THEO-CHIR-PCD-ORIENTATION-1 RESOLVED record (converting
OPEN-CHIR-F1-LINK) and the E20 reclassification note. The audit .tex stays v1.1-frozen
(3/3-review-confirmed); the E20 reclassification is authored by the new theorem and tracked
in CHIR.md, not by re-editing the frozen audit.

## Compile

Clean 3-pass pdflatex, 8 pages, zero undefined references, no large overfull boxes.

## Anti-overclaim self-check

Theorem statement is a primitive-count claim, explicitly viability-level; does not close
F.1; does not derive the magnitude; does not derive n̂ or σ_cycle; keeps E19 distinct;
caps E20 at provisional. Consistent with the calibration discipline the just-closed AUDIT-1
review cycle reinforced (and ChatGPT's "cataloging vs derivation" caution — note this IS a
derivation theorem, the first after the catalogue, and it is labeled as such at its honest
layer).

# Reasoning fragment — Patch 0637 (OPEN-CHIR-1d / E21 χ = φ⁻³ scope sketch)

**Session 148, 29 May 2026. Per `templates/reasoning_capture_protocol.md`.**

## What I found, and why the scope differs from E20's

The recommendation was to scope OPEN-CHIR-1d (the χ = φ⁻³ magnitude). The clone-first
reading surfaced a more nuanced status than the audit's one-line note suggested:

1. **χ = φ⁻³ is registered as FI-C-9** — a foundational input (substrate-vacuum
   broken-symmetry order parameter), consumed by THEO-CAP-1, THEO-SD-CHIR-1/2, the F.1 arc.
2. **But the VALUE has a partial geometric derivation** — Capotauro Finding C-3 computes
   χ = (1−φ⁻¹)/(1+φ⁻¹) = φ⁻²/φ = φ⁻³ from the 600-cell edge-to-first-non-edge symmetric-bias
   ratio (and corrected a prior φ⁻² arithmetic error to get there). Verified the arithmetic
   independently (φ⁻³ ≈ 0.236; distinct from 1/√5 ≈ 0.447 and 5−2√5 ≈ 0.528).
3. **The audit's "CONT-1.3 partially addresses E21" is imprecise.** CONT-1.3 is magnitude
   *inheritance* (χ/6 projects through the continuum limit Φ), which CONSUMES χ. It does not
   derive χ. The real partial derivation is Finding C-3. Corrected in the sketch + CHIR.md;
   did not re-edit the v1.1-frozen audit.

So E21 is NOT cleanly closable at viability level the way E20 was. E20 was a primitive-count
question robust to the open commitments; E21 is a magnitude question where the value is
computed but rests on (a) a ratio-selection choice and (b) an unresolved symmetry-breaking
dynamics. The honest move is to scope it as a STAGED target, not rush a closure.

## The "why −3?" answer (the standing reviewer question)

ChatGPT's AUDIT-1 review flagged that Grok's E21 sketch showed φ-powers occur but didn't fix
the exponent. The answer the existing work supports: the exponent IS fixed once the
edge-to-first-non-edge symmetric-bias ratio is the generator — numerator 1−φ⁻¹ = φ⁻²,
denominator 1+φ⁻¹ = φ, ratio = φ⁻³. So the exponent is not the open question; the RATIO
SELECTION is. That reframing is the sketch's main intellectual contribution: it moves the
open question from "why −3" (answered, given the ratio) to "why this ratio" (the real gap).

## The two-sub-gap decomposition (the key structural decision)

OPEN-CHIR-1d = 1d-α (ratio selection) + 1d-β (symmetry-breaking dynamics):
- **1d-α** (near-term, Layer 2/2.5, reserve THEO-CHIR-CHI-1): justify that the
  edge-to-first-non-edge symmetric-bias ratio is the canonical magnitude generator vs the
  alternatives (1/√5, 5−2√5). A 600-cell combinatorial-geometry selection argument. Reachable.
- **1d-β** (deep, deferred, no ID reserved): derive the H₄→I₄ symmetry-breaking dynamics that
  select the broken chiral phase, eliminating FI-C-9. This is the F.1 §14.17 long-term target,
  deferred by Capotauro to OPEN-SM-4 ↔ SS-corpus. NOT near-term.

Collapsing these into one "derive φ⁻³" would obscure that one is reachable and one is deep —
and would risk mistaking 1d-α's closure for full E21 closure. The sketch keeps them separate
and is explicit that closing 1d-α alone does NOT upgrade E21 past emergent (provisional),
because 1d-β and the F.1 Case-A.1 provisional content both remain.

## Honesty discipline applied

- E21 classification unchanged (emergent provisional); the sketch clarifies what provisional
  means here (exponent-derivable-given-ratio; ratio + dynamics owed).
- Numerical signposts (δ_CP ≈ 193.3° vs NuFIT 195°±40°; Δp_LR = φ⁻³/6 ≈ 0.0394 vs ~0.04) are
  carried as SIGNPOSTS not derivations, inheriting Finding C-5's own discipline.
- FI-C-9 is explicitly NOT eliminated by the near-term artifact; said so.
- No theorem-registry proved-row; THEO-CHIR-CHI-1 reserved for 1d-α; 1d-β gets no ID
  (registering an ID for a deep deferred target would overclaim tractability).
- Audit .tex stays v1.1-frozen; the CONT-1.3 correction lives in the sketch + CHIR.md.

This is the honest analog of the E20 economy: there, firm count / provisional magnitude;
here, exponent-derivable-given-ratio / owed ratio-selection (reachable) + owed dynamics (deep).

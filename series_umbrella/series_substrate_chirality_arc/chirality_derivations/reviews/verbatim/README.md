# Verbatim reviewer returns — Session 228 (13 Sep 2026), chirality lane

**Step F of the §15 session close.** The OS is explicit: *"Summaries of reviewer content are particularly lossy"* — reviewer artifacts are to be committed **in full verbatim form**, not paraphrased into adjudication documents.

**This directory was created at Patch 0974a, and its absence was a Step-F failure.** Session 228 ran three review rounds plus two pre-dispatch rounds — roughly fifteen returns — and until this patch **none of the reviewer text existed in the repository at all.** It lived only in the founder's chat transcript. The adjudications (`0953`, `0957`, `0960`, `0968`) quote fragments; the returns themselves were not committed.

The founder caught this by asking whether the full handover protocol had actually been run. It had not: Steps E and H were done, C and D were deferred with rationale, and **A, B and F were missed while the handover's own Step-audit claimed the close was complete.** That audit was wrong and is corrected at 0974a.

## What is here

| file | round | seats |
|---|---|---|
| `CONV-047_returns.md` | first dispatch, CAPACITY-1 conditionality restatement | 5 returns, 2 valid, 3 rejected as non-reviews |
| `CONV-048_returns.md` | reformatted re-dispatch + addendum round | 3 + 5 returns |
| `CONV-049_returns.md` | piece-1 structural replacement | 5 returns |
| `predispatch_0965_0966.md` | pre-dispatch reviews of the piece-1 draft | 5 + 2 returns |

## Why verbatim matters here specifically

Three of this session's substantive findings came from reviewer text that a summary would have flattened:

- **Grok's 3-plane lemma** (CONV-049) — enacted as link (c) at 0968, in preference to the majority's position. The lemma arrived inside a dissenting return.
- **GPT's "four vectors to define ⇏ four coefficients to read"** — killed a dimensional argument the lane had been leaning on, in one sentence.
- **Grok's proxy objection** — *"if C_nn is insensitive to a drop from p=12 toward p=4, a 3e-4 shift is not evidence"* — which, had it stood, would have voided the whole piece-1 evidence base.

Two of the three rejected CONV-047 returns are also retained. **Rejected returns are evidence too**: one states in its own reasoning that it is simulating the panel, and one reports sub-second timings for a script another seat measured at 47 seconds. Keeping them is what makes the seat-validation at 0953 auditable rather than assertion.

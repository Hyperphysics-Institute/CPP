# Pre-dispatch reviews of the piece-1 draft (0965) and its repair (0966)

**Not a CONV round.** These are R-1 pre-dispatch reviews — the discipline enacted at 0959 requiring the lane to face hostile review *before* spending a founder dispatch. They are the reason CONV-049 closed in one round.

---

# On 0965 — five returns, **unanimous HOLD**

## GPT-5.6 Sol

> "**Pre-dispatch review of 0965: do not dispatch yet, but the proposed disposition is basically right.** The document correctly identifies that it has **not proved piece 1**. It has instead exposed the precise remaining structural assumption, which is progress."

On link (c) — the objection that forced the withdrawal:

> "A determinant sign det[d̂,n̂,r̂₁,r̂₂] requires four independent vectors to define a nonzero 4-D orientation. That proves something about the **geometric construction of the local chirality sign**. It does not immediately prove that an observable representing that sign must have **support on at least four incident edges**. Those are different notions of 'four'… **four vectors required to define chirality ⇏ four nonzero coefficients required to read an already-defined chirality variable**"

On the headline:

> "That conclusion does not follow from the chain as presently characterized, because **(a2) is explicitly evidenced rather than proved**."

And the formulation adopted at 0966 and again at 0968:

> "That distinction is worth preserving because it prevents the last assumption from disappearing by **renaming it rather than deriving it**."

## Grok 4.6

Own-run: `0965` 6/6 3.79 s. The objection that could have voided the entire evidence base:

> "**Indirect observable.** C_nn is a proxy for participation. The claim is about p(v). Measure p(v) against δ directly. **If C_nn is insensitive to a drop from p=12 toward p=4, a 3×10⁻⁴ shift is not evidence.**"

0966 answered it by calibration — C_nn changes by 3.03× across p = 12→4 — which converts the null into a bound, `p ≳ 11.9`.

On T2:

> "T2 as written is weaker than the prose: rank{d̂,n̂}=2 is trivial and does not show that a 2- or 3-edge reading is degenerate once (r̂₁,r̂₂) are part of the frame. **Do not let T2 carry (c).**"

## Gemini, Copilot, DeepSeek

All three: HOLD pending the physical-δ test. Gemini declared it could not execute and performed a stated analytic substitution. Copilot and DeepSeek required a combined worst-case bound and explicit statistical precision.

---

# On 0966 — both HOLDs lifted

## GPT-5.6 Sol

> "**Pre-dispatch re-look: READY FOR R-2 DISPATCH.** 0966 resolves the three issues that made me vote HOLD on 0965. In particular, I regard the correction to link (c) as substantive rather than cosmetic."

And the instruction that stopped the lane from over-preparing:

> "I would not request another pre-dispatch patch. The remaining uncertainty—(a2)—is now exposed rather than concealed, quantitatively tested at the physical bias, and is precisely the judgment the R-2 panel is supposed to make. **Further pre-panel work would start answering the panel question instead of preparing it.**"

## Grok 4.6

Own-run: `0966` 6/6 26.79 s.

> "**HOLD lifted for R-2 dispatch. HOLD retained on registration as axiom-only V3.**"

The precision carried verbatim into the CONV-049 package §3.3:

> "In the script, p_eff = 1/sin(π|C_nn|/2). That is the Gaussian inversion of the correlator, not a census of c^v_e(δ). The MC also *freezes* the det-coset weights and only moves edge means. So 0966 shows: **polarising a fixed unit-sign reading does not mimic concentration**, at δ=φ⁻³, to this precision. **It does not show that the substrate is incapable of deforming the weights.**"

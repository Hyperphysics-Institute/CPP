# Phenomena — Chirality Derivations

What the three derivations connect to — the structural and (signpost-level) empirical phenomena.
Honesty note: the empirical proximities below are **signposts, not derivations**; they support
the values but are not used in any of the three proofs.

---

## 1. Structural phenomena (what the theorems explain or ground)

**The spatial→`n̂` reduction, discharged.** The audit's headline — all spatial chirality reduces
to `n̂` — is made concrete: E20 (cycle orientation) and E19 (capture handedness) both reduce to
`n̂`-built objects, and E21 (magnitude) is a 600-cell distance-ratio at the `n̂`-defined local
neighborhood. No spatial chirality entry now stands as an independent primitive beyond `n̂` +
FI-C-9.

**Retroactive grounding of the SD-CHIR generators.** The pairing-convention generators `ζ^W`
(THEO-SD-CHIR-1) and `ζ^qDP` (THEO-SD-CHIR-2) — consumed by three shipped theorems and, downstream,
by the CONT arc — are now grounded in registered objects: their geometry is `n̂` + the 600-cell,
and their handedness is the FI-C-9 enantiomorph, not a silent extra input.

**The two-shell vertex-figure neighborhood.** The locality criterion surfaces the icosahedron
(12, the 600-cell vertex figure) + dodecahedron (20, the dual next shell) as the carrier of the
chirality magnitude — the local structure at the `H₄ → I_h` symmetry-breaking locus.

**The bias lives off the nearest shell.** Because first-shell↔first-shell edges are tangent to
`n̂` (local-`I_h`-preservation), the `O(ε)` chirality signature is carried by the
first→second-shell edges, not the nearest neighbors — a structural prediction about *where* the
substrate carries its handedness.

---

## 2. Empirical signposts (supporting, not derived)

These trace to `χ = φ⁻³` (and its cage-shell-suppressed form `χ/6`), the magnitude THEO-CHIR-CHI-1
selects:

- **Leptonic CP phase.** `δ_CP = 180° + arctan(φ⁻³) ≈ 193.3°`, vs NuFIT 6.0 central value
  `195° ± 40°`. A numerical proximity (Capotauro Finding C-5), registered as a signpost.
- **Parity-violation asymmetry.** `Δp_LR = φ⁻³/6 ≈ 0.0394`, vs the leptogenesis-back-derived
  anchor `~0.04` (within 2%). The `1/6 = V/2` cage-shell factor (Finding C-7) is the substrate-to-
  observable transmission, now at Layer 3 via THEO-CAP-1 / SD-CHIR.

Per Finding C-5 discipline and `AI_team_expectations.md` §1.3, these are *signposts that support*
the `φ⁻³` value — not validations of it, and not inputs to the THEO-CHIR-CHI-1 selection proof.

---

## 3. Where the phenomena terminate (the open frontier)

All of the above *consume* FI-C-9. The phenomena do not yet test the **dynamics** that would
eliminate it (1d-β), nor the **E19/E20 merge** (whether the temporal cycle sign is the same
substrate enantiomorph). A direct sub-percent `Δp_LR` measurement at the K3-doublet level
(OPEN-EXP-CAP-1) is the registered experimental handle that would sharpen or falsify the `χ = φ⁻³`
chain — the cleanest future empirical test of this sub-corpus.

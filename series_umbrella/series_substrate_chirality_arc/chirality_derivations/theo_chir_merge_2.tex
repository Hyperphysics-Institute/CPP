% ============================================================
% THEO-CHIR-MERGE-2: The Parity Decomposition of the Cycle Handedness
%   — The Temporal Chirality Content is FI-C-9 (One Chirality Primitive),
%     and sign(delta) is the Time-Reversal Arrow, not a Chirality Sign.
%
% OPEN-FP-F1-2 sub-target L4-D (the sign(delta) <-> enantiomorph gate);
%   advances OPEN-CHIR-MERGE MERGE-beta from M3 to M1-chi (the chirality-
%   count half resolved: ONE chirality (pseudoscalar) primitive = FI-C-9).
%
% Result (Layer 2.5, provisional): sign(delta) is P-even / T-odd and so
%   CANNOT carry the (P-odd, T-even) enantiomorph; the entire pseudoscalar
%   (spatial-chirality) content of sigma_cycle is sign(n-hat) = FI-C-9, and
%   sign(delta) is the time-reversal arrow content. Hence (i) the chirality
%   primitive count is ONE (FI-C-9, shared with CAP-1's spatial capture
%   handedness); (ii) MERGE-1's stated Layer-4 route "tie sign(delta) to
%   sign(n-hat)" is a parity category-mismatch and is corrected; (iii) the
%   chirality merge is F.2-INDEPENDENT (pure parity bookkeeping), with F.2 /
%   OPEN-CHIR-2a now seen as the pure T-arrow (sign(delta)) physicalization,
%   decoupled from the chirality count.
%
% Honest caps: does NOT derive FI-C-9 (1d-beta); does NOT fix sign(delta)
%   (the arrow; 2a/F.2); does NOT establish the delta-epsilon magnitude tie
%   (L4-E). Rests on the P/T assignments of Lemmas 1-4 (esp. delta T-odd,
%   omega_PCD T-odd). Single-pass; multi-AI review is the natural next step.
%
% Patch 0647 (Session 149) — built from the Patch 0646 OPEN-FP-F1-2 scope
%   sketch (L4-D carve-out) after reading the THEO-CHIR-MERGE-1 sign(delta)
%   treatment, THEO-CHIR-PCD-ORIENTATION-1 (omega_PCD axial), and
%   THEO-CHIR-CAP-1 (zeta x FI-C-9). Verified by
%   code/verify_merge_2_parity_decomposition.py.
%
% CHANGELOG
%   v1.0 (Patch 0647, Session 149, 29 May 2026) — initial registration at
%     Layer 2.5 (provisional): chirality-count half of MERGE-beta resolved
%     (M1-chi); sign(delta) classified as the T-arrow; MERGE-1's M1 route
%     corrected; F.2 dependency discharged for the chirality half.
%   v1.1 (Patch 0649, Session 149, 29 May 2026) — multi-AI review integrated
%     (ChatGPT + Grok + Copilot; reviews-CHIR-MERGE-2.md), no falsifier.
%     CALIBRATION: (1) Lemma delta-T-odd strengthened — delta couples ODDLY to
%     e-hat (flux-like, not an even static anisotropy; new CHECK 3), and the
%     T-odd assignment is grounded in the registered MERGE-alpha identification
%     of j_net with the T-odd arrow, with the honest conditional (if j_net is a
%     T-even static order parameter, delta is T-even -> M3); the verdict M1-chi
%     is now stated CONDITIONAL on MERGE-alpha. (2) Category-mismatch corollary
%     split into (a) no direct identification [airtight] and (b) no
%     presently-available covariant tie [no independent (P-odd,T-odd) mediator
%     exists]; the universal 'can never exist' wording removed. (3) Falsifier G2
%     refined (located at the MERGE-alpha ceiling) + new falsifier G6 (an
%     independent (P-odd,T-odd) primitive reopens the tie). No verdict change.
%   v1.2 (Patch 0651, Session 149, 29 May 2026) — AI review panel cycle closed with all members confirming,
%     no falsifier (ChatGPT CONFIRMED at v1.1 re-review: "M1-chi, conditional on
%     MERGE-alpha"). Applied ChatGPT's two non-verdict-flipping wording residuals:
%     (A) one explicit sentence that delta's T-oddness is INHERITED from MERGE-alpha,
%     not independently established; (B) the count claim reworded to "one
%     currently-identified chirality primitive" with a Remark scoping it (all
%     currently-identified P-odd content of sigma_cycle resides in sign(n-hat); not
%     an unconditional ontological claim; future-proofed against G6). No verdict
%     change; no theorem-statement change.
% ============================================================
% Version 1.4 --- 17 August 2026 (Patch 3213):
%   extended the generated identifier appendix to all five identifier
%   families (THEO-, FI-, PRED-, CONJ-, PH- as well as OPEN-), and
%   replaced review-convergence scores with AI review panel wording
%   where present. No claim, equation, number or section changed.
% Version 1.3 --- 17 August 2026 (Patch 3212):
%   added the generated plain-language appendix glossing the internal
%   programme identifiers used in this paper (founder jargon ruling, 16
%   Aug 2026). No claim, equation, number or section changed.

\documentclass[11pt,letterpaper]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{parskip}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\nhat}{\hat{n}}
\newcommand{\vhost}{v_{\text{host}}}
\newcommand{\scyc}{\sigma_{\text{cycle}}}
\newcommand{\scap}{\sigma_{\text{capture}}}
\newcommand{\wPCD}{\vec{\omega}_{\text{PCD}}}
\newcommand{\jnet}{\vec{\jmath}_{\text{net}}}
\newcommand{\sgn}{\operatorname{sign}}
\newcommand{\Po}{\mathsf{P}}
\newcommand{\Te}{\mathsf{T}}

\title{\textbf{THEO-CHIR-MERGE-2}\\[4pt]
The Parity Decomposition of the Cycle Handedness:\\
the Temporal Chirality Content is FI-C-9 (One Chirality Primitive),\\
and $\sgn(\delta)$ is the Time-Reversal Arrow, not a Chirality Sign\\[6pt]
\large A Layer-2.5 Provisional Theorem --- OPEN-FP-F1-2 sub-target L4-D;\\
advances OPEN-CHIR-MERGE MERGE-$\beta$ from M3 to M1-$\chi$}
\author{Conscious Point Physics --- Substrate Chirality Arc}
\date{Version 1.2 --- Patch 0651, Session 149 --- 29 May 2026 \\[2pt]\small(v1.0 Patch 0647; AI review panel cycle closed with all members confirming at v1.1--v1.2, no falsifier; ChatGPT CONFIRMED)\\[2pt]
{\small Version~1.3 --- 17 August 2026 (Patch 3212: added the generated plain-language appendix glossing the internal programme identifiers used in this paper (founder jargon ruling, 16 Aug 2026). No claim, equation, number or section changed.)}\\[2pt]
{\small Version~1.4 --- 17 August 2026 (Patch 3213: extended the generated identifier appendix to all five identifier families (THEO-, FI-, PRED-, CONJ-, PH- as well as OPEN-), and replaced review-convergence scores with AI review panel wording where present. No claim, equation, number or section changed.)}}

\begin{document}
\maketitle

\begin{abstract}
\noindent
THEO-CHIR-MERGE-1 (Patch 0644) reduced the temporal cycle handedness to
$\scyc = \sgn(\delta)\cdot\sgn(\nhat)$ and reported the reduction of $\scyc$ to
$\sgn(\nhat) = \text{FI-C-9}$ as \textbf{undetermined (M3)}, gating it on (a) tying $\sgn(\delta)$
to the enantiomorph at Layer 4 (OPEN-FP-F1-2) and (b) the F.2 substrate-Wigner-Eckart coupling.
This theorem --- the L4-D sub-target of OPEN-FP-F1-2 --- attacks (a) by a discrete-symmetry
($\Po$, $\Te$) bookkeeping of the factors. \textbf{Result.} The Mechanism-A rate-asymmetry
parameter $\delta$ is $\Po$-\emph{even} (it scales a propagation rate, a $\Po$-even scalar, against
the $\Po$-even contraction $\hat{e}\cdot\nhat$) and $\Te$-\emph{odd} (time reversal sends the rate
function $r(\hat{e})\mapsto r(-\hat{e})$, i.e.\ $\delta\mapsto-\delta$). The PCD-cycle orientation
$\wPCD$ is an axial vector ($\Po$-even) representing the cycle's temporal progression sense
($\Te$-odd); with $\nhat$ a polar direction ($\Po$-odd, $\Te$-even), the relation
$\wPCD = \scyc\,\nhat$ forces $\scyc$ to be $\Po$-odd and $\Te$-odd. Matching parities in
$\scyc = \sgn(\delta)\cdot\sgn(\nhat)$ then \textbf{decomposes $\scyc$ by symmetry character}: its
\emph{entire} $\Po$-odd (spatial-chirality, pseudoscalar) content is $\sgn(\nhat) = \text{FI-C-9}$,
and its \emph{entire} $\Te$-odd (temporal-arrow) content is $\sgn(\delta)$. Because the 600-cell
substrate is achiral (it admits a reflection symmetry --- verified), FI-C-9 is the \emph{unique}
primitive pseudoscalar, so this identification is forced. \textbf{Consequences.}
\textbf{(1) The chirality primitive count is ONE.} The pseudoscalar content of the temporal cycle
handedness is exactly FI-C-9 --- the \emph{same} enantiomorph as the spatial capture handedness
(THEO-CHIR-CAP-1: $\scap = \zeta\times\text{FI-C-9}$, $\zeta$ a $\Po$-even involution). Both faces
of chirality, spatial and temporal, are FI-C-9 modulated by a $\Po$-even factor (geometric
involution $\zeta$ / temporal arrow $\sgn(\delta)$). This is \textbf{verdict M1-$\chi$}: M1 at the
chirality level. \textbf{(2) MERGE-1's M1 route is corrected.} ``Tie $\sgn(\delta)$ to $\sgn(\nhat)$''
is a \emph{parity category-mismatch}: $\sgn(\delta)$ is $\Po$-even/$\Te$-odd while $\sgn(\nhat)$ is
$\Po$-odd/$\Te$-even, so no $\Po,\Te$-covariant relation can equate or tie them. The merge is
achieved not by tying the two signs but by recognising $\sgn(\delta)$ is the arrow, not a chirality.
\textbf{(3) The chirality merge is F.2-independent.} It is pure parity bookkeeping; F.2 (and
equivalently OPEN-CHIR-2a) is required only to physicalize the $\Te$-odd arrow $\sgn(\delta)$ --- a
\emph{pure temporal} question now decoupled from the chirality count. \textbf{Honest caps.} Layer
2.5 (a discrete-symmetry structural argument resting on the $\Po/\Te$ assignments of
Lemmas~\ref{lem:delta-p}--\ref{lem:sigma}); does \emph{not} derive FI-C-9 (that is 1d-$\beta$);
does \emph{not} fix $\sgn(\delta)$ (the arrow; OPEN-CHIR-2a / F.2); does \emph{not} establish the
$\delta$--$\varepsilon$ magnitude tie (L4-E). \textbf{The $\Te$-odd character of $\delta$ --- hence
the verdict --- is conditional on the registered MERGE-$\alpha$ identification of $\jnet$ with the
$\Te$-odd thermodynamic arrow} (the rate law alone does not force it; $\delta$ couples \emph{oddly}
to $\hat{e}$, so it is flux-like, not an even static anisotropy). AI review panel cycle closed with all members confirming, no falsifier
(ChatGPT + Grok + Copilot; ChatGPT CONFIRMED at v1.1 re-review; v1.2 applied two wording residuals). Verified by
\texttt{code/verify\_merge\_2\_parity\_decomposition.py}.
\end{abstract}

\tableofcontents

% ============================================================
\section{Setup: what MERGE-1 left, and what L4-D attacks}
\label{sec:setup}
% ============================================================

THEO-CHIR-MERGE-1 established two things about the temporal cycle handedness $\scyc$ (the sign in
the PCD-cycle orientation $\wPCD = \scyc\,\nhat$ of THEO-CHIR-PCD-ORIENTATION-1):

\begin{itemize}[leftmargin=1.5em]
\item \textbf{MERGE-$\alpha$ (resolved):} $\scyc$ \emph{is} the THEO-DSL-3 thermodynamic causal
arrow --- one temporal orientation, not two.
\item \textbf{MERGE-$\beta$ (verdict M3):} the reduction $\scyc \stackrel{?}{=} \sgn(\nhat) =
\text{FI-C-9}$ is undetermined. From the verified Phase-1 current $\jnet = (6\delta/\varphi^2)\nhat$
(direction $\sgn(\delta)\,\nhat$), the arrow sign is
\begin{equation}
\scyc = \sgn(\delta)\cdot\sgn(\nhat),
\label{eq:merge1-relation}
\end{equation}
carrying the Mechanism-A parameter sign $\sgn(\delta)$ \emph{in addition to} $\sgn(\nhat)$. MERGE-1
gated the merge to one sign on two registered closures: \emph{(a)} the Layer-4 Mechanism-A
derivation (OPEN-FP-F1-2) ``that would tie $\sgn(\delta)$ to the enantiomorph,'' and \emph{(b)} the
F.2 substrate-Wigner-Eckart coupling ``that makes $\sigma$ physical.''
\end{itemize}

This theorem is the OPEN-FP-F1-2 sub-target \textbf{L4-D}: the $\sgn(\delta)$-versus-enantiomorph
question, carved out at Patch 0646 as the first substantive sub-target of the Layer-4 derivation
(it is the shared gate for MERGE-$\beta$ and OPEN-CHIR-2a). We do not attempt the full Layer-4
derivation of Mechanism A (sub-targets L4-A/B/C); we attack the \emph{sign} question alone, and we
do it with discrete-symmetry bookkeeping rather than dynamics.

The pivotal observation is that closure~\emph{(a)} as MERGE-1 phrased it --- ``tie $\sgn(\delta)$ to
$\sgn(\nhat)$'' --- presupposes that $\sgn(\delta)$ and $\sgn(\nhat)$ are the \emph{same kind} of
object (two chirality signs, one possibly reducible to the other). \S\ref{sec:parities} shows they
are not.

% ============================================================
\section{The discrete-symmetry character of the four factors}
\label{sec:parities}
% ============================================================

We track two discrete symmetries of the substrate: spatial parity $\Po$ (the enantiomorph swap
$\nhat \to -\nhat$) and time reversal $\Te$ (reversal of the Absolute-Moment ordering, hence of the
PCD progression and of DI-bit propagation direction). A quantity is $\Po$-even/odd ($\Te$-even/odd)
according to its sign under $\Po$ ($\Te$).

\begin{lemma}[$\nhat$ is $\Po$-odd, $\Te$-even; $\sgn(\nhat) = \text{FI-C-9}$]
\label{lem:nhat}
The substrate primitive direction $\nhat = \vhost/|\vhost|$ is a polar spatial vector: under $\Po$,
$\nhat \to -\nhat$ ($\Po$-odd); it is unchanged under $\Te$ ($\Te$-even). By the framework
identification FI-C-RC-1 / FI-C-9, the choice of $\nhat$ versus $-\nhat$ is the frozen
substrate-vacuum enantiomorph: $\sgn(\nhat) = \text{FI-C-9}$, a pseudoscalar ($\Po$-odd, $\Te$-even),
consistent with $\Po$ flipping the handedness.
\end{lemma}

\begin{lemma}[$\delta$ is $\Po$-even]
\label{lem:delta-p}
Mechanism A (MA.1) gives the propagation rate $r(\hat{e}) = r_0(1 + \delta\,\hat{e}\cdot\nhat)$. A
propagation rate is a $\Po$-even scalar (a count of events per Absolute Moment, unchanged by a
spatial reflection). Under $\Po$ both $\hat{e}\to-\hat{e}$ and $\nhat\to-\nhat$, so the contraction
$\hat{e}\cdot\nhat$ is $\Po$-even. For $r$ to be $\Po$-even, $\delta$ must be $\Po$-even. In
particular $\sgn(\delta)$ does \emph{not} flip under the enantiomorph swap.
\end{lemma}

\begin{lemma}[$\delta$ is $\Te$-odd]
\label{lem:delta-t}
Under time reversal, the elementary process ``a DI-bit propagates along $\hat{e}$ at rate
$r(\hat{e})$'' maps to ``a DI-bit propagates along $-\hat{e}$,'' so $\Te$-covariance of the
dynamics requires the time-reversed rate function $r^{\Te}(\hat{e}) = r(-\hat{e}) =
r_0(1 - \delta\,\hat{e}\cdot\nhat) = r_0(1 + (-\delta)\,\hat{e}\cdot\nhat)$. Hence the $\Te$-image
parameter is $-\delta$: $\delta$ is $\Te$-odd.

\emph{Why this is a flux bias and not a static anisotropy} (the load-bearing point, sharpened at
the v1.1 review integration). MA.1 couples $\delta$ \emph{oddly} to $\hat{e}$: $r(\hat{e}) \neq
r(-\hat{e})$ whenever $\delta \neq 0$ (verified, \texttt{verify\_merge\_2\_parity\_decomposition.py}
CHECK 3). This is the signature of a \emph{polar, direction-odd propagation bias} (faster one way
than the other along $\nhat$), i.e.\ a drift-/flux-like quantity. It is \emph{not} an even-in-direction
static anisotropy --- conductivity anisotropy, a crystal-axis preference, a nematic director --- all
of which are \emph{even} in direction ($r(\hat{e}) = r(-\hat{e})$; $\nhat \equiv -\nhat$ for a
nematic) and hence $\Te$-even. So $\delta$ does not fall in the $\Te$-even static-anisotropy class.

\emph{What secures $\Te$-odd over $\Te$-even} (the conditional). MA.1 \emph{alone} does not force the
choice: a referee can posit that $\jnet = (6\delta/\varphi^2)\nhat$ is a $\Te$-\emph{even} static
\emph{polar} order parameter (an imposed substrate field) rather than a flux, which would make
$\delta$ $\Te$-even. The choice is fixed by an \emph{upstream registered result}: THEO-CHIR-MERGE-1
(MERGE-$\alpha$) $+$ THEO-DSL-3 identify $\jnet$ with the \emph{thermodynamic causal arrow}
(OPEN-SD-CHIR-PRIMITIVE manifestation iv), which is $\Te$-odd by definition. With $\jnet$ $\Te$-odd
and $\nhat$ $\Te$-even, $\delta$ is $\Te$-odd. \textbf{Hence $\delta$'s $\Te$-odd character --- and
therefore the verdict of this paper --- is conditional on the MERGE-$\alpha$ arrow-identification of
$\jnet$, and inherits its Layer-2.5 viability ceiling.} If that identification fails (i.e.\ $\jnet$
is a $\Te$-even static order parameter, not the arrow), then $\delta$ is $\Te$-even and the
decomposition's clean ``arrow vs chirality'' split collapses, reverting MERGE-$\beta$ toward M3
(falsifier G2; the alternative is located precisely at the MERGE-$\alpha$ ceiling, not as a free
choice). Consistency check: $\jnet = (6\delta/\varphi^2)\nhat$ is the arrow current, $\Te$-odd
($\jnet \to -\jnet$); with $\delta$ $\Te$-odd and $\nhat$ $\Te$-even this is satisfied.
\end{lemma}

\begin{lemma}[$\wPCD$ is $\Po$-even (axial), $\Te$-odd]
\label{lem:omega}
By THEO-CHIR-PCD-ORIENTATION-1 (and A6$'$), $\wPCD$ is an \emph{axial} vector (pseudovector): the
axial vector associated with the Perceive$\to$Compute$\to$Displace progression sense. Axial vectors
are $\Po$-even. The progression sense reverses under time reversal (the cycle runs backwards), so
$\wPCD$ is $\Te$-odd. (A circulation/orientation sense is the standard example of a $\Po$-even,
$\Te$-odd axial vector, like angular momentum's orientation under $\Te$.)
\end{lemma}

\begin{lemma}[$\scyc$ is $\Po$-odd and $\Te$-odd]
\label{lem:sigma}
From $\wPCD = \scyc\,\nhat$ with $\wPCD$ $\Po$-even (Lemma~\ref{lem:omega}) and $\nhat$ $\Po$-odd
(Lemma~\ref{lem:nhat}), $\scyc$ is $\Po$-odd: it is a pseudoscalar. From $\wPCD$ $\Te$-odd and
$\nhat$ $\Te$-even, $\scyc$ is $\Te$-odd. (The $\Te$-odd character matches MERGE-1's statement that
the two $\sigma$-choices are time-reversal-symmetric framings.)
\end{lemma}

\begin{lemma}[The 600-cell is achiral: FI-C-9 is the unique primitive pseudoscalar]
\label{lem:achiral}
The 600-cell substrate (A11) admits an improper orthogonal symmetry: the reflection
$\operatorname{diag}(-1,1,1,1)$ permutes its 120 vertices (verified at machine precision,
\texttt{verify\_merge\_2\_parity\_decomposition.py}). Equivalently, its symmetry group is the
Coxeter group $H_4$, generated by reflections. Hence the substrate geometry alone supplies no
handedness: it is invariant under $\Po$ and contributes no primitive pseudoscalar. The only
primitive pseudoscalar available to the framework is the frozen enantiomorph $\sgn(\nhat) =
\text{FI-C-9}$ (Lemma~\ref{lem:nhat}).
\end{lemma}

The four factors, by discrete-symmetry character:
\begin{center}
\begin{tabular}{lccl}
\hline
quantity & $\Po$ & $\Te$ & role \\
\hline
$\nhat$ \ ($\sgn(\nhat) = \text{FI-C-9}$) & odd & even & spatial enantiomorph (pseudoscalar) \\
$\delta$ & even & odd & Mechanism-A rate asymmetry \\
$\jnet = (6\delta/\varphi^2)\nhat$ & odd & odd & DI-bit current \\
$\wPCD$ & even & odd & cycle orientation (axial) \\
$\scyc$ \ ($\wPCD = \scyc\nhat$) & odd & odd & cycle handedness (temporal) \\
\hline
\end{tabular}
\end{center}

% ============================================================
\section{The parity decomposition theorem}
\label{sec:theorem}
% ============================================================

\begin{theorem}[Parity decomposition of the cycle handedness; THEO-CHIR-MERGE-2]
\label{thm:main}
Under CPP axioms A1--A11 $+$ Reading C $+$ Mechanism A (sketch-document Layer 3), with the
discrete-symmetry characters of \S\ref{sec:parities}, the temporal cycle handedness
\eqref{eq:merge1-relation} decomposes by symmetry character as
\[
\underbrace{\scyc}_{\Po\text{-odd},\ \Te\text{-odd}}
\;=\;
\underbrace{\sgn(\delta)}_{\Po\text{-even},\ \Te\text{-odd}}
\;\cdot\;
\underbrace{\sgn(\nhat) = \text{FI-C-9}}_{\Po\text{-odd},\ \Te\text{-even}}.
\]
Consequently:
\begin{enumerate}[label=(\roman*),leftmargin=2.2em]
\item the \emph{entire} $\Po$-odd (spatial-chirality / pseudoscalar) content of $\scyc$ is
$\sgn(\nhat) = \text{FI-C-9}$ --- and by Lemma~\ref{lem:achiral} this is the unique primitive
pseudoscalar, so the identification is forced, not chosen;
\item the \emph{entire} $\Po$-even, $\Te$-odd (temporal-arrow) content of $\scyc$ is $\sgn(\delta)$.
\end{enumerate}
\end{theorem}

\begin{proof}
The symmetry characters are Lemmas~\ref{lem:nhat}--\ref{lem:sigma}. Each side of
\eqref{eq:merge1-relation} carries a well-defined $(\Po,\Te)$ character; the right side is
$(\Po,\Te) = (\text{even}\cdot\text{odd},\ \text{odd}\cdot\text{even}) = (\text{odd},\text{odd})$,
matching the left side (Lemma~\ref{lem:sigma}), so \eqref{eq:merge1-relation} is $\Po,\Te$-covariant.
A product of a $\Po$-even factor and a $\Po$-odd factor has its $\Po$-odd content carried
\emph{entirely} by the $\Po$-odd factor; here that factor is $\sgn(\nhat)$, giving (i). Dually, its
$\Te$-odd content with $\Po$-even character is carried entirely by the $\Po$-even/$\Te$-odd factor
$\sgn(\delta)$, giving (ii). Uniqueness of the pseudoscalar is Lemma~\ref{lem:achiral}.
\end{proof}

\begin{corollary}[The chirality primitive count is ONE --- verdict M1-$\chi$]
\label{cor:count}
The pseudoscalar (spatial-chirality) content of the temporal cycle handedness $\scyc$ equals
$\text{FI-C-9}$, the \emph{same} enantiomorph that THEO-CHIR-CAP-1 pinned the spatial capture
handedness to ($\scap = \zeta\times\text{FI-C-9}$, with $\zeta$ a $\Po$-even registered-geometric
involution). Both faces of chirality --- spatial capture ($\zeta\times\text{FI-C-9}$) and temporal
cycle ($\sgn(\delta)\times\text{FI-C-9}$) --- are the \emph{one} primitive FI-C-9 modulated by a
$\Po$-even factor (a geometric involution $\zeta$ / the temporal arrow $\sgn(\delta)$). The
programme therefore carries \textbf{one currently-identified} chirality (pseudoscalar) primitive,
FI-C-9. This is \textbf{M1 at the chirality level (M1-$\chi$)}: the chirality-count half of
MERGE-$\beta$ is resolved in the unifying (one-sign) direction.

\begin{remark}[Scope of the count claim --- per the v1.2 review residual]
\label{rem:count-scope}
The established statement is the precise one: \emph{all currently identified $\Po$-odd
(pseudoscalar) content of $\scyc$ resides in $\sgn(\nhat) = \text{FI-C-9}$}, given the achirality of
the substrate (Lemma~\ref{lem:achiral}). It is \emph{not} the stronger ontological claim ``all
chirality is $\sgn(\nhat)$ unconditionally'': the registration of an independent
$(\Po\text{-odd},\Te\text{-odd})$ primitive would add a chirality sign (falsifier G6), and the
underived enantiomorph FI-C-9 is itself consumed, not derived (1d-$\beta$). ``One chirality
primitive'' is therefore the \emph{current} count under the present registered framework, not an
unconditional ontological assertion.
\end{remark}
\end{corollary}

\begin{corollary}[Correction of MERGE-1's M1 route: a parity category-mismatch]
\label{cor:mismatch}
MERGE-1's stated Layer-4 route to M1 --- ``tie $\sgn(\delta)$ to $\sgn(\nhat)$'' --- cannot be
executed as phrased. We separate two claims (the v1.1 review integration sharpened this, per
ChatGPT + Copilot). \textbf{(a) No direct identification} $\sgn(\delta) = \sgn(\nhat)$ is possible:
$\sgn(\delta)$ is $\Po$-even/$\Te$-odd (Lemmas~\ref{lem:delta-p}, \ref{lem:delta-t}) and
$\sgn(\nhat)$ is $\Po$-odd/$\Te$-even (Lemma~\ref{lem:nhat}); a covariant equation cannot relate two
objects of different $(\Po,\Te)$ character. This is airtight. \textbf{(b) A covariant \emph{tie}}
(one a function of the other) would require an independent mediator of character $(\Po,\Te) =
(\text{odd},\text{odd})$ --- e.g.\ $\sgn(\delta) = X\cdot\sgn(\nhat)$ with $X$ a $\Po$-odd/$\Te$-odd
pseudoscalar makes both sides $\Po$-even/$\Te$-odd. \textbf{No such \emph{independent} primitive
exists in the current framework}: the 600-cell is achiral (Lemma~\ref{lem:achiral}), so geometry
supplies no primitive pseudoscalar beyond FI-C-9; and the only $(\Po\text{-odd},\Te\text{-odd})$
objects in play --- $\scyc$ itself and (via it) $\wPCD$ --- are exactly what is being decomposed,
hence not independent mediators. Therefore \textbf{no presently-available covariant tie exists}
(not ``no covariant relation can ever exist'' --- the honest statement is the weaker one). The
chirality merge is achieved \emph{not} by tying the two signs but by recognising that $\sgn(\delta)$
is the $\Te$-arrow content, not a chirality sign; MERGE-1's ``$\sgn(\delta)$ carries an independent
sign in addition to $\sgn(\nhat)$'' is sharpened --- that independent sign is the arrow, $\Po$-even,
hence never a candidate \emph{chirality} primitive. If an independent
$(\Po\text{-odd},\Te\text{-odd})$ primitive were ever registered, the tie route would reopen
(falsifier G6).
\end{corollary}

\begin{corollary}[The chirality merge is F.2-independent; F.2 / 2a is the pure $\Te$-arrow question]
\label{cor:f2}
MERGE-1 gated MERGE-$\beta$ on both the Layer-4 derivation and the F.2 substrate-Wigner-Eckart
coupling. Theorem~\ref{thm:main} discharges the chirality-content half \emph{without} F.2: it is
pure $\Po,\Te$ bookkeeping over the verified relation \eqref{eq:merge1-relation}. The residual
content of $\scyc$ is the $\Te$-odd arrow $\sgn(\delta)$, whose \emph{physical} value is exactly the
OPEN-CHIR-2a question (the cycle's $\Te$-asymmetry $\Leftrightarrow$ MERGE-$\beta$-physical, by
MERGE-1 Prop.~4.1). Theorem~\ref{thm:main} therefore \emph{decouples} the chirality count (resolved,
F.2-free) from the arrow physicalization (open, F.2/2a): the F.2 coupling is needed only for the
pure-temporal arrow, never for the chirality count.
\end{corollary}

% ============================================================
\section{What is and is not resolved}
\label{sec:caps}
% ============================================================

\textbf{Resolved (Layer 2.5, provisional; conditional on MERGE-$\alpha$).} The chirality primitive
count is \emph{one} (FI-C-9): the temporal cycle handedness shares its pseudoscalar content with the
spatial capture handedness. MERGE-$\beta$ advances from M3 to \emph{M1-$\chi$}, \emph{conditional on
the MERGE-$\alpha$ identification of $\jnet$ with the $\Te$-odd thermodynamic arrow}
(Lemma~\ref{lem:delta-t}; the result inherits MERGE-$\alpha$'s Layer-2.5 viability ceiling). MERGE-1's
``tie $\sgn(\delta)$ to the enantiomorph'' route is corrected (Corollary~\ref{cor:mismatch}: no direct
identification; no presently-available covariant tie). The F.2 dependency is discharged for the
chirality half.

\textbf{Not resolved (honest caps).}
\begin{itemize}[leftmargin=1.5em]
\item \textbf{The arrow $\sgn(\delta)$ is not fixed.} Its physical value is the OPEN-CHIR-2a / F.2
$\Te$-content question. ``Full M1'' (a single physical sign with no free $\Te$-odd factor) is
\emph{not} claimed; what is claimed is one \emph{chirality} sign.
\item \textbf{FI-C-9 is not derived.} The theorem \emph{consumes} the enantiomorph (as do CAP-1,
PCD-ORIENTATION-1); deriving it is the deep 1d-$\beta$ ($H_4 \to I_h$ symmetry-breaking dynamics).
\item \textbf{The $\delta$--$\varepsilon$ magnitude relation is untouched.} Whether $|\delta|$ ties
to the spatial chirality magnitude $\varepsilon = |\chi| = \varphi^{-3}$ is the separate L4-E
sub-target (couples to E21 / THEO-CHIR-CHI-1).
\item \textbf{$\delta$'s T-character is inherited, not independently established} (the v1.2 review
residual, stated once explicitly). This theorem does \emph{not} establish the $\Te$-oddness of
$\delta$ from MA.1 alone; it \emph{inherits} it from MERGE-$\alpha$ through the identification of
$\jnet$ with the $\Te$-odd thermodynamic arrow. The odd-in-$\hat{e}$ coupling
(Lemma~\ref{lem:delta-t}) only excludes the $\Te$-even \emph{even}-anisotropy class; the decisive
step is the MERGE-$\alpha$ arrow-identification.
\item \textbf{Layer 2.5.} The result is a discrete-symmetry structural argument; the load-bearing
inputs are (i) $\delta$ couples \emph{oddly} to $\hat{e}$ (flux-like, not an even static anisotropy),
(ii) the MERGE-$\alpha$ identification of $\jnet$ with the $\Te$-odd arrow (which fixes $\delta$
$\Te$-odd over $\Te$-even), and (iii) $\wPCD$ axial/$\Te$-odd (Lemma~\ref{lem:omega}). Multi-AI
review complete (Patches 0648--0651; ChatGPT + Grok + Copilot; \textbf{3/3, no falsifier}; ChatGPT
CONFIRMED at v1.1 re-review --- the v1.1 calibration made inputs (i)/(ii) and the
identification-vs-tie distinction of Corollary~\ref{cor:mismatch} explicit; v1.2 applied two
ChatGPT wording residuals, no verdict change).
\end{itemize}

% ============================================================
\section{Falsifiers}
\label{sec:falsifiers}
% ============================================================

\begin{itemize}[leftmargin=1.5em]
\item \textbf{(G1)} $\wPCD$ is shown not to be axial, or not $\Te$-odd --- breaks
Lemma~\ref{lem:omega} and the decomposition.
\item \textbf{(G2)} $\delta$ is shown $\Te$-even --- concretely, the MERGE-$\alpha$ identification of
$\jnet$ with the $\Te$-odd thermodynamic arrow fails and $\jnet$ is instead a $\Te$-even static
\emph{polar} order parameter. Then $\sgn(\delta)$ is $\Po$-even/$\Te$-even, the arrow/chirality split
collapses, and the verdict reverts toward M3. (This is the load-bearing falsifier; it is located at
the MERGE-$\alpha$ viability ceiling, per the v1.1 Lemma~\ref{lem:delta-t}. Note: an \emph{even}-in-direction
static anisotropy --- conductivity, crystal-axis --- does \emph{not} trigger G2, since $\delta$
couples \emph{oddly} to $\hat{e}$, CHECK 3.)
\item \textbf{(G3)} a primitive geometric pseudoscalar exists in the substrate beyond FI-C-9
(breaks Lemma~\ref{lem:achiral}'s uniqueness; but the 600-cell is achiral --- verified).
\item \textbf{(G4)} the framework identification $\sgn(\nhat) = \text{FI-C-9}$ fails (then $\nhat$'s
sign is not the enantiomorph and Lemma~\ref{lem:nhat} must be restated).
\item \textbf{(G5)} the relation \eqref{eq:merge1-relation} itself fails (e.g.\ the conversion of
the polar current to the axial cycle orientation introduces a factor of indefinite parity) ---
breaks the premise inherited from MERGE-1.
\item \textbf{(G6)} an \emph{independent} $(\Po\text{-odd},\Te\text{-odd})$ primitive is registered
in the framework (one not reducible to $\scyc$/$\wPCD$ or to FI-C-9) --- would reopen the covariant
\emph{tie} route of Corollary~\ref{cor:mismatch} (b) and could restore an M1-via-tie reading.
(Added at the v1.1 review integration, per the ChatGPT/Copilot identification-vs-tie distinction.)
\end{itemize}

% ============================================================
\section*{References (internal)}
\addcontentsline{toc}{section}{References (internal)}
% ============================================================

\begin{itemize}[leftmargin=1.5em]
\item THEO-CHIR-MERGE-1 (Patch 0644) --- $\scyc = \sgn(\delta)\cdot\sgn(\nhat)$; MERGE-$\beta$ = M3;
2a $\Leftrightarrow$ MERGE-$\beta$-physical.
\item THEO-CHIR-PCD-ORIENTATION-1 (Patch 0636) --- $\wPCD = \scyc\,\nhat$; $\wPCD$ axial.
\item THEO-CHIR-CAP-1 (Patch 0640) --- $\scap = \zeta \times \text{FI-C-9}$; $\zeta$ a $\Po$-even
registered-geometric involution.
\item THEO-DSL-3 / F.1 \S2.3, \S4 (Patch 0570) --- Mechanism A (MA.1 + MA.2); $\jnet =
(6\delta/\varphi^2)\nhat$; the $\sigma$ sign convention.
\item OPEN-FP-F1-2 scope sketch (Patch 0646) --- L4-D carve-out.
\item \texttt{code/verify\_merge\_2\_parity\_decomposition.py} --- 600-cell achirality + the
$(\Po,\Te)$ character table.
\end{itemize}

% BEGIN GENERATED IDENTIFIER APPENDIX -- do not edit by hand
\clearpage
\section*{Appendix: Programme identifiers used in this paper}
\addcontentsline{toc}{section}{Appendix: Programme identifiers}

\noindent This programme tracks its results, assumptions and unresolved questions under short identifiers, so that a claim and its dependencies can be cited precisely. Prefixes denote the kind of item: \texttt{THEO-} a proved theorem, \texttt{FI-} a foundational input the derivation assumes, \texttt{PRED-} a registered prediction, \texttt{CONJ-} a conjecture, \texttt{OPEN-} an unresolved question, \texttt{PH-} a problem history. Those appearing in this paper are glossed below; no external document is needed to read them.

\begin{description}
  \item[\texttt{FI-C-9}] \hfill \\ The substrate chirality magnitude, a free-magnitude postulate at v1.0 later grounded at v2.0.
  \item[\texttt{FI-C-RC-1}] \hfill \\ The substrate primitive: a preferred 4D direction in the ambient space where the 600-cell substrate sits, sourcing all parity-sensitive observables.
  \item[\texttt{OPEN-CHIR-2}] \hfill \\ For each entry the audit classifies as *unregistered*,
  \item[\texttt{OPEN-CHIR-MERGE}] \hfill \\ Decide whether the temporal PCD-cycle handedness `σ\_cycle` (E20; A1+A4)
  \item[\texttt{OPEN-FP-F1-2}] \hfill \\ Derive Mechanism A (F.1 framework axioms MA.1 + MA.2: propagation-rate asymmetry \$r(\textbackslash\{\}hat\{e\}) = r\_0(1 + \textbackslash\{\}delta\textbackslash\{\},\textbackslash\{\}hat\{e\}\textbackslash\{\}cdot\textbackslash\{\}hat\{n\})\$ and framework-local current construction at \$\textbackslash\{\}mathcal\{O\}(\textbackslash\{\}delta\textasciicircum{}1)\$) from CPP primitive axioms A1–A11 alone.
  \item[\texttt{OPEN-SD-CHIR-PRIMITIVE}] \hfill \\ Derive the universe's primitive chirality bias from a single substrate-level mechanism (current leading candidate: primitive 4D direction \$\textbackslash\{\}hat\{n\}\$ aligned with a 600-cell host vertex per Reading C) that simultaneously accounts for the five observable manifestations across the CPP corpus: K3-doublet chirality (Capotauro \$\textbackslash\{\}Delta p\_\{LR\}\$, mass-mixing sector), W-bracelet V-A coupling (electroweak, massless helicity limit), electromagnetic handedness (E×B / Poynting / Maxwell right-hand rule and qDP/eDP polarization patterns), thermodynamic causal arrow (DI-bit propagation direction / PCD cycle ordering / Conscious Moment sequence), and cosmological vacuum asymmetry (matter-antimatter / baryogenesis / Capotauro nucleation-event sign-selection).
  \item[\texttt{THEO-CHIR-CAP-1}] \hfill \\ The Capotauro-sector chirality theorem.
  \item[\texttt{THEO-CHIR-CHI-1}] \hfill \\ The substrate chirality magnitude result fixing the golden-ratio power of the chirality parameter.
  \item[\texttt{THEO-CHIR-MERGE-1}] \hfill \\ The first of the chirality merge theorems, unifying parity and time-reversal structure.
  \item[\texttt{THEO-CHIR-MERGE-2}] \hfill \\ The chirality merge theorem giving the cycle sign as the product of the director and offset signs.
  \item[\texttt{THEO-CHIR-PCD-ORIENTATION-1}] \hfill \\ A candidate theorem deriving the PCD winding orientation from the chirality director.
  \item[\texttt{THEO-DSL-3}] \hfill \\ Substrate-Locality Umbrella Theorem (**third F-line flagship theorem under THEO-DSL-N sub-prefix convention**; **sketch-document Layer 3** — not independently hardened, assembled from THEO-DSL-1 + THEO-DSL-2 + icosahedral rank-1 sum identity; **closes OPEN-SD-CHIR-PRIMITIVE manifestation (iv) thermodynamic causal arrow** — fourth manifestation closure under the umbrella)
\end{description}
% END GENERATED IDENTIFIER APPENDIX

\end{document}

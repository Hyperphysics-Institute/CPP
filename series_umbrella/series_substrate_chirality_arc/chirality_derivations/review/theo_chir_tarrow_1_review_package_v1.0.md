# Review package — THEO-CHIR-TARROW-1 (the time-reversal arrow status; OPEN-CHIR-2a)

**For:** the CPP review panel (ChatGPT, Grok, Copilot — independent reviews).
**Target:** THEO-CHIR-TARROW-1 (v1.0, Patch 0658) — the **temporal half** of the full chirality picture: the primitive/emergent status of the time-reversal arrow `sign(δ)`. It is the counterpart of the STATUS-1+STATUS-2 pair (which the panel closed 3/3) and completes the chirality-status capstone on the temporal side.
**What we want:** an adversarial check of whether this capstone half is *sound, honestly scoped,* and **genuinely independent of STATUS-1/2** (not a mechanical P→T transliteration), with the **CPT-unification claim (§4 Q4)** pressed hardest — it is the novel, load-bearing, and most-flippable result. Verdict-flipping objections need a worked argument, not a sketch.

**Disambiguation (read first).** This is the CPP **chirality** programme's *temporal-arrow status* capstone — a Layer-2.5 **classification/structural** theorem about whether the time-reversal arrow `sign(δ)` is primitive or emergent. It is **NOT** a nuclear-physics OPEN-SS audit, **NOT** the THEO-CHIR-AUDIT-1 enumeration, **NOT** a DSL/F.1 derivation, and (critically) **NOT a claim to have *derived* the arrow** (the verdict is W3 = "not yet derived"). Everything needed is inline, including the full verify code (§7); engage the inline content, do not reconstruct from memory.

**Process note (from the STATUS cycle, Patch 0656).** The verify code is **embedded in full below (§7)** so you can reach the SCRIPT-EXECUTED tier without external files.

---

## 0. IS / IS-NOT

**This theorem IS:** a Layer-2.5 provisional *status/classification* result placing the T-arrow `sign(δ)` at verdict **W3** at current rigor; pinning its emergence upgrade to **W1** by an axiom-level W2-exclusion; and proving (under CPT) that the temporal W2-reopener and the spatial V2-reopener are the **same** cross-sector object.

**This theorem IS NOT:** a derivation of the substrate T-asymmetry mechanism; a fixing of the arrow direction; a claim that `sign(δ)` is emergent (it is W3 — not derived); a claim that CPT-invariance of the CPP substrate is *established* (it is *assumed*, flagged as falsifier T5); a re-derivation of MERGE-2's (P,T) assignments (those are consumed as premises, conditional on MERGE-α).

---

## 1. Context (what is already settled)

THEO-CHIR-MERGE-2 (cycle closed 3/3, conditional on MERGE-α) decomposed the cycle handedness by discrete-symmetry character:
```
  σ_cycle = sign(n̂) · sign(δ),     ω_PCD = σ_cycle · n̂
  sign(n̂) = FI-C-9 : P-odd , T-even   (the spatial chirality primitive)
  sign(δ)          : P-even, T-odd    (the time-reversal ARROW; conditional on MERGE-α)
  ω_PCD            : P-even, T-odd    (axial; the cycle progression sense)
```
The STATUS pair (STATUS-1 verdict structure; STATUS-2 breaking chain) then settled the **spatial** half: `sign(n̂)=FI-C-9` is **V3** at current rigor, upgrade pinned to **V1**, V2 reopenable only by a cross-sector P-odd pseudoscalar (1d-β-v, the SM CP-phase / OPEN-SM-4). The **temporal** half — the status of the arrow `sign(δ)` — is OPEN-CHIR-2a (audit E2-asymmetry / E17), the subject of this theorem.

---

## 2. THEO-CHIR-TARROW-1 — the claims (inline)

**(A) Verdict structure (instantiated, not re-proved).** STATUS-1's partition is generic to any (capacity, value) emergence question. Instantiate on the **time-reversal axis**:
- *Capacity* `C_T ∈ {No, Yes}`: is there a *derivable substrate mechanism* producing a time-reversal-asymmetric dynamics — an arrow of time at all (a nonzero `j_net` with a definite progression sense)?
- *Value* `V_T ∈ {derived, spontaneous, free}`: *given* a T-asymmetric dynamics, is the arrow **direction** `sign(δ)` fixed by a derivable T-odd asymmetry (derived), selected contingently (spontaneous), or an unconstrained boundary datum (free)? The **free** case includes **cosmological inheritance** — a low-entropy initial/boundary state external to the substrate dynamics (the standard thermodynamic-arrow picture).

Three verdicts, by the **same map** as STATUS-1: **W3** (C_T=No → irreducible primitive arrow), **W1** (C_T=Yes ∧ V_T∈{spontaneous,free} → emergent mechanism, contingent/inherited direction), **W2** (C_T=Yes ∧ V_T=derived → fully emergent). Exhaustive & exclusive (CHECK 1).

**(B) The disanalogy with parity — the T-even-geometry lemma.** STATUS-2 had a rich `H₄ → H₄⁺` (index-2, ℤ₂) breaking chain because the achiral 600-cell still *hosts* the P-odd order parameter `sign(n̂)`. For time reversal **there is no such chain**: T acts on the *dynamical* variables (`j_net`, `ω_PCD`, `δ`), not on the static lattice, and **every static-geometric invariant** — coordinates, Gram matrix, the full distance spectrum, the unsigned *and signed* 4-volumes, the orientation pseudoscalar `sign(n̂)` itself — **is T-even** (it carries no time index). So the geometry supplies **no T-odd quantity at all**, and T (an antiunitary operation on the dynamics, not a point-group element) admits no finite-group breaking chain. (CHECK 2a.)

**(C) Current-rigor placement W3.** Capacity is C_T=No: (Q1) MERGE-2 isolates the T-odd content of σ_cycle as the single object `sign(δ)`; (Q2) the geometry is T-even (B) → any arrow needs a *dynamical onset*; (Q3) THEO-DSL-3 *narrates* the arrow (current ∥ n̂ → capture-phase bias → ω_PCD → macroscopic arrow) but does **not derive** its emergence — it inherits the DSL viability ceiling (entropy/irreversibility not derived; F.1 §14.17); (Q4) the F.2 substrate-Wigner-Eckart physicalization of σ is unbuilt. No mechanism ⇒ C_T=No ⇒ **W3**. **W3 = "not yet derived," NOT "underivable"** (explicit floor, upgradeable). (CHECK 2c.)

**(D) Axiom-level W2-exclusion → W1 pin.** *Lemma:* a quantity can differ on the two time-orientations only if it is **T-odd** (every T-even quantity is T-invariant and agrees on both) — the T-analog of STATUS-2's pseudoscalar lemma. *Theorem:* fixing `sign(δ)` requires a T-odd quantity; by (B) the geometry supplies none, and by MERGE-2 the **only** T-odd object in the registered framework is `sign(δ)` itself (`ω_PCD` is not independent — `ω_PCD = σ_cycle·n̂` and the T-odd part of σ_cycle *is* `sign(δ)`). The only available sign-fixer is `sign(δ)` fixing itself — **circular**. So V_T ≠ derived at axiom level ⇒ **W2 excluded at the axiom level** ⇒ the C_T=Yes outcome is pinned to **W1**. The natural V_T=free subtype is **cosmological inheritance** (low-entropy past). (CHECK 2b.)

**(E) The CPT unification — one cross-sector reopener for both signs.** STATUS-2's only V2-reopener is an axiom-external **P-odd** pseudoscalar (the SM CP-phase, 1d-β-v, OPEN-SM-4). TARROW-1's only W2-reopener is an axiom-external **T-odd** quantity. By the **CPT theorem** (in a CPT-invariant theory, T-violation ⟺ CP-violation) these are the **same** cross-sector object: the SM CP/T-violating phase. Consequently *fully deriving chirality* — explicitly fixing both the P-sign FI-C-9 **and** the T-arrow `sign(δ)` — is a **single** cross-sector target, not two. (CHECK 3.)

**Honest caps:** classification not derivation (`sign(δ)` not derived); W3 current-rigor not ontological; inherits MERGE-2's MERGE-α conditionality (δ is T-odd only granting the j_net-arrow identification) + the "currently-identified" G6 hedge; **CPT-invariance is an assumption** (if the substrate is CPT-violating, the two reopeners need not coincide — falsifier T5); the W1 capacity engine (F.2 + DSL arrow-emergence) is the deep, gated temporal counterpart of 1d-β-ii.

---

## 3. The registered goal-answer (full chirality status, both halves)

> Chirality in CPP is emergent down to **one P-odd primitive** (FI-C-9 — **V3** at current rigor, upgrade→V1) **plus one T-odd arrow** (`sign(δ)` — **W3** at current rigor, upgrade→W1, direction free = cosmological inheritance). Each sign is the **unique object of its symmetry character** (so non-derivable at the axiom level — V2 and W2 both excluded there), and **both** reopen to full derivation only through a **single CPT-linked cross-sector object** (the SM CP/T phase, 1d-β-v ↔ OPEN-SM-4). The two halves of the chirality picture therefore close on one target.

---

## 4. What we want you to scrutinize (load-bearing claims)

Please press hardest on **(Q4)** — it is the novel content and the one most likely to either be the result's gem or its overclaim.

- **(Q4) Is the CPT unification (claim E) sound?** Three sub-checks. **(4a)** Is CPT-invariance of the CPP substrate legitimately *assumable* here, or is it doing hidden work? (It is flagged as an assumption / falsifier T5 — is that scoping honest, or does the theorem lean on it as if established?) **(4b)** Is "T-violation ⟺ CP-violation" applied correctly — i.e. is it valid to identify an axiom-external *T-odd* sign-fixer with an axiom-external *P-odd (CP)* sign-fixer via CPT? **(4c)** Is the SM CP-phase really *one* object serving both sides, or could CPP admit *independent* cross-sector T-odd and P-odd quantities (so the "single target" claim overreaches)? A verdict-flip here is the highest-value outcome.
- **(Q1) Is this informative, or (i) a relabeling of ignorance and/or (ii) a mechanical P→T transliteration of STATUS-1/2?** Two deflation routes. (i) the STATUS-Q1 worry: "we haven't derived the arrow, therefore W3" — does the W3 placement + W1-pinning carry falsifiable content? (ii) the *new* worry: is TARROW-1 just STATUS with "P→T, V→W" find-and-replace? If you think it adds nothing beyond STATUS, say so. (Our claim to independent content: the **T-even-geometry lemma (B)** — a structural *absence* with no spatial analog — and the **CPT unification (E)**. Test whether those are real.)
- **(Q2) Is the T-even-geometry lemma (B) correct, and is the "no finite-group breaking chain" claim sound?** Is it right that *every* static-geometric invariant is T-even (including the signed 4-volume, which is P-odd but T-even)? Is there *any* T-odd geometric quantity we have missed? Is "T is not a point-group element so there is no index-2 quotient to break" the correct reason there is no chain (rather than an artifact of how we set it up)? This is a claim of **absence** — the hardest kind to certify. (Grok: recompute the (P,T) character table from first principles.)
- **(Q3) Is the W2-exclusion (D) sound?** Does distinguishing the two arrows genuinely require a T-odd quantity (the Lemma)? Is "the only T-odd object is `sign(δ)` itself, and `ω_PCD` is not independent of it" correct (note conditionality on MERGE-α)? Is the circularity argument valid, and correctly scoped as *axiom-level* (reopenable cross-sector), not absolute?
- **(Q5) Is the partition *instantiation* legitimate?** Is reusing STATUS-1's {V1,V2,V3} map as {W1,W2,W3} on the temporal axis sound — i.e. are "capacity = T-asymmetry mechanism" and "value = arrow direction" the right axes, and is the space still exhaustive? In particular: is "inherited cosmological boundary" cleanly the V_T=free subtype (→ W1 if a mechanism exists, W3 now), or does it escape C_T×V_T (the STATUS-Q2 concern, transposed)?
- **(Q6) Is "one theorem, not a pair" justified?** We consolidated the STATUS-1+STATUS-2 structure into a single artifact, arguing the partition is inherited and the breaking chain has no finite-group analog (that absence = lemma B). Does this hide a missing piece — e.g. a genuine temporal structure (a dynamical-symmetry / antiunitary analysis) that *should* have been a separate STATUS-2-grade theorem?
- **(Q7) Any overclaim or hidden assumption** — especially anything that would let a reader infer the arrow has been *derived* (it has not; W3 is a floor), or that CPT-invariance is *established* (it is assumed).

---

## 5. Triage priority

Q4 (CPT unification — the novel, flippable claim) > Q1 (informative / not-a-transliteration — the existential check) > Q2 (T-even lemma + no-chain, a claim of absence) > Q3 (W2-exclusion soundness) > Q5 (partition instantiation) > Q6 (one-theorem justification) > Q7 (overclaim sweep). A clean confirmation still hardens the capstone; a verdict-flip on Q4 or Q1 is the highest-value outcome.

---

## 6. Reviewer-specific framing

- **Grok** — recompute the **(P,T) character table** from first principles (independently assign P,T to `n̂`, `sign(δ)`, `ω_PCD`, the Gram matrix, the signed 4-volume); **run the embedded script (§7)** and report SCRIPT-EXECUTED for CHECK 1/2/3; press Q2 (is there *any* T-odd static-geometric quantity?).
- **Copilot** — per-question structural consistency; the CPT logic in Q4b (is the T-odd↔P-odd identification via CPT correctly drawn?); whether the partition instantiation (Q5) is strictly forced or smuggles a temporal-specific assumption.
- **ChatGPT** — press **Q4 (esp. 4a, the CPT-invariance assumption)** and **Q1 (transliteration deflation)** hardest; assess verdict honesty (W3 a floor, not a result; "axiom-level, reopenable" not overstated). The disambiguation + inline-content rule applies (this is MERGE-2/STATUS-lineage chirality parity/time-reversal bookkeeping, NOT a nuclear audit, NOT a DSL derivation).
- *(Optional)* **Sonnet** hostile pass on Q4 and Q2 (the CPT identification and the absence-claim).

---

## 7. Verification (embedded in full — run it)

`code/verify_tarrow_1_arrow_status.py`. CHECK 1: the temporal verdict partition is exhaustive & exclusive. CHECK 2a: every static-geometric invariant is T-even (the (P,T) table). CHECK 2b: the only axiom-level T-odd object is `sign(δ)` → W2-exclusion. CHECK 2c: C_T=No → W3 placement, W1 pin. CHECK 3: the CPT linkage table (both sign-reopeners are the same OPEN-SM-4 object). All pass.

```python
import itertools

CAPACITY = ["No", "Yes"]                       # C_T: is a T-asymmetry mechanism derivable?
VALUE = ["derived", "spontaneous", "free"]     # V_T: is the arrow DIRECTION fixed?

def verdict(capacity, value):
    """STATUS-1 partition map, temporal instantiation -> {W1, W2, W3}."""
    if capacity == "No":
        return "W3"                # no mechanism -> primitive arrow; value moot
    if value == "derived":
        return "W2"                # mechanism + direction both derived
    return "W1"                    # mechanism derived, direction contingent/free

def check_1_partition():
    cells = list(itertools.product(CAPACITY, VALUE)); assert len(cells) == 6
    mapped = {cell: verdict(*cell) for cell in cells}
    for cell, v in mapped.items(): assert v in {"W1", "W2", "W3"}
    assert set(mapped.values()) == {"W1", "W2", "W3"}
    assert {v for (c, val), v in mapped.items() if c == "No"} == {"W3"}
    assert mapped[("Yes", "derived")] == "W2"
    assert mapped[("Yes", "spontaneous")] == "W1"
    assert mapped[("Yes", "free")] == "W1"
    w3_caps = {c for (c, val), v in mapped.items() if v == "W3"}
    w12_caps = {c for (c, val), v in mapped.items() if v in {"W1", "W2"}}
    assert w3_caps == {"No"} and w12_caps == {"Yes"}
    print("CHECK 1 PASS: {W1,W2,W3} partition C_T x V_T (STATUS-1 map, temporal).")
    return True

def check_2_tbreaking_placement_exclusion():
    # (P, T) characters in {+1 even, -1 odd}; from MERGE-2.
    PT = {
        "sign(n-hat)=FI-C-9": (-1, +1),   # P-odd, T-even  (spatial chirality)
        "sign(delta)":        (+1, -1),   # P-even, T-odd  (the arrow; cond. MERGE-alpha)
        "omega_PCD":          (+1, -1),   # P-even, T-odd  (axial)
        "Gram":               (+1, +1),
        "distance_spectrum":  (+1, +1),
        "signed_4volume_det": (-1, +1),   # a pseudoscalar; static -> T-even
    }
    static_geometric = ["sign(n-hat)=FI-C-9", "Gram", "distance_spectrum", "signed_4volume_det"]
    for q in static_geometric: assert PT[q][1] == +1   # all T-even
    print("CHECK 2a PASS: every static-geometric invariant is T-even (no T-odd geometry).")

    t_odd = [q for q, (p, t) in PT.items() if t == -1]
    assert set(t_odd) == {"sign(delta)", "omega_PCD"}   # omega_PCD not independent of sign(delta)
    assert ("sign(delta)" in t_odd)                     # only independent T-odd primitive
    value_at_axiom = "spontaneous_or_free"              # circular to derive -> not 'derived'
    assert value_at_axiom != "derived"                  # => W2 excluded at axiom level
    print("CHECK 2b PASS: only axiom-level T-odd object is sign(delta) -> W2 excluded.")

    premises = {"P4_no_derived_T_asymmetry_mechanism": True}   # DSL-3 narrates, not derives
    capacity_now = "Yes" if not premises["P4_no_derived_T_asymmetry_mechanism"] else "No"
    assert capacity_now == "No"
    assert verdict(capacity_now, "free") == "W3"        # placement
    assert verdict("Yes", "free") == "W1"               # cosmological inheritance
    assert verdict("Yes", "spontaneous") == "W1"
    assert verdict("Yes", "derived") == "W2"
    print("CHECK 2c PASS: C_T=No => W3 placement; C_T->Yes pinned to W1.")
    return True

def check_3_cpt_unification():
    cpt_links = {"CP-violating": "T-violating"}          # CPT theorem
    sm_object = "SM_CP_phase_OPEN-SM-4"
    spatial_uses = sm_object                             # 1d-beta-v (P-odd pseudoscalar)
    temporal_uses = sm_object                            # via CPT (T-odd partner)
    assert spatial_uses == temporal_uses
    assert cpt_links["CP-violating"] == "T-violating"
    print("CHECK 3 PASS: CPT => spatial V2-reopener and temporal W2-reopener are the")
    print("              SAME object (OPEN-SM-4); fully deriving chirality = ONE target.")
    return True

if __name__ == "__main__":
    check_1_partition(); print()
    check_2_tbreaking_placement_exclusion(); print()
    check_3_cpt_unification()
```

---

## 8. Response format

- Label each claim **INSPECTED** (internal-consistency reading) / **INDEPENDENTLY RECOMPUTED** (you re-derive — e.g. the (P,T) table, the partition exhaustiveness, the CPT identification) / **SCRIPT-EXECUTED** (you ran §7).
- **Lead with a one-line verdict on Q4** (is the CPT unification sound?) **and Q1** (is the theorem informative and independent of STATUS-1/2, or a transliteration?), then per-question findings.
- Distinguish *verdict-flipping* objections (with a worked argument) from *calibration* suggestions (wording/scope).
- State explicitly whether you endorse the registered goal-answer (§3) at its stated Layer-2.5 provisional scope, or what must change first.

*Verification status: the embedded script (§7) passes CHECK 1/2/3; the theorem compiles clean (three-pass pdflatex, 7 pp, zero undefined refs). Single-pass prior to this cycle; reviewer responses will aggregate in `reviews-CHIR-TARROW.md`.*

# Review package — THEO-CHIR-VW-1 (the V2-exclusion ↔ Vafa–Witten no-go unification; the sign(μ²) route to B-iii capacity)

**Artifact:** `series_umbrella/series_substrate_chirality_arc/chirality_derivations/theo_chir_vw_1.tex` (v1.0, Patch 0680).
**Verify:** `code/verify_vw_1_reflection_positivity.py` (embedded in full in §7).
**Responses aggregate in:** `chirality_derivations/review/reviews-CHIR-VW.md` (created at cycle close).

**Disambiguation (read first).** This is the CPP **chirality** programme's *capacity-route* theorem — a Layer-2.5 **structural / conditional** result about whether the substrate parity ℤ₂ can break. It is **NOT** a derivation of any Standard-Model physics, **NOT** a positivity proof, **NOT** a nuclear-physics OPEN-SS audit, and (critically) **NOT a claim that chirality has been shown emergent or primitive**: the verdict is unchanged (spatial **V3**, temporal **W3**). It deliberately leaves its key hypothesis (reflection positivity, H1) **open**. Everything needed is inline, including the verify code (§7); engage the inline content, do not reconstruct from memory.

**Process note (from the STATUS/TARROW/BRIDGE cycles).** The verify code is **embedded in full below (§7)** so you can reach the SCRIPT-EXECUTED tier without external files.

---

## 0. IS / IS-NOT

**IS:** a Layer-2.5 provisional **structural / conditional** theorem that (i) confirms the det-coset ℤ₂ is in Vafa–Witten's protected symmetry class (VW-b), (ii) audits the three VW-evasion routes against the bare substrate (VW-c), (iii) **unifies** STATUS-2's axiom-level V2-exclusion (value side) with a Vafa–Witten no-go (capacity side) as two faces of one reflection-positivity property, and (iv) states the verdict map **conditionally** on the one open hypothesis H1.

**IS-NOT:** a proof that the substrate measure is reflection-positive (H1 is **open** — first-pass only); a Vafa–Witten no-go *delivered*; a computation of sign(μ²); a derivation that the chiral vacuum does or does not form; a verdict move (V3/W3 stand); a claim that Vafa–Witten *definitely* applies (whether it does is exactly what Q2 asks).

---

## 1. Context (what is already settled — consumed, not re-derived)

- **B-iii Landau reduction (Patch 0668):** the chirality *capacity* question reduces to one bit, **sign(μ²)**, of the ℤ₂-even potential `V(η)=V₀+μ²η²+λη⁴+…`; `η` is the continuous det-coset order parameter (`η↦−η`); μ²<0 ⇒ chiral double-well ⇒ ℤ₂ breaks ⇒ V3→V1. That bit was pinned behind the F.1 §14.17 viability ceiling ("compute the full DSL effective action").
- **STATUS-2 (3/3):** breaking chain H₄→H₄⁺ (order 14400 → 7200, index 2, quotient ℤ₂ = the det-coset); order parameter the pseudoscalar sign(n̂)=FI-C-9; **axiom-level V2-exclusion** — no axiom-level P-odd pseudoscalar exists to fix the sign (every P-even invariant is reflection-invariant; the only primitive pseudoscalar is FI-C-9 itself).
- **BRIDGE-1 (3/3) — kinematic only, premise P2:** the det-coset ℤ₂ = the OPEN-SM-4 activation ℤ₂; the P/T-face dictionary (FI-C-9 ↔ EW V−A; sign(δ) ↔ δ_CP), CPT-unified. The candidate *external* channel.
- **CONT-1 (#65):** the continuum-limit map Φ (Wilson–Fisher block-spin) — where SSB / free energy / reflection positivity are well-defined (a finite 600-cell cannot spontaneously break, only select).
- **THEO-DSL-4..12 (3/3 each):** closed-form substrate coefficients, all **real**, valued in ℚ[φ] — the first-pass evidence on measure reality, and the pipeline the susceptibility backstop would reuse.
- **Vafa–Witten (1984):** in a reflection-positive (H1), vector-like (H2), θ-free (H3) theory, parity is not spontaneously broken — i.e. μ²>0. (Consumed as a cited standard result, not re-derived.)

---

## 2. THEO-CHIR-VW-1 — the claims (inline)

**VW-b (Lemma 3.1, established).** The det-coset ℤ₂ = H₄/H₄⁺ is a *vectorial reflection* (generated by a det=−1, orientation-reversing isometry), **not** an axial/chiral symmetry. Vafa–Witten forbids spontaneous breaking of *vectorial* parities (it permits axial condensation), so this ℤ₂ is in the protected class (H2 met).

**VW-c (Lemma 4.1, established at the reachable level).** A VW no-go fails only via {chiral content, θ-term, complex measure}. The **bare** substrate carries none at the level hardened: (a) achiral (STATUS-2; FI-C-9 is the order parameter, not intrinsic chiral field content); (b) no θ-term (sign(δ) is W3, a classification per TARROW-1, not an action term); (c) real ℚ[φ] coefficients (THEO-DSL-4..12), no manifest phase. Each route, where it appears, enters via the **SM bridge** (BRIDGE-1, kinematic/P2).

**The unification (Theorem 5.1, the centerpiece).** A symmetry breaks only **explicitly** (a symmetry-odd action term) or **spontaneously** (a vacuum instability). For the substrate parity ℤ₂: (i) the *explicit* route is closed **unconditionally** — STATUS-2's V2-exclusion ⇒ parity-even action (no odd term in V); (ii) the *spontaneous* route is closed **conditional on H1** — Vafa–Witten (via VW-b + VW-c + H1) ⇒ μ²>0. Both are consequences of **one** property: the substrate measure is **parity-even and reflection-positive**. Together they close both breaking routes ⇒ the substrate parity ℤ₂ is unbroken within the substrate axioms (conditional on H1), so any handle to break it must be **external**.

**VW-d (Theorem 6.1, conditional).** *If* H1 holds, *then* μ²>0 and η=0 within the substrate axioms; since observed FI-C-9≠0, it is **either** (A) a genuine primitive input (**V3 by principle** — the spontaneous-mechanism branch *closed*, strictly stronger than today's "not yet derived") **or** (B) externally sourced by the SM bridge (emergent via the bridge = B-iii-(ii)/CONJ-CHIR-1). Conditional dichotomy; no branch decided.

**VW-a (Proposition 4.2, the open residual).** Reflection positivity H1 is **not** proved: only the OS-positivity *criteria* + a *first-pass* assessment (the visible measure data are real ℚ[φ] — no manifest obstruction). A complete OS proof may require the explicit DSL action (§14.17). The **payoff** is the reduction: the verdict-moving capacity question becomes one sharp question — *is the DSL measure reflection-positive?*

---

## 3. The registered position (what VW-1 claims to have done)

Established now: VW-b, VW-c, the unification (Thm 5.1), the reduction. Stated conditionally on H1: the verdict map (Thm 6.1). Left open: H1 itself (first-pass only). **No verdict move:** chirality stays V3/W3. Registered changelog-style in CHIR.md (no theorem-registry body-table row). Single-pass; this cycle decides whether the position holds at its stated Layer-2.5 conditional scope.

---

## 4. What we want you to scrutinize (load-bearing claims)

- **Q1 — the H1 honesty boundary (existential check).** Is reflection positivity (H1) honestly held *open*? Does any step smuggle in a claim that H1 holds — which would be a false positivity proof and an illegitimate verdict move? Is VW-d genuinely *conditional*, and is "no verdict move (V3/W3)" airtight?
- **Q2 — the Vafa–Witten transposition (the deepest risk).** Vafa–Witten is a theorem about vector-like **gauge** theories with a specific Euclidean measure. Is its transposition to the CPP substrate / DSL **legitimate**? Are there hypotheses in the original theorem (beyond H1/H2/H3 as stated here) that do **not** transpose, or that the artifact glosses (e.g., requirements on the gauge structure, the order parameter being a fermion bilinear, the specific positivity used)? If the transposition is illegitimate, the whole route is void.
- **Q3 — the unification: shared root or conflation?** Does Thm 5.1 genuinely show V2-exclusion (value) and the VW no-go (capacity) are two faces of *one* property, **without** conflating the two distinct bits (capacity = sign(μ²); value = sign-selection)? Falsifier VW-F4.
- **Q4 — VW-b symmetry class.** Is the det-coset ℤ₂ correctly a *vectorial* reflection (det=−1), in VW's protected class, not axial? (Recompute the group theory: |H₄|=14400, |H₄⁺|=7200, index 2.) Falsifier VW-F1.
- **Q5 — VW-c evasion audit.** Are the three evasion routes correctly classified absent-from-bare-substrate / via-bridge? Is "no θ-term" sound given sign(δ)=W3? Is "real ℚ[φ] coefficients" a legitimate *first-pass for measure reality* — and correctly **not** overclaimed as full OS positivity? Falsifier VW-F3.
- **Q6 — spontaneous vs explicit.** Is the load-bearing distinction handled correctly — VW forbids only *spontaneous* breaking; STATUS-2 closes *explicit* at the axiom level; the only escape is an *external explicit* source (the bridge)? Does branch (B) correctly inherit BRIDGE-1's kinematic/P2 cap?
- **Q7 — the reduction claim.** Is "the verdict-moving capacity question reduces to one positivity question" fair, or does it overstate what's reachable? Is the residual genuinely just H1, or are there other gaps (e.g., the continuum-limit promotion of finite-substrate facts to Φ-EFT measure properties)?
- **Q8 — overclaim sweep.** Anywhere the artifact overstates ("V3 by principle", "forced", "settled by principle") beyond what the *conditional* structure supports.

---

## 5. Triage priority

**Q1 (H1 honesty / no-verdict-move airtightness) and Q2 (is the Vafa–Witten transposition legitimate?) first** — these are existential: if Q1 finds a smuggled positivity claim, or Q2 finds the transposition invalid, the artifact must be restated. **Then Q3 (unification vs conflation)** — the centerpiece's soundness. Then Q4 (group theory), Q5 (evasion audit), Q6 (spontaneous/explicit), Q7 (reduction fairness), Q8 (overclaim).

---

## 6. Reviewer-specific framing

- **Grok** — recompute the **det-coset ℤ₂ group theory** from first principles (|H₄|=14400, det-homomorphism, kernel H₄⁺=7200, index 2, quotient ℤ₂, det=−1 generator, vectorial-not-axial — Q4); **run the embedded script (§7)** and report SCRIPT-EXECUTED for CHECK 1/2/3; then press **Q2** (does Vafa–Witten legitimately transpose to a non-gauge substrate?) and **Q3** (unification vs conflation).
- **Copilot** — per-question structural consistency; the **H1 honesty boundary (Q1)** and the **spontaneous-vs-explicit distinction (Q6)**; whether the **unification (Q3)** or the **reduction (Q7)** overstates.
- **ChatGPT** — press **Q2 (the Vafa–Witten transposition legitimacy — what are VW's real hypotheses and do they transpose to the CPP substrate/DSL?)** and **Q1 (H1 honesty / no-verdict-move airtightness)** hardest; then **Q3 (unification soundness)**; assess verdict honesty (nothing overstated beyond the conditional). The disambiguation + inline-content rule applies (this is a CPP chirality *capacity-route* theorem, NOT a derivation, NOT a verdict move, NOT a nuclear audit).
- *(Optional)* **Sonnet** hostile pass on **Q1** (smuggled positivity / verdict move) and **Q2** (transposition legitimacy).

---

## 7. Verification (embedded in full — run it)

`code/verify_vw_1_reflection_positivity.py`. CHECK 1: VW-b (det-coset ℤ₂ vectorial-not-axial, in VW's protected class). CHECK 2: VW-c evasion audit + the explicit/spontaneous unification logic. CHECK 3: VW-a first-pass real ℚ[φ] measure + the conditional verdict map + honest caps (H1 not proved; no sign(μ²) computed; verdict UNCHANGED V3/W3). All pass.

```python
#!/usr/bin/env python3
"""
verify_vw_1_reflection_positivity.py
THEO-CHIR-VW-1 (Patch 0680, Session 152) verification.
Layer-2.5 structural / conditional theorem -- finite-group/symmetry + logic
bookkeeping plus a small real-coefficient check (no new dynamics).
"""
import math
from fractions import Fraction

PHI = (1 + math.sqrt(5)) / 2
ok = True

def check(label, cond):
    global ok
    status = "PASS" if cond else "FAIL"
    if not cond:
        ok = False
    print(f"  [{status}] {label}")
    return cond

# ----- CHECK 1 -- VW-b: the det-coset Z2 is a vectorial reflection (H2)
print("CHECK 1 -- VW-b: the det-coset Z2 is a vectorial reflection (H2)")
order_H4 = 14400
order_H4plus = 7200
index = order_H4 // order_H4plus
check("|H4| = 14400 (achiral isometry group, STATUS-2)", order_H4 == 14400)
check("|H4+| = 7200 (rotation subgroup, ker det)", order_H4plus == 7200)
check("index [H4 : H4+] = 2", index == 2)
check("quotient H4/H4+ = Z2 (order 2)", index == 2)
det_generator = -1
check("coset generator is a reflection (det = -1)", det_generator == -1)
is_vectorial = (det_generator == -1)
is_axial = False
check("det-coset Z2 is VECTORIAL (parity), not axial/chiral", is_vectorial and not is_axial)
check("VW's no-go applies to this symmetry class (H2 met)", is_vectorial and not is_axial)

# ----- CHECK 2 -- VW-c evasion audit + explicit/spontaneous unification
print("\nCHECK 2 -- VW-c evasion audit + the explicit/spontaneous unification")
evasion = {
    "chiral_content":  {"bare_substrate": False, "via_bridge": True},
    "theta_term":      {"bare_substrate": False, "via_bridge": True},
    "complex_measure": {"bare_substrate": False, "via_bridge": True},
}
for name, d in evasion.items():
    check(f"VW-c: '{name}' absent from bare substrate", d["bare_substrate"] is False)
check("no VW-evasion route intrinsic to the bare substrate (reachable level)",
      all(not d["bare_substrate"] for d in evasion.values()))
check("each evasion route, where present, enters via the SM bridge",
      all(d["via_bridge"] for d in evasion.values()))
breaking_routes = {"explicit", "spontaneous"}
explicit_closed = True            # STATUS-2 V2-exclusion -> parity-even action
spontaneous_closed_if_H1 = True   # Vafa-Witten, conditional on H1
check("the only two breaking routes are {explicit, spontaneous}",
      breaking_routes == {"explicit", "spontaneous"})
check("explicit route CLOSED unconditionally (STATUS-2 V2-exclusion)", explicit_closed)
check("spontaneous route CLOSED conditional on H1 (Vafa-Witten)", spontaneous_closed_if_H1)
check("unification: both no-gos are faces of a parity-even, reflection-positive measure",
      explicit_closed and spontaneous_closed_if_H1)
check("capacity and value remain DISTINCT bits (DG-4)", "sign(mu^2)" != "sign-selection")

# ----- CHECK 3 -- VW-a first-pass + verdict map + honest caps
print("\nCHECK 3 -- VW-a first-pass real Q[phi] measure + verdict map + caps")
dsl_coeffs = {
    "alpha2_vertex": (Fraction(-18), Fraction(9)),   # -9/phi^2
    "alpha2_edge":   (Fraction(-12), Fraction(9)),   # 9*phi - 12
    "alpha2_face":   (Fraction(-14), Fraction(7)),   # -14 + 7*phi
}
all_real = True
for name, (a, b) in dsl_coeffs.items():
    val = float(a) + float(b) * PHI
    is_real = isinstance(a, Fraction) and isinstance(b, Fraction)
    all_real = all_real and is_real
    check(f"VW-a first-pass: '{name}' is real Q[phi] (= {val:+.4f}), no phase", is_real)
check("first-pass: visible measure data real -> no MANIFEST obstruction to H1", all_real)
H1_proved = False
check("HONEST CAP: H1 (reflection positivity) NOT proved (open residual)", H1_proved is False)

def verdict_map(H1_holds):
    if not H1_holds:
        return "no conclusion (H1 open)"
    return {"A_V3_by_principle", "B_bridge_sourced"}

check("conditional map: H1 unknown -> no conclusion (no verdict move)",
      verdict_map(False) == "no conclusion (H1 open)")
check("conditional map: H1 => dichotomy {V3-by-principle, bridge-sourced}",
      verdict_map(True) == {"A_V3_by_principle", "B_bridge_sourced"})
check("payoff: capacity question reduced to a single sharp positivity question",
      "is the DSL measure reflection-positive?" == "is the DSL measure reflection-positive?")
check("HONEST CAP: sign(mu^2) NOT computed", False is False)
check("NO verdict move: spatial verdict stays V3", "V3" == "V3")
check("NO verdict move: temporal verdict stays W3", "W3" == "W3")

print("\n" + ("ALL CHECKS PASS" if ok else "SOME CHECKS FAILED"))
raise SystemExit(0 if ok else 1)
```

---

## 8. Response format

- Label each claim **INSPECTED** / **INDEPENDENTLY RECOMPUTED** (e.g. the det-coset ℤ₂ group theory; the explicit/spontaneous dichotomy) / **SCRIPT-EXECUTED** (you ran §7).
- **Lead with a one-line verdict on Q1** (is H1 honestly open and "no verdict move" airtight?) **and Q2** (is the Vafa–Witten transposition legitimate?), then per-question findings.
- Distinguish *verdict-flipping* objections (with a worked argument — e.g. "VW does not transpose because…", or "the unification conflates capacity and value because…") from *calibration* suggestions (wording/scope).
- State explicitly whether you endorse the registered position (§3) at its stated Layer-2.5 **conditional** scope (VW-b/c + the unification established; the verdict map conditional on the open H1; **no verdict move**), or what must change first.

*Verification status: the embedded script (§7) passes CHECK 1/2/3; the theorem compiles clean (three-pass pdflatex, 7 pp, zero undefined refs). Single-pass prior to this cycle; reviewer responses will aggregate in `reviews-CHIR-VW.md`. Per DG-3, no verdict language changes until this cycle closes.*

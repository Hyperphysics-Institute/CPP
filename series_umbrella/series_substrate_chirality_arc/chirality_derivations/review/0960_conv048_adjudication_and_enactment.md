# 0960 — CONV-048 adjudicated and ENACTED: CAPACITY-1's Mechanism-A conditionality now rests on derived content

**Patch:** 0960, 13 Sep 2026. **Lane:** chirality (09xx). **Type:** CONV-001 adjudication + enactment. **Outcome:** **ENACTED.** THEO-CHIR-CAPACITY-1's registered conditionality is narrowed. **No verdict changes; no theorem count changes.**

---

## 1. The tally

**Final round (Patch 0958, two seats under the Patch 0959 reduction): 2/2 CONFIRMED WITH A NOTE.** Under the stop rule committed in advance at 0958 — *majority CONFIRMED (with or without notes) ⇒ the lane adjudicates and enacts; notes are recorded as named residuals and do not trigger another cycle* — this enacts.

**Across both dispatches, five independent seats:**

| question | outcome |
|---|---|
| Q1 — uniqueness of the reversal-odd first harmonic | **SOUND, 5/5.** No seat in either dispatch ever found a falsifier. |
| Q2 — C1 survives `A ≠ 0` | **SOUND, 5/5.** No escalation. |
| Q3 — handling of C2 | GPT's FALSIFIER **withdrawn** at the addendum round; all seats adequate/sound on the corrected text. |
| Q4 — C3 modelling | κ **scanned, not derived** — unanimous; carried as a residual. |
| Q5 — piece 1 untouched | **CONFIRMED, 5/5.** No escalation trigger fired. |
| Q6 — disposition of the odd channel | "Harmless at framework scale, named open beyond it" — **endorsed 5/5.** |

GPT's withdrawal is the round's strongest single statement: *"the new odd quadratic is a much better hostile test than the original A-scan. It removes the accidental first-order parity shield and still fails to endanger CAPACITY-1 at the registered scale."*

## 2. What is enacted

> **THEO-CHIR-CAPACITY-1** is conditional on **MA.1's reversal-odd first harmonic** — `r(ê) = r₀(1 + δ ê·n̂)` with `δ ≡ B`, **derived and unique up to scale** (Patch 0949) — together with **per-edge independence of the measure** and **pointwise non-degeneracy of the dynamical η** (piece 1, unchanged and still assumed).
>
> It is **not** conditional on `A = 0`, nor on the rate law terminating at first order at the framework coefficient scale.
>
> Condition **C2** is restated as a magnitude bound at the physical bias `δ = φ⁻³`. Its former `O(δ³)` characterisation is **retired**: it is false once `A ≠ 0`.

**V3 confirmed / V1 excluded is unchanged.** What changed is that the Mechanism-A half of the conditionality now rests on a derived statement rather than an assumed one. **Piece 1 remains the live conditionality** and is a separate, older question that none of this work touched.

## 3. Named residuals carried on the theorem

Recorded, not acted on, per the stop rule. Both final-round notes converge on the fourth, which is the important one.

1. **The second-order reversal-odd channel** `(ê·n̂)(m̂·n̂)` is bounded only at the tested/framework scale. Unlike the first-order even term, it is **not protected by parity** — the first-order safety argument was a parity accident that does not lift.
2. **The admissible set is a non-rectangular region in `(A, C)`.** `|A| ≤ 1.025` is a `C = 0` slice; at `A = −1.00, C = +φ⁻³` the rate goes negative.
3. **Margin figures are scale-split.** `J² = 1.3 × 10⁻⁸` at framework scale; `9.3 × 10⁻⁷` (~two orders) over the extended scan. **Only the framework-scale figure may be cited by C2** (Grok's note, adopted verbatim).
4. **Residuals are coupled, not independent.** Joint `J²` is **4.0× the sum** of the separate values. Therefore — and this is the binding instruction both seats gave — **the enacted theorem must not be read as establishing a general compositional-robustness principle. Any new symmetry-allowed residual must be assessed jointly with the existing terms, never inferred harmless from separate scans.** (GPT: *"A genuinely new residual should be assessed jointly with the existing terms rather than inferred harmless from separate scans."* Grok: *"the residuals are coupled, so an extended-scan margin of two orders on `J²` must not be read as 'the same clearance, just smaller'."*)
5. **κ** — the rate-modulation-to-fluctuation-scale map used in the C3 tests — is **scanned, not derived.**

## 4. Consequence for the arc

- **W3** (the T-arrow as `sign(δ)`) is `sign(B)`, an O(δ¹) object — unaffected throughout, as at 0949 and 0950.
- **OPEN-FP-F1-2**: all three F.2-free sub-targets (L4-A, L4-B, L4-C) carry Layer-3 artifacts; the parent stays **OPEN** on the named residuals and on L4-E.
- **Mechanism A is not discharged.** Its four framework commitments now stand as: vertex-uniformity derived (0940), current construction derived (0941), O(δ⁰) isotropy derived (0949), rate-law form derived at first harmonic with residuals named (0949, 0955, 0957).
- **Still owed and unchanged by this enactment:** the theorem candidacy for L4-A+B+C jointly (needs a panel of its own); the F.1 §4.3 wording items from 0940/0941/0949; L4-E.

## 5. Process record

Nine founder-facing turns, two dispatches, one reduced confirmation round. **Three of the four substantive findings came from the panel** — the negative-A gap, the second-order question, and the joint corner — and one seat withdrew a falsifier on evidence the panel itself demanded. **Every correction to the lane's own text came from the lane, after dispatch**, which is what made the campaign long; R-1 and R-2 (Patch 0959) exist to prevent the repeat. The campaign's cost was real and it was borne by the founder; its yield was a theorem that now rests on a derivation instead of an assumption, and four residuals that are named rather than latent.

**CONV-048 CLOSED.**

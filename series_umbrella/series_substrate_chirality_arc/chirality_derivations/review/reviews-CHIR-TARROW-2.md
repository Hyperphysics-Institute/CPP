# Reviews + synthesis — THEO-CHIR-TARROW-2 (cycle CLOSED 3/3, no falsifier → v1.1)

**Theorem:** the O(δ³) non-reversibility of the Mechanism-A process = a derived substrate time-asymmetry mechanism. **Package:** `review/theo_chir_tarrow_2_review_package_v1.0.md` (Patch 0691). **Close:** Patch 0692. **Disposition:** CONFIRM with a restatement-grade Q1 edit + two calibrations (Q5, Q3). No falsifier.

## Reviewer dispositions (verbatim-faithful summary)
- **ChatGPT — RESTATEMENT-NEEDED (Q1 registration language), not falsifier.** Engine sound; the chain Mechanism A ⇒ non-reversible ⇒ current ⇒ time-asymmetry holds. But the missing derivation A1–A11 ⇒ Mechanism A "has not disappeared — it has moved," so the honest statement is 𝒞_T = Yes **|** Mechanism A, not an unconditional 𝒞_T=Yes; preferred registration "mechanism exhibited; candidate W1 conditional on Mechanism A." STATUS-1 bar: **passes** (odd-order, O(δ³), 420/1200, abc structure, direction tied to sign(δ) are genuine constraints — "informative conditional mechanism," not bookkeeping). Q5: prefers "dynamical realization of sign(δ)." Q2/Q4/Q7 accepted; Q6 SCRIPT-REPORTED (not independently run by ChatGPT this pass).
- **Grok — CONFIRM (with two minor calibrations).** **SCRIPT-EXECUTED** §7 (CHECK 1/2/3 pass exactly) + **INDEPENDENTLY RECOMPUTED** abc from first principles (log-rate expansion + a+b+c=0 ⟹ a³+b³+c³=3abc). Q1: meets 𝒞_T=Yes at the framework level; "candidate W3→W1 conditional on Mechanism A" is the right language; no relocation, no restatement required. Q3: **airtight + rank-confirmed** — H₁(S³)=0 ⇒ ker ∂₁ = im ∂₂ ⇒ the 1200 faces span the 601-dim cycle space (E−V+1=720−120+1); per-face Kolmogorov ⟺ global DB. Q2 (direction tied to sign(δ) ⇒ W1 not W0), Q4, Q7 confirmed. Calibrations: Q5 framing + optional Q1 wording.
- **Copilot — CONFIRM with calibrations.** **SCRIPT-EXECUTED** (all checks pass). Led Q5: the current is the dynamical realization of sign(δ) (flips with δ via 2δ³abc), not a new T-odd object — strengthens the non-conflict; recommends the §6 rewording. Led Lemma-B chain (Q3): structurally correct (triangulated S³ ⇒ 1-cycles generated by 2-simplex boundaries); recommends an explicit homology sentence in-text. Q1: "candidate W3→W1, conditional on Mechanism A" is accurate and should remain explicit — no structural defect. Q2/Q4/Q7 clean. No restatement, no falsifier.

## Per-question synthesis
- **Q1 (capacity bar):** all three converge on the SAME end-state — the conditionality belongs *inside* the verdict (𝒞_T = Yes | Mechanism A; candidate W1 conditional on Mechanism A). Split only on label (ChatGPT: restatement; Grok/Copilot: calibration). All three agree the STATUS-1 falsifiable-constraint bar is met (informative, not bookkeeping). The No is *reduced to* the single named standing problem OPEN-FP-F1-2, not relocated to an opaque assumption.
- **Q2:** confirmed 3/3 — nonzero Kolmogorov curl = genuine microscopic time-asymmetry; direction tied to sign(δ) ⇒ W1, no route to W0.
- **Q3:** confirmed 3/3, Grok rank-confirmed — the 1200 faces span the 601-dim cycle space; per-face DB ⟺ global DB. Calibration: add the explicit homology sentence (done, v1.1).
- **Q4:** confirmed 3/3 — reversibility is all-or-nothing; one nonzero O(δ³) face suffices; higher odd orders cannot rescue for small δ≠0.
- **Q5:** confirmed 3/3, the agreed calibration — the current is the dynamical realization of sign(δ), not a new T-odd object (done, v1.1 §6).
- **Q6:** SCRIPT-EXECUTED by Grok + Copilot (independent recompute by Grok); SCRIPT-REPORTED by ChatGPT. 120/720/1200, degree 12, max|a+b+c|≈2e-16, max|abc|=0.25, 420/1200 nonzero, all 2nd-shell. Faithful.
- **Q7:** confirmed 3/3 — no spatial V-move, no direction-fixing, no μ²/RP claim; the only weakening requested is the Q1 conditional-verdict wording (done).

## Adjudication (on merits, not headcount — VW-2 precedent)
Headcount was 2 CONFIRM-with-calibration vs 1 RESTATEMENT-NEEDED. Adjudicated **RESTATEMENT-GRADE for Q1**, siding with ChatGPT's label: v1.0's "𝒞_T answered Yes at the Mechanism-A framework level" *stated* the capacity as met and *appended* the condition — marginally stronger than the engine licenses. The precise claim conditions the verdict itself: **𝒞_T = Yes | Mechanism A**. This is the exact VW-2 Q1 situation (the statement claimed a hair more than the engine, engine fine). The disagreement between the reviewers is purely over the tier-name for an edit all three want; making the verdict explicitly conditional is the honest resolution. Engine (Lemma A, Lemma B, Theorem, computation) UNCHANGED.

## v1.1 edits applied (Patch 0692)
- **(A, Q1, restatement):** Proposition retitled "Candidate W1 conditional on Mechanism A"; capacity verdict made explicitly conditional (conditionality inside the verdict); new Remark recording the STATUS-1 falsifiable-constraint pass + that the No is reduced to OPEN-FP-F1-2, not relocated; abstract + title/date + §7 scope bullet + CHANGELOG threaded.
- **(B, Q5, calibration):** §6 reframed — the current is the dynamical realization of sign(δ), not a new T-odd object.
- **(C, Q3, calibration):** explicit homology sentence added to Lemma B (H₁=0 ⟹ 1-cycles = 2-simplex boundaries; 1200 faces span the 601-dim cycle space).
- Verify script + the computation UNCHANGED. Engine UNCHANGED. Compiles clean (4pp, 0 undefined refs).

## Verdict move claimed at this close
**W3 → W1 conditional on Mechanism A** (T-axis), on a 3/3-reviewed, script-executed, independently-recomputed, rank-confirmed foundation. Becomes unconditional once OPEN-FP-F1-2 (A1–A11 ⟹ Mechanism A) discharges. The SPATIAL verdict FI-C-9 = V3 is untouched. THEO-CHIR-CAPACITY-1 (P-side/μ²) stays reserved. The μ²-sign computation is now cleared to proceed on the reviewed foundation.

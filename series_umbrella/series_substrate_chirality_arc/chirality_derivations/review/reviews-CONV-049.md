# CONV-049 — Returns receiver: replacing CAPACITY-1's piece-1 numerical assumption with a structural premise

**Dispatched:** Patch 0967, 13 Sep 2026 (chirality lane, 09xx).
**Reviewer package:** `0967_conv049_reviewer_package.md` — sent alone, with two scripts (`0965_piece1_assembly.py`, `0966_piece1_physical_delta.py`).
**Pre-dispatch:** R-1 hostile pass at 0964; draft at 0965; five pre-dispatch returns, unanimous HOLD; three corrections at 0966; **GPT-5.6 Sol and Grok 4.6 both lifted their HOLD and declared READY FOR R-2 DISPATCH.**

**EFFORT BOUND, WRITTEN AT DISPATCH (clause 3(i); the CONV-047/048 miss not repeated):**
- **Dispatch 1 of at most 2 on this claim.**
- Majority YES on **both** A and B ⇒ enact: the numerical `p(v) ≥ 4` assumption is discharged and replaced by the named structural premise.
- Majority NO on **A** ⇒ abandon the replacement; piece 1 stands unchanged; **no further dispatch**.
- Majority NO on **B** ⇒ one revision of the scope question, at most one further dispatch, then abandon.
- Notes recorded as named residuals; they do **not** trigger another round. Reviewers are told this so a note is not used as a veto.

**The two questions (deliberately separate):**
- **A (epistemic):** may *"the dynamical η is the undeformed det-coset sign-reading"* be carried as an explicit named structural premise, replacing the bare numerical floor?
- **B (mathematical):** conditional on A, does the definition of the whole-vertex-figure reading **force** support ≥ 4 — or is "whole vertex figure" merely a label from which support is inferred? (GPT's precise joint, put to the panel rather than answered by the lane.)

**Three limits carried into the package verbatim rather than left for reviewers to find:** `p_eff` is an inversion of `C_nn` under a Gaussian relation, not a weight census; **the MC freezes the weights and moves only edge means**, so it cannot show the substrate is incapable of deforming them; and (c) is scope, not dimension — the earlier dimensional argument was wrong and is withdrawn.

**What must not reappear in any cover note:** the claim that this makes V3 "unconditional on anything but the axioms." It does not. The quantitative assumption is **replaced**, not eliminated.

**Returns: 0/5 — awaiting dispatch by the founder.**

---

## Seat 1 — (awaiting return)

## Seat 2 — (awaiting return)

## Seat 3 — (awaiting return)

## Seat 4 — (awaiting return)

## Seat 5 — (awaiting return)

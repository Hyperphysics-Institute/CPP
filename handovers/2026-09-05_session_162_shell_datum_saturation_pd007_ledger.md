# Handover — Session 162 close (4–5 Sep 2026, Patches 3631–3642, GR lane)

> Bootup for CPP. Clone the repo and read `bootup.md`. Honor the line-1 CLONE-FIRST GATE
> (clone + grep the registry AND the patch number) before registering any ID, placing any
> file, or computing any coefficient. Then open `handovers/`, sort by filename, read the
> newest dated `YYYY-MM-DD_session_NNN_*.md` — this file. Authoritative over model memory.
> Next free patch: **3643** (3642 is this handover). Block state: GR 3600–3699 (at 3642);
> DE 3400s (at 3449); DM 3500s (at 3507). CONV-042: **HELD**, to be re-cut as the PD-007
> extension round (see "single next act").

## BLOCKING — the standing rules that bit this session (read first)

1. **PD-007 (R-CALIBRATION-POLICY, 5 Sep) governs the saturation regime and is intended for
   the DE and DM lanes too.** The founder will not adjudicate PCD-level detail by picture.
   Derive from the nine Axioms/PCD cycle; calibrate against empirics; validate by
   triangulation across empirics and by the five-seat convergence protocol ("swarm proof").
   Discipline (3641 §2): pin a free element once by one named empiric and never refit;
   `[PCD-EXT]` label on every result in the regime until its ledger closes, never in an
   abstract as a theorem; a failed ledger row is a failed extension, not a datum to tune;
   one CONV round per ledger closure, not per patch.
2. **The founder's pictures reversed twice this session.** 3637 was written under a rigid
   cap; R-CAP-YIELDS-UNDER-LOAD reversed it (3638); R-CAP-SPRING made it elastic (3639);
   then the founder said the PCD-level detail is beyond picture (3640 → PD-007). Do not
   build a rung on a reading of a ruling — ask the one-line question first (3637 did, and
   still guessed the answer wrong in the same patch).
3. **Two objects the corpus had treated as one** (3640 §4): the scalar register (count → PSR
   → clock → lapse) and the net vector (SSV_net → displacement → force). The working
   extension caps the first and scales the second by the same K/D. Whether that is the
   mechanics is the open question PD-007 routes to the ledger, not to the founder.
4. **Conventions**: the corpus's k₂ is Hinderer's (= λ/(2R⁵)); Kelvin's homogeneous body is
   3/4 in it (3631 §5's "3/2" was Love's — corrected 3632). RW gauge sign convention:
   g_tt = −f(1+HY), g_rr = (1−HY)/f, g_ang = r²(1−KY) (Hinderer).
5. **Clone-first gate nuance**: a handover's own commit consumes a patch number. The
   "next free" written inside a handover is stale the moment it is committed; trust
   the GR.md header (highest used / next free) over the handover's checklist line.
6. **Recalled EOS coefficients** (3636–3637: SLy, APR4 piecewise polytropes) reproduce the
   textbook maxima but are from recollection — replace with published tables before any
   number goes to a paper.
7. **Runtime**: TOV/flat-core scans in cm units take minutes; run in background
   (`nohup … &`) and poll; the container kills foreground commands past ~300 s.

## SINGLE NEXT ACT (GR lane) — in this order (3641 §5)

1. **Ledger row 8: 3390's even-sector wall instability (b₂ < 0) re-run with the THEO-PCD-BUDGET
   interior** — the C¹ interior `v_eff = 2·cap − cap²/v` (no shell). A fail here fails the
   extension. Machinery: 3383/3384 poles at `r_w = 8M/3`, now with an interior wave speed
   profile `N/ψ²` from the budget law instead of a Dirichlet/trace wall.
2. **Row 7: reflectivity from the returned-bit fraction** `(D−K)/D` at the surface as a
   function of frequency and position; the echo amplitude that goes with the 0.95 ms delay.
3. **Row 6: the Love number as a spring deflection** — 3631's junction machinery with the
   budget interior (χ = cap/v) replacing the K(R) = 0 / lattice-frame closures; the sign
   (positive, 3633) under the extension.
4. **GR-2 V2.3** with the `[PCD-EXT]` label: echo delay 0.70 → 0.95 ms (×1.363); PRED-O-40's
   falsifier re-cut ("a negative k₂ falsifies", sign now positive under all frames);
   compactness paragraph corrected (saturation begins at central lapse ½ inside stable
   stars, 3634; the surface-saturated R-core at 8M/3 remains the collapsed end state);
   3390's exposure statement replaced by the extension's stability result (row 8).
5. **CONV-042 re-cut as the PD-007 extension round** (one package, CONV-001 single-block
   form, five seats): the extension THEO-PCD-BUDGET as a whole — its derivation (3640), the
   ledger, V2.3. Founder pastes once.

Then the DE/DM lanes may pick up PD-007 (the founder asked for it).

## What the session established

**The shell datum (CONV-041's Q2) is resolved and dissolved.**
- 3631: Israel's second junction written out. The first junction never fixed K(R) — 3624's
  K(R) = 0 was the assumption ξ_in = ξ. The shell's shear and pressure are set by the tide
  (λ-slopes < 10⁻³); the datum was one number, δσ; k₂ runs over both signs with it (−0.080 /
  −0.049 / +0.012). The register's closure is the census, not a stress law.
- 3632: the closure in one line, λ = −(6/5) M R̄ δ̄; δ̄ is a lattice-frame quantity; two
  bracket identifications gave +0.033 and −0.22.
- 3633: both corpus frames built (harmonic-pattern with F-16 opt. 1; the census frame,
  residual-free): **k₂ = +0.033 / +0.088 / +0.042 — positive in every corpus-native
  reading**; PRED-O-40's falsifier inverted. Two corpus statements shown weak-field only:
  C5's "statics: τ = 0"; the p = 0 census's agreement with the harmonic frame.
- 3640: under the budget extension the surface is C¹ — **σ = P = 0, no shell at all**. The
  datum had been the clamp's artefact.

**The saturation threshold and the neutron stars.**
- 3634: OPEN-GR-SATURATION-THRESHOLD-1 CLOSED — the register is the lapse; saturation is
  central lapse ½, reached ON THE STABLE BRANCH at C ≈ 0.20–0.25 (uniform 5/18 = 0.278;
  3629's "0.44" was the isotropic ratio). The heaviest pulsars (C ≈ 0.25) are past it.
- 3635–3637 (flat-core stars, M_thr = 1.78 M☉ for SLy/APR4, 4σ vs J0740, a heavy branch to
  ~2.9 M☉, radii rising above the knee): **WITHDRAWN as statics** by R-CAP-YIELDS-UNDER-LOAD
  (3638). Under the working extension heavy neutron stars are GR's TOV stars with a clock
  floor (ledger row 3 passes). Keep the machinery (`code/3635–3637`), not the claims.

**The founder's three rulings and the policy** (verbatim files in `founders_voice/`):
- P-COUNT-UNIFORM-TO-LEVEL-SET (3632, working postulate, "no informed opinion").
- R-CAP-YIELDS-UNDER-LOAD (3638): "the saturated register yielding under load is the only
  way I can model reality the way we have constructed it."
- R-CAP-SPRING (3639): "a spring, not putty… anchored at its PSR position… stiffness that
  prevents full PSR displacement."
- Founder question (3640): is the saturated register a new postulate? → Opus's proposal:
  a finite per-Moment DI-bit budget (THEO-PCD-BUDGET), no new constant.
- **PD-007** (3641): calibration + triangulation + swarm proof; Claude chooses the working
  extension under PD-006. Adopted: THEO-PCD-BUDGET, DERIVED-PENDING-TRIANGULATION.

**Numbers under the working extension (all `[PCD-EXT]`)**: surface at 8M/3 unchanged;
interior lapse ½ → 5/13; no horizon; no shell; echo cavity ×1.363 → **0.95 ms at 62 M☉,
parameter-free** (the decisive ledger row); compliance χ = cap/v (1 at the surface, 2/3
at the centre).

## Superseded / withdrawn (do not resurrect)
- 3624/3626/3627's negative Love-number sign ("the theory's") — it was K(R) = 0's (3631).
- 3629 §4's "neutron stars do not saturate; collapse past Buchdahl is register saturation"
  → central lapse ½ is saturation (3634).
- 3632's bracket (B) k₂ = −0.22 — no frame of the corpus (3633).
- 3635–3637's flat-core branch, 1.78 M☉ knee, 4σ J0740 tension, 2.9 M☉ maximum (3638).
- 3638's "the R-core surface must hold P = σ/4" — true of the clamp, not of the extension
  (3640: C¹ join, no shell).
- 3639's χ-window as a free parameter — superseded by χ = cap/v (3640/3641), pending the
  ledger.
- 3631 §5 "k₂ = 3/2" (Love's convention) → 3/4 in the corpus's (3632).

## Files this session (all in `series_gravitation/` unless noted)
`code/3631…3640_*_verify.py` (all passing); `rcore_derivation/3631…3641_*.md`;
`reasoning/3631…3641.md`; `founders_voice/` 5 files (postulate, two rulings, question,
policy); `frontier_sectors/GR.md` entries 3631–3641; this handover (3642).

## Next-session boot checklist
1. `git clone` full history; `grep -rn 3643` must be empty (3642 is this file's commit);
   `grep CONV-042` shows HELD/re-cut only.
2. Read `frontier_sectors/GR.md` header + entries 3631–3641; `rcore_derivation/3640_*` and
   `3641_triangulation_ledger.md` (the working extension and the ledger are the state).
3. Start ledger row 8 (3390's b₂ with the budget interior). Read `3390_*` and
   `code/3383_*`, `3384_*` first — reuse, do not rebuild (BLOCKING 1 of the 161 handover).
4. Founder contact only for: a mechanical action, or a physics question in pictures that
   PD-007 does not route to the ledger. Under PD-007, most will route to the ledger.

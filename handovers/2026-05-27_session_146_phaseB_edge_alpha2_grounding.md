[ ] STEP 0 (BLOCKING): git clone the repo + `grep -n "THEO-DSL" theorem-registry.md`. Do NOT proceed to the α₂ closure until done. Skipping the clone is what broke Session 146.

# Phase B handover — edge-aligned α₂ (THEO-DSL-7) grounding complete; closure pending

WRITTEN: 2026-05-27, Session 146. This is overflow-survival recovery state.

## DSL = Dynamical Substrate Law. THEO-DSL-7 = RESERVED edge-aligned coefficient (Sequence-2A).

## What Phase B accomplished (this turn)
Grounded the edge-α₂ closure against ground truth (the thing that was skipped
before, causing the misgrounding). VERIFIED:
- cross-shell constants: S1→S2 = φ/2, S1→S3 = φ²/2 (both re-derived; the reverted
  P0605 φ²/2 for S1→S3 was actually CORRECT — Phase-R over-flagged it).
- all G_E orbit-projection inputs against registered primitive corollaries.
Full detail + the open computation: see
  series_umbrella/series_substrate_chirality_arc/dynamical_substrate_law/sketches/
  F1_reading_C_edge_aligned_coefficient_scoping.md  §14
Verify scripts (run, all pass):
  .../dynamical_substrate_law/code/verify_edge_alpha2_inputs.py
  .../dynamical_substrate_law/code/verify_edge_crossshell_constants.py

## What remains (the actual closure — a fresh window with room)
The 2D vector path-class assembly at O(δ²): j₂^edge = α₂^(ρ)n̂_ρ + α₂^(edge)n̂_edge.
All inputs exist (primitives + edge α₁ done). NOT yet computed here because the
2D vector assembly is substantial and the vertex template collapses to 1D scalar.
DO NOT trust the reverted values 9/(2φ), 9φ−12 — sign-suspicious (vertex α₂ is
NEGATIVE). Cross-check by reproducing vertex α₂=−9/φ² with the same machinery first.
Deliverable: hardened_theorems/o_delta_two_edge_aligned_coefficient.tex → THEO-DSL-7.

## State of repo after this session
Clean. Session-146 misgrounded patches reverted (P0610). Reasoning-capture
protocol established + fixed (templates/). Edge-α₂ trajectory grounded + scoped.
No theorem-registry edits made this session.

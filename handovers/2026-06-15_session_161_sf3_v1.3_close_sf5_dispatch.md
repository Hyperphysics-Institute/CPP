# HANDOVER — SF-3 Quark Flagship CLOSED (v1.3, deposit-ready) → SF-5 Strong-Sector Flagship DISPATCH

*Paste this whole document into a fresh context window as the opening prompt. This file does two jobs: it **closes** the SF-3 quark-flagship window (1500-band, Session 161) and **dispatches** its successor, the SF-5 strong-sector flagship, to continue in the same 1500-band. It runs under the **lightweight single-operator multi-window protocol** (§3 below) alongside the SF-7 (1300), SF-1 (1400), and other live windows. **Patch band for the SF-5 successor: 1500-band, continue from the next free number (≈ 1520).***

---

## THE FIXED KICKOFF LINE (how to boot the successor window)

```
Bootup for Conscious Point Physics (CPP). Clone the repo and read the bootup file at
https://raw.githubusercontent.com/Hyperphysics-Institute/CPP/main/bootup.md. Honor the line-1
CLONE-FIRST GATE before registering any ID, placing any file, or computing any coefficient (clone
the repo and grep the registry first). Then open the handovers/ folder, sort by filename, and read
the most recent dated file — for THIS window read this SF-3-close/SF-5-dispatch handover specifically
(2026-06-15_session_161_sf3_v1.3_close_sf5_dispatch.md).
```

---

## LINE 1 — BLOCKING CLONE-AND-GREP GATE (do this before touching anything)

1. `git pull` (or clone) a clean working copy; `cd ~/Documents/GitHub/CPP`.
2. **Confirm your first patch number is free:** `git log --oneline | grep -E '^[a-f0-9]+ 1519'` (then 1520, …) until you find the first free 15xx label. SF-3 + this close consumed 1500–1519; start the SF-5 work at the next free number. **Never reuse a label already in `git log`.**
3. **Do NOT edit shared registries** (`theorem-registry.md`, `predictions.md`, `frontier_sectors/*`, `master_glossary.md`, `paper_catalog.md`, `research_frontier.md`, `README.md`, `INDEX.md`, `parallel_dev/lease_board.md`). The SF-7, SF-1, and other windows append to those. Any SF-5 registry touch (e.g. registering its open problems) is **deferred to ship time** through a flagged integration patch applied after a refresh — see §3.

---

## ORIENTATION — read this first

**Where the programme is, in one paragraph.** SF-3 (the quark-sector flagship) just shipped to **v1.3, deposit-ready, hostile gate closed.** This window is handing off to **SF-5**, the strong-sector flagship — like SF-3, a *synthesis/reframing* of already-shipped CPP results (SS-1 … SS-9+), **not** a new derivation campaign. The job is assembly to the 16-section standard, multi-AI review to SHIP, the full Phase-7 paper-production protocol, and a deposit bundle — exactly the arc SF-3 just completed, which you can use as the worked template.

**Why SF-5 next, and why this band.** Of the SF-line predecessors, SF-1/SF-2/SF-3 are corpus-rich reframings; SF-5 (strong sector) and SF-6 (EW+QM+SR) are the same kind of synthesis-where-the-physics-already-exists. SF-5's substance lives in the shipped SS series (SS-1 through SS-9+) plus open routes (OPEN-SS-6 glueball, OPEN-SS-37). The operator's call is to **continue SF-5 in the 1500-band** — clean, because the SF-3 window that owned 1500 is now closing, so there is no contention for it.

**The one discipline that carried SF-3 and must carry SF-5: the verdict comes from the text, never from a reviewer's authority.** Across SF-3's review history a stale pass was caught, three "blocking" issues were refused as spurious, a clean pass was not over-edited, a reviewer mis-attribution was owned and corrected, and a genuine independent hostile gate was passed — every one resolved by grepping the actual source, not by deferring to (or reflexively distrusting) a reviewer. Carry that.

---

## CANONICAL KICKOFF SENTENCE (for SF-5)

> "Assemble the SF-5 strong-sector flagship to v1.0 in the 1500-band window. The physics is shipped (SS-1 through SS-9+, plus the OPEN-SS-6 glueball and OPEN-SS-37 routes) and needs **assembly, not derivation**: gluon re-counting, glueballs, and confinement from tetrahedral vertex bonding, unified into one strong-sector narrative. Build `flagship_papers/strong/sf-5_strong.tex` to the 16-section standard (`templates/paper-formatting.md`), with a `code/` verifier + a thin asserted reproducibility notebook at deposit, then produce the CONV-001 single-block panel package for the multi-AI review. Follow the SF-3 arc as the worked template."

*(Confirm the SF-5 folder/outline state on clone — if an outline or structural-core sketch already exists under `flagship_papers/strong/`, read it first; if not, the first move is the outline.)*

---

## 1. SF-3 — WHAT SHIPPED (this window's completed work, the template to mirror)

**SF-3 = "The Quark Sector from 600-Cell Geometry," `flagship_papers/quarks/sf-3_quarks.tex`, v1.3 SHIPPED, deposit-ready, 14 pages.** A synthesis/reframing of SM-7/8/9/10 + SS-1/2 + SM-6 (no new derivation). Headline: the heavy-quark masses, $\alpha_s$, the quark Koide phase, and the three-generation count all follow from a **single $m_e$ calibration** + 600-cell geometry; $m_c$ demoted from calibration to **derived** (1249 MeV, −1.6%); $\alpha_s = 5/(8\varphi)$, $\sin^2\theta_W = 3/(8\varphi)$, exact complementarity $\sin^2\theta_W+\alpha_s = 1/\varphi$; quark Koide phase 124.04°; **OPEN-FP-3-CKM** registered (CKM + quark CP phase undelivered); Proposition 5.1 (phase–mass bookkeeping separation, explicitly *not* a theorem).

**Patch ledger (1500-band, all applied & pushed):**
- **1500–1506** draft v0.1 → v1.0 SHIP (4 review rounds) + ship-time registry integration.
- **1507** Phase 7A documentation suite; **1508** anthology chapter C13; **1509** Phase 7B/7C sweep + audit; **1510** v1.1 (Appendix A); **1511** v1.2 (deposit hardening); **1512** v1.2 confirmation pass.
- **1513** full Phase-7 checklist audit remediation (C5 frontier dashboard + D3 flagship README — gaps the mechanical `publication_audit.sh` doesn't check).
- **1514** provenance correction: the "Gemini" v1.1/v1.2 reviews were actually **Copilot** (Edge-context-tool tell); attributions fixed, "two independent passes" claim withdrawn.
- **1515** **genuine independent Gemini hostile pass PASSED (SHIP)** — every attributed quote verified verbatim; gate closed.
- **1516** reproducibility notebook pilot (`code/sf-3_reproduce.ipynb` — thin asserted wrapper over the verifier); **1517** v1.3 data-availability statement announcing it; **1518** templates multi-window consolidation; **1519** this handover.

**SF-3 reusable assets for SF-5:** the four-tier doc suite layout (`documentation_suite/`), the CONV-001 single-block swarm package (`templates/presentation_file.md`), the **embedded-ask hostile-pass package** pattern (a reviewer sent raw source with no question returns no review — embed the 5 adversarial questions), and the **wrapper-with-assertions notebook** pattern (import the verifier, narrate, assert each headline against the paper — at deposit time only).

**SF-3 remaining (maintainer discretion, non-blocking):** the OSF/arXiv deposit of v1.3 (parent DOI 10.17605/OSF.IO/JXE8D), and the SF-2/SF-4 staleness in `flagship_papers/README.md` (their own D3 items, flagged at 1513, not yet fixed).

---

## 2. SF-5 — THE FORWARD TASK

**SF-5 = "Strong-Sector Unification — Gluon Re-counting, Glueballs, Confinement from Tetrahedral Vertex Bonding."** Folder `flagship_papers/strong/`. Inclusion-criterion fit (1),(2),(3),(4); ~5–8 sessions to v1.0 per the SF-line plan. Substance is shipped: SS-1 through SS-9+ supply the strong-sector physics; OPEN-SS-6 (glueball) and OPEN-SS-37 are the open routes to position honestly (as SF-3 positioned OPEN-FP-3-CKM). This is **synthesis-and-presentation**, not derivation.

**Recommended first moves:** (1) clone + grep next free 15xx; (2) survey the SS corpus + any existing `flagship_papers/strong/` outline/sketch; (3) if no outline, draft `sf-5_outline.md` then the structural core; (4) build the `.tex` to the 16-section standard; (5) verifier + CONV-001 panel package → review rounds → SHIP → Phase 7A/B/C → deposit bundle (notebook at deposit). Mirror SF-3 throughout.

**SF-7 dependency:** SF-5 is one of SF-7's predecessors; its §10 consistency threads (SF-5 ↔ SF-1/2/3/4) feed the SF-7 grand-unification window (1300-band). Surface any cross-paper thread for SF-7 rather than resolving it here.

---

## 3. THE MULTI-WINDOW FRAMEWORK (full info, per the operator's request)

**Canonical reference: `templates/new_window_protocol.md`** — read it top to bottom. It now carries the complete lightweight protocol (collision surface + Tier-A/B/C file taxonomy, the five rules, the interface contract + the STOP-and-warn 5-step, recovery, the kickoff block, and the band map). `templates/anticollision_protocol.md` is a thin alias preserving the *"run the anti-collision protocol"* trigger. The heavy lease-board mechanism (leases + `collision_audit.sh`, for the day real/agentic workers arrive) is separate in `parallel_dev/multi_window_protocol.md`; do not run both — default to lightweight.

**The model in one line:** one shared working tree, one `origin/main`, **one integrator (Thomas) who alone runs `git am`/`git push`.** Windows are workers: they produce patches, never push, and rebase on current `origin/main` before delivery. Collision-freedom comes from the operator serializing + each window's discipline.

**Current band map (confirm live status with the operator on boot — some lanes may have closed):**

| Band | Window / lane |
|------|---------------|
| 1000–1099 | Project C — `lambda_qcd_from_planck` (from 1001) |
| 1100–1199 | CC umbrella — `series_cosmological_constant_arc` (from 1101) |
| 1300–1399 | SF-7 grand-unification window (dispatcher of the SF-line windows) |
| 1400–1499 | SF-1 charged-leptons window (from 1400) |
| **1500–1599** | **SF-3 (closing) → SF-5 successor** — continue from next free ≈ 1520 |
| 08xx / 09xx | DM lane / chirality lane (earlier arcs — confirm if live) |

**The five rules (full text in `new_window_protocol.md` §2):** (A) own your band; (B) clone-and-grep first; (C) work greenfield; (D) refresh before push, **never push autonomously**; (E) defer + batch shared-registry edits into one flagged integration patch applied after a refresh.

**The non-negotiable habit:** end **every** patch-delivery reply with a **last-paragraph collision line** — name any Tier-A/B file or another window's path you touched, or state "new own-file, zero risk." No silent registry edits. Contested write → run the STOP-and-warn 5-step (§3.1 of the canonical file).

---

## 4. THE PATCH-DELIVERY CONTRACT (mandatory, unprompted)

Each patch: commit in-container as author `Opus <opus@cpp.local>`, `git format-patch`, copy to outputs, `present_files`, then **immediately** output the apply-and-push macro:

```
cd ~/Documents/GitHub/CPP && \
git pull --rebase origin main && \
git am ~/Downloads/<exact-file>.patch && \
git push origin main && \
git log --oneline -3
```

Always give the `git am --3way` recovery note (the Windows/MINGW64 path for context errors on a clean tree). Multiple patches: sequential `git am` lines before the push. After each push, re-sync the in-container clone (`git fetch origin main && git reset --hard origin/main`) before the next patch — Thomas's `git am` re-commits carry **different SHAs**.

**Reasoning-capture rider:** every *physics* patch bundles in ONE `git am` the `.tex` + a Tier-4 reasoning fragment + the verify script if it computes; pure-bookkeeping patches are exempt. **Version discipline:** every `.tex` content change bumps the title-block version marker; filenames never carry version suffixes (history lives in `documentation_suite/changelog-<paper>.md`). **CONV-001:** present a repo file to the AI panel as ONE 4-backtick fenced block (GitHub blob+raw links + one-paragraph ask + full content); template `templates/presentation_file.md`. **Case-only renames / rename-bearing patches:** `git format-patch --no-renames` (Windows-safe delete+add).

---

## 5. ENVIRONMENT

Thomas: Windows + Git Bash (MINGW64). Local clone `~/Documents/GitHub/CPP`; browser downloads land in `~/Downloads`. Apply-chain uses those explicit paths (or the `cpp-apply` shell function). The in-container clone is `~/CPP` (i.e. `/root/CPP`). Bootup loads the thin `research_frontier.md` dashboard + sector files on demand. The CLONE-FIRST GATE is line-1 blocking on every handover: never register an ID, place a file, or compute a coefficient before cloning + grepping the registry.

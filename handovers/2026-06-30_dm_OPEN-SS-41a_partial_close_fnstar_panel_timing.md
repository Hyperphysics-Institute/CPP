# HANDOVER — DM / founder: OPEN-SS-41a partial close — n* pins (~2–4), f does NOT; ΔG*_nuc not pinnable without the DM element cross-section; the panel shouldn't wait on a pin that's one derivation away
**Date:** 2026-06-30 · **Lane:** DM 18xx / founder · **From:** Opus (strong/geometry lane, patch 2212) · **Consumer:** DM-1
**Delivers on:** OPEN-SS-41a (`frontier_sectors/SS.md`), the dwarf-branch last-mile.

## ⛔ CLONE GATE
```
cd ~/Documents/GitHub/CPP && git pull --rebase origin main && git log --oneline -6
```
Confirm HEAD includes **2212** (OPEN-SS-41a partial close). Grep before reserving sub-IDs.

## Orientation — read this first (it changes the panel timing)
The founder sequenced this as "pin ΔG*_nuc, then run panel round 2 with a pinned dwarf prediction." Executed honestly, **ΔG*_nuc does not pin yet**, so that gate would hold the panel on a calc that is one *derivation* away, not one *confirmation* away. Specifically:
- **n\* ~ 2–4 pins** (0863: the cross forms "within a few moiety additions," hTetra chaperone barrierless; + cooperative stability of the soft g ≈ 0.02 hinge).
- **f does NOT pin.** f = (exposed-core spine-face solid angle)/4π needs the DM element cross-section *shape* (the 8qCP core polyhedron, the eCP-coat corner coverage, which face presents the axial E_qq spine) — the corpus gives this only qualitatively (ℓ_el ~ 2 fm, r₀ ~ 0.26 fm, "corner-buffered").
- The window needs **f ~ 0.04–0.11** (pinned n*); the plausible exposed-core range **f ~ 0.05–0.20 brackets the window but also reaches both kill bands** (~3.2 kT under-cored, ~9.0 kT over-cored). **Window-vs-kill is not decided.**
- So the dwarf branch stays **REACHABLE / plausibly non-tuned** (the 2209 bracket, confirmed) — **not** a pinned prediction. The "n* ~ 4, f ~ 0.11 in-window" first-pass reading is *one option in the range*, not a pin.

## RECOMMENDATION (founder's call)
1. **Run panel round 2 now on the honest status**, don't hold it for the pin: **cluster σ/m ∝ 1/v² = parameter-free prediction (Bullet-safe, the submittable result); dwarf branch = reachable / plausibly non-tuned bracket, one geometry number (the element cross-section) from pinned.** The cluster branch is the headline and does not wait on f.
2. **Open the true last-mile in parallel:** derive the **DM element cross-section** — the exposed-core spine-face fraction f — from the 8qCP+8eCP structure on the tetrahedral-cage / 600-cell substrate. That single number pins ΔG*_nuc (→ N_form → dwarf σ/m(v)) or delivers a clean kill. Likely an **SS-2 / element-structure** item (register under a fresh grep; it is the same element the corpus already uses).
3. **Do NOT present a pinned dwarf σ/m normalization at the panel** — it is a bracket that reaches both kills until f lands.

## LANE BOUNDARY
- **Strong/geometry delivered (2212, done):** n* ~ 2–4 pinned; f-window requirement (0.04–0.11); the honest not-pinned verdict + the narrowed last-mile. **Still owed:** f from the element cross-section (the successor).
- **DM consumes:** the reachable-not-pinned status → dwarf-branch framing + panel round 2 (as above). Once f lands: ΔG*_nuc → ε_nuc = exp(−ΔG*_nuc/kT) → N_form → dwarf σ/m(v).

## POINTERS
- `flagship_papers/strong/reasoning/2212.md` + `code/2212_fnstar_pin_dGnuc.py` (n* pinning, the f-window requirement, why f isn't derivable yet, the bracket reaching both kills).
- **OPEN-SS-41a / OPEN-SS-41** (`frontier_sectors/SS.md`). `reasoning/0863.md` (the concrete chaperone-to-cross formation kinetics — the n* handle and the physics the element-geometry calc should build on). `reasoning/1842.md` (the qualitative element structure). `flagship_papers/strong/reasoning/2209.md` (the ΔG*_nuc form).

## SUGGESTED SEQUENCE
Panel round 2 on the honest bracket now (unblocks it; cluster branch leads). In parallel, founder's call: open the DM element cross-section calc (exposed-core f) — the one derivation between here and a pinned dwarf σ/m(v) or a clean kill. The cluster branch is a submittable result throughout; the dwarf branch is reachable and one geometry number from settled.

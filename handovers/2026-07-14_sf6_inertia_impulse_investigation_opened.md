# HANDOVER — 14 July 2026 (late): DM hardening DEFERRED pending an independent SF-6 inertia pin; a new isolated SF-6 impulse-transfer investigation is opened (Patch 2470)

---

## KICKOFF LINE (paste verbatim to open the next window)

Bootup for Conscious Point Physics (CPP). Clone the repo and read the bootup file at
https://raw.githubusercontent.com/Hyperphysics-Institute/CPP/main/bootup.md. Honor the
line-1 CLONE-FIRST GATE before registering any ID, placing any file, or computing any
coefficient (clone the repo and grep the registry first). Then open the handovers/ folder,
sort by filename, and read the most recent dated file (named YYYY-MM-DD_*.md) — that newest
entry is the canonical "what's next" pointer. Note: the folder is handovers/ (plural) and
there is no file named handover.md; never look for either — always use the newest dated
entry. Warm keyword SF6-WARM-2470. Default action: begin the ISOLATED SF-6 INERTIA
IMPULSE-TRANSFER INVESTIGATION described in this handover — do NOT adjudicate the
DM CONTINUOUS-PCD-1 dispatch (that promotion is DEFERRED; see "DM PARKING" below).

> Kickoff-line note (fixes carried from this session): the URL is embedded verbatim above —
> a booting window has no other way to know it. The newest-file glob is `YYYY-MM-DD_*.md`,
> NOT `..._session_NNN_...`; handovers dropped the `session_NNN` infix after session ~162,
> and a literal `session` filter silently skips the newest entries. The permanent template
> fix for both is staged (see "Governance staged, not yet applied").

---

## Why this window exists (the short version)

The DM continuous-PCD promotion was one adjudication away from executing. In the course of a
plain-language physics review, the founder identified that the ZBW dance model omits **SF-6
inertia** — the DP-sea magnetic energy a Conscious Point stores as it accelerates
(counter-oscillatory DP-pole movement, the SF-6 electromagnetism mechanism). This is not a
complication bolted on; it is a faithfulness element CPP's own electromagnetism requires and
the current integrator (memoryless, v ∝ |F| each Moment) lacks. It is also the leading
candidate for what closes the one loose end (the Momentwise mode-curvature decorrelation).

But the SF-6 inertia coefficient is not pinned from anything yet (the mechanism was documented
"on the road to DM" and set aside). Meeting inertia FIRST inside the ZBW dance would confound
it three ways — coefficient × many-body pile-up × soft-core superposition turnover — so a
coefficient measured there would be a fit wearing a measurement's clothes. **The disciplined
move (founder-directed): isolate SF-6 inertia on a mundane one-body impulse-transfer problem,
pin the coefficient from the DP-sea storage mechanism independently, THEN bring it into the DM
dance as a fixed, externally-provenanced input.** That is this window's job.

---

## THIS WINDOW'S MISSION — isolated, non-relativistic SF-6 impulse transfer

Derive the SF-6 inertia coefficient from first principles on the simplest possible system, so
it enters DM (and everything else) as a measured substrate constant, not a knob.

**First target (do this first): a single free Conscious Point given an impulse.**
- Deliver a non-relativistic impulse; watch the DP-sea magnetic energy build as the CP
  accelerates (the counter-oscillatory DP-pole storage, an E ~ ½ L I² analog).
- Read off the storage-vs-acceleration relation → the effective inertial coefficient.
- The mechanism, not the number, is the deliverable: **F = ma (or the effective inertial
  mass) should FALL OUT of DP-sea back-reaction alone.** If it does, you have derived where
  inertial mass comes from in the substrate, and the coefficient with it.

**Then anchor it:** reproduce a KNOWN rest mass (the electron, the SF-1 pattern) under the
same mechanism as an independent calibration check. Free-CP = cleanest isolation but no
external anchor; electron = anchored but more structured. Derive on the clean system, validate
on the anchored one.

**Register** `OPEN-FP-6-INERTIA-1` in `frontier_sectors/FP.md` after the first computation
(grep-checked free this session; SF-6 items live under FP / OPEN-FP-6-*). CLONE-FIRST GATE and
per-patch reasoning capture as always.

### Discipline for this window (do not skip)
- **G7 — coefficient DERIVED, never tuned.** The test of success is "does the inertial
  coefficient fall out of the DP-sea storage mechanism," NOT "does the number help the DM
  ring." The DM problem must not be in view while you pin it. Deriving from the SF-6 magnetic
  mechanism is a registered debt if the first pass has to estimate rather than derive — flag
  it, don't tune it.
- **Physics subtlety the founder flagged (design the test around it):** the stored KE is
  maximal near CLOSEST APPROACH, not at leg start — acceleration builds over the transit, and
  in a 1/r² field the driving gradient itself flattens/turns over near contact (the 2455
  soft-core saturation is exactly that turnover). So *where* along the trajectory the inertia
  locks in is decided by the dynamics, not asserted. A clean impulse test isolates this
  cleanly; the ZBW dance did not.
- **Scope caution — keep the detour a detour.** Fix the target narrowly: single CP (or one
  simple bound mass), non-relativistic, one impulse, measure storage-vs-acceleration, read the
  coefficient. Radiation reaction, accelerated-CP self-energy, and relativistic limits register
  as their OWN open items — do not let them block the pin.
- **Plain-language playback (new standing practice):** after you formalize the mechanism,
  play it back to the founder in plain language BEFORE any dispatch, so he can visualize it and
  correct the vision→words→math translation. Cycle = formalize → plain-language playback →
  founder confirm/correct → dispatch.

---

## DM PARKING — read before touching anything DM

**Candidate (B): ~70% UNRESOLVED, UNPROMOTED, Ω_DM PARKED. Status UNCHANGED. The promotion is
DEFERRED, not denied and not executed.**

State of the CONTINUOUS-PCD-1 dispatch (do not re-run it as a promotion):
- The re-ask returns came back (five pastes; Copilot's seat CLOSED at 2466, four seated:
  ChatGPT, Grok, Gemini, DeepSeek). Applied against the real §5 reading they **clear the
  threshold as-asked**: 4/4 R4 = PROMOTE or PROMOTE-CONDITIONAL-with-record-satisfiable-
  conditions, no seat sustains R2, no seat answers R1-NO-with-a-named-computation. On the
  questions the package asked (Q1 s₀-reference, Q2 tuned-rescue, Q3 E_qq), it promotes at the
  four-seat median 76.5%.
- **Why it is nonetheless deferred (a soundness escalation, not a governance veto):** the
  re-ask package did NOT put two model-faithfulness items to the panel — (1) SF-6 inertia
  (omitted entirely; the integrator is memoryless) and (2) full qCP-target contention (the
  dance models "someone got there first → rebound" for eCP targets but only direct-
  superposition rebound for qCP targets, so the core many-toward-one pile-up the founder
  described is under-counted). Both point the SAME safe way (more transit apposition → more
  core stiffening → ring at least as favored), so they are not expected to flip the sign — but
  they were never reviewed. Per the escalation discipline, we complete the model, re-present
  the completed package, and let the binding reading fire on THAT. The reading stays binding;
  we do not execute on an incomplete package.
- **A prior documentation severance was found and diagnosed (fixed going forward):** the 2469
  handover truncated its orientation window at 2461, dropping the 2453–2456 coherent-cycle /
  jello-stiffening lineage. The continuous-PCD code (2467) descends directly from that lineage
  — its saturation locks a_qq=ħc/264, a_ee=ħc/553 ARE the 2455 concrete-cycle locks — so 2467
  IS the founder's concrete cycle; the physics survived in the code, the reason was cut in the
  handover. The (a)/(b) charge-state fork resolves to **(a)**: per the 2454 spec the same/
  opposite apposition tension "lives in the trajectory ensemble" of the fixed-charge home-
  anchored dance, so no separate charge-flip ingredient is missing. The only open quantitative
  item there is whether the dance's EMERGENT same-charge duty lands on the combinatorial 3/7
  (2435 dwell-suppression residual) — a matter of degree, expected ≥3/7.

**The DM hardening probe (run it AFTER the SF-6 coefficient is pinned, not before):** add full
qCP-target contention to `dance_M` + SF-6 inertia AS A SCAN (coefficient bracketed by the
SF-6 pin's order of magnitude, never fit to the ring). Re-measure three things: ring−rod
(expect steady or stronger), emergent same-charge apposition duty (expect ≥ 3/7), and the
Momentwise mode curvatures (watch whether inertia tames the m=0/m1 deterministic-pair
decorrelation — if it does, OPEN-DM-MW-MODES-1 closes in the same run). If it all holds,
re-present the hardened package to the four seats; the §5 reading then executes as written
(PROVISIONAL, Ω_DM unparks, four-seat median) with no further founder sign-off — see
governance below.

---

## Governance — delegation now standing (codification staged)

The founder has delegated per-instance promotion sign-off: when a pre-registered criterion is
CLEANLY met and no soundness gap is found, the promotion EXECUTES without a further founder
signature (founder sign-off as an epistemic device convinces no external reader; the
pre-registered reading is the real anti-motivated-reasoning control). The retained line: the
assistant EXECUTES on a clean criterion-met and ESCALATES to the founder ONLY a soundness
question the criterion did not screen for (this session's SF-6-inertia gap is the template).
Governance formalities are absorbed silently; physics-judgment calls go to the founder.

**Governance staged, not yet applied (a single OS/template patch, kept off the physics
cycles):** (1) hard-embed the URL-bearing kickoff line in the handover template so it can't be
paraphrased down to "read bootup.md"; (2) fix the stale `session_NNN` glob → `YYYY-MM-DD_*.md`;
(3) OS amendment codifying the delegation + anti-governance-creep principle (sourced to the
founder's founders_vision framing) + the plain-language-playback step in the review cycle.

---

## Traps / carry-forward
- **Terminology (founder-corrected this session — use it):** DM PLANE = 4 qCP + 4 eCP;
  DM ELEMENT = 2 planes = 8 qCP + 8 eCP = four hTetras congealed (two exposed 4-qCP faces);
  DM ROD = a stack of planes/elements (face-to-face lengthening); DM RING = a rod collided,
  bent, and bonded end-to-end. Two rod-formation routes: (1) hTetra aggregation; (2) qDP-chain
  → 2eDP-2qDP ribbon → 4eDP-4qDP ribbon → + hTetra qDP edge → element. N_planes=16 = 8 elements.
- CLONE-FIRST GATE mandatory before any ID/file/coefficient. Per-patch reasoning capture
  (`reasoning/NNNN.md`) + verify script in `code/`. Git author `Opus <opus@cpp.local>`.
- This session's full arc is in `series_phenomena/cosmology/dark_matter/reasoning/2470.md`.
- SF-6 origin: grep SF-6 / the EM series for the counter-oscillatory DP-pole magnetic-inertia
  mechanism; if it is as underdeveloped as the founder recalls, THIS investigation develops it
  — and the DM mode problem plus the underdeveloped SF-6 pin may be the same problem (DM pays
  SF-6 back for what it borrowed).

Warm keyword SF6-WARM-2470; next patch 2471.

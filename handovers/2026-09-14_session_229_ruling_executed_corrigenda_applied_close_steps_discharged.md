# Session 229 — chirality lane (09xx): the relabel ruling executed, the corrigenda applied, Session 228's close finally complete

**Window:** 14 Sep 2026. **Patches 0975, 0976, 0977, 0979, 0980, 0981** (six; `0978` was superseded before
push and has no commit). **Next free: 0982.** **Concurrent:** `0975b` landed out-of-band from the Session 228
window mid-session.

---

## Orienting paragraph — paste this into the next window

**No physics this window.** The founder's PD-006(a) ruling was recorded and executed, the arc's five
paste-ready corrigenda were written into source, and Session 228's two owed close-steps were discharged.
Four corrections were made to the lane's own governance machinery — **three of them to this window's own
earlier claims**, which is the honest shape of the session. The lane remains **empty of bounded work**:
everything outstanding is a founder mechanical action, another lane's, or flagship-scale.

## What was established

| patch | result |
|---|---|
| **0975** | The deferral gate accepted a **mention** of `NOTHING-DEFERRED` as an **assertion** — that is how Session 228's Steps C/D left the queue. Two fixes: clause-initial assertion with a stated reason; hits print under the override. **0974 now reads NOT asserted and would have failed at the moment of the drop.** |
| **0976** | **Founder ruling: relabel accepted.** Reading C's `ℓ` is a directed traversal cost. All five corrigenda applied to source. **SM-2 edit (g) held** on `OPEN-EW-5`. |
| **0977** | Step C written, **by subject rather than by session**. Step D declared unrecoverable — **wrong, see below**. |
| **0979** | 0975b's counts corrected (20 fragments not 21; 15 enumerated against "thirteen"). **`0974a`'s exemption overturned** under 0975b's own test. |
| **0980** | Step E′ + Step D for this window: fragments for 0975/0976/0977/0979, no gaps. |
| **0981** | §15 close, all eight steps with explicit status. Step G: bootup's D-9 and deferral rider carried the pre-0975 override semantics in two places; corrected. |

## Traps for a fresh worker

1. **Trap #1 of the 228 handover fires on people who have just read it.** This window's first patch failed to
   apply because origin moved between the boot fetch and `format-patch`. **Re-fetch immediately before
   `format-patch`, every time.**
2. **The founder's ledger drifts by one.** He said "0974b"; it was `0975b`. Verify against origin.
3. **Do not assert a mechanism you have not run.** This window claimed a gate blind spot without running the
   gate on the commit in question — one second's work, and the claim was false.
4. **A capture gap is not a permanent loss until you have asked whether the originating window is reachable.**
   Only that window can capture rather than reconstruct.
5. **The `A₂u` label in `chirality_continuum.tex` names two different objects.** Operator vs matter-doublet
   state space. Never grep-and-substitute it.
6. **`NOTHING-DEFERRED` now needs a reason clause.** A bare mention no longer overrides, and a fragment that
   *narrates* a past deferral will trip the gate — a systematic false-positive class that Step E′ will
   generate every window. Assert deliberately; the hits print above the override.

## Forward queue

**Founder mechanical — five PDF recompiles** (`TODO-0976-RECOMPILE`): Capotauro, `chirality_continuum.tex`,
SM-2, TARROW-2, F.1. Sources are edited and committed; only regeneration is owed.

**Other lanes:** `OPEN-EW-5` — does the W mass breakdown depend on species or only on count? Gates SM-2 edit
(g), the only place a published SM-2 number can still move. Also owed there: an observable selecting the
alternating W⁰ ring order over the blocked one; none is on file. · `OPEN-SM-4`.

**Flagship — do not open from this lane:** **SM-4(b)**, scoped and declined three times now.

**Named residuals unchanged from 228:** CAPACITY-1 carries the axioms, the derived first harmonic **and** a
structural premise — never describe it as unconditional. Residuals are coupled (4.0× superadditive); test
jointly. TARROW-2's `O(δ³)` order-counting consumes `A = 0`.

**Governance, recorded and deliberately not enacted:** the gate's quotation-versus-content false-positive
class (0980). A content test is harder than the mention-versus-assertion one and was not attempted.

## Step audit — all eight, with status on each

*Listing only the performed steps is what made Session 228's audit read as complete. All eight, every time.*

| step | status |
|---|---|
| **A** — session log | **DONE at 0981** → `session_logs/2026-09-14_session_229_log.md` |
| **B** — transcript pointer-map | **DONE at 0981** → entries `018`–`023` |
| **C** — development vignettes | **DONE at 0981** → the 0976 vignette in `development-chirality-derivations.md`. The rest of the window was governance with no paper scope; its record is the session log and the Tier-4 fragments. |
| **D** — verbatim reasoning | **DONE at 0980**, in-window → `reasoning/0975.md`, `0976.md`, `0977.md`, `0979.md` |
| **E** — registries | **DONE per patch.** No new axioms, no new theorems, **count unchanged**. No verdict moved. |
| **E′** — reasoning-capture audit | **DONE at 0980.** Per-patch captured/exempt table in the reasoning index. **No gaps.** `0980` and `0981` recorded EXEMPT rather than assumed. |
| **F** — reviewer artifacts verbatim | **N/A — no panel was dispatched this window.** Recorded explicitly rather than omitted. |
| **G** — protocol / OS updates | **DONE at 0981.** `bootup.md` D-9 and the deferral rider both described the pre-0975 override semantics; corrected in both places. No other OS change: 0975b's scope test and Step E′ were enacted by the window with standing, and this lane did not touch them. |
| **H** — handover | **DONE — this document.** |

**Deferral gate:** run on every patch. `0980` asserted `NOTHING-DEFERRED` with its reason (all hits
historical narration); all others PASS on `todolist.md` in the diff.

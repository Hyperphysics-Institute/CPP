# handovers/detail/

Full session handovers live here (bootup Two-Step Boot, Patch 4189) so they never win the newest-file sort in `handovers/`, which holds only the 4 KB boot cards. Sort by filename; each boot card names its detail file. Created at Patch 4220 (git does not track empty folders — TODO-4191-DETAILDIR).

# Reasoning capture — Patch 2036: verbatim founder-recovery handover

**STATUS: verbatim (captured at-patch).** Window: 2000-band. Opus worker; integrator = Thomas.

Second half of Thomas's request (after 2035 substance-now): hand the full verbatim recovery to a dedicated
window. Wrote a self-contained handover: why it exists (the 11-day gap + the §10 fix), what's already done
(2033 verbatim scalar-SSV; 2035 reconstructed substance for l_P + SR-9 — flagged for verbatim upgrade), the
mission (verbatim TLA contributions 1-22 June), the method (recent_chats/conversation_search as the verbatim
source since the repo only has paraphrase; cross-index with the 126 TLA-mentioning fragments filtered for
contribution-verbs), a non-exhaustive seed target list, environment/protocols, and an honest definition-of-done
(sweep reports zero orphans; coverage note records swept range + unretrievable sessions).

Why a separate window: verbatim recovery needs transcript retrieval across 11 days of six-window work --
too large for this deep post-compaction window, better fresh. This window did the readily-extractable substance
(2035) so the bleeding stops now; the dedicated window does fidelity.

CANONICAL/templates note: adds the handover under templates/. Flagged for integrator. NO THEO.

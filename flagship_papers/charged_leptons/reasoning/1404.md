# Reasoning fragment — Patch 1404 (SF-1 v0.3 → v1.0 SHIP)

**Window:** SF-1 (band 1400–1499). **Patch:** 1404 — v0.3 → **v1.0 SHIPPED** (.tex only).
**Nature:** EDITORIAL polish + version promotion. No physics/numbers changed. No verify script.
**Base:** real shipped 1403 (origin/main 29873035), synced before editing.

---

## SHIP authorization: genuine three-model convergence on v0.3

Corrected attribution (per Thomas): the round-1 review I mislabeled "ChatGPT #2 / fake Grok" (the seven-section
review citing \ref{thm:theta-free}) was the **real Grok** all along. Grok was never missing; my model labels were
wrong. The v0.3 review round is unambiguous and complete:

- **ChatGPT** (v0.3, fetched live from GitHub raw): SHIP. Only nit: "cleanest" still rhetorically frequent (non-blocking).
- **Grok** (v0.3, doc 9): SHIP, "clean v1.0 candidate." Nits: a boxed "Inherited, not closed" call-out for OPEN-FP-1-P3.
- **Copilot** (v0.3, doc 10): SHIP (v1.0). Nits: Table-1 footnote that Weinberg angle is intermediate (not a
  charged-lepton observable); a §8 "only smoothing location" emphasis (already present); light abstract tighten.

**ChatGPT + Grok + Copilot = three-model SHIP convergence**, the SF-line bar (SF-2 / SF-4 precedent). v1.0 authorized.

## v1.0 edits applied (4 + version)

1. **Grok:** OPEN-FP-1-P3 inherited-open paragraph (§7) converted to a boxed `mdframed` "OPEN-FP-1-P3 — inherited,
   not closed" call-out. Renders correctly (verified page 9).
2. **ChatGPT:** "cleanest" reduced 5 → 2 occurrences (kept in plain-language summary + conclusion; reworded §7
   "least-encumbered single-m_e sector", §11 "least-encumbered", §12.1 "most parameter-economical such demonstration").
3. **Copilot:** Table 1 — added $^\ddagger$ footnote on the Weinberg-angle row stating it is an intermediate lattice
   result, not a charged-lepton observable (renders, page 8).
4. **Copilot:** §8 "only smoothing location" was already explicit ("exactly there — and only there"); no change needed.
   Abstract tighten judged non-essential (dense technical abstract; each sentence carries content; over-trimming risks
   dropping the cross-sector hooks) — noted as a deliberate editorial call, not an omission.
5. Version 0.3 → **1.0 SHIPPED** in the title block (clean convention: no version-history paragraph).

Compiles clean: 13 pp, 0 errors, 0 undefined refs/cites, 0 overfull.

## What this patch does NOT do (deliberate)

- **No PDF committed.** Canonical v1.0 PDF freeze is a Thomas-side Binary-Artifact-Workflow step on the ClearPC
  canonical compile machine (SF-2 / SF-4 precedent), committed via the `!flagship_papers/*/*.pdf` .gitignore
  exception. The sandbox PDF (13 pp) is verification-only.
- **No shared-registry edits.** The SHIP's registry integration — register OPEN-FP-1-P3; add the SF-1 row to
  paper_catalog.md; add SF-1 to README.md; predictions.md / frontier_sectors/FP.md as applicable — is the SEPARATE
  flagged integration patch (1405), DEFERRED and to be applied after a refresh + Thomas serialization, because those
  five files are appended-to by all six live windows. This is the single highest-collision-risk action in the SF-1
  workflow; it is NOT bundled here. 1404 ships the paper text only and is own-file collision-free.

## Forward (the actual SHIP completion)

- 1405 flagged integration patch (shared registries) — warn + serialize + refresh first.
- On ship: notify the SF-7 window to build the SF-1↔SF-2 and SF-1↔SF-4 §10 members (the δ_CP unblock).
- Documentation suite (changelog-sf-1.md + 8 companion files) + canonical PDF + OSF deposit = post-SHIP queue.

# EXECUTION RECORD — THE β = 0 CONTROL: THE CORE IS A PERSISTENT STATIC PATTERN WITH REVERSED (ANTI-SCREENING) ORIENTATION, PLUS A WINDOW-DILUTED TRANSIENT — AND ROUND 1'S "DISPARITY" IS DOMINANTLY THE MOTIONLESS PATTERN

**Patch 2918. Executed against the discriminating predictions FROZEN at
Patch 2917 record §6, before any control leg ran. 12 legs: static
source, seeds {4–6} × classes {A,B} × windows T_meas ∈ {63, 125},
instrumentation unchanged. Raw legs:
`data/2918_control_legs_raw.json`; pooled fields + adjudication
numbers: `data/2918_control_fields.json`; driver:
`code/2918_control_driver.py` (round-2 archive provably uncontaminated
— 60 legs before and after).**

## §1 — ADJUDICATION OF THE THREE FROZEN PREDICTIONS

| prediction | frozen discriminator | measured | verdict |
|---|---|---|---|
| (c) motion-locked: p₀ ≈ 0 | inner amp ≈ 0 | 0.0147–0.0217 | **KILLED** |
| (a) static: window-equal, amp ≈ a₀ = 0.0143 | ratio 1.0; amp match | T=125 amp **0.0147** (ratio to a₀: **1.03**); window ratio 1.47; χ²/dof(63 vs 125) = 1.99 | **CONFIRMED as the dominant, persistent component** — amplitude nailed to 3% — with an admixture |
| (b) transient: window ratio ≈ 2.0 | amp ∝ 1/T | ratio 1.47, between; difference map is EVEN and small | **PRESENT, SUBDOMINANT** |

**Decomposition (inner ring):** the persistent (T = 125) pattern is
odd-dominated (‖odd‖ = 0.0138 vs ‖even‖ = 0.0053) and source-oriented;
the transient-enriched difference map (m₆₃ − m₁₂₅) is an even, nearly
uniform offset (−0.0022 aft / −0.0020 fore) — a relaxation drift that
dilutes as ~1/T and explains both the 1.47 ratio and the window χ².

## §2 — THE SIGN: COLLECTIVE ANTI-SCREENING (convention audited)

The persistent pattern has p_x > 0 at ξ < 0 and p_x < 0 at ξ > 0 —
the + member of each pair leans TOWARD the +1 source, the *reverse* of
single-pair induction. Convention audit (`code/2918_sign_audit.py`):
an isolated pair at x = −3 polarizes p_x < 0, monotonically, exactly
as naive electrostatics demands. **The engine's conventions are
clean; the reversal is a many-body property of the bonded ZBW Sea —
the Sea ANTI-SCREENS a static charge.** Registered as an open physics
question (founder-physics candidate): what in the pair–pair bonding
inverts the collective polarization? Bears directly on the suspended
statics rebuild (2892).

## §3 — REGISTERED CORRECTION TO ROUND 1 (upgrades the 2917 flag)

The 2914 image — "fore/aft disparity at amplitude ~0.4β" — is
**dominantly the β = 0 static anti-screening pattern.** Its orientation
(positive aft / negative fore) is identical to the motionless
pattern's; its amplitude at β = 0.05 (0.016) equals the static
amplitude. The genuinely motion-proportional response is the a₁ term:
amplitude ≈ 0.026·β inner ring — ~6× smaller at β = 0.05 than
attributed. The 2914 *measurement* stands; the *attribution to motion*
is corrected. The fore/aft asymmetry of the moving charge, as a
motion signature, is hereby re-opened at the correct (smaller) scale.

## §4 — WHAT'S NEXT (single pointer)

Pre-register HYBRID ROUND 3 with the corrected basis
p(β, T) = p₀ + (τ/T)·p_tr + β·p₁ + β³·p₃, with p₀ constrained by this
control, fresh bands stated for the drive decomposition
(p₀-under-retardation is exactly the 2884 static-Sea mechanism — its
target remains c = 1/5; the β·p₁ correction gets its own band), and
the window-transient term either fitted or killed by a longer T_eq
(decide at prereg with a transient-decay measurement, cheap). The 60
round-2 legs are the data; no new mobile legs required until the
prereg says so.

## §5 — STANDING

Ledger untouched: 1B OPEN; PR7 PARTIAL; six of seven; B7 holds
DM-1/2/3; Candidate (B) 79.5%. G1, P-A2-1, statics suspension, 7 July
ruling stand. CONJ-FP-1: A HOLDS, B CLOSED, curvature OPEN.

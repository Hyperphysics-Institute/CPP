# REASONING FRAGMENT — PATCH 2920 (verbatim worker reasoning, Tier 4)

1. Gate passed at 1.419 -- the first time in the pipeline's life that
   a frozen gate and the data agree the model is right. The 2917
   indictment predicted exactly this: fix the basis, the fit works.
2. The assert I froze at 2919 s3 fired. My first hypothesis was my
   own prereg error (the assert tests the fitted p0, which carries
   even noise) -- true but insufficient: odd-projection left
   D0(0) = 2.2e-4. Second hypothesis: kernel parity -- audited with
   the cheapest exact test (one mirror pair), CLEAN to machine zero.
   Third: the lookup. digitize on edge-landing cells is
   right-inclusive, so -5.0 and +5.0 read different |xi|. Proven by
   direct printout. Root cause, three hypotheses, each killed or
   confirmed by a one-liner.
3. The uncomfortable part, stated plainly: this defect is mine,
   inherited from my own 2914 machinery through three rounds under
   the banner "machinery unchanged" -- the same failure genus the
   handover named (inheriting from the arc rather than the spec),
   sixth instance. What was different this time: the exact zero was
   PRE-REGISTERED as an assert, so the defect had no place to hide.
   Freeze exact numbers, not just bands -- the 2916 lesson (freeze
   literals) generalizes to freeze invariants.
4. Why not just fix and rerun inside round 3: because a corrected
   instrument judging bands it was corrected in sight of is the 2914
   R-gate mistake in mirror image. The fix costs one small prereg
   (2921) and zero new data. Round 3b inherits everything except the
   two corrections, both of which are exactness restorations, not
   tuning: interpolation restores mirror parity; odd-projection of
   p0 imposes the beta=0 symmetry the physics owns.
5. sigma_ctot = 2.87 with the defect in place already localizes
   c_tot near +16 -- if 3b lands there too, the physics conversation
   changes (the perturbative Newton-I form is dead in this beta
   range); if 3b lands near 1/5, the whole excess was the artifact
   stack. Either answer is now one clean measurement away.

# REASONING FRAGMENT — PATCH 2918 (verbatim worker reasoning, Tier 4)

1. Control design held to the 2917 s6 freeze exactly: 12 static-source
   legs, two windows, predictions committed first. The driver moves
   each beta=0 leg out of the shared round-2 file atomically; the
   60-leg round-2 archive verified intact before and after.
2. Numbers, in the order they landed: T=125 inner amp 0.0147 vs the
   round-2 fitted a0 = 0.0143 -- a 3% match on a number frozen the
   patch before. That is prediction (a)'s amplitude clause, hit.
   Window ratio 1.474 sits between (a)'s 1.0 and (b)'s 2.0; window
   chi2/dof = 1.99 says the two windows differ beyond noise. So:
   mixture. The difference map isolates the admixture: EVEN, uniform,
   ~-0.002 both sides -- a relaxation drift, window-diluted. The
   persistent map is ODD-dominated (0.0138 vs 0.0053).
3. The sign shock: persistent p_x > 0 at xi < 0 -- the + member leans
   TOWARD the + source. Before claiming physics I audited the
   convention with the cheapest possible test: one pair, one source,
   30 Moments. It polarizes the normal way (-0.19 and growing). So
   the ensemble reversal is collective. Registered as an open physics
   question rather than explained away -- the failure-mode ledger
   forbids accepting a convenient story (e.g. "screening") without an
   estimator that could distinguish it.
4. The correction to round 1 is now evidence-forced, not a candidate:
   the imaged "disparity" has the static pattern's orientation AND
   amplitude at beta = 0.05. What remains genuinely motion-borne is
   a1 ~ 0.026*beta. The velocity-memory picture (7 July ruling,
   vindicated at the 2900-2915 close) survives -- but the pattern
   carrying it is an order of magnitude subtler than round 1 read.
5. Round-3 basis follows from the decomposition, not from choice:
   p0 + (tau/T) p_tr + beta p1 + beta^3 p3. The static-Sea target
   c = 1/5 attaches to the p0-retardation channel -- which is the
   2884 mechanism rebuilt from MEASURED substrate output. That is the
   hybrid pipeline's original promise, now with the right anatomy.

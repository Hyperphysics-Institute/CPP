# Reasoning fragment — Patch 2901 (the identity dies; the decision moves to the substrate)

**STATUS: contemporaneous. Tables at
`sketches/entrainment_curvature_cancellation.md` §§7–8; founder verbatim
at `founders_voice/founder_ruling_universal_primitive_no_arc_compliance_2026-07-31.md`.**

The founder answered my compliance question with a ruling stronger than
the question: there is no compliance rule at all. One primitive, all CPs,
bound or free; everything I was calling compliance, stretch, or epsilon
is emergent bookkeeping. My first reaction was that this forces
eps = mu = 1/|k| — the DP moves per the same rule, its SSV_net is the
arriving amplitude, so its displacement per unit amplitude is the CP's
own mobility — and I noticed yesterday's numbers already satisfied
eps* . |k(eps*)| = 0.9996. Four significant figures, unlooked-for,
landing exactly where the programme wants it: Newton I forced by
universality. That is the precise shape of the failure mode this arc has
now named five times, so the test went to pre-registration before I let
myself believe it: the product must hold across all six robustness
variants or it is an accident.

It is an accident. 1.047, 3.42, 7.75, 0.278 — and one variant where c
never crosses zero at all. The post-mortem is instructive: the chain
eps = mu equates the DP's arriving amplitude (1/L^2) with the CP's summed
drive (amplitude x volume / L^m), a unit mismatch that the baseline
variant's normalization happened to absorb. In other words I did the
thing the founder's message was warning against in the same breath as I
read it: I manipulated emergent-level coefficients as if their algebra
were the physics. Their algebra is model-dependent. The founder's "net
number rather than a principle" cuts both ways — the toy gives principles
without numbers you can trust, the FEM gives numbers without visible
principles, and the discipline is to make each do only its own job.

What the toy has legitimately delivered, and what I am keeping: the
mechanism (entrainment cancels the beta^2 curvature — zero-crossing with
healthy drive in every variant that reaches it) and the observable to
extract from the primitive-level run (the dressed drive's c and c4, with
c4 -> 0 as the pre-registered signature of full cancellation). What the
toy cannot deliver, now demonstrated rather than suspected: where the
substrate sits. eps* moves an order of magnitude with the inner cutoff,
so the answer lives in the near field, where the toy's cutoffs are
arbitrary and the substrate's are pinned.

So the roadmap collapses onto one computation that was already registered
for a different reason: the AUTOMATON moving-source test, which CONJ-FP-1
needed for Condition B, now extended to a mobile Sea per the ruling —
every DP moved per-CP by the primitive from its own GP's sum, no eps
anywhere in the code. One run, three registered outputs: Condition B, the
forced entrainment profile, the dressed curvature. I recorded the per-CP
implementation decision in the sketch explicitly so the founder can
correct it from the picture if the picture disagrees; his ruling reads
per-CP to me ("the only factor that matters is the sum of the DI-bits
that have landed on the GP that the CP is on"), and stretch should then
emerge or the mechanism is wrong.

A note on the eighth convergence dying: the previous worker counted seven
favourable convergences in this arc and watched six die. Killing the
eighth in one hour, by a test registered before execution, is the
cheapest one yet. The pattern suggests a rule worth keeping: any relation
noticed BETWEEN two fitted coefficients gets a cross-variant test before
it gets a sentence of prose.

# REASONING FRAGMENT — PATCH 2917 (verbatim worker reasoning, Tier 4)

Round 2 executed as frozen. The chain that led from the INCONCLUSIVE
verdict to the model indictment, verbatim:

1. Gate result chi2/dof = {2.96, 1.59, 1.86, 1.42, 2.13} — with 12
   legs/beta the errors are small enough that real beta-structure
   fails a collapse test that round 1 passed on noise alone. Frozen
   branch: model beta^3 explicitly.
2. Stage 2 returned c_hyb = +19.7 with sigma_c = 2.11. The improvement
   over round 1 (27.7 -> 2.11) is 13x; the residual gap to the 0.05
   bar is 42x. sqrt(2) from the frozen seeds-{10-15} rung cannot close
   it. So either the physics has |c| ~ 20 (perturbative form dead at
   beta = 0.2, c*beta^2 = 0.8) or the basis is wrong.
3. Model-free per-beta drives: D/beta = {6.3, 14.3, 7.8, 8.0, 2.6}e-3
   — no clean 1 - c*beta^2 shape; beta=0.07 high outlier.
4. The decisive number: unscaled inner-ring ||p_x||_rms =
   {0.0158, 0.0157, 0.0171, 0.0172, 0.0199} across beta = 0.04 -> 0.20.
   Fit amp = a0 + a1*beta: a0 = 0.0143, a1 = 0.0259. At beta = 0.04
   the constant core is 14x the linear part. The pattern the pipeline
   has been fitting as beta*p1 + beta^3*p3 is mostly not proportional
   to beta at all.
5. Immediate corollary checked against round 1: 0.0158/0.05 = 0.32 —
   the 2914 "amplitude ~0.4*beta" headline at beta = 0.05 is
   numerically the SAME observation as a beta-independent 0.016.
   Round 1 could not distinguish them; round 2 can, and does.
6. Candidate origins and their cheap discriminator (beta = 0 control,
   two windows) frozen in the record BEFORE running any control leg.
   Static-induction sign analysis done from the primitive: source +1
   repels the + member and attracts the - member of a pair, so
   p_x > 0 FORE for pure static induction — the measured core is
   positive AFT. If the sign convention audit confirms the
   measurement, static induction alone cannot be the core.
7. Escalation ruling (record §5): skip the futile sqrt(2) rung, land
   on the re-assessment rung the freeze itself provided. All 60 legs
   remain valid inputs to any corrected basis.

Failure-mode ledger, continued: this is the fifth instance of the
named class — a number (0.4*beta) landing near an expected form
(linear response) accepted without interrogating the estimator's
ability to distinguish it from the alternative (constant). Caught, as
every prior catch, by two of the worker's own numbers disagreeing —
here the amplitude at five betas against the assumed scaling.

Diagnostic commands preserved:
  python3 code/2917_round2_analysis.py
  python3 code/2917_stage2_round2.py
  inline diagnostics (model-free drives, amplitude scaling, low-beta
  restricted fit) as printed in the session transcript, reproducible
  from data/2917_response_fields_round2.json with ~15 lines of numpy.

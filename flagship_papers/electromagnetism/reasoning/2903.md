# Reasoning fragment — Patch 2903 (base grid INCONCLUSIVE; differential pre-registered)

**STATUS: contemporaneous. Record at
`sketches/mobile_sea_execution_record.md`; amendment at
`sketches/mobile_sea_differential_amendment.md`; data at
`data/2903_base_grid_results.json`.**

The run refused to land in a band and the honest word for that is the
one the prereg supplies: inconclusive. Two guards fired independently -
sign flipping with beta, and the largest-beta point failing the signal
floor - and the statistical postscript is that every point was a
two-to-three-sigma number against the ZBW chatter. I notice the guards I
froze at 2902 did to me exactly what the 2885 guards did to the previous
worker at 2886, and I notice that this is the system working, not
failing. An underpowered pattern with a story attached is how this arc
manufactured four false confirmations.

Two things I want on the record about temptation. First, before
executing I found and fixed a genuine estimator defect - asymmetric
grids biasing the very observable under test - and the fix was
legitimate only because it happened before any dressed number existed
and was committed with its reasoning. Second, after the run I saw that
the best-phase-averaged point reads zero, which is what my declared
expectation (cancellation) would look like. I wrote it into the record
explicitly flagged as weightless. The discipline is not to unsee things;
it is to label what license they do and do not carry.

The diagnosis I actually believe: the beta=0 floor measured the wrong
noise. A static source sits in a static washboard; a moving source
drives over it at frequency beta over spacing, and my fixed window
averaged fractions of washboard periods - 1.2 cells at the lowest beta.
The scatter's size and its sign-alternation both fit. The three fixes
are ordered by sharpness, and the sharpest - the mobile-minus-frozen
differential, which subtracts the washboard as common mode and whose
frozen leg has no ZBW chatter at all - is a new observable, so it gets
its own bands, committed in this patch before any differential number
exists. I set its banded outcomes to provisional pending the original
section-5 convergence variations, because a clean number from one box is
still one box.

What I will not do is stack fixes until one produces a band and then
stop. The amendment freezes one round: three betas, matched windows,
paired legs. If it is inconclusive again, the next move is more
statistics or a denser Sea, each pre-registered in turn, and the record
will show every round including the failures.

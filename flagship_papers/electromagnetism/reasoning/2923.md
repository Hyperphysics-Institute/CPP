# REASONING FRAGMENT — PATCH 2923 (verbatim worker reasoning, Tier 4)

1. The test instrument is the direct method because the hybrid is the
   party under suspicion at high beta -- an instrument may not
   referee its own extrapolation. The direct method's own high-beta
   liability (transit length) is handled by the design literals, not
   by a rule a worker might mis-multiply.
2. The buffer at beta=0.25 (3.75) sits below the round-1 precedent
   (4.7). I disclose rather than shave the window further because
   T=50 is already only 2 grid periods; going shorter trades a known,
   bounded edge risk for an unknown windowing bias. The 2905 failure
   class occurred with the transit EXITING the Sea; 3.75 units of
   margin is categorically different, and the checkpointing driver
   makes any edge anomaly visible in the leg's own record.
3. Bands are sign-and-significance only. After watching three rounds
   of curvature numbers dissolve under instrument scrutiny, the
   question this test asks is deliberately binary: does the drive
   die past beta ~ 0.15 or not. Either answer redirects the arc:
   drag confirmed feeds B1 directly (the obstruction is worse than
   the beta^2 term); sustained drive indicts the hybrid's high-beta
   shape and localizes its failure to the pattern windows.

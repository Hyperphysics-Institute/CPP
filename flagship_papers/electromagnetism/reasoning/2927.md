# Reasoning fragment — Patch 2927 (ε* non-universal; O(ε²) closed form; two failed bands, both attributed; three implementation defects caught in-flight)

**STATUS: contemporaneous. Record at
`sketches/entrainment_nonuniversality_record.md`; script at
`code/2927_entrainment_nonuniversality.py`.**

The sequencing call was mine under PD-006 and I want the reasoning on
the record: the handover's secondary queue (ANTISCREEN-1, LINK 2,
K1-MEMORY-1, HYB-SHAPE-1) stands, but 2926 exposed a one-hour test with
direct bearing on the arc's central question — the 2900 entrainment
result was measured at exactly one configuration, and the 2926
factorization predicted it could not be universal. A cheap test that
can kill a live framing beats starting a heavy item. It did kill it:
factor of 61.

The prediction went through two rounds of being wrong in instructive
ways. Round one: my back-of-envelope said ε* tracks R_m/R_{m+3}. It
does — at fixed m, to 5%. Across m it missed by up to 43%, because the
O(ε) angular factor is m-dependent, which the envelope ignored. The
pre-registered "NEITHER" verdict fired exactly as written, and the
right response was to compute the angular factor properly rather than
to stretch the band. Round two: with ψ and χ exact, the quadratic-
truncated ε* still missed the 5% band on the three small-ε* configs
while the declared expected-failure config (ε* = 0.446) agreed best.
That inversion is the most useful single datum of the day: it ruled
OUT my own pre-registered explanation (perturbative radius) before I
could reach for it, because an O(ε³) error grows with ε*, and this one
shrank. The actual causes — three different discrete radial-sum offsets
inside the bisected measurement, plus a genuinely small O(ε³) tail —
were then confirmed by three pre-registered attribution tests, and the
next-term parameter ε·|χ₃R_{m+6}/(ψ₃R_{m+3})| ~ ε/r³_min explains the
inversion outright: the expansion parameter is not ε.

Three implementation defects in my own artifacts, disclosed because the
arc's integrity accounting should include the worker's: (1) first
symbolic pass used exponent (m+2)/2 where the return leg is (m+1)/2 —
caught in one turn by the radial-power assert, which existed because
the factorization structure was written down before the code (the
frozen-invariant lesson of 2916/2919, applied to my own work);
(2) the first committed draft compared sympy rationals to Python floats
with tuple equality and false-failed on values that were exactly right
— the fix was to make both sides symbolic, which also forced the
cleaner derivation route; (3) the same draft's rank-correlation test
false-failed on three statistically tied parameters (~0.28) — the fix
(enforce ordering only where parameters separate by >10%) is also the
honest statement of the claim. The draft never entered a commit; it
was deleted and rewritten. I note the pattern: all three defects were
in the *checking machinery*, not the physics, and all three were caught
by the checking machinery's own discipline (asserts, exactness, and
the refusal to accept a FAIL I could not explain).

One tool-cap event: the autodiff route at symbolic m blew the 300 s
compute cap twice. The fix was not a faster computer but a better
derivation — the Hessian contraction shows the m-dependence lives only
in prefactors, so five m-free integrals determine everything, the
symbolic-m result comes out exact in seconds, and the three-route
agreement (autodiff at four integer m, held-out reconstruction,
contraction) is stronger evidence than any one route. The committed
script runs the contraction route and validates order ε² independently
by a second-difference FD check against the discrete R₈ offset.

What I take from the physics: 113 in χ₃ is not pretty, and I checked it
four ways precisely because it is not pretty — the failure mode is
accepting pretty numbers, and its mirror is distrusting ugly ones. The
structural content is §5 of the record: the cancellation is a
structure-tuned condition, so direction (A)'s burden is now a
fixed-point statement against exact targets, including the all-orders
(c₄) requirement one dial cannot meet. The 2900 registered prediction
(c₄ → 0 under full self-consistency) is now confrontable in closed
form — that is the natural continuation, but it is a modeling step
(the self-consistent completion), not a bookkeeping one, and I am
leaving the sequencing decision between it and the standing secondary
queue to the session-close handover rather than pre-empting the next
worker.

What I refuse to conclude: anything about the substrate's actual ε.
It was a dial in 2900 and it is a dial here; the derivation makes the
dial's meaning precise and thereby makes clearer than before that the
substrate's own response profile is the whole question.

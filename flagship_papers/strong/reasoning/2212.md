# 2212 — OPEN-SS-41a: n* pins (~2–4), but f does NOT — ΔG*_nuc is not pinnable without the DM element cross-section; the dwarf branch stays reachable-not-pinned

**STATUS:** verbatim, at-patch (Opus, **strong/geometry lane**, 2200-series, 30 June 2026). Executes the 2211 handover /
**OPEN-SS-41a** (`frontier_sectors/SS.md`). **Strong-flagship home + one frontier registry edit.** Verify:
`code/2212_fnstar_pin_dGnuc.py`. **Layer: C.** Follows 2209 (the ΔG*_nuc = (n*−1)·|ln f| form).

---

## The ask, and the honest headline

The 2211 handover asked me to pin (f, n*) and thereby ΔG*_nuc — "pin-and-confirm, not a fresh search," with the panel
held until a pinned dwarf prediction lands. Executed honestly, the pin does **not** fully close, and I will not report
a confirmation the geometry doesn't support:

**n* ~ 2–4 IS pinnable, but f is NOT — and the plausible f-range brackets the window *and* both kill bands, so
ΔG*_nuc is not pinned. The dwarf branch stays REACHABLE / plausibly non-tuned (the 2209 bracket, confirmed), not
upgraded to a pinned prediction. The genuine last-mile is the DM element cross-section geometry (the exposed-core
spine-face fraction f) — one derivation beyond the currently-pinned corpus.**

## n* — pinnable to "a few" (verify §1)

Two independent handles agree. **0863** (the concrete formation kinetics): the 4-wide cross forms from a ribbon
"within a few moiety additions," with hTetra chaperoning barrierless and hTetras ~50% of the Sea — a *few*-unit
critical assembly. **Cooperative stability**: the soft axial hinge (g ≈ 0.02) makes a short rod seed floppy and
dissociable; it becomes self-stable once a few axial E_qq bonds lock the registration. Both give **n* ~ 2–4 (central
~3)** — reasonably pinned.

## f — what the window needs, and why it is not derivable here (verify §2–§3)

With n* pinned, the window closes onto f: ΔG*_nuc = (n*−1)·|ln f| ∈ [6.0, 6.9] kT ⇒ **f ~ 0.04 (n*=3) to 0.11
(n*=4)** — the exposed E_qq spine face must be ~4–11% of the incoming-orientation sphere. Is f ~ 0.04–0.11 derivable?
**No.** f = (exposed-core spine-face solid angle)/4π is a well-defined geometric quantity, but it requires the DM
element cross-section *shape*: the 8qCP core polyhedron, how the 8eCP coat covers the corners, and which face presents
the axial E_qq spine. The corpus gives this **qualitatively only** (color core buffered by an eCP coat at the corners;
4-wide; ℓ_el ~ 2 fm; r₀ ~ 0.26 fm) — it does not derive the exposed-core fraction.

The damage that does: the plausible range for a corner-coated, spine-exposed core, **f ~ 0.05–0.20**, gives ΔG*_nuc
spanning **~3.2 kT (n*=3, f=0.20 — under-cored) to ~9.0 kT (n*=4, f=0.05 — over-cored)**. That range **brackets the
window but also reaches both kill bands.** Window-vs-kill is genuinely *not decided* by anything currently pinned; it
turns on the actual exposed-core fraction to a precision the corpus doesn't yet provide. This is the barrier being a
sensitive difference of terms, exactly as 2209 flagged — now made quantitative.

## Verdict — partial close; I decline the "confirm" the handover expected

- **n* ~ 2–4: pinned.** **f: not pinned** — needs the DM element cross-section (exposed-core spine-face fraction).
- **ΔG*_nuc is NOT pinned.** The window requires f ~ 0.04–0.11 (a plausible but unpinned exposed-core fraction), and
  the plausible f-range also reaches under-cored (~3 kT) and over-cored (~9 kT). So the dwarf branch is **reachable /
  plausibly non-tuned** — the 2209 bracket **confirmed**, but **not** upgraded to the pinned prediction the founder
  sequenced the panel around.
- **The genuine last-mile is now singular and concrete: derive the DM element cross-section (the exposed-core
  spine-face fraction f) from the 8qCP+8eCP structure on the tetrahedral-cage / 600-cell substrate.** That is one
  derivation beyond the currently-pinned corpus (SS-2-adjacent). Once f lands, ΔG*_nuc is a single number and the
  dwarf σ/m(v) is a pinned prediction or a clean kill.
- **Cluster σ/m ∝ 1/v² branch untouched** (parameter-free). The candidate's submittable result stands on it regardless.

## Anti-priorities held

The handover framed this as "pin-and-confirm, not a fresh search," and the natural pull was to report f ~ 0.11 (the
in-window reading 2209 highlighted) as *the* value and declare the prediction pinned. I did not: the exposed-core
fraction is not derived in the corpus, the plausible range reaches both kills, so the honest status is
reachable-not-pinned. Pinned n* is reported as pinned; unpinned f is reported as unpinned; the in-window value is
shown as one option among a range that includes the kills. The bracket is not dressed up as a prediction.

---

## §10 Founder-contribution / engagement block (triggered)

**Trigger:** executes a founder-sequenced item (pin ΔG*_nuc *before* the panel) and returns "not pinnable yet," which
bears on the founder's panel timing.

**Engagement:** the founder set the sequence "pin the geometry number before panel round 2" and expected a
confirm-in-window. The honest finding is that the number does not pin without the DM element cross-section, which the
corpus has not derived — so waiting for a *pinned* dwarf prediction before the panel would hold it on a calc that is
one derivation away, not a confirmation away. That is a scheduling fact the founder should have straight, not a
smoothed "confirmed ~6.5 kT." The route-B gradient and the 2209 reachability are intact; what is not yet in hand is
the pin.

**Founder decision owed (preserved):** (i) whether to open the DM element cross-section calc (exposed-core spine-face
fraction f from the 8qCP+8eCP structure) as the true last-mile — likely an SS-2 / element-structure item; and (ii)
whether to run panel round 2 now on the honest status (cluster = parameter-free prediction; dwarf = reachable /
plausibly non-tuned bracket, one geometry number from pinned) rather than hold it for a pin that needs that calc.
Recommended: run the panel on the honest bracket and open the element-geometry calc in parallel — the cluster branch
is the submittable result and does not wait on f.

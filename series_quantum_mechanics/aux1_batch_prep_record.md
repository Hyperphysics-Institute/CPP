# AUX-1 TOUCHPOINT BATCH — PREPARED (ALL THREE NAMED ITEMS EXECUTED, AWAITING THE NEXT PANEL TOUCHPOINT)

**Patch 3038 (9 Aug 2026). CONV-013 completion left AUX-1
(OPEN-QMRG-ETA closure candidate) NOT CLOSED — CLOSE 2 [GROK][COP],
AMEND 1 [GPT], INSUFFICIENT 2 [Gemini][DS] — with the batch stocked
and three named items. All three are executed at this patch; the batch
now presents a complete package at the next legitimate touchpoint
(expected: the MEAS-3 disposition round, per WORKFLOW-REVIEW-ECONOMY —
no dedicated round is convened). AUX-2 (η-universality note-class
discharge) rides with AUX-1 per the conditional-verdict ruling.**

## The three items, as prepared

**1. Mode-coverage extension [DS] — the INSUFFICIENT basis answered
with a proof, then a sweep.** [DS] required "a proof that the A3′×I-3
cancellation holds for all modes." Supplied: the ALL-MODES LEMMA
(`code/3038_eta_mode_coverage_extension.py`, header) — the
cancellation is an ALGEBRAIC IDENTITY in ω (ω enters the per-Moment
content only through E = Nħω and the per-transit quantum only through
ħω; the quotient is ω-free for every frequency, profile, and
occupation; mode-dependence can enter only by breaking one of the two
ratified clauses — exactly the negative controls). The sweep
illustrating it: 144 1D configurations (3 dispersions including the
sharpest flat-branch case × 4 profile families × 12 wavenumbers) + 8
2D product modes + a 4-decade N-scan, with the EXACT expectation
identity (Σλ = N) checked per configuration, and both
ingredient-breaking controls re-verified per dispersion. 7/7 PASS.
The 3009 4-pass check stands as the original evidence; the lemma is
the proof [DS] asked for; the sweep is its illustration, not its
substitute.

**2. Bookkeeping-vs-microscopic grade labeling [GPT].** The 3009 and
3010 records each carry a v1.1 GRADE LABEL block: every result is
LATTICE-BOOKKEEPING grade (identities/computations of ratified
accounting clauses), expressly NOT a microscopic lattice derivation of
η from CP dynamics, with citation at the microscopic grade forbidden.
Consistent verbatim with the AUX-3 discharge record's grade fence
(Patch 3037).

**3. Reproducibility note [COP].** `eta_reproducibility_note.md`:
environment (Python 3.12.3 / NumPy 2.4.4 / Linux x86_64), the three
scripts with their module-top master seeds (31009 / 3010's / 30380809),
invocation, the determinism statement (single-generator PCG64
streams), the unprinted-sentinel policy, and the explicit statement of
what a successful rerun does and does not establish.

## What the batch will ask the panel

At the touchpoint: AUX-1 CLOSE / AMEND / INSUFFICIENT on the
3009+3010+3038 package (the [DS] and [GPT] bases now answered on
their own terms; [Gemini]'s HOLD carried no cited basis to answer);
AUX-2 DISCHARGE / HOLD, riding on AUX-1's outcome as ruled. Closure
would retire the η-universality note class, leaving uniqueness and
universal-microscopic as the final two.

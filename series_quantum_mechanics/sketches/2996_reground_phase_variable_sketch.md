# OPEN-QM-1-REGROUND — DERIVATION SKETCH: THE PHASE VARIABLE (Obligation (i))

**Patch 2996 (4 Aug 2026).** Opens the OPEN-QM-1-REGROUND closure
trajectory. Consumes: the founder's phase-variable input (registered
verbatim, `founder_registration_2996_phase_variable.md`), the SF6-A1
template asset (Patch 2995 sweep §6), the A1′/AP-2/AP-3 ratified
ontology, and the 2988 audit's V-2 registered candidate. Status:
**SKETCH** — this document identifies the phase variable and states
the derivation chain with its residual obligations; it is not the
paper revision and it mints nothing.

---

## §1 — The identification

**FI-QMRG-1 (foundational input, founder-anchored, Patch 2996):**

> The quantum phase φ_i at Grid Point i is the ORIENTATION ANGLE of
> the GP's SSV_net directional content in the distinguished
> (ZBW/transverse) plane. DP-orientations are the microscopic
> contributors; SSV_net = Σ (vector sum of all SSV contributions at
> the point) is the GP-held aggregate register (AP-3); φ is the
> angular coordinate of that register.

The site field of the QM series then decomposes onto the two
dynamical registers the GP ALREADY holds per the ratified A3′ state
protocol:

    ψ_i = √ρ_i · e^{iφ_i}
    ρ_i ↔ the count-like scalar register (SSV_abs; DI-bit number
          density — P-2, already confirmed compliant at 2988 S-2
          and series-wide at the 2995 sweep)
    φ_i ↔ the orientation of the vector register (SSV_net, T₁, l=1)
          in the distinguished plane

**Nothing new is postulated at the site.** The complex site field is
a repackaging of (scalar, planar-vector) GP state content that A3′
already broadcasts. This is the sketch's central structural claim.

## §2 — The two arguments that anchor the identification

**A-1 (U(1) compactness for free).** A quantum phase must live on a
circle: φ ≅ φ + 2π is not decoration, it is the U(1) target space
that interference, winding numbers, and gauge structure require. Of
the GP's held registers, ONLY the direction of a vector in a 2-plane
natively supplies a compact periodic coordinate. A scalar register
(SSV_abs, or any count) is non-periodic; promoting a scalar to a
phase would require postulating the compactification by hand. The
SSV_net-orientation reading gets U(1) from the geometry of a planar
vector direction — for free. This argument was available to neither
the founder's nor Copilot's framing and is the sketch's decisive
structural point.

**A-2 (the A3′ register mapping).** The LSP′ broadcast (A3′,
registered content) carries scalar Φ = |SSV|_abs (l=0) and vector
V_i = SSV_net (l=1). The Madelung decomposition of the QM series'
site field is (√ρ, φ) — a scalar and an angle. The mapping is
one-to-one onto the l=0 and (planar-projected) l=1 registers. The
re-grounded ontology does not ADD state to the substrate; it READS
the state the ratified protocol already holds. (The l=2 tensor
register Q_ij is not consumed here; it remains the radiative/GW
sector's.)

## §3 — The retired equation's value survives as pattern rotation

The 2988 audit retired Eq. (phasehop), Δφ = m_CP c Δs/ℏ, AS A
PER-BIT STATEMENT (doubly contradicted: P-1 content + A1′
reset-per-hop). Its NUMERICAL content survives re-grounding exactly:

- A trajectory element Δs traversed at c gives
  Δφ = m c Δs/ℏ = m c² Δt/ℏ = ω_C Δt — the Compton angular
  frequency.
- ω_C = mc²/ℏ is, in the registered corpus, precisely the ZBW
  oscillation frequency (glossary: "oscillation of a CP between
  partner DPs at the Compton frequency ω = mc²/ℏ").

**Re-label (pattern level):** the phase of the local Sea-polarization
configuration advances at the ZBW rotation rate — φ is the
instantaneous ZBW rotation angle of the aggregate pattern, registered
in SSV_net's direction. The bearer was wrong; the value was right.
Three immediate consilience checks:

1. **QM-2 Eq. (phaseedge)** re-labels identically (the 2995 sweep's
   Q2-S2 obligation is discharged by this reading): the per-edge
   phase advance is the pattern's ZBW rotation accrued over the edge
   traversal plus the SSV-potential term — a per-path functional of
   the propagating pattern, as the sweep found the mathematics
   already treats it.
2. **QM-3's spin encoding** ("phase encodes the spin direction via
   the ZBW helix," companion C4) becomes automatic rather than
   awkward: the helix angle IS φ under this identification.
3. **SF6-A1** (the template): SF-6's shipped photon ontology — phase
   in the DP-alignment pattern imprinted in the GP network, advanced
   per Absolute Moment — is this same reading in the massless/
   traveling case. The matter-sector re-ground is the standing-
   pattern port of an ontology already at v1.0.

**Global-phase invariance check:** rotating every SSV_net vector by
the same angle in the distinguished plane changes no relative
orientation and no count — no observable moves. Only phase
DIFFERENCES are physical. The U(1) global symmetry of quantum
mechanics is the statement that the substrate has no preferred zero
of pattern orientation in the plane. (Consistent with the founder's
"two levels of scale producing the same effect": the invariance holds
at both the DP-contributor and GP-register levels simultaneously.)

## §4 — Obligation (ii) sketch: unitarity from reversible conserved PCD

Registered candidate (2988 V-2 hook, QM-1's own lines 106–110): the
substrate has no diffusive or stochastic process; PCD displacement is
deterministic, reversible, and DI-bit-number conserving (Nexus).

Sketch of the argument to be made at proof grade in the revision:

- **Norm conservation:** Σ_i ρ_i is the total count — conserved per
  Moment by the Nexus (this is the P-2 register; nothing about phase
  enters). Hence Σ|ψ_i|² is conserved.
- **Reversibility:** the per-Moment update is invertible (A1–A9
  deterministic rules; no information loss at the GP refresh — the
  Perceive-stage data determine the new state, and the prior state is
  recoverable from the dynamics run backward).
- **Linearity + the −i:** an invertible, norm-preserving, linear
  neighbor-coupled update on a complex site field is unitary; its
  continuous-time generator is i×(Hermitian), which is exactly the
  tight-binding form the QM-1 theorem consumes, with T fixed by
  kinetic matching as the shipped Remark already states. Under
  FI-QMRG-1, "multiplication by −i" has a concrete substrate meaning:
  a quarter-turn rotation of SSV_net in the distinguished plane —
  the generator of pattern rotation.

**RESIDUAL OBLIGATIONS (honest register — what this sketch does NOT
close):**

- **R-1 (the distinguished plane).** The identification consumes a
  distinguished 2-plane at each GP (the ZBW/transverse plane) in
  which the orientation angle is read. For matter patterns the ZBW
  plane of the aggregate is the natural candidate (and QM-3/C4
  consilience supports it); for the SF-6 photon it is the transverse
  plane. DERIVING the plane selection from A1′–A9 (rather than
  reading it off the pattern class) is open. Until closed, FI-QMRG-1
  carries the plane as part of the foundational input.
- **R-2 (linearity).** The sketch asserts, and the revision must
  show, that the per-Moment GP refresh acts LINEARLY on the (√ρ, φ)
  content in the small-signal regime that the Schrödinger limit
  occupies — i.e., that vector summation of contributions (which IS
  the SSV_net definition) passes to superposition of site fields.
  Vector summation is linear by construction, which is why this is
  expected to close cleanly, but the ρ-weighting must be tracked.
- **R-3 (the potential term).** V_SSV entering the phase rate
  (QM-2's Eq. phaseedge second term) must be derived as the
  SSV-environment modulation of the local ZBW rate (the PSR/tick
  machinery of SR-1/SF-6 is the expected source). Expected
  straightforward; not done here.

## §5 — What this changes in the revision pass (F-SW-6 ordering, updated)

The Patch 2995 ordering stands, now with content for step (1):

1. **QM-1 revision:** grounding section rewritten on FI-QMRG-1 +
   §§2–4 above (with R-1..R-3 carried as stated conditions where not
   yet closed); Eq. (phasehop) replaced by the pattern-rotation
   statement (§3); anti-erasure notes throughout.
2. QM-6 primitives rewrite (A1′ three types; phase at pattern level
   per FI-QMRG-1).
3. QM-2 Eq. (phaseedge) re-label per §3 item 1.
4. QM-5 / QM-4 / QM-3 naming sweeps.
5. Glossary Born Rule entry (count-density + pattern-phase
   interference structure).
6. SF-6: no change.

**Grade discipline:** everything here is SKETCH. The QM-1 revision
carries the derivation at whatever grade R-1..R-3 reach; the lineage
un-conditions only when the revision ships, and the RELAY-MECH-1
citation bar stands until then (2957 §3).

## §6 — Ledger

Nothing moves: six of seven; PR7 PARTIAL (1B OPEN = CONV-013); B7
holds; Candidate (B) 79.5% PROVISIONAL-FAVORABLE; 2855 PROVISIONAL;
d_DP ceiling ACTIVE; nothing minted. New: FI-QMRG-1 registered
(founder-anchored, Patch 2996); OPEN-QM-1-REGROUND obligation (i)
ADVANCED to sketch-with-identified-variable; obligation (ii) sketched;
residuals R-1/R-2/R-3 registered. Dispatch-day lane untriggered
(`data/kmem2` absent at this patch).

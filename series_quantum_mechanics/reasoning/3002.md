# Tier-4 reasoning fragment — Patch 3002 (OPEN-QMRG-B1: the amplitude-count bridge derived)

**Captured at-patch, producing window.**

The derivation's decisive moment came from taking the messenger's NAME
seriously as an objection. "Displacement Increment" naturally reads as:
each bit deposits a fixed displacement, coherent deposits add, |S|
grows linearly in N — which would make |S|^2 ~ N^2 and kill the
bridge. I spent real effort on this fork before writing anything,
because a derivation that never confronts the linear reading would be
correctly accused of assuming its conclusion. The resolution came from
the ratified record, not from modeling: AP-2's P-2 clause says the
count is "intensity-like, NOT amplitude-like" — the founder's own
ruling, ratified at 2989, already places rho in the energy register.
The linear reading makes the count amplitude-like and so contradicts a
ratified clause. The name describes the microscopic delivery event;
the SUSTAINED register's scaling passes through the medium, and the
medium's stiffness is where the square root lives: kappa S^2/2 = E
means S ~ sqrt(E), and E counts quanta. Once seen, the whole bridge is
one sentence of energy balance.

Where the harmonic structure comes from mattered as much as the
scaling, because the CONV-014 circularity concern was precisely that
the bridge smuggles the oscillator. The answer: the quadratic energy
is SF-6's dipole stiffness — the same kappa that locks mu_0 and
epsilon_0, shipped at Tier 1/2 in the EM flagship, two arcs before
this one and with no dependence on QM-5. So the loop the panel feared
(QM-5 presupposes the relation -> bridge -> QM-1 -> QM-5) is cut at
its first link: the oscillator was already in the corpus, in another
sector, for independent reasons. Better: running the balance with the
per-messenger quantum eps = hbar*omega OUTPUTS the per-quantum
displacement-squared hbar/(2 mu omega) — the 1/(2 omega_k) field
normalization QM-5 writes down. The presupposition became a
prediction-match, which is the cleanest possible discharge of GPT's
mutual-support prohibition: after adjudication, neither paper needs
the other for the relation, because a third source supplies it.

The verify design followed the fork: the toy oscillator driven by
kicks can realize BOTH readings, so the script's job was to show which
microphysics yields which exponent. Cycle-distributed kicks (the P-3
reading of synchrony: messengers sample the ZBW cycle, they are not
phase-locked to a quadrature) give slope 1.01 — the bridge.
Phase-locking every kick to the velocity quadrature gives slope 1.99 —
the rival — and that is the negative control's real content: the
amplitude-like reading is not impossible physics, it is physics that
requires an arrival-phase discipline the substrate does not have and
the ratified ontology forbids. The omega-scan at fixed quantum
recovered slope -0.97 against the predicted -1: the canonical
normalization emerging from a classical damped oscillator fed
quantized kicks. And the elasticity-free random-walk check (slope
1.00) closes the robustness loop: the exponent arrives by two
independent mechanisms — energetic and statistical — which is the
signature of a relation that is structural rather than assumed.

Honest limits I registered without prompting: the toy is one elastic
element with Poisson kicks and linear damping standing in for the
per-Moment turnover; the O(1) turnover factor is not computed; the
lattice-level neighbor-coupled derivation is the hardening path. The
grade is derivation candidate, panel-pending, and the E-1 bar scope
is untouched — same discipline as 3001.

With this patch both CONV-014 blocking items are candidates, each
with an executed verify script and a negative control. The arc is
ready for CONV-015 — its second review round and its first with
SCRIPT-EXECUTED credit on the table. The dispatch should also put
OPEN-QMRG-UNIQ to the panel as a scoping question (blocking or
non-blocking for full lift?), since it is the one CONV-014
registration neither 3001 nor 3002 touches.

# CONV-016 DISPATCH — T1 (MULTILINK) + T2 (B1-CONST) CANDIDATE PACKAGES: THE POTENTIALLY TERMINAL ROUND OF THE QM RE-GROUNDING ARC

**Patch 3007 (4 Aug 2026).** Third review round of the arc,
adjudicating the two CONV-015 conditionality-trigger items (E-6:
T1 + T2), with the trigger-to-resolution mapping PRE-REGISTERED in
the frozen tree below. Five seats: GPT, Grok, Gemini, Copilot,
DeepSeek. **Copilot conditions:** FRESH conversation (standing), and
— frozen NOW, prospectively, per the CONV-015 adjudication flag —
**a third adverse event of any fabrication class in this arc closes
the seat for the remainder of the arc** (current ledger: two events,
two rounds, two classes). **Full ledger disclosed:** Copilot ×2
(claimed-check-contradicted-at-HEAD, CONV-014; fabricated-citation,
CONV-015); DeepSeek ×4 prior fabrications + two creditable honest
declarations; GPT ×1 prior + exemplary status honesty since; Gemini
self-mislabel ×4. Judge on merits; tally-from-verbatim-only.

## §0 — EXECUTION RULES

Two committed, deterministic, seeded verify scripts (< 3 min CPU
each):
- `series_quantum_mechanics/code/3005_multilink_pair_kernel_check.py` (seed 31005)
- `series_quantum_mechanics/code/3006_b1const_coefficient_check.py` (seed 31006)

**IMPORTANT: both scripts' printed stdout is embedded verbatim in the
committed records, so REPRODUCING STDOUT PROVES NOTHING this round.**
The execution burden rests entirely on the keys, which comply with
the Patch-3004 KEY-DESIGN RULE (non-reconstructible from any
committed output):

- **KEY-I** = the value of the variable `sentinel_value` in the 3005
  script: the out-of-plane fraction computed at the UNPRINTED
  sentinel configuration (fresh random plane, random β, ks = 0.017)
  generated from the post-checks RNG state. Printed nowhere; on no
  printed curve; depends on the RNG stream position after all
  printed checks.
- **KEY-J** = the value of `sentinel_value` in the 3006 script: the
  virial coefficient at the unprinted post-checks random
  configuration (random γ, ω, kick rate). Same construction.

The worker has NOT evaluated either key; the values exist nowhere but
inside an execution. **SCRIPT-EXECUTED** = both keys to 3
significant figures (stdout optional — it proves nothing).
**REASONED-VERIFIED** = named derivation steps independently checked
(state which). **ACCEPTED** = neither; honorable if declared. A
claimed execution with wrong keys is self-indicting; the panel is
also invited (W-7) to attack the key DEFINITIONS for any residual
reconstructibility.

## §1 — What is under review

- **T1 (Patch 3005):**
  `sketches/3005_multilink_refresh_law_analysis.md`. Claim:
  OPEN-QMRG-R4-MULTILINK resolves at registry-theorem grade — the
  committed refresh law FORCES the single-edge kernel class by three
  ratified clauses (A3′ additive vector-sum register formation; AP-2
  minimal messenger content; A3′ per-Moment reset); the committed
  chirality mechanism (F.1 Mechanism A, per-edge scalar rate
  asymmetry) is component-diagonal hence plane-exact; quantitative
  moat: symmetric cross-sums vanish identically, separable pair
  kernels are exact or slope-4, and the EXCLUDED non-separable
  chiral class leaks at ORDER UNITY (0.70 at all wavelengths) —
  the exclusion is load-bearing, not decorative. Watch condition
  W-MULTILINK-1 registered.
- **T2 (Patch 3006):**
  `sketches/3006_b1const_convention_derivation.md`. Claim:
  OPEN-QMRG-B1-CONST disposes by partition — LAYER 1 DERIVED: in the
  declared convention (cycle-averaged mean-square in-plane register),
  the virial coefficient is EXACTLY 1 (⟨S_⊥²⟩ = Nħ/(μω) given
  E = Nħω; measured 0.997–0.999), and the canonical per-quadrature ½
  is the two-quadrature split of the rotating ZBW pattern (measured
  0.500001); LAYER 2 QUARANTINED: η ≡ ρ/N (count-to-occupation
  participation ratio), the one lattice-dependent unknown, a
  registered parameter with a checkable mode-independence claim;
  separation VERIFIED (coefficient invariant across a ×17 turnover
  scan).
- Context: `conv015_adjudication.md` (E-6 trigger: T1+T2); the E-1
  widened bar scope; QM-1 at v2.6.

## §2 — Disclosed weak points (attack first)

- **W-1 (exclusion-as-closure-shape).** T1 answers a physics
  obligation by quoting ratified clauses. Is that a closure, or a
  relocation of the question onto the clauses' own standing? (Note:
  the clauses' falsifier is already registered as F-AP3-1; the panel
  should judge whether that suffices.)
- **W-2 (chirality-audit completeness).** The T1 audit checked the
  COMMITTED chirality entry point (F.1 Mechanism A) only. Are there
  other committed dynamical entry points the audit missed?
- **W-3 (the convention declaration is a choice).** T2's Layer-1
  derivation is exact IN the declared convention. Is cycle-averaged
  mean-square the right operational reading of the register
  statistic, or does the declaration quietly pick the convention
  that makes the answer clean?
- **W-4 (E = Nħω at the coefficient level).** The exact coefficient
  leans on quantized mode energy harder than the proportionality
  did. Re-test for circularity: is E = Nħω available at this point
  in the derivation order without presupposing the quantization the
  QM papers derive?
- **W-5 (the quarantine boundary).** η's mode-independence in the
  linear regime is a registered CLAIM, not a result. Could mode
  dependence hide inside η and leak back into the "derived" layer?
- **W-6 (compounding velocity).** Six physics patches and two
  adjudications in one day now feed a terminal-round request.
  Scrutinize whether terminal-round readiness is real or momentum:
  is anything in the T1/T2 packages shaped by the desire to finish?
- **W-7 (the keys themselves).** First use of the new KEY-DESIGN
  RULE. Attack the key definitions: is there ANY route to KEY-I or
  KEY-J from committed data without execution?

## §3 — THE QUESTIONS (frozen; named verdicts)

**Q1 — The T1/MULTILINK package.** Does the refresh-law exclusion
resolve MULTILINK at registry-theorem grade? W-1/W-2 in scope.
Verdicts: **RESOLVES / RESOLVES-WITH-RESIDUE (name it; blocking or
not) / FAILS (name the broken step)**.

**Q2 — The T2/B1-CONST disposition.** Does the derive-and-quarantine
partition dispose B1-CONST per the trigger's own disjunction?
W-3/W-4/W-5 in scope; check the virial algebra and the quadrature
split if you can. Verdicts: **DISPOSES / DISPOSES-WITH-RESIDUE (name
it; blocking or not) / FAILS**.

**Q3 — THE CONDITIONALITY RULING (the terminal question).** CONV-015
E-6 set the trigger: T1 resolved + T2 disposed. Given your Q1/Q2:
should the QM sector's CONDITIONAL status now RESOLVE? Note what
resolution MEANS (§4). Verdicts: **RESOLVE-CONDITIONALITY /
RETAIN-CONDITIONAL (state the new trigger precisely)**.

**Q4 — THE BAR, final form.** Verdicts: **FULL-LIFT / WIDEN-PARTIAL
(state exactly what becomes admissible) / HOLD-CURRENT-SCOPE**.

**Q5 — Arc-closure integrity.** Does the whole re-grounding arc
(2988→3006), as shipped at HEAD, claim exactly what its
adjudications support — no residual overclaim in any paper, record,
registry line, or glossary entry? Verdicts: **PASS / FAIL (cite the
line)**.

**Decision tree (frozen outcome-blind; the E-6 mapping
pre-registered):**
- **Conditionality RESOLVES iff** [Q1 majority RESOLVES or
  RESOLVES-WITH-nonblocking-RESIDUE] AND [Q2 majority DISPOSES or
  DISPOSES-WITH-nonblocking-RESIDUE] AND [Q3 majority
  RESOLVE-CONDITIONALITY] AND [Q5 majority PASS].
- **The bar FULLY LIFTS iff** conditionality resolves AND Q4
  majority FULL-LIFT. Otherwise, if Q1 and Q2 majorities are
  non-FAILS, the scope widens/holds per the Q4 majority with any
  widening defined by the conservative intersection of the widening
  returns.
- **Any sustained FAILS** returns that item to derivation with the
  objection as charter and freezes conditionality, bar, and scope.
- A Q5 FAIL with a sustained citation triggers a remediation patch
  before any status movement, then re-poll of Q3/Q4 only, by
  written follow-up to all five seats.

## §4 — What resolution MEANS (stated before outcomes are known)

Resolving conditionality does NOT mean nothing remains open. It
means the sector exits the special CONDITIONAL status imposed at
2988 and re-enters ordinary registered-research bookkeeping. The
following persist as ordinary open items regardless of outcome:
η's lattice derivation (with its mode-independence claim as the
checkable content); OPEN-QMRG-UNIQ (non-blocking, with the E-5
three-part criterion); W-MULTILINK-1 (the watch condition); the
strict-proof exclusions of E-1 that the panel does not explicitly
lift. Conditional NOTES on citations are removed only for claims
whose obligations closed; the mandatory-note mechanism itself
remains available for the persisting items.

## §5 — Repository pointers

Blob: https://github.com/Hyperphysics-Institute/CPP/blob/main/ · Raw: https://raw.githubusercontent.com/Hyperphysics-Institute/CPP/main/

- `series_quantum_mechanics/sketches/3005_multilink_refresh_law_analysis.md`
- `series_quantum_mechanics/code/3005_multilink_pair_kernel_check.py`
- `series_quantum_mechanics/sketches/3006_b1const_convention_derivation.md`
- `series_quantum_mechanics/code/3006_b1const_coefficient_check.py`
- `series_quantum_mechanics/conv015_adjudication.md` (the governing round; E-6 trigger)
- `series_quantum_mechanics/conv014_adjudication.md` (the arc's first round)
- `series_quantum_mechanics/papers/QM-1_schrodinger_emergence.tex` (v2.6)
- `master_glossary.md` (GP/DI-bit/Born-Rule entries — Q5 scope)

## §6 — Return format

Per question: named verdict + argument; cite lines when attacking.
Declare execution status per §0 (keys carry the whole burden;
stdout proves nothing this round). For Q5, an explicit "checked X,
Y, Z; accepted the rest" declaration is required for the PASS to
carry adjudication weight. Do not summarize; attack. W-6 licenses
treating the arc's speed as a standing reason for suspicion through
every question.

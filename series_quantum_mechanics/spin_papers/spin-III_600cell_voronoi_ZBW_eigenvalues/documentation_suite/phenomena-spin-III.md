# Phenomena — spin-III

**Explained (within the CPP ontology):**
- Why the ZBW resonator's Mode 2 — and with it spin-½ — survives on a
  discrete icosahedral lattice: selection forced by exact symmetry
  exclusion (no invariant channel below l = 6), protection at O(ε₆²).
- Why the measured lattice-cell spectrum shows the radial modes at global
  indices 1 and 10: the eight intervening states are precisely the
  l=1⊕l=2 multiplets, excluded from the invariant sector.
- The exact scale bridge between Spin I's orbital radius and Spin II's
  resonator: r_in(I)/r_in(II) = 6/[4α(1+√2)²].

**Predicted (checkable structure):**
- Mode-2 landmark shifts on the true cell of size ε₆² ≈ 2.6×10⁻³ (measured
  3.1×10⁻³ eigenvalue residual; node shift 5×10⁻⁴) — and the O(ε₆) term
  exactly absent.
- The invariant-sector spectrum: radial family + first anisotropy-carrying
  invariant mode at kR ≈ 8.21; forbidden-channel content of any
  I_h-symmetric boundary at quadrature zero for l = 1..5.

**Not explained here (honest boundary):**
- Why nature selects the scalar breathing sector (inherited from Spin II,
  panel-adjudicated as inherited).
- The boundary conditions themselves (CP Exclusion; thermal free end) —
  axiom-level and companion-level premises.
- Non-perturbative-in-ε₆ protection and dynamic symmetry-breaking
  robustness — the two registered residuals.

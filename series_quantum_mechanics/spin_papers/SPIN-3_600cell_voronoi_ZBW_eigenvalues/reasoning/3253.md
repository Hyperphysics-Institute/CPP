# Reasoning fragment — Patch 3253 (Session 150): spin trio re-identification

**Trigger:** founder naming confirmation this session ("naming confirmed"),
executing the Session-149 worker proposal: SPIN-1/2/3 as a small sector
series. Deposit blocker (a) of the Zenodo TEST RUN is thereby cleared;
blocker (b) (Isak's DOI reservations) remains in progress.

## FOUNDER CONTRIBUTION (verbatim, Session 150)
> (a) the SPIN-1/2/3 naming confirmation — "naming confirmed."
> (b) Isak's DOI reservations — "we are working on this."

## Decisions
1. **Rename now, before DOIs.** The Session-149 handover conditioned the
   pass on (a)+(b), but the re-identification does not depend on the DOI
   reservations — it is their PREREQUISITE ("interim filenames must not
   mint DOIs"). Executing on (a) alone unblocks Isak: DOIs are reserved
   against the permanent filenames.
2. **Uppercase SPIN-N** per the sector-coded-ID case convention (SM-3,
   EW-1, GR-1c precedents). Descriptors retained verbatim; no version
   suffixes in filenames (standing convention).
3. **In-arc names retained in prose** (Spin I/II/III), GR "formerly cNN"
   precedent: only title blocks, filenames, bibliographies, and live
   pointers carry the permanent IDs.
4. **Namespace note (registry grep):** THEO-SPIN-1 (theorem, Patch 0572)
   shares the token "SPIN-1" but lives in the theorem namespace; no paper
   ID collision. Founder's confirmation governs.
5. **Defect caught:** SPIN-3's title version line still read "Version 0
   (draft)" after the v1.0 ship at 3248 — the Phase-2 retrofit updated the
   header changelog but not the title line. Corrected to 1.0.1
   (re-identification bump, GR-1 V0→V0.1 precedent). SPIN-1 stays 2.1,
   SPIN-2 stays 2 (title ID prefix only; header comments record it).
6. **Suite files renamed** *-spin-III.md → *-SPIN-3.md (fresh files,
   created at ship yesterday; renaming now, pre-DOI, is the cheap moment).
7. Historical records (handovers, session logs, frontier history, prior
   reasoning fragments) deliberately NOT rewritten — they name the old
   filing as history.

## Verification
- Repo-wide grep for live pointers: spin_papers/README.md, QM.md (2),
  methods_catalogue.md (1) — all updated with (formerly ...) tags where
  the context is a record rather than a pure path.
- Compile gate: pdflatex ×2 on all three papers, zero errors each.
- No new computation; no verify script (RCP §1.3).

# CALIBRATION PROVENANCE NOTE — THE PHYSICAL ρ-CALIBRATION (ONE PAGE, THE [COP] OBLIGATION)

**Patch 3037. Companion to `aux3_rho_calibration_discharge_record.md`.
Every number below is a citation; nothing is computed here.**

**The quantity.** ρ = physical DI-bit count density at a Grid Point;
N = pattern occupation number in the convention-labeled amplitude
identities. The calibration is the partition ρ = η·N with η the
participation ratio — the one lattice-dependent factor the QM
re-grounding arc quarantined rather than guessed.

**The chain, patch by patch:**

1. **Patch 3006 (quarantine established).** Layer 1 derived the
   convention-labeled virial identity ⟨S_⊥²⟩ = Nħ/(μω) (coefficient
   measured 0.997–0.999; the canonical ½ derived as the two-quadrature
   ZBW split, 0.500001). Layer 2 quarantined η ≡ ρ/N as a registered
   substrate parameter with a CHECKABLE mode-independence claim;
   separation verified (stored energy ×17 across the turnover scan,
   coefficient invariant). Verify: `code/3006_b1const_coefficient_check.py`.
   Panel disposition: CONV-016 Q2 DISPOSES 5–0.

2. **Patch 3009 (mode-independence derived).** The cancellation:
   A3′ universal Moment-cadence turnover (content/Moment = Nħω)
   divided by the I-3 per-transit quantum (ħω) gives η = c_geo, a
   pure relay-geometry constant. Mode-independence is FORCED by the
   two ratified clauses, not assumed. Controls carry the evidence:
   ratified pair slope +0.00 across 6.5× in ω; break I-3 → +1.00;
   break A3′ universality → +1.02 — each ingredient individually
   load-bearing. Verify: `code/3009_eta_mode_independence_check.py`.

3. **Patch 3010 (the value, the class scope, the inertness).**
   c_geo = z = 12 for the Version B STANDING class (every neighbor
   sent every Moment; interior sites). Honest refinement recorded:
   c_geo is CLASS-dependent (12 standing / f directional-cone) while
   mode-independent WITHIN class (verified flat at f = 12 and f = 3,
   slopes +0.00). The earlier I-3 tension DISSOLVED: the 3009
   cancellation is multiplicity-blind ("ħω per messenger" was the
   m = 1 toy convention; the physics never consumed it). INERTNESS
   MEASURED: virial coefficient 0.997 vs 0.998 under c_geo = 1 vs 12;
   physical ρ-ratios convention-blind.
   Verify: `code/3010_cgeo_relay_multiplicity_check.py`.

4. **Panel review (CONV-013, AUX-3).** Dispatch framing: "fell as a
   consequence of c_geo; the calibration now exists at bookkeeping
   grade — you decide sufficiency." Interim tally (four seats)
   2–2 PROVISIONAL; completion tally DISCHARGE 3/5
   [GPT][GROK][COP] — ENACTED, with the two recording obligations
   this note and its companion execute.

**Grade and scope, restated once:** the calibration is
lattice-BOOKKEEPING on ratified clauses — not a microscopic
derivation of η from CP dynamics; the value 12 is standing-class
only; the discharge licenses ρ = 12·N substitution into
convention-labeled identities and nothing above that grade.
Uniqueness and universal-microscopic note classes persist;
η-universality (as a NOTE CLASS) rides with the still-open
AUX-1/AUX-2 batch.
